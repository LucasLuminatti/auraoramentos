---
phase: 09-multi-tenancy-rls
plan: 06
type: execute
wave: 5
depends_on: [09-05]
files_modified:
  - .planning/phases/09-multi-tenancy-rls/09-SMOKE-RESULTS.md
autonomous: true
requirements: [RLS-01, RLS-02]
must_haves:
  truths:
    - "Login como colab A → vê apenas Smoke A — Arq e Smoke A — Cli (não vê B)"
    - "Login como colab B → vê apenas Smoke B — Arq e Smoke B — Cli (não vê A)"
    - "Login como admin Lenny → vê AMBOS os arquitetos Smoke e AMBOS os clientes Smoke (e os reais)"
    - "Cada caso de teste tem status PASS ou FAIL documentado em 09-SMOKE-RESULTS.md"
    - "Phase 9 fecha smoke 100% PASS (todos os casos PASS); FAIL bloqueia phase"
  artifacts:
    - path: ".planning/phases/09-multi-tenancy-rls/09-SMOKE-RESULTS.md"
      provides: "Resultado bilateral por caso de teste"
      contains: "## Smoke Cases"
  key_links:
    - from: "Login A → UI listing"
      to: "Apenas Smoke A — *"
      via: "RLS policy SELECT com user_id = auth.uid()"
      pattern: "user_id = auth.uid"
    - from: "Login admin → UI listing"
      to: "Smoke A + B + reais"
      via: "RLS policy SELECT com has_role admin"
      pattern: "has_role"
---

<objective>
Validar smoke bilateral em produção via Playwright MCP: logar como colab A, colab B, e admin Lenny, verificando em cada caso que o usuário vê EXATAMENTE os cadastros esperados (e nada mais). Documentar PASS/FAIL por caso em `09-SMOKE-RESULTS.md`. Esse é o gate final que fecha success criterion #5 do roadmap.

Purpose: Estrutura RLS criada em 09-04 não vale nada se comportamento bilateral não for confirmado no app real. Playwright em prod é o teste-fim-de-jogo.

Output: `.planning/phases/09-multi-tenancy-rls/09-SMOKE-RESULTS.md` com 7 casos (3 colab-A + 3 colab-B + 1 admin) marcados PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-multi-tenancy-rls/09-CONTEXT.md
@.planning/phases/09-multi-tenancy-rls/09-SMOKE-SETUP.md
@.planning/phases/09-multi-tenancy-rls/09-PUSH-LOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright bilateral smoke — A, B, admin</name>
  <files>.planning/phases/09-multi-tenancy-rls/09-SMOKE-RESULTS.md</files>
  <read_first>
    - .planning/phases/09-multi-tenancy-rls/09-SMOKE-SETUP.md (emails + senhas + nomes dos cadastros)
    - .planning/phases/09-multi-tenancy-rls/09-CONTEXT.md (D-14 — 4 casos de teste mínimos)
    - src/components/ArquitetoAutocomplete.tsx + ClienteList.tsx + ClienteFilterAutocomplete.tsx (telas a checar — colab não tem tab "Cadastros" direto; checar via wizard / Index)
  </read_first>
  <action>
    Executar 7 casos via Playwright MCP em `https://orcamentosaura.com.br`. Para cada caso, capturar screenshot (opcional mas registrar pasta se sim) e listar o que está visível na tela.

    **CASO 1 — COLAB-A-VE-SO-SEUS (Arquitetos)**
    1. Logout se logado
    2. Login com `{EMAIL_A}` / `SmokeA2026!` (vide 09-SMOKE-SETUP.md)
    3. Navegar até wizard de novo orçamento ou para tela onde arquiteto autocomplete aparece (Step 2 ou ClienteDialog do wizard)
    4. Disparar autocomplete digitando "Smoke" no campo Arquiteto
    5. **Esperado:** vê só `Smoke A — Arq`. NÃO vê `Smoke B — Arq`.
    6. Resultado: PASS se só A aparecer, FAIL se B aparecer.

    **CASO 2 — COLAB-A-VE-SO-SEUS (Clientes)**
    1. Logado como A (continuar sessão)
    2. Navegar até onde tem lista/autocomplete de clientes (wizard Step 1 ou Step 2)
    3. Disparar autocomplete/filter por "Smoke"
    4. **Esperado:** vê só `Smoke A — Cli`. NÃO vê `Smoke B — Cli`.
    5. PASS/FAIL.

    **CASO 3 — COLAB-A-NAO-EDITA-B (UPDATE bloqueado)**
    1. Logado como A
    2. Via console DevTools (Playwright `evaluate`): rodar
       ```js
       const sb = window.supabase || (await import('/src/integrations/supabase/client.ts')).supabase;
       const r = await sb.from('arquitetos').update({contato: 'hacked'}).eq('nome', 'Smoke B — Arq').select();
       JSON.stringify(r);
       ```
       Alternativa se import dinâmico não funcionar: usar fetch direto à API REST do Supabase com bearer token do localStorage.
    3. **Esperado:** `r.data` é `[]` (zero linhas afetadas — RLS filtrou UPDATE). NÃO retorna erro 4xx, mas data vazia.
    4. PASS/FAIL.

    **CASOS 4-6 — Simétrico com COLAB-B:**
    4. COLAB-B-VE-SO-SEUS (Arquitetos): logout + login B + autocomplete arquiteto → só B
    5. COLAB-B-VE-SO-SEUS (Clientes): autocomplete cliente → só B
    6. COLAB-B-NAO-EDITA-A: tentar update via console → data vazia

    **CASO 7 — ADMIN-VE-TODOS**
    1. Logout
    2. Login como admin Lenny (`lenny.wajcberg@luminattiled.com.br` ou conta admin equivalente — Lenny fornece senha se necessário, ou usar sessão existente)
    3. Navegar para `/admin` → tab Cadastros → Arquitetos
    4. **Esperado:** lista contém AMBOS `Smoke A — Arq` E `Smoke B — Arq` (plus arquitetos reais pré-existentes)
    5. Tab Clientes: **Esperado:** lista contém AMBOS `Smoke A — Cli` E `Smoke B — Cli`
    6. PASS/FAIL.

    Documentar em `.planning/phases/09-multi-tenancy-rls/09-SMOKE-RESULTS.md`:

    ```markdown
    # 09-SMOKE-RESULTS — Multi-tenancy RLS bilateral (Plan 09-06)

    **Executed:** {ISO datetime}
    **Method:** Playwright MCP (mcp__plugin_playwright_playwright__*) em https://orcamentosaura.com.br
    **Phase:** 09-multi-tenancy-rls
    **Requirements:** RLS-01, RLS-02

    ## Smoke Cases

    ### COLAB-A-VE-SO-SEUS (Arquitetos)
    - **Status:** PASS / FAIL
    - **Logged in as:** {EMAIL_A}
    - **Action:** Autocomplete arquiteto buscando "Smoke" em {tela}
    - **Expected:** apenas "Smoke A — Arq"
    - **Observed:** {literal lista do que apareceu}
    - **Verdict:** {PASS|FAIL + razão}

    ### COLAB-A-VE-SO-SEUS (Clientes)
    [mesma estrutura]

    ### COLAB-A-NAO-EDITA-B
    - **Status:** PASS / FAIL
    - **Action:** UPDATE em arquitetos via supabase client com filter `nome = 'Smoke B — Arq'`
    - **Expected:** r.data === []
    - **Observed:** {literal JSON de r}
    - **Verdict:**

    ### COLAB-B-VE-SO-SEUS (Arquitetos)
    [mesma estrutura, simétrico]

    ### COLAB-B-VE-SO-SEUS (Clientes)
    [mesma estrutura]

    ### COLAB-B-NAO-EDITA-A
    [mesma estrutura]

    ### ADMIN-VE-TODOS
    - **Status:** PASS / FAIL
    - **Logged in as:** {ADMIN_EMAIL}
    - **Tela:** /admin → tab Cadastros → Arquitetos + Clientes
    - **Expected:** lista contém Smoke A — Arq, Smoke B — Arq, Smoke A — Cli, Smoke B — Cli + outros
    - **Observed:** {nomes literais que apareceram, ou pelo menos confirmação dos 4 Smoke}
    - **Verdict:**

    ## Summary

    | Case | Status |
    |------|--------|
    | COLAB-A-VE-SO-SEUS-ARQ | PASS/FAIL |
    | COLAB-A-VE-SO-SEUS-CLI | PASS/FAIL |
    | COLAB-A-NAO-EDITA-B | PASS/FAIL |
    | COLAB-B-VE-SO-SEUS-ARQ | PASS/FAIL |
    | COLAB-B-VE-SO-SEUS-CLI | PASS/FAIL |
    | COLAB-B-NAO-EDITA-A | PASS/FAIL |
    | ADMIN-VE-TODOS | PASS/FAIL |

    **Overall:** {N}/7 PASS

    ## Issues Found
    [Se algum FAIL: descrição literal do bug + screenshot path + hipótese de root cause + sugestão de fix (volta pra 09-03 ou hotfix novo). Se 7/7 PASS: "Nenhum issue. Phase 9 RLS-01/RLS-02 validados."]

    ## Console Errors
    [Lista qualquer erro JS no console durante os 7 fluxos — esperado: zero]
    ```

    Se algum caso FAIL: NÃO atualizar success criterion como PASS. Phase fica em block-state, Lenny decide hotfix.

    Console error check: durante cada login/navegação, ler console do navegador via Playwright `console_messages` MCP tool. Se houver `error` level message relacionado a RLS / 401 / 403 inesperado, registrar.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f='.planning\\phases\\09-multi-tenancy-rls\\09-SMOKE-RESULTS.md'; (Test-Path $f) -and (Select-String -Path $f -Pattern '7/7 PASS' -Quiet)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/09-multi-tenancy-rls/09-SMOKE-RESULTS.md` exists
    - File contains literal heading `## Smoke Cases`
    - File contains all 7 case headings: `### COLAB-A-VE-SO-SEUS (Arquitetos)`, `### COLAB-A-VE-SO-SEUS (Clientes)`, `### COLAB-A-NAO-EDITA-B`, `### COLAB-B-VE-SO-SEUS (Arquitetos)`, `### COLAB-B-VE-SO-SEUS (Clientes)`, `### COLAB-B-NAO-EDITA-A`, `### ADMIN-VE-TODOS`
    - File contains literal `**Overall:** 7/7 PASS` (não 6/7, não 5/7 — phase só fecha em 7/7)
    - File contains literal `## Issues Found` section, com texto "Nenhum issue. Phase 9 RLS-01/RLS-02 validados." se overall=7/7
    - File contains literal `## Console Errors` section
    - Each case has `**Observed:**` line populated with actual literal content (not `{...}` placeholder)
  </acceptance_criteria>
  <done>7/7 PASS em SMOKE-RESULTS.md. Phase 9 success criterion #5 fechado. Se FAIL, abrir issue/hotfix antes de marcar done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (Playwright) → prod app | JWT tokens carregados; comportamento real validado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Severity | Mitigation Plan |
|-----------|----------|-----------|-------------|----------|-----------------|
| T-09-F01 | I (Information disclosure) | Smoke A vê dados de B (RLS bypass) | mitigate | HIGH | Caso 1+2 explicitamente checa essa hipótese; FAIL bloqueia phase |
| T-09-F02 | E (Elevation) | Smoke A consegue UPDATE em B (WITH CHECK bypass) | mitigate | HIGH | Caso 3+6 checam via supabase client direto |
| T-09-F03 | I | Admin não vê dados (overcorreção do RLS) | mitigate | HIGH | Caso 7 garante admin vê tudo |
| T-09-F04 | D (DoS) | Playwright timeout vira FAIL falso | mitigate | LOW | Aumentar timeout do Playwright se necessário; retry uma vez antes de marcar FAIL |
| T-09-F05 | R (Repudiation) | Smoke marcado PASS sem observação concreta | mitigate | MEDIUM | acceptance_criteria exige Observed populado literal, não placeholder |
</threat_model>

<verification>
- 09-SMOKE-RESULTS.md com 7/7 PASS
- Cada caso tem Observed literal
- Nenhum console error de RLS
</verification>

<success_criteria>
Bilateral isolation confirmado em prod. RLS-01 e RLS-02 entregues e validados. Phase 9 pronta pra fechar (pendente cleanup em 09-07).
</success_criteria>

<output>
After completion, create `.planning/phases/09-multi-tenancy-rls/09-06-SUMMARY.md` resumindo: 7 casos rodados, score final (esperado 7/7), bugs encontrados (se houver), e tempo de execução total.
</output>
