---
phase: 09-multi-tenancy-rls
plan: 05
type: execute
wave: 4
depends_on: [09-04]
files_modified:
  - .planning/phases/09-multi-tenancy-rls/09-SMOKE-SETUP.md
autonomous: false
requirements: [RLS-01, RLS-02]
must_haves:
  truths:
    - "2 colabs de smoke (A e B) existem em prod com email distinto, signup completo, allowed_users entry, e colaborador auto-criado"
    - "Cada colab cadastrou 1 arquiteto e 1 cliente via UI (totalizando 2 arquitetos Smoke + 2 clientes Smoke em prod)"
    - "Nomes seguem padrão 'Smoke A — Arq', 'Smoke A — Cli', 'Smoke B — Arq', 'Smoke B — Cli' (filtráveis em cleanup)"
    - "09-SMOKE-SETUP.md documenta emails, user_ids (auth.users.id) e ids dos arquitetos/clientes criados"
  artifacts:
    - path: ".planning/phases/09-multi-tenancy-rls/09-SMOKE-SETUP.md"
      provides: "Inventário dos dados de smoke (users + arquitetos + clientes)"
      contains: "## Smoke Users"
  key_links:
    - from: "09-SMOKE-SETUP.md"
      to: "09-06 (Playwright bilateral)"
      via: "user_ids + nomes dos cadastros pra Playwright loginar e validar"
      pattern: "Smoke A"
---

<objective>
Preparar os dados de smoke em produção: 2 contas de colaborador reais (A e B) criadas via signup completo, cada uma com 1 arquiteto + 1 cliente cadastrado pela UI. Output: arquivo `09-SMOKE-SETUP.md` com inventário detalhado pra Playwright (09-06) consumir.

Purpose: Sem 2 contas reais com dados próprios, não dá pra provar isolamento bilateral (success criterion #5 do roadmap). Padrão herdado da Phase 6 (v1.0 closure) que validou Drive da mesma forma.

Output: `.planning/phases/09-multi-tenancy-rls/09-SMOKE-SETUP.md` + 2 colabs + 2 arquitetos + 2 clientes em prod.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-multi-tenancy-rls/09-CONTEXT.md
@.planning/phases/09-multi-tenancy-rls/09-PUSH-LOG.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [HUMAN] Lenny libera emails reais para signup e confirma SMTP rota</name>
  <files>(nenhum arquivo modificado nesta task — checkpoint humano)</files>
  <action>Pause aqui pra Lenny decidir as 2 contas de email a usar no smoke. Sem CLI/API que substitua: emails reais precisam ter inbox acessível pra confirmar signup link Supabase Auth. Não tentar automatizar criação de aliases — Lenny configura inbox externamente.</action>
  <what-built>Migration aplicada (09-04 SUCCESS). Agora precisa de 2 emails reais com inbox acessível pra completar signup (Supabase Auth manda link de confirmação)</what-built>
  <how-to-verify>
    Lenny decide:
    1. **Opção A** (preferida): usar `smoke-colab-a@luminattiled.com.br` + `smoke-colab-b@luminattiled.com.br` (criar aliases em Microsoft 365). Confirmar que recebe email no inbox que ele controla (provavelmente forward pro Lenny ou shared mailbox).
    2. **Opção B** (fallback): usar `lennywajcberg+smoke-a@gmail.com` + `+smoke-b@gmail.com` se Resend não rejeitar (gmail subaddressing). Lenny confirma que recebe ambos no inbox dele.
    3. Confirmar que SMTP de prod tá em pé (custom Resend `onboarding@resend.dev`) e que `request-access` está funcionando (fix 16c0b14 está em prod).
  </how-to-verify>
  <resume-signal>Lenny responde com os 2 emails escolhidos (formato literal). Esse string vira input do Task 2.</resume-signal>
  <verify>Lenny respondeu no chat com 2 emails literais e confirmou inbox acessível.</verify>
  <done>2 emails fornecidos por Lenny; Task 2 pode prosseguir.</done>
</task>

<task type="auto">
  <name>Task 2: Criar 2 colabs via Playwright + cadastrar 1 arquiteto + 1 cliente por colab</name>
  <files>.planning/phases/09-multi-tenancy-rls/09-SMOKE-SETUP.md</files>
  <read_first>
    - .planning/phases/09-multi-tenancy-rls/09-CONTEXT.md (D-12..D-14 — fluxo do smoke)
    - src/pages/Auth.tsx (fluxo de signup: senha 8 chars + Upper + Lower + Num + Special, CPF/telefone/setor obrigatórios)
    - Os 2 emails fornecidos no Task 1 checkpoint
  </read_first>
  <action>
    Passo 1 — Adicionar emails em `allowed_users`:
    Via MCP `mcp__plugin_supabase_supabase__execute_sql`:
    ```sql
    INSERT INTO public.allowed_users (email) VALUES
      ('{EMAIL_A}'),
      ('{EMAIL_B}')
    ON CONFLICT (email) DO NOTHING;
    ```
    Substituir `{EMAIL_A}` e `{EMAIL_B}` pelos emails do checkpoint Task 1.

    Passo 2 — Signup colab A via Playwright MCP (mcp__plugin_playwright_playwright__*):
    1. Navegar para `https://orcamentosaura.com.br/auth` (ou domínio prod)
    2. Clicar em "Criar conta" / aba signup
    3. Preencher:
       - Email: `{EMAIL_A}`
       - Senha: `SmokeA2026!` (atende regex: 8 chars, Upper, Lower, Number, Special)
       - Nome: `Smoke Colab A`
       - CPF: `111.444.777-35` (válido — checksum correto) ou outro CPF válido inventado
       - Telefone: `(11) 99999-0001`
       - Setor: qualquer setor disponível no dropdown (anotar qual)
    4. Submit
    5. Lenny precisa abrir o inbox e clicar no link de confirmação Supabase Auth. **Aviso:** Playwright não consegue ler inbox de email. Se confirmação for via link Supabase, pausar pra Lenny confirmar manualmente — registrar timestamp no SMOKE-SETUP.md.
    6. Após confirmação, login com `{EMAIL_A}` + senha; verificar redirecionamento para `/` (Index).

    Passo 3 — Cadastrar 1 arquiteto + 1 cliente como A:
    Ainda logado como A via Playwright:
    1. Não rota de cadastro de arquiteto pra colab (arquitetos são admin-only no UI). **Importante:** D-13 do contexto fala "cada colab cria 1 arquiteto + 1 cliente via UI" — confirmar se a UI permite. Se não permitir (admin-only), fazer INSERT direto via SQL com user_id manual:
    ```sql
    -- Como contornar: INSERT direto via MCP execute_sql (não tem RLS porque MCP usa owner token, mas user_id manualmente preenchido)
    INSERT INTO public.arquitetos (nome, contato, user_id)
    VALUES ('Smoke A — Arq', 'arq-a@smoke.test', '{USER_ID_A}');
    INSERT INTO public.clientes (nome, contato, user_id)
    VALUES ('Smoke A — Cli', 'cli-a@smoke.test', '{USER_ID_A}');
    ```
    Onde `{USER_ID_A}` vem de `SELECT id FROM auth.users WHERE email='{EMAIL_A}';`.

    Caminho alternativo (preferido se UI permitir): logar como A, ir em Cadastros (se tab acessível) ou em wizard step 2 (que tem botão "Criar Cliente"), criar 1 cliente "Smoke A — Cli". Arquiteto: se autocomplete tem botão "Criar novo arquiteto", usar; senão fallback SQL.

    Capturar `id` (UUID) do arquiteto e cliente criados.

    Passo 4 — Repetir passo 2 e 3 para colab B com:
    - Email: `{EMAIL_B}`
    - Senha: `SmokeB2026!`
    - Nome: `Smoke Colab B`
    - CPF: `222.555.888-46` (ou outro CPF válido distinto)
    - Telefone: `(11) 99999-0002`
    - Setor: idem A ou diferente
    - Cadastros: `Smoke B — Arq` + `Smoke B — Cli`

    Passo 5 — Documentar tudo em `.planning/phases/09-multi-tenancy-rls/09-SMOKE-SETUP.md`:
    ```markdown
    # 09-SMOKE-SETUP — Dados de Smoke RLS (Plan 09-05)

    **Created:** {ISO datetime}
    **Phase:** 09-multi-tenancy-rls
    **Purpose:** Inventário de 2 colabs + 4 cadastros para Playwright bilateral (09-06)

    ## Smoke Users (auth.users + colaboradores)

    | Email | user_id (auth.users.id) | Nome | Senha (clear, smoke only) | Confirmed at |
    |-------|-------------------------|------|---------------------------|--------------|
    | {EMAIL_A} | {uuid_A} | Smoke Colab A | SmokeA2026! | {ISO} |
    | {EMAIL_B} | {uuid_B} | Smoke Colab B | SmokeB2026! | {ISO} |

    ## Smoke Cadastros

    | Tipo | Nome | id | user_id (dono) |
    |------|------|----|----------------|
    | Arquiteto | Smoke A — Arq | {uuid} | {uuid_A} |
    | Cliente | Smoke A — Cli | {uuid} | {uuid_A} |
    | Arquiteto | Smoke B — Arq | {uuid} | {uuid_B} |
    | Cliente | Smoke B — Cli | {uuid} | {uuid_B} |

    ## Method per User
    - Signup completo via Auth.tsx (CPF/telefone/setor obrigatórios atendidos)
    - Confirmação de email manual por Lenny ({timestamp})
    - Auto-criação de `colaboradores` via `create-colaborador` edge function (verificada via SELECT)
    - Cadastros via {UI/SQL fallback} — registrar qual caminho cada cadastro tomou

    ## Notes
    - Cleanup será feito em 09-07 (DELETE arquitetos/clientes Smoke %; auth.users ficam como pending cleanup)
    - Senhas em clear text neste arquivo são intencionais (smoke-only); arquivo NÃO deve ser publicado em repo externo
    ```

    Preencher com valores reais, não placeholders.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f='.planning\\phases\\09-multi-tenancy-rls\\09-SMOKE-SETUP.md'; (Test-Path $f) -and (Select-String -Path $f -Pattern '## Smoke Users' -Quiet) -and (Select-String -Path $f -Pattern 'Smoke A — Arq' -Quiet) -and (Select-String -Path $f -Pattern 'Smoke B — Cli' -Quiet)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/09-multi-tenancy-rls/09-SMOKE-SETUP.md` exists
    - File contains literal `## Smoke Users` heading
    - File contains literal `## Smoke Cadastros` heading
    - File contains literal nome `Smoke A — Arq`, `Smoke A — Cli`, `Smoke B — Arq`, `Smoke B — Cli` (4 cadastros)
    - File contains 2 distinct UUIDs in user_id column (uuid_A != uuid_B)
    - SQL query verifies: `SELECT count(*) FROM public.arquitetos WHERE nome LIKE 'Smoke %'` returns 2
    - SQL query verifies: `SELECT count(*) FROM public.clientes WHERE nome LIKE 'Smoke %'` returns 2
    - SQL query verifies: `SELECT count(*) FROM auth.users WHERE email LIKE '%smoke%' OR email IN ('{EMAIL_A}','{EMAIL_B}')` returns 2
    - SQL query verifies: `SELECT count(*) FROM public.colaboradores WHERE user_id IN (uuid_A, uuid_B)` returns 2
  </acceptance_criteria>
  <done>2 users de smoke + 2 arquitetos + 2 clientes criados em prod, todos com user_id corretamente associado ao dono. SMOKE-SETUP.md documentado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → prod (writes via signup UI + MCP SQL) | Cria usuários e dados reais em prod; cleanup em 09-07 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Severity | Mitigation Plan |
|-----------|----------|-----------|-------------|----------|-----------------|
| T-09-E01 | I (Information disclosure) | Senhas em clear text no SMOKE-SETUP.md | accept | LOW | Arquivo é repo privado; senhas só usadas pra teste; serão invalidadas em cleanup |
| T-09-E02 | T (Tampering) | Colab smoke acaba sendo usado por terceiro | accept | LOW | Cleanup em 09-07 + pending cleanup nota em [[project_aura_pending_cleanup]] |
| T-09-E03 | E (Elevation) | Algum colab smoke ganha role admin acidentalmente | mitigate | MEDIUM | Verificar via SQL após signup: `SELECT count(*) FROM user_roles WHERE user_id IN (uuid_A,uuid_B) AND role='admin'` = 0 |
| T-09-E04 | I (Information disclosure) | Insert SQL direto bypassa WITH CHECK (MCP usa owner token) | mitigate | MEDIUM | INSERT inclui user_id explícito do colab; bypass é intencional pra setup, smoke 09-06 valida que comportamento real (via user JWT) honra RLS |
</threat_model>

<verification>
- 2 colabs ativos
- 2 arquitetos + 2 clientes em prod com user_id correto
- Nenhum colab smoke tem role admin
</verification>

<success_criteria>
Setup pronto pra 09-06 rodar Playwright bilateral. Cobertura de RLS-01/RLS-02 success criterion #5 começa aqui e fecha em 09-06.
</success_criteria>

<output>
After completion, create `.planning/phases/09-multi-tenancy-rls/09-05-SUMMARY.md` resumindo: 2 emails criados, 4 cadastros criados (com ids), confirmações de email manuais (timestamps), e fallbacks usados (UI vs SQL).
</output>
