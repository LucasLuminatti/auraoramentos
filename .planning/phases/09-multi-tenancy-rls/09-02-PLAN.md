---
phase: 09-multi-tenancy-rls
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-multi-tenancy-rls/09-PUSH-LOG.md
autonomous: true
requirements: [RLS-01, RLS-02]
must_haves:
  truths:
    - "Snapshot pré-migration de pg_policies para arquitetos e clientes está documentado em 09-PUSH-LOG.md"
    - "Snapshot inclui count e lista de policies por tabela (esperado: arquitetos com 2 policies legadas, clientes com 4 policies legadas)"
    - "Estado de RLS (relrowsecurity) também documentado pra cada tabela"
  artifacts:
    - path: ".planning/phases/09-multi-tenancy-rls/09-PUSH-LOG.md"
      provides: "PRE-PUSH snapshot pg_policies para baseline"
      contains: "## PRE-PUSH pg_policies snapshot"
  key_links:
    - from: "09-PUSH-LOG.md"
      to: "09-04 (apply migration)"
      via: "POST-PUSH snapshot é anexado depois pra diff"
      pattern: "PRE-PUSH"
---

<objective>
Capturar snapshot baseline read-only de `pg_policies` e `pg_class.relrowsecurity` para as tabelas `public.arquitetos` e `public.clientes` em produção, ANTES da migration ser aplicada. Output: arquivo `09-PUSH-LOG.md` (template aberto, será completado em 09-04 com POST-PUSH).

Purpose: Validar a hipótese D-02 (policies legadas listadas) e garantir diff observável pós-apply (D-08). Sem PRE-PUSH, não há como provar que a migration realmente alterou o estado correto.

Output: `.planning/phases/09-multi-tenancy-rls/09-PUSH-LOG.md` com snapshot inicial.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-multi-tenancy-rls/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot pg_policies + relrowsecurity PRE-MIGRATION</name>
  <files>.planning/phases/09-multi-tenancy-rls/09-PUSH-LOG.md</files>
  <read_first>
    - .planning/phases/09-multi-tenancy-rls/09-CONTEXT.md (D-02 lista policies legadas esperadas)
    - .planning/phases/08-cadastros-opcionalizar-imagens-manuais/PUSH-LOG.md se existir (template de formato; senão usar template abaixo)
  </read_first>
  <action>
    Rodar via MCP `mcp__plugin_supabase_supabase__execute_sql` (read-only, sem migration) as duas queries:

    ```sql
    -- Query 1: policies em arquitetos e clientes
    SELECT
      schemaname, tablename, policyname, permissive, roles, cmd,
      qual, with_check
    FROM pg_policies
    WHERE schemaname = 'public'
      AND tablename IN ('arquitetos', 'clientes')
    ORDER BY tablename, cmd, policyname;

    -- Query 2: RLS enabled status
    SELECT
      n.nspname AS schema,
      c.relname AS table,
      c.relrowsecurity AS rls_enabled,
      c.relforcerowsecurity AS rls_forced
    FROM pg_class c
    JOIN pg_namespace n ON n.oid = c.relnamespace
    WHERE n.nspname = 'public'
      AND c.relname IN ('arquitetos', 'clientes')
    ORDER BY c.relname;
    ```

    Esperado por D-02 (validar; se divergir, anotar):
    - `arquitetos`: 2 policies — `"Anyone can read arquitetos"` (SELECT, qual=true) e `"Admins can manage arquitetos"` (ALL, has_role admin)
    - `clientes`: 4 policies — `"Anyone can read clientes"` (SELECT true), `"Authenticated users can insert clientes"` (INSERT with_check true), `"Authenticated users can update clientes"` (UPDATE true/true), `"Authenticated users can delete clientes"` (DELETE true). Nomes exatos podem variar — registrar o que voltar.
    - `relrowsecurity = true` em ambas (Phase 7 já enabled ou Drive padrão). Se for false, anotar — a migration tem `ENABLE ROW LEVEL SECURITY` idempotente que vai cobrir.

    Criar `.planning/phases/09-multi-tenancy-rls/09-PUSH-LOG.md` com este formato:

    ```markdown
    # 09-PUSH-LOG — Multi-tenancy RLS (RLS-01 + RLS-02)

    **Phase:** 09-multi-tenancy-rls
    **Migration:** supabase/migrations/20260514000001_arquitetos_clientes_rls.sql (TBD em 09-03)
    **Push method:** mcp__plugin_supabase_supabase__apply_migration (D-08)

    ---

    ## PRE-PUSH pg_policies snapshot

    **Captured:** {ISO datetime}
    **Method:** mcp__plugin_supabase_supabase__execute_sql

    ### Table: public.arquitetos

    | policyname | cmd | roles | qual | with_check |
    |------------|-----|-------|------|------------|
    | {...} | {...} | {...} | {...} | {...} |

    **Total policies (arquitetos):** {N}
    **RLS enabled:** {true/false}

    ### Table: public.clientes

    | policyname | cmd | roles | qual | with_check |
    |------------|-----|-------|------|------------|
    | {...} | {...} | {...} | {...} | {...} |

    **Total policies (clientes):** {N}
    **RLS enabled:** {true/false}

    ### Divergência com D-02 (se houver)
    [Lista nominal de qualquer policy NÃO prevista em D-02, ou policy prevista mas ausente. Esses precisam ser tratados em 09-03 — DROP IF EXISTS cobre ausência; presença não-prevista precisa de DROP adicional.]

    ---

    ## POST-PUSH pg_policies snapshot
    [TODO — preenchido em 09-04 após apply]

    ---

    ## Apply Log
    [TODO — preenchido em 09-04]
    ```

    Preencher concretamente o bloco PRE-PUSH com o resultado das queries MCP. Deixar POST-PUSH e Apply Log como TODO (serão preenchidos em 09-04).

    **Importante:** Se aparecer policy não prevista em D-02 (ex: alguma policy criada em phase intermediária esquecida), registrar na seção "Divergência com D-02". Plan 09-03 vai precisar adicionar DROP IF EXISTS para essa policy também.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path '.planning\\phases\\09-multi-tenancy-rls\\09-PUSH-LOG.md' -Pattern 'PRE-PUSH pg_policies snapshot' -Quiet"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/09-multi-tenancy-rls/09-PUSH-LOG.md` exists
    - File contains literal heading `## PRE-PUSH pg_policies snapshot`
    - File contains literal heading `### Table: public.arquitetos`
    - File contains literal heading `### Table: public.clientes`
    - File contains literal line starting with `**Total policies (arquitetos):**` followed by a numeric value (not `{N}`)
    - File contains literal line starting with `**Total policies (clientes):**` followed by a numeric value
    - File contains literal section `### Divergência com D-02 (se houver)` either with "Nenhuma" or with concrete policy names
    - File contains placeholder `[TODO — preenchido em 09-04 após apply]` for POST-PUSH section
    - File contains placeholder `[TODO — preenchido em 09-04]` for Apply Log section
  </acceptance_criteria>
  <done>09-PUSH-LOG.md commitado com snapshot PRE-PUSH concreto; divergências com D-02 (se houver) listadas pra 09-03 ajustar drops.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → prod DB (read-only via MCP) | MCP usa owner token; SELECT em pg_catalog é seguro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-B01 | I (Information disclosure) | Snapshot exposes policy logic | accept | Policy logic é público no repo; sem leak adicional |
| T-09-B02 | T (Tampering) | Snapshot manualmente editado | mitigate | acceptance_criteria exige Total policies como número, não placeholder |
</threat_model>

<verification>
- 09-PUSH-LOG.md existe com PRE-PUSH preenchido (sem placeholders `{N}` no lugar de counts)
- Divergência com D-02 explicitada
</verification>

<success_criteria>
Baseline do estado de policies pré-migration documentado; 09-03 sabe exatamente quais DROP IF EXISTS precisa cobrir.
</success_criteria>

<output>
After completion, create `.planning/phases/09-multi-tenancy-rls/09-02-SUMMARY.md` resumindo: contagem de policies legadas por tabela, divergência com D-02 (sim/não + detalhe), input concreto pra 09-03 (lista de DROPs necessários).
</output>
