---
phase: 09-multi-tenancy-rls
plan: 07
type: execute
wave: 6
depends_on: [09-06]
files_modified:
  - .planning/phases/09-multi-tenancy-rls/09-CLEANUP-LOG.md
autonomous: true
requirements: [RLS-01, RLS-02]
must_haves:
  truths:
    - "Os 2 arquitetos Smoke e 2 clientes Smoke foram deletados de prod"
    - "Os 2 auth.users dos colabs smoke foram registrados como pending cleanup em [[project_aura_pending_cleanup]] (não deletáveis via SQL público)"
    - "Os 2 colaboradores correspondentes foram desativados ou marcados como pending cleanup"
    - "09-CLEANUP-LOG.md documenta: counts antes/depois do DELETE, status auth.users, e nota em pending cleanup"
  artifacts:
    - path: ".planning/phases/09-multi-tenancy-rls/09-CLEANUP-LOG.md"
      provides: "Log de cleanup pós-smoke"
      contains: "## DELETE results"
  key_links:
    - from: "DELETE arquitetos/clientes Smoke %"
      to: "0 linhas residuais"
      via: "SQL via MCP execute_sql (owner token bypassa RLS pra cleanup)"
      pattern: "DELETE FROM public.arquitetos"
---

<objective>
Cleanup pós-smoke: deletar os 4 cadastros Smoke (2 arquitetos + 2 clientes) via SQL, registrar os 2 auth.users como pending cleanup (não deletáveis sem service role), e fechar Phase 9 com documentação completa.

Purpose: D-15 explicitamente exige cleanup ao fim do smoke pra não deixar lixo em prod. auth.users ficam pendentes — Lenny anota em `[[project_aura_pending_cleanup]]` pra housekeeping futuro.

Output: 0 linhas com nome `Smoke %` em arquitetos/clientes; 09-CLEANUP-LOG.md documentando o cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-multi-tenancy-rls/09-CONTEXT.md
@.planning/phases/09-multi-tenancy-rls/09-SMOKE-SETUP.md
@.planning/phases/09-multi-tenancy-rls/09-SMOKE-RESULTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DELETE smoke data + documentar pending cleanup</name>
  <files>.planning/phases/09-multi-tenancy-rls/09-CLEANUP-LOG.md</files>
  <read_first>
    - .planning/phases/09-multi-tenancy-rls/09-SMOKE-SETUP.md (inventário de IDs a deletar)
    - .planning/phases/09-multi-tenancy-rls/09-SMOKE-RESULTS.md (confirmação 7/7 PASS antes de cleanup — se FAIL, NÃO cleanar)
    - .planning/phases/09-multi-tenancy-rls/09-CONTEXT.md (D-15: cleanup pattern)
  </read_first>
  <action>
    Passo 0 — Sanity check: confirmar 09-SMOKE-RESULTS.md tem `**Overall:** 7/7 PASS`. Se não, ABORTAR — não cleanar dados que ainda servem pra debug.

    Passo 1 — COUNT before via MCP execute_sql:
    ```sql
    SELECT
      (SELECT count(*) FROM public.arquitetos WHERE nome LIKE 'Smoke %') AS arq_smoke,
      (SELECT count(*) FROM public.clientes WHERE nome LIKE 'Smoke %') AS cli_smoke,
      (SELECT count(*) FROM auth.users WHERE id IN ('{uuid_A}','{uuid_B}')) AS smoke_users,
      (SELECT count(*) FROM public.colaboradores WHERE user_id IN ('{uuid_A}','{uuid_B}')) AS smoke_colabs;
    ```
    Esperado: arq_smoke=2, cli_smoke=2, smoke_users=2, smoke_colabs=2.

    Substituir `{uuid_A}` e `{uuid_B}` pelos UUIDs concretos de 09-SMOKE-SETUP.md.

    Passo 2 — DELETE smoke data:
    ```sql
    -- Não há FK de orçamentos para clientes/arquitetos que bloqueie delete (ON DELETE SET NULL ou CASCADE conforme schema atual)
    -- Se houver FK RESTRICT impedindo, abortar e abrir nota pra Lenny revisar.

    DELETE FROM public.arquitetos WHERE nome LIKE 'Smoke %';
    DELETE FROM public.clientes WHERE nome LIKE 'Smoke %';
    ```

    Passo 3 — Desativar (não deletar) colaboradores:
    ```sql
    UPDATE public.colaboradores
    SET ativo = false  -- assumindo coluna `ativo`; se não existir, pular e registrar em pending cleanup
    WHERE user_id IN ('{uuid_A}','{uuid_B}');
    ```
    Se a tabela `colaboradores` não tem coluna `ativo`, fazer SELECT do schema primeiro pra confirmar; se não existir, pular UPDATE e registrar em pending cleanup.

    Passo 4 — COUNT after:
    ```sql
    SELECT
      (SELECT count(*) FROM public.arquitetos WHERE nome LIKE 'Smoke %') AS arq_smoke,
      (SELECT count(*) FROM public.clientes WHERE nome LIKE 'Smoke %') AS cli_smoke;
    ```
    Esperado: 0, 0.

    Passo 5 — Documentar em `.planning/phases/09-multi-tenancy-rls/09-CLEANUP-LOG.md`:
    ```markdown
    # 09-CLEANUP-LOG — Pós-smoke cleanup (Plan 09-07)

    **Executed:** {ISO datetime}
    **Phase:** 09-multi-tenancy-rls
    **Triggered:** Smoke 7/7 PASS confirmado em 09-SMOKE-RESULTS.md
    **Method:** mcp__plugin_supabase_supabase__execute_sql (owner token bypassa RLS)

    ## COUNT before
    | Item | Count |
    |------|-------|
    | arquitetos LIKE 'Smoke %' | 2 |
    | clientes LIKE 'Smoke %' | 2 |
    | auth.users (smoke) | 2 |
    | colaboradores (smoke) | 2 |

    ## DELETE results
    | Statement | Rows affected |
    |-----------|---------------|
    | DELETE FROM public.arquitetos WHERE nome LIKE 'Smoke %' | 2 |
    | DELETE FROM public.clientes WHERE nome LIKE 'Smoke %' | 2 |
    | UPDATE public.colaboradores SET ativo=false WHERE user_id IN (...) | 2 (ou skipped: razão) |

    ## COUNT after
    | Item | Count |
    |------|-------|
    | arquitetos LIKE 'Smoke %' | 0 |
    | clientes LIKE 'Smoke %' | 0 |

    ## Pending Cleanup
    Os 2 auth.users dos colabs smoke NÃO foram deletados (D-15 — requer service role REST API; SQL público não permite).

    | user_id | email | created |
    |---------|-------|---------|
    | {uuid_A} | {EMAIL_A} | {ISO} |
    | {uuid_B} | {EMAIL_B} | {ISO} |

    **Action item:** adicionar entrada em `[[project_aura_pending_cleanup]]` (memory user) listando os 2 emails + user_ids para housekeeping manual via Supabase dashboard ou script com service role key. Não-bloqueante para fechar Phase 9.

    ## Phase 9 closure status
    - RLS-01: DELIVERED (clientes RLS validado bilateral)
    - RLS-02: DELIVERED (arquitetos RLS validado bilateral)
    - Smoke 7/7 PASS (09-SMOKE-RESULTS.md)
    - Cleanup completo (4 rows deleted, 2 users pending housekeeping)
    ```

    Preencher com valores reais. NÃO commitar memory `project_aura_pending_cleanup.md` — deixar pro próximo /gsd-finish-phase ou Lenny atualizar manualmente.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f='.planning\\phases\\09-multi-tenancy-rls\\09-CLEANUP-LOG.md'; (Test-Path $f) -and (Select-String -Path $f -Pattern 'COUNT after' -Quiet) -and (Select-String -Path $f -Pattern 'Pending Cleanup' -Quiet) -and (Select-String -Path $f -Pattern 'Phase 9 closure status' -Quiet)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/09-multi-tenancy-rls/09-CLEANUP-LOG.md` exists
    - File contains literal heading `## COUNT before` and `## COUNT after`
    - File contains literal heading `## DELETE results`
    - File contains literal heading `## Pending Cleanup`
    - File contains literal heading `## Phase 9 closure status`
    - File contains literal `RLS-01: DELIVERED`
    - File contains literal `RLS-02: DELIVERED`
    - SQL crosscheck: `SELECT count(*) FROM public.arquitetos WHERE nome LIKE 'Smoke %'` returns 0
    - SQL crosscheck: `SELECT count(*) FROM public.clientes WHERE nome LIKE 'Smoke %'` returns 0
    - File contains 2 distinct UUIDs in Pending Cleanup section (the auth.users left over)
  </acceptance_criteria>
  <done>4 rows Smoke deletados de prod, 2 auth.users registrados como pending cleanup, Phase 9 fechada com RLS-01 e RLS-02 DELIVERED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → prod DB (DELETE via MCP) | Owner token bypassa RLS; restrito a nomes LIKE 'Smoke %' |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Severity | Mitigation Plan |
|-----------|----------|-----------|-------------|----------|-----------------|
| T-09-G01 | T (Tampering) | DELETE com filtro errado apaga dados reais | mitigate | HIGH | Filtro `LIKE 'Smoke %'` é estrito; COUNT before+after dá sanity check (esperado: 2→0, não 2→muito) |
| T-09-G02 | I (Information disclosure) | 2 auth.users smoke continuam ativos em prod | accept | LOW | Senhas só conhecidas no SMOKE-SETUP.md (repo privado); colaboradores desativados; pending cleanup tracked |
| T-09-G03 | D (DoS) | DELETE cascade quebra orçamentos FK | mitigate | MEDIUM | Sanity check: smoke users não criaram orçamentos (smoke não testa wizard); se FK explodir, abortar pra Lenny |
| T-09-G04 | R (Repudiation) | Cleanup sem audit | mitigate | LOW | CLEANUP-LOG.md commitado deixa trace |
</threat_model>

<verification>
- 0 rows Smoke em arquitetos/clientes pós-cleanup
- 09-CLEANUP-LOG.md commitado com counts before/after
- Pending cleanup nota com 2 user_ids
</verification>

<success_criteria>
Phase 9 limpa e fechada. RLS-01 + RLS-02 entregues e validados. Lixo mínimo (2 auth.users) registrado pra housekeeping.
</success_criteria>

<output>
After completion, create `.planning/phases/09-multi-tenancy-rls/09-07-SUMMARY.md` resumindo: counts antes/depois, status colaboradores (desativados/pending), 2 auth.users em pending cleanup, e closure status RLS-01/RLS-02 DELIVERED.
</output>
