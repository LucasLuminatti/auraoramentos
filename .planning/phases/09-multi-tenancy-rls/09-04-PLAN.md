---
phase: 09-multi-tenancy-rls
plan: 04
type: execute
wave: 3
depends_on: [09-03]
files_modified:
  - .planning/phases/09-multi-tenancy-rls/09-PUSH-LOG.md
autonomous: false
requirements: [RLS-01, RLS-02]
user_setup:
  - service: supabase
    why: "Apply migration via MCP (owner token already in MCP). Lenny revisa antes do apply — política de segurança pra prod."
must_haves:
  truths:
    - "Migration 20260514000001_arquitetos_clientes_rls.sql aplicada em prod com sucesso (zero erros)"
    - "POST-PUSH snapshot pg_policies documenta 8 policies novas (4 por tabela)"
    - "Policies legadas (D-02) não aparecem mais no POST-PUSH snapshot"
    - "Diff PRE → POST documentado em Apply Log"
    - "build + lint passam (sem regressão TS)"
  artifacts:
    - path: ".planning/phases/09-multi-tenancy-rls/09-PUSH-LOG.md"
      provides: "Apply Log + POST-PUSH snapshot completos"
      contains: "## POST-PUSH pg_policies snapshot"
  key_links:
    - from: "Migration apply"
      to: "Prod RLS state"
      via: "mcp__plugin_supabase_supabase__apply_migration"
      pattern: "apply_migration"
---

<objective>
[BLOCKING] Aplicar a migration `20260514000001_arquitetos_clientes_rls.sql` em produção via MCP `apply_migration`, capturar snapshot POST-PUSH de `pg_policies`, e atualizar `09-PUSH-LOG.md` com o Apply Log + diff PRE/POST.

Purpose: Sem este push, nenhuma das policies novas existe em prod e RLS-01/RLS-02 ficam paper-tigers. Phase 9 NÃO pode passar verification sem essa task — build/lint passariam falsamente porque types.ts não muda.

Output: Migration aplicada em prod + 09-PUSH-LOG.md atualizado com POST-PUSH snapshot + Apply Log + status confirmado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-multi-tenancy-rls/09-CONTEXT.md
@.planning/phases/09-multi-tenancy-rls/09-PUSH-LOG.md
@supabase/migrations/20260514000001_arquitetos_clientes_rls.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Human review da migration antes do apply</name>
  <files>supabase/migrations/20260514000001_arquitetos_clientes_rls.sql</files>
  <action>Human review only: Lenny abre a migration no editor, valida estrutura conforme how-to-verify, e responde approved/needs-changes. Sem código novo nesta task — somente gate manual antes do Task 2 aplicar em prod.</action>
  <what-built>Migration SQL escrita em 09-03 (`supabase/migrations/20260514000001_arquitetos_clientes_rls.sql`)</what-built>
  <how-to-verify>
    1. Abrir o arquivo `supabase/migrations/20260514000001_arquitetos_clientes_rls.sql` no editor
    2. Conferir os 5 blocos: (1) DEFAULT auth.uid, (2) DROP legadas, (3) ENABLE RLS, (4) Policies arquitetos, (5) Policies clientes
    3. Conferir BEGIN no topo + COMMIT no fundo (atomicidade D-07)
    4. Conferir que o pattern é literalmente igual ao do Drive (Blocos 5/6 de `20260504000001_drive_rls_user_id.sql`) com apenas trocas de "arquivos"/"pastas" por "arquitetos"/"clientes"
    5. Conferir que NÃO tem `ADD COLUMN`, `SET NOT NULL` ou DDL além do `SET DEFAULT` (Phase 7 já cuidou disso)
    6. Conferir lista de DROP IF EXISTS contra o snapshot PRE-PUSH em 09-PUSH-LOG.md — toda policy legada listada precisa aparecer no DROP
  </how-to-verify>
  <resume-signal>Lenny responde "approved" pra liberar apply, ou descreve problema (que volta pra 09-03).</resume-signal>
  <verify>Lenny respondeu approved no chat.</verify>
  <done>Aprovação humana recebida; pode prosseguir para Task 2 (apply).</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration via MCP + capturar POST-PUSH</name>
  <files>.planning/phases/09-multi-tenancy-rls/09-PUSH-LOG.md</files>
  <read_first>
    - supabase/migrations/20260514000001_arquitetos_clientes_rls.sql (conteúdo completo a passar pro MCP)
    - .planning/phases/09-multi-tenancy-rls/09-PUSH-LOG.md (PRE-PUSH baseline pra diff)
  </read_first>
  <action>
    Passo 1 — Apply via MCP:
    Chamar `mcp__plugin_supabase_supabase__apply_migration` com:
    - `name`: `arquitetos_clientes_rls`
    - `query`: conteúdo literal completo do arquivo `supabase/migrations/20260514000001_arquitetos_clientes_rls.sql` (incluindo BEGIN/COMMIT)

    Se MCP retornar erro:
    - Capturar mensagem de erro literal
    - NÃO tentar fallback automático — registrar em Apply Log e parar pra Lenny decidir (push CLI manual ou rollback)

    Passo 2 — Capturar POST-PUSH snapshot:
    Rodar via MCP `mcp__plugin_supabase_supabase__execute_sql` as mesmas queries do 09-02:
    ```sql
    SELECT schemaname, tablename, policyname, permissive, roles, cmd, qual, with_check
    FROM pg_policies
    WHERE schemaname='public' AND tablename IN ('arquitetos','clientes')
    ORDER BY tablename, cmd, policyname;

    SELECT n.nspname AS schema, c.relname AS table, c.relrowsecurity AS rls_enabled, c.relforcerowsecurity AS rls_forced
    FROM pg_class c JOIN pg_namespace n ON n.oid=c.relnamespace
    WHERE n.nspname='public' AND c.relname IN ('arquitetos','clientes')
    ORDER BY c.relname;
    ```

    Esperado:
    - `arquitetos`: 4 policies novas (`"Colabs read own arquitetos, admins read all"`, `"Colabs insert own arquitetos"`, `"Colabs update own arquitetos, admins update all"`, `"Colabs delete own arquitetos, admins delete all"`)
    - `clientes`: 4 policies novas (`"Colabs read own clientes, admins read all"`, `"Colabs insert own clientes"`, `"Colabs update own clientes, admins update all"`, `"Colabs delete own clientes, admins delete all"`)
    - `relrowsecurity = true` em ambas
    - Policies legadas do PRE-PUSH não aparecem mais

    Passo 3 — Verificar DEFAULT auth.uid():
    ```sql
    SELECT table_schema, table_name, column_name, column_default
    FROM information_schema.columns
    WHERE table_schema='public' AND table_name IN ('arquitetos','clientes') AND column_name='user_id';
    ```
    Esperado: `column_default = 'auth.uid()'` (ou variante `auth.uid()::uuid`) em ambas as linhas.

    Passo 4 — Build + lint local pra sanidade:
    ```bash
    npm run build
    npm run lint
    ```
    Esperado: exit 0 em ambos. Se falhar, registrar em Apply Log e parar (improvável — types.ts não muda).

    Passo 5 — Atualizar 09-PUSH-LOG.md:
    Substituir as seções `## POST-PUSH pg_policies snapshot` e `## Apply Log` (que estavam como TODO) por conteúdo concreto:

    ```markdown
    ## POST-PUSH pg_policies snapshot

    **Captured:** {ISO datetime}
    **Method:** mcp__plugin_supabase_supabase__execute_sql

    ### Table: public.arquitetos
    | policyname | cmd | roles | qual | with_check |
    |------------|-----|-------|------|------------|
    | Colabs delete own arquitetos, admins delete all | DELETE | {authenticated} | (user_id = auth.uid() OR has_role(auth.uid(),'admin'::app_role)) | NULL |
    | Colabs insert own arquitetos | INSERT | {authenticated} | NULL | (user_id = auth.uid()) |
    | Colabs read own arquitetos, admins read all | SELECT | {authenticated} | (user_id = auth.uid() OR has_role(...)) | NULL |
    | Colabs update own arquitetos, admins update all | UPDATE | {authenticated} | (...) | (...) |

    **Total policies (arquitetos):** 4
    **RLS enabled:** true

    ### Table: public.clientes
    | ... 4 linhas similares ... |

    **Total policies (clientes):** 4
    **RLS enabled:** true

    ### DEFAULT verification
    | table | column | column_default |
    |-------|--------|----------------|
    | arquitetos | user_id | auth.uid() |
    | clientes | user_id | auth.uid() |

    ## Apply Log

    **Method:** mcp__plugin_supabase_supabase__apply_migration
    **Name:** arquitetos_clientes_rls
    **Started:** {ISO datetime}
    **Result:** SUCCESS
    **Duration:** {N}ms (se MCP reportar)
    **Errors:** none

    ### Diff PRE → POST
    - **Dropped:** {N} legacy policies (listados pelos nomes do PRE-PUSH)
    - **Created:** 8 new policies (4 arquitetos + 4 clientes)
    - **DEFAULT added:** arquitetos.user_id → auth.uid(), clientes.user_id → auth.uid()
    - **RLS state:** unchanged (already enabled in both pre-push)

    ### Build sanity
    - npm run build: exit 0
    - npm run lint: exit 0
    ```

    Preencher de fato os dados retornados (não placeholders). Se algum count divergir do esperado (≠4 por tabela, ou DEFAULT não confirmado), abortar com erro claro no Apply Log e parar pra Lenny intervir.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f='.planning\\phases\\09-multi-tenancy-rls\\09-PUSH-LOG.md'; (Select-String -Path $f -Pattern 'POST-PUSH pg_policies snapshot' -Quiet) -and (Select-String -Path $f -Pattern 'Result:\s*SUCCESS' -Quiet) -and (Select-String -Path $f -Pattern '\*\*Total policies \(arquitetos\):\*\*\s*4' -Quiet) -and (Select-String -Path $f -Pattern '\*\*Total policies \(clientes\):\*\*\s*4' -Quiet)"</automated>
  </verify>
  <acceptance_criteria>
    - `09-PUSH-LOG.md` contains literal heading `## POST-PUSH pg_policies snapshot`
    - `09-PUSH-LOG.md` contains literal heading `## Apply Log`
    - `09-PUSH-LOG.md` contains literal `**Result:** SUCCESS`
    - `09-PUSH-LOG.md` contains literal `**Total policies (arquitetos):** 4`
    - `09-PUSH-LOG.md` contains literal `**Total policies (clientes):** 4`
    - `09-PUSH-LOG.md` contains literal `### DEFAULT verification` and the literal string `auth.uid()` in the column_default column for both tables
    - `09-PUSH-LOG.md` contains literal `### Diff PRE → POST`
    - `09-PUSH-LOG.md` contains literal `- npm run build: exit 0`
    - `09-PUSH-LOG.md` contains literal `- npm run lint: exit 0`
    - SQL query verifies: `SELECT count(*) FROM pg_policies WHERE schemaname='public' AND tablename IN ('arquitetos','clientes')` returns 8
    - SQL query verifies: `SELECT column_default FROM information_schema.columns WHERE table_schema='public' AND table_name='arquitetos' AND column_name='user_id'` returns string starting with `auth.uid`
  </acceptance_criteria>
  <done>Migration aplicada em prod com SUCCESS, POST-PUSH snapshot capturado e diff PRE→POST documentado em 09-PUSH-LOG.md. Build+lint passaram. 8 policies novas confirmadas via SQL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → prod DB (write via MCP owner token) | Apply migration cruza essa boundary; auditoria via Apply Log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Severity | Mitigation Plan |
|-----------|----------|-----------|-------------|----------|-----------------|
| T-09-D01 | T (Tampering) | Migration aplicada parcialmente (não-atômica) | mitigate | HIGH | BEGIN/COMMIT atômico no SQL (D-07); apply_migration MCP roda como single transaction |
| T-09-D02 | I (Information disclosure) | Janela de drop legacy → create new sem RLS | mitigate | HIGH | Atomic transaction garante visibilidade só no COMMIT |
| T-09-D03 | D (Denial of service) | Apply trava prod por lock longo | accept | LOW | DROP/CREATE POLICY em tabelas pequenas (~poucos milhares de linhas) — milissegundos |
| T-09-D04 | R (Repudiation) | Apply sem audit | mitigate | MEDIUM | Apply Log em 09-PUSH-LOG.md + commit no git deixam trace |
| T-09-D05 | E (Elevation) | Rollback necessário e não documentado | accept | LOW | BEGIN/COMMIT — falha == rollback automático; CLI manual com `supabase db reset` se necessário (fora escopo) |
</threat_model>

<verification>
- Migration aplicada em prod
- POST-PUSH = 8 policies, 0 legadas
- DEFAULT auth.uid() confirmado em ambas as colunas
- Build + lint exit 0
</verification>

<success_criteria>
RLS-01 e RLS-02 estruturalmente em vigor em prod. Smoke (09-06) vai confirmar comportamento bilateral. Phase NÃO passa sem essa task — mas smoke ainda precisa rodar pra fechar success criterion #5 do roadmap.
</success_criteria>

<output>
After completion, create `.planning/phases/09-multi-tenancy-rls/09-04-SUMMARY.md` resumindo: status do apply (SUCCESS/FAIL), 8 policies criadas listadas por nome, DEFAULT confirmado em ambas, divergência (se houver) entre PRE e POST além das esperadas.
</output>
