---
phase: 09-multi-tenancy-rls
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - supabase/migrations/20260514000001_arquitetos_clientes_rls.sql
autonomous: true
requirements: [RLS-01, RLS-02]
must_haves:
  truths:
    - "Migration file exists com BEGIN/COMMIT atômico contendo todos os 5 blocos de D-07"
    - "ALTER COLUMN user_id SET DEFAULT auth.uid() está presente para arquitetos e clientes (D-04)"
    - "Os 6 DROP IF EXISTS de policies legadas estão presentes (2 arquitetos + 4 clientes — ajustar conforme divergência 09-02)"
    - "8 CREATE POLICY estão presentes (4 por tabela) replicando padrão Drive Blocos 5/6 literalmente"
    - "ENABLE ROW LEVEL SECURITY idempotente está presente para ambas as tabelas"
    - "COMMENT ON POLICY citando Phase 9 RLS-01/RLS-02 e padrão Drive D-02 está em cada uma das 8 policies"
  artifacts:
    - path: "supabase/migrations/20260514000001_arquitetos_clientes_rls.sql"
      provides: "Migration RLS arquitetos+clientes"
      contains: "CREATE POLICY"
  key_links:
    - from: "20260514000001_arquitetos_clientes_rls.sql"
      to: "supabase/migrations/20260504000001_drive_rls_user_id.sql"
      via: "Padrão literal (Blocos 5+6) replicado com nome da tabela trocado"
      pattern: "Colabs (read|insert|update|delete) own"
---

<objective>
Escrever a migration `supabase/migrations/20260514000001_arquitetos_clientes_rls.sql` que (em BEGIN/COMMIT atômico) define DEFAULT auth.uid() em `user_id`, dropa as policies legadas e cria as 8 policies novas (4 por tabela) replicando 1:1 o pattern Drive Blocos 5 e 6.

Purpose: Esta é a migration de RLS que entrega RLS-01 (clientes) e RLS-02 (arquitetos). Padrão Drive já validado em prod desde Phase 4 (zero regressão em 14 dias). Replicar literalmente reduz risco de divergência sutil.

Output: 1 arquivo SQL pronto para `apply_migration` (09-04 aplica).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-multi-tenancy-rls/09-CONTEXT.md
@.planning/phases/09-multi-tenancy-rls/09-PUSH-LOG.md
@.planning/phases/09-multi-tenancy-rls/09-PREFLIGHT.md
@supabase/migrations/20260504000001_drive_rls_user_id.sql
@supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever migration arquitetos+clientes RLS</name>
  <files>supabase/migrations/20260514000001_arquitetos_clientes_rls.sql</files>
  <read_first>
    - supabase/migrations/20260504000001_drive_rls_user_id.sql (Blocos 5 e 6 — TEMPLATE 1:1 D-01)
    - supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql (carry-over Phase 7; não modificar)
    - .planning/phases/09-multi-tenancy-rls/09-PUSH-LOG.md (lista concreta de DROP IF EXISTS necessários da seção "Divergência com D-02")
    - .planning/phases/09-multi-tenancy-rls/09-CONTEXT.md (D-04..D-08 — DEFAULT, RLS, INSERT strict, BEGIN/COMMIT)
  </read_first>
  <action>
    Criar o arquivo `supabase/migrations/20260514000001_arquitetos_clientes_rls.sql` com EXATAMENTE este conteúdo (substituir `{...}` apenas se 09-PUSH-LOG.md indicar policy não prevista a dropar):

    ```sql
    -- Phase 9: Multi-tenancy RLS em arquitetos + clientes (RLS-01, RLS-02)
    -- Refs:
    --   D-01: pattern Drive Blocos 5+6 (supabase/migrations/20260504000001_drive_rls_user_id.sql) replicado 1:1
    --   D-02: drop das policies legadas "Anyone can read", "Admins can manage", "Authenticated users can ..."
    --   D-03: RLS já enabled em ambas (Phase 7); ENABLE ROW LEVEL SECURITY mantido idempotente
    --   D-04: ALTER COLUMN user_id SET DEFAULT auth.uid() em ambas (defesa contra regressão Phase 7)
    --   D-05: WITH CHECK strict no INSERT (user_id = auth.uid()) — defesa em camadas
    --   D-06: admin NÃO pode criar arquiteto/cliente em nome de outro colab (replica Drive)
    --   D-07: migration ÚNICA, BEGIN/COMMIT atômico
    -- Phase 7 ref: migration 20260511000001_arquitetos_clientes_user_id.sql já adicionou user_id NOT NULL + FK RESTRICT + index.
    -- Phase 8 ref: commit 71d28d7 (08-05) já injeta user_id no payload do dialog; vira redundância segura após D-04.

    BEGIN;

    -- =========================================================================
    -- Bloco 1 — DEFAULT auth.uid() em user_id (D-04)
    -- Cinto-e-suspensórios: callers que esquecerem user_id no payload pegam o uid de quem está logado.
    -- =========================================================================
    ALTER TABLE public.arquitetos ALTER COLUMN user_id SET DEFAULT auth.uid();
    ALTER TABLE public.clientes   ALTER COLUMN user_id SET DEFAULT auth.uid();

    -- =========================================================================
    -- Bloco 2 — DROP das policies legadas (D-02)
    -- Idempotente via IF EXISTS — divergência com 09-PUSH-LOG.md cobre eventuais nomes diferentes.
    -- =========================================================================
    DROP POLICY IF EXISTS "Anyone can read arquitetos" ON public.arquitetos;
    DROP POLICY IF EXISTS "Admins can manage arquitetos" ON public.arquitetos;

    DROP POLICY IF EXISTS "Anyone can read clientes" ON public.clientes;
    DROP POLICY IF EXISTS "Authenticated users can insert clientes" ON public.clientes;
    DROP POLICY IF EXISTS "Authenticated users can update clientes" ON public.clientes;
    DROP POLICY IF EXISTS "Authenticated users can delete clientes" ON public.clientes;
    -- Variantes possíveis (idempotência defensiva):
    DROP POLICY IF EXISTS "Authenticated users can manage clientes" ON public.clientes;
    DROP POLICY IF EXISTS "Admins can manage clientes" ON public.clientes;

    -- =========================================================================
    -- Bloco 3 — ENABLE ROW LEVEL SECURITY (D-03, idempotente)
    -- =========================================================================
    ALTER TABLE public.arquitetos ENABLE ROW LEVEL SECURITY;
    ALTER TABLE public.clientes   ENABLE ROW LEVEL SECURITY;

    -- =========================================================================
    -- Bloco 4 — Policies em arquitetos (RLS-02 / pattern Drive Bloco 5 replicado)
    -- =========================================================================
    CREATE POLICY "Colabs read own arquitetos, admins read all"
      ON public.arquitetos FOR SELECT TO authenticated
      USING (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'));

    CREATE POLICY "Colabs insert own arquitetos"
      ON public.arquitetos FOR INSERT TO authenticated
      WITH CHECK (user_id = auth.uid());

    CREATE POLICY "Colabs update own arquitetos, admins update all"
      ON public.arquitetos FOR UPDATE TO authenticated
      USING (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'))
      WITH CHECK (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'));

    CREATE POLICY "Colabs delete own arquitetos, admins delete all"
      ON public.arquitetos FOR DELETE TO authenticated
      USING (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'));

    COMMENT ON POLICY "Colabs read own arquitetos, admins read all" ON public.arquitetos IS
      'Phase 9 RLS-02 / padrão Drive D-02. Colab vê só os arquitetos que cadastrou; admin vê tudo via has_role.';
    COMMENT ON POLICY "Colabs insert own arquitetos" ON public.arquitetos IS
      'Phase 9 RLS-02 / padrão Drive D-02. WITH CHECK strict: user_id = auth.uid() (admin não pode criar em nome de outro — D-06).';
    COMMENT ON POLICY "Colabs update own arquitetos, admins update all" ON public.arquitetos IS
      'Phase 9 RLS-02 / padrão Drive D-02. Update permitido para dono ou admin.';
    COMMENT ON POLICY "Colabs delete own arquitetos, admins delete all" ON public.arquitetos IS
      'Phase 9 RLS-02 / padrão Drive D-02. Delete permitido para dono ou admin.';

    -- =========================================================================
    -- Bloco 5 — Policies em clientes (RLS-01 / pattern Drive Bloco 6 replicado)
    -- =========================================================================
    CREATE POLICY "Colabs read own clientes, admins read all"
      ON public.clientes FOR SELECT TO authenticated
      USING (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'));

    CREATE POLICY "Colabs insert own clientes"
      ON public.clientes FOR INSERT TO authenticated
      WITH CHECK (user_id = auth.uid());

    CREATE POLICY "Colabs update own clientes, admins update all"
      ON public.clientes FOR UPDATE TO authenticated
      USING (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'))
      WITH CHECK (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'));

    CREATE POLICY "Colabs delete own clientes, admins delete all"
      ON public.clientes FOR DELETE TO authenticated
      USING (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'));

    COMMENT ON POLICY "Colabs read own clientes, admins read all" ON public.clientes IS
      'Phase 9 RLS-01 / padrão Drive D-02. Colab vê só os clientes que cadastrou; admin vê tudo via has_role.';
    COMMENT ON POLICY "Colabs insert own clientes" ON public.clientes IS
      'Phase 9 RLS-01 / padrão Drive D-02. WITH CHECK strict: user_id = auth.uid() (admin não pode criar em nome de outro — D-06).';
    COMMENT ON POLICY "Colabs update own clientes, admins update all" ON public.clientes IS
      'Phase 9 RLS-01 / padrão Drive D-02. Update permitido para dono ou admin.';
    COMMENT ON POLICY "Colabs delete own clientes, admins delete all" ON public.clientes IS
      'Phase 9 RLS-01 / padrão Drive D-02. Delete permitido para dono ou admin.';

    COMMIT;
    ```

    Se 09-PUSH-LOG.md (Divergência com D-02) listar uma policy adicional não prevista (ex: `"Some other legacy policy"`), adicionar um `DROP POLICY IF EXISTS "..." ON public.{tabela};` extra no Bloco 2 antes de gravar.

    Não criar nenhum arquivo de tipos (Supabase types.ts já cobriu colunas user_id na Phase 7 — RLS não muda tipos).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f='supabase\\migrations\\20260514000001_arquitetos_clientes_rls.sql'; (Test-Path $f) -and ((Select-String -Path $f -Pattern 'SET DEFAULT auth.uid' -Quiet) -and (Select-String -Path $f -Pattern 'CREATE POLICY' -Quiet))"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260514000001_arquitetos_clientes_rls.sql` exists
    - File contains literal `BEGIN;` and `COMMIT;` (atomic transaction)
    - File contains literal `ALTER TABLE public.arquitetos ALTER COLUMN user_id SET DEFAULT auth.uid()`
    - File contains literal `ALTER TABLE public.clientes   ALTER COLUMN user_id SET DEFAULT auth.uid()` (or single-space variant)
    - File contains exactly 8 occurrences of `CREATE POLICY` (4 per table)
    - File contains exactly 2 occurrences of `ENABLE ROW LEVEL SECURITY`
    - File contains literal `WITH CHECK (user_id = auth.uid())` at least 2 times (one per INSERT policy)
    - File contains literal `public.has_role(auth.uid(), 'admin')` at least 6 times (SELECT+UPDATE+DELETE per table)
    - File contains at least 6 `DROP POLICY IF EXISTS` lines (D-02 minimum baseline)
    - File contains literal `COMMENT ON POLICY` 8 times (one per policy)
    - Migration file does NOT contain `ALTER COLUMN user_id SET NOT NULL` (Phase 7 já fez)
    - Migration file does NOT contain `ADD COLUMN user_id` (Phase 7 já fez)
  </acceptance_criteria>
  <done>Migration escrita e commitada (NÃO aplicada ainda — 09-04 aplica). Acceptance criteria todos satisfeitos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (Supabase REST) | Requests com JWT cruzam; RLS é o gate |
| anon → DB | Sem policy para anon = bloqueado por default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Severity | Mitigation Plan |
|-----------|----------|-----------|-------------|----------|-----------------|
| T-09-C01 | E (Elevation of privilege) | INSERT com user_id arbitrário | mitigate | HIGH | WITH CHECK strict `user_id = auth.uid()` (D-05); admin não pode criar em nome de outro (D-06) |
| T-09-C02 | I (Information disclosure) | Janela entre DROP e CREATE expondo dados | mitigate | HIGH | BEGIN/COMMIT atômico (D-07) — visibilidade só muda no COMMIT |
| T-09-C03 | T (Tampering) | DEFAULT auth.uid() falha silenciosa em contexto anon | mitigate | MEDIUM | RLS bloqueia INSERT anon (sem policy `to anon`); frontend exige login antes do INSERT |
| T-09-C04 | R (Repudiation) | Admin INSERT cross-user sem audit trail | accept | LOW | D-06 rejeita admin cross-user; transferência via UPDATE deixa trace em updated_at |
| T-09-C05 | I (Information disclosure) | RLS bypassed via SECURITY DEFINER function | accept | LOW | has_role é a única SD function consumida; ela é SECURITY DEFINER e cuidadosamente escrita, validada em Drive |
</threat_model>

<verification>
- Migration file presente e atomicamente delimitado
- Padrão Drive replicado verbatim (mesmos USING/WITH CHECK)
- Acceptance criteria todos passam (grep crosscheck)
</verification>

<success_criteria>
Migration pronta para apply. Replicação literal Drive D-01..D-08 atendida. Threats T-09-C01..C03 mitigados; T-09-C04..C05 aceitos com justificativa.
</success_criteria>

<output>
After completion, create `.planning/phases/09-multi-tenancy-rls/09-03-SUMMARY.md` resumindo: timestamp do filename, contagem de policies criadas (8), contagem de DROP IF EXISTS (≥6), divergência com D-02 absorvida (sim/não).
</output>
