---
phase: 01-schema-prep
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - supabase/migrations/20260423000001_create_arquitetos.sql
  - supabase/migrations/20260423000002_clientes_arquiteto_contato_cpf.sql
  - supabase/migrations/20260423000003_produtos_arquiteto.sql
  - supabase/migrations/20260423000004_colaboradores_cpf_telefone_setor.sql
  - src/integrations/supabase/types.ts
autonomous: true
requirements:
  - ARQ-01
  - ARQ-03
  - ARQ-04
  - ARQ-05
tags: [supabase, migrations, schema, db-push, types]
must_haves:
  truths:
    - "Tabela public.arquitetos existe em producao com colunas id (uuid pk), nome (text not null), contato (text nullable), created_at (timestamptz default now())"
    - "Coluna public.clientes.arquiteto_id (uuid, nullable, FK -> arquitetos com ON DELETE SET NULL) existe em producao"
    - "Coluna public.clientes.contato (text nullable) e public.clientes.cpf_cnpj (text nullable) existem em producao"
    - "Coluna public.produtos.arquiteto_id (uuid, nullable, FK -> arquitetos com ON DELETE SET NULL) existe em producao"
    - "Colunas public.colaboradores.cpf (text nullable), public.colaboradores.telefone (text nullable) e public.colaboradores.setor (text nullable, CHECK constraint allowing comercial/projetos/logistica/financeiro/null) existem em producao"
    - "src/integrations/supabase/types.ts foi regenerado e contem entrada arquitetos: em Tables e os campos novos em clientes/produtos/colaboradores"
    - "RLS habilitado em arquitetos com policy de SELECT permissiva para autenticado e policy FOR ALL restrita a has_role(auth.uid(), 'admin')"
    - "Indices idx_arquitetos_nome, idx_clientes_arquiteto_id e idx_produtos_arquiteto_id existem"
    - "Os 4 commits (1 por migration) e o commit de regen dos types estao no historico de main com mensagens conventional em ingles"
    - "Cada um dos 5 commits foi pushado para origin/main"
  artifacts:
    - path: "supabase/migrations/20260423000001_create_arquitetos.sql"
      provides: "CREATE TABLE arquitetos + RLS + policies + index"
      contains: "CREATE TABLE public.arquitetos"
    - path: "supabase/migrations/20260423000002_clientes_arquiteto_contato_cpf.sql"
      provides: "ALTER TABLE clientes adding arquiteto_id, contato, cpf_cnpj + index"
      contains: "ADD COLUMN arquiteto_id"
    - path: "supabase/migrations/20260423000003_produtos_arquiteto.sql"
      provides: "ALTER TABLE produtos adding arquiteto_id + index"
      contains: "ADD COLUMN arquiteto_id"
    - path: "supabase/migrations/20260423000004_colaboradores_cpf_telefone_setor.sql"
      provides: "ALTER TABLE colaboradores adding cpf, telefone, setor + CHECK constraint"
      contains: "check_colaboradores_setor"
    - path: "src/integrations/supabase/types.ts"
      provides: "Auto-generated TS types reflecting the 4 new migrations"
      contains: "arquitetos:"
  key_links:
    - from: "public.clientes.arquiteto_id"
      to: "public.arquitetos.id"
      via: "FK constraint with ON DELETE SET NULL"
      pattern: "REFERENCES public\\.arquitetos\\(id\\) ON DELETE SET NULL"
    - from: "public.produtos.arquiteto_id"
      to: "public.arquitetos.id"
      via: "FK constraint with ON DELETE SET NULL"
      pattern: "REFERENCES public\\.arquitetos\\(id\\) ON DELETE SET NULL"
    - from: "src/integrations/supabase/types.ts"
      to: "Producao Supabase schema"
      via: "supabase gen types typescript --linked"
      pattern: "arquitetos: \\{"
---

<objective>
Aplicar as 4 migrations aditivas no Supabase de producao (ARQ-01, ARQ-03, ARQ-04, USR-01..03 schema, CLI-01/02 schema), regenerar `src/integrations/supabase/types.ts` e commitar tudo. Este plano fecha 4 dos 5 Success Criteria do ROADMAP Phase 1; o restante (smoke test sem regressao) eh validado em Plan 03.

Justificativa do shape (Plan 02 + Plan 03 = 2 planos novos):
- Plan 02 = trabalho 100% autonomo (escrever SQL, rodar CLI, commit, push). Cabe num bloco coeso.
- Plan 03 = checkpoint:human-verify (Lenny precisa abrir Vercel kappa + admin + Supabase Dashboard pra confirmar zero regressao). Separar evita misturar trabalho autonomo com verificacao humana — se smoke falhar, gap closure eh cirurgico.
- Tudo numa migration unica = perde rollback granular + commits limpos por requirement. Codebase ja tem precedente de migrations separadas.

Purpose: Fase 2 (Cadastros & Arquiteto CRUD), Fase 3 (Produtos & Importacao), Fase 4 (Admin reorg + Drive RLS) e Fase 6 (Filtros) **dependem totalmente** das colunas e tabelas criadas aqui. Sem types.ts regenerado, codigo da Fase 2 nem compila.

Output: 4 arquivos SQL novos em supabase/migrations/, types.ts atualizado, 5 commits no historico de main pushados pra origin, schema de producao com tabela arquitetos + 6 colunas novas distribuidas entre clientes/produtos/colaboradores.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-schema-prep/01-RESEARCH.md
@.planning/phases/01-schema-prep/PREFLIGHT.md
@supabase/migrations/20260319000001_campos_tecnicos_produtos.sql
@supabase/migrations/20260416000001_orcamentos_ambientes_tipo.sql

<interfaces>
<!-- Funcoes/policies/tabelas existentes em prod que estas migrations referenciam.
     Confirmadas em 01-RESEARCH.md (ja em git) e validadas pelo PREFLIGHT.md item 5. -->

Funcao em prod (criada em 20260218165401_*.sql):
```sql
-- Assinatura: public.has_role(_user_id uuid, _role app_role) RETURNS boolean
-- Usada nas policies FOR ALL de arquitetos
public.has_role(auth.uid(), 'admin')
```

Tipo enum em prod (criado em 20260218165401_*.sql):
```sql
-- public.app_role com valores: 'admin', 'user' (e talvez outros — confirmado em prod via PREFLIGHT)
```

Tabelas existentes (todas com RLS habilitado e linhas reais em prod):
- public.clientes — sera estendida com 3 colunas
- public.produtos — sera estendida com 1 coluna
- public.colaboradores — sera estendida com 3 colunas + 1 CHECK constraint

Nenhuma das tabelas existentes recebe NOT NULL/DROP/RENAME nesta fase — todas as colunas novas sao nullable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever as 4 migrations SQL e commitar uma a uma</name>
  <files>
    supabase/migrations/20260423000001_create_arquitetos.sql,
    supabase/migrations/20260423000002_clientes_arquiteto_contato_cpf.sql,
    supabase/migrations/20260423000003_produtos_arquiteto.sql,
    supabase/migrations/20260423000004_colaboradores_cpf_telefone_setor.sql
  </files>
  <read_first>
    - .planning/phases/01-schema-prep/01-RESEARCH.md (secao "Code Examples" — SQL pronto para as 4 migrations; secao "Decisao: Commits e ordem de execucao" — ordem A-E)
    - .planning/phases/01-schema-prep/PREFLIGHT.md (confirmar item 5 has_role=yes; item 4 colaboradores.setor ainda nao existe; item 3 sem gap de migration history)
    - supabase/migrations/20260319000001_campos_tecnicos_produtos.sql (modelo de migration aditiva mais recente — replicar estilo)
    - supabase/migrations/20260416000001_orcamentos_ambientes_tipo.sql (segundo modelo recente — confirmar padrao de COMMENT ON COLUMN)
  </read_first>
  <action>
Escrever os 4 arquivos SQL **exatamente** como abaixo (SQL completo, nao placeholder) e commitar **um por vez** com mensagem conventional em ingles. Nao rodar `supabase db push` ainda — esta task so prepara os artefatos em git.

**Pre-check (antes de escrever qualquer arquivo):**
```bash
cd /c/Users/lenny/Desktop/Luminatti/automa_aura/auraoramentos

# Confirmar que PREFLIGHT.md aponta has_role=yes e setor=does-not-exist
grep -A 1 "Funcao has_role em prod" .planning/phases/01-schema-prep/PREFLIGHT.md
grep -A 1 "colaboradores.setor" .planning/phases/01-schema-prep/PREFLIGHT.md

# Se algum desses falhar, ABORTAR e abrir checkpoint:decision (ver acceptance criteria)
```

**Migration 1 — `supabase/migrations/20260423000001_create_arquitetos.sql`:**
```sql
-- Entidade nova: arquiteto. Cliente origina do arquiteto; produto pode ser "do arquiteto X".
-- Refs: REQUIREMENTS ARQ-01, PROJECT.md "Arquiteto = entidade propria com FK".

CREATE TABLE public.arquitetos (
  id UUID NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY,
  nome TEXT NOT NULL,
  contato TEXT,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now()
);

ALTER TABLE public.arquitetos ENABLE ROW LEVEL SECURITY;

-- Leitura aberta a qualquer autenticado (consistente com clientes/produtos atuais).
CREATE POLICY "Anyone can read arquitetos"
  ON public.arquitetos FOR SELECT
  USING (true);

-- Escrita restrita a admin (mais estrito que clientes/colaboradores — nova entidade,
-- comecamos com a security certa desde o dia 1; CRUD fica em Fase 2).
CREATE POLICY "Admins can manage arquitetos"
  ON public.arquitetos FOR ALL
  TO authenticated
  USING (public.has_role(auth.uid(), 'admin'))
  WITH CHECK (public.has_role(auth.uid(), 'admin'));

-- Index para busca por nome (autocomplete/CRUD admin em Fase 2; volume baixo, btree serve).
CREATE INDEX idx_arquitetos_nome ON public.arquitetos (nome);

COMMENT ON TABLE public.arquitetos IS 'Arquitetos que originam projetos. Cliente e produto podem ter arquiteto_id (nullable, ON DELETE SET NULL).';
```

**Commit 1:**
```bash
git add supabase/migrations/20260423000001_create_arquitetos.sql
git commit -m "feat(db): create arquitetos table (ARQ-01)

- Adds public.arquitetos with id (uuid pk), nome (text not null), contato (text nullable), created_at (timestamptz)
- Enables RLS with permissive SELECT and admin-only FOR ALL via has_role
- Adds idx_arquitetos_nome btree for admin autocomplete (Phase 2)"
git push origin main
```

**Migration 2 — `supabase/migrations/20260423000002_clientes_arquiteto_contato_cpf.sql`:**
```sql
-- Clientes ganham: vinculo com arquiteto (nullable), contato e CPF/CNPJ (opcionais).
-- Refs: REQUIREMENTS ARQ-03 (FK), CLI-01 (contato), CLI-02 (CPF/CNPJ).
-- Todas nullable — registros existentes em prod permanecem validos sem alteracao.

ALTER TABLE public.clientes
  ADD COLUMN arquiteto_id UUID REFERENCES public.arquitetos(id) ON DELETE SET NULL,
  ADD COLUMN contato TEXT,
  ADD COLUMN cpf_cnpj TEXT;

CREATE INDEX idx_clientes_arquiteto_id ON public.clientes (arquiteto_id);

COMMENT ON COLUMN public.clientes.arquiteto_id IS 'Arquiteto que originou este cliente. Nullable: cliente avulso e valido. ON DELETE SET NULL.';
COMMENT ON COLUMN public.clientes.contato IS 'Nome/info de contato do cliente (opcional, texto livre).';
COMMENT ON COLUMN public.clientes.cpf_cnpj IS 'CPF ou CNPJ do cliente. Sem validacao semantica neste marco (REQUIREMENTS out-of-scope).';
```

**Commit 2:**
```bash
git add supabase/migrations/20260423000002_clientes_arquiteto_contato_cpf.sql
git commit -m "feat(db): add arquiteto_id, contato, cpf_cnpj to clientes (ARQ-03, CLI-01, CLI-02 schema)

- ARQ-03: FK arquiteto_id (nullable, ON DELETE SET NULL) + idx_clientes_arquiteto_id (preps Phase 6 FIL-01)
- CLI-01: contato (text nullable, free form)
- CLI-02: cpf_cnpj (text nullable, no semantic validation per REQUIREMENTS)"
git push origin main
```

**Migration 3 — `supabase/migrations/20260423000003_produtos_arquiteto.sql`:**
```sql
-- Produtos ganham vinculo com arquiteto (nullable).
-- Ref: REQUIREMENTS ARQ-04. ARQ-05 (orcamentos via cliente) nao precisa schema change.

ALTER TABLE public.produtos
  ADD COLUMN arquiteto_id UUID REFERENCES public.arquitetos(id) ON DELETE SET NULL;

CREATE INDEX idx_produtos_arquiteto_id ON public.produtos (arquiteto_id);

COMMENT ON COLUMN public.produtos.arquiteto_id IS 'Arquiteto dono do produto (ex: linha exclusiva). Nullable. ON DELETE SET NULL. Backfill manual via admin (PROD-03, Fase 2).';
```

**Commit 3:**
```bash
git add supabase/migrations/20260423000003_produtos_arquiteto.sql
git commit -m "feat(db): add arquiteto_id FK to produtos (ARQ-04)

- Nullable FK to arquitetos with ON DELETE SET NULL
- idx_produtos_arquiteto_id btree (preps Phase 6 FIL-02)
- Existing 90+ products keep arquiteto_id NULL until manual backfill in Phase 2 (PROD-03)"
git push origin main
```

**Migration 4 — `supabase/migrations/20260423000004_colaboradores_cpf_telefone_setor.sql`:**
```sql
-- Colaboradores ganham campos de cadastro expandido: CPF, telefone, setor (enum-like via CHECK).
-- Todos nullable — colaboradores existentes ja logados nao sao bloqueados.
-- Refs: REQUIREMENTS USR-01 (CPF), USR-02 (telefone), USR-03 (setor) — schema apenas;
--       USR-04 back-fill via UI fica para Fase 2.
--
-- Decisao: TEXT + CHECK constraint em vez de CREATE TYPE AS ENUM.
-- Justificativa: padrao dominante do codebase (5 ocorrencias em produtos vs 1 em app_role);
-- setor pode evoluir (Marketing, RH); ALTER TYPE ADD VALUE tem limitacoes em transacao.
-- Ver 01-RESEARCH.md secao "CHECK Constraint vs CREATE TYPE ENUM para `setor`".

ALTER TABLE public.colaboradores
  ADD COLUMN cpf TEXT,
  ADD COLUMN telefone TEXT,
  ADD COLUMN setor TEXT,
  ADD CONSTRAINT check_colaboradores_setor CHECK (
    setor IN ('comercial', 'projetos', 'logistica', 'financeiro') OR setor IS NULL
  );

COMMENT ON COLUMN public.colaboradores.cpf IS 'CPF do colaborador. Validacao algoritmica no app (signup, USR-01). Nullable: colaboradores antigos preenchem via USR-04 sem bloqueio.';
COMMENT ON COLUMN public.colaboradores.telefone IS 'Telefone BR do colaborador. Formato livre (mascara/validacao no app, USR-02). Nullable.';
COMMENT ON COLUMN public.colaboradores.setor IS 'Setor da Luminatti. Valores atuais: comercial, projetos, logistica, financeiro (USR-03). CHECK constraint permite extensao futura via DROP/ADD CONSTRAINT sem ALTER TYPE.';
```

**Commit 4:**
```bash
git add supabase/migrations/20260423000004_colaboradores_cpf_telefone_setor.sql
git commit -m "feat(db): add cpf, telefone, setor to colaboradores (USR-01..03 schema)

- cpf (text nullable): validation in app layer (Phase 2 signup)
- telefone (text nullable): BR phone, masking in app
- setor (text nullable): CHECK constraint on comercial/projetos/logistica/financeiro
- TEXT+CHECK chosen over CREATE TYPE ENUM to allow future sector evolution without ALTER TYPE pain
- Phase 2 USR-04 enables existing collaborators to backfill these fields without being blocked"
git push origin main
```

**Verificacao final desta task:**
```bash
ls supabase/migrations/20260423000001_create_arquitetos.sql
ls supabase/migrations/20260423000002_clientes_arquiteto_contato_cpf.sql
ls supabase/migrations/20260423000003_produtos_arquiteto.sql
ls supabase/migrations/20260423000004_colaboradores_cpf_telefone_setor.sql
git log -4 --oneline    # deve mostrar os 4 commits feat(db) na ordem
```

**Se PREFLIGHT.md indicou has_role=no ou colaboradores.setor ja existia em prod:** PARAR. Abrir um checkpoint:decision com o usuario antes de continuar — Migration 1 e/ou Migration 4 precisam de ajuste (ex: criar has_role inline na migration; ou skip Migration 4 se coluna ja existe). Documentar no SUMMARY e nao tentar consertar sem decisao explicita.
  </action>
  <verify>
    <automated>cd "C:/Users/lenny/Desktop/Luminatti/automa_aura/auraoramentos" && test -f supabase/migrations/20260423000001_create_arquitetos.sql && test -f supabase/migrations/20260423000002_clientes_arquiteto_contato_cpf.sql && test -f supabase/migrations/20260423000003_produtos_arquiteto.sql && test -f supabase/migrations/20260423000004_colaboradores_cpf_telefone_setor.sql && grep -q "CREATE TABLE public.arquitetos" supabase/migrations/20260423000001_create_arquitetos.sql && grep -q "ADD COLUMN arquiteto_id" supabase/migrations/20260423000002_clientes_arquiteto_contato_cpf.sql && grep -q "ADD COLUMN arquiteto_id" supabase/migrations/20260423000003_produtos_arquiteto.sql && grep -q "check_colaboradores_setor" supabase/migrations/20260423000004_colaboradores_cpf_telefone_setor.sql && [ "$(git log --oneline | grep -cE 'feat\(db\):.*(ARQ-01|ARQ-03|ARQ-04|USR-01)')" -ge 4 ]</automated>
  </verify>
  <acceptance_criteria>
    - 4 arquivos SQL existem em supabase/migrations/ com os nomes exatos `20260423000001..04_*.sql`
    - Migration 1 contem `CREATE TABLE public.arquitetos`, `ENABLE ROW LEVEL SECURITY`, ambas as policies (SELECT permissivo + FOR ALL admin), `CREATE INDEX idx_arquitetos_nome`
    - Migration 2 contem 3 `ADD COLUMN` (arquiteto_id, contato, cpf_cnpj), FK com `ON DELETE SET NULL`, `CREATE INDEX idx_clientes_arquiteto_id`
    - Migration 3 contem `ADD COLUMN arquiteto_id`, FK com `ON DELETE SET NULL`, `CREATE INDEX idx_produtos_arquiteto_id`
    - Migration 4 contem 3 `ADD COLUMN` (cpf, telefone, setor) e `CONSTRAINT check_colaboradores_setor` com `OR setor IS NULL`
    - Cada um dos 4 commits tem mensagem comecando com `feat(db):` e referenciando o requirement ID correspondente
    - `git log -4 --oneline` mostra os 4 commits na ordem (1 → 4) sem outros commits intercalados
    - `git status --porcelain` retorna vazio para os 4 arquivos novos (todos commitados)
    - Cada commit foi pushado: `git rev-parse main` == `git rev-parse origin/main`
    - **Bloqueador honrado:** se PREFLIGHT.md indicou has_role=no ou setor ja existe em prod, task PAROU sem criar a migration afetada e abriu pendencia explicita
  </acceptance_criteria>
  <done>4 migrations SQL escritas com conteudo completo (nao placeholder), 4 commits feat(db) no historico de main e pushados para origin. Pronto para Task 2 aplicar via `supabase db push`.</done>
</task>

<task type="auto">
  <name>Task 2: Dry-run, push das 4 migrations, regenerar types e commit</name>
  <files>
    src/integrations/supabase/types.ts,
    .planning/phases/01-schema-prep/PUSH-LOG.md
  </files>
  <read_first>
    - .planning/phases/01-schema-prep/01-RESEARCH.md (secao "Comandos de execucao" — passos 2-5; secao "Common Pitfalls" — Pitfall 1 (gap), 3 (schema flag), 6 (--password))
    - .planning/phases/01-schema-prep/PREFLIGHT.md (confirmar item 3 migration list alinhado; reconciliar antes se gap detectado)
    - supabase/migrations/20260423000001_create_arquitetos.sql (existe da Task 1)
    - src/integrations/supabase/types.ts (estado atual antes do regen — referencia para diff)
  </read_first>
  <action>
Aplicar as 4 migrations em prod via `supabase db push --linked`, regenerar `src/integrations/supabase/types.ts`, validar diff e commitar.

**Pre-check (CRITICO):**
```bash
cd /c/Users/lenny/Desktop/Luminatti/automa_aura/auraoramentos

# 1. Confirmar que estamos no projeto certo
supabase status --linked
# Deve mostrar project_ref jkewlaezvrbuicmncqbj

# 2. Confirmar que PREFLIGHT registrou migration history sem gap
grep -A 5 "## 3. Migration history" .planning/phases/01-schema-prep/PREFLIGHT.md

# 3. Confirmar que SUPABASE_DB_PASSWORD esta acessivel
#    Se nao estiver no env: rodar `export SUPABASE_DB_PASSWORD="<senha>"` em sessao interativa,
#    OU passar via flag --password no comando push (CLI prompta TTY se ausente).
```

**Passo 1 — Dry-run:**
```bash
supabase db push --linked --dry-run
```
Output esperado: lista as 4 migrations `20260423000001..04` como "would be applied". Se aparecer migration que voce nao escreveu nesta task, ABORTAR e abrir checkpoint:decision (Pitfall 1 do RESEARCH).

Salvar output em `.planning/phases/01-schema-prep/PUSH-LOG.md`:
```bash
mkdir -p .planning/phases/01-schema-prep/
{
  echo "# Phase 1 — Migration Push Log"
  echo ""
  echo "## Dry-run output ($(date -Iseconds))"
  echo ""
  echo '```'
  supabase db push --linked --dry-run 2>&1
  echo '```'
} > .planning/phases/01-schema-prep/PUSH-LOG.md
```

**Passo 2 — Push real (UNICO push para as 4 migrations juntas):**
```bash
supabase db push --linked --password "$SUPABASE_DB_PASSWORD" --yes 2>&1 | tee -a .planning/phases/01-schema-prep/PUSH-LOG.md
```

(Se `SUPABASE_DB_PASSWORD` nao estiver no env, rodar sem `--password` — CLI vai prompt TTY. No log, escrever uma linha indicando "password fornecida via prompt interativo".)

Apos sucesso, append no PUSH-LOG.md:
```markdown
## Push real ($(date -Iseconds))
- Status: success
- Migrations aplicadas: 20260423000001, 20260423000002, 20260423000003, 20260423000004
```

**Passo 3 — Verificar via migration list:**
```bash
supabase migration list --linked >> .planning/phases/01-schema-prep/PUSH-LOG.md
```
As 4 migrations devem aparecer agora como aplicadas em remoto e local.

**Passo 4 — Regenerar types.ts (UMA UNICA VEZ, depois de TODAS as migrations):**
```bash
supabase gen types typescript --linked --schema public > src/integrations/supabase/types.ts
```

**Passo 5 — Sanity check do diff de types.ts:**
```bash
git diff --stat src/integrations/supabase/types.ts
# Esperado: ~60-100 linhas adicionadas. Se >300 ou <30, INVESTIGAR.

git diff src/integrations/supabase/types.ts | grep -E "^\+.*arquitetos|^\+.*arquiteto_id|^\+.*cpf|^\+.*telefone|^\+.*setor|^\+.*contato|^\+.*cpf_cnpj" | head -20
# Deve mostrar:
#  - Nova entrada arquitetos: { Row: ..., Insert: ..., Update: ..., Relationships: ... }
#  - Novas linhas arquiteto_id em clientes e produtos
#  - Novas linhas cpf, telefone, setor em colaboradores
#  - Novas linhas contato, cpf_cnpj em clientes
```

Se o diff mostrar **adicoes em schemas alem de public** (auth, storage, etc), INVESTIGAR — o `--schema public` deve ter limitado. Pitfall 3 do RESEARCH.

**Passo 6 — Verificar que typecheck ainda passa (zero regressao no consumo atual):**
```bash
npx tsc --noEmit
# Esperado: zero erros. Codigo atual nao consome as colunas novas, entao adicionar campos
# nas Row interfaces nao quebra nada.
# Se aparecer erro: INVESTIGAR — pode ser que algum componente faz Row<"colaboradores"> com
# desestruturacao estrita, mas isso seria pre-existente. Documentar e abrir checkpoint.
```

**Passo 7 — Commitar tudo:**
```bash
git add src/integrations/supabase/types.ts .planning/phases/01-schema-prep/PUSH-LOG.md
git commit -m "chore(types): regenerate Supabase types after Phase 1 migrations

- Adds arquitetos table type (Row/Insert/Update/Relationships)
- Adds arquiteto_id, contato, cpf_cnpj to clientes types
- Adds arquiteto_id to produtos types
- Adds cpf, telefone, setor to colaboradores types
- PUSH-LOG.md records dry-run + push outputs and migration list state"
git push origin main
```

**Passo 8 — Verificacao final:**
```bash
git log -5 --oneline
# Deve mostrar 5 commits no topo: 4 feat(db) da Task 1 + 1 chore(types) desta task
git status --porcelain
# Deve estar vazio
```

**Se db push falhar:**
- Erro "migration X already applied": rodar `supabase migration repair --status applied <version>` conforme orientacao do output.
- Erro "function has_role does not exist": ABORTAR. Abrir checkpoint:decision — has_role precisava ter sido confirmada no PREFLIGHT (item 5). Pode ser necessario inline a definicao na migration de arquitetos.
- Erro "constraint violation on setor": investigar `SELECT DISTINCT setor FROM colaboradores`. Pitfall 5 do RESEARCH (esperado: coluna nao existia, entao zero violacao).

**NAO** tentar fazer hotfix sem checkpoint — schema em prod com migration parcialmente aplicada eh estado fragil.
  </action>
  <verify>
    <automated>cd "C:/Users/lenny/Desktop/Luminatti/automa_aura/auraoramentos" && test -f .planning/phases/01-schema-prep/PUSH-LOG.md && grep -q "Status: success" .planning/phases/01-schema-prep/PUSH-LOG.md && grep -q "arquitetos:" src/integrations/supabase/types.ts && grep -q "arquiteto_id" src/integrations/supabase/types.ts && grep -E "cpf|telefone|setor" src/integrations/supabase/types.ts | head -3 && npx tsc --noEmit && [ "$(git log --oneline -1 | grep -c 'chore(types)')" -eq 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-schema-prep/PUSH-LOG.md` existe com secoes "Dry-run output" e "Push real" (Status: success)
    - `supabase migration list --linked` (output salvo em PUSH-LOG) mostra as 4 migrations 20260423000001..04 como aplicadas em remoto e local
    - `src/integrations/supabase/types.ts` foi regenerado: contem `arquitetos:` em Tables, `arquiteto_id` em pelo menos 2 lugares (clientes + produtos), `cpf`, `telefone`, `setor` em colaboradores, `contato` e `cpf_cnpj` em clientes
    - `git diff --stat HEAD~1 src/integrations/supabase/types.ts` mostra entre 30 e 300 linhas adicionadas (sanidade — nem trivial nem explosivo)
    - `npx tsc --noEmit` retorna codigo 0 (sem erros novos)
    - Commit `chore(types): regenerate Supabase types after Phase 1 migrations` existe e foi pushado para origin/main
    - `git status --porcelain` vazio ao final
  </acceptance_criteria>
  <done>Schema de prod atualizado com as 4 migrations aplicadas; types.ts em sync; PUSH-LOG.md registra evidencia do push. Phase 1 Success Criteria 2-4 do ROADMAP atendidos. Plan 03 (smoke) pode comecar.</done>
</task>

</tasks>

<verification>
- `supabase migration list --linked` mostra `20260423000001..04` aplicadas em local + remoto
- `src/integrations/supabase/types.ts` foi regenerado e contem entradas para `arquitetos` (Tables), `arquiteto_id`/`contato`/`cpf_cnpj` em `clientes`, `arquiteto_id` em `produtos`, `cpf`/`telefone`/`setor` em `colaboradores`
- `npx tsc --noEmit` passa sem erros novos (zero regressao no codigo existente — nenhum componente atual consome as colunas novas)
- `git log` mostra 5 novos commits no topo (4 feat(db) + 1 chore(types)), todos pushados para origin/main
- `git status --porcelain` retorna vazio
- PUSH-LOG.md registra outputs reais (dry-run + push) para evidencia auditavel
</verification>

<success_criteria>
1. ROADMAP Phase 1 Success Criterion #2 satisfeito: tabela `arquitetos` existe em producao com `id`, `nome`, `contato` (nullable), `created_at`
2. ROADMAP Phase 1 Success Criterion #3 satisfeito: `arquiteto_id` (nullable, FK) em `clientes` e `produtos`
3. ROADMAP Phase 1 Success Criterion #4 satisfeito: `cpf`, `telefone`, `setor` (nullable) em `colaboradores`; `contato` e `cpf_cnpj` em `clientes`
4. ARQ-01, ARQ-03, ARQ-04, ARQ-05 marcados como concluidos (ARQ-05 nao precisou schema change — orcamentos chegam em arquiteto via cliente, conforme RESEARCH)
5. Codigo da Fase 2 podera importar `Database["public"]["Tables"]["arquitetos"]["Row"]` direto de `types.ts` sem nenhum trabalho adicional de tipo
</success_criteria>

<output>
Ao final, criar `.planning/phases/01-schema-prep/01-02-SUMMARY.md` registrando:
- Hashes dos 5 commits criados (4 feat(db) + 1 chore(types))
- Output abreviado do `supabase migration list --linked` final (mostrando as 4 migrations aplicadas)
- Numero de linhas adicionadas em types.ts (sanity number do diff stat)
- Resultado do `npx tsc --noEmit`
- Qualquer reconciliacao de migration history que precisou ser feita (migration repair)
- Confirmacao explicita: "Plan 03 (smoke) liberado para execucao"
</output>
