---
phase: 01-schema-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/.gitignore
  - supabase/.temp/cli-latest
  - supabase/functions/request-access/index.ts
  - supabase/functions/review-access/index.ts
autonomous: false
requirements:
  - PREP-01
tags: [supabase, git, preflight, edge-functions]
must_haves:
  truths:
    - "supabase/config.toml aponta para project_id jkewlaezvrbuicmncqbj e esta commitado"
    - "supabase/.temp/ esta no gitignore e nao aparece mais em git status"
    - "Decisao sobre edge functions (commit ou revert) foi tomada com base no status do dominio Resend e esta registrada no commit"
    - "git status --porcelain retorna vazio ao final do plano"
    - "Nao ha gap entre migration list --linked e ls supabase/migrations/ OU o gap foi reconciliado com migration repair"
  artifacts:
    - path: "supabase/.gitignore"
      provides: "Ignora supabase/.temp/ (estado local do CLI)"
      contains: ".temp/"
    - path: "supabase/config.toml"
      provides: "project_id correto e verify_jwt=false para edge functions"
      contains: "jkewlaezvrbuicmncqbj"
  key_links:
    - from: "supabase/config.toml"
      to: "Supabase projeto jkewlaezvrbuicmncqbj (sa-east-1)"
      via: "project_id ao topo do arquivo"
      pattern: "project_id = \"jkewlaezvrbuicmncqbj\""
---

<objective>
Resolver PREP-01: deixar git limpo antes de qualquer migration. Preflight das 4 Open Questions do RESEARCH.md, decidir o destino dos diffs pendentes (config.toml sempre commit; edge functions condicionado ao status do dominio Resend), e garantir que o estado do historico de migrations em prod esta alinhado com o git.

Purpose: Migrations aplicadas sem git limpo criam deriva permanente entre codigo versionado e estado de prod. Edge functions com dominio nao verificado commitadas + deployadas quebram email de request-access em prod.

Output: git status limpo, config.toml + .gitignore commitados, edge functions commitadas OU mantidas como diff pendente com TODO explicito, decisoes registradas em mensagens de commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-prep/01-RESEARCH.md
@supabase/config.toml
@supabase/functions/request-access/index.ts
@supabase/functions/review-access/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight — validar estado Resend/functions/migrations/colaboradores</name>
  <files>.planning/phases/01-schema-prep/PREFLIGHT.md</files>
  <read_first>
    - .planning/phases/01-schema-prep/01-RESEARCH.md (secao "Open Questions" — 4 perguntas; secao "Assumptions Log" A2/A3/A4)
    - supabase/config.toml (estado atual do arquivo no working tree)
    - supabase/functions/request-access/index.ts (estado atual do diff)
    - supabase/functions/review-access/index.ts (estado atual do diff)
  </read_first>
  <action>
Rodar os 4 preflight checks exigidos pelas Open Questions do RESEARCH.md. Criar `.planning/phases/01-schema-prep/PREFLIGHT.md` documentando os resultados para que as proximas tasks tenham base factual. NAO aplicar nenhuma mudanca de schema ou fazer commits nesta task — so checagem.

Comandos exatos a rodar (executar cada um e gravar o output bruto em PREFLIGHT.md):

```bash
# 1. Supabase CLI versao e link
supabase --version
supabase status --linked

# 2. Listar funcoes deployadas vs local
supabase functions list --linked

# 3. Comparar historico de migrations local vs remoto
supabase migration list --linked

# 4. Sanity check: colaboradores.setor ainda nao existe em prod
#    (expected behavior: erro "column colaboradores.setor does not exist" — isso eh o estado desejado)
#    Rodar via supabase SQL editor OU via:
#    supabase db execute --linked --sql "SELECT DISTINCT setor FROM colaboradores LIMIT 1;"
#    Se rodar via dashboard: anotar manualmente o erro/resultado.

# 5. Verificar se a funcao has_role(uuid, app_role) ja existe em prod (precisamos dela na migration de arquitetos)
#    Supabase Dashboard -> Database -> Functions -> procurar por has_role
#    OU: ver migration 20260218165401_*.sql em supabase/migrations/ para confirmar que foi criada

# 6. Status Resend dominio orcamentosaura.com.br
#    Abrir https://resend.com/domains (Lucas eh owner, Lenny eh teammate — acesso ja configurado por MEMORY.md)
#    Procurar por "orcamentosaura.com.br" na lista de dominios
#    Anotar status: "Verified" | "Pending" | "Not found"
```

Conteudo do PREFLIGHT.md (template — preencher com resultados reais):

```markdown
# Phase 1 Preflight Checks — 2026-04-23

## 1. CLI e link
- supabase --version: <output>
- supabase status --linked output: <paste>
- project_ref esperado: jkewlaezvrbuicmncqbj

## 2. Edge functions deployadas
- supabase functions list --linked output:
  <paste tabela>
- request-access deployado: yes/no
- review-access deployado: yes/no
- Version/updated_at de cada uma: <valor>

## 3. Migration history
- supabase migration list --linked output:
  <paste tabela>
- Migrations em git nao aplicadas em prod: <lista ou "nenhuma">
- Migrations em prod nao em git: <lista ou "nenhuma">
- Decisao: prosseguir direto | reconciliar antes (migration repair)

## 4. Sanity: colaboradores.setor
- Resultado do SELECT DISTINCT setor FROM colaboradores: <erro esperado de coluna nao existe | ou valores atuais>
- Desejado: erro "column does not exist"
- Resultado real: <>

## 5. Funcao has_role em prod
- Existe em prod: yes/no
- Fonte: dashboard / migration 20260218165401 / outro
- Decisao: policy de arquitetos pode usar has_role(auth.uid(), 'admin') sem risco

## 6. Resend dominio orcamentosaura.com.br
- Status: Verified | Pending | Not found
- Registros DNS (SPF/DKIM) validados: yes/no
- Data da verificacao (se Verified): <>

## Decisoes desbloqueadas
- Commit config.toml: SIM (sempre — so atualiza project_id em git)
- Commit edge functions: [SIM se Resend=Verified] | [NAO — manter como diff pendente com TODO]
- Reconciliar migrations antes de Plan 02: yes/no
```
  </action>
  <verify>
    <automated>test -f .planning/phases/01-schema-prep/PREFLIGHT.md &amp;&amp; grep -q "Resend dominio" .planning/phases/01-schema-prep/PREFLIGHT.md &amp;&amp; grep -q "Decisoes desbloqueadas" .planning/phases/01-schema-prep/PREFLIGHT.md</automated>
  </verify>
  <acceptance_criteria>
    - PREFLIGHT.md existe em .planning/phases/01-schema-prep/
    - Arquivo contem as 6 secoes numeradas (CLI, functions, migration history, colaboradores.setor, has_role, Resend)
    - Secao "Decisoes desbloqueadas" foi preenchida com SIM/NAO explicito para cada decisao
    - Outputs dos comandos `supabase status --linked`, `supabase functions list --linked` e `supabase migration list --linked` estao colados no arquivo (nao parafraseados)
  </acceptance_criteria>
  <done>PREFLIGHT.md commitavel e contem dados factuais para decidir o escopo do commit de PREP-01. Nenhuma alteracao em config/functions/migrations ainda.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decidir escopo do commit de PREP-01</name>
  <decision>Commitar edge functions request-access e review-access agora, ou manter como diff pendente com TODO?</decision>
  <context>
Baseado na secao "Decisoes desbloqueadas" do PREFLIGHT.md gerado na Task 1.

Fatos relevantes do RESEARCH.md:
- config.toml diff contem `project_id = "qirsfbypqfeobcnkgspk"` → `"jkewlaezvrbuicmncqbj"` + blocos `[functions.request-access]` e `[functions.review-access]` com verify_jwt=false. Commit deste arquivo eh SEMPRE seguro (so atualiza git com o project_id de prod).
- Edge functions diffs trocam `onboarding@resend.dev` por `noreply@orcamentosaura.com.br`. Se dominio nao estiver verificado no Resend, commit + deploy quebra email de request-access em producao.
- Git rm --cached supabase/.temp/cli-latest + adicionar `supabase/.temp/` ao `.gitignore` eh sempre seguro.

Regras para a decisao:
- Se PREFLIGHT item 6 = Verified → commitar config.toml + functions + .gitignore em UM commit.
- Se PREFLIGHT item 6 = Pending/Not found → commitar APENAS config.toml + .gitignore; DEIXAR os diffs das edge functions como pendentes (nao `git checkout` para nao perder o trabalho) e registrar TODO em PREFLIGHT.md com a pendencia; uma fase de infra separada vai tratar.
  </context>
  <options>
    <option id="option-a">
      <name>Commit tudo (Resend verified)</name>
      <pros>Git 100% limpo. Proximo dev nao se confunde com diff pendente. PREP-01 fechado definitivamente.</pros>
      <cons>Se Resend nao estiver realmente verified, emails de request-access param. Baixo trafico, mas visivel.</cons>
    </option>
    <option id="option-b">
      <name>Commit parcial (Resend pending/not found)</name>
      <pros>Zero risco de quebrar email em prod. Explicita que ha infra pendente.</pros>
      <cons>Git continua com 2 arquivos modificados ate fase de infra. STATE.md precisa registrar isso.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (commit tudo) ou option-b (commit parcial com TODO)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Aplicar git cleanup conforme decisao + commit</name>
  <files>
    supabase/.gitignore,
    supabase/.temp/cli-latest (git rm --cached),
    supabase/config.toml,
    supabase/functions/request-access/index.ts (apenas se option-a),
    supabase/functions/review-access/index.ts (apenas se option-a),
    .planning/phases/01-schema-prep/PREFLIGHT.md
  </files>
  <read_first>
    - .planning/phases/01-schema-prep/PREFLIGHT.md (resultado da decisao e preflight checks)
    - supabase/config.toml (confirmar estado)
    - .planning/phases/01-schema-prep/01-RESEARCH.md (secao "Decisao: Commits e ordem de execucao", Commit A)
  </read_first>
  <action>
Executar o git cleanup com base na decisao da Task 2. Criar supabase/.gitignore, remover .temp/cli-latest do tracking, adicionar config.toml, condicionalmente adicionar edge functions.

Passos concretos:

```bash
cd /c/Users/lenny/Desktop/Luminatti/automa_aura/auraoramentos

# 1. Criar supabase/.gitignore (o arquivo nao existe hoje — verificado no RESEARCH)
cat > supabase/.gitignore <<'EOF'
# Supabase CLI local state
.temp/
.branches/
EOF

# 2. Remover .temp/cli-latest do tracking (fica untracked e depois sera ignorado pelo .gitignore)
git rm --cached supabase/.temp/cli-latest

# 3. Garantir que supabase/.temp/linked-project.json nao virou tracked por engano
git ls-files supabase/.temp/ | grep -q linked-project.json && echo "ERRO: linked-project.json tracked" || echo "ok"

# 4. Stage config.toml (SEMPRE)
git add supabase/config.toml supabase/.gitignore

# 5. Stage edge functions SE E SOMENTE SE decisao foi option-a
# (option-a) => rodar os dois git adds
# (option-b) => pular os dois git adds e adicionar TODO ao final do PREFLIGHT.md documentando o diff pendente

# Opcao A:
git add supabase/functions/request-access/index.ts supabase/functions/review-access/index.ts

# Opcao B (nao rodar o git add acima — em vez disso):
#   Apendar ao PREFLIGHT.md o bloco:
#   ## TODO pendente (post-phase 1)
#   - Commit e deploy das edge functions request-access/review-access quando dominio orcamentosaura.com.br for verificado no Resend
#   - Diffs preservados no working tree (git diff --stat exibe 2 arquivos)

# 6. Stage PREFLIGHT.md (sempre — eh registro permanente)
git add .planning/phases/01-schema-prep/PREFLIGHT.md

# 7. Commit com mensagem conforme opcao
# Opcao A:
git commit -m "chore(supabase): commit pending config + edge functions and ignore .temp/ (PREP-01)

- config.toml: project_id pointing to jkewlaezvrbuicmncqbj (prod) + verify_jwt=false blocks
- functions/request-access + review-access: use noreply@orcamentosaura.com.br (Resend domain verified per PREFLIGHT.md)
- supabase/.gitignore: ignore .temp/ and .branches/ (CLI local state)
- git rm --cached supabase/.temp/cli-latest
- PREFLIGHT.md: documented 6 preflight checks and decision rationale"

# Opcao B:
git commit -m "chore(supabase): commit config + ignore .temp/ (PREP-01, partial)

- config.toml: project_id pointing to jkewlaezvrbuicmncqbj (prod)
- supabase/.gitignore: ignore .temp/ and .branches/ (CLI local state)
- git rm --cached supabase/.temp/cli-latest
- PREFLIGHT.md: documented checks; edge functions diff DEFERRED (Resend domain not yet verified)"

# 8. Push para remote (instrucao global CLAUDE.md: sempre push apos commit)
git push origin main

# 9. Verificar estado final
git status --porcelain
# Expectativa option-a: vazio (ou so a pasta untracked .planning/phases/ se ainda nao commitada)
# Expectativa option-b: 2 linhas ("M supabase/functions/request-access/index.ts", "M supabase/functions/review-access/index.ts")
```

Se `supabase migration list --linked` na Task 1 mostrou gap, adicionar passo extra ANTES do commit:

```bash
# Reconciliacao de migrations (apenas se gap detectado)
supabase migration repair --status applied <version> [--linked]
# OU se prod esta a frente: supabase db pull [--linked] e commitar o diff resultante
```
  </action>
  <verify>
    <automated>cd "C:/Users/lenny/Desktop/Luminatti/automa_aura/auraoramentos" &amp;&amp; grep -q "jkewlaezvrbuicmncqbj" supabase/config.toml &amp;&amp; test -f supabase/.gitignore &amp;&amp; grep -q ".temp" supabase/.gitignore &amp;&amp; ! git ls-files --error-unmatch supabase/.temp/cli-latest 2&gt;/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep project_id supabase/config.toml` retorna `project_id = "jkewlaezvrbuicmncqbj"`
    - `supabase/.gitignore` existe e contem a string `.temp/`
    - `git ls-files supabase/.temp/cli-latest` retorna vazio (arquivo nao mais tracked)
    - Apos commit: `git log -1 --oneline` menciona `PREP-01`
    - Option-a: `git status --porcelain` para os 3 arquivos de supabase (config.toml, functions/request-access, functions/review-access) retorna vazio
    - Option-b: `git status --porcelain` mostra apenas os 2 arquivos de functions como modified; PREFLIGHT.md tem secao TODO pendente
    - `git push origin main` foi executado com sucesso (branch up-to-date)
  </acceptance_criteria>
  <done>PREP-01 concluido. Git no estado acordado pela decisao da Task 2. PREFLIGHT.md registrado no repo. Plan 02 pode comecar.</done>
</task>

</tasks>

<verification>
- `git status --porcelain` apos o plano esta alinhado com a opcao escolhida (option-a: limpo; option-b: so as 2 edge functions pendentes)
- `supabase/config.toml` aponta para o projeto de prod
- `.planning/phases/01-schema-prep/PREFLIGHT.md` documenta o estado factual da infra e a decisao tomada
- Funcao `has_role` confirmada como presente em prod (desbloqueia migration de arquitetos em Plan 02)
- Gap de migration history (se existiu) foi reconciliado ANTES de qualquer push de nova migration
</verification>

<success_criteria>
1. PREP-01 marcada como concluida (REQUIREMENTS.md sera atualizado ao final do marco)
2. Roadmap Phase 1 Success Criteria #1 satisfeita: "git status limpo — edge functions e config.toml commitados OU revertidos com decisao explicita"
3. Zero risco de deploy automatico de edge functions com dominio nao verificado quebrando email em prod
</success_criteria>

<output>
Ao final, criar `.planning/phases/01-schema-prep/01-01-SUMMARY.md` registrando:
- Qual opcao (a ou b) foi escolhida e o motivo factual (estado do Resend)
- Hash do commit de PREP-01
- Resultado do migration list --linked (gap ou alinhado)
- Resultado da checagem has_role em prod
- Qualquer TODO pendente herdado para fases futuras
</output>
