---
phase: 01-schema-prep
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - .planning/phases/01-schema-prep/SMOKE-RESULTS.md
autonomous: false
requirements:
  - ARQ-01
  - ARQ-03
  - ARQ-04
  - ARQ-05
tags: [smoke-test, regression, production, manual-verification]
must_haves:
  truths:
    - "Wizard de orcamento (Step 1 -> Step 2 -> Step 3) navega sem crash em prod com schema novo aplicado"
    - "Listagem de clientes existentes em prod retorna sem TypeError mesmo com colunas arquiteto_id/contato/cpf_cnpj retornando null"
    - "Listagem de produtos existentes em prod retorna sem TypeError mesmo com arquiteto_id null"
    - "Listagem de colaboradores em prod retorna sem TypeError mesmo com cpf/telefone/setor null"
    - "Login de admin e colaborador continua funcionando"
    - "Geracao de PDF a partir de orcamento existente (snapshot antigo) ainda renderiza sem erro"
    - "Supabase Dashboard mostra tabela arquitetos vazia e as 6 colunas novas distribuidas em clientes/produtos/colaboradores"
    - "SMOKE-RESULTS.md commitado com checklist preenchido (PASS/FAIL por item) + screenshots ou output textual onde aplicavel"
  artifacts:
    - path: ".planning/phases/01-schema-prep/SMOKE-RESULTS.md"
      provides: "Evidencia auditavel do smoke test pos-migrations"
      contains: "PASS"
  key_links:
    - from: "Vercel kappa (auraoramentos-kappa.vercel.app)"
      to: "Supabase prod (jkewlaezvrbuicmncqbj)"
      via: "Cliente Supabase JS (queries que agora retornam colunas novas com null)"
      pattern: "VITE_SUPABASE_URL"
---

<objective>
Validar manualmente em producao (Vercel kappa) que as 4 migrations aplicadas no Plan 02 nao quebram o wizard de orcamento, login, admin nem a geracao de PDF de snapshot antigo. Esta eh a verificacao do **5º Success Criterion** do ROADMAP Phase 1: "Wizard de orcamento, login e admin continuam funcionando em producao sem regressao visivel (colunas novas vazias nao quebram render)".

Justificativa do shape (plano dedicado para smoke):
- Plan 02 e autonomo (executa SQL + commit). Plan 03 exige interacao humana (Lenny abre browser, navega, observa). Misturar viola a regra "checkpoint + implementation in same plan".
- Se algum smoke falhar, gap closure cirurgico em vez de re-rodar Plan 02 inteiro.
- Vercel pega o codigo do main automaticamente (ja foi pushado no Plan 02), mas types.ts regenerado nao muda comportamento runtime — ele so afeta typecheck/IntelliSense. Logo, "regressao em prod" so pode vir de: (a) query Supabase que falha por mudanca de schema (improvavel, schema aditivo), (b) bug pre-existente exposto por timing.

Purpose: Caso uma query existente seja sensivel a quantidade ou nome de colunas, ou algum select estrito (`select('id, nome, ...')`) tenha sido escrito de forma fragil, este eh o momento de detectar antes de Fase 2 comecar a usar os campos novos.

Output: SMOKE-RESULTS.md em `.planning/phases/01-schema-prep/` com checklist PASS/FAIL preenchido pelo usuario (Lenny) e commitado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-schema-prep/01-RESEARCH.md
@.planning/phases/01-schema-prep/01-02-SUMMARY.md
@.planning/phases/01-schema-prep/PUSH-LOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gerar SMOKE-RESULTS.md template e rodar checagens automatizadas</name>
  <files>.planning/phases/01-schema-prep/SMOKE-RESULTS.md</files>
  <read_first>
    - .planning/phases/01-schema-prep/01-RESEARCH.md (secao "Smoke Test pos-migration" — 5 itens)
    - .planning/phases/01-schema-prep/01-02-SUMMARY.md (confirmar que Plan 02 fechou com sucesso e listar hashes dos commits)
    - .planning/phases/01-schema-prep/PUSH-LOG.md (confirmar 4 migrations aplicadas)
  </read_first>
  <action>
Criar o esqueleto do SMOKE-RESULTS.md preenchendo as partes que sao automatizadas (typecheck, grep no types.ts, contagem de migrations) e deixando os itens de UI/manual em aberto para a Task 2 (checkpoint:human-verify).

```bash
cd /c/Users/lenny/Desktop/Luminatti/automa_aura/auraoramentos

# Coletar dados automatizados
TYPECHECK_OUTPUT=$(npx tsc --noEmit 2>&1 || echo "FAILED")
ARQUITETOS_GREP=$(grep -c "arquitetos:" src/integrations/supabase/types.ts)
ARQUITETO_ID_GREP=$(grep -c "arquiteto_id" src/integrations/supabase/types.ts)
SETOR_GREP=$(grep -c "setor:" src/integrations/supabase/types.ts)
MIGRATION_FILES=$(ls supabase/migrations/20260423* 2>/dev/null | wc -l)
LAST_5_COMMITS=$(git log -5 --oneline)
```

Escrever `.planning/phases/01-schema-prep/SMOKE-RESULTS.md` com o seguinte conteudo (preencher com os valores reais coletados acima):

```markdown
# Phase 1 — Smoke Test Results

**Date:** <today, formato YYYY-MM-DD>
**Tester:** Lenny Wajcberg
**Environment under test:** auraoramentos-kappa.vercel.app (production) + Supabase Dashboard (jkewlaezvrbuicmncqbj)
**Reference:** ROADMAP Phase 1 Success Criterion #5 — "Wizard de orcamento, login e admin continuam funcionando em producao sem regressao visivel"

## Automated checks (preenchido automaticamente)

| Check | Result |
|-------|--------|
| `npx tsc --noEmit` retorna 0 | <PASS/FAIL — colar primeira linha do output se FAIL> |
| `grep -c "arquitetos:" src/integrations/supabase/types.ts` >= 1 | <numero> |
| `grep -c "arquiteto_id" src/integrations/supabase/types.ts` >= 2 (clientes + produtos) | <numero> |
| `grep -c "setor:" src/integrations/supabase/types.ts` >= 1 | <numero> |
| `ls supabase/migrations/20260423*` retorna 4 arquivos | <numero> |
| Ultimos 5 commits incluem 4 `feat(db):` + 1 `chore(types):` | <colar git log -5 --oneline> |

## Manual checks (preencher na Task 2)

### 1. Wizard de orcamento (Vercel kappa, login como colaborador)

**Steps:**
1. Abrir https://auraoramentos-kappa.vercel.app em aba anonima
2. Login com conta de colaborador (nao admin)
3. Step 1: Selecionar colaborador (dropdown deve listar) e tipo de revisao
4. Step 2: Selecionar cliente existente (dropdown deve listar clientes — agora com colunas novas null)
5. Step 2: Adicionar 1 ambiente, adicionar 1 sistema de iluminacao a partir do catalogo
6. Step 3: Tela de revisao deve carregar, mostrando totais

**Expected:** zero crash; nenhum console error como "Cannot read property X of undefined" ou "TypeError"; lista de clientes/produtos renderiza.

**Result:** [ ] PASS  [ ] FAIL — <descrever falha>

**Console errors observados (se houver):** <colar>

### 2. Login admin + navegacao em abas

**Steps:**
1. Logout do colaborador, login com conta admin
2. Acessar /admin
3. Aba Produtos: lista deve renderizar com produtos existentes (arquiteto_id null em todos)
4. Aba Clientes: lista deve renderizar (contato/cpf_cnpj/arquiteto_id null em todos)
5. Aba Colaboradores: lista deve renderizar (cpf/telefone/setor null em todos)
6. Aba Orcamentos/Pedidos: lista deve renderizar (orcamentos antigos preservados)
7. Aba Excecoes: lista deve renderizar

**Expected:** todas as abas carregam sem crash; nenhum console error.

**Result:** [ ] PASS  [ ] FAIL — <descrever>

**Console errors observados:** <colar>

### 3. Renderizacao de orcamento antigo + geracao de PDF

**Steps:**
1. Como admin (ou colaborador dono), abrir um orcamento ja existente em prod (snapshot persistido antes destas migrations)
2. Conferir Step 3 / visualizacao de detalhe — totais, ambientes, itens
3. Acionar geracao de PDF
4. Abrir o PDF gerado e verificar que cliente, ambientes, sistemas e totais aparecem

**Expected:** orcamento antigo renderiza; PDF abre normalmente; nenhum campo do snapshot reclama de coluna ausente.

**Result:** [ ] PASS  [ ] FAIL — <descrever>

### 4. Supabase Dashboard — Schema visivel

**Steps:**
1. Abrir Supabase Dashboard (projeto jkewlaezvrbuicmncqbj) -> Table Editor
2. Confirmar que tabela `arquitetos` aparece e esta vazia (0 rows)
3. Em `arquitetos`: clicar e ver as 4 colunas (`id`, `nome`, `contato`, `created_at`)
4. Em `clientes`: confirmar que aparecem as colunas `arquiteto_id` (uuid, nullable, FK), `contato` (text, nullable), `cpf_cnpj` (text, nullable)
5. Em `produtos`: confirmar coluna `arquiteto_id` (uuid, nullable, FK)
6. Em `colaboradores`: confirmar colunas `cpf`, `telefone`, `setor` (todas text, todas nullable)
7. Em `colaboradores`: confirmar a CHECK constraint `check_colaboradores_setor` em Database -> Constraints (ou via SQL Editor: `\d colaboradores`)

**Expected:** todos os items confirmados visualmente.

**Result:** [ ] PASS  [ ] FAIL — <colar screenshot ou descrever>

### 5. Edge functions ainda respondem

**Steps:**
1. Em uma aba anonima (sem sessao), acessar a tela de pedido de acesso (request-access flow) em https://auraoramentos-kappa.vercel.app/...
2. Submeter pedido de acesso com email teste
3. Verificar resposta (sem 5xx)

**Expected:** form aceita o submit; sem 500. (Email pode ou nao chegar dependendo da decisao de PREP-01 sobre Resend — registrar separado.)

**Result:** [ ] PASS  [ ] FAIL — <descrever>

**Resend status (se aplicavel):** <Verified | Pending | N/A — depende do PREP-01 option-a vs option-b>

## Overall verdict

- [ ] **All PASS** — Phase 1 fechada, pronto para `/gsd-plan-phase 2`
- [ ] **Falhas detectadas** — registrar abaixo, abrir gap closure plan ou hotfix migration

## Gaps detectados (se houver)

<lista>
- Gap N: <descricao>
  - Componente: <arquivo / tela>
  - Sintoma: <erro / comportamento>
  - Hipotese: <causa>
  - Acao proposta: <hotfix / migration compensatoria / aceitar como conhecido>
</lista>
```

Substituir `<placeholder>` pelos valores reais coletados via shell. Para os items manuais (1-5), deixar `<descrever>` como esta (Lenny preenche na Task 2).

Stage e commitar:
```bash
git add .planning/phases/01-schema-prep/SMOKE-RESULTS.md
git commit -m "docs(01-schema-prep): seed smoke test results template with automated checks"
git push origin main
```
  </action>
  <verify>
    <automated>cd "C:/Users/lenny/Desktop/Luminatti/automa_aura/auraoramentos" && test -f .planning/phases/01-schema-prep/SMOKE-RESULTS.md && grep -q "Automated checks" .planning/phases/01-schema-prep/SMOKE-RESULTS.md && grep -q "Manual checks" .planning/phases/01-schema-prep/SMOKE-RESULTS.md && grep -q "Overall verdict" .planning/phases/01-schema-prep/SMOKE-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `.planning/phases/01-schema-prep/SMOKE-RESULTS.md` existe
    - Contem secoes "Automated checks", "Manual checks" (com 5 sub-secoes numeradas), "Overall verdict" e "Gaps detectados"
    - Os 6 valores de "Automated checks" foram preenchidos com numeros reais (nao com placeholders `<numero>`)
    - Linha de "tsc --noEmit" mostra PASS (typecheck do Plan 02 ja deveria ter passado — duplo check aqui)
    - Commit `docs(01-schema-prep): seed smoke test results template` existe e foi pushado
  </acceptance_criteria>
  <done>SMOKE-RESULTS.md existe com a secao automatizada preenchida; secao manual aguarda Task 2 (checkpoint humano).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test manual em prod (Vercel kappa) e Supabase Dashboard</name>
  <what-built>
    No Plan 02 foram aplicadas 4 migrations aditivas em producao (jkewlaezvrbuicmncqbj):
    - Tabela `arquitetos` criada (vazia)
    - 3 colunas adicionadas em `clientes` (arquiteto_id, contato, cpf_cnpj — todas nullable)
    - 1 coluna adicionada em `produtos` (arquiteto_id — nullable)
    - 3 colunas adicionadas em `colaboradores` (cpf, telefone, setor — todas nullable)
    - `src/integrations/supabase/types.ts` regenerado e pushado para origin/main (Vercel ja redeployou)

    Nenhum codigo de aplicacao foi alterado nesta fase — somente schema + types. Logo nao deveria haver regressao visivel, mas a Task 2 valida isso EM PRODUCAO antes da Fase 2 comecar a usar os campos.
  </what-built>
  <how-to-verify>
    Abrir `.planning/phases/01-schema-prep/SMOKE-RESULTS.md` (criado pela Task 1) e executar os 5 checks manuais documentados nele, marcando PASS/FAIL e colando evidencia (console errors, screenshots em texto, mensagens de erro):

    1. **Wizard de orcamento (5 min)** — login como colaborador em https://auraoramentos-kappa.vercel.app, navegar Step 1 -> Step 2 -> Step 3, criar 1 ambiente com 1 sistema. Sem crash, sem erro de console.

    2. **Admin abas (3 min)** — login como admin, /admin, conferir que abas Produtos, Clientes, Colaboradores, Orcamentos, Excecoes carregam sem erro.

    3. **Orcamento antigo + PDF (3 min)** — abrir 1 orcamento ja existente em prod, conferir Step 3 ou tela de detalhe, gerar PDF, abrir PDF. Sem campos quebrados.

    4. **Supabase Dashboard (3 min)** — abrir https://supabase.com/dashboard/project/jkewlaezvrbuicmncqbj -> Table Editor. Confirmar tabela `arquitetos` (vazia, 4 colunas), e colunas novas em clientes (3) / produtos (1) / colaboradores (3 + CHECK constraint).

    5. **Edge functions (2 min)** — em aba anonima, abrir tela de request-access (se acessivel) e submeter pedido. Sem 500.

    Apos preencher SMOKE-RESULTS.md:
    - Se todos PASS: marcar `[x] All PASS`, fechar com `git add .planning/phases/01-schema-prep/SMOKE-RESULTS.md && git commit -m "docs(01-schema-prep): smoke test PASS — phase 1 closed" && git push origin main`
    - Se houve FAIL: preencher secao "Gaps detectados" com componente, sintoma, hipotese e acao proposta. Commit `docs(01-schema-prep): smoke test detected N gaps`. Em seguida, decidir com Lenny se eh hotfix migration agora (gap closure plan 01-04) ou aceitavel para Fase 2.

    **Atencao:** Resend / email do request-access pode falhar dependendo do que foi decidido em Plan 01 (option-a vs option-b). Isso NAO eh regressao das migrations — registrar como nota separada, nao como FAIL deste smoke.
  </how-to-verify>
  <resume-signal>
    Digite "approved" se todos os 5 itens passaram (e SMOKE-RESULTS.md esta commitado com `[x] All PASS`), ou "gaps: <descricao curta>" se algum item falhou. Em caso de gaps, decidir com o Lenny se abrir `/gsd-plan-phase 1 --gaps` agora ou diferir para Fase 2.
  </resume-signal>
</task>

</tasks>

<verification>
- `.planning/phases/01-schema-prep/SMOKE-RESULTS.md` existe com checklist preenchido (PASS/FAIL por item)
- Secao "Overall verdict" tem uma das duas opcoes marcada
- Se "All PASS": commit final do plano contem mensagem `smoke test PASS — phase 1 closed`
- Se "Falhas detectadas": secao "Gaps detectados" tem ao menos 1 gap descrito com componente + sintoma + hipotese + acao
- Vercel kappa continua acessivel e responsivo (zero 5xx)
</verification>

<success_criteria>
1. ROADMAP Phase 1 Success Criterion #5 satisfeito: "Wizard de orcamento, login e admin continuam funcionando em producao sem regressao visivel (colunas novas vazias nao quebram render)"
2. Phase 1 fecha com 5/5 Success Criteria atendidos (1 a 4 pelos planos 01 e 02; #5 por este plano)
3. Phase 2 desbloqueada — schema novo testado em prod, types.ts em sync, zero regressao conhecida
4. Qualquer gap detectado tem registro auditavel em SMOKE-RESULTS.md com plano de acao explicito
</success_criteria>

<output>
Ao final, criar `.planning/phases/01-schema-prep/01-03-SUMMARY.md` registrando:
- Veredito final do smoke (All PASS ou lista de gaps)
- Data e duracao do smoke manual
- Hashes dos commits criados neste plano (template + final)
- Confirmacao explicita: "Phase 1 fechada — `/gsd-plan-phase 2` liberado" OU "Phase 1 com gaps pendentes — proximo passo: <acao decidida>"
- Se houve gap: numero/descricao curta de cada gap e onde foi tratado (gap closure plan, deferral para fase 2, accept-as-known)
</output>
