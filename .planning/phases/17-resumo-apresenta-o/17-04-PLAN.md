---
phase: 17-resumo-apresenta-o
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Step2Ambientes.tsx
autonomous: false
requirements: [RES-05]
tags: [ui, validation, shadcn, wizard]
must_haves:
  truths:
    - "Ao avançar do Step 2 para o Step 3, se houver itens incompletos (fita sem driver, driver sem fita, perfil sem fita, peça sem lâmpada esperada), um AlertDialog advisory lista cada item e o ambiente onde está"
    - "O advisory é NÃO-bloqueante: o colaborador pode 'Continuar mesmo assim' (avança) ou 'Revisar' (cancela e volta ao Step 2)"
    - "O advisory roda DEPOIS do bloqueio de metragem e da remoção de vazios (Phase 16), operando sobre os ambientes já limpos"
    - "Quando não há itens incompletos, o avanço segue direto sem dialog"
  artifacts:
    - path: "src/components/Step2Ambientes.tsx"
      provides: "Gate handleNext com detecção de incompletos + AlertDialog advisory"
      contains: "Continuar mesmo assim"
  key_links:
    - from: "handleNext (após remoção de vazios)"
      to: "AlertDialog advisory"
      via: "setAdvisoryItems + setAdvisoryOpen quando itensIncompletos.length > 0; AlertDialogAction chama onNext()"
      pattern: "itensIncompletos"
---

<objective>
Adicionar um aviso advisory NÃO-bloqueante ao gate Step 2 → Step 3 (RES-05 / D-12..D-16) que lista sistemas/peças incompletos antes de avançar, reaproveitando o mesmo ponto de saída (`handleNext`) do gate da Phase 16.

Purpose: O colaborador pode avançar para a revisão com um sistema parcialmente preenchido por engano (fita sem driver, peça sem a lâmpada esperada — UAT #4). O aviso torna a inconsistência visível sem bloquear casos legítimos (ex.: cliente fornece o próprio driver). Distinto do bloqueio de metragem da Phase 16 (erro estrutural → bloqueia); aqui é advisory.
Output: `Step2Ambientes.tsx` com detecção de 4 gatilhos + AlertDialog advisory pendurado no `handleNext` existente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-resumo-apresenta-o/17-CONTEXT.md
@.planning/phases/17-resumo-apresenta-o/17-RESEARCH.md
@.planning/phases/16-c-lculo-metragem/16-CONTEXT.md

<interfaces>
<!-- Contratos confirmados no código real. -->

Gate ATUAL em src/components/Step2Ambientes.tsx:34-83 (handleNext), ordem após Phase 16:
```
1. ambientes.length === 0 → toast.error, return            [bloqueio]
2. CALC-01: sistemasInvalidos (fita sem perfil, metragem null/0) → toast.error, return  [bloqueio]
3. ambientesLimpos = ambientes sem sistemas totalmente vazios (!fita.codigo && !driver.codigo && !perfil)
4. se removidos > 0 → onChange(ambientesLimpos) + toast.info  [remoção, não bloqueia]
5. onNext()
```
O componente é function component sem useState hoje. Recebe props { ambientes, onChange, onNext, onPrev }. Importa `toast` de sonner.

Tipos (src/types/orcamento.ts):
- `SistemaIluminacao`: { fita: ItemFitaLED (com .codigo, .descricao), driver: ItemDriver (com .codigo, .descricao), perfil?: ItemPerfil (com .descricao), local?, ... }
- `Ambiente`: { nome: string, luminarias: ItemLuminaria[], sistemas: SistemaIluminacao[] }
- `ItemLuminaria`: tem `.descricao` (e possivelmente `.tipo_produto`)

Predicado-fonte para "espera lâmpada" — Regra #24 em AmbienteCard.tsx:104-108 (REUTILIZAR, NÃO inventar nova heurística):
```typescript
const temBaseLampada = /\b(GU10|E27|MR11|MR16|AR70|AR111|PAR20|PAR30|DICROICA|DICRO)\b/.test(d);
const temLedIntegrado = /LED\s+INTEGRADO|COM\s+LED/.test(d);
// "espera lâmpada" = temBaseLampada && !temLedIntegrado  (d = descricao em UPPERCASE)
```

AlertDialog disponível: `@/components/ui/alert-dialog` (padrão em DriveExplorer.tsx:566-579). Botões nativos: AlertDialogCancel + AlertDialogAction.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Predicado de detecção + lista de itens incompletos no handleNext</name>
  <read_first>
    - src/components/Step2Ambientes.tsx:34-83 (handleNext atual — gate da Phase 16)
    - src/components/AmbienteCard.tsx:104-108 (Regra #24 — predicado temBaseLampada/temLedIntegrado a reutilizar)
    - src/types/orcamento.ts (SistemaIluminacao, Ambiente, ItemLuminaria — campos .codigo/.descricao/.perfil)
    - .planning/phases/17-resumo-apresenta-o/17-RESEARCH.md (seção "D-15 CHECKPOINT — Regra de detecção", Caminho A recomendado; Code Example "Advisory RES-05")
  </read_first>
  <action>
Em `src/components/Step2Ambientes.tsx`, adicionar a lógica de detecção dos 4 gatilhos (D-14) e calcular a lista de incompletos DENTRO de `handleNext`, na ordem de severidade correta (D-16: bloqueio metragem → remoção de vazios → advisory), operando sobre `ambientesLimpos` (pós-remoção de vazios).

1. No topo do arquivo (acima do componente), adicionar o tipo e os predicados de detecção (Caminho A — regex síncrono, reutilizando o predicado da Regra #24; D-15 resolvido pela pesquisa):

```typescript
interface AdvisoryItem {
  ambienteNome: string;
  tipo: 'fita-sem-driver' | 'driver-sem-fita' | 'perfil-sem-fita' | 'peca-sem-lampada';
  descricao: string;
}

// Reutiliza o predicado da Regra #24 (AmbienteCard.tsx:104-108)
function luminariaPrecisaLampada(descricao: string): boolean {
  const d = (descricao ?? '').toUpperCase();
  const temBaseLampada = /\b(GU10|E27|MR11|MR16|AR70|AR111|PAR20|PAR30|DICROICA|DICRO)\b/.test(d);
  const temLedIntegrado = /LED\s+INTEGRADO|COM\s+LED/.test(d);
  return temBaseLampada && !temLedIntegrado;
}

function ambienteTemLampada(amb: Ambiente): boolean {
  return amb.luminarias.some(
    (l) => /l[âa]mpada/i.test(l.descricao ?? '') || (l as any).tipo_produto === 'lampada'
  );
}
```

2. Dentro de `handleNext`, DEPOIS do bloco de remoção de vazios (após L80, antes de `onNext()` em L82), montar a lista de incompletos a partir de `ambientesLimpos` (D-16 — opera sobre os ambientes já limpos, Pitfall 5):

```typescript
const itensIncompletos: AdvisoryItem[] = [];
for (const amb of ambientesLimpos) {
  for (const sis of amb.sistemas) {
    if (sis.fita.codigo && !sis.driver.codigo) {
      itensIncompletos.push({ ambienteNome: amb.nome, tipo: 'fita-sem-driver', descricao: sis.fita.descricao });
    }
    if (sis.driver.codigo && !sis.fita.codigo) {
      itensIncompletos.push({ ambienteNome: amb.nome, tipo: 'driver-sem-fita', descricao: sis.driver.descricao });
    }
    if (sis.perfil && !sis.fita.codigo) {
      itensIncompletos.push({ ambienteNome: amb.nome, tipo: 'perfil-sem-fita', descricao: sis.perfil.descricao });
    }
  }
  if (!ambienteTemLampada(amb)) {
    for (const lum of amb.luminarias) {
      if (luminariaPrecisaLampada(lum.descricao)) {
        itensIncompletos.push({ ambienteNome: amb.nome, tipo: 'peca-sem-lampada', descricao: lum.descricao });
      }
    }
  }
}

if (itensIncompletos.length > 0) {
  setAdvisoryItems(itensIncompletos);
  setAdvisoryOpen(true);
  return; // aguarda decisão do usuário (não-bloqueante — D-12)
}

onNext();
```

NÃO remover nem reordenar os bloqueios da Phase 16 (metragem) nem a remoção de vazios — apenas inserir o passo advisory ANTES do `onNext()` final. A supressão por ambiente (`!ambienteTemLampada`) segue a recomendação de menor ruído (Open Question 3 do RESEARCH).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Step2Ambientes.tsx` contém `interface AdvisoryItem`
    - `src/components/Step2Ambientes.tsx` contém o regex `GU10|E27|MR11|MR16|AR70|AR111|PAR20|PAR30|DICROICA|DICRO`
    - `src/components/Step2Ambientes.tsx` contém os 4 tipos: `fita-sem-driver`, `driver-sem-fita`, `perfil-sem-fita`, `peca-sem-lampada`
    - `src/components/Step2Ambientes.tsx` contém `itensIncompletos` e o early-return que chama `setAdvisoryOpen(true)`
    - `npx tsc --noEmit -p tsconfig.app.json` sai com código 0
  </acceptance_criteria>
  <done>handleNext detecta os 4 gatilhos sobre ambientesLimpos, na ordem de severidade D-16; abre o advisory quando há incompletos; segue direto quando não há.</done>
</task>

<task type="auto">
  <name>Task 2: AlertDialog advisory não-bloqueante (Revisar / Continuar mesmo assim)</name>
  <read_first>
    - src/components/Step2Ambientes.tsx (estado pós Task 1 — handleNext com itensIncompletos)
    - src/components/DriveExplorer.tsx:566-579 (padrão AlertDialog em uso no projeto)
    - src/components/ui/alert-dialog.tsx (componentes disponíveis)
    - .planning/phases/17-resumo-apresenta-o/17-RESEARCH.md (Code Example "Advisory RES-05 — AlertDialog no gate do Step 2"; Open Question 1)
  </read_first>
  <action>
Adicionar estado e o AlertDialog advisory que apresenta a lista e oferece "Revisar" (cancela) ou "Continuar mesmo assim" (avança). D-13: listar claramente, explicar o que falta, permitir revisar ou prosseguir conscientemente.

1. Imports no topo (junto aos demais `@/components/ui/`):
```tsx
import { useState } from "react";
import {
  AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent,
  AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle,
} from "@/components/ui/alert-dialog";
import { AlertTriangle } from "lucide-react";
```

2. Estado dentro do componente:
```tsx
const [advisoryOpen, setAdvisoryOpen] = useState(false);
const [advisoryItems, setAdvisoryItems] = useState<AdvisoryItem[]>([]);
```

3. Mapa de rótulos legíveis por tipo (copy literal pt-BR):
```tsx
const ADVISORY_LABELS: Record<AdvisoryItem['tipo'], string> = {
  'fita-sem-driver': 'Fita sem driver',
  'driver-sem-fita': 'Driver sem fita',
  'perfil-sem-fita': 'Perfil sem fita LED',
  'peca-sem-lampada': 'Peça sem lâmpada (não tem LED integrado)',
};
```

4. Renderizar o AlertDialog no JSX retornado pelo componente (ex.: ao final, antes do fechamento do container raiz). "Continuar mesmo assim" chama `onNext()`; "Revisar" apenas fecha o dialog (registro visual de que houve aviso — D-13):
```tsx
<AlertDialog open={advisoryOpen} onOpenChange={setAdvisoryOpen}>
  <AlertDialogContent>
    <AlertDialogHeader>
      <AlertDialogTitle className="flex items-center gap-2">
        <AlertTriangle className="h-5 w-5 text-yellow-600" />
        Alguns itens parecem incompletos
      </AlertDialogTitle>
      <AlertDialogDescription>
        Verifique se foi intencional. Você pode revisar ou continuar mesmo assim.
      </AlertDialogDescription>
    </AlertDialogHeader>
    <ul className="max-h-60 overflow-y-auto space-y-2 text-sm">
      {advisoryItems.map((it, i) => (
        <li key={i} className="rounded-md border bg-muted/30 px-3 py-2">
          <span className="font-medium">{it.ambienteNome}</span>
          {' — '}
          <span className="text-muted-foreground">{ADVISORY_LABELS[it.tipo]}</span>
          <div className="text-xs text-muted-foreground truncate">{it.descricao}</div>
        </li>
      ))}
    </ul>
    <AlertDialogFooter>
      <AlertDialogCancel>Revisar</AlertDialogCancel>
      <AlertDialogAction onClick={() => { setAdvisoryOpen(false); onNext(); }}>
        Continuar mesmo assim
      </AlertDialogAction>
    </AlertDialogFooter>
  </AlertDialogContent>
</AlertDialog>
```

O AlertDialog é não-bloqueante por design (D-12): "Continuar mesmo assim" sempre permite avançar. Não adicionar lógica que impeça o avanço. Garantir que `autonomous: false` desta plan reflete o checkpoint humano abaixo.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Step2Ambientes.tsx` importa de `@/components/ui/alert-dialog`
    - `src/components/Step2Ambientes.tsx` contém `Continuar mesmo assim` e `Revisar`
    - `src/components/Step2Ambientes.tsx` contém `onClick={() => { setAdvisoryOpen(false); onNext(); }}` (ou equivalente que chama onNext)
    - `src/components/Step2Ambientes.tsx` contém `ADVISORY_LABELS` com os 4 tipos
    - `npx tsc --noEmit -p tsconfig.app.json` sai com código 0
  </acceptance_criteria>
  <done>AlertDialog lista os incompletos por ambiente, com "Revisar" (cancela) e "Continuar mesmo assim" (chama onNext); não-bloqueante.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação do advisory no gate Step 2 → Step 3</name>
  <action>Checkpoint de verificação humana — sem implementação. O executor pausa e instrui o usuário a validar o advisory no wizard conforme <how-to-verify>.</action>
  <what-built>
    Gate Step 2 → Step 3 agora exibe um AlertDialog advisory (não-bloqueante) listando itens incompletos: fita sem driver, driver sem fita, perfil sem fita, peça sem lâmpada esperada. O colaborador pode "Revisar" (volta ao Step 2) ou "Continuar mesmo assim" (avança).
  </what-built>
  <how-to-verify>
    1. `npm run dev`, montar um ambiente com um sistema só de fita SEM driver (e metragem válida, pra não cair no bloqueio da Phase 16). Clicar "Avançar".
    2. Confirmar que aparece o dialog "Alguns itens parecem incompletos" listando "Fita sem driver" no ambiente correto.
    3. Clicar "Revisar" → o dialog fecha e PERMANECE no Step 2.
    4. Clicar "Avançar" de novo → "Continuar mesmo assim" → AVANÇA para o Step 3.
    5. Testar gatilho de lâmpada: adicionar uma luminária cuja descrição contenha uma base (ex.: "SPOT GU10") SEM nenhuma lâmpada no ambiente → avançar → confirmar item "Peça sem lâmpada". Adicionar uma "LÂMPADA GU10" no mesmo ambiente → avançar → confirmar que o aviso de lâmpada some.
    6. Montar um orçamento totalmente correto (fita+driver+metragem) → avançar → confirmar que NÃO aparece dialog (segue direto).
    7. Console do navegador sem erros JS; o bloqueio de metragem da Phase 16 continua bloqueando quando aplicável (regressão).
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Validação client-side advisory sobre dados do próprio orçamento em memória. Sem auth, rede ou exposição nova. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Denial of Service (UX) | handleNext (gate síncrono) | accept | Detecção é regex síncrono O(n) sobre itens já em memória; sem query assíncrona (Caminho A do RESEARCH evita travamento/duplo-clique do Pitfall 3). |

Nenhum threat de alta severidade introduzido. RLS/auth inalterados.
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.app.json` passa
- `npm run build` sai com código 0
- Checkpoint humano aprovado (4 gatilhos, Revisar/Continuar, supressão por lâmpada, sem dialog quando completo, regressão Phase 16 intacta)
</verification>

<success_criteria>
- Advisory lista os 4 gatilhos (D-14) por ambiente ao avançar (RES-05)
- Não-bloqueante: "Continuar mesmo assim" sempre avança (D-12)
- Roda após bloqueio de metragem e remoção de vazios (D-16); reutiliza o predicado da Regra #24 (D-15)
- Sem itens incompletos → avança direto sem dialog
</success_criteria>

<output>
After completion, create `.planning/phases/17-resumo-apresenta-o/17-04-SUMMARY.md`
</output>
