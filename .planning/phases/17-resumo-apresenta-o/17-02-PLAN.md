---
phase: 17-resumo-apresenta-o
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/components/Step3Revisao.tsx
autonomous: false
requirements: [RES-01, RES-02, RES-03]
tags: [ui, presentation, shadcn, refactor]
must_haves:
  truths:
    - "O Resumo Global de Fitas na tela mostra o breakdown por Ambiente — Local de cada fita (ex.: Sala — Sanca · 12m) abaixo da descrição"
    - "A linha de fita inline no card do ambiente exibe rótulo de referência ao Resumo de Fitas (não mais 'Global →' cru), deixando claro que é referência e não compra"
    - "O bloco Resumo Global de Drivers é colapsável, inicia recolhido e está rotulado como análise interna que não vai ao PDF"
  artifacts:
    - path: "src/components/Step3Revisao.tsx"
      provides: "Resumo de Fitas com LOCAL breakdown; fita inline com rótulo referência; bloco de drivers colapsável"
      contains: "Resumo de Fitas"
  key_links:
    - from: "gruposFita (calcularRolosPorGrupo)"
      to: "Resumo Global de Fitas (tabela)"
      via: "g.localBreakdown mapeado em chips text-xs text-muted-foreground"
      pattern: "localBreakdown"
    - from: "Resumo Global de Drivers header"
      to: "tabela de drivers"
      via: "Collapsible/CollapsibleTrigger/CollapsibleContent defaultOpen={false}"
      pattern: "Collapsible"
---

<objective>
Reorganizar a apresentação do Step 3 (tela) para resolver RES-01 (LOCAL no resumo de fitas), RES-02 (fita não duplicada/confusa) e RES-03 (drivers por ambiente como fonte, bloco global rebaixado a análise interna).

Purpose: O cliente/colaborador precisa enxergar, sem ambiguidade, (1) em quais ambientes/locais a fita é usada, (2) que o Resumo de Fitas é a fonte oficial de compra e a fita no card é só referência, e (3) que os drivers por ambiente são o pedido — o bloco global de drivers é só otimização interna. Consome os campos novos de `GrupoFita` da Plan 01.
Output: `Step3Revisao.tsx` com três mudanças visuais distintas, sem tocar em cálculos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-resumo-apresenta-o/17-CONTEXT.md
@.planning/phases/17-resumo-apresenta-o/17-RESEARCH.md
@.planning/phases/17-resumo-apresenta-o/17-01-SUMMARY.md

<interfaces>
<!-- Contratos que o executor consome. Plan 01 já entregou os campos novos. -->

GrupoFita (após Plan 01, src/types/orcamento.ts):
```typescript
export interface LocalBreakdown { label: string; demanda: number; }
export interface GrupoFita {
  codigo: string; descricao: string; demandaTotal: number;
  metragemRolo: 5 | 10 | 15; precoUnitario: number; precoMinimo: number;
  rolos: { tamanho: number; quantidade: number }[];
  qtdRolosTotal: number; subtotal: number;
  localBreakdown?: LocalBreakdown[];  // Plan 01
  imagemUrl?: string;                 // Plan 01
}
```

Pontos de mudança confirmados em src/components/Step3Revisao.tsx:
- L655: célula da fita inline hoje = `<TableCell ...italic">Global →</TableCell>`
- L713-753: bloco "Resumo Global de Fitas LED" (tabela com colunas Código | Descrição | Demanda | Rolos Sugeridos | Preço Un. | Subtotal); itera `gruposFita.map((g) => ...)` em L732
- L755-802: bloco "Resumo Global de Drivers" (div wrapper com header em L757-761, tabela de `resumoDrivers.map` em L775)

Componente Collapsible disponível: `@/components/ui/collapsible` (CollapsibleTrigger, CollapsibleContent).
Badge já importado/usado no arquivo. Ícones de `lucide-react`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LOCAL breakdown no Resumo Global de Fitas + rótulo referência na fita inline</name>
  <read_first>
    - src/components/Step3Revisao.tsx:636-656 (linha de fita inline — célula "Global →" em L655)
    - src/components/Step3Revisao.tsx:712-753 (bloco Resumo Global de Fitas, map em L732)
    - src/types/orcamento.ts (GrupoFita com localBreakdown? e imagemUrl? — entregue na Plan 01)
    - .planning/phases/17-resumo-apresenta-o/17-RESEARCH.md (seção "Step3Revisao — três mudanças distintas", Mudança 1 e 2)
  </read_first>
  <action>
Duas mudanças no `Step3Revisao.tsx`, ambas display-only (sem tocar cálculo):

**(A) Rótulo referência na fita inline (RES-02 / D-01)** — linha 655:
Hoje a célula é:
```tsx
<TableCell className="text-right text-xs text-muted-foreground italic">Global →</TableCell>
```
Trocar o texto por um rótulo que deixe explícito que a fita está contabilizada no Resumo de Fitas (não tem preço por ambiente). Usar o texto literal: **"incluída no Resumo de Fitas"** (decisão D-01, copy literal do CONTEXT.md). Manter a célula `text-right text-xs text-muted-foreground italic` para preservar o estilo discreto:
```tsx
<TableCell className="text-right text-xs text-muted-foreground italic">incluída no Resumo de Fitas</TableCell>
```

**(B) LOCAL breakdown no Resumo Global de Fitas (RES-01 / D-04/D-05)** — dentro do `gruposFita.map((g) => ...)` (L732-747), na célula de Descrição (L735, `<TableCell>{g.descricao}</TableCell>`):
Expandir a célula de Descrição para mostrar o breakdown por local abaixo do nome (decisão de design recomendada pelo RESEARCH: expandir a célula em vez de adicionar coluna, para não quebrar o layout mobile). Cada entrada de `g.localBreakdown` vira um chip discreto. Formato literal por entrada: `label · {demanda}m` (ex.: "Sala — Sanca · 12m"). Substituir L735 por:
```tsx
<TableCell>
  <div>{g.descricao}</div>
  {g.localBreakdown && g.localBreakdown.length > 0 && (
    <div className="mt-1 flex flex-wrap gap-1">
      {g.localBreakdown.map((lb, i) => (
        <Badge key={i} variant="outline" className="text-[10px] font-normal text-muted-foreground">
          {lb.label} · {lb.demanda}m
        </Badge>
      ))}
    </div>
  )}
</TableCell>
```
Isso responde D-02: o breakdown mostra EM QUAIS ambientes/locais a fita é usada, mantendo a linha única por código (D-03 — agrupamento e otimização de rolos preservados) e o total já visível na coluna Demanda. Guard `g.localBreakdown &&` garante que orçamentos antigos (sem o campo) não quebrem.

NÃO alterar as demais colunas (Demanda, Rolos, Preço, Subtotal) — a otimização de rolos cross-projeto permanece intacta.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Step3Revisao.tsx` contém `incluída no Resumo de Fitas`
    - `src/components/Step3Revisao.tsx` NÃO contém mais `Global →`
    - `src/components/Step3Revisao.tsx` contém `g.localBreakdown` e `lb.label} · {lb.demanda}m`
    - `npx tsc --noEmit -p tsconfig.app.json` sai com código 0
  </acceptance_criteria>
  <done>Fita inline rotulada "incluída no Resumo de Fitas"; Resumo de Fitas exibe chips "Ambiente — Local · Xm" por fita; rolos/subtotal inalterados.</done>
</task>

<task type="auto">
  <name>Task 2: Rebaixar Resumo Global de Drivers a análise interna colapsável</name>
  <read_first>
    - src/components/Step3Revisao.tsx:755-802 (bloco Resumo Global de Drivers — div wrapper, header L757-761, tabela L763-801)
    - src/components/ui/collapsible.tsx (Collapsible, CollapsibleTrigger, CollapsibleContent)
    - .planning/phases/17-resumo-apresenta-o/17-RESEARCH.md (Code Example "Step3Revisao — Collapsible para bloco de drivers", Mudança 3)
  </read_first>
  <action>
Tornar o bloco "Resumo Global de Drivers" (L755-802) colapsável, recolhido por padrão e rotulado como análise interna (RES-03 / D-11). NÃO remover o bloco (o insight de economia cross-ambiente tem valor) e NÃO mexer no cálculo de `resumoDrivers` nem no conteúdo da tabela.

1. Adicionar import no topo do arquivo (junto aos demais imports de `@/components/ui/`):
```tsx
import { Collapsible, CollapsibleTrigger, CollapsibleContent } from "@/components/ui/collapsible";
```
E garantir `ChevronDown` no import de `lucide-react` (adicionar se ainda não estiver lá).

2. Envolver o bloco (de L756 `{resumoDrivers.length > 0 && (` até o fechamento em L802) com `<Collapsible defaultOpen={false}>`. O header atual (div em L757-761) vira o `CollapsibleTrigger`; a tabela (div `p-5` em L762-801) vira o `CollapsibleContent`. Reescrever o header para deixar claro que é análise interna que NÃO vai ao PDF do cliente (D-11):

```tsx
{resumoDrivers.length > 0 && (
  <Collapsible defaultOpen={false}>
    <div className="rounded-xl border bg-card shadow-sm overflow-hidden">
      <CollapsibleTrigger className="w-full text-left">
        <div className="bg-muted/50 px-5 py-3 border-b flex items-center justify-between">
          <div>
            <h3 className="text-sm font-semibold text-muted-foreground flex items-center gap-2">
              Análise de Otimização de Drivers
              <Badge variant="outline" className="text-xs">interno</Badge>
            </h3>
            <p className="text-xs text-muted-foreground">Ferramenta de análise — não aparece no PDF do cliente</p>
          </div>
          <ChevronDown className="h-4 w-4 text-muted-foreground shrink-0" />
        </div>
      </CollapsibleTrigger>
      <CollapsibleContent>
        <div className="p-5">
          {/* tabela atual de resumoDrivers — mover o conteúdo existente das L763-800 para cá, SEM alterar */}
        </div>
      </CollapsibleContent>
    </div>
  </Collapsible>
)}
```

Mover o conteúdo INTERNO atual (a `<Table>` de drivers + o `<p>` explicativo das L796-799) intacto para dentro do `<CollapsibleContent><div className="p-5">`. Trocar o título antigo "Resumo Global de Drivers" pelo novo "Análise de Otimização de Drivers" e remover a aparência de bloco-de-compra (fundo `bg-accent/30` → `bg-muted/50`, título de `text-lg`/`text-foreground` → `text-sm`/`text-muted-foreground`) para que não compita visualmente com os drivers por ambiente (que são a fonte oficial, D-10). Estado inicial recolhido (`defaultOpen={false}`).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Step3Revisao.tsx` contém `import { Collapsible, CollapsibleTrigger, CollapsibleContent } from "@/components/ui/collapsible"`
    - `src/components/Step3Revisao.tsx` contém `Análise de Otimização de Drivers`
    - `src/components/Step3Revisao.tsx` contém `defaultOpen={false}`
    - `src/components/Step3Revisao.tsx` NÃO contém mais o título `Resumo Global de Drivers`
    - `npx tsc --noEmit -p tsconfig.app.json` sai com código 0
  </acceptance_criteria>
  <done>Bloco de drivers global é Collapsible recolhido por padrão, rotulado "interno"/"não aparece no PDF", visualmente secundário; cálculo e tabela inalterados.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual do Step 3 (LOCAL, referência, colapso de drivers)</name>
  <action>Checkpoint de verificação humana — sem implementação. O executor pausa e instrui o usuário a validar visualmente no navegador conforme <how-to-verify>.</action>
  <what-built>
    Step 3 reorganizado: (1) chips "Ambiente — Local · Xm" no Resumo Global de Fitas; (2) rótulo "incluída no Resumo de Fitas" na fita dentro do card do ambiente; (3) bloco de drivers global virou "Análise de Otimização de Drivers" colapsável, recolhido, marcado como interno.
  </what-built>
  <how-to-verify>
    1. `npm run dev` e abrir o app (http://localhost:8080).
    2. Montar um orçamento com 2 ambientes usando a MESMA fita LED em locais diferentes (ex.: Ambiente "Sala" com local "Sanca" e Ambiente "Cozinha" com local "Marcenaria"). Avançar até o Step 3.
    3. No card de cada ambiente, confirmar que a linha da Fita mostra "incluída no Resumo de Fitas" (não "Global →") e sem preço por ambiente.
    4. No "Resumo Global de Fitas LED", confirmar que abaixo da descrição da fita aparecem chips tipo "Sala — Sanca · Xm" e "Cozinha — Marcenaria · Ym", e que a Demanda total e os Rolos Sugeridos continuam corretos (otimização cross-projeto intacta).
    5. Confirmar que o bloco "Análise de Otimização de Drivers" aparece recolhido, com badge "interno", e expande ao clicar.
    6. Conferir o console do navegador: sem erros JS.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Mudança puramente de apresentação client-side (relabeling, chips, colapso) sobre dados que o colaborador já possui. Sem auth, rede ou exposição nova. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02 | Information Disclosure | Resumo de Fitas (tela) | accept | Breakdown por local expõe apenas dados do próprio orçamento ao colaborador que o montou; nenhum dado cross-tenant é introduzido. RLS inalterado. |

Nenhum threat de alta severidade introduzido. RLS/auth inalterados.
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.app.json` passa
- `npm run build` sai com código 0
- Checkpoint humano aprovado (chips LOCAL, rótulo referência, colapso de drivers, console limpo)
</verification>

<success_criteria>
- Resumo de Fitas mostra breakdown "Ambiente — Local · Xm" por fita (RES-01)
- Fita inline rotulada "incluída no Resumo de Fitas" — sem duplicação confusa (RES-02)
- Bloco global de drivers rebaixado a análise interna colapsável, drivers por ambiente seguem como fonte (RES-03)
- Nenhum cálculo alterado; otimização de rolos preservada
</success_criteria>

<output>
After completion, create `.planning/phases/17-resumo-apresenta-o/17-02-SUMMARY.md`
</output>
