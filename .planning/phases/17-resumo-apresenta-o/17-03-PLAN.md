---
phase: 17-resumo-apresenta-o
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/lib/pdfTemplates/v2.ts
autonomous: false
requirements: [RES-01, RES-02]
tags: [pdf, presentation]
must_haves:
  truths:
    - "O Resumo de Fitas do PDF (blocoResumoFitas) exibe a foto/thumbnail da fita quando ela tem imagemUrl, e o placeholder vazio quando não tem"
    - "Cada linha de fita no Resumo de Fitas do PDF mostra chips de breakdown por Ambiente — Local (ex.: Sala — Sanca · 12m)"
    - "A função rowFita (fita inline por sistema) permanece intocada — só blocoResumoFitas muda"
    - "PDFs de orçamentos antigos (fitas sem imagemUrl) renderizam sem quebrar, usando o placeholder thumb-empty"
  artifacts:
    - path: "src/lib/pdfTemplates/v2.ts"
      provides: "blocoResumoFitas com foto (thumb) e chips de LOCAL breakdown"
      contains: "thumb(g.imagemUrl)"
  key_links:
    - from: "calcularRolosPorGrupo (GrupoFita.imagemUrl + localBreakdown)"
      to: "blocoResumoFitas"
      via: "thumb(g.imagemUrl) na thumb-cell + chip() por entrada de localBreakdown"
      pattern: "thumb\\(g\\.imagemUrl\\)"
---

<objective>
Atualizar SOMENTE o `blocoResumoFitas` do PDF v2 para exibir a foto da fita (D-08, fold do TODO) e o breakdown por Ambiente — Local (RES-01 / D-07), preservando o layout v2 aprovado e mantendo `rowFita` (fita inline por sistema) intocada.

Purpose: O Resumo de Fitas do PDF é a fonte oficial de compra para o cliente. Adicionar a foto e o LOCAL torna o resumo mais legível e explicável sem mexer significativamente no layout aprovado (conservadorismo D-07/D-09). Consome os campos `imagemUrl` e `localBreakdown` de `GrupoFita` entregues pela Plan 01.
Output: `blocoResumoFitas` em `v2.ts` com thumbnail real + chips de LOCAL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-resumo-apresenta-o/17-CONTEXT.md
@.planning/phases/17-resumo-apresenta-o/17-RESEARCH.md
@.planning/phases/17-resumo-apresenta-o/17-01-SUMMARY.md

<interfaces>
<!-- Contratos consumidos. Plan 01 entregou imagemUrl + localBreakdown em GrupoFita. -->

GrupoFita (após Plan 01): inclui `imagemUrl?: string` e `localBreakdown?: { label: string; demanda: number }[]`.

Helpers já presentes em src/lib/pdfTemplates/v2.ts:
- `thumb(url: string | undefined): string` (L65) — renderiza `<img class="thumb">` ou `<div class="thumb-empty">` quando url ausente. Já tem fallback embutido.
- `chip(text: string, variant?: "orange"): string` (L60) — span estilizado; variante default = cinza neutro.
- `esc(...)` — escape HTML.

Estado ATUAL de blocoResumoFitas (src/lib/pdfTemplates/v2.ts:266-296):
```typescript
const rows = grupos.map(g => {
  const rolosStr = g.rolos.map(r => `${r.quantidade}×${r.tamanho}m`).join(" + ");
  const chipsHtml = [chip(`${g.demandaTotal}m demanda`, "orange"), chip(rolosStr), chip(`${g.qtdRolosTotal} rolos`)].join("");
  return `
    <tr class="item-row">
      <td class="thumb-cell"><div class="thumb-empty"></div></td>
      <td class="desc-cell">
        <div class="desc-name">${esc(g.descricao)}</div>
        <div class="chips">${chipsHtml}</div>
      </td>
      ...`;
});
```

`rowFita` está em v2.ts:127 — NÃO TOCAR (D-07; Pitfall 2 do RESEARCH).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Foto da fita + LOCAL breakdown no blocoResumoFitas do PDF</name>
  <read_first>
    - src/lib/pdfTemplates/v2.ts:265-296 (blocoResumoFitas — função inteira)
    - src/lib/pdfTemplates/v2.ts:60-68 (helpers chip e thumb)
    - src/types/orcamento.ts (GrupoFita com imagemUrl? e localBreakdown? — entregue na Plan 01)
    - .planning/phases/17-resumo-apresenta-o/17-RESEARCH.md (Code Example "blocoResumoFitas no PDF v2 — foto + LOCAL"; Pitfalls 2 e 6)
  </read_first>
  <action>
Editar APENAS a função `blocoResumoFitas` (`src/lib/pdfTemplates/v2.ts:266`). Duas mudanças dentro do `grupos.map(g => ...)`:

**(A) Foto da fita (D-08)** — substituir a thumb-cell fixa (L275):
De:
```html
<td class="thumb-cell"><div class="thumb-empty"></div></td>
```
Para usar o helper `thumb()` com a `imagemUrl` do grupo:
```html
<td class="thumb-cell">${thumb(g.imagemUrl)}</td>
```
`thumb()` já trata `undefined` retornando o `<div class="thumb-empty">` (Pitfall 6 — orçamentos antigos sem imagemUrl renderizam o placeholder, sem guard adicional).

**(B) LOCAL breakdown (RES-01 / D-04/D-05)** — adicionar chips de local após os chips existentes (L272). Construir os chips de local a partir de `g.localBreakdown` (formato literal por entrada: `label · demanda` + "m", ex.: "Sala — Sanca · 12m") e concatenar ao `chipsHtml`:
```typescript
const localChips = (g.localBreakdown ?? [])
  .map(lb => chip(`${lb.label} · ${lb.demanda}m`))
  .join("");
const chipsHtml = [
  chip(`${g.demandaTotal}m demanda`, "orange"),
  chip(rolosStr),
  chip(`${g.qtdRolosTotal} rolos`),
  localChips,
].filter(Boolean).join("");
```
`chip()` já faz `esc()` no texto internamente — não duplicar escape. O `(g.localBreakdown ?? [])` garante segurança em orçamentos antigos sem o campo.

NÃO alterar `rowFita` (v2.ts:127) nem qualquer outra função (D-07 — fita inline por sistema permanece como está; Pitfall 2). NÃO mexer no cálculo de `totalFitas`, nas demais células (qty, watts, price, sku, subtotal) nem na estrutura `<section>`/`<table>` do bloco.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/pdfTemplates/v2.ts` contém `thumb(g.imagemUrl)`
    - `src/lib/pdfTemplates/v2.ts` contém `g.localBreakdown ?? []` e `chip(`${lb.label} · ${lb.demanda}m`)`
    - `src/lib/pdfTemplates/v2.ts` NÃO contém mais `<td class="thumb-cell"><div class="thumb-empty"></div></td>` (substituído por thumb())
    - A função `rowFita` permanece inalterada (diff toca apenas blocoResumoFitas)
    - `npx tsc --noEmit -p tsconfig.app.json` sai com código 0
  </acceptance_criteria>
  <done>blocoResumoFitas usa thumb(g.imagemUrl) e chips de LOCAL; rowFita intocado; orçamentos sem imagemUrl renderizam o placeholder.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação visual do Resumo de Fitas no PDF (foto + LOCAL)</name>
  <action>Checkpoint de verificação humana — sem implementação. O executor pausa e instrui o usuário a validar o PDF gerado conforme <how-to-verify>.</action>
  <what-built>
    Resumo de Fitas do PDF v2 agora mostra a foto da fita (ou placeholder vazio) e chips de breakdown por "Ambiente — Local". A fita inline por sistema (rowFita) ficou exatamente como estava.
  </what-built>
  <how-to-verify>
    1. `npm run dev`, montar um orçamento com a mesma fita em 2 locais distintos (ex.: Sala — Sanca, Cozinha — Marcenaria) e onde a fita tenha imagem cadastrada.
    2. No Step 3, gerar o PDF (template v2). Abrir o PDF gerado.
    3. Na seção "RESUMO DE FITAS": confirmar que aparece a foto da fita à esquerda e chips "Sala — Sanca · 12m" / "Cozinha — Marcenaria · 8m" junto dos chips de demanda/rolos.
    4. Confirmar que o restante do PDF (fita inline dentro de cada sistema/ambiente, layout geral, total) está visualmente idêntico ao anterior.
    5. Gerar um PDF de um orçamento ANTIGO (fita sem imagem): confirmar que o placeholder vazio aparece no lugar da foto, sem quebrar o layout.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imagemUrl → `<img src>` no HTML do PDF | A URL da imagem da fita é embutida no HTML gerado client-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Injection (XSS) | thumb()/blocoResumoFitas | mitigate | `thumb()` já aplica `esc()` na URL antes de inserir em `src` (v2.ts:67); chips de local passam por `chip()` que também faz `esc()`. Nenhuma string entra crua no HTML. |
| T-17-04 | Information Disclosure | foto da fita no PDF | accept | imagemUrl é do catálogo público de produtos que o colaborador já vê; PDF é gerado para o próprio orçamento. Sem dado cross-tenant. |

Nenhum threat de alta severidade introduzido. RLS/auth inalterados.
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.app.json` passa
- `npm run build` sai com código 0
- Checkpoint humano aprovado (foto + chips no Resumo de Fitas; rowFita idêntico; orçamento antigo OK)
</verification>

<success_criteria>
- blocoResumoFitas exibe a foto da fita (thumb) e chips "Ambiente — Local · Xm" (RES-01)
- rowFita (fita inline) intocado — sem duplicação/regressão no PDF (RES-02 / D-07)
- Orçamentos antigos sem imagemUrl renderizam placeholder sem quebrar
</success_criteria>

<output>
After completion, create `.planning/phases/17-resumo-apresenta-o/17-03-SUMMARY.md`
</output>
