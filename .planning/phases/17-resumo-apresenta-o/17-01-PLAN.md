---
phase: 17-resumo-apresenta-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/orcamento.ts
autonomous: true
requirements: [RES-01]
tags: [pdf, presentation, calculation, refactor]
must_haves:
  truths:
    - "calcularRolosPorGrupo retorna, para cada código de fita, um breakdown por Ambiente — Local com a metragem de cada local"
    - "Cada GrupoFita carrega a imagemUrl da fita (ou undefined em fitas antigas sem imagem)"
    - "A soma das demandas do localBreakdown é igual ao demandaTotal do grupo"
    - "Orçamentos antigos sem imagemUrl/local continuam computando rolos exatamente como antes (qtdRolosTotal e subtotal inalterados)"
  artifacts:
    - path: "src/types/orcamento.ts"
      provides: "GrupoFita estendido com localBreakdown? e imagemUrl?; calcularRolosPorGrupo populando ambos"
      contains: "localBreakdown"
  key_links:
    - from: "calcularRolosPorGrupo loop"
      to: "GrupoFita.localBreakdown"
      via: "Map<string, number> de label Ambiente — Local acumulando calcularDemandaFita(sis)"
      pattern: "localBreakdown"
---

<objective>
Estender a camada de dados compartilhada para que o Resumo de Fitas (tela E PDF) consiga exibir o LOCAL de cada fita (breakdown por "Ambiente — Local") e a foto da fita, sem duplicar lógica e sem quebrar o cálculo de rolos existente.

Purpose: `calcularRolosPorGrupo` é a única função consumida tanto pela tela (`Step3Revisao`) quanto pelo PDF (`blocoResumoFitas`). Estendê-la propaga RES-01 (LOCAL no resumo) e a foto da fita (D-08) para os dois consumidores automaticamente, sem código duplicado. É o pré-requisito (upstream) das Plans 02 (tela) e 03 (PDF).
Output: Tipo `GrupoFita` com dois campos opcionais novos (`localBreakdown?`, `imagemUrl?`); um tipo `LocalBreakdown`; `calcularRolosPorGrupo` populando ambos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-resumo-apresenta-o/17-CONTEXT.md
@.planning/phases/17-resumo-apresenta-o/17-RESEARCH.md

<interfaces>
<!-- Contratos relevantes extraídos do código real. O executor usa estes diretamente. -->

Estado ATUAL de GrupoFita (src/types/orcamento.ts:368-378):
```typescript
export interface GrupoFita {
  codigo: string;
  descricao: string;
  demandaTotal: number;
  metragemRolo: 5 | 10 | 15;
  precoUnitario: number;
  precoMinimo: number;
  rolos: { tamanho: number; quantidade: number }[];
  qtdRolosTotal: number;
  subtotal: number;
}
```

Estado ATUAL do Map interno de calcularRolosPorGrupo (src/types/orcamento.ts:381):
```typescript
const grupos = new Map<string, { descricao: string; demanda: number; metragemRolo: 5 | 10 | 15; precoUnitario: number; precoMinimo: number }>();
```

Fontes do label "Ambiente — Local":
- `SistemaIluminacao.local` (sub-área Sanca/Rasgo — opcional)
- `Ambiente.nome`
- `ItemFitaLED.imagemUrl` (já existe, opcional) — fonte de `imagemUrl`

Helper já presente: `calcularDemandaFita(sis)` (usado hoje na linha 387).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender o tipo GrupoFita com localBreakdown e imagemUrl</name>
  <read_first>
    - src/types/orcamento.ts:368-378 (interface GrupoFita atual)
    - src/types/orcamento.ts:64 (ItemFitaLED.imagemUrl — confirmar que existe e é opcional)
    - .planning/phases/17-resumo-apresenta-o/17-RESEARCH.md (bloco "GrupoFita — extensão aditiva", seção Architecture Patterns)
  </read_first>
  <behavior>
    - Compilação TS: `GrupoFita` aceita `localBreakdown` como array de `LocalBreakdown` e `imagemUrl` como string, ambos opcionais.
    - Backward-compat: código existente que constrói `GrupoFita` sem os campos novos continua compilando (campos opcionais).
    - `LocalBreakdown` é exportado e tem exatamente `{ label: string; demanda: number }`.
  </behavior>
  <action>
Em `src/types/orcamento.ts`, imediatamente ANTES da interface `GrupoFita` (linha ~368), adicionar a interface exportada:

```typescript
export interface LocalBreakdown {
  /** Identificador de origem da fita: "Ambiente — Local" (com local) OU "Ambiente" (sem local) */
  label: string;
  /** Metragem de fita demandada nesse Ambiente — Local */
  demanda: number;
}
```

Em seguida, estender a interface `GrupoFita` adicionando DOIS campos opcionais ao final (depois de `subtotal`), de forma ADITIVA (não remover nem reordenar campos existentes):

```typescript
  /** NOVO (Phase 17 / RES-01 D-04/D-05): breakdown da demanda por "Ambiente — Local" */
  localBreakdown?: LocalBreakdown[];
  /** NOVO (Phase 17 / RES-01 D-08): URL da imagem/thumbnail da fita para o Resumo de Fitas do PDF */
  imagemUrl?: string;
```

Os campos DEVEM ser opcionais (`?`) — D-06 exige mudança aditiva que não quebre snapshots nem PDF v1/v2. `GrupoFita` é computado em runtime (nunca serializado em `orcamentos.ambientes`), então a extensão é zero-risco; ainda assim, manter opcional para que orçamentos antigos cujas fitas não têm `imagemUrl` produzam `imagemUrl: undefined` sem erro.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/orcamento.ts` contém `export interface LocalBreakdown`
    - `src/types/orcamento.ts` contém `localBreakdown?: LocalBreakdown[]`
    - `src/types/orcamento.ts` contém `imagemUrl?: string` dentro de `GrupoFita`
    - `npx tsc --noEmit -p tsconfig.app.json` sai com código 0
  </acceptance_criteria>
  <done>GrupoFita estende localBreakdown? e imagemUrl?; LocalBreakdown exportado; typecheck passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Popular localBreakdown e imagemUrl em calcularRolosPorGrupo</name>
  <read_first>
    - src/types/orcamento.ts:380-442 (calcularRolosPorGrupo completo — algoritmo de rolos NÃO muda)
    - src/types/orcamento.ts (calcularDemandaFita — assinatura usada na linha 387)
    - .planning/phases/17-resumo-apresenta-o/17-RESEARCH.md (Code Example "calcularRolosPorGrupo — estrutura interna com breakdown")
  </read_first>
  <behavior>
    - Test 1: para um grupo com 2 sistemas da mesma fita em "Sala — Sanca" (12m) e "Cozinha — Marcenaria" (8m), `localBreakdown` = [{label:"Sala — Sanca", demanda:12}, {label:"Cozinha — Marcenaria", demanda:8}] e demandaTotal = 20.
    - Test 2: sistema sem `local` (local vazio/undefined) num ambiente "Lavabo" → label = "Lavabo" (só o nome do ambiente).
    - Test 3: dois sistemas da mesma fita no MESMO label são SOMADOS numa única entrada de breakdown (não duplicam a linha).
    - Test 4 (backward-compat): com a mesma entrada, `qtdRolosTotal` e `subtotal` são idênticos ao comportamento anterior (algoritmo de rolos intocado).
    - Test 5: fita com `imagemUrl` definido → grupo.imagemUrl reflete o valor; fita sem imagemUrl → grupo.imagemUrl undefined (sem crash).
    - Invariante: soma das demandas em localBreakdown === demandaTotal.
  </behavior>
  <action>
Em `calcularRolosPorGrupo` (`src/types/orcamento.ts:380`), estender o Map interno e o loop SEM alterar o algoritmo de otimização de rolos (D-03: não quebrar agrupamento por código).

1. Estender o tipo do Map (linha 381) para carregar `imagemUrl?: string` e um acumulador de locais `localAcc: Map<string, number>` (label → demanda acumulada):

```typescript
const grupos = new Map<string, {
  descricao: string;
  demanda: number;
  metragemRolo: 5 | 10 | 15;
  precoUnitario: number;
  precoMinimo: number;
  imagemUrl?: string;
  localAcc: Map<string, number>;
}>();
```

2. Dentro do loop `for (const amb of ambientes) { for (const sis of amb.sistemas) {` (linha 383-401), DEPOIS de `const demanda = calcularDemandaFita(sis);`, calcular o label conforme D-05 (formato literal "Ambiente — Local", com travessão "—" cercado por espaços):

```typescript
const label = (sis.local && sis.local.trim())
  ? `${amb.nome} — ${sis.local.trim()}`
  : amb.nome;
```

No ramo `if (existing)`: além de `existing.demanda += demanda;`, acumular no localAcc:
```typescript
existing.localAcc.set(label, (existing.localAcc.get(label) ?? 0) + demanda);
```

No ramo `else` (criação do grupo): incluir `imagemUrl: sis.fita.imagemUrl` e inicializar o localAcc com a primeira entrada:
```typescript
grupos.set(key, {
  descricao: sis.fita.descricao,
  demanda,
  metragemRolo: sis.fita.metragemRolo,
  precoUnitario: sis.fita.precoUnitario,
  precoMinimo: sis.fita.precoMinimo,
  imagemUrl: sis.fita.imagemUrl,
  localAcc: new Map([[label, demanda]]),
});
```

3. Na construção do `resultado` (linha 428, `resultado.push({...})`), adicionar os dois campos novos convertendo o Map em array (Map preserva ordem de inserção, igual ao padrão de `agruparPorLocal`):

```typescript
localBreakdown: Array.from(g.localAcc.entries()).map(([label, demanda]) => ({ label, demanda })),
imagemUrl: g.imagemUrl,
```

NÃO mexer no laço de tamanhos de rolo (15/10/5), no cálculo de `qtdRolosTotal` nem em `subtotal` (D-03 — otimização global de rolos preservada). Sobre Pitfall 4 do RESEARCH: dois ambientes de mesmo nome com mesmo local geram o mesmo label e SERÃO somados — comportamento aceito (display-only, não-bloqueante).
  </action>
  <verify>
    <automated>npm run test -- src/types</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/orcamento.ts` contém `localAcc` (acumulador de locais no Map interno)
    - `src/types/orcamento.ts` contém a string literal ``${amb.nome} — ${sis.local.trim()}`` (label "Ambiente — Local")
    - `src/types/orcamento.ts` contém `imagemUrl: sis.fita.imagemUrl`
    - `calcularRolosPorGrupo` retorna objetos com `localBreakdown` e `imagemUrl` populados
    - `npx tsc --noEmit -p tsconfig.app.json` sai com código 0
    - Teste novo cobrindo Test 1–5 acima passa (`npm run test -- src/types`)
  </acceptance_criteria>
  <done>localBreakdown reflete "Ambiente — Local" com demanda somada por label; imagemUrl carregado da fita; soma do breakdown === demandaTotal; rolos/subtotal inalterados; testes passam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Esta plan altera apenas uma função pura de cálculo client-side sobre dados que o colaborador já possui. Nenhuma entrada não-confiável nova, nenhuma chamada de rede, nenhuma exposição de dados. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | GrupoFita (campo computado) | accept | Tipo computado em runtime, nunca serializado em `orcamentos.ambientes`; extensão aditiva opcional não altera dados persistidos nem RLS. Sem nova superfície. |

Nenhum threat de alta severidade introduzido. RLS/auth inalterados.
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.app.json` passa (0 erros)
- `npm run test -- src/types` passa (incluindo o novo teste de breakdown)
- `npm run build` sai com código 0
- Grep confirma `localBreakdown`, `LocalBreakdown`, `imagemUrl` e o label "Ambiente — Local" em orcamento.ts
</verification>

<success_criteria>
- GrupoFita estende `localBreakdown?: LocalBreakdown[]` e `imagemUrl?: string` (aditivo, opcional)
- `calcularRolosPorGrupo` popula ambos sem alterar o cálculo de rolos/subtotal
- Soma das demandas do breakdown === demandaTotal por grupo
- Orçamentos antigos (fita sem imagemUrl, sistema sem local) computam sem erro
</success_criteria>

<output>
After completion, create `.planning/phases/17-resumo-apresenta-o/17-01-SUMMARY.md`
</output>
