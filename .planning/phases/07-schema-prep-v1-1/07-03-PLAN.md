---
phase: 07-schema-prep-v1-1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260511000003_orcamentos_status_enum.sql
autonomous: true
requirements: [AUTO-03]

must_haves:
  truths:
    - "Tabela orcamentos.status tem CHECK constraint válida com 4 valores: rascunho|aprovado|perdido|pendente (após push do Plan 07-04)"
    - "Linhas com status='fechado' foram migradas in-place para 'aprovado' (UPDATE SET status='aprovado' WHERE status='fechado')"
    - "DEFAULT 'rascunho' é mantido (zero mudança no comportamento de criação)"
    - "Pre-flight assert garante que NÃO existem valores fora do enum antes de aplicar a CHECK (defesa contra dados inesperados)"
    - "Tipo TS em src/types/orcamento.ts:109 fica desatualizado de propósito — Phase 10 sincroniza"
  artifacts:
    - path: "supabase/migrations/20260511000003_orcamentos_status_enum.sql"
      provides: "UPDATE fechado→aprovado + ADD CHECK constraint + pre-flight assert"
      contains: "CHECK (status IN ('rascunho','aprovado','perdido','pendente'))"
  key_links:
    - from: "20260511000003_orcamentos_status_enum.sql"
      to: "Phase 10 (WIZ-04 status badges)"
      via: "Schema com 4 valores enforce via CHECK"
      pattern: "status IN \\('rascunho','aprovado','perdido','pendente'\\)"
---

<objective>
Criar migration aditiva `20260511000003_orcamentos_status_enum.sql` que: (1) atualiza dado in-place de `'fechado'` → `'aprovado'`, (2) adiciona CHECK constraint enforçando enum de 4 valores (`rascunho|aprovado|perdido|pendente`), (3) mantém DEFAULT `'rascunho'`.

Purpose: Desbloquear Phase 10 (WIZ-04: status badges em Pedidos). Schema continua aditivo (coluna não muda formato — só ganha CHECK); UPDATE de dado é a única exceção registrada (D-10).
Output: 1 arquivo SQL pronto para `supabase db push` (executado no Plan 07-04, em terceiro lugar — D-21).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-schema-prep-v1-1/07-CONTEXT.md
@supabase/migrations/20260213150619_c40f8f90-794b-4d08-90b8-635ef7968cc9.sql
@src/types/orcamento.ts
@src/components/Step3Revisao.tsx

<interfaces>
orcamentos (existente):
  id UUID PK,
  data DATE NOT NULL DEFAULT CURRENT_DATE,
  valor NUMERIC NOT NULL DEFAULT 0,
  status TEXT NOT NULL DEFAULT 'rascunho',  -- HOJE: livre, sem CHECK
  cliente_id UUID NOT NULL REFERENCES clientes(id),
  colaborador_id UUID NOT NULL REFERENCES colaboradores(id),
  created_at TIMESTAMPTZ
  RLS: "Anyone can read orcamentos" USING (true) — NÃO MEXER

src/types/orcamento.ts:109 (FICA DESATUALIZADO DE PROPÓSITO — D-13):
  export type StatusOrcamento = 'rascunho' | 'fechado' | 'perdido'
  -- Sync TS para 'rascunho' | 'aprovado' | 'perdido' | 'pendente' é trabalho da Phase 10 (WIZ-04)

src/components/Step3Revisao.tsx:224 (não afetado):
  status: "rascunho"  -- continua válido após CHECK
</interfaces>

<status_change_summary>
| Antes (livre) | Depois (CHECK) | Ação |
|---------------|----------------|------|
| 'rascunho' | 'rascunho' | mantém |
| 'fechado' | 'aprovado' | UPDATE in-place (D-10) |
| 'perdido' | 'perdido' | mantém |
| (novo) | 'pendente' | enum permite |
| qualquer outro | (NÃO permitido) | pre-flight assert aborta |
</status_change_summary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration orcamentos.status enum (D-10..D-14)</name>
  <files>supabase/migrations/20260511000003_orcamentos_status_enum.sql</files>
  <read_first>
    - supabase/migrations/20260213150619_c40f8f90-794b-4d08-90b8-635ef7968cc9.sql (definição original orcamentos.status)
    - .planning/phases/07-schema-prep-v1-1/07-CONTEXT.md (D-10 a D-14)
    - src/types/orcamento.ts (linha 109 — confirma valores históricos)
    - src/components/Step3Revisao.tsx (linha 224 — confirma único INSERT escreve 'rascunho')
  </read_first>
  <action>
Criar `supabase/migrations/20260511000003_orcamentos_status_enum.sql` com EXATAMENTE este conteúdo:

```sql
-- Phase 7 / Plan 03: orcamentos.status enum via CHECK constraint (AUTO-03 corolário, destrava WIZ-04)
-- Refs: D-10 (UPDATE fechado→aprovado in-place — única mutação destrutiva de DADO; schema continua aditivo),
--       D-11 (ADD CHECK constraint após UPDATE — trava regressão futura),
--       D-12 (manter DEFAULT 'rascunho' — zero mudança no wizard),
--       D-13 (TS em src/types/orcamento.ts:109 fica desatualizado de propósito — Phase 10 sincroniza),
--       D-14 (pre-flight assert defensivo: nenhum valor fora do enum antes da CHECK).
--
-- Step3Revisao.tsx:224 grava 'rascunho' — continua válido após CHECK.

BEGIN;

-- =========================================================================
-- Bloco 1 — UPDATE in-place: fechado → aprovado (D-10)
-- Renomeia o status legado 'fechado' para o novo nome 'aprovado'.
-- Schema permanece aditivo (coluna não muda forma); só o dado muda.
-- =========================================================================
UPDATE public.orcamentos
   SET status = 'aprovado'
 WHERE status = 'fechado';

-- =========================================================================
-- Bloco 2 — Pre-flight assert (D-14): nenhum valor fora do enum
-- Defesa contra valores inesperados em prod (typo, status manual antigo, etc).
-- Se aparecer algo, RAISE EXCEPTION aborta a transação ANTES da CHECK ser aplicada.
-- =========================================================================
DO $$
DECLARE bad_count int;
BEGIN
  SELECT count(*) INTO bad_count
    FROM public.orcamentos
   WHERE status NOT IN ('rascunho','aprovado','perdido','pendente');
  IF bad_count > 0 THEN
    RAISE EXCEPTION 'Migration aborted: % linha(s) com status fora do enum (rascunho|aprovado|perdido|pendente). Investigue antes de aplicar CHECK.', bad_count;
  END IF;
END $$;

-- =========================================================================
-- Bloco 3 — ADD CHECK constraint (D-11)
-- Trava regressão futura. Hoje status é TEXT NOT NULL DEFAULT 'rascunho' sem CHECK.
-- =========================================================================
ALTER TABLE public.orcamentos
  ADD CONSTRAINT orcamentos_status_check
  CHECK (status IN ('rascunho','aprovado','perdido','pendente'));

-- =========================================================================
-- Bloco 4 — Manter DEFAULT 'rascunho' (D-12)
-- DEFAULT já existe na coluna; não alteramos. Step3Revisao.tsx continua gravando 'rascunho' explícito.
-- =========================================================================
-- (no-op intencional — DEFAULT 'rascunho' permanece como definido em 20260213150619)

COMMENT ON COLUMN public.orcamentos.status IS
  'Status do orçamento. Phase 7 AUTO-03 corolário: enum {rascunho, aprovado, perdido, pendente} enforce via orcamentos_status_check. fechado→aprovado migrado in-place (D-10). DEFAULT rascunho mantido. Phase 10 (WIZ-04) sincroniza tipo TS.';

COMMIT;
```

Notas de execução:
- A ordem importa: UPDATE PRIMEIRO, depois ASSERT, depois CHECK (D-14 + D-11)
- Sem `ALTER COLUMN` — não muda formato/tipo, só adiciona CONSTRAINT
- DEFAULT 'rascunho' permanece (D-12: no-op intencional, comentado no SQL)
- NÃO mexer em `src/types/orcamento.ts` aqui (D-13: Phase 10 cuida)
- NÃO rodar `supabase db push` aqui — Plan 07-04 cuida (e é o ÚLTIMO da ordem D-21: status sai por último)
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260511000003_orcamentos_status_enum.sql && grep -q "UPDATE public.orcamentos" supabase/migrations/20260511000003_orcamentos_status_enum.sql && grep -q "ADD CONSTRAINT orcamentos_status_check" supabase/migrations/20260511000003_orcamentos_status_enum.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260511000003_orcamentos_status_enum.sql` existe
    - `grep "UPDATE public.orcamentos" supabase/migrations/20260511000003_orcamentos_status_enum.sql` retorna 1 match
    - `grep "SET status = 'aprovado'" supabase/migrations/20260511000003_orcamentos_status_enum.sql` retorna 1 match
    - `grep "WHERE status = 'fechado'" supabase/migrations/20260511000003_orcamentos_status_enum.sql` retorna 1 match (D-10)
    - `grep "RAISE EXCEPTION 'Migration aborted" supabase/migrations/20260511000003_orcamentos_status_enum.sql` retorna 1 match (D-14)
    - `grep "status NOT IN ('rascunho','aprovado','perdido','pendente')" supabase/migrations/20260511000003_orcamentos_status_enum.sql` retorna 1 match
    - `grep "ADD CONSTRAINT orcamentos_status_check" supabase/migrations/20260511000003_orcamentos_status_enum.sql` retorna 1 match (D-11)
    - `grep "CHECK (status IN ('rascunho','aprovado','perdido','pendente'))" supabase/migrations/20260511000003_orcamentos_status_enum.sql` retorna 1 match
    - `grep "COMMENT ON COLUMN public.orcamentos.status" supabase/migrations/20260511000003_orcamentos_status_enum.sql` retorna 1 match
    - Arquivo começa com `BEGIN;` e termina com `COMMIT;` (atomicidade D-20)
    - Arquivo NÃO contém `ALTER COLUMN status` (formato não muda — só CHECK)
    - Arquivo NÃO contém `DROP DEFAULT` nem `SET DEFAULT` (DEFAULT 'rascunho' preservado — D-12)
    - Arquivo NÃO modifica `src/types/orcamento.ts` (D-13: Phase 10 cuida)
  </acceptance_criteria>
  <done>Migration com UPDATE in-place + pre-flight assert + CHECK constraint + COMMENT. Schema aditivo preservado, DEFAULT mantido, TS intencionalmente desatualizado.</done>
</task>

</tasks>

<verification>
- Arquivo existe, ordem dos blocos correta (UPDATE → ASSERT → CHECK), DEFAULT preservado, TS não tocado
- Sintaxe SQL válida (validation real só no `supabase db push` do Plan 07-04)
</verification>

<success_criteria>
- 1 migration file criado: UPDATE fechado→aprovado, pre-flight assert, ADD CHECK constraint
- Schema aditivo (sem ALTER COLUMN, sem DROP), DEFAULT mantido
- TS intencionalmente desatualizado (D-13)
- Pronto para push no Plan 07-04 (último da ordem D-21)
</success_criteria>

<output>
After completion, create `.planning/phases/07-schema-prep-v1-1/07-03-SUMMARY.md` documentando arquivo criado, decisões D-10..D-14, e nota de que TS sync é Phase 10.
</output>
