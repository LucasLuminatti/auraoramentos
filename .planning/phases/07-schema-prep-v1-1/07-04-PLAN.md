---
phase: 07-schema-prep-v1-1
plan: 04
type: execute
wave: 2
depends_on: [07-01, 07-02, 07-03]
files_modified:
  - .planning/phases/07-schema-prep-v1-1/07-AUDIT-PRODUCT-VARIANTS.md
  - .planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md
  - .planning/phases/07-schema-prep-v1-1/07-SMOKE-RESULTS.md
  - .planning/phases/07-schema-prep-v1-1/07-SUMMARY.md
autonomous: false
requirements: [RLS-03, AUTO-03]

must_haves:
  truths:
    - "Auditoria SQL de product_variants masters está documentada em 07-AUDIT-PRODUCT-VARIANTS.md (D-16)"
    - "Gaps de descrição rica são registrados como FOLLOW-UP, NÃO bloqueiam fechamento da Phase 7 (D-17)"
    - "supabase db push aplicou as 3 migrations em prod na ordem user_id → data_nascimento → status (D-21)"
    - "07-PUSH-LOG.md tem timestamps + counts antes/depois para cada migration"
    - "07-SMOKE-RESULTS.md documenta os 4 checks D-22 (4 SELECT counts, JOIN query, CHECK count, Playwright /)"
    - "Wizard, login, admin, PDF v1/v2, Drive continuam funcionando (zero regressão visível)"
    - "07-SUMMARY.md fecha a phase listando o que foi entregue + FOLLOW-UPs (D-17)"
  artifacts:
    - path: ".planning/phases/07-schema-prep-v1-1/07-AUDIT-PRODUCT-VARIANTS.md"
      provides: "Resultado da query D-16 + gap policy aplicada (D-17, D-18)"
    - path: ".planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md"
      provides: "Timestamps + counts antes/depois para cada uma das 3 migrations"
    - path: ".planning/phases/07-schema-prep-v1-1/07-SMOKE-RESULTS.md"
      provides: "Resultados dos 4 checks D-22"
    - path: ".planning/phases/07-schema-prep-v1-1/07-SUMMARY.md"
      provides: "Fechamento da Phase 7"
  key_links:
    - from: "supabase db push"
      to: "Supabase prod (jkewlaezvrbuicmncqbj)"
      via: "CLI Lenny já autenticado"
      pattern: "supabase db push"
    - from: "Smoke Playwright"
      to: "https://orcamentosaura.com.br"
      via: "MCP Playwright (login + abrir / + console limpo)"
---

<objective>
Fechar a Phase 7 executando, em ordem: (1) auditoria SQL de cobertura de descrição rica em `product_variants` (D-16); (2) **[BLOCKING]** `supabase db push` em prod aplicando as 3 migrations dos Plans 07-01..07-03 na ordem D-21; (3) smoke de regressão D-22 (4 checks); (4) escrever PUSH-LOG, SMOKE-RESULTS e SUMMARY.

Purpose: Confirmar critério de sucesso #5 do ROADMAP (zero regressão em prod) e deixar a phase fechada com rastreabilidade completa.
Output: 4 documentos da phase + schema de prod atualizado.

**autonomous: false** — Plan inteiro pausa para Lenny acompanhar/executar o `supabase db push` e o login do Playwright.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-schema-prep-v1-1/07-CONTEXT.md
@supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql
@supabase/migrations/20260511000002_clientes_data_nascimento.sql
@supabase/migrations/20260511000003_orcamentos_status_enum.sql
@supabase/migrations/20260501000001_products_and_variants.sql
@src/lib/productAttributes.ts

<interfaces>
Migrations a empurrar (ordem D-21):
  1º — 20260511000001_arquitetos_clientes_user_id.sql (mais delicada: pre-flight + backfill)
  2º — 20260511000002_clientes_data_nascimento.sql (trivial)
  3º — 20260511000003_orcamentos_status_enum.sql (UPDATE in-place + CHECK)

CLI Supabase (Lenny já autenticado em prod):
  supabase db push  -- pega TODAS as migrations pendentes em ordem alfabética dos timestamps;
                    -- como os 3 arquivos têm timestamps 000001/000002/000003 do mesmo dia,
                    -- já sai na ordem D-21 automaticamente.

Audit SQL (D-16):
  SELECT count(*), origem
    FROM product_variants
   WHERE origem = 'master'
     AND (atributos->>'temperatura_k' IS NULL
          OR atributos->>'irc' IS NULL
          OR atributos->>'nicho' IS NULL)
   GROUP BY origem;
  -- D-18: SKUs origem='coringa' (AU001..AU016) e origem='legado' NÃO entram na auditoria.
</interfaces>

<smoke_checks_d22>
1. SQL: SELECT count(*) FROM clientes; SELECT count(*) FROM arquitetos; SELECT count(*) FROM orcamentos; SELECT count(*) FROM product_variants;
   → Comparar com snapshot pré-push (registrar em PUSH-LOG.md). Counts devem ser idênticos.
2. SQL: SELECT o.id, c.nome, a.nome FROM orcamentos o JOIN clientes c ON c.id = o.cliente_id LEFT JOIN arquitetos a ON a.id = c.arquiteto_id LIMIT 5;
   → Confirma JOIN típico do wizard/Pedidos ainda funciona.
3. SQL: SELECT count(*) FROM orcamentos WHERE status IN ('rascunho','aprovado','perdido','pendente');
   → Deve bater EXATO com SELECT count(*) FROM orcamentos (CHECK constraint válida pra todas as linhas).
4. Playwright: abrir https://orcamentosaura.com.br, login Lenny, abrir / (wizard), confirmar Step 1 renderiza colaborador + cliente sem erro JS no console.
</smoke_checks_d22>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditoria product_variants vs descrição rica (D-16, D-17, D-18)</name>
  <files>.planning/phases/07-schema-prep-v1-1/07-AUDIT-PRODUCT-VARIANTS.md</files>
  <read_first>
    - supabase/migrations/20260501000001_products_and_variants.sql (entender schema product_variants.atributos JSONB)
    - src/lib/productAttributes.ts (mapMasterRow — confirma quais campos vão para atributos)
    - .planning/phases/07-schema-prep-v1-1/07-CONTEXT.md §D-15..D-18
  </read_first>
  <action>
1. Rodar a query D-16 contra prod (via Supabase Studio SQL Editor OU `supabase db remote sql` se CLI suportar):
   ```sql
   -- Total de masters
   SELECT count(*) AS total_masters
     FROM public.product_variants
    WHERE origem = 'master';

   -- Masters com gaps em qualquer um dos 3 campos críticos
   SELECT count(*) AS masters_com_gaps, origem
     FROM public.product_variants
    WHERE origem = 'master'
      AND (atributos->>'temperatura_k' IS NULL
           OR atributos->>'irc' IS NULL
           OR atributos->>'nicho' IS NULL)
    GROUP BY origem;

   -- Detalhe por campo (ajuda a priorizar follow-up)
   SELECT
     count(*) FILTER (WHERE atributos->>'temperatura_k' IS NULL) AS gap_temperatura_k,
     count(*) FILTER (WHERE atributos->>'irc' IS NULL)           AS gap_irc,
     count(*) FILTER (WHERE atributos->>'nicho' IS NULL)         AS gap_nicho,
     count(*) FILTER (WHERE atributos->>'cor_iluminacao' IS NULL) AS gap_cor_iluminacao
     FROM public.product_variants
    WHERE origem = 'master';
   ```

2. Escrever `.planning/phases/07-schema-prep-v1-1/07-AUDIT-PRODUCT-VARIANTS.md` com:
   - Header (Phase 7, AUDIT, data, requirement WIZ-05 contexto)
   - Query SQL exata executada
   - Resultado tabelado (total_masters, masters_com_gaps, gap_temperatura_k, gap_irc, gap_nicho, gap_cor_iluminacao)
   - Decisão D-18 explícita: SKUs origem='coringa' (AU001..AU016) e origem='legado' NÃO entram na auditoria
   - Gap policy D-17: gaps NÃO bloqueiam fechamento da Phase 7 — viram FOLLOW-UP
   - Lista de FOLLOW-UPs (se houver gaps): "Re-importar master via ImportMaster" e/ou "Edição manual via admin (Phase 8 FORM-03 cobre coringas)"
   - Confirmação que Phase 10 WIZ-05 success criteria #5 já trata variant sem dado renderizando só o nome cru

3. Não alterar nenhuma migration. NÃO criar migration nova mesmo se gaps existirem (D-15: campos ficam no JSONB; D-17: gaps são FOLLOW-UP).
  </action>
  <verify>
    <automated>test -f .planning/phases/07-schema-prep-v1-1/07-AUDIT-PRODUCT-VARIANTS.md && grep -q "total_masters" .planning/phases/07-schema-prep-v1-1/07-AUDIT-PRODUCT-VARIANTS.md && grep -q "FOLLOW-UP" .planning/phases/07-schema-prep-v1-1/07-AUDIT-PRODUCT-VARIANTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `.planning/phases/07-schema-prep-v1-1/07-AUDIT-PRODUCT-VARIANTS.md` existe
    - `grep "atributos->>'temperatura_k'" .planning/phases/07-schema-prep-v1-1/07-AUDIT-PRODUCT-VARIANTS.md` retorna pelo menos 1 match
    - `grep "atributos->>'irc'" .planning/phases/07-schema-prep-v1-1/07-AUDIT-PRODUCT-VARIANTS.md` retorna pelo menos 1 match
    - `grep "atributos->>'nicho'" .planning/phases/07-schema-prep-v1-1/07-AUDIT-PRODUCT-VARIANTS.md` retorna pelo menos 1 match
    - `grep -E "total_masters\\s*[:=]|total_masters[^a-z]" .planning/phases/07-schema-prep-v1-1/07-AUDIT-PRODUCT-VARIANTS.md` retorna match com número
    - `grep -i "FOLLOW-UP\\|follow-up" .planning/phases/07-schema-prep-v1-1/07-AUDIT-PRODUCT-VARIANTS.md` retorna pelo menos 1 match (D-17)
    - `grep -i "coringa\\|AU001" .planning/phases/07-schema-prep-v1-1/07-AUDIT-PRODUCT-VARIANTS.md` retorna pelo menos 1 match (D-18)
    - NÃO foi criada nenhuma nova migration .sql para descrição rica (D-15)
  </acceptance_criteria>
  <done>Auditoria documentada com query + resultados + gap policy. Zero migrations adicionais.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 [BLOCKING]: supabase db push em prod (D-19, D-20, D-21)</name>
  <files>(modifica schema prod, sem alterar arquivos do repo)</files>
  <read_first>
    - supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql
    - supabase/migrations/20260511000002_clientes_data_nascimento.sql
    - supabase/migrations/20260511000003_orcamentos_status_enum.sql
    - .planning/phases/07-schema-prep-v1-1/07-CONTEXT.md §D-19..D-22
  </read_first>
  <what-built>
3 migration files prontas (Plans 07-01, 07-02, 07-03). Esta task as aplica em produção em ORDEM (D-21):
  1º user_id (mais delicada — pre-flight assert pode falhar primeiro)
  2º data_nascimento (trivial)
  3º status (UPDATE in-place + CHECK)

Como os timestamps são 000001 / 000002 / 000003 do mesmo dia, `supabase db push` já aplica nessa ordem automaticamente.
  </what-built>
  <how-to-verify>
**Pré-push (capturar snapshot — Claude executa via SQL antes do push):**
```sql
SELECT count(*) AS pre_clientes        FROM public.clientes;
SELECT count(*) AS pre_arquitetos      FROM public.arquitetos;
SELECT count(*) AS pre_orcamentos      FROM public.orcamentos;
SELECT count(*) AS pre_product_variants FROM public.product_variants;
SELECT status, count(*) FROM public.orcamentos GROUP BY status;  -- pra confirmar quantas linhas tinham 'fechado'
```
Anotar resultados (vão para 07-PUSH-LOG.md na próxima task).

**Push (Lenny executa interativamente, Claude acompanha):**
```bash
cd C:/Users/lenny/Desktop/Luminatti/automa_aura/auraoramentos
supabase db push
```
Esperar:
- Migration 20260511000001 aplica → "Migration applied" sem erro
- Migration 20260511000002 aplica → "Migration applied" sem erro
- Migration 20260511000003 aplica → "Migration applied" sem erro

**Pós-push (Claude executa via SQL):**
```sql
SELECT count(*) AS post_clientes        FROM public.clientes;
SELECT count(*) AS post_arquitetos      FROM public.arquitetos;
SELECT count(*) AS post_orcamentos      FROM public.orcamentos;
SELECT count(*) AS post_product_variants FROM public.product_variants;
SELECT status, count(*) FROM public.orcamentos GROUP BY status;  -- 'fechado' deve ter virado 'aprovado'
SELECT column_name, data_type, is_nullable
  FROM information_schema.columns
 WHERE table_schema='public' AND table_name='clientes'
   AND column_name IN ('user_id','data_nascimento');
SELECT column_name, data_type, is_nullable
  FROM information_schema.columns
 WHERE table_schema='public' AND table_name='arquitetos' AND column_name='user_id';
SELECT conname, pg_get_constraintdef(oid)
  FROM pg_constraint
 WHERE conrelid = 'public.orcamentos'::regclass AND conname='orcamentos_status_check';
```

**Critério de sucesso da task:**
- 3 migrations aplicadas sem erro
- pre_X == post_X para todas as 4 tabelas (zero linhas perdidas)
- clientes.user_id e arquitetos.user_id existem como UUID NOT NULL
- clientes.data_nascimento existe como DATE nullable
- orcamentos_status_check existe e o CHECK definition contém 'rascunho','aprovado','perdido','pendente'
- Nenhuma linha em orcamentos com status='fechado' (todas viraram 'aprovado')

Se ALGUMA migration falhar:
- Pre-flight assert do user_id estourou? → não há admin em user_roles+colaboradores. Investigar e re-rodar.
- Pre-flight assert do status estourou? → existem valores fora do enum em prod. Listar com `SELECT DISTINCT status FROM orcamentos WHERE status NOT IN (...)`, decidir mapeamento, ajustar migration ou fazer UPDATE manual antes.
- CHECK violation em status? → mesmo caso acima.
  </how-to-verify>
  <acceptance_criteria>
    - Output do `supabase db push` mostra 3 migrations aplicadas sem erro
    - Counts pré-push == counts pós-push para clientes, arquitetos, orcamentos, product_variants
    - `information_schema.columns` confirma `clientes.user_id UUID NOT NULL`, `clientes.data_nascimento DATE NULL`, `arquitetos.user_id UUID NOT NULL`
    - `pg_constraint` retorna `orcamentos_status_check` com definition `CHECK ((status = ANY (ARRAY['rascunho'::text, 'aprovado'::text, 'perdido'::text, 'pendente'::text])))` (ou equivalente)
    - `SELECT count(*) FROM orcamentos WHERE status='fechado'` retorna 0
    - Lenny aprovou explicitamente os resultados
  </acceptance_criteria>
  <resume-signal>Lenny digita "approved" depois de validar os 3 push outputs e os SELECT pós-push. Se algo deu ruim, descrever o erro pra ajustar.</resume-signal>
  <action>Esta task é um checkpoint:human-verify (gate=blocking). Antes de pausar para Lenny: (1) Claude executa os SELECTs pré-push do bloco how-to-verify (Supabase Studio SQL editor ou MCP equivalente) e anota counts em memória; (2) Claude prepara terminal com `cd C:/Users/lenny/Desktop/Luminatti/automa_aura/auraoramentos && supabase db push`; (3) Lenny executa o push interativamente (Claude observa output stream); (4) Claude executa os SELECTs pós-push e compara com snapshot pré-push; (5) Claude apresenta resultado consolidado a Lenny aguardando aprovação. NÃO commitar nada. NÃO escrever PUSH-LOG.md aqui — Task 3 cuida.</action>
  <verify>
    <automated>echo "Manual checkpoint — supabase db push é interativo. Validation real acontece via SELECTs pós-push e aprovação explícita de Lenny."</automated>
  </verify>
  <done>3 migrations applied OK em prod, counts pré==pós (4 tabelas), schema confirmado via information_schema, status=fechado erradicado, Lenny aprovou explicitamente.</done>
</task>

<task type="auto">
  <name>Task 3: Escrever 07-PUSH-LOG.md (D-19, D-20, D-21)</name>
  <files>.planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md</files>
  <read_first>
    - .planning/phases/07-schema-prep-v1-1/07-CONTEXT.md §D-19..D-22
    - (resultados in-memory dos SELECTs pré/pós-push da Task 2)
  </read_first>
  <action>
Escrever `.planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md` com a estrutura:

```markdown
# Phase 7 — Push Log

**Project:** AURA (Supabase prod jkewlaezvrbuicmncqbj, region sa-east-1)
**Push command:** `supabase db push`
**Date:** YYYY-MM-DD HH:MM (timezone)
**Operator:** Lenny Wajcberg
**Push order (D-21):** user_id → data_nascimento → status

## Migration push order

| # | Migration | Domain | Risk | Outcome |
|---|-----------|--------|------|---------|
| 1 | 20260511000001_arquitetos_clientes_user_id.sql | RLS-03 (user_id em arquitetos+clientes) | Alta (pre-flight + backfill) | OK / FAIL |
| 2 | 20260511000002_clientes_data_nascimento.sql | AUTO-03 (data_nascimento) | Baixa (aditivo trivial) | OK / FAIL |
| 3 | 20260511000003_orcamentos_status_enum.sql | AUTO-03 corolário (CHECK enum) | Média (UPDATE in-place + assert) | OK / FAIL |

## Snapshot counts (pré-push vs pós-push)

| Tabela | Pré-push | Pós-push | Diff | OK? |
|--------|----------|----------|------|-----|
| clientes | NN | NN | 0 | YES |
| arquitetos | NN | NN | 0 | YES |
| orcamentos | NN | NN | 0 | YES |
| product_variants | NN | NN | 0 | YES |

## Status distribution (orcamentos)

| Status | Pré-push | Pós-push | Notas |
|--------|----------|----------|-------|
| rascunho | NN | NN | mantém |
| fechado | NN | 0 | UPDATE → aprovado (D-10) |
| aprovado | 0 | NN | herdado de fechado |
| perdido | NN | NN | mantém |
| pendente | 0 | 0 | enum permite, ainda não usado |

## Schema confirmation (information_schema)

- `clientes.user_id` → UUID, NOT NULL, FK auth.users(id) ON DELETE RESTRICT — OK
- `clientes.data_nascimento` → DATE, NULLABLE — OK
- `arquitetos.user_id` → UUID, NOT NULL, FK auth.users(id) ON DELETE RESTRICT — OK
- `orcamentos_status_check` → CHECK (status IN ('rascunho','aprovado','perdido','pendente')) — OK

## Indexes criados

- `idx_arquitetos_user_id` BTREE — OK
- `idx_clientes_user_id` BTREE — OK
- `idx_clientes_data_nascimento` BTREE — OK

## Comments (rastreabilidade)

- arquitetos.user_id: cita "Phase 7 RLS-03 / pattern Drive D-02 errata"
- clientes.user_id: idem
- clientes.data_nascimento: cita "Phase 7 AUTO-03 / cron Phase 12"
- orcamentos.status: cita enum + UPDATE fechado→aprovado + Phase 10 sync TS

## Issues encontrados

- (nenhum) OU descrição + resolução

<hr/>
*PUSH-LOG gerado pós-execução interativa do Plan 07-04 Task 2.*
```

Substituir os "NN" pelos valores reais capturados na Task 2 (Lenny passou os números).
  </action>
  <verify>
    <automated>test -f .planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md && grep -q "## Migration push order" .planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md && grep -q "## Snapshot counts" .planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `.planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md` existe
    - `grep "## Migration push order" .planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md` retorna 1 match
    - `grep "## Snapshot counts" .planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md` retorna 1 match
    - `grep "20260511000001_arquitetos_clientes_user_id.sql" .planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md` retorna 1 match
    - `grep "20260511000002_clientes_data_nascimento.sql" .planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md` retorna 1 match
    - `grep "20260511000003_orcamentos_status_enum.sql" .planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md` retorna 1 match
    - `grep -i "rls-03\\|auto-03" .planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md` retorna pelo menos 2 matches
    - Snapshot counts preenchidos com números reais (não "NN")
  </acceptance_criteria>
  <done>PUSH-LOG completo, rastreabilidade clara, números reais capturados.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 [BLOCKING]: Smoke de regressão D-22 (4 checks)</name>
  <files>.planning/phases/07-schema-prep-v1-1/07-SMOKE-RESULTS.md</files>
  <read_first>
    - .planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md (counts pós-push)
    - .planning/phases/07-schema-prep-v1-1/07-CONTEXT.md §D-22, §D-23
  </read_first>
  <what-built>
3 migrations aplicadas em prod (Task 2). Esta task valida critério de sucesso #5 do ROADMAP: zero regressão no wizard, login, admin, PDF v1/v2, Drive.

Os 4 checks D-22:
1. SQL counts (clientes, arquitetos, orcamentos, product_variants) — já capturados no PUSH-LOG, confirmar consistência
2. SQL JOIN típico do wizard/Pedidos
3. SQL CHECK constraint válida (count com WHERE status IN (...) bate com count total)
4. Playwright: abrir https://orcamentosaura.com.br, login Lenny, abrir / (wizard), Step 1 renderiza sem erro JS no console
  </what-built>
  <how-to-verify>
**Check 1 — SQL counts (consistência com PUSH-LOG):**
```sql
SELECT 'clientes' AS t, count(*) FROM public.clientes
UNION ALL SELECT 'arquitetos', count(*) FROM public.arquitetos
UNION ALL SELECT 'orcamentos', count(*) FROM public.orcamentos
UNION ALL SELECT 'product_variants', count(*) FROM public.product_variants;
```
PASS se counts == os do PUSH-LOG.

**Check 2 — SQL JOIN típico do wizard/Pedidos:**
```sql
SELECT o.id, c.nome AS cliente, a.nome AS arquiteto
  FROM public.orcamentos o
  JOIN public.clientes c ON c.id = o.cliente_id
  LEFT JOIN public.arquitetos a ON a.id = c.arquiteto_id
 LIMIT 5;
```
PASS se retorna 5 linhas (ou todas se houver < 5) sem erro.

**Check 3 — SQL CHECK constraint válida:**
```sql
SELECT
  (SELECT count(*) FROM public.orcamentos) AS total,
  (SELECT count(*) FROM public.orcamentos
    WHERE status IN ('rascunho','aprovado','perdido','pendente')) AS in_enum;
```
PASS se total == in_enum.

**Check 4 — Playwright (Lenny acompanha visualmente):**
Claude usa `mcp__plugin_playwright_playwright__*`:
1. browser_navigate → https://orcamentosaura.com.br
2. Login com credenciais Lenny (lenny.wajcberg@luminattiled.com.br) — Lenny digita senha
3. browser_navigate → https://orcamentosaura.com.br/ (Step 1 do wizard)
4. browser_snapshot — confirmar que Step 1 carrega: select de colaborador visível, autocomplete de cliente visível
5. browser_console_messages — confirmar ZERO erro JS (warnings ok, erros não)
6. browser_close

PASS se: Step 1 renderizou normalmente + console limpo de erros.

**Smoke total:** 4 PASS = phase fechada com zero regressão. Qualquer FAIL → registrar em SMOKE-RESULTS.md com decisão (bloquear ou seguir, D-23).
  </how-to-verify>
  <acceptance_criteria>
    - Arquivo `.planning/phases/07-schema-prep-v1-1/07-SMOKE-RESULTS.md` criado seguindo padrão v1.0
    - Documento contém Check 1 (counts), Check 2 (JOIN), Check 3 (CHECK válido), Check 4 (Playwright)
    - Cada check tem status PASS/FAIL explícito
    - Se FAIL: registrar bug + decisão (bloquear/seguir per D-23)
    - `grep "Check 1\\|Check 2\\|Check 3\\|Check 4" .planning/phases/07-schema-prep-v1-1/07-SMOKE-RESULTS.md` retorna 4+ matches
    - `grep -E "PASS|FAIL" .planning/phases/07-schema-prep-v1-1/07-SMOKE-RESULTS.md` retorna pelo menos 4 matches
    - Lenny aprovou
  </acceptance_criteria>
  <resume-signal>Lenny digita "approved" depois dos 4 checks. Se algum FAIL: descrever pra decidir.</resume-signal>
  <action>Esta task é um checkpoint:human-verify (gate=blocking). Antes de pausar: (1) Claude executa Check 1 (4 SELECT counts), Check 2 (JOIN orcamentos+clientes+arquitetos LIMIT 5), Check 3 (count IN enum vs total) via Supabase SQL; (2) Claude usa MCP Playwright (`mcp__plugin_playwright_playwright__*`) para Check 4 — browser_navigate https://orcamentosaura.com.br, login Lenny (Lenny digita senha), browser_navigate /, browser_snapshot Step 1, browser_console_messages confirma zero erro JS; (3) Claude escreve `07-SMOKE-RESULTS.md` com os 4 checks formatados (D-23 padrão v1.0, cada check PASS/FAIL explícito); (4) Claude apresenta resultado a Lenny aguardando aprovação. Se qualquer check FAIL: registrar bug e decidir bloquear/seguir per D-23 antes de aprovar.</action>
  <verify>
    <automated>test -f .planning/phases/07-schema-prep-v1-1/07-SMOKE-RESULTS.md && grep -cE "PASS|FAIL" .planning/phases/07-schema-prep-v1-1/07-SMOKE-RESULTS.md | grep -qE "^[4-9]|^[1-9][0-9]" && echo OK</automated>
  </verify>
  <done>4 smoke checks executados, 07-SMOKE-RESULTS.md documenta cada um com PASS/FAIL, Lenny aprovou (ou bug foi registrado e decisão D-23 tomada).</done>
</task>

<task type="auto">
  <name>Task 5: Escrever 07-SUMMARY.md (fechamento da phase)</name>
  <files>.planning/phases/07-schema-prep-v1-1/07-SUMMARY.md</files>
  <read_first>
    - .planning/phases/07-schema-prep-v1-1/07-AUDIT-PRODUCT-VARIANTS.md
    - .planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md
    - .planning/phases/07-schema-prep-v1-1/07-SMOKE-RESULTS.md
    - .planning/phases/07-schema-prep-v1-1/07-CONTEXT.md (D-XX completos)
    - .planning/phases/07-schema-prep-v1-1/07-01-SUMMARY.md
    - .planning/phases/07-schema-prep-v1-1/07-02-SUMMARY.md
    - .planning/phases/07-schema-prep-v1-1/07-03-SUMMARY.md
    - $HOME/.claude/get-shit-done/templates/summary.md (estrutura padrão)
  </read_first>
  <action>
Escrever `.planning/phases/07-schema-prep-v1-1/07-SUMMARY.md` consolidando a phase. Estrutura:

```markdown
# Phase 7: Schema & Prep v1.1 — Summary

**Phase:** 07-schema-prep-v1-1
**Status:** SHIPPED
**Date:** 2026-05-11
**Requirements delivered:** RLS-03, AUTO-03

## What was delivered

3 migrations aditivas aplicadas em prod (zero regressão confirmada via smoke D-22):

1. `20260511000001_arquitetos_clientes_user_id.sql` — `user_id UUID NOT NULL` em arquitetos + clientes (RLS-03)
   - Pattern Drive D-02 errata replicado (Blocos 1-4 apenas — policies ficam pra Phase 9)
   - Pre-flight assert + backfill admin mais antigo + NOT NULL + 2 indexes BTREE
2. `20260511000002_clientes_data_nascimento.sql` — `data_nascimento DATE NULL` em clientes (AUTO-03)
   - Aditivo trivial + index BTREE
3. `20260511000003_orcamentos_status_enum.sql` — UPDATE fechado→aprovado + CHECK constraint (AUTO-03 corolário, destrava WIZ-04)
   - Pre-flight assert + UPDATE in-place + ADD CONSTRAINT
   - DEFAULT 'rascunho' preservado

## Decisions implementadas

- D-01 (FK ON DELETE RESTRICT) — Plan 07-01
- D-02 (backfill admin mais antigo) — Plan 07-01
- D-03 (NOT NULL após backfill) — Plan 07-01
- D-04 (index BTREE user_id) — Plan 07-01
- D-05 (COMMENT rastreabilidade) — Plans 07-01, 07-02, 07-03
- D-06 (NÃO criar policies — Phase 9) — Plan 07-01
- D-07 (data_nascimento NULL sem default) — Plan 07-02
- D-08 (index BTREE simples) — Plan 07-02
- D-09 (COMMENT cita Phase 12) — Plan 07-02
- D-10 (UPDATE fechado→aprovado in-place) — Plan 07-03
- D-11 (ADD CHECK constraint) — Plan 07-03
- D-12 (DEFAULT rascunho mantido) — Plan 07-03
- D-13 (TS desatualizado de propósito) — Phase 10 sync
- D-14 (pre-flight assert anti-regressão) — Plan 07-03
- D-15 (descrição rica em JSONB, sem migration) — Plan 07-04 audit
- D-16 (auditoria via SQL) — Plan 07-04 → 07-AUDIT-PRODUCT-VARIANTS.md
- D-17 (gaps viram FOLLOW-UP, não bloqueiam) — Plan 07-04 audit
- D-18 (coringa/legado fora da auditoria) — Plan 07-04 audit
- D-19 (3 migrations separadas) — Plans 07-01..07-03
- D-20 (BEGIN/COMMIT por arquivo) — Plans 07-01..07-03
- D-21 (ordem de push) — Plan 07-04 Task 2
- D-22 (smoke 4 checks) — Plan 07-04 Task 4 → 07-SMOKE-RESULTS.md
- D-23 (formato smoke results) — 07-SMOKE-RESULTS.md

## Smoke results (D-22)

Resumo (detalhe em 07-SMOKE-RESULTS.md):
- Check 1 SQL counts: PASS
- Check 2 SQL JOIN wizard: PASS
- Check 3 SQL CHECK válida: PASS
- Check 4 Playwright Step 1: PASS

(ajustar conforme resultado real da Task 4)

## Audit findings (product_variants)

Resumo (detalhe em 07-AUDIT-PRODUCT-VARIANTS.md):
- Total masters: NN
- Masters com gaps: NN
- Gaps por campo: temperatura_k=NN, irc=NN, nicho=NN, cor_iluminacao=NN

## FOLLOW-UPs (não bloqueiam Phase 7)

- [ ] FOLLOW-UP-WIZ-05-AUDIT: re-importar master via ImportMaster OU edição manual no admin (Phase 8 FORM-03 cobre coringas) — só se gaps significativos. Phase 10 WIZ-05 success criteria #5 já trata variant sem dado.
- [ ] FOLLOW-UP-PHASE-10: sincronizar `src/types/orcamento.ts:109` para incluir 'aprovado' e 'pendente' + regenerar `src/integrations/supabase/types.ts` (D-13)

## Downstream impact

- **Phase 9 (RLS multi-tenancy)**: pode escrever policies `USING (user_id = auth.uid() OR has_role(admin))` em arquitetos + clientes — coluna pronta
- **Phase 10 (WIZ)**: pode adicionar status badges + sync TS — CHECK pronta
- **Phase 12 (cron aniversário)**: pode escrever cron + edge function — `data_nascimento` pronta

## Files changed

- supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql (new)
- supabase/migrations/20260511000002_clientes_data_nascimento.sql (new)
- supabase/migrations/20260511000003_orcamentos_status_enum.sql (new)
- .planning/phases/07-schema-prep-v1-1/07-AUDIT-PRODUCT-VARIANTS.md (new)
- .planning/phases/07-schema-prep-v1-1/07-PUSH-LOG.md (new)
- .planning/phases/07-schema-prep-v1-1/07-SMOKE-RESULTS.md (new)
- .planning/phases/07-schema-prep-v1-1/07-SUMMARY.md (new)

<hr/>
*Phase 7 fechada em YYYY-MM-DD. Próxima fase: 8 ou 9 (paralelizáveis).*
```

Atualizar com números reais e timestamps reais ao escrever.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-schema-prep-v1-1/07-SUMMARY.md && grep -q "Requirements delivered: RLS-03, AUTO-03" .planning/phases/07-schema-prep-v1-1/07-SUMMARY.md && grep -q "FOLLOW-UP" .planning/phases/07-schema-prep-v1-1/07-SUMMARY.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `.planning/phases/07-schema-prep-v1-1/07-SUMMARY.md` existe
    - `grep "RLS-03" .planning/phases/07-schema-prep-v1-1/07-SUMMARY.md` retorna pelo menos 1 match
    - `grep "AUTO-03" .planning/phases/07-schema-prep-v1-1/07-SUMMARY.md` retorna pelo menos 1 match
    - `grep -E "D-0[1-9]|D-1[0-9]|D-2[0-3]" .planning/phases/07-schema-prep-v1-1/07-SUMMARY.md | wc -l` retorna pelo menos 23 (todas D-XX referenciadas)
    - `grep "FOLLOW-UP" .planning/phases/07-schema-prep-v1-1/07-SUMMARY.md` retorna pelo menos 1 match
    - `grep "Phase 9\\|Phase 10\\|Phase 12" .planning/phases/07-schema-prep-v1-1/07-SUMMARY.md` retorna pelo menos 3 matches (downstream impact)
    - `grep "20260511000001\\|20260511000002\\|20260511000003" .planning/phases/07-schema-prep-v1-1/07-SUMMARY.md` retorna pelo menos 3 matches
  </acceptance_criteria>
  <done>SUMMARY consolida tudo: 23 D-XX, 2 requirements, 3 migrations, 3 docs auxiliares, FOLLOW-UPs claros, downstream impact mapeado.</done>
</task>

</tasks>

<verification>
- 4 documentos criados em `.planning/phases/07-schema-prep-v1-1/` (AUDIT, PUSH-LOG, SMOKE-RESULTS, SUMMARY)
- 3 migrations aplicadas em prod sem erro (output `supabase db push` confirma)
- Counts pré==pós para 4 tabelas; status='fechado' == 0 pós-push
- 4 smoke checks PASS (counts, JOIN, CHECK, Playwright)
- Todas as 23 decisões D-XX rastreadas no SUMMARY
- Requirements RLS-03 e AUTO-03 marcáveis como delivered
</verification>

<success_criteria>
- Schema de prod tem `arquitetos.user_id NOT NULL`, `clientes.user_id NOT NULL`, `clientes.data_nascimento NULL`, `orcamentos_status_check` ativo
- Linha com `status='fechado'` migrada para `'aprovado'`
- Wizard, login, admin, PDF v1/v2, Drive seguem funcionando (smoke confirmou)
- Phase 7 pronta para fechar; Phases 8 (paralelo), 9 (consome user_id), 10 (consome status), 12 (consome data_nascimento) desbloqueadas
</success_criteria>

<output>
Documentos da phase já criados durante a execução. ROADMAP.md update + commit é responsabilidade do orchestrator.
</output>
