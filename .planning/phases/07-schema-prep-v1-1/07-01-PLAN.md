---
phase: 07-schema-prep-v1-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql
autonomous: true
requirements: [RLS-03]

must_haves:
  truths:
    - "Tabelas arquitetos e clientes têm coluna user_id UUID NOT NULL em produção (após push do Plan 07-04)"
    - "user_id referencia auth.users(id) com ON DELETE RESTRICT"
    - "Linhas legadas têm user_id preenchido com o admin mais antigo"
    - "Pre-flight assert aborta a migration se não existir admin em user_roles + colaboradores"
    - "Index BTREE existe em arquitetos.user_id e clientes.user_id (Phase 9 vai filtrar por user_id = auth.uid())"
    - "RLS policies existentes (USING true) permanecem inalteradas (Phase 9 cuida das policies)"
  artifacts:
    - path: "supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql"
      provides: "ADD COLUMN user_id em arquitetos+clientes, pre-flight, backfill admin mais antigo, NOT NULL, indexes, comments"
      contains: "ALTER TABLE public.arquitetos ADD COLUMN user_id"
  key_links:
    - from: "20260511000001_arquitetos_clientes_user_id.sql"
      to: "auth.users(id)"
      via: "FK ON DELETE RESTRICT"
      pattern: "REFERENCES auth\\.users\\(id\\) ON DELETE RESTRICT"
    - from: "20260511000001_arquitetos_clientes_user_id.sql"
      to: "user_roles + colaboradores"
      via: "Pre-flight assert + backfill admin mais antigo"
      pattern: "INNER JOIN public\\.colaboradores c ON c\\.user_id = ur\\.user_id"
---

<objective>
Criar migration aditiva `20260511000001_arquitetos_clientes_user_id.sql` que adiciona `user_id UUID NOT NULL` em `arquitetos` e `clientes`, replicando exatamente o pattern Drive D-02 errata (validado em prod v1.0).

Purpose: Desbloquear Phase 9 (RLS multi-tenancy) sem mexer em UI. Phase 7 só prepara o schema — as RLS policies de `arquitetos`/`clientes` ficam para Phase 9 (D-06).
Output: 1 arquivo SQL pronto para `supabase db push` (executado no Plan 07-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-schema-prep-v1-1/07-CONTEXT.md
@supabase/migrations/20260504000001_drive_rls_user_id.sql
@supabase/migrations/20260213150619_c40f8f90-794b-4d08-90b8-635ef7968cc9.sql
@supabase/migrations/20260423000001_create_arquitetos.sql
@supabase/migrations/20260423000002_clientes_arquiteto_contato_cpf.sql

<interfaces>
<!-- Schema atual relevante -->

clientes (after v1.0 migrations):
  id UUID PK, nome TEXT NOT NULL, email TEXT, telefone TEXT,
  contato TEXT, cpf_cnpj TEXT, arquiteto_id UUID REFERENCES arquitetos(id),
  created_at TIMESTAMPTZ
  RLS: "Anyone can read clientes" USING (true) — NÃO MEXER nesta phase

arquitetos (created in 20260423000001):
  id UUID PK, nome TEXT NOT NULL, contato TEXT, created_at TIMESTAMPTZ
  RLS: "Anyone can read arquitetos" USING (true) + "Admins can manage arquitetos" — NÃO MEXER

user_roles (sistema de roles):
  user_id UUID, role TEXT ('admin' | ...)

colaboradores:
  id UUID PK, user_id UUID, nome TEXT, ..., created_at TIMESTAMPTZ
</interfaces>

<reference_template>
<!-- Drive D-02 pattern (Blocos 1-4 do `20260504000001_drive_rls_user_id.sql`) -->
<!-- IMPORTANTE: replicar APENAS Blocos 1-4 (pre-flight + ADD COLUMN + backfill + NOT NULL). -->
<!-- NÃO replicar Blocos 5-8 (RLS policies, bucket, storage policies) — Phase 7 NÃO faz policies (D-06). -->

Divergência consciente vs Drive:
- Drive usou ON DELETE SET NULL (arquivos órfãos viram NULL)
- Phase 7 usa ON DELETE RESTRICT (D-01: cliente/arquiteto carrega histórico de orçamentos; admin precisa reassignar antes de deletar auth.user)
</reference_template>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration arquitetos+clientes user_id (D-01..D-06)</name>
  <files>supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql</files>
  <read_first>
    - supabase/migrations/20260504000001_drive_rls_user_id.sql (TEMPLATE — copiar Blocos 1-4, IGNORAR Blocos 5-8)
    - supabase/migrations/20260213150619_c40f8f90-794b-4d08-90b8-635ef7968cc9.sql (definição clientes — NÃO tem user_id)
    - supabase/migrations/20260423000001_create_arquitetos.sql (definição arquitetos — NÃO tem user_id)
    - .planning/phases/07-schema-prep-v1-1/07-CONTEXT.md (D-01 a D-06)
  </read_first>
  <action>
Criar o arquivo `supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql` com EXATAMENTE este conteúdo (substitua nomes só se houver typo):

```sql
-- Phase 7 / Plan 01: ADD COLUMN user_id em arquitetos + clientes (RLS-03)
-- Refs: D-01 (FK ON DELETE RESTRICT — divergência Drive D-05 que usou SET NULL),
--       D-02 (backfill admin mais antigo — pattern Drive D-04),
--       D-03 (NOT NULL após backfill), D-04 (index BTREE),
--       D-05 (COMMENT citando fase + decisão),
--       D-06 (NÃO criar RLS policies — Phase 9 cuida disso).
--
-- IMPORTANTE: Esta migration NÃO mexe em policies. As policies "Anyone can read"
-- (clientes/arquitetos) e "Admins can manage arquitetos" permanecem inalteradas.
-- Phase 9 (RLS-01/RLS-02) vai trocar as policies para `user_id = auth.uid() OR has_role(admin)`.

BEGIN;

-- =========================================================================
-- Bloco 1 — Pre-flight assert (Drive Pitfall 6)
-- Aborta migration se não existir admin com colaborador. Backfill precisa de pelo menos 1.
-- =========================================================================
DO $$
DECLARE admin_count int;
BEGIN
  SELECT count(*) INTO admin_count
    FROM public.user_roles ur
    INNER JOIN public.colaboradores c ON c.user_id = ur.user_id
    WHERE ur.role = 'admin';
  IF admin_count = 0 THEN
    RAISE EXCEPTION 'Migration aborted: no admin user found (Drive Pitfall 6 — backfill needs at least one admin in user_roles + colaboradores)';
  END IF;
END $$;

-- =========================================================================
-- Bloco 2 — ADD COLUMN user_id (nullable inicialmente)
-- D-01: ON DELETE RESTRICT (não SET NULL como Drive). Cliente/arquiteto carrega histórico
-- de orçamentos; bloquear delete do auth.user até admin reassignar manualmente.
-- =========================================================================
ALTER TABLE public.arquitetos
  ADD COLUMN user_id uuid REFERENCES auth.users(id) ON DELETE RESTRICT;

ALTER TABLE public.clientes
  ADD COLUMN user_id uuid REFERENCES auth.users(id) ON DELETE RESTRICT;

CREATE INDEX idx_arquitetos_user_id ON public.arquitetos(user_id);
CREATE INDEX idx_clientes_user_id ON public.clientes(user_id);

COMMENT ON COLUMN public.arquitetos.user_id IS
  'auth.users.id do colaborador dono do arquiteto. Phase 7 RLS-03 / pattern Drive D-02 errata. Phase 9 cria policies USING (user_id = auth.uid() OR has_role admin).';
COMMENT ON COLUMN public.clientes.user_id IS
  'auth.users.id do colaborador dono do cliente. Phase 7 RLS-03 / pattern Drive D-02 errata. Phase 9 cria policies USING (user_id = auth.uid() OR has_role admin).';

-- =========================================================================
-- Bloco 3 — Backfill: legados → admin mais antigo (D-02 / Drive D-04)
-- ORDER BY created_at ASC LIMIT 1 garante determinismo se múltiplos admins existirem.
-- =========================================================================
WITH admin_user AS (
  SELECT ur.user_id
  FROM public.user_roles ur
  INNER JOIN public.colaboradores c ON c.user_id = ur.user_id
  WHERE ur.role = 'admin'
  ORDER BY c.created_at ASC
  LIMIT 1
)
UPDATE public.arquitetos
   SET user_id = (SELECT user_id FROM admin_user)
 WHERE user_id IS NULL;

WITH admin_user AS (
  SELECT ur.user_id
  FROM public.user_roles ur
  INNER JOIN public.colaboradores c ON c.user_id = ur.user_id
  WHERE ur.role = 'admin'
  ORDER BY c.created_at ASC
  LIMIT 1
)
UPDATE public.clientes
   SET user_id = (SELECT user_id FROM admin_user)
 WHERE user_id IS NULL;

-- =========================================================================
-- Bloco 4 — NOT NULL após backfill (D-03)
-- =========================================================================
ALTER TABLE public.arquitetos ALTER COLUMN user_id SET NOT NULL;
ALTER TABLE public.clientes   ALTER COLUMN user_id SET NOT NULL;

-- NOTA: Sem RLS policies aqui (D-06). Phase 9 (RLS-01/RLS-02) cuida disso.
--       As policies atuais "Anyone can read" continuam ativas e não são afetadas.

COMMIT;
```

Notas de execução:
- O arquivo deve ser criado EM `supabase/migrations/` com EXATAMENTE o nome `20260511000001_arquitetos_clientes_user_id.sql`
- Use line endings LF (não CRLF) para consistência com migrations anteriores
- NÃO criar/dropar policies RLS — esse é trabalho do Plan 9-XX
- NÃO rodar `supabase db push` aqui — push é o Plan 07-04 (autonomous: false, Lenny executa)
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql && grep -c "ALTER TABLE public.arquitetos" supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql | grep -q "^[2-9]" && grep -c "ALTER TABLE public.clientes" supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql | grep -q "^[2-9]" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql` existe
    - `grep "RAISE EXCEPTION 'Migration aborted: no admin user found" supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql` retorna 1 match (pre-flight assert presente)
    - `grep "ADD COLUMN user_id uuid REFERENCES auth.users(id) ON DELETE RESTRICT" supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql` retorna 2 matches (arquitetos + clientes)
    - `grep "CREATE INDEX idx_arquitetos_user_id" supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql` retorna 1 match
    - `grep "CREATE INDEX idx_clientes_user_id" supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql` retorna 1 match
    - `grep "ALTER COLUMN user_id SET NOT NULL" supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql` retorna 2 matches
    - `grep "COMMENT ON COLUMN public.arquitetos.user_id" supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql` retorna 1 match
    - `grep "COMMENT ON COLUMN public.clientes.user_id" supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql` retorna 1 match
    - Arquivo começa com `BEGIN;` e termina com `COMMIT;` (atomicidade D-20)
    - Arquivo NÃO contém `CREATE POLICY` nem `DROP POLICY` (D-06: policies ficam pra Phase 9)
    - Arquivo NÃO contém `ENABLE ROW LEVEL SECURITY` (RLS já está habilitado em ambas tabelas)
  </acceptance_criteria>
  <done>Migration file salvo, atomicidade garantida via BEGIN/COMMIT, pre-flight + backfill + NOT NULL + indexes + comments todos presentes, ZERO policies criadas/dropadas.</done>
</task>

</tasks>

<verification>
- Arquivo `supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql` existe e passa todos os greps acima
- `grep -c "BEGIN;\|COMMIT;" supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql` retorna 2
- Sintaxe SQL válida (validation real só no `supabase db push` do Plan 07-04)
</verification>

<success_criteria>
- 1 migration file criado seguindo pattern Drive D-02 errata (Blocos 1-4 apenas)
- Pre-flight assert + backfill admin mais antigo + NOT NULL + 2 indexes + 2 comments
- ZERO RLS policies criadas/dropadas (D-06)
- Pronto para `supabase db push` no Plan 07-04
</success_criteria>

<output>
After completion, create `.planning/phases/07-schema-prep-v1-1/07-01-SUMMARY.md` documentando arquivo criado, blocos do template Drive replicados, decisões D-01..D-06 implementadas, e nota de que o push acontece no Plan 07-04.
</output>
