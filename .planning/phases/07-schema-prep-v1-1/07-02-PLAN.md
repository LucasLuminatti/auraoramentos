---
phase: 07-schema-prep-v1-1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260511000002_clientes_data_nascimento.sql
autonomous: true
requirements: [AUTO-03]

must_haves:
  truths:
    - "Tabela clientes tem coluna data_nascimento DATE NULL em produção (após push do Plan 07-04)"
    - "Coluna é nullable (sem default, sem backfill — preenchida via FORM-02 da Phase 8)"
    - "Index BTREE existe em clientes(data_nascimento) para uso na Phase 12 (cron de aniversário)"
    - "COMMENT na coluna cita Phase 7 AUTO-03 / cron Phase 12"
  artifacts:
    - path: "supabase/migrations/20260511000002_clientes_data_nascimento.sql"
      provides: "ADD COLUMN data_nascimento DATE NULL + index BTREE + comment"
      contains: "ALTER TABLE public.clientes"
  key_links:
    - from: "20260511000002_clientes_data_nascimento.sql"
      to: "Phase 12 (AUTO-01/AUTO-02 cron pg_cron)"
      via: "Coluna data_nascimento que o cron vai filtrar"
      pattern: "data_nascimento DATE"
---

<objective>
Criar migration aditiva `20260511000002_clientes_data_nascimento.sql` que adiciona `data_nascimento DATE NULL` em `clientes` mais index BTREE.

Purpose: Desbloquear Phase 12 (cron de aniversário). Campo nullable, sem backfill — admin preenche via FORM-02 da Phase 8.
Output: 1 arquivo SQL pronto para `supabase db push` (executado no Plan 07-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-schema-prep-v1-1/07-CONTEXT.md
@supabase/migrations/20260213150619_c40f8f90-794b-4d08-90b8-635ef7968cc9.sql
@supabase/migrations/20260423000002_clientes_arquiteto_contato_cpf.sql

<interfaces>
clientes (after v1.0 + Plan 07-01 push):
  id UUID PK, nome TEXT NOT NULL, email TEXT, telefone TEXT,
  contato TEXT, cpf_cnpj TEXT, arquiteto_id UUID REFERENCES arquitetos(id),
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE RESTRICT, -- Plan 07-01
  created_at TIMESTAMPTZ
  -- Esta migration adiciona: data_nascimento DATE NULL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration clientes.data_nascimento (D-07..D-09)</name>
  <files>supabase/migrations/20260511000002_clientes_data_nascimento.sql</files>
  <read_first>
    - supabase/migrations/20260423000002_clientes_arquiteto_contato_cpf.sql (exemplo de ALTER aditivo simples em clientes)
    - supabase/migrations/20260213150619_c40f8f90-794b-4d08-90b8-635ef7968cc9.sql (definição original clientes)
    - .planning/phases/07-schema-prep-v1-1/07-CONTEXT.md (D-07, D-08, D-09)
  </read_first>
  <action>
Criar `supabase/migrations/20260511000002_clientes_data_nascimento.sql` com EXATAMENTE este conteúdo:

```sql
-- Phase 7 / Plan 02: ADD COLUMN data_nascimento em clientes (AUTO-03 corolário)
-- Refs: D-07 (DATE NULL, sem default, sem backfill — admin preenche via FORM-02 Phase 8),
--       D-08 (index BTREE simples — sem index funcional MONTH/DAY, premature optimization),
--       D-09 (COMMENT cita fase + cron Phase 12).
--
-- Desbloqueia: Phase 12 (cron pg_cron diário + edge function Resend, AUTO-01/AUTO-02)
-- que filtra `WHERE EXTRACT(MONTH FROM data_nascimento) = ... AND EXTRACT(DAY FROM ...) = ...`

BEGIN;

ALTER TABLE public.clientes
  ADD COLUMN data_nascimento DATE;

CREATE INDEX idx_clientes_data_nascimento ON public.clientes(data_nascimento);

COMMENT ON COLUMN public.clientes.data_nascimento IS
  'Data de nascimento do cliente (opcional). Phase 7 AUTO-03 / consumido pelo cron de aniversário Phase 12 (AUTO-01/AUTO-02). Preenchimento via FORM-02 Phase 8.';

COMMIT;
```

Notas de execução:
- Sem `NOT NULL`, sem `DEFAULT` (D-07: campo opcional)
- Sem backfill (não há dado histórico para preencher)
- Index BTREE comum (D-08: index funcional MONTH/DAY fica para Phase 12 se profiling indicar)
- Use line endings LF
- NÃO rodar `supabase db push` aqui — Plan 07-04 cuida
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260511000002_clientes_data_nascimento.sql && grep -q "ADD COLUMN data_nascimento DATE" supabase/migrations/20260511000002_clientes_data_nascimento.sql && grep -q "CREATE INDEX idx_clientes_data_nascimento" supabase/migrations/20260511000002_clientes_data_nascimento.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260511000002_clientes_data_nascimento.sql` existe
    - `grep "ALTER TABLE public.clientes" supabase/migrations/20260511000002_clientes_data_nascimento.sql` retorna pelo menos 1 match
    - `grep "ADD COLUMN data_nascimento DATE" supabase/migrations/20260511000002_clientes_data_nascimento.sql` retorna 1 match
    - `grep -E "data_nascimento DATE\\s*(NULL|;|$)" supabase/migrations/20260511000002_clientes_data_nascimento.sql` retorna match (sem NOT NULL, sem DEFAULT)
    - `grep "CREATE INDEX idx_clientes_data_nascimento ON public.clientes(data_nascimento)" supabase/migrations/20260511000002_clientes_data_nascimento.sql` retorna 1 match
    - `grep "COMMENT ON COLUMN public.clientes.data_nascimento" supabase/migrations/20260511000002_clientes_data_nascimento.sql` retorna 1 match
    - Arquivo começa com `BEGIN;` e termina com `COMMIT;`
    - Arquivo NÃO contém `NOT NULL` na linha do ADD COLUMN nem DEFAULT
  </acceptance_criteria>
  <done>Migration trivial e aditiva criada, sem default/backfill, index BTREE pronto, comment rastreável.</done>
</task>

</tasks>

<verification>
- Arquivo existe, BEGIN/COMMIT presentes, ADD COLUMN sem NOT NULL/DEFAULT, index criado, comment com rastreabilidade
</verification>

<success_criteria>
- 1 migration file aditivo criado, pronto para push no Plan 07-04
</success_criteria>

<output>
After completion, create `.planning/phases/07-schema-prep-v1-1/07-02-SUMMARY.md` documentando arquivo criado e decisões D-07..D-09 implementadas.
</output>
