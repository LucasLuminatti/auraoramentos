---
phase: 04-drive-rls-reorganiza-o-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260504000001_drive_rls_user_id.sql
  - src/integrations/supabase/types.ts
autonomous: false
requirements: [ACC-01, ACC-02, ACC-04]
must_haves:
  truths:
    - "Coluna user_id (uuid, FK auth.users) existe em cliente_arquivos e arquivo_pastas, NOT NULL após backfill"
    - "Bucket cliente-arquivos é privado (public = false)"
    - "RLS de cliente_arquivos e arquivo_pastas filtra por user_id = auth.uid() OR has_role(auth.uid(), 'admin')"
    - "Storage policies do bucket cliente-arquivos delegam acesso à tabela cliente_arquivos (Estratégia B — sem path-prefix)"
    - "Linhas legadas (sem user_id) foram atribuídas ao admin mais antigo via JOIN com user_roles role='admin'"
    - "supabase db push aplicou a migration sem erro"
    - "src/integrations/supabase/types.ts foi regenerado e contém user_id em cliente_arquivos / arquivo_pastas"
  artifacts:
    - path: "supabase/migrations/20260504000001_drive_rls_user_id.sql"
      provides: "Migration aditiva: ADD COLUMN user_id, backfill, NOT NULL, RLS, bucket privado, storage policies"
      contains: "ALTER TABLE public.cliente_arquivos ADD COLUMN user_id"
    - path: "src/integrations/supabase/types.ts"
      provides: "Types regenerados após db push (user_id em cliente_arquivos e arquivo_pastas)"
      contains: "user_id"
  key_links:
    - from: "supabase/migrations/20260504000001_drive_rls_user_id.sql"
      to: "auth.users(id)"
      via: "FK em user_id (cliente_arquivos e arquivo_pastas)"
      pattern: "REFERENCES auth\\.users"
    - from: "RLS policies"
      to: "has_role(auth.uid(), 'admin')"
      via: "função SECURITY DEFINER já existente"
      pattern: "has_role\\(auth\\.uid\\(\\), 'admin'\\)"
    - from: "Storage policies (storage.objects)"
      to: "cliente_arquivos.arquivo_path"
      via: "EXISTS subquery para validar dono via tabela"
      pattern: "EXISTS \\(SELECT 1 FROM public\\.cliente_arquivos"
---

<objective>
Foundation da Phase 4: aplicar migration aditiva que adiciona `user_id` (FK `auth.users(id)`) em `cliente_arquivos` e `arquivo_pastas`, faz backfill atribuindo legados ao admin mais antigo, marca colunas NOT NULL, recria RLS direta (`user_id = auth.uid() OR has_role(auth.uid(), 'admin')`), torna o bucket `cliente-arquivos` privado e instala storage policies que delegam controle de acesso à tabela `cliente_arquivos` (Estratégia B do RESEARCH — sem path-prefix, sem migrar paths antigos).

Purpose: ACC-01, ACC-02 e a base de ACC-04 dependem dessa coluna existir e da RLS estar correta. Toda a Phase 4 (Plans 02..05) lê estes contratos. Sem Wave 1 verde, nada do resto roda.

Output: Migration SQL aplicada em produção via `supabase db push`, types regenerados, bucket privado, RLS funcional. Manual smoke check (admin/colab) faz parte do checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-drive-rls-reorganiza-o-admin/04-CONTEXT.md
@.planning/phases/04-drive-rls-reorganiza-o-admin/04-RESEARCH.md

# Phase 3 patterns (RLS aditiva via has_role)
@supabase/migrations/20260501000001_products_and_variants.sql
@supabase/migrations/20260501000002_storage_bucket_produtos_imagens.sql

# Bucket original (vai mudar public=true → public=false)
@supabase/migrations/20260302192445_503d0a52-6648-487a-bc49-5ade350105cc.sql

# Função has_role (já existente, SECURITY DEFINER)
@supabase/migrations/20260218165401_f86d5757-ddaf-4bce-befb-c69015b62f13.sql

<interfaces>
<!-- Schema atual relevante (de src/integrations/supabase/types.ts) -->

cliente_arquivos (sem user_id hoje):
  id uuid PK
  cliente_id uuid (FK clientes)
  projeto_id uuid? (FK projetos)
  pasta_id uuid? (FK arquivo_pastas)
  nome text
  descricao text?
  categoria text
  arquivo_path text       -- usado para createSignedUrl no Plan 02
  arquivo_url text        -- legado público (vira "podre" pós-Phase 4)
  tamanho int
  created_at timestamptz

arquivo_pastas (sem user_id hoje):
  id uuid PK
  cliente_id uuid
  projeto_id uuid?
  pasta_pai_id uuid?
  nome text
  created_at timestamptz

colaboradores (referência — NÃO usar para RLS direta):
  id uuid PK              -- distinto de auth.uid()
  user_id uuid            -- FK auth.users.id (= auth.uid())

user_roles (para descobrir admin no backfill):
  user_id uuid
  role app_role            -- enum: 'admin' | 'user'

-- has_role(uuid, app_role) RETURNS boolean LANGUAGE sql STABLE SECURITY DEFINER
-- Já em uso em RLS de products/product_variants/produtos-imagens.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration drive_rls_user_id (aditiva, backfill, RLS, bucket privado, storage policies)</name>
  <files>supabase/migrations/20260504000001_drive_rls_user_id.sql</files>
  <action>
Criar arquivo `supabase/migrations/20260504000001_drive_rls_user_id.sql` com a sequência completa abaixo. Tudo dentro de `BEGIN; ... COMMIT;`.

**Header de comentário (D-XX referenciados):**
```sql
-- Phase 4 / Plan 01: RLS por user_id em cliente_arquivos + arquivo_pastas + bucket privado
-- Refs: D-02 (errata: user_id em vez de colaborador_id), D-03 (RLS direta com auth.uid()),
--       D-04 (backfill admin mais antigo), D-06 (bucket privado), D-09 (errata: storage policy via tabela)
-- Estratégia B do RESEARCH: storage policies delegam acesso à tabela cliente_arquivos.
```

**Bloco 1 — Pre-flight assert (Pitfall 6 do RESEARCH):**
```sql
-- Garante que existe pelo menos 1 admin com colaborador, senão backfill estoura NOT NULL
DO $$
DECLARE admin_count int;
BEGIN
  SELECT count(*) INTO admin_count
    FROM public.user_roles ur
    INNER JOIN public.colaboradores c ON c.user_id = ur.user_id
    WHERE ur.role = 'admin';
  IF admin_count = 0 THEN
    RAISE EXCEPTION 'Migration aborted: no admin user found (Pitfall 6 — backfill needs at least one admin in user_roles + colaboradores)';
  END IF;
END $$;
```

**Bloco 2 — ADD COLUMN user_id (nullable inicialmente):**
```sql
ALTER TABLE public.cliente_arquivos
  ADD COLUMN user_id uuid REFERENCES auth.users(id) ON DELETE SET NULL;

ALTER TABLE public.arquivo_pastas
  ADD COLUMN user_id uuid REFERENCES auth.users(id) ON DELETE SET NULL;

CREATE INDEX idx_cliente_arquivos_user_id ON public.cliente_arquivos(user_id);
CREATE INDEX idx_arquivo_pastas_user_id ON public.arquivo_pastas(user_id);

COMMENT ON COLUMN public.cliente_arquivos.user_id IS
  'auth.users.id do colaborador dono do arquivo. RLS direta: user_id = auth.uid() OR has_role(...,admin). Phase 4 D-02 errata.';
COMMENT ON COLUMN public.arquivo_pastas.user_id IS
  'auth.users.id do colaborador dono da pasta. Phase 4 D-02 errata.';
```

ON DELETE SET NULL (D-05 — não cascade; arquivos órfãos viram nulos e o passo de re-atribuição abaixo + WARN no app cobrem o caso). Lenny pode escolher reatribuir manualmente.

**Bloco 3 — Backfill: legados → admin mais antigo (D-04):**
```sql
-- Pega o admin mais antigo (LIMIT 1 ORDER BY created_at — Assumption A1 do RESEARCH)
WITH admin_user AS (
  SELECT ur.user_id
  FROM public.user_roles ur
  INNER JOIN public.colaboradores c ON c.user_id = ur.user_id
  WHERE ur.role = 'admin'
  ORDER BY c.created_at ASC
  LIMIT 1
)
UPDATE public.cliente_arquivos
   SET user_id = (SELECT user_id FROM admin_user)
 WHERE user_id IS NULL;

WITH admin_user AS (
  SELECT ur.user_id
  FROM public.user_roles ur
  INNER JOIN public.colaboradores c ON c.user_id = ur.user_id
  WHERE ur.role = 'admin'
  ORDER BY c.created_at ASC
  LIMIT 1
)
UPDATE public.arquivo_pastas
   SET user_id = (SELECT user_id FROM admin_user)
 WHERE user_id IS NULL;
```

**Bloco 4 — NOT NULL após backfill:**
```sql
ALTER TABLE public.cliente_arquivos ALTER COLUMN user_id SET NOT NULL;
ALTER TABLE public.arquivo_pastas    ALTER COLUMN user_id SET NOT NULL;
```

**Bloco 5 — RLS em cliente_arquivos (drop policies abertas, criar novas D-03):**
```sql
-- Drop policies legadas (nomes podem variar; usar IF EXISTS)
DROP POLICY IF EXISTS "Authenticated users can read cliente_arquivos" ON public.cliente_arquivos;
DROP POLICY IF EXISTS "Authenticated users can insert cliente_arquivos" ON public.cliente_arquivos;
DROP POLICY IF EXISTS "Authenticated users can update cliente_arquivos" ON public.cliente_arquivos;
DROP POLICY IF EXISTS "Authenticated users can delete cliente_arquivos" ON public.cliente_arquivos;

ALTER TABLE public.cliente_arquivos ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Colabs read own arquivos, admins read all"
  ON public.cliente_arquivos FOR SELECT TO authenticated
  USING (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'));

CREATE POLICY "Colabs insert own arquivos"
  ON public.cliente_arquivos FOR INSERT TO authenticated
  WITH CHECK (user_id = auth.uid());

CREATE POLICY "Colabs update own arquivos, admins update all"
  ON public.cliente_arquivos FOR UPDATE TO authenticated
  USING (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'))
  WITH CHECK (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'));

CREATE POLICY "Colabs delete own arquivos, admins delete all"
  ON public.cliente_arquivos FOR DELETE TO authenticated
  USING (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'));
```

**Bloco 6 — RLS em arquivo_pastas (mesmo pattern):**
```sql
DROP POLICY IF EXISTS "Authenticated users can read arquivo_pastas" ON public.arquivo_pastas;
DROP POLICY IF EXISTS "Authenticated users can insert arquivo_pastas" ON public.arquivo_pastas;
DROP POLICY IF EXISTS "Authenticated users can update arquivo_pastas" ON public.arquivo_pastas;
DROP POLICY IF EXISTS "Authenticated users can delete arquivo_pastas" ON public.arquivo_pastas;

ALTER TABLE public.arquivo_pastas ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Colabs read own pastas, admins read all"
  ON public.arquivo_pastas FOR SELECT TO authenticated
  USING (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'));

CREATE POLICY "Colabs insert own pastas"
  ON public.arquivo_pastas FOR INSERT TO authenticated
  WITH CHECK (user_id = auth.uid());

CREATE POLICY "Colabs update own pastas, admins update all"
  ON public.arquivo_pastas FOR UPDATE TO authenticated
  USING (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'))
  WITH CHECK (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'));

CREATE POLICY "Colabs delete own pastas, admins delete all"
  ON public.arquivo_pastas FOR DELETE TO authenticated
  USING (user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'));
```

**Bloco 7 — Bucket privado (D-06):**
```sql
UPDATE storage.buckets SET public = false WHERE id = 'cliente-arquivos';
```

**Bloco 8 — Storage policies (D-09 errata: via tabela cliente_arquivos, NÃO path-prefix):**
```sql
-- Drop policies abertas/antigas do bucket cliente-arquivos
DROP POLICY IF EXISTS "Public can view cliente-arquivos" ON storage.objects;
DROP POLICY IF EXISTS "Authenticated can upload cliente-arquivos" ON storage.objects;
DROP POLICY IF EXISTS "Authenticated can delete cliente-arquivos" ON storage.objects;
DROP POLICY IF EXISTS "Authenticated can update cliente-arquivos" ON storage.objects;
-- Nomes possíveis das policies originais (migration 20260302192445):
DROP POLICY IF EXISTS "Anyone can view files" ON storage.objects;
DROP POLICY IF EXISTS "Authenticated users can upload files" ON storage.objects;
DROP POLICY IF EXISTS "Authenticated users can delete files" ON storage.objects;

-- SELECT (download via signed URL): permitido se há registro em cliente_arquivos com path == name
-- e o caller é dono OU admin. createSignedUrl chama com bearer do usuário, então auth.uid() funciona.
CREATE POLICY "Read cliente-arquivos via cliente_arquivos table"
  ON storage.objects FOR SELECT TO authenticated
  USING (
    bucket_id = 'cliente-arquivos'
    AND EXISTS (
      SELECT 1 FROM public.cliente_arquivos ca
      WHERE ca.arquivo_path = storage.objects.name
        AND (ca.user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'))
    )
  );

-- INSERT: qualquer authenticated pode subir blob; consistência (user_id no row da tabela)
-- é garantida pelo INSERT da app que roda na MESMA transação lógica.
CREATE POLICY "Authenticated upload cliente-arquivos"
  ON storage.objects FOR INSERT TO authenticated
  WITH CHECK (bucket_id = 'cliente-arquivos');

-- DELETE: dono ou admin via tabela cliente_arquivos
CREATE POLICY "Delete cliente-arquivos via cliente_arquivos table"
  ON storage.objects FOR DELETE TO authenticated
  USING (
    bucket_id = 'cliente-arquivos'
    AND EXISTS (
      SELECT 1 FROM public.cliente_arquivos ca
      WHERE ca.arquivo_path = storage.objects.name
        AND (ca.user_id = auth.uid() OR public.has_role(auth.uid(), 'admin'))
    )
  );
```

**Não faça:** path-prefix policies (Pitfall 3 do RESEARCH — quebra legados). Não migrar `arquivo_path` existente. Não fazer `UPDATE` em `arquivo_url` (legado vira podre conscientemente — D-08).
  </action>
  <verify>
    <automated>cat supabase/migrations/20260504000001_drive_rls_user_id.sql | grep -E "(ADD COLUMN user_id|UPDATE storage\\.buckets SET public = false|EXISTS \\(SELECT 1 FROM public\\.cliente_arquivos)" | wc -l</automated>
    Esperado: pelo menos 4 matches (2 ADD COLUMN + 1 UPDATE bucket + 2 EXISTS storage policies).
  </verify>
  <done>
    Arquivo `supabase/migrations/20260504000001_drive_rls_user_id.sql` existe, contém os 8 blocos (assert, ADD COLUMN, backfill, NOT NULL, RLS x2 tabelas, bucket privado, storage policies via tabela).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] supabase db push + regenerar types</name>
  <what-built>Migration `20260504000001_drive_rls_user_id.sql` criada localmente.</what-built>
  <action>Pausa o executor até o usuário confirmar via resume-signal abaixo. Ver instruções em &lt;how-to-verify&gt;.</action>
  <how-to-verify>
    Executar localmente (Lenny acompanha o terminal):

    1. `cd C:\Users\lenny\Desktop\Luminatti\automa_aura\auraoramentos`
    2. `npx supabase db push` — confirmar diff e aceitar (deve mostrar a nova migration `20260504000001`).
    3. Se push ok, regenerar types:
       `npx supabase gen types typescript --project-id jkewlaezvrbuicmncqbj > src/integrations/supabase/types.ts`
    4. Verificar via SQL editor (https://supabase.com/dashboard/project/jkewlaezvrbuicmncqbj/sql/new):
       ```sql
       SELECT count(*) FILTER (WHERE user_id IS NULL) AS null_count, count(*) AS total
         FROM public.cliente_arquivos;
       SELECT count(*) FILTER (WHERE user_id IS NULL) AS null_count, count(*) AS total
         FROM public.arquivo_pastas;
       SELECT public FROM storage.buckets WHERE id = 'cliente-arquivos';
       ```
       Esperado: `null_count = 0` em ambas as tabelas (NOT NULL impede outro resultado), `public = false` no bucket.
    5. Smoke RLS: logar como colaborador NÃO-admin e abrir Drive → deve continuar mostrando o que vê hoje (admin é dono dos legados, então colab vê apenas o próprio — nada para colab novo). Logar como Lenny → continua vendo tudo.

    **Se algo falhar:** revisar log de `db push`, ajustar migration, repetir. NÃO seguir para Wave 2 antes de tudo verde.
  </how-to-verify>
  <resume-signal>Digite "migration aplicada" ou descreva o erro encontrado.</resume-signal>
  <done>
    `db push` executado com sucesso, `types.ts` atualizado contém `user_id` em `cliente_arquivos` e `arquivo_pastas`, smoke check confirma RLS aplicada (admin vê tudo, colab apenas próprio).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → Supabase REST/Storage | Caller pode ser colab autenticado ou admin; RLS é a única defesa |
| Storage objects → cliente_arquivos table | Storage policies fazem JOIN com tabela; tabela é fonte da verdade de dono |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | cliente_arquivos / arquivo_pastas SELECT | mitigate | RLS direta `user_id = auth.uid() OR has_role(...,admin)` substitui USING(true) atual; ASVS V4.1 |
| T-04-02 | Elevation of Privilege | INSERT em cliente_arquivos | mitigate | Policy WITH CHECK `user_id = auth.uid()` impede colab plantar registro de outro user; ASVS V4.2 |
| T-04-03 | Information Disclosure | bucket cliente-arquivos GET público | mitigate | `UPDATE storage.buckets SET public = false` + signed URLs 24h (Plan 02); ASVS Storage Access Control |
| T-04-04 | Information Disclosure | Storage SELECT objetos sem path-prefix | mitigate | Storage policy SELECT delega a EXISTS na tabela `cliente_arquivos` com mesma cláusula RLS |
| T-04-05 | Tampering | Backfill atribui legados a admin errado se houver múltiplos | accept | A1 do RESEARCH: hoje só Lenny é admin; ORDER BY created_at ASC LIMIT 1 garante determinismo se múltiplos |
| T-04-06 | Repudiation | URLs públicas antigas em PDFs persistidos | accept | D-08 — decisão consciente; bucket privado torna URL antiga inválida (HTTP 400), sem vazamento |
| T-04-07 | Denial of Service | EXISTS subquery em storage policy adiciona latência por GET | accept | Volume baixo (<100 arquivos por cliente/projeto); índice em `arquivo_path` se necessário no futuro |
</threat_model>

<verification>
- Migration aplicada via `supabase db push` sem erro
- `SELECT public FROM storage.buckets WHERE id='cliente-arquivos'` retorna `false`
- `cliente_arquivos.user_id` e `arquivo_pastas.user_id` ambos NOT NULL com 0 nulls após backfill
- `pg_policies` lista as 4 novas policies em cada tabela do Drive
- `pg_policies` em `storage.objects` mostra as 3 novas policies (SELECT/INSERT/DELETE) para `cliente-arquivos`
- `src/integrations/supabase/types.ts` regenerado contém `user_id: string` em `cliente_arquivos` e `arquivo_pastas`
- Smoke manual: admin vê tudo, colab novo vê vazio (porque legados pertencem ao admin)
</verification>

<success_criteria>
1. Migration aplicada e idempotente (DROP POLICY IF EXISTS protege re-execução em ambientes locais)
2. Bucket `cliente-arquivos` privado, sem perda de objetos
3. RLS direta funcional em ambas as tabelas
4. Storage policies não usam path-prefix (Pitfall 3 evitado)
5. Types regenerados — Plan 02 pode importar `user_id` sem erro de TypeScript
</success_criteria>

<output>
Após completar, criar `.planning/phases/04-drive-rls-reorganiza-o-admin/04-01-SUMMARY.md` com:
- Migration ID aplicada
- Counts pós-backfill (legados atribuídos)
- Confirmação de bucket privado
- Path para types regenerados
- Notas para Plan 02 (createSignedUrl + INSERT com user_id)
</output>
