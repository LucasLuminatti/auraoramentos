---
phase: 04-drive-rls-reorganiza-o-admin
plan: 05
type: execute
wave: 5
depends_on: [04-03, 04-04]
files_modified:
  - src/pages/OrcamentoDetalhe.tsx
  - src/App.tsx
  - src/pages/Admin.tsx
autonomous: false
requirements: [ADM-01]
must_haves:
  truths:
    - "Rota /admin/orcamento/:id existe protegida por AdminRoute"
    - "Página OrcamentoDetalhe mostra cliente, arquiteto (via clientes.arquitetos), colaborador, projeto, status, totais e snapshot ambientes (D-20)"
    - "Página mostra histórico de exceções (price_exceptions filtradas por orcamento_id) quando houver"
    - "Botão 'Re-emitir PDF' chama gerarPdfHtml com PdfParams montado a partir do snapshot e baixa o PDF (D-19)"
    - "Botão 'Voltar para lista' navega para /admin?tab=pedidos (D-21)"
    - "Linha da tabela de Pedidos no Admin é clicável e leva para /admin/orcamento/:id (fecha o todo open desde 2026-04-27)"
    - "Snapshot antigo (sem campos novos) renderiza sem crash via optional chaining"
  artifacts:
    - path: "src/pages/OrcamentoDetalhe.tsx"
      provides: "Página read-only com fetch de orcamento + clientes(arquitetos) + colaboradores + projetos + price_exceptions + Re-emitir PDF"
      contains: "OrcamentoDetalhe"
    - path: "src/App.tsx"
      provides: "Rota nova /admin/orcamento/:id protegida por AdminRoute"
      contains: "/admin/orcamento/:id"
    - path: "src/pages/Admin.tsx"
      provides: "Tabela de Pedidos com linha clicável que navega para a página detalhe"
      contains: "/admin/orcamento/"
  key_links:
    - from: "src/App.tsx Routes"
      to: "src/pages/OrcamentoDetalhe.tsx"
      via: "<Route path='/admin/orcamento/:id' element={<AdminRoute><OrcamentoDetalhe /></AdminRoute>} />"
      pattern: "/admin/orcamento/:id"
    - from: "src/pages/OrcamentoDetalhe.tsx Re-emitir PDF"
      to: "src/lib/gerarPdfHtml.ts gerarPdfHtml"
      via: "PdfParams montado a partir do snapshot orcamento.ambientes + clientes/colaboradores"
      pattern: "gerarPdfHtml\\("
    - from: "src/pages/Admin.tsx TabsContent value='pedidos' TableRow"
      to: "/admin/orcamento/:id"
      via: "navigate(`/admin/orcamento/${o.id}`) no onClick da linha"
      pattern: "navigate\\(`/admin/orcamento/"
---

<objective>
Entregar ADM-01: página dedicada `/admin/orcamento/:id` (read-only, exceto botão Re-emitir PDF) que mostra cliente + arquiteto (via cliente) + colaborador + projeto + status + ambientes (do snapshot) + totais + histórico de exceções. Tornar linha de Pedidos clicável (também fecha o todo `2026-04-27-admin-orcamentos-row-nao-clicavel`).

Decisões locked: D-18 (página dedicada, não modal), D-19 (read-only + Re-emitir PDF), D-20 (conteúdo), D-21 (Voltar leva para /admin?tab=pedidos).

Output: Componente novo + nova rota + linha clicável.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-drive-rls-reorganiza-o-admin/04-CONTEXT.md
@.planning/phases/04-drive-rls-reorganiza-o-admin/04-RESEARCH.md
@.planning/phases/04-drive-rls-reorganiza-o-admin/04-03-SUMMARY.md

# Todo a fechar
@.planning/todos/pending/2026-04-27-admin-orcamentos-row-nao-clicavel.md

@src/App.tsx
@src/pages/Admin.tsx
@src/lib/gerarPdfHtml.ts
@src/integrations/supabase/types.ts

<interfaces>
<!-- gerarPdfHtml signature (verificado em RESEARCH Área 7): -->
interface PdfParams {
  clienteNome: string;
  projetoNome: string;
  colaborador: string;
  tipo: string;
  ambientes: Ambiente[];      // do snapshot orcamentos.ambientes (Json)
  logoBase64?: string;
}
function gerarPdfHtml(params: PdfParams): Promise<void>;  // baixa o PDF

<!-- Schema relevante (do types.ts): -->
orcamentos: {
  id: string,
  cliente_id: string,
  projeto_id: string | null,
  colaborador_id: string,         // FK colaboradores.id (NÃO é auth.uid())
  data: string,
  valor: number,
  status: 'rascunho' | 'enviado' | 'aprovado' | 'fechado' | 'perdido',
  tipo: string,                   // 'Primeiro Orçamento' | 'Revisão 01' | ...
  ambientes: Json,                // snapshot completo
  motivo_perda: string | null,
  fechado_at: string | null,
  created_at: string,
}

clientes: { id, nome, email, telefone, contato, cpf_cnpj, arquiteto_id (FK arquitetos) }
arquitetos: { id, nome, contato }
colaboradores: { id, nome, ... }
projetos: { id, nome }

price_exceptions: { id, orcamento_id, produto_codigo, produto_descricao, preco_solicitado, preco_minimo, status, solicitante_id, created_at }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar OrcamentoDetalhe.tsx (fetch + render + Re-emitir PDF)</name>
  <files>src/pages/OrcamentoDetalhe.tsx</files>
  <action>
Criar `src/pages/OrcamentoDetalhe.tsx`. Estrutura:

**Imports:**
```typescript
import { useEffect, useState } from "react";
import { useParams, useNavigate } from "react-router-dom";
import { supabase } from "@/integrations/supabase/client";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { ArrowLeft, FileDown, Loader2 } from "lucide-react";
import { format } from "date-fns";
import { ptBR } from "date-fns/locale";
import { toast } from "sonner";
import { gerarPdfHtml } from "@/lib/gerarPdfHtml";
import logo from "@/assets/logo.png";
import type { Ambiente } from "@/types/orcamento";
```

**Tipos locais:**
```typescript
interface OrcamentoFull {
  id: string;
  data: string;
  valor: number;
  status: string;
  tipo: string;
  ambientes: Ambiente[];          // parseado do JSON
  motivo_perda: string | null;
  fechado_at: string | null;
  created_at: string;
  clientes: {
    nome: string;
    email: string | null;
    telefone: string | null;
    contato: string | null;
    cpf_cnpj: string | null;
    arquitetos: { nome: string; contato: string | null } | null;
  } | null;
  colaboradores: { nome: string } | null;
  projetos: { nome: string } | null;
}

interface ExceptionRow {
  id: string;
  produto_codigo: string;
  produto_descricao: string;
  preco_solicitado: number;
  preco_minimo: number;
  status: string;
  created_at: string;
}
```

**Componente:**

```typescript
const OrcamentoDetalhe = () => {
  const { id } = useParams<{ id: string }>();
  const navigate = useNavigate();
  const [orc, setOrc] = useState<OrcamentoFull | null>(null);
  const [exceptions, setExceptions] = useState<ExceptionRow[]>([]);
  const [loading, setLoading] = useState(true);
  const [logoBase64, setLogoBase64] = useState<string>("");
  const [gerando, setGerando] = useState(false);

  useEffect(() => {
    if (!id) return;
    const load = async () => {
      setLoading(true);
      // Fetch orcamento + relations
      const { data, error } = await supabase
        .from("orcamentos")
        .select(`
          id, data, valor, status, tipo, ambientes, motivo_perda, fechado_at, created_at,
          clientes ( nome, email, telefone, contato, cpf_cnpj,
            arquitetos ( nome, contato )
          ),
          colaboradores ( nome ),
          projetos ( nome )
        `)
        .eq("id", id)
        .single();
      if (error || !data) {
        toast.error("Erro ao carregar orçamento");
        setLoading(false);
        return;
      }
      // ambientes vem como Json — parsear defensivamente (Pitfall 8 do RESEARCH)
      const ambientesParsed = Array.isArray(data.ambientes) ? data.ambientes : [];
      setOrc({ ...data, ambientes: ambientesParsed } as OrcamentoFull);

      // Fetch exceptions
      const { data: exData } = await supabase
        .from("price_exceptions")
        .select("id, produto_codigo, produto_descricao, preco_solicitado, preco_minimo, status, created_at")
        .eq("orcamento_id", id)
        .order("created_at", { ascending: true });
      setExceptions((exData ?? []) as ExceptionRow[]);

      setLoading(false);
    };
    load();
  }, [id]);

  // Carrega logo em base64 para o PDF (mesmo padrão que o Step3 usa)
  useEffect(() => {
    fetch(logo)
      .then((r) => r.blob())
      .then((b) => {
        const reader = new FileReader();
        reader.onloadend = () => setLogoBase64(reader.result as string);
        reader.readAsDataURL(b);
      })
      .catch(() => {});
  }, []);

  const handleReemitirPdf = async () => {
    if (!orc) return;
    setGerando(true);
    try {
      await gerarPdfHtml({
        clienteNome: orc.clientes?.nome ?? "—",
        projetoNome: orc.projetos?.nome ?? "—",
        colaborador: orc.colaboradores?.nome ?? "—",
        tipo: orc.tipo,
        ambientes: orc.ambientes ?? [],
        logoBase64: logoBase64 || undefined,
      });
      toast.success("PDF gerado");
    } catch (e) {
      toast.error("Erro ao gerar PDF");
    } finally {
      setGerando(false);
    }
  };

  // ... statusLabel/statusClass copiar do Admin.tsx
```

**JSX:**

Header (sticky top com logo + título + botões):
```tsx
<header className="border-b bg-card shadow-sm">
  <div className="mx-auto flex max-w-6xl items-center justify-between px-4 py-3">
    <div className="flex items-center gap-3">
      <img src={logo} alt="Logo" className="h-12 w-auto" />
      <span className="text-lg font-semibold">Detalhes do Pedido</span>
    </div>
    <div className="flex items-center gap-2">
      <Button variant="outline" size="sm" onClick={() => navigate("/admin?tab=pedidos")}>
        <ArrowLeft className="mr-1 h-4 w-4" /> Voltar para lista
      </Button>
      <Button size="sm" onClick={handleReemitirPdf} disabled={gerando || !orc} className="gap-1.5">
        {gerando ? <Loader2 className="h-4 w-4 animate-spin" /> : <FileDown className="h-4 w-4" />}
        Re-emitir PDF
      </Button>
    </div>
  </div>
</header>
```

Body (`<main className="mx-auto max-w-6xl px-4 py-6 space-y-6">`):

- **Loading state:** se `loading`, renderizar `<div className="flex items-center gap-2 text-muted-foreground"><Loader2 className="h-4 w-4 animate-spin" />Carregando...</div>`.
- **Not found:** se `!orc`, mostrar Card "Orçamento não encontrado".

Cards na ordem:
1. **Card "Resumo"**: Status (Badge com cor), Tipo, Data, Valor (R$ formatado), Fechado em (se houver), Motivo de perda (se houver).
2. **Card "Cliente e Arquiteto"**: Nome cliente / contato / email / telefone / CPF-CNPJ / Arquiteto (nome + contato) — usar optional chaining.
3. **Card "Projeto e Colaborador"**: nome do projeto + nome do colaborador responsável.
4. **Card "Ambientes"**: para cada `ambiente` em `orc.ambientes`, mostrar nome + lista de luminárias (codigo, descrição, qty, preço unit, total) + lista de sistemas (fita + driver + perfil opcional, com totais). Render robusto com `?? []` em todos os arrays para snapshots antigos.
5. **Card "Histórico de Exceções"** (renderiza só se `exceptions.length > 0`): tabela com Data / Produto (código + descrição) / Preço solicitado / Preço mínimo / Status (Badge).

Exemplos de render dos ambientes:
```tsx
{orc.ambientes.map((amb, i) => (
  <div key={amb.id ?? i} className="border rounded-lg p-3">
    <h4 className="font-semibold mb-2">{amb.nome ?? `Ambiente ${i + 1}`}</h4>
    {(amb.luminarias ?? []).length > 0 && (
      <Table>...</Table>
    )}
    {(amb.sistemas ?? []).length > 0 && (
      <div className="mt-2 space-y-2">
        {amb.sistemas.map((s, j) => (
          <div key={s.id ?? j} className="text-sm">
            <strong>Sistema {j + 1}:</strong> Fita {s.fita?.codigo ?? "—"}, Driver {s.driver?.codigo ?? "—"}
            {s.perfil && `, Perfil ${s.perfil.codigo}`}
          </div>
        ))}
      </div>
    )}
  </div>
))}
```

**Default export:**
```typescript
export default OrcamentoDetalhe;
```

**Não faça:** edição de campos (D-19 read-only). Não recalcular preços a partir do produto atual — usar valores do snapshot. Não fazer fetch de useUserRole — AdminRoute já protege. Não criar tipos novos em `@/types/orcamento.ts` — usar os existentes.
  </action>
  <verify>
    <automated>cd auraoramentos && test -f src/pages/OrcamentoDetalhe.tsx && grep -c "gerarPdfHtml" src/pages/OrcamentoDetalhe.tsx && grep -c "/admin?tab=pedidos" src/pages/OrcamentoDetalhe.tsx</automated>
    Esperado: arquivo existe, gerarPdfHtml ≥ 1, /admin?tab=pedidos ≥ 1.
  </verify>
  <done>
    - `src/pages/OrcamentoDetalhe.tsx` existe e exporta default
    - Fetch usa join clientes(arquitetos) + colaboradores + projetos + price_exceptions
    - Re-emitir PDF chama `gerarPdfHtml` com `PdfParams` montado do snapshot
    - Voltar navega para `/admin?tab=pedidos`
    - Render robusto com optional chaining (Pitfall 8)
    - `npm run build` verde
  </done>
</task>

<task type="auto">
  <name>Task 2: Adicionar rota /admin/orcamento/:id em App.tsx</name>
  <files>src/App.tsx</files>
  <action>
Em `src/App.tsx`:

1. Adicionar import: `import OrcamentoDetalhe from "./pages/OrcamentoDetalhe";`
2. Dentro do `<Routes>`, **logo após** `<Route path="/admin/upload-imagens" ... />` (mantendo grupo das rotas admin), adicionar:
```tsx
<Route path="/admin/orcamento/:id" element={<AdminRoute><OrcamentoDetalhe /></AdminRoute>} />
```

NÃO mexer em mais nada.
  </action>
  <verify>
    <automated>cd auraoramentos && grep -c "/admin/orcamento/:id" src/App.tsx && grep -c "OrcamentoDetalhe" src/App.tsx</automated>
    Esperado: ambos ≥ 1.
  </verify>
  <done>
    - Rota `/admin/orcamento/:id` registrada e protegida por `AdminRoute`
    - Import de OrcamentoDetalhe adicionado
    - `npm run build` verde
  </done>
</task>

<task type="auto">
  <name>Task 3: Tornar linha de Pedidos clicável em Admin.tsx</name>
  <files>src/pages/Admin.tsx</files>
  <action>
Em `src/pages/Admin.tsx`, dentro do `TabsContent value="pedidos"` (criado pelo Plan 03):

1. Localizar o `<TableRow key={o.id}>` dentro do map de `orcamentos`.
2. Adicionar `onClick`, `role="button"`, `className="cursor-pointer hover:bg-muted/50"`:
```tsx
<TableRow
  key={o.id}
  role="button"
  className="cursor-pointer hover:bg-muted/50"
  onClick={() => navigate(`/admin/orcamento/${o.id}`)}
>
```
3. **Importante:** o botão `<Flag>` "Encerrar negociação" dentro da TableCell de Ações precisa **stopPropagation** para não navegar quando clicado:
```tsx
<button
  className="p-1 rounded hover:bg-muted transition-colors"
  title="Encerrar negociação"
  onClick={(e) => {
    e.stopPropagation();    // ← novo: impede navegação
    setEncerrarOrcId(o.id);
    setEncerrarOpen(true);
  }}
>
```

NÃO mexer em mais nada.
  </action>
  <verify>
    <automated>cd auraoramentos && grep -c 'navigate(`/admin/orcamento/' src/pages/Admin.tsx && grep -c "stopPropagation" src/pages/Admin.tsx</automated>
    Esperado: ambos ≥ 1.
  </verify>
  <done>
    - TableRow de pedido tem onClick que navega para `/admin/orcamento/${o.id}`
    - cursor-pointer + hover state aplicados
    - Botão Flag (encerrar) tem `e.stopPropagation()` para não conflitar
    - `npm run build` verde
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Smoke OrcamentoDetalhe via Playwright MCP</name>
  <what-built>Página /admin/orcamento/:id read-only com Re-emitir PDF + linha clicável em Pedidos.</what-built>
  <action>Pausa o executor até o usuário confirmar via resume-signal abaixo. Ver instruções em &lt;how-to-verify&gt;.</action>
  <how-to-verify>
    Pipeline automático:

    1. **Code review** sobre OrcamentoDetalhe.tsx + App.tsx + Admin.tsx diff.
    2. **Playwright MCP** (npm run dev port 8080):
       - Login admin → /admin?tab=pedidos → ver tabela.
       - Hover sobre uma linha → cursor:pointer + bg muted.
       - Clicar em uma linha → navegar para /admin/orcamento/{id}. Página renderiza com 4-5 cards (Resumo / Cliente e Arquiteto / Projeto e Colaborador / Ambientes / Histórico se houver).
       - Verificar Cliente Card: nome + email + telefone + arquiteto (nome+contato se preenchido).
       - Ambientes: ver lista de luminárias e sistemas (fita+driver) com códigos do snapshot.
       - Botão "Re-emitir PDF": clicar → PDF baixa (`browser_handle_dialog` ou verificar download em /downloads).
       - Botão "Voltar para lista": clicar → URL volta para /admin?tab=pedidos com filtro/scroll preservado.
       - Repetir clique em pedido com `status=enviado/aprovado` que tem o botão Flag → clicar no Flag → modal de Encerrar abre **sem navegar para detalhes** (stopPropagation funcional).
       - Carregar /admin/orcamento/{id-de-orcamento-antigo-snapshot} → não pode crashar (Pitfall 8). Render mesmo com luminarias/sistemas vazios ou ausentes.
    3. Console JS: 0 erros vermelhos.
  </how-to-verify>
  <resume-signal>Digite "orcamento detalhe ok" ou descreva o problema.</resume-signal>
  <done>
    Code review passou; navegação clique-para-detalhe funciona; flag stopPropagation correto; PDF re-emite; voltar funciona; snapshot antigo não crasha.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL param :id → fetch orcamentos | ID arbitrário do user; RLS de orcamentos cobre (admin vê tudo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Information Disclosure | Não-admin acessa /admin/orcamento/:id | mitigate | AdminRoute já bloqueia e redireciona; ASVS V4.1 |
| T-04-20 | Tampering | URL ID inválido (UUID malformed) | mitigate | `.single()` retorna error; toast + render "Orçamento não encontrado" |
| T-04-21 | Spoofing | Snapshot adulterado em DB tem campos faltando e crasha render | mitigate | optional chaining em todos os acessos (Pitfall 8); `?? []` em arrays |
| T-04-22 | Information Disclosure | console.log de orcamento completo expõe dados de cliente | mitigate | Não logar `orc` ou `data` em console.log |
</threat_model>

<verification>
- `npm run build` verde
- Smoke Playwright: navegação, render de 4 cards, PDF re-emite, voltar, stopPropagation, snapshot antigo
- todo `2026-04-27-admin-orcamentos-row-nao-clicavel.md` movido para `done/` no Plan 06 (docs closure)
</verification>

<success_criteria>
1. ADM-01 satisfeito (D-18..D-21): página dedicada read-only, Re-emitir PDF, Voltar preserva filtro
2. Linha de Pedidos clicável fecha o todo aberto
3. Snapshot antigo não crasha
4. AdminRoute protege a página
</success_criteria>

<output>
Após completar, criar `.planning/phases/04-drive-rls-reorganiza-o-admin/04-05-SUMMARY.md` com:
- Componente criado + estrutura
- Cobertura D-XX (18,19,20,21)
- Confirmação row-clicável + Flag stopPropagation
- Notas para Plan 06 fechar todo `2026-04-27-admin-orcamentos-row-nao-clicavel.md`
</output>
