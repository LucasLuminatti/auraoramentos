---
phase: 04-drive-rls-reorganiza-o-admin
plan: 03
type: execute
wave: 3
depends_on: [04-01]
files_modified:
  - src/pages/Admin.tsx
  - src/components/AdminDashboard.tsx
  - src/components/AdminExceptions.tsx
autonomous: false
requirements: [ADM-03, ADM-04, ADM-05]
must_haves:
  truths:
    - "Admin tem 5 sub-tabs no nível superior: Início / Cadastros / Pedidos / Preços / Exceções"
    - "Cadastros tem sub-tabs: Produtos / Arquitetos / Clientes / Colaboradores"
    - "Preços tem sub-tabs: Atualização / Importação"
    - "URL state ?tab=X&sub=Y persiste seleção e suporta reload"
    - "Sub-tab Início renderiza AdminDashboard simplificado (sem 'Distribuição por Status')"
    - "Aba Exceções tem bloco de ajuda inline (Card) explicando o fluxo de exceção (D-22)"
    - "AdminDashboard não importa nem renderiza PieChart/Pie/Cell/statusData"
    - "Tabs antigas (?tab=produtos / ?tab=clientes etc) são redirecionadas ou caem em default sem crash"
  artifacts:
    - path: "src/pages/Admin.tsx"
      provides: "Estrutura de 5 sub-tabs com sub-tabs aninhadas (Cadastros + Preços) + URL state ?tab=X&sub=Y"
      contains: "TabsList"
    - path: "src/components/AdminDashboard.tsx"
      provides: "Dashboard simplificado sem Distribuição por Status (D-25)"
      contains: "Receita Mensal"
    - path: "src/components/AdminExceptions.tsx"
      provides: "Bloco Card de ajuda fixo no topo explicando fluxo de exceção (D-22/D-23)"
      contains: "fluxo de exceção"
  key_links:
    - from: "src/pages/Admin.tsx Tabs onValueChange"
      to: "useSearchParams setSearchParams"
      via: "?tab=X&sub=Y"
      pattern: "setSearchParams\\(\\{.*tab.*sub"
    - from: "TabsContent value='inicio'"
      to: "<AdminDashboard orcamentos={orcamentos} />"
      via: "default tab quando ?tab missing"
      pattern: "TabsContent value=\"inicio\""
---

<objective>
Reorganizar `src/pages/Admin.tsx` em 5 sub-tabs no nível superior (Início / Cadastros / Pedidos / Preços / Exceções) com sub-tabs aninhadas onde D-10 pede; persistir seleção via `?tab=X&sub=Y` (D-11); embutir AdminDashboard como sub-tab "Início" (D-26 errata); simplificar AdminDashboard removendo "Distribuição por Status" (D-25); inserir bloco de ajuda inline na aba Exceções (D-22/D-23).

**NÃO entrega ADM-01 (página /admin/orcamento/:id)** — isso é Plan 05.
**NÃO entrega ADM-02 (tela de preços batch)** — isso é Plan 04. Aqui apenas reservamos o `TabsContent value="atualizacao"` em Preços com placeholder Card "Em construção (Plan 04)".

Purpose: ADM-03 + ADM-04 + ADM-05 entregues em um único plan porque tocam o mesmo arquivo principal (`Admin.tsx`) e dois colaterais diretos (`AdminDashboard.tsx`, `AdminExceptions.tsx`). Plans paralelos quebrariam por overlap.

Output: Admin reorganizado, dashboard limpo, ajuda inline em Exceções. Plan 04 e Plan 05 vão preencher os slots reservados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-drive-rls-reorganiza-o-admin/04-CONTEXT.md
@.planning/phases/04-drive-rls-reorganiza-o-admin/04-RESEARCH.md
@.planning/phases/04-drive-rls-reorganiza-o-admin/04-01-SUMMARY.md

@src/pages/Admin.tsx
@src/components/AdminDashboard.tsx
@src/components/AdminExceptions.tsx

<interfaces>
<!-- Atualmente Admin.tsx usa: -->
const VALID_TABS = ["dashboard", "excecoes", "importacao", "produtos",
                    "colaboradores", "orcamentos", "clientes", "arquitetos"];

<!-- Estrutura nova alvo: -->
TOP_TABS = ["inicio", "cadastros", "pedidos", "precos", "excecoes"];

SUB_TABS_BY_TAB = {
  inicio: [],                                              // sem sub
  cadastros: ["produtos", "arquitetos", "clientes", "colaboradores"],
  pedidos: [],                                             // sem sub (lista)
  precos: ["atualizacao", "importacao"],                   // atualizacao = Plan 04 placeholder
  excecoes: [],                                            // sem sub
};

DEFAULT_SUB_BY_TAB = {
  cadastros: "produtos",
  precos: "atualizacao",
};

<!-- importacao já tem sub-tabs internas (master/produtos/imagens/precos) — manter como tertiary level dentro de Preços > Importação. -->

<!-- Backwards-compat: ?tab=produtos → tab=cadastros&sub=produtos
                       ?tab=clientes → tab=cadastros&sub=clientes
                       ?tab=arquitetos → tab=cadastros&sub=arquitetos
                       ?tab=colaboradores → tab=cadastros&sub=colaboradores
                       ?tab=orcamentos → tab=pedidos
                       ?tab=importacao → tab=precos&sub=importacao
                       ?tab=dashboard → tab=inicio
                       ?tab=excecoes → tab=excecoes (igual) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplificar AdminDashboard (remover Distribuição por Status)</name>
  <files>src/components/AdminDashboard.tsx</files>
  <action>
Editar `src/components/AdminDashboard.tsx`:

1. **Remover `statusData` useMemo** (linhas ~152-164 — bloco completo desde `const statusData = useMemo` até `}, [filtered]);`).
2. **Remover o Card "Distribuição por Status"** (linhas ~242-259 — desde `<Card>` que contém `<CardTitle ...>Distribuição por Status</CardTitle>` até o `</Card>` correspondente).
3. **Ajustar o grid pai** (linha ~222): hoje é `<div className="grid gap-4 md:grid-cols-2">` envolvendo 2 cards (Receita Mensal + Distribuição). Após remover Distribuição, mudar para single column (`<div className="space-y-4">`) ou manter `md:grid-cols-1` para o card único Receita Mensal ocupar espaço inteiro. Escolher single column com `space-y-4` para combinar com o resto do dashboard.
4. **Limpar imports não usados**: remover `PieChart`, `Pie`, `Cell` do import de `recharts` se não forem mais referenciados (verificar com grep no próprio arquivo após remoção).

Manter intactos: 6 KPI cards, Receita Mensal, Motivos de Perda, Top 5 Clientes, seletor de período (D-24).
  </action>
  <verify>
    <automated>cd auraoramentos && grep -c "Distribuição por Status" src/components/AdminDashboard.tsx && grep -cE "^import.*PieChart" src/components/AdminDashboard.tsx</automated>
    Esperado: 0 e 0 (string removida + import removido).
  </verify>
  <done>
    - `Distribuição por Status` não aparece em AdminDashboard.tsx
    - `statusData` removido
    - imports `PieChart`, `Pie`, `Cell` removidos se não usados
    - Receita Mensal continua renderizando
  </done>
</task>

<task type="auto">
  <name>Task 2: Adicionar bloco de ajuda inline em AdminExceptions (ADM-03)</name>
  <files>src/components/AdminExceptions.tsx</files>
  <action>
Editar `src/components/AdminExceptions.tsx`:

1. Adicionar import de `Card`, `CardContent`, `CardHeader`, `CardTitle`, `CardDescription` de `@/components/ui/card` (se não existir já).
2. Adicionar import do ícone `HelpCircle` (ou `Info`) de `lucide-react`.
3. No JSX retornado, **antes** do filtro/tabela existente, inserir o Card de ajuda fixo:

```tsx
<Card className="mb-4 border-blue-200 bg-blue-50/30">
  <CardHeader className="pb-2">
    <CardTitle className="text-sm font-semibold flex items-center gap-2">
      <HelpCircle className="h-4 w-4 text-blue-700" />
      Como funciona o fluxo de exceção de preço
    </CardTitle>
  </CardHeader>
  <CardContent className="text-sm text-muted-foreground space-y-1">
    <p>
      <strong>Solicitação:</strong> O colaborador identifica uma violação no Step 3 do orçamento (preço cotado abaixo do mínimo)
      e clica em <em>"Solicitar exceção"</em> no item violado, justificando.
    </p>
    <p>
      <strong>Aprovação:</strong> Você (admin) recebe notificação em tempo real e pode aprovar ou rejeitar usando os botões
      <CheckCircle className="inline h-3 w-3 mx-1 text-green-600" /> /
      <XCircle className="inline h-3 w-3 mx-1 text-red-600" /> abaixo, ou abrir o chat
      <MessageSquare className="inline h-3 w-3 mx-1" /> para conversar antes de decidir.
    </p>
    <p>
      <strong>Após aprovação:</strong> O colaborador consegue gerar PDF com o preço acordado. O histórico fica visível
      na visualização detalhada de pedido (admin > Pedidos > clique no pedido).
    </p>
    <p>
      <strong>Filtre acima por status</strong> para ver pendências, aprovadas ou rejeitadas.
    </p>
  </CardContent>
</Card>
```

Posicionar o Card antes do `<div>` que contém o filtro `Select` + `Table`. Se o componente atual não tem wrapper de espaçamento, envolver tudo em `<div className="space-y-4">`.

Não tocar no comportamento do `fetchExceptions`, `handleAction`, `openChat` ou `Subscription`.
  </action>
  <verify>
    <automated>cd auraoramentos && grep -c "Como funciona o fluxo de exceção de preço" src/components/AdminExceptions.tsx</automated>
    Esperado: 1.
  </verify>
  <done>
    - Card de ajuda renderiza no topo de AdminExceptions com 4 parágrafos explicativos (solicitação, aprovação, pós-aprovação, filtro)
    - Imports de Card e HelpCircle adicionados
    - Comportamento existente (filtro/aprovar/rejeitar/chat) intocado
  </done>
</task>

<task type="auto">
  <name>Task 3: Reorganizar Admin.tsx em 5 sub-tabs com sub-tabs aninhadas + URL state ?tab=X&amp;sub=Y</name>
  <files>src/pages/Admin.tsx</files>
  <action>
Refatorar a estrutura de Tabs de `src/pages/Admin.tsx`. Manter TODA a lógica de fetch, dialogs, handlers — só JSX/estado de tabs muda.

**Mudança A — Substituir constantes de tabs:**
```typescript
const TOP_TABS = ["inicio", "cadastros", "pedidos", "precos", "excecoes"] as const;
type TopTab = (typeof TOP_TABS)[number];

const SUB_TABS_BY_TAB: Record<TopTab, readonly string[]> = {
  inicio: [],
  cadastros: ["produtos", "arquitetos", "clientes", "colaboradores"],
  pedidos: [],
  precos: ["atualizacao", "importacao"],
  excecoes: [],
};

const DEFAULT_SUB_BY_TAB: Partial<Record<TopTab, string>> = {
  cadastros: "produtos",
  precos: "atualizacao",
};

// Backward-compat: tabs antigos → novo (?tab=X&sub=Y)
const LEGACY_TAB_MAP: Record<string, { tab: TopTab; sub?: string }> = {
  dashboard: { tab: "inicio" },
  produtos: { tab: "cadastros", sub: "produtos" },
  clientes: { tab: "cadastros", sub: "clientes" },
  arquitetos: { tab: "cadastros", sub: "arquitetos" },
  colaboradores: { tab: "cadastros", sub: "colaboradores" },
  orcamentos: { tab: "pedidos" },
  importacao: { tab: "precos", sub: "importacao" },
  excecoes: { tab: "excecoes" },
};
```

**Mudança B — Substituir `tabParam`/`activeTab`/`handleTabChange`:**
```typescript
const rawTab = searchParams.get("tab");
const rawSub = searchParams.get("sub");

// Normalize legacy tabs
const legacy = rawTab && LEGACY_TAB_MAP[rawTab];
const activeTab: TopTab = legacy
  ? legacy.tab
  : TOP_TABS.includes(rawTab as TopTab)
    ? (rawTab as TopTab)
    : "inicio";

const validSubs = SUB_TABS_BY_TAB[activeTab];
const candidateSub = legacy?.sub ?? rawSub ?? DEFAULT_SUB_BY_TAB[activeTab] ?? "";
const activeSub = validSubs.includes(candidateSub) ? candidateSub : (DEFAULT_SUB_BY_TAB[activeTab] ?? "");

// Effect: se URL veio com legacy ou inválido, normaliza (replace)
useEffect(() => {
  const needsNormalize =
    (rawTab && LEGACY_TAB_MAP[rawTab]) ||
    (rawTab && !TOP_TABS.includes(rawTab as TopTab)) ||
    (validSubs.length > 0 && rawSub !== activeSub) ||
    (validSubs.length === 0 && rawSub);
  if (needsNormalize) {
    const next: Record<string, string> = { tab: activeTab };
    if (validSubs.length > 0) next.sub = activeSub;
    setSearchParams(next, { replace: true });
  }
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [rawTab, rawSub]);

const handleTabChange = (value: string) => {
  const t = value as TopTab;
  const next: Record<string, string> = { tab: t };
  const defSub = DEFAULT_SUB_BY_TAB[t];
  if (defSub) next.sub = defSub;
  setSearchParams(next, { replace: true });
};

const handleSubChange = (sub: string) => {
  setSearchParams({ tab: activeTab, sub }, { replace: true });
};
```

**Mudança C — Substituir o JSX de Tabs (TabsList superior + TabsContent):**

Estrutura final:

```tsx
<Tabs value={activeTab} onValueChange={handleTabChange}>
  <TabsList className="mb-6">
    <TabsTrigger value="inicio">Início</TabsTrigger>
    <TabsTrigger value="cadastros">Cadastros</TabsTrigger>
    <TabsTrigger value="pedidos">Pedidos</TabsTrigger>
    <TabsTrigger value="precos">Preços</TabsTrigger>
    <TabsTrigger value="excecoes">Exceções</TabsTrigger>
  </TabsList>

  {/* INÍCIO — D-26 */}
  <TabsContent value="inicio">
    <AdminDashboard orcamentos={orcamentos} />
  </TabsContent>

  {/* CADASTROS — sub-tabs */}
  <TabsContent value="cadastros">
    <Tabs value={activeSub} onValueChange={handleSubChange}>
      <TabsList className="mb-4">
        <TabsTrigger value="produtos">Produtos</TabsTrigger>
        <TabsTrigger value="arquitetos">Arquitetos</TabsTrigger>
        <TabsTrigger value="clientes">Clientes</TabsTrigger>
        <TabsTrigger value="colaboradores">Colaboradores</TabsTrigger>
      </TabsList>
      <TabsContent value="produtos">{/* JSX existente da aba produtos */}</TabsContent>
      <TabsContent value="arquitetos">{/* JSX existente da aba arquitetos */}</TabsContent>
      <TabsContent value="clientes">{/* JSX existente da aba clientes */}</TabsContent>
      <TabsContent value="colaboradores">{/* JSX existente da aba colaboradores */}</TabsContent>
    </Tabs>
  </TabsContent>

  {/* PEDIDOS — lista (mantém comportamento atual; Plan 05 adiciona link para /admin/orcamento/:id) */}
  <TabsContent value="pedidos">
    {/* JSX existente da aba orcamentos */}
  </TabsContent>

  {/* PREÇOS — sub-tabs */}
  <TabsContent value="precos">
    <Tabs value={activeSub} onValueChange={handleSubChange}>
      <TabsList className="mb-4">
        <TabsTrigger value="atualizacao">Atualização</TabsTrigger>
        <TabsTrigger value="importacao">Importação</TabsTrigger>
      </TabsList>
      <TabsContent value="atualizacao">
        {/* Placeholder — Plan 04 substitui por <PrecosBatch /> */}
        <Card>
          <CardHeader>
            <CardTitle className="text-base">Atualização de Preços</CardTitle>
            <CardDescription>Em construção — entregue pelo Plan 04 desta phase.</CardDescription>
          </CardHeader>
          <CardContent>
            <p className="text-sm text-muted-foreground">
              Tela de edição inline em batch para preço de tabela e preço mínimo.
              Por enquanto, edite individualmente em <strong>Cadastros &gt; Produtos</strong>.
            </p>
          </CardContent>
        </Card>
      </TabsContent>
      <TabsContent value="importacao" className="space-y-6">
        {/* JSX existente da aba importacao (importSubTab interno + ImportMaster/Produtos/Imagens) */}
      </TabsContent>
    </Tabs>
  </TabsContent>

  {/* EXCEÇÕES */}
  <TabsContent value="excecoes">
    <AdminExceptions />
  </TabsContent>
</Tabs>
```

**Mudança D — Mover o JSX existente das tabs antigas para os slots corretos:**
- TabsContent existente `dashboard` → conteúdo de `inicio`
- TabsContent existente `produtos` → dentro de cadastros > produtos
- TabsContent existente `arquitetos` → dentro de cadastros > arquitetos
- TabsContent existente `clientes` → dentro de cadastros > clientes
- TabsContent existente `colaboradores` → dentro de cadastros > colaboradores
- TabsContent existente `orcamentos` → conteúdo de `pedidos`
- TabsContent existente `importacao` (com `importSubTab` interno mantido) → dentro de precos > importacao
- TabsContent existente `excecoes` → conteúdo de `excecoes` no topo

NÃO mexer na lógica do `importSubTab` interno (master/produtos/imagens/precos) — fica como está dentro do TabsContent novo.

**Mudança E — Limpar:** remover `VALID_TABS` antigo e `AdminTab` type antigo.

Cuidado com chaves duplicadas: TabsContent value="produtos" agora aparece dentro do <Tabs> aninhado de Cadastros — Radix Tabs trabalha relativo ao <Tabs> pai mais próximo, então OK.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run lint 2>&1 | grep -E "Admin\\.tsx" | head -5; npm run build 2>&1 | tail -20</automated>
    Esperado: lint sem erros novos em Admin.tsx; build verde.
  </verify>
  <done>
    - 5 sub-tabs Início/Cadastros/Pedidos/Preços/Exceções renderizam
    - Cadastros tem 4 sub-tabs aninhadas (Produtos/Arquitetos/Clientes/Colaboradores)
    - Preços tem 2 sub-tabs aninhadas (Atualização placeholder + Importação)
    - URL `?tab=cadastros&sub=produtos` carrega na sub-tab correta após reload
    - URL legada `?tab=produtos` redireciona para `?tab=cadastros&sub=produtos` via useEffect
    - Início default quando sem `?tab`
    - `npm run build` verde
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Smoke Admin (5 sub-tabs + URL state + dashboard limpo + ajuda exceções) via Playwright MCP</name>
  <what-built>Admin reorganizado em 5 sub-tabs, dashboard sem PieChart Distribuição, ajuda inline em Exceções.</what-built>
  <action>Pausa o executor até o usuário confirmar via resume-signal abaixo. Ver instruções em &lt;how-to-verify&gt;.</action>
  <how-to-verify>
    Pipeline automático:

    1. **Code review** sobre o diff de Admin.tsx + AdminDashboard.tsx + AdminExceptions.tsx.
    2. **Playwright MCP** (npm run dev port 8080):
       - Login como admin (Lenny) → /admin → deve abrir em "Início" com 6 KPI cards + Receita Mensal + Motivos de Perda + Top 5 Clientes. **Não deve ter card "Distribuição por Status"**.
       - Clicar em "Cadastros" → URL vira `?tab=cadastros&sub=produtos` → ver tabela de produtos com botão "+ Novo Produto".
       - Trocar sub-tab para "Arquitetos" → URL vira `?tab=cadastros&sub=arquitetos` → tabela de arquitetos com "+ Novo Arquiteto".
       - Trocar sub-tab para "Clientes" → tabela com "+ Novo Cliente".
       - Trocar sub-tab para "Colaboradores" → lista colaboradores.
       - Reload em `?tab=cadastros&sub=clientes` → carrega direto na sub-tab clientes.
       - Clicar em "Pedidos" → URL `?tab=pedidos` → tabela de orçamentos.
       - Clicar em "Preços" → URL `?tab=precos&sub=atualizacao` → ver Card placeholder "Em construção — Plan 04".
       - Trocar para "Importação" → ver os 4 cards horizontais (Master / Produtos / Imagens / Preços).
       - Clicar em "Exceções" → ver o **Card azul de ajuda no topo** com 4 parágrafos (solicitação/aprovação/pós-aprovação/filtre acima) + filtro + tabela.
       - Reload em URL legada `?tab=produtos` → deve redirecionar para `?tab=cadastros&sub=produtos`.
    3. Console JS: 0 erros vermelhos.
  </how-to-verify>
  <resume-signal>Digite "admin reorg ok" ou descreva o problema.</resume-signal>
  <done>
    Code review passou, Playwright executou todos os cenários (5 sub-tabs + sub-tabs aninhadas + URL state + legacy redirect + dashboard limpo + ajuda em exceções) sem erros.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → Admin tab routing | useSearchParams aceita strings arbitrárias do user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering | URL `?tab=foo&sub=bar` injeta UI inválida | mitigate | Whitelist via `TOP_TABS` + `SUB_TABS_BY_TAB` (Pitfall 4 do RESEARCH); fallback para `inicio` |
| T-04-13 | Information Disclosure | Bloco de ajuda em Exceções vaza fluxo interno | accept | Aba protegida por AdminRoute; só admin acessa |
| T-04-14 | Denial of Service | useEffect de normalização entra em loop | mitigate | `setSearchParams(..., { replace: true })` + dependencies `[rawTab, rawSub]` evita loop (testar com console open) |
</threat_model>

<verification>
- Build verde (`npm run build`)
- Lint sem novos erros
- Smoke Playwright passa em todos os cenários
- URL legacy redireciona uma única vez (não loop)
- Dashboard renderiza sem `PieChart` no DOM (verificar `data-testid` ou simples ausência do título)
- AdminExceptions Card de ajuda visível
</verification>

<success_criteria>
1. ADM-04 satisfeito: 5 grupos no nível superior, sub-tabs aninhadas onde D-10 manda
2. ADM-05 satisfeito: dashboard simplificado vira sub-tab Início, sem Distribuição por Status
3. ADM-03 satisfeito: ajuda inline em Exceções
4. URL state `?tab=X&sub=Y` funciona com reload e back-button
5. Backwards compat para tabs antigas
6. Slot `precos > atualizacao` reservado para Plan 04 (placeholder Card)
</success_criteria>

<output>
Após completar, criar `.planning/phases/04-drive-rls-reorganiza-o-admin/04-03-SUMMARY.md` com:
- Estrutura final de Tabs (top + sub)
- Mapeamento de tabs legadas → novas
- Confirmação dashboard limpo
- Confirmação ajuda em Exceções
- Slot reservado para Plan 04 (precos > atualizacao)
</output>
