---
phase: 04-drive-rls-reorganiza-o-admin
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - src/components/PrecosBatch.tsx
  - src/components/__tests__/PrecosBatch.test.tsx
  - src/pages/Admin.tsx
autonomous: false
requirements: [ADM-02]
must_haves:
  truths:
    - "Sub-tab Preços > Atualização renderiza componente PrecosBatch (substitui o placeholder do Plan 03)"
    - "PrecosBatch lista produtos paginados (50/página) com inline edit em preco_tabela e preco_minimo"
    - "Filtros funcionam: arquiteto (select), categoria (select), 'sem preço' (checkbox)"
    - "Edição de uma célula adiciona o id ao mapa de pendingChanges; linha alterada destaca visualmente (bg-yellow-50)"
    - "Botão 'Salvar X alterações' fica fixo no rodapé enquanto há pendentes; X mostra count"
    - "Validação inline: bloqueia save com toast.error se preco_minimo > preco_tabela em qualquer linha (D-17)"
    - "Batch save executa Promise.all de updates em product_variants com editado_manualmente=true (D-16) e mostra toast de sucesso ou erro parcial"
    - "Após save bem-sucedido, pendingChanges é limpo e a lista é refetched"
  artifacts:
    - path: "src/components/PrecosBatch.tsx"
      provides: "Componente de edição inline batch de preços (D-12..D-17) + função pura validarPendingChanges exportada"
      contains: "pendingChanges"
    - path: "src/components/__tests__/PrecosBatch.test.tsx"
      provides: "3 testes de validarPendingChanges (D-17 logic)"
      contains: "validarPendingChanges"
    - path: "src/pages/Admin.tsx"
      provides: "Sub-tab Preços > Atualização wireada para <PrecosBatch />"
      contains: "<PrecosBatch"
  key_links:
    - from: "src/components/PrecosBatch.tsx handleSave"
      to: "supabase.from('product_variants').update"
      via: "Promise.all de N updates com editado_manualmente=true"
      pattern: "editado_manualmente: true"
    - from: "src/pages/Admin.tsx TabsContent value='atualizacao'"
      to: "<PrecosBatch />"
      via: "import direto, substituindo placeholder do Plan 03"
      pattern: "<PrecosBatch"
---

<objective>
Entregar ADM-02: tela dedicada de atualização de preços em batch via componente novo `src/components/PrecosBatch.tsx`. Inline edit em tabela paginada com filtros (arquiteto, categoria, sem preço), validação preco_min ≤ preco_tabela (D-17), batch save com `editado_manualmente=true` (D-16), highlight de linha alterada, paginação 50/página (D-12).

Substitui o placeholder do Plan 03 dentro de `Admin.tsx > Preços > Atualização`.

Purpose: Operação periódica (~1x/mês — vide D-18 da Phase 3 que defere o IMP-02 CSV) precisa de fluxo eficiente — abrir 50 produtos, ajustar onde precisa, salvar tudo de uma vez. Edição via ProdutoEditDialog atual (modal) não escala.

Output: Componente novo + arquivo de teste + wire em Admin. Plan 04 não toca Schema (preco_tabela/preco_minimo já existem em product_variants desde Phase 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-drive-rls-reorganiza-o-admin/04-CONTEXT.md
@.planning/phases/04-drive-rls-reorganiza-o-admin/04-RESEARCH.md
@.planning/phases/04-drive-rls-reorganiza-o-admin/04-03-SUMMARY.md

# Phase 3 — pattern de editado_manualmente
@.planning/phases/03-produtos-importa-o/03-CONTEXT.md

@src/pages/Admin.tsx
@src/integrations/supabase/types.ts

<interfaces>
<!-- product_variants schema (do types.ts pós Phase 3): -->
product_variants: {
  id: string,
  codigo: string,
  descricao: string | null,
  nome: string | null,
  preco_tabela: number | null,
  preco_minimo: number | null,
  arquiteto_id: string | null,
  imagem_url: string | null,
  categoria: string | null,
  tipo_produto: string | null,
  editado_manualmente: boolean,
  origem: 'master' | 'legado' | 'coringa' | 'manual',
}

arquitetos: { id: string, nome: string, contato: string | null }

<!-- Pattern do RESEARCH Área 8: -->
const [pendingChanges, setPendingChanges] = useState<Map<string, { preco_tabela: number; preco_minimo: number }>>(new Map());
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar PrecosBatch.tsx + testes de validacao</name>
  <files>src/components/PrecosBatch.tsx, src/components/__tests__/PrecosBatch.test.tsx</files>
  <behavior>
- Test 1: validarPendingChanges retorna {valid: false, errorId} quando alguma entry tem preco_minimo > preco_tabela
- Test 2: validarPendingChanges retorna {valid: true} quando todas entries têm preco_minimo ≤ preco_tabela
- Test 3: validarPendingChanges retorna {valid: true} para Map vazio
  </behavior>
  <action>
Criar `src/components/PrecosBatch.tsx`. Estrutura completa:

**Imports:**
```typescript
import { useState, useEffect } from "react";
import { supabase } from "@/integrations/supabase/client";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Checkbox } from "@/components/ui/checkbox";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { toast } from "sonner";
import { Loader2, Save, ChevronLeft, ChevronRight } from "lucide-react";
import { cn } from "@/lib/utils";
```

**Tipos locais + função pura exportada:**
```typescript
interface ProdutoRow {
  id: string;
  codigo: string;
  descricao: string | null;
  nome: string | null;
  preco_tabela: number | null;
  preco_minimo: number | null;
  arquiteto_id: string | null;
  categoria: string | null;
  tipo_produto: string | null;
}

interface PendingChange {
  preco_tabela: number;
  preco_minimo: number;
}

const PAGE_SIZE = 50;

export function validarPendingChanges(
  pending: Map<string, PendingChange>
): { valid: true } | { valid: false; errorId: string } {
  for (const [id, c] of pending) {
    if (c.preco_minimo > c.preco_tabela) {
      return { valid: false, errorId: id };
    }
  }
  return { valid: true };
}
```

**Componente principal — estado:**
- `produtos: ProdutoRow[]`
- `arquitetos: { id: string; nome: string }[]`
- `categorias: string[]` (distinct)
- `loading: boolean`
- `saving: boolean`
- `page: number` (0-indexed)
- `totalCount: number`
- `pendingChanges: Map<string, PendingChange>`
- `filterArquitetoId: string` ('' = todos)
- `filterCategoria: string` ('' = todas)
- `filterSemPreco: boolean`

**Effects:**
- Mount: fetch arquitetos (`select id, nome from arquitetos order by nome`); fetch categorias distinct (workaround: `select categoria from product_variants where categoria is not null` + dedupe local).
- `[page, filterArquitetoId, filterCategoria, filterSemPreco]`: refetch via `fetchProdutos`.

**fetchProdutos:**
```typescript
const fetchProdutos = async () => {
  setLoading(true);
  let q = supabase
    .from("product_variants")
    .select(
      "id, codigo, descricao, nome, preco_tabela, preco_minimo, arquiteto_id, categoria, tipo_produto",
      { count: "exact" }
    )
    .order("codigo");
  if (filterArquitetoId) q = q.eq("arquiteto_id", filterArquitetoId);
  if (filterCategoria) q = q.eq("categoria", filterCategoria);
  if (filterSemPreco) q = q.or("preco_tabela.is.null,preco_tabela.eq.0");
  const from = page * PAGE_SIZE;
  const to = from + PAGE_SIZE - 1;
  const { data, count, error } = await q.range(from, to);
  if (error) {
    toast.error("Erro ao carregar produtos");
    setLoading(false);
    return;
  }
  setProdutos((data ?? []) as ProdutoRow[]);
  setTotalCount(count ?? 0);
  setLoading(false);
};
```

**Bloqueio de mudança de filtro/página com pending:**
Cada handler de filtro/página verifica `pendingChanges.size > 0` antes de aplicar:
```typescript
const guardPending = (): boolean => {
  if (pendingChanges.size > 0) {
    toast.error("Salve ou descarte as alterações pendentes antes de mudar filtro/página");
    return true;
  }
  return false;
};
```

**handleEdit:**
```typescript
const handleEdit = (id: string, field: "preco_tabela" | "preco_minimo", raw: string) => {
  const num = raw === "" ? 0 : parseFloat(raw);
  if (isNaN(num) || num < 0) return;
  setPendingChanges((prev) => {
    const next = new Map(prev);
    const original = produtos.find((p) => p.id === id);
    if (!original) return prev;
    const base: PendingChange = next.get(id) ?? {
      preco_tabela: Number(original.preco_tabela ?? 0),
      preco_minimo: Number(original.preco_minimo ?? 0),
    };
    next.set(id, { ...base, [field]: num });
    return next;
  });
};
```

**handleSave:**
```typescript
const handleSave = async () => {
  const validation = validarPendingChanges(pendingChanges);
  if (!validation.valid) {
    const prod = produtos.find((p) => p.id === validation.errorId);
    toast.error(`Preço mínimo > preço tabela em ${prod?.codigo ?? validation.errorId}`);
    return;
  }
  setSaving(true);
  try {
    const updates = [...pendingChanges.entries()].map(([id, c]) =>
      supabase
        .from("product_variants")
        .update({
          preco_tabela: c.preco_tabela,
          preco_minimo: c.preco_minimo,
          editado_manualmente: true,  // D-16
        })
        .eq("id", id)
    );
    const results = await Promise.all(updates);
    const errors = results.filter((r) => r.error);
    if (errors.length > 0) {
      toast.error(
        `${errors.length} de ${results.length} updates falharam — recarregue e tente os restantes`
      );
    } else {
      toast.success(
        `${results.length} produto${results.length !== 1 ? "s" : ""} atualizado${results.length !== 1 ? "s" : ""}`
      );
    }
    setPendingChanges(new Map());
    await fetchProdutos();
  } finally {
    setSaving(false);
  }
};
```

**JSX — estrutura:**
1. Toolbar (flex gap-3): Select arquiteto / Select categoria / Checkbox "Sem preço cadastrado" / contador "X de Y produtos".
2. Tabela (rounded-xl border) com colunas: Código (mono) / Descrição / Categoria / Arquiteto / Preço Tabela (Input number step="0.01") / Preço Mínimo (Input). Linha highlight: `<TableRow className={cn(pendingChanges.has(p.id) && "bg-yellow-50")}>`.
3. Inputs renderizam o valor pendente se existir, senão o original:
```tsx
const pending = pendingChanges.get(p.id);
const valorTabela = pending ? pending.preco_tabela : (p.preco_tabela ?? 0);
const valorMinimo = pending ? pending.preco_minimo : (p.preco_minimo ?? 0);
<Input type="number" step="0.01" min="0" value={valorTabela}
  onChange={(e) => handleEdit(p.id, "preco_tabela", e.target.value)}
  className="w-28 text-right" />
```
4. Paginação (flex justify-between mt-3): `<Button onClick={() => { if (!guardPending()) setPage((p) => Math.max(0, p - 1)); }}><ChevronLeft /></Button>` + texto "Página X de Y" (Y = `Math.ceil(totalCount / PAGE_SIZE)`) + ChevronRight.
5. Footer sticky quando `pendingChanges.size > 0` (vide RESEARCH Área 8 — sticky bottom-0 bg-background border-t mt-4 py-3 px-4 flex items-center justify-between shadow-md): "X alteração(ões) pendente(s)" + botão "Descartar" (limpa Map) + botão "Salvar X alterações" (chama handleSave; mostra Loader2 quando saving).

**Default export `PrecosBatch`.**

**Arquivo `src/components/__tests__/PrecosBatch.test.tsx`:**
```typescript
import { describe, it, expect } from "vitest";
import { validarPendingChanges } from "../PrecosBatch";

describe("validarPendingChanges (D-17)", () => {
  it("retorna invalido com errorId quando preco_minimo > preco_tabela", () => {
    const m = new Map([
      ["id-1", { preco_tabela: 100, preco_minimo: 50 }],
      ["id-2", { preco_tabela: 80, preco_minimo: 90 }],
    ]);
    const r = validarPendingChanges(m);
    expect(r.valid).toBe(false);
    if (!r.valid) expect(r.errorId).toBe("id-2");
  });

  it("retorna valido quando todas as entries tem preco_minimo <= preco_tabela", () => {
    const m = new Map([
      ["id-1", { preco_tabela: 100, preco_minimo: 50 }],
      ["id-2", { preco_tabela: 80, preco_minimo: 80 }],
    ]);
    expect(validarPendingChanges(m).valid).toBe(true);
  });

  it("retorna valido para Map vazio", () => {
    expect(validarPendingChanges(new Map()).valid).toBe(true);
  });
});
```

**Não faça:** bulk ops avançadas X% (D-15 defere); cachear via TanStack Query (over-engineering); trocar de filtro/página sem salvar (toast bloqueia via guardPending).
  </action>
  <verify>
    <automated>cd auraoramentos && npm run test -- --run src/components/__tests__/PrecosBatch.test.tsx</automated>
    Esperado: 3 testes passam.
  </verify>
  <done>
    - `src/components/PrecosBatch.tsx` existe com componente + função `validarPendingChanges` exportada
    - 3 testes em `__tests__/PrecosBatch.test.tsx` passam
    - `npm run build` verde
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire PrecosBatch em Admin > Precos > Atualizacao</name>
  <files>src/pages/Admin.tsx</files>
  <action>
Em `src/pages/Admin.tsx`:

1. Adicionar import: `import PrecosBatch from "@/components/PrecosBatch";`
2. Localizar o `<TabsContent value="atualizacao">` criado pelo Plan 03 (placeholder Card "Em construção — Plan 04").
3. Substituir todo o conteúdo do `<TabsContent value="atualizacao">` por:
```tsx
<TabsContent value="atualizacao">
  <PrecosBatch />
</TabsContent>
```

Não tocar em mais nada.
  </action>
  <verify>
    <automated>cd auraoramentos && grep -c "<PrecosBatch" src/pages/Admin.tsx</automated>
    Esperado: ≥ 1.
  </verify>
  <done>
    - Sub-tab Preços > Atualização renderiza `<PrecosBatch />`
    - Placeholder do Plan 03 removido
    - `npm run build` verde
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke PrecosBatch via Playwright MCP</name>
  <what-built>Tela de atualização de preços em batch funcional na sub-tab Preços > Atualização.</what-built>
  <action>Pausa o executor até o usuário confirmar via resume-signal abaixo. Ver instruções em &lt;how-to-verify&gt;.</action>
  <how-to-verify>
    Pipeline automático:

    1. **Code review** sobre PrecosBatch.tsx + Admin.tsx diff.
    2. **Playwright MCP** (npm run dev port 8080):
       - Login admin → /admin?tab=precos&sub=atualizacao → ver tabela com 50 produtos, paginação.
       - Filtrar por arquiteto: select arquiteto → tabela filtra → page reset (esperado: filtro reseta page=0).
       - Filtrar "Sem preço cadastrado" (checkbox) → ver apenas produtos com preco_tabela null/0.
       - Editar preco_tabela de uma linha (ex: AU001) → linha fica amarela → footer aparece com "1 alteração pendente" + botões Descartar/Salvar.
       - Editar preco_minimo da mesma linha pra valor MAIOR que preco_tabela → clicar Salvar → ver toast.error "Preço mínimo > preço tabela em AU001". Sem update no banco.
       - Corrigir preco_minimo (≤ preco_tabela) → clicar Salvar → toast.success "1 produto atualizado" → footer some → linha não está mais amarela.
       - Verificar via aba Cadastros > Produtos que o produto agora tem `editado_manualmente=true` (impossível ver na UI direto, mas via SQL editor: `SELECT codigo, preco_tabela, editado_manualmente FROM product_variants WHERE codigo='AU001'` deve retornar `t`).
       - Editar 5 produtos diferentes em uma página → 5 alterações pendentes → mudar página de paginação SEM salvar → toast.error "Salve ou descarte as alterações pendentes" — mudança de página bloqueada.
       - Descartar → footer some → mudar página agora funciona.
    3. Console JS: 0 erros vermelhos.
  </how-to-verify>
  <resume-signal>Digite "precos batch ok" ou descreva o problema.</resume-signal>
  <done>
    Code review passou; Playwright executou os cenários (filtros, edit single, validação D-17, batch save, editado_manualmente=true, guard de mudança de página) sem regressões.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → product_variants UPDATE | Admin editado controla preço; RLS já existe (admins manage product_variants) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Tampering | Colab não-admin chama UPDATE em product_variants via console | mitigate | RLS existente "Admins manage product_variants" (Phase 3) bloqueia; ASVS V4.2 |
| T-04-16 | Tampering | Save parcial (Promise.all não atômico) deixa estado inconsistente | accept | D-15 + Pitfall 5 do RESEARCH; toast diferenciado por count de erros; user vê parcial e tenta de novo |
| T-04-17 | Tampering | Override de master subsequente sobre edição manual | mitigate | D-16 + Phase 3 D-08: `editado_manualmente=true` setado em todo save → ImportMaster respeita |
| T-04-18 | Input Validation | Input numérico aceita NaN ou negativo | mitigate | parseFloat + isNaN + `< 0` ignora; min="0" no Input; validarPendingChanges bloqueia preco_min > preco_tabela; ASVS V5 |
</threat_model>

<verification>
- 3 testes Vitest passam (validarPendingChanges)
- `npm run build` verde
- Smoke Playwright: filtro, edit, validação, batch save, editado_manualmente=true, guard
- SQL pós-save: `editado_manualmente = true` na linha tocada
- Pendings limpos após save bem-sucedido
</verification>

<success_criteria>
1. ADM-02 satisfeito (D-12..D-17): paginação 50, inline edit, filtros, batch save, validação inline, editado_manualmente=true, highlight visual
2. Sem bulk ops avançadas (D-15)
3. RLS existente cobre permissão (admin only)
4. Plan 03 placeholder substituído
</success_criteria>

<output>
Após completar, criar `.planning/phases/04-drive-rls-reorganiza-o-admin/04-04-SUMMARY.md` com:
- Componente criado + estrutura
- Cobertura D-XX (12,13,14,15,16,17)
- Resultado smoke (paginação, filtros, validação, batch save, editado_manualmente)
- Notas de UX
</output>
