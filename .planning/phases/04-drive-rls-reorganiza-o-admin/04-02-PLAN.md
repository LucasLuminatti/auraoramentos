---
phase: 04-drive-rls-reorganiza-o-admin
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/DriveExplorer.tsx
autonomous: false
requirements: [ACC-03, ACC-04]
must_haves:
  truths:
    - "Upload de arquivo via DriveExplorer seta user_id = auth.uid() na linha de cliente_arquivos"
    - "Upload de pasta via DriveExplorer seta user_id = auth.uid() na linha de arquivo_pastas"
    - "Download/abrir arquivo gera signed URL com 24h via supabase.storage.createSignedUrl(path, 86400)"
    - "Listagem do Drive não usa mais arquivo_url legado para abrir/baixar"
    - "Colaborador NÃO admin não vê arquivos/pastas de outro colaborador (RLS do Plan 01 + UI alinhada)"
  artifacts:
    - path: "src/components/DriveExplorer.tsx"
      provides: "Migrado para signed URLs + INSERT com user_id em cliente_arquivos e arquivo_pastas"
      contains: "createSignedUrl"
  key_links:
    - from: "src/components/DriveExplorer.tsx handleUpload"
      to: "cliente_arquivos.user_id"
      via: "supabase.auth.getUser() injetado no INSERT"
      pattern: "user_id: user\\.id"
    - from: "src/components/DriveExplorer.tsx handleCriarPasta"
      to: "arquivo_pastas.user_id"
      via: "supabase.auth.getUser() injetado no INSERT"
      pattern: "user_id: user\\.id"
    - from: "src/components/DriveExplorer.tsx handleDownload (novo)"
      to: "supabase.storage.from('cliente-arquivos').createSignedUrl"
      via: "clique no link/botão de download"
      pattern: "createSignedUrl\\(.*?86400"
---

<objective>
Refatorar `DriveExplorer.tsx` para alinhar com o modelo de dados pós-Plan 01:
- Trocar `getPublicUrl` por `createSignedUrl(path, 86400)` em todos os caminhos de download/abrir arquivo
- Injetar `user_id = auth.uid()` em todos os INSERTs (`cliente_arquivos` no upload, `arquivo_pastas` no `handleCriarPasta`)
- Remover dependência do campo legado `arquivo_url` para acesso (manter coluna no INSERT só por compatibilidade de schema com snapshots — pode ficar `null`)

Purpose: ACC-03 (UI filtra por usuário) já é satisfeita pela RLS do Plan 01, mas a UI deve não-quebrar (signed URLs) e ACC-04 exige que upload associe ao colaborador automaticamente. Este plan entrega o "Drive comportamento de site normal" (D-01).

Output: `DriveExplorer.tsx` atualizado, smoke manual via Playwright/MCP confirma upload + download + isolamento por colaborador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-drive-rls-reorganiza-o-admin/04-CONTEXT.md
@.planning/phases/04-drive-rls-reorganiza-o-admin/04-RESEARCH.md
@.planning/phases/04-drive-rls-reorganiza-o-admin/04-01-SUMMARY.md

@src/components/DriveExplorer.tsx
@src/integrations/supabase/types.ts

<interfaces>
<!-- Pós Plan 01, types regenerados expõem: -->
cliente_arquivos.Insert: {
  cliente_id: string,
  projeto_id?: string | null,
  pasta_id?: string | null,
  nome: string,
  descricao?: string | null,
  categoria: string,
  arquivo_path: string,
  arquivo_url?: string | null,    // legado — pode passar null
  tamanho: number,
  user_id: string,                 // ← OBRIGATÓRIO pós Plan 01
}

arquivo_pastas.Insert: {
  cliente_id: string,
  projeto_id?: string | null,
  pasta_pai_id?: string | null,
  nome: string,
  user_id: string,                 // ← OBRIGATÓRIO pós Plan 01
}

<!-- Padrão de signed URL (Pattern do RESEARCH Área 5): -->
const { data, error } = await supabase.storage
  .from('cliente-arquivos')
  .createSignedUrl(arq.arquivo_path, 86400); // 24h
// data?.signedUrl é a URL temporária
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar handleDownload com createSignedUrl + injetar user_id em INSERTs</name>
  <files>src/components/DriveExplorer.tsx</files>
  <action>
Editar `src/components/DriveExplorer.tsx`. Mudanças cirúrgicas — preservar todo o resto do componente.

**Mudança A — Adicionar função `handleDownload` (acima do `return` da função componente, próximo aos outros handlers):**
```typescript
const handleDownload = async (arq: Arquivo) => {
  const { data, error } = await supabase.storage
    .from("cliente-arquivos")
    .createSignedUrl(arq.arquivo_path, 86400); // 24h — D-07
  if (error || !data?.signedUrl) {
    toast.error("Erro ao gerar link de download");
    return;
  }
  window.open(data.signedUrl, "_blank", "noopener,noreferrer");
};
```

**Mudança B — Substituir TODOS os `<a href={arq.arquivo_url}>` por `<button onClick={() => handleDownload(arq)}>`** mantendo classes/ícones existentes. Há 4 ocorrências aproximadas (grid view: 1 download icon; list view: 1 nome com link + 1 download icon — verificar com grep `arquivo_url` no arquivo, todas devem ser trocadas exceto em cleanup do `arquivo_url` no INSERT que será null abaixo).

Exemplo de substituição no list view (link no nome do arquivo):
```tsx
// ANTES:
<a href={arq.arquivo_url} target="_blank" rel="noopener noreferrer" className="text-sm text-foreground hover:underline truncate">
  {arq.nome}
</a>
// DEPOIS:
<button
  onClick={() => handleDownload(arq)}
  className="text-sm text-foreground hover:underline truncate text-left"
>
  {arq.nome}
</button>
```

E substituir os ícones de download (`<a href={arq.arquivo_url}>...<Download/>...</a>`) por `<button onClick={() => handleDownload(arq)}>...<Download/>...</button>`.

**Mudança C — Em `handleUpload`, injetar `user_id` no INSERT da tabela e setar `arquivo_url` como `null` (legado):**
```typescript
// Logo após o supabase.storage.from('cliente-arquivos').upload(...) ter sido bem-sucedido:
const { data: { user } } = await supabase.auth.getUser();
if (!user) {
  toast.error("Usuário não autenticado");
  setUploading(false);
  return;
}

// REMOVER as linhas:
//   const { data: urlData } = supabase.storage.from('cliente-arquivos').getPublicUrl(path);
// e a linha `arquivo_url: urlData.publicUrl,` no insert.

const { error: insertError } = await supabase.from("cliente_arquivos").insert({
  cliente_id: clienteId,
  projeto_id: projetoId || null,
  pasta_id: pastaId || null,
  nome: selectedFile.name,
  descricao: uploadDescricao.trim() || null,
  categoria: uploadCategoria,
  arquivo_path: path,
  arquivo_url: null,   // legado — bucket privado pós-Phase 4 (D-06/D-08)
  tamanho: selectedFile.size,
  user_id: user.id,    // ACC-04 + Plan 01 RLS
});
```

**Mudança D — Em `handleCriarPasta`, injetar `user_id`:**
```typescript
const handleCriarPasta = async () => {
  if (!novaPastaNome.trim() || !clienteId) return;
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) {
    toast.error("Usuário não autenticado");
    return;
  }
  const { error } = await supabase.from("arquivo_pastas").insert({
    nome: novaPastaNome.trim(),
    cliente_id: clienteId,
    projeto_id: projetoId || null,
    pasta_pai_id: pastaId || null,
    user_id: user.id,   // Plan 01 RLS
  });
  // ... resto inalterado
};
```

**Mudança E — interface `Arquivo` local ainda referencia `arquivo_url`. Mantenha o tipo (snapshot/legados podem trazer string), mas remova qualquer uso para acesso. O campo continua no SELECT atual (linha 144) — mantenha; tornou-se ignorável.

**Não faça:** não remover a coluna `arquivo_url` do schema (Plan 01 já mantém). Não tentar regenerar URLs legadas (D-08 aceita quebra). Não cachear signed URL com TanStack Query no v1 (over-engineering — RESEARCH Área 5 considera aceitável gerar on-demand para volume <100).

**Lógica de erro:** Manter pattern existente — `toast.error('mensagem em PT-BR')` + `return`. Sem novos try/catch.
  </action>
  <verify>
    <automated>cd auraoramentos && grep -c "createSignedUrl" src/components/DriveExplorer.tsx && grep -c "user_id: user.id" src/components/DriveExplorer.tsx && grep -c "getPublicUrl" src/components/DriveExplorer.tsx</automated>
    Esperado: createSignedUrl ≥ 1, user_id: user.id ≥ 2 (upload + criarPasta), getPublicUrl == 0.
  </verify>
  <done>
    - `handleDownload` existe e usa createSignedUrl(path, 86400)
    - 0 ocorrências de `getPublicUrl` no arquivo
    - INSERT em cliente_arquivos contém `user_id: user.id` e `arquivo_url: null`
    - INSERT em arquivo_pastas contém `user_id: user.id`
    - Todas as `<a href={arq.arquivo_url}>` foram trocadas por `<button onClick={() => handleDownload(arq)}>` (grep `arq\\.arquivo_url` deve retornar 0)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke Drive (admin + colab) via Playwright MCP</name>
  <what-built>DriveExplorer migrado para signed URLs + RLS por user_id.</what-built>
  <action>Pausa o executor até o usuário confirmar via resume-signal abaixo. Ver instruções em &lt;how-to-verify&gt;.</action>
  <how-to-verify>
    Pipeline automático (do CLAUDE.md global):

    1. **Code review:** invocar `requesting-code-review` skill sobre o diff de DriveExplorer.tsx. Corrigir achados reais antes de seguir.
    2. **Playwright MCP** (rodar com `npm run dev` no port 8080):
       - **Cenário admin (Lenny):** login → /drive → navegar até um cliente com arquivo legado → clicar em arquivo → deve abrir em nova aba (signed URL ~/storage/v1/object/sign/cliente-arquivos/...?token=...). Console sem erros.
       - **Cenário admin upload:** /drive → cliente → "Upload" → escolher PDF/jpg → enviar → ver toast "Arquivo enviado!" → clicar no novo arquivo → abre signed URL.
       - **Cenário admin criar pasta:** /drive → "Nova Pasta" → nome → criar → pasta aparece.
       - **Cenário colab (criar segunda conta de teste OU usar conta colab existente):** login → /drive → ver apenas próprios arquivos (e nada do admin). Upload → próprio arquivo aparece. Trocar para admin → admin vê o arquivo do colab também.
       - **Cenário URL antiga:** abrir uma URL `arquivo_url` antiga (se sobreviveu em algum snapshot/email) → deve retornar 400 (esperado, D-08).
    3. **Console JS:** verificar 0 erros vermelhos durante todo o fluxo.

    **Se algo falhar:** descrever o cenário; corrigir e repetir Task 2 inteira.
  </how-to-verify>
  <resume-signal>Digite "drive ok" ou descreva o problema (ex: "signed URL retorna 403 para colab").</resume-signal>
  <done>
    Code review passou; Playwright executou os 5 cenários sem erro; admin vê tudo, colab vê só o seu, signed URLs funcionam.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Supabase Storage (signed URL) | URL pré-assinada com expiração 24h vinda do client autenticado |
| DriveExplorer → cliente_arquivos / arquivo_pastas INSERT | RLS WITH CHECK valida `user_id = auth.uid()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Information Disclosure | Signed URL vazada (compartilhamento ad-hoc) | accept | TTL 24h limita janela; aceitável para uso interno (ASVS V4.3) |
| T-04-09 | Spoofing | INSERT em cliente_arquivos com user_id de outro user | mitigate | RLS WITH CHECK do Plan 01 + client envia `auth.uid()` direto via `supabase.auth.getUser()` |
| T-04-10 | Tampering | Cleanup de delete no Storage falha mas registro removido (orfão) | accept | Pattern atual de `handleDelete` já remove storage primeiro; em falha de tabela, blob fica órfão (lixo recolhível depois) |
| T-04-11 | Information Disclosure | console.log de signed URL | mitigate | Não logar `data.signedUrl` em nenhum console.log do componente |
</threat_model>

<verification>
- `npm run lint` sem novos erros em DriveExplorer.tsx
- `npm run build` ok
- Smoke Playwright: admin vê tudo, colab vê só o próprio, signed URLs abrem (HTTP 200)
- Console do navegador: 0 erros JS durante o fluxo
</verification>

<success_criteria>
1. ACC-03 satisfeito (UI alinhada com RLS — colab não vê de outro)
2. ACC-04 satisfeito (upload seta user_id automaticamente)
3. Bucket privado funciona em produção via signed URL
4. Nenhuma regressão visível no Drive (criar pasta, navegar breadcrumb, deletar, view grid/list)
</success_criteria>

<output>
Após completar, criar `.planning/phases/04-drive-rls-reorganiza-o-admin/04-02-SUMMARY.md` com:
- Lista de mudanças aplicadas em DriveExplorer.tsx
- Resultado do smoke Playwright (cenários executados)
- Notas de qualquer ajuste de UX descoberto
</output>
