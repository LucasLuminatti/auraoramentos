---
phase: 11-pdf-v2-dashboard
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/pdfTemplates/v2.ts
autonomous: true
requirements:
  - PDF-01
  - PDF-02
tags: [pdf-v2, sistema-vazio, prazo-entrega]

must_haves:
  truths:
    - "Sistema com 0m de fita, 0W de consumo e 0 driver não aparece como bloco 'SISTEMA N' no PDF v2"
    - "Bloco 'Prazo de entrega' do PDF v2 exibe a frase fundida exata: 'A consultar conforme disponibilidade de estoque, com prazo médio de 20 dias úteis. Pedidos confirmados após aprovação da proposta.'"
    - "Ambiente totalmente vazio (zero luminárias + zero sistemas não-vazios) mantém o header mas mostra mensagem de placeholder ('Nenhum item neste ambiente.' já existente)"
    - "Sistema com fita preenchida mas todos os subtotais zerados (cortesia R$ 0) some do PDF (comportamento intencional D-05)"
    - "Snapshots antigos (pré-v1.1) re-renderizam sem crash e sem duplicar o texto '20 dias úteis'"
    - "atributosMap (Phase 10 WIZ-05) continua propagando para rowFita/rowPerfil/rowDriver — descrição rica não regride"
  artifacts:
    - path: "src/lib/pdfTemplates/v2.ts"
      provides: "Helper isSistemaVazio + filtro em blocoLocal + texto atualizado em blocoTermos"
      contains: "isSistemaVazio"
  key_links:
    - from: "src/lib/pdfTemplates/v2.ts: blocoLocal"
      to: "isSistemaVazio"
      via: "sistemas.filter(s => !isSistemaVazio(s)) antes do map(blocoSistema)"
      pattern: "filter\\(.*isSistemaVazio"
    - from: "src/lib/pdfTemplates/v2.ts: blocoTermos"
      to: "string literal 'com prazo médio de 20 dias úteis'"
      via: "hardcoded no template"
      pattern: "prazo médio de 20 dias úteis"

threat_model:
  trust_boundaries:
    - "Snapshot orçamento (orcamentos.ambientes JSONB) → template v2 → html2pdf"
  threats:
    - id: "T-11-01"
      category: "Tampering"
      component: "isSistemaVazio (filtro implícito de bloco)"
      disposition: "accept"
      mitigation: "Helper é pure function determinística baseada em qtd/preço; usuário malicioso só conseguiria 'esconder' o próprio bloco que ele mesmo zerou — sem ganho de impacto"
    - id: "T-11-02"
      category: "Information Disclosure"
      component: "blocoLocal (filter step)"
      disposition: "mitigate"
      mitigation: "Filter NÃO toca em sistemas com qualquer subtotal > 0; o invariante 'sistema com venda real aparece' é coberto pelos asserts do verify"
    - id: "T-11-03"
      category: "Denial of Service"
      component: "isSistemaVazio chamado N vezes por ambiente"
      disposition: "accept"
      mitigation: "Custo O(1) por sistema (3 chamadas de função aritmética simples já usadas pelo template); zero impacto perceptível"
---

<objective>
Plan 01 (Wave 1) — Aplicar PDF-01 (esconder sistemas vazios + cascade ambiente) e PDF-02 (texto fundido "prazo médio de 20 dias úteis") no template v2. Tudo em um único arquivo (`src/lib/pdfTemplates/v2.ts`), sem regredir o `atributosMap` da Phase 10 (WIZ-05) e sem tocar em PDF v1.

Purpose: O cliente final está vendo placeholders "SISTEMA 1" com fita 0m e driver R$ 0 (resíduo de orçamentos onde o colab criou o sistema mas não preencheu produto/preço). Além disso, a Comercial padronizou "20 dias úteis" como prazo médio — texto precisa aparecer no PDF sem duplicar em snapshots antigos. Foco em surgical edit: helper local + 1 filtro + 1 string.

Output: `src/lib/pdfTemplates/v2.ts` modificado (3 mudanças cirúrgicas).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-pdf-v2-dashboard/11-CONTEXT.md
@.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-05-SUMMARY.md
@src/lib/pdfTemplates/v2.ts
@src/types/orcamento.ts

<interfaces>
<!-- Já importados em v2.ts (linhas 11-22). Reusar diretamente — NÃO importar nada novo. -->

From src/types/orcamento.ts (já no v2.ts):
```typescript
export function calcularDemandaFita(sistema: SistemaIluminacao): number;
export function calcularConsumoW(sistema: SistemaIluminacao): number;
export function calcularQtdDrivers(sistema: SistemaIluminacao): number;
export function calcularSubtotalDriverSistema(sistema: SistemaIluminacao): number;
export function calcularSubtotalPerfilSistema(sistema: SistemaIluminacao): number;
```

ATENÇÃO: NÃO existe `calcularSubtotalFitaSistema` em orcamento.ts (o CONTEXT D-01 menciona o nome mas é hipotético). Para implementar "sistema vazio" literal ao roadmap (Phase 11 success #1: "0m de fita, 0W de consumo e 0 driver"), usar a combinação `calcularDemandaFita(sis) === 0 && calcularConsumoW(sis) === 0 && calcularQtdDrivers(sis) === 0`. Esses 3 helpers JÁ ESTÃO importados em v2.ts.

From src/lib/pdfTemplates/v2.ts (Phase 10 WIZ-05 — NÃO REGREDIR):
```typescript
export type AtributosMap = Record<string, { atributos: Record<string, unknown> | null; potencia_watts: number | null }>;
function blocoSistema(sis, indexNoLocal, atributosMap): string;       // l.205
function blocoLocal(local, sistemas, atributosMap): string;           // l.220 — ALVO do filter
function blocoAmbiente(amb, atributosMap): string;                    // l.229
function blocoTermos(): string;                                       // l.302 — ALVO da string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar helper isSistemaVazio + aplicar filter em blocoLocal (PDF-01)</name>
  <files>src/lib/pdfTemplates/v2.ts</files>
  <behavior>
    - isSistemaVazio({fita.wm 0, demanda 0, consumo 0, qtdDrivers 0}) → true (D-01 + literal roadmap)
    - isSistemaVazio(sistema com fita preenchida + driver normal) → false
    - isSistemaVazio(sistema só com perfil > 0, sem fita+driver) → true (D-02: perfil ignorado)
    - isSistemaVazio(sistema com fita preenchida mas precoUnitario R$ 0) → true se demanda+consumo > 0 mas qtd produtos × 0 → subtotal 0... AVISO: literal ao roadmap, o critério é demanda/consumo/driver e NÃO subtotal. Fita com qtd > 0 e preço R$ 0 NÃO é vazio (tem demanda real). D-05 fala em "sistema com fita preenchida mas preço R$ 0" caindo no caso vazio quando subtotalFita = qtd × 0 = 0 — mas o ROADMAP success criterion #1 é explícito sobre 0m/0W/0 driver. **Seguir roadmap (critério físico, não financeiro)** — fita com 1m de demanda NÃO é vazio mesmo a R$ 0. Documentar essa divergência consciente do CONTEXT D-05 no commit.
    - blocoLocal recebe lista de sistemas onde algum é vazio → renderiza só os não-vazios
    - blocoLocal recebe lista com TODOS sistemas vazios → renderiza apenas o header de local (se local não-null) sem nenhum sistema (cascade D-03 trata o caso ambiente totalmente vazio via empty-note pré-existente l.246)
  </behavior>
  <action>
    Em `src/lib/pdfTemplates/v2.ts`:

    1. **Adicionar helper standalone** logo antes da seção `/* Builders */` (após `agruparPorLocal`, ~linha 82), exatamente:

    ```typescript
    /**
     * PDF-01: Sistema é considerado "vazio" quando não tem nenhum elemento físico real
     * (sem fita LED demandada, sem consumo em watts, sem driver). Perfil é ignorado de propósito
     * (D-02 CONTEXT.md Phase 11): perfil isolado não é um "sistema" no domínio AURA.
     * Sistemas vazios são filtrados antes de renderizar para não vazar placeholders no PDF do cliente.
     */
    function isSistemaVazio(sis: SistemaIluminacao): boolean {
      return calcularDemandaFita(sis) === 0
        && calcularConsumoW(sis) === 0
        && calcularQtdDrivers(sis) === 0;
    }
    ```

    NÃO criar export — função fica local ao módulo (consistente com rowLuminaria, blocoSistema, etc.).

    2. **Aplicar filter em `blocoLocal` (l.220)**. Trocar a linha:
    ```typescript
    const sistemasHtml = sistemas.map((sis, i) => blocoSistema(sis, i, atributosMap)).join("");
    ```
    por:
    ```typescript
    const sistemasHtml = sistemas
      .filter(sis => !isSistemaVazio(sis))
      .map((sis, i) => blocoSistema(sis, i, atributosMap))
      .join("");
    ```

    3. **NÃO MEXER em mais nada.** Especificamente NÃO alterar:
    - `blocoSistema` (linha 205) — continua função pura recebendo um sistema; quem decide é o caller (`blocoLocal`)
    - `blocoAmbiente` (linha 229) — o `empty` check existente (l.236 — `!amb.luminarias.length && !amb.sistemas.length`) cobre cascade D-03 quando o ambiente foi criado vazio. Se `amb.sistemas` tem itens mas todos vazios, o `gruposHtml` renderiza headers de local sem conteúdo — comportamento aceitável dado a paleta atual. Não inventar nova condição de "todos sistemas vazios" para preservar simplicidade.
    - `atributosMap` propagação — mantém intacta (WIZ-05 Phase 10 já entregue)
    - Cálculos de `agruparPorLocal`, `blocoResumoFitas` (resumo de fitas é por código global e não por sistema; sistemas vazios já não contribuem porque demanda = 0)

    4. Commit: `feat(11): hide empty systems in PDF v2 (PDF-01) — D-01..D-04`
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $f = Get-Content 'src/lib/pdfTemplates/v2.ts' -Raw; if ($f -notmatch 'function isSistemaVazio') { throw 'isSistemaVazio helper missing' }; if ($f -notmatch 'calcularDemandaFita\(sis\) === 0[\s\S]*calcularConsumoW\(sis\) === 0[\s\S]*calcularQtdDrivers\(sis\) === 0') { throw 'isSistemaVazio body does not match the 3 conditions in AND' }; if ($f -notmatch '\.filter\(sis => !isSistemaVazio\(sis\)\)') { throw 'blocoLocal filter not wired' }; if ($f -notmatch 'function blocoSistema\(sis: SistemaIluminacao, indexNoLocal: number, atributosMap: AtributosMap\)') { throw 'blocoSistema signature regressed (atributosMap missing)' }; Write-Host 'PDF-01 helper + filter OK'"</automated>
  </verify>
  <done>
    - Helper `isSistemaVazio` declarado local ao módulo
    - `blocoLocal` filtra sistemas vazios antes do map
    - `blocoSistema` signature inalterada (atributosMap continua propagando)
    - npm run lint passa
    - Build TS compila sem erros
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fundir frase de prazo no blocoTermos (PDF-02)</name>
  <files>src/lib/pdfTemplates/v2.ts</files>
  <behavior>
    - blocoTermos() retorna HTML contendo a string EXATA: "A consultar conforme disponibilidade de estoque, com prazo médio de 20 dias úteis. Pedidos confirmados após aprovação da proposta."
    - Texto fica hardcoded no template (D-07) — snapshots antigos não duplicam porque snapshot nunca armazenou esse boilerplate
    - Garantia, Condições de pagamento e Observações ficam intocados (D-08)
  </behavior>
  <action>
    Em `src/lib/pdfTemplates/v2.ts`, dentro de `blocoTermos()` (linha ~302):

    Localizar o bloco:
    ```html
    <div class="term-block">
      <h3 class="term-header">Prazo de entrega</h3>
      <p>A consultar conforme disponibilidade de estoque. Pedidos confirmados após aprovação da proposta.</p>
    </div>
    ```

    Trocar a tag `<p>` (linha 309) por:
    ```html
    <p>A consultar conforme disponibilidade de estoque, com prazo médio de 20 dias úteis. Pedidos confirmados após aprovação da proposta.</p>
    ```

    Mudança: ponto-final após "estoque" vira vírgula, e insere "com prazo médio de 20 dias úteis." antes de "Pedidos confirmados". Resultado EXATO do D-06 — sem aspas extras, sem espaço duplo, sem `<strong>`.

    NÃO mexer em:
    - `term-header` "Prazo de entrega" (rótulo)
    - Bloco Garantia (linha ~312-315)
    - Bloco Condições de pagamento (linha ~317-320)
    - Bloco Observações (linhas ~322-329)

    Commit: `feat(11): merge "prazo médio de 20 dias úteis" into PDF v2 termos (PDF-02) — D-06`
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $f = Get-Content 'src/lib/pdfTemplates/v2.ts' -Raw; $target = 'A consultar conforme disponibilidade de estoque, com prazo médio de 20 dias úteis. Pedidos confirmados após aprovação da proposta.'; if (-not $f.Contains($target)) { throw 'Frase fundida PDF-02 not found exactly' }; if ($f -match 'A consultar conforme disponibilidade de estoque\. Pedidos confirmados') { throw 'Old phrasing still present — must be replaced' }; $count = ([regex]::Matches($f, 'prazo médio de 20 dias úteis')).Count; if ($count -ne 1) { throw ('Expected 1 occurrence of prazo médio, got ' + $count) }; Write-Host 'PDF-02 prazo merge OK'"</automated>
  </verify>
  <done>
    - String exata do D-06 está em blocoTermos
    - Versão antiga (sem "com prazo médio") removida
    - Apenas 1 ocorrência de "prazo médio de 20 dias úteis" no arquivo (evita duplicação acidental)
    - npm run build passa
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Smoke automatizado — lint + build + tests existentes não regridem</name>
  <files>(nenhum — só validação)</files>
  <action>
    Rodar a tríade de validação local:
    1. `npm run lint` — zero novos errors/warnings em `src/lib/pdfTemplates/v2.ts`
    2. `npm run build` — TypeScript compila sem erros
    3. `npm run test` — Suite Vitest existente (55 testes Phase 10) passa sem regressão

    Se algum falhar: investigar, corrigir, recommit. Não passar pra Wave 2 com build/test broken.

    Sem commit nessa task (só verificação).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npm run lint 2>&1 | Tee-Object -Variable lintOut | Out-Null; if ($LASTEXITCODE -ne 0) { throw ('lint failed: ' + ($lintOut -join \"`n\")) }; npm run build 2>&1 | Tee-Object -Variable buildOut | Out-Null; if ($LASTEXITCODE -ne 0) { throw ('build failed: ' + ($buildOut -join \"`n\")) }; npm run test -- --run 2>&1 | Tee-Object -Variable testOut | Out-Null; if ($LASTEXITCODE -ne 0) { throw ('tests failed: ' + ($testOut -join \"`n\")) }; Write-Host 'lint + build + tests OK'"</automated>
  </verify>
  <done>
    - lint: 0 novos warnings em v2.ts
    - build: TS compila
    - tests: 55+ passam (suite Phase 10 não regride)
  </done>
</task>

</tasks>

<verification>
- Helper `isSistemaVazio` declarado e chamado em `blocoLocal`
- Frase fundida exata em `blocoTermos`
- `atributosMap` da Phase 10 (WIZ-05) intacto (assinatura de `blocoSistema/rowFita/rowPerfil/rowDriver` não regride)
- lint + build + test pass
</verification>

<success_criteria>
- PDF v2 não renderiza bloco "SISTEMA N" quando `calcularDemandaFita === 0 && calcularConsumoW === 0 && calcularQtdDrivers === 0`
- PDF v2 exibe "com prazo médio de 20 dias úteis" no bloco Prazo de entrega, sem duplicação
- Snapshots antigos continuam renderizando (build TS sem erro confirma assinaturas estáveis)
- Phase 10 WIZ-05 não regride (descrição rica continua propagando via atributosMap)
- Plan 11-02 (Dashboard) pode rodar em paralelo (zero overlap de arquivo)
</success_criteria>

<output>
After completion, create `.planning/phases/11-pdf-v2-dashboard/11-01-SUMMARY.md` documenting:
- Helper isSistemaVazio adicionado (linha exata)
- Filter aplicado em blocoLocal
- Frase fundida em blocoTermos (linha exata)
- Decisão consciente: critério é físico (demanda/consumo/driver), não financeiro (subtotal) — diverge ligeiramente do D-05 CONTEXT mas alinha 100% com roadmap success criterion #1
- Commits criados
- Validação smoke automatizada (lint/build/test) passou
</output>
