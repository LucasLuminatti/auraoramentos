---
phase: 11-pdf-v2-dashboard
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AdminDashboard.tsx
autonomous: true
requirements:
  - DASH-01
tags: [dashboard, admin, kpi-cleanup, tanstack-query]

must_haves:
  truths:
    - "Os 6 cards de métrica (Receita Efetiva, Receita Prevista, Pipeline, Ticket Médio, Conversão, Ciclo Médio) NÃO aparecem na tab Início"
    - "Um único card 'Orçamentos em Aberto' aparece com 2 linhas (Rascunho: R$ X / Pendente: R$ Y) + divider + Total: R$ X+Y"
    - "O card busca dados via useQuery direto de Supabase com queryKey ['orcamentos-em-aberto']"
    - "Definição de em aberto = status IN ('rascunho','pendente'); soma da coluna orcamentos.valor cross-rep (admin vê todos)"
    - "Empty state (zero orçamentos em aberto): card mostra Rascunho R$ 0,00 / Pendente R$ 0,00 / Total R$ 0,00 — NÃO esconde o card"
    - "Gráfico 'Receita Mensal' (BarChart pendente/aprovado) é mantido inalterado"
    - "Tabela 'Top 5 Clientes' é mantida; label muda de 'por Receita Fechada' para 'por Receita Aprovada' (D-13)"
    - "Imports lucide-react órfãos (Trophy KPI, DollarSign, TrendingUp, Target, BarChart3, Clock) são removidos; Trophy do Top 5 fica"
    - "useMemo kpis (linha 59-102) é removido por completo — sem código morto"
  artifacts:
    - path: "src/components/AdminDashboard.tsx"
      provides: "Tab Início com card único 'Orçamentos em Aberto' + gráfico + Top 5"
      contains: "Orçamentos em Aberto"
  key_links:
    - from: "AdminDashboard.tsx: useQuery"
      to: "supabase.from('orcamentos').select('status, valor').in('status', ['rascunho','pendente'])"
      via: "TanStack Query"
      pattern: "useQuery.*orcamentos-em-aberto"
    - from: "AdminDashboard.tsx: card 'Orçamentos em Aberto'"
      to: "agregação local (rascunho + pendente + total)"
      via: "useMemo derivado do resultado da query"
      pattern: "Orçamentos em Aberto"

threat_model:
  trust_boundaries:
    - "Admin authenticated UI → Supabase REST (orcamentos.SELECT)"
  threats:
    - id: "T-11-04"
      category: "Information Disclosure"
      component: "useQuery orcamentos-em-aberto (cross-rep aggregate)"
      disposition: "accept"
      mitigation: "Apenas admin chega no AdminDashboard (route guard via AdminRoute em Admin.tsx); RLS de orcamentos atualmente USING(true) para SELECT (Phase 10 D-32 explicitamente fora do escopo) — soma cross-rep é o comportamento esperado pelo D-11"
    - id: "T-11-05"
      category: "Tampering"
      component: "Agregação client-side (sum no JS)"
      disposition: "accept"
      mitigation: "Admin pode inspecionar e refazer o cálculo; nenhum dado sensível além do que ele já vê em /admin/pedidos. Comparação via SQL no smoke (plan 11-03) é o verifier autorizado"
    - id: "T-11-06"
      category: "Denial of Service"
      component: "Query sem LIMIT em orcamentos"
      disposition: "mitigate"
      mitigation: "Query filtra status IN ('rascunho','pendente') — em prod hoje (~dezenas de orcs em aberto) o payload é trivial. Se crescer >5k linhas, vira phase de paginação/agregação server-side"
    - id: "T-11-07"
      category: "Information Disclosure"
      component: "Remoção de KPIs (Receita Efetiva, etc.)"
      disposition: "accept"
      mitigation: "Dados antigos não viram secret; cleanup é UX, não security. Cards eram derived state, fonte (orcamentos.valor + status) continua acessível"
---

<objective>
Plan 02 (Wave 1) — Simplificar a tab Início do admin: remover 6 cards de KPI (Receita Efetiva/Prevista/Pipeline/Ticket Médio/Conversão/Ciclo Médio) e substituir por 1 card único "Orçamentos em Aberto" quebrado por status (Rascunho + Pendente + Total) via TanStack Query. Manter gráfico mensal e Top 5 Clientes; renomear label "Receita Fechada" → "Receita Aprovada" (Phase 7 enum rename).

Purpose: Os 6 cards atuais misturam métricas (ticket médio, conversão, ciclo) que o Lenny não usa no dia-a-dia e ficaram defasados após o rename de status. O único número que importa pra abrir o dashboard é "quanto tem em aberto agora?" — somatório cross-rep de rascunho + pendente. Cleanup completo dos useMemo órfãos pra não deixar código morto.

Output: `src/components/AdminDashboard.tsx` enxuto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-pdf-v2-dashboard/11-CONTEXT.md
@src/components/AdminDashboard.tsx
@src/pages/Admin.tsx

<interfaces>
<!-- Conhecidos hoje no AdminDashboard.tsx -->

```typescript
// Props (preserved):
interface AdminDashboardProps {
  orcamentos: Orcamento[];
}

// A REMOVER:
// - useMemo kpis (l.59-102) — calculava receitaEfetiva, receitaPrevista, pipeline, ticketMedio, taxaConversao, cicloMedio
// - const cards = [...] (l.156-163) — 6 items
// - Grid de cards (l.183-195) — 6 Cards renderizando { title, value, icon }
// - lucide imports não usados após cleanup: DollarSign, TrendingUp, Target, BarChart3, Clock

// A MANTER:
// - topClientes useMemo (l.105-117) — Top 5
// - monthlyData useMemo (l.120-140) — gráfico (já usa chaves "aprovado"/"pendente" corretas)
// - motivosData useMemo (l.143-154) — Motivos de Perda
// - Trophy import — usado no Top 5 (l.251)
// - ThumbsDown import — usado no Motivos de Perda (l.224)
// - Period filter (l.42-48 PERIOD_OPTIONS + l.168-180 Select)

// A ADICIONAR:
// - import { useQuery } from "@tanstack/react-query"
// - import { supabase } from "@/integrations/supabase/client"
// - import { FileClock } from "lucide-react" (icon do card único — Claude's Discretion na escolha; FileClock combina com "em aberto")
// - useQuery hook para fetch SELECT status, valor FROM orcamentos WHERE status IN ('rascunho','pendente')
// - Card "Orçamentos em Aberto" renderizando rascunho/pendente/total
```

NOTA: TanStack Query já está em uso no projeto (initialized em src/App.tsx). Padrão de queryKey + queryFn já estabelecido em Step3Revisao.tsx (Phase 10 batch lookup product_variants) — reusar abordagem.

A coluna `orcamentos.valor` é `numeric` em Postgres → vem como `number | string` no Supabase JS SDK. Garantir `Number(o.valor)` antes do soma (já faz isso no kpis antigo l.64).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remover 6 cards + useMemo kpis + imports órfãos (DASH-01 cleanup)</name>
  <files>src/components/AdminDashboard.tsx</files>
  <behavior>
    - Após edit: arquivo NÃO contém referências a receitaEfetiva, receitaPrevista, pipeline, ticketMedio, taxaConversao, cicloMedio
    - Após edit: imports lucide-react NÃO contém DollarSign, TrendingUp, Target, BarChart3, Clock (mas mantém Trophy e ThumbsDown)
    - Após edit: imports date-fns NÃO contém differenceInDays (era só usado pelo cicloMedio)
    - Após edit: o grid grid-cols-2 ... lg:grid-cols-6 dos KPI cards (l.183-195) NÃO existe mais
    - Após edit: const cards = [...] (l.156-163) NÃO existe mais
    - Após edit: monthlyData, topClientes, motivosData useMemos PERMANECEM
    - Após edit: label "Top 5 Clientes por Receita Fechada" (l.252) vira "Top 5 Clientes por Receita Aprovada" (D-13)
  </behavior>
  <action>
    Em `src/components/AdminDashboard.tsx`:

    1. **Imports lucide-react (l.5)** — manter só os usados após cleanup:
    ```typescript
    // Antes:
    import { DollarSign, TrendingUp, Target, BarChart3, Clock, Trophy, ThumbsDown } from "lucide-react";
    // Depois:
    import { Trophy, ThumbsDown, FileClock } from "lucide-react";
    ```
    (`FileClock` será o icon do card novo; é puramente decorativo — Claude's Discretion. Se preferir `Inbox` ou `Hourglass`, vale também — mas FileClock combina com "orçamento + tempo aberto".)

    2. **Import date-fns (l.6)** — remover `differenceInDays`:
    ```typescript
    // Antes:
    import { differenceInDays, subDays, startOfMonth, subMonths, format, isAfter } from "date-fns";
    // Depois:
    import { subDays, startOfMonth, subMonths, format, isAfter } from "date-fns";
    ```

    3. **Remover useMemo kpis** (l.59-102) por completo. Apaga do `const kpis = useMemo(() => {` até o `}, [filtered]);` que fecha o hook.

    4. **Remover `const cards = [...]`** (l.156-163) por completo.

    5. **Remover o grid de KPI cards** (l.182-195) — bloco JSX:
    ```jsx
    {/* KPI Cards */}
    <div className="grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-6">
      {cards.map((c) => ( ... ))}
    </div>
    ```
    Apaga inteiro (incluindo comentário).

    6. **Renomear label Top 5** (l.252) — D-13:
    ```jsx
    // Antes:
    Top 5 Clientes por Receita Fechada
    // Depois:
    Top 5 Clientes por Receita Aprovada
    ```

    7. **Validar monthlyData** (l.120-140): conferir se o useMemo ainda usa chaves `pendente`/`aprovado` (não `fechado`). Olhar — já está correto pela Phase 10 D-33 (commit 0225952 etc.). Se sobrar referência a "fechado" em qualquer lugar do arquivo, corrigir agora. Buscar literal `"fechado"` ou `'fechado'`.

    NÃO mexer em:
    - props interface `AdminDashboardProps` (l.25-27)
    - useMemo `filtered` (l.53-57)
    - useMemo `topClientes` (l.105-117)
    - useMemo `monthlyData` (l.120-140) — exceto se tiver string "fechado" residual
    - useMemo `motivosData` (l.143-154)
    - Period filter (Select + PERIOD_OPTIONS)
    - Card Receita Mensal (BarChart, l.199-217)
    - Card Motivos de Perda (l.220-244)
    - Card Top 5 Clientes (l.247-278) — exceto a label da CardTitle

    Commit: `refactor(11): remove 6 KPI cards from AdminDashboard + rename Fechada→Aprovada (DASH-01 partial) — D-09,D-13`
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $f = Get-Content 'src/components/AdminDashboard.tsx' -Raw; foreach ($needle in @('receitaEfetiva','receitaPrevista','ticketMedio','taxaConversao','cicloMedio','const cards =','differenceInDays','Receita Fechada')) { if ($f -match [regex]::Escape($needle)) { throw ('Residual reference still present: ' + $needle) } }; foreach ($needle in @('Receita Aprovada','useMemo','topClientes','monthlyData')) { if ($f -notmatch [regex]::Escape($needle)) { throw ('Missing expected: ' + $needle) } }; if ($f -match 'DollarSign|TrendingUp|Target|BarChart3, Clock|Clock,' -and $f -match 'from \"lucide-react\"') { $importLine = ($f -split \"`n\" | Select-String 'from \"lucide-react\"').Line; if ($importLine -match 'DollarSign|TrendingUp|Target|BarChart3|\bClock\b') { throw ('Orphan lucide imports in line: ' + $importLine) } }; if (Select-String -Path 'src/components/AdminDashboard.tsx' -Pattern 'fechado' -CaseSensitive:$false -Quiet) { $matches = (Select-String -Path 'src/components/AdminDashboard.tsx' -Pattern 'fechado' -CaseSensitive:$false).Line -join '; '; throw ('Residual fechado: ' + $matches) }; Write-Host 'cleanup OK'"</automated>
  </verify>
  <done>
    - useMemo kpis removido
    - const cards removido
    - Grid de 6 cards removido
    - Imports órfãos removidos (DollarSign, TrendingUp, Target, BarChart3, Clock, differenceInDays)
    - Label "Fechada" → "Aprovada"
    - Zero ocorrência de "fechado" no arquivo (case-insensitive)
    - npm run build passa
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adicionar card único "Orçamentos em Aberto" com useQuery (DASH-01 core)</name>
  <files>src/components/AdminDashboard.tsx</files>
  <behavior>
    - Hook useQuery dispara SELECT status, valor FROM orcamentos WHERE status IN ('rascunho','pendente') em Supabase
    - queryKey estável: ['orcamentos-em-aberto']
    - Resultado é agregado client-side em { rascunho: number, pendente: number, total: number }
    - Loading state: card mostra valores como "—" ou "R$ ..." discreto (Claude's Discretion)
    - Error state: card mostra "Erro ao carregar" (toast opcional)
    - Empty state (resultado []): card mostra R$ 0,00 / R$ 0,00 / R$ 0,00 (D-15)
    - Renderiza ANTES do Period filter section ou logo após — Claude's Discretion (recomendação: logo após "Visão Geral" header, antes do gráfico)
  </behavior>
  <action>
    Em `src/components/AdminDashboard.tsx`:

    1. **Adicionar imports no topo do arquivo**:
    ```typescript
    import { useQuery } from "@tanstack/react-query";
    import { supabase } from "@/integrations/supabase/client";
    ```
    (Verificar se a importação `@/integrations/supabase/client` é correta — sim, é. Veja Step3Revisao.tsx e similares.)

    2. **Adicionar hook useQuery** dentro do componente `AdminDashboard`, logo após `const [period, setPeriod] = useState("180");`:

    ```typescript
    // DASH-01: somatório de orçamentos em aberto (cross-rep) — D-10, D-11, D-14
    const { data: emAberto, isLoading: emAbertoLoading } = useQuery({
      queryKey: ['orcamentos-em-aberto'],
      queryFn: async () => {
        const { data, error } = await supabase
          .from('orcamentos')
          .select('status, valor')
          .in('status', ['rascunho', 'pendente']);
        if (error) throw error;
        return data ?? [];
      },
    });

    const emAbertoTotais = useMemo(() => {
      const rascunho = (emAberto ?? [])
        .filter((o) => o.status === 'rascunho')
        .reduce((s, o) => s + Number(o.valor ?? 0), 0);
      const pendente = (emAberto ?? [])
        .filter((o) => o.status === 'pendente')
        .reduce((s, o) => s + Number(o.valor ?? 0), 0);
      return { rascunho, pendente, total: rascunho + pendente };
    }, [emAberto]);
    ```

    3. **Adicionar o card no JSX**, logo após o header `<div className="flex items-center justify-between"> ... </div>` (l.168-180), ANTES do bloco `{/* Charts */}`:

    ```jsx
    {/* Orçamentos em Aberto (DASH-01) */}
    <Card className="border-l-4 border-l-primary">
      <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-3">
        <CardTitle className="text-sm font-medium">Orçamentos em Aberto</CardTitle>
        <FileClock className="h-4 w-4 text-muted-foreground" />
      </CardHeader>
      <CardContent className="space-y-2">
        <div className="flex items-center justify-between text-sm">
          <span className="text-muted-foreground">Rascunho</span>
          <span className="font-medium">{emAbertoLoading ? '—' : formatCurrency(emAbertoTotais.rascunho)}</span>
        </div>
        <div className="flex items-center justify-between text-sm">
          <span className="text-muted-foreground">Pendente</span>
          <span className="font-medium">{emAbertoLoading ? '—' : formatCurrency(emAbertoTotais.pendente)}</span>
        </div>
        <div className="border-t pt-2 flex items-center justify-between">
          <span className="font-semibold">Total</span>
          <span className="text-lg font-bold">{emAbertoLoading ? '—' : formatCurrency(emAbertoTotais.total)}</span>
        </div>
      </CardContent>
    </Card>
    ```

    Markup respeita design tokens (border-l-primary, text-muted-foreground, etc.). Sem cor hardcoded.

    4. **NÃO regredir** nada de Task 1 (cleanup já feito).

    Commit: `feat(11): add card "Orçamentos em Aberto" via useQuery (DASH-01) — D-10,D-11,D-14,D-15`
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $f = Get-Content 'src/components/AdminDashboard.tsx' -Raw; foreach ($needle in @('useQuery','queryKey: [''orcamentos-em-aberto'']','.in(''status'', [''rascunho'', ''pendente''])','Orçamentos em Aberto','emAbertoTotais','formatCurrency(emAbertoTotais.rascunho)','formatCurrency(emAbertoTotais.pendente)','formatCurrency(emAbertoTotais.total)')) { if (-not $f.Contains($needle)) { throw ('Missing: ' + $needle) } }; if ($f -notmatch 'from \"@tanstack/react-query\"') { throw 'useQuery import missing' }; if ($f -notmatch 'from \"@/integrations/supabase/client\"') { throw 'supabase import missing' }; npm run build 2>&1 | Out-Null; if ($LASTEXITCODE -ne 0) { throw 'TypeScript build failed' }; Write-Host 'card em aberto OK'"</automated>
  </verify>
  <done>
    - useQuery declarado com queryKey ['orcamentos-em-aberto']
    - Query Supabase filtra status IN ('rascunho','pendente')
    - emAbertoTotais useMemo agrega rascunho + pendente + total
    - Card renderizado com 3 linhas (Rascunho, Pendente, Total) usando formatCurrency
    - Loading state mostra "—"
    - Build TS compila sem erros
    - npm run lint sem novos warnings
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Smoke automatizado — lint + build + tests não regridem</name>
  <files>(nenhum — só validação)</files>
  <action>
    Rodar:
    1. `npm run lint` — zero novos errors/warnings em `src/components/AdminDashboard.tsx`
    2. `npm run build` — TypeScript compila
    3. `npm run test` — Suite Vitest passa (não esperamos novos testes nesta task; só não regredir)

    Sem commit.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; npm run lint 2>&1 | Tee-Object -Variable lintOut | Out-Null; if ($LASTEXITCODE -ne 0) { throw ('lint failed: ' + ($lintOut -join \"`n\")) }; npm run build 2>&1 | Tee-Object -Variable buildOut | Out-Null; if ($LASTEXITCODE -ne 0) { throw ('build failed: ' + ($buildOut -join \"`n\")) }; npm run test -- --run 2>&1 | Tee-Object -Variable testOut | Out-Null; if ($LASTEXITCODE -ne 0) { throw ('tests failed: ' + ($testOut -join \"`n\")) }; Write-Host 'lint + build + tests OK'"</automated>
  </verify>
  <done>
    - lint: 0 novos warnings
    - build: TS compila
    - tests: suite Phase 10 não regride
  </done>
</task>

</tasks>

<verification>
- 6 cards de KPI removidos + useMemo kpis apagado + imports órfãos limpos
- Card único "Orçamentos em Aberto" renderiza com useQuery + agregação rascunho/pendente/total
- Label "Receita Fechada" → "Receita Aprovada" (D-13)
- Gráfico mensal + Top 5 + Motivos de Perda preservados
- lint + build + test pass
- Plan 11-01 (PDF v2) corre em paralelo (zero overlap de arquivos)
</verification>

<success_criteria>
- Tab Início abre com 1 card destacado em vez de 6 cards
- Card "Orçamentos em Aberto" mostra valores corretos (validação via SQL no plan 11-03)
- Loading state não quebra a UI
- Empty state (zero em aberto) mostra R$ 0,00 / R$ 0,00 / R$ 0,00
- Zero código morto (kpis, cards, differenceInDays, lucide órfãos removidos)
</success_criteria>

<output>
After completion, create `.planning/phases/11-pdf-v2-dashboard/11-02-SUMMARY.md` documenting:
- Imports antes/depois (linha exata da diff)
- useMemo kpis removido (linha inicial/final)
- Card "Orçamentos em Aberto" adicionado (snippet final)
- queryKey escolhido + staleTime (default TanStack)
- Decisão Claude's Discretion: icon (FileClock) + paleta (border-l-primary)
- Commits criados
- Validação smoke automatizada passou
</output>
