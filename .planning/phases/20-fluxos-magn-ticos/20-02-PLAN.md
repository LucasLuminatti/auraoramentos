---
phase: 20-fluxos-magn-ticos
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/components/ComposicaoCard.tsx
autonomous: true
requirements: [SIST-01, SIST-02, DRV-01, DRV-02, COMP-01, COMP-02, COMP-03]
must_haves:
  truths:
    - "O card exibe o trilho âncora (código/descrição readonly, qtd e preço editáveis) e a carga total derivada de Σ(módulo.potenciaW × quantidade)"
    - "O colaborador adiciona módulos por busca escopada à família (48V → magneto_48v; 24V → tiny_magneto), nunca vendo trilhos/conectores/drivers"
    - "O painel de driver mostra o estado correto (sem_carga / recomendado / aplicado_ok / subdimensionado / excede_200w) e o botão 'Aplicar' insere o driver na composicao[] num clique"
    - "Para 48V o painel usa LM2343/LM2344 (×1.05); para 24V busca o menor driver 24V compatível via Supabase"
    - "A busca de driver dentro de uma composição 48V só retorna drivers 48V (voltage lock por construção, sem toast de erro)"
    - "O checklist marca presença/ausência de conector (MAGNETO→LM2338; TINY→LM3168 OU LM3169) e kit LM2987 só se trilho for embutir; atalho '+ Adicionar' insere o SKU correto"
  artifacts:
    - path: "src/components/ComposicaoCard.tsx"
      provides: "Card de composição magnética: trilho âncora, módulos, painel de driver, checklist, voltage lock"
      contains: "const ComposicaoCard"
      min_lines: 180
  key_links:
    - from: "src/components/ComposicaoCard.tsx"
      to: "calcularCargaComposicao + recomendarDriver48V (orcamento.ts)"
      via: "import dos helpers do Plano 01"
      pattern: "recomendarDriver48V"
    - from: "src/components/ComposicaoCard.tsx"
      to: "REGRAS_COMPOSICAO (orcamento.ts)"
      via: "leitura do checklist por família"
      pattern: "REGRAS_COMPOSICAO\\[item.sistema"
    - from: "src/components/ComposicaoCard.tsx"
      to: "ProdutoAutocomplete (filtroSistema / filtroVoltagem)"
      via: "busca escopada de módulos e driver"
      pattern: "filtroVoltagem"
---

<objective>
Criar `src/components/ComposicaoCard.tsx` — o sub-componente que renderiza um sistema composto MAGNETO 48V / TINY 24V dentro do AmbienteCard: cabeçalho com badge de tipo e carga, trilho âncora, lista de módulos com "+ Adicionar módulo" (busca escopada), painel de driver com "Aplicar"/"Alterar"/"Reaplicar", e checklist de componentes obrigatórios com atalho "+ Adicionar". É autocontido — recebe `item` (a luminária raiz) e callbacks `onChange`/`onRemove`, sem conhecer o ambiente pai.

Purpose: isolar toda a UI nova de risco-baixo num arquivo separado (RESEARCH recomenda manter AmbienteCard < 800 linhas); o Plano 03 apenas importa e renderiza este componente. Interface-first: o contrato do componente existe antes do AmbienteCard wirá-lo.
Output: ComposicaoCard funcional consumindo os helpers do Plano 01, sem alterar nenhum dado de Fita Padrão.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-fluxos-magn-ticos/20-CONTEXT.md
@.planning/phases/20-fluxos-magn-ticos/20-RESEARCH.md
@.planning/phases/20-fluxos-magn-ticos/20-UI-SPEC.md

<interfaces>
<!-- Contratos prontos do Plano 01 + tipos existentes. NÃO reexplorar. -->

Helpers do Plano 01 (orcamento.ts) — usar diretamente:
```typescript
export function calcularCargaComposicao(composicao: ItemComposicao[] | undefined): number;
export type RecomendacaoDriver48V =
  | { estado: 'sem_carga' }
  | { estado: 'recomendado'; sku: 'LM2343' | 'LM2344'; potenciaW: 100 | 200; potenciaSeguraW: number }
  | { estado: 'excede_200w'; potenciaSeguraW: number };
export function recomendarDriver48V(cargaTotalW: number): RecomendacaoDriver48V;
```

ItemLuminaria / ItemComposicao (orcamento.ts:23,43) — a raiz da composição é um ItemLuminaria com `composicao: ItemComposicao[]`, `sistema: 'magneto_48v'|'tiny_magneto'`, `tensao: 48|24`. Vocabulário de papel (Phase 19 / D-05): 'modulo' | 'driver_recomendado' | 'conector_energia' | 'kit_fixacao'.

REGRAS_COMPOSICAO (orcamento.ts:150):
```typescript
magneto_48v: { conectoresObrigatorios: ['LM2338'], kitFixacaoEmbutir: 'LM2987' }
tiny_magneto: { conectoresObrigatorios: ['LM3168','LM3169'], kitFixacaoEmbutir: 'LM2987' }
```

MARGEM_SEGURANCA_DRIVER = 1.05 (orcamento.ts:144).

ProdutoAutocomplete (após Plano 01 task 2) aceita: filtro, filtroVoltagem, filtroSistema.

Padrão de busca de driver async (AmbienteCard.tsx:148 buscarDriverSugerido) — copiar para 24V:
```typescript
const { data } = await supabase.from('produtos')
  .select('id, codigo, descricao, preco_tabela, preco_minimo, voltagem:tensao, driver_potencia_w:potencia_watts')
  .eq('tipo_produto', 'driver').eq('tensao', 24)
  .gte('potencia_watts', cargaTotalW * MARGEM_SEGURANCA_DRIVER)
  .not('descricao', 'ilike', '%DESCONTINUAR%')
  .order('potencia_watts', { ascending: true }).limit(1);
```

PrecoInput — definido localmente no AmbienteCard hoje; recriar uma versão local equivalente no ComposicaoCard (Input type=number, w-28, borda destructive se value<min).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ComposicaoCard — estrutura, trilho âncora, módulos e carga total</name>
  <files>src/components/ComposicaoCard.tsx</files>
  <read_first>
    - src/components/AmbienteCard.tsx (linhas 25-41 PrecoInput; 380-407 linha de luminária; 49 uid; 148-161 buscarDriverSugerido — padrão a copiar)
    - src/components/ProdutoAutocomplete.tsx (props após Plano 01)
    - .planning/phases/20-fluxos-magn-ticos/20-UI-SPEC.md (seções "Card de Composição Magnética": header, trilho âncora, lista de módulos, botão "+ Adicionar módulo" — classes Tailwind verbatim)
    - .planning/phases/20-fluxos-magn-ticos/20-RESEARCH.md (Pattern 4 snapshot de módulo; Pattern 5 carga; Pitfall 2 potenciaW null → "?W"; Pitfall 6 re-render)
    - src/types/orcamento.ts (ItemComposicao:43, calcularSubtotalComposicao:286)
  </read_first>
  <action>
Criar `src/components/ComposicaoCard.tsx`. Componente recebe props:
```typescript
interface ComposicaoCardProps {
  item: ItemLuminaria;                 // a luminária raiz (trilho âncora) com composicao[]
  onChange: (item: ItemLuminaria) => void;
  onRemove: () => void;
  indice: number;                      // para o label "Sistema N" no header
}
```

Imports: `Badge`, `Button`, `Input` de `@/components/ui/*`; `Plus`, `Trash2`, `Check`, `AlertCircle` de `lucide-react`; `cn` de `@/lib/utils`; `supabase` de `@/integrations/supabase/client`; `ProdutoAutocomplete` de `./ProdutoAutocomplete`; de `@/types/orcamento`: `ItemLuminaria`, `ItemComposicao`, `Produto`, `calcularCargaComposicao`, `recomendarDriver48V`, `calcularSubtotalComposicao`, `formatarMoeda`, `MARGEM_SEGURANCA_DRIVER`, `REGRAS_COMPOSICAO`. Usar imports `@/` — NUNCA relativos `../`.

Definir `PrecoInput` local equivalente ao do AmbienteCard (Input type number, className `w-28`, borda `border-destructive text-destructive` quando `min>0 && value<min`).

Derivar do `item`:
- `is48V = item.sistema === 'magneto_48v'` (tensao 48); `is24V = item.sistema === 'tiny_magneto'` (tensao 24).
- `familiaSistema = item.sistema` (passado como filtroSistema na busca de módulos).
- `cargaTotalW = calcularCargaComposicao(item.composicao)`.
- `modulos = (item.composicao ?? []).filter(c => c.papel === 'modulo')`.

Header (UI-SPEC "Header do card"): `<div className="flex items-center justify-between px-4 py-2 bg-muted/40 border-b">`. À esquerda: badge de tipo — `<Badge variant="outline" className="text-[10px] px-1 py-0 font-semibold border-amber-500 text-amber-700 bg-amber-50">MAGNETO 48V</Badge>` para 48V, ou `border-violet-400 text-violet-700 bg-violet-50` com texto `TINY 24V` para 24V; texto `Sistema {indice + 1}`; badge de carga `<Badge variant="secondary" className="text-xs">Carga: {cargaTotalW}W</Badge>` (só quando cargaTotalW > 0). À direita: `<Button size="icon" variant="ghost" className="h-7 w-7 text-destructive">` com `<Trash2 className="h-3.5 w-3.5" />` chamando `onRemove`.

Corpo: `<div className="rounded-lg border bg-muted/20 overflow-hidden">` envolvendo header + `<div className="p-4 space-y-3">`.

Trilho âncora (UI-SPEC "Trilho âncora") — linha readonly + qtd/preço editáveis:
- `<Input value={item.codigo} readOnly className="bg-muted/50" />` (código)
- `<Input value={item.descricao} readOnly className="bg-muted/50" />` (descrição)
- Qtd: `<Input type="number" min={1} value={item.quantidade} onChange={...} className="w-20 h-8" />` → `onChange({ ...item, quantidade })`
- Preço: `<PrecoInput value={item.precoUnitario} min={item.precoMinimo} onChange={(v) => onChange({ ...item, precoUnitario: v })} />`
- Badge subtotal composição: `formatarMoeda(item.precoUnitario * item.quantidade + calcularSubtotalComposicao(item))`.

Lista de módulos (UI-SPEC "Lista de módulos") — para cada `ItemComposicao` papel='modulo', renderizar linha compacta: código readonly `w-28`, descrição readonly `flex-1`, qtd `Input w-20`, badge potência `<Badge variant="outline" className="text-xs">{m.potenciaW != null ? m.potenciaW + 'W' : '?W'}</Badge>` (Pitfall 2: "?W" quando undefined), `PrecoInput`, `<Button size="icon" variant="ghost" className="h-7 w-7 text-destructive">` com `Trash2 h-3.5 w-3.5`. Editar qtd/preço atualiza o item dentro de `composicao[]` por `id` (não índice). Remover filtra por `id`.

Botão "+ Adicionar módulo" (UI-SPEC): controla um estado local `mostrarBuscaModulo` que revela um `ProdutoAutocomplete filtro="luminaria" filtroSistema={familiaSistema}` (escopa à família via Plano 01 — só módulos da família, exclui trilho/conector/driver/kit). Ao selecionar, criar `ItemComposicao` (Pattern 4 snapshot):
```typescript
const novoModulo: ItemComposicao = {
  id: crypto.randomUUID(),
  codigo: produto.codigo, descricao: produto.descricao, quantidade: 1,
  precoUnitario: Math.round((produto.preco_tabela || 0) * 100) / 100,
  precoMinimo: Math.round((produto.preco_minimo || 0) * 100) / 100,
  imagemUrl: produto.imagem_url || undefined,
  papel: 'modulo', obrigatorio: false,
  potenciaW: produto.driver_potencia_w ?? undefined,   // snapshot de potencia_watts
};
onChange({ ...item, composicao: [...(item.composicao ?? []), novoModulo] });
```
Botão: `<Button variant="outline" size="sm" className="gap-2 mt-2">` com `<Plus className="h-4 w-4" /> + Adicionar módulo`.

Estado vazio de módulos: ainda renderiza o painel de driver no estado "sem_carga" (próxima task). NÃO bloquear montagem se carga = 0.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "const ComposicaoCard" auraoramentos/src/components/ComposicaoCard.tsx` → 1 match
    - `grep -c "from \"@/" auraoramentos/src/components/ComposicaoCard.tsx` → ao menos 4 (imports com alias)
    - `grep -c "from \"\\.\\./" auraoramentos/src/components/ComposicaoCard.tsx` → 0 (nenhum import relativo `../`)
    - `grep -n "calcularCargaComposicao" auraoramentos/src/components/ComposicaoCard.tsx` → ao menos 1
    - `grep -n "filtroSistema={familiaSistema}" auraoramentos/src/components/ComposicaoCard.tsx` → 1 match
    - `grep -n "papel: 'modulo'" auraoramentos/src/components/ComposicaoCard.tsx` → 1 match
    - `grep -n "'?W'" auraoramentos/src/components/ComposicaoCard.tsx` → 1 match (Pitfall 2)
    - `grep -n "MAGNETO 48V" auraoramentos/src/components/ComposicaoCard.tsx` e `grep -n "TINY 24V"` → 1 match cada (badges pt-BR)
    - `npm run build` verde
  </acceptance_criteria>
  <done>Card renderiza header + trilho + módulos + carga total derivada; adicionar/editar/remover módulo funciona; build verde.</done>
</task>

<task type="auto">
  <name>Task 2: Painel de driver (DRV-01/02), voltage lock (COMP-03) e checklist (COMP-01/02)</name>
  <files>src/components/ComposicaoCard.tsx</files>
  <read_first>
    - src/components/ComposicaoCard.tsx (resultado da Task 1)
    - src/components/AmbienteCard.tsx (148-161 buscarDriverSugerido; 55-56 ambienteRef padrão de reconciliação pós-await; 594 filtroVoltagem em ProdutoAutocomplete)
    - .planning/phases/20-fluxos-magn-ticos/20-UI-SPEC.md (seções "Painel de driver" — os 5 estados com classes Tailwind verbatim e Copywriting Contract; "Checklist de componentes obrigatórios" com layout e regras de presença; "Embutir detection")
    - .planning/phases/20-fluxos-magn-ticos/20-RESEARCH.md (Pattern 6/7/8/9; Painel de Driver 5 estados; Checklist REGRAS_COMPOSICAO; Voltage Lock; Pitfall 3 race condition; Pitfall 2 sem driver 24V)
    - .planning/phases/20-fluxos-magn-ticos/20-CONTEXT.md (D-07 buckets, D-08 >200W, D-10 conector TINY OR + default LM3168, D-11 kit só embutir, D-12 voltage lock por construção)
  </read_first>
  <action>
Adicionar ao `ComposicaoCard.tsx`, abaixo da lista de módulos:

**(A) Painel de driver (DRV-01/DRV-02).** Derivar o estado SEM state próprio de modo (RESEARCH "Painel de Driver"):
- `driverAplicado = (item.composicao ?? []).find(c => c.papel === 'driver_recomendado')`.
- `cargaTotalW` já calculado na Task 1.

Para **48V** (`is48V`): usar `recomendarDriver48V(cargaTotalW)` do Plano 01:
- `estado='sem_carga'` → painel `rounded-md border border-dashed p-3 bg-muted/30 text-xs text-muted-foreground` com texto exato `"Adicione módulos para calcular o driver recomendado."` (sem botão).
- `estado='excede_200w'` → painel `rounded-md border border-amber-400/40 bg-amber-50 px-3 py-2 text-xs text-amber-900`, texto exato (Copywriting Contract): `"Atenção: carga total {cargaTotalW}W excede 200W."` + `"Recomendado dividir em {Math.ceil(cargaTotalW/200)} circuitos com driver LM2344 (200W) cada."` + `"A divisão do trilho é decisão de projeto — adicione os drivers manualmente."` — SEM botão "Aplicar" (D-08, não auto-insere).
- `estado='recomendado'` (carga ≤ 200W, sem driver aplicado) → painel `bg-blue-50 border border-blue-400/40 px-3 py-2 text-xs text-blue-900`: `"Driver recomendado: {sku} ({potenciaW}W) — 1 unidade"` + `"Carga: {cargaTotalW}W × 1,05 = {potenciaSeguraW}W calculados"` + `<Button size="sm" variant="default" className="h-8">Aplicar</Button>`.
- driver aplicado E `driverAplicado.potenciaW >= cargaTotalW * 1.05` → estado `aplicado_ok`: `bg-green-50 border border-green-400/40` com `<Check className="h-3.5 w-3.5 text-green-700" />` + `"Driver aplicado: {driverAplicado.codigo} ({driverAplicado.potenciaW}W) × {driverAplicado.quantidade}"` + `<Button size="sm" variant="outline" className="h-8">Alterar</Button>` (remove o driver da composicao[] por id).
- driver aplicado MAS `driverAplicado.potenciaW < cargaTotalW * 1.05` → estado `subdimensionado`: `bg-amber-50 border border-amber-400/40`: `"Driver atual ({driverAplicado.potenciaW}W) insuficiente para a carga atual ({cargaTotalW}W)."` + `"Recomendado: {sku} ({potenciaW}W)"` + `<Button size="sm" variant="default" className="h-8">Reaplicar recomendação</Button>`.

Botão "Aplicar"/"Reaplicar" para 48V: fetch do produto driver por SKU recomendado (LM2343/LM2344) via Supabase, snapshot no add-time, inserir `ItemComposicao` papel='driver_recomendado' (remover driver anterior se reaplicando):
```typescript
const { data } = await supabase.from('produtos')
  .select('id, codigo, descricao, preco_tabela, preco_minimo, driver_potencia_w:potencia_watts')
  .eq('codigo', skuRecomendado).limit(1);
const drv = data?.[0];
const driverItem: ItemComposicao = {
  id: crypto.randomUUID(), codigo: drv.codigo, descricao: drv.descricao, quantidade: 1,
  precoUnitario: Math.round((drv.preco_tabela||0)*100)/100,
  precoMinimo: Math.round((drv.preco_minimo||0)*100)/100,
  papel: 'driver_recomendado', obrigatorio: true,
  potenciaW: drv.driver_potencia_w ?? undefined,
};
```
Reconciliação pós-await (Pitfall 3): usar um `useRef(item)` atualizado por `useEffect`, e ao aplicar reconstruir `composicao` a partir do ref mais recente, localizando por `item.id` — não sobrescrever edições concorrentes. Filtrar qualquer driver_recomendado anterior antes de inserir o novo. Chamar `onChange` com a composição reconciliada.

Para **24V** (`is24V`): em vez de recomendarDriver48V, manter um state local `{ sugestao24v, buscando }`. Em `useEffect([cargaTotalW])`, se `is24V && cargaTotalW > 0 && !driverAplicado`, buscar o menor driver 24V (Pattern 7, copiar buscarDriverSugerido com `.eq('tensao',24).gte('potencia_watts', cargaTotalW*MARGEM_SEGURANCA_DRIVER).order('potencia_watts',{ascending:true}).limit(1)`). Se encontrado → painel `recomendado` com SKU real + "Aplicar". Se nenhum → painel `text-xs text-destructive` com texto exato: `"Nenhum driver 24V compatível no catálogo para {cargaTotalW}W. Selecione manualmente."`. NÃO há limite de 200W para 24V.

**(B) Voltage lock (COMP-03/D-12).** Onde houver busca de driver manual via autocomplete (o estado "Alterar"/seleção manual opcional), usar `<ProdutoAutocomplete filtro="driver" filtroVoltagem={is48V ? 48 : 24} ... />`. O lock é por construção — driver de outra voltagem nunca aparece; NÃO emitir toast de erro de voltagem.

**(C) Checklist (COMP-01/COMP-02).** Abaixo do painel de driver. Ler `const regras = REGRAS_COMPOSICAO[item.sistema ?? '']`; se `!regras` → não renderizar checklist (return null nessa seção). Layout: `<div className="rounded-md border px-3 py-2 space-y-1">` com título `<span className="text-xs font-semibold text-muted-foreground uppercase">Componentes obrigatórios</span>`.

Conector:
- presença: `const temConector = regras.conectoresObrigatorios.some(sku => (item.composicao ?? []).some(c => c.codigo === sku))` — para TINY isso já cobre o OR LM3168/LM3169 (D-10), não acusa falta por cor.
- presente → `<Check className="h-3.5 w-3.5 text-green-600" />` + texto `"Conector {regras.conectoresObrigatorios[0]} — presente"` (sem botão).
- ausente → `<AlertCircle className="h-3.5 w-3.5 text-amber-500" />` + `"Conector {regras.conectoresObrigatorios[0]} — ausente"` + `<Button size="sm" variant="outline" className="h-6 text-[10px] gap-1">+ Adicionar</Button>`. SKU default do atalho: LM2338 (MAGNETO) ou LM3168 (TINY, D-10). Ao clicar, fetch por SKU e inserir `ItemComposicao` papel='conector_energia', obrigatorio=true, snapshot.

Kit de fixação (D-11):
- `const ehEmbutir = /EMBUTIR/i.test(item.descricao)` (descrição do trilho âncora). Só renderizar a linha do kit se `ehEmbutir`.
- presença: `item.composicao?.some(c => c.codigo === regras.kitFixacaoEmbutir)` (LM2987).
- presente → Check + `"Kit Fixação {regras.kitFixacaoEmbutir} (embutir) — presente"`; ausente → AlertCircle + "ausente" + botão "+ Adicionar" inserindo papel='kit_fixacao', obrigatorio=true.

Helper interno `adicionarComponentePorSku(sku, papel)`: fetch Supabase `select('id, codigo, descricao, preco_tabela, preco_minimo')` `.eq('codigo', sku).limit(1)`, criar ItemComposicao snapshot e `onChange({ ...item, composicao: [...(item.composicao ?? []), novo] })`. Toda inserção usa reconciliação por `item.id` se houver await (Pitfall 3).

Todos os textos em pt-BR conforme Copywriting Contract. Sem emojis nos estados novos. Usar tokens de design / classes do UI-SPEC verbatim.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run build 2>&1 | tail -15 && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "recomendarDriver48V" auraoramentos/src/components/ComposicaoCard.tsx` → ao menos 1
    - `grep -n "papel: 'driver_recomendado'" auraoramentos/src/components/ComposicaoCard.tsx` → 1 match
    - `grep -n "filtroVoltagem={is48V ? 48 : 24}" auraoramentos/src/components/ComposicaoCard.tsx` → 1 match (voltage lock)
    - `grep -n "REGRAS_COMPOSICAO\[item.sistema" auraoramentos/src/components/ComposicaoCard.tsx` → 1 match
    - `grep -n "papel: 'conector_energia'" auraoramentos/src/components/ComposicaoCard.tsx` → 1 match
    - `grep -n "papel: 'kit_fixacao'" auraoramentos/src/components/ComposicaoCard.tsx` → 1 match
    - `grep -nE "/EMBUTIR/i" auraoramentos/src/components/ComposicaoCard.tsx` → 1 match (D-11)
    - `grep -n "LM3168" auraoramentos/src/components/ComposicaoCard.tsx` → ao menos 1 (default TINY, D-10)
    - `grep -n "excede 200W" auraoramentos/src/components/ComposicaoCard.tsx` → 1 match (D-08, sem botão Aplicar nesse ramo)
    - `grep -n "Nenhum driver 24V compatível" auraoramentos/src/components/ComposicaoCard.tsx` → 1 match
    - `grep -n "\.eq('tensao', 24)" auraoramentos/src/components/ComposicaoCard.tsx` → 1 match (pool 24V)
    - `npm run build` verde; `npm run lint` sem erros novos
  </acceptance_criteria>
  <done>Painel de driver com 5 estados + 48V/24V; voltage lock por construção; checklist por família com atalho; tudo pt-BR; build/lint verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catálogo (Supabase reads) → composição | SKU/preço/potência do driver e conector entram como snapshot via fetch por código |
| composição em memória → snapshot JSONB persistido | luminarias[].composicao montada no card vira parte do orçamento salvo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Tampering | aplicar driver / adicionar componente (fetch por SKU) | mitigate | Snapshot imutável no add-time; quantidade/preço editáveis pelo seller (auditável, reversível — D-07); reconciliação por item.id pós-await evita escrita em alvo errado (Pitfall 3) |
| T-20-06 | Spoofing/voltage | seleção de driver incompatível | mitigate | Voltage lock por construção (filtroVoltagem) — driver de outra voltagem não aparece; impossível selecionar voltagem inválida pela UI (COMP-03) |
| T-20-07 | Information Disclosure | fetch de produtos por SKU | accept | Catálogo `produtos` legível por autenticados; RLS existente inalterada; sem coluna/tabela nova |
| T-20-08 | Tampering | carga derivada com potenciaW null | mitigate | Pitfall 2 — exibe "?W" em vez de "0W"; não bloqueia montagem; seller pode adicionar driver manualmente |
</threat_model>

<verification>
- `npm run build` verde (componente typesafe consumindo helpers do Plano 01)
- `npm run lint` sem novos erros
- Voltage lock: driver de voltagem incompatível não aparece (filtroVoltagem)
- Nenhuma dependência de dados na tabela produto_composicao (checklist lê só REGRAS_COMPOSICAO — RESEARCH Anti-Pattern)
- Todos os textos pt-BR conforme Copywriting Contract; sem emojis nos estados novos
</verification>

<success_criteria>
- ComposicaoCard renderiza trilho + módulos + carga + painel driver (5 estados) + checklist
- 48V usa LM2343/LM2344 ×1.05; 24V busca menor driver 24V compatível
- "Aplicar" insere driver_recomendado em 1 clique, reversível
- Checklist marca conector (OR para TINY) e kit (só embutir), atalho insere SKU correto
- Voltage lock por construção, sem toast de erro
</success_criteria>

<output>
After completion, create `.planning/phases/20-fluxos-magn-ticos/20-02-SUMMARY.md`
</output>
