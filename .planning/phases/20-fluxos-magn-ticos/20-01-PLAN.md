---
phase: 20-fluxos-magn-ticos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/orcamento.ts
  - src/types/orcamento.test.ts
  - src/hooks/useProdutoSearch.ts
autonomous: true
requirements: [SIST-01, SIST-02, DRV-01, COMP-03]
must_haves:
  truths:
    - "Dado um produto, detectarTipoAncora retorna 'fita' | 'magneto_48v' | 'tiny_magneto' | 'modular' | 'luminaria' com a prioridade correta (fita antes do fallback)"
    - "Dada uma composicao[], calcularCargaComposicao soma potenciaW × quantidade só dos itens papel='modulo'"
    - "Dada uma carga 48V, recomendarDriver48V retorna LM2343 (≤100W), LM2344 (≤200W) ou null (>200W) aplicando margem ×1.05"
    - "A busca de módulos escopada por sistema (filtroSistema) só retorna produtos da família correta excluindo trilhos/conectores/drivers/kits"
    - "Os 5 calc sites de Fita Padrão permanecem byte-idênticos"
  artifacts:
    - path: "src/types/orcamento.ts"
      provides: "detectarTipoAncora, calcularCargaComposicao, recomendarDriver48V helpers (puros)"
      contains: "export function detectarTipoAncora"
    - path: "src/types/orcamento.test.ts"
      provides: "Testes unitários dos 3 helpers + guard de regressão Fita Padrão"
      contains: "detectarTipoAncora"
    - path: "src/hooks/useProdutoSearch.ts"
      provides: "filtroSistema opcional para busca escopada de módulos por família"
      contains: "filtroSistema"
  key_links:
    - from: "src/types/orcamento.ts (helpers novos)"
      to: "REGRAS_COMPOSICAO + MARGEM_SEGURANCA_DRIVER"
      via: "import/leitura de constantes existentes"
      pattern: "MARGEM_SEGURANCA_DRIVER"
    - from: "src/hooks/useProdutoSearch.ts"
      to: "supabase produtos.sistema"
      via: ".eq('sistema', filtroSistema)"
      pattern: "filtroSistema"
---

<objective>
Entregar a camada de dados/lógica pura que os cards de composição (Plano 02/03) consomem: helpers de detecção de tipo, cálculo de carga e recomendação de driver 48V em `orcamento.ts`, mais a extensão `filtroSistema` em `useProdutoSearch` para a busca escopada de módulos por família. Esta é a camada de contratos — interface-first, sem UI.

Purpose: Plano 02 (ComposicaoCard) e Plano 03 (AmbienteCard) precisam destes contratos prontos para não fazer "scavenger hunt" no codebase. Funções puras testáveis isoladamente protegem os 5 calc sites de Fita Padrão.
Output: helpers exportados + testes verdes + filtroSistema funcional, sem tocar nenhum dos 5 calc sites protegidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-fluxos-magn-ticos/20-CONTEXT.md
@.planning/phases/20-fluxos-magn-ticos/20-RESEARCH.md

<interfaces>
<!-- Contratos existentes (orcamento.ts) que os helpers novos consomem. NÃO reexplorar o codebase. -->

Produto (orcamento.ts:1):
```typescript
export interface Produto {
  codigo: string; descricao: string;
  preco_tabela: number; preco_minimo: number;
  voltagem?: number | null;            // alias de tensao
  driver_potencia_w?: number | null;   // alias de potencia_watts
  sistema_magnetico?: string | null;   // alias de product_variants.sistema: 'magneto_48v'|'tiny_magneto'|'s_mode'|...
  tipo_produto?: string | null;        // 'fita'|'driver'|'perfil'|'spot'|'conector'|'kit_fixacao'|...
}
```

ItemComposicao (orcamento.ts:43) — JÁ EXISTE, NÃO redefinir:
```typescript
export interface ItemComposicao {
  id: string; codigo: string; descricao: string;
  quantidade: number; precoUnitario: number; precoMinimo: number; imagemUrl?: string;
  papel: 'modulo' | 'driver_recomendado' | 'driver_obrigatorio' | 'conector_energia' | 'kit_fixacao' | 'acessorio_opcional';
  obrigatorio: boolean;
  comprimento?: number;   // Phase 21
  potenciaW?: number;     // Phase 20 — snapshot de potencia_watts
}
```

Constantes existentes a reusar (NÃO duplicar):
```typescript
export const MARGEM_SEGURANCA_DRIVER = 1.05;          // orcamento.ts:144
export const REGRAS_COMPOSICAO = { magneto_48v: {...}, tiny_magneto: {...} }; // orcamento.ts:150
```

ProdutoFiltro (useProdutoSearch.ts:5) — JÁ inclui 'conector'/'kit_fixacao':
```typescript
export type ProdutoFiltro = 'fita' | 'driver' | 'perfil' | 'conector' | 'kit_fixacao' | 'luminaria' | 'todos';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helpers puros de detecção, carga e driver 48V em orcamento.ts</name>
  <files>src/types/orcamento.ts, src/types/orcamento.test.ts</files>
  <read_first>
    - src/types/orcamento.ts (ler linhas 1-340: Produto:1, ItemLuminaria:23, ItemComposicao:43, MARGEM_SEGURANCA_DRIVER:144, REGRAS_COMPOSICAO:150, os calc sites 187/204/217/302, analisarMagneto48V:317)
    - .planning/phases/20-fluxos-magn-ticos/20-RESEARCH.md (Pattern 1, 5, 6 — assinaturas verbatim; tabela de roteamento; Pitfall 1 prioridade fita)
    - .planning/phases/20-fluxos-magn-ticos/20-CONTEXT.md (D-02 roteamento, D-06 carga, D-07 buckets driver, D-08 >200W)
  </read_first>
  <behavior>
    - detectarTipoAncora(produto): tipo_produto==='fita' → 'fita'; sistema_magnetico==='magneto_48v' → 'magneto_48v'; sistema_magnetico==='tiny_magneto' → 'tiny_magneto'; sistema_magnetico==='s_mode' → 'modular'; qualquer outro / null → 'luminaria'. Prioridade: 'fita' é checada ANTES do fallback (Pitfall 1).
    - calcularCargaComposicao([]) === 0; com módulos papel='modulo' soma potenciaW×quantidade; ignora itens papel='driver_recomendado'/'conector_energia'/'kit_fixacao'; potenciaW undefined conta como 0.
    - recomendarDriver48V(carga): carga 0 → null com motivo 'sem_carga'; carga tal que carga×1.05 ≤ 100 → { sku:'LM2343', potenciaW:100 }; ≤ 200 → { sku:'LM2344', potenciaW:200 }; > 200 → { sku:null, excede:true } (D-08, NÃO auto-divide).
    - Os 5 calc sites permanecem inalterados (teste de guard por assinatura literal).
  </behavior>
  <action>
Adicionar 3 funções puras EXPORTADAS em `src/types/orcamento.ts`, posicionadas logo após `REGRAS_COMPOSICAO` (após a linha 165) — NÃO mexer em nenhuma função entre as linhas 167-340 (calc sites e analisarMagneto48V ficam intactos).

1. `detectarTipoAncora`:
```typescript
export type TipoAncora = 'luminaria' | 'fita' | 'magneto_48v' | 'tiny_magneto' | 'modular';

/** Roteamento product-first (Phase 20 / D-02). Detecta o fluxo a partir do produto âncora.
 *  Prioridade: 'fita' ANTES do fallback (fita tem sistema_magnetico null — Pitfall 1). */
export function detectarTipoAncora(produto: Produto): TipoAncora {
  if (produto.tipo_produto === 'fita') return 'fita';
  if (produto.sistema_magnetico === 'magneto_48v') return 'magneto_48v';
  if (produto.sistema_magnetico === 'tiny_magneto') return 'tiny_magneto';
  if (produto.sistema_magnetico === 's_mode') return 'modular';
  return 'luminaria'; // fallback gracioso D-03 — nunca interrompe
}
```

2. `calcularCargaComposicao` (carga derivada D-06; soma só papel='modulo'):
```typescript
/** Carga total (W) de uma composição = Σ(potenciaW × quantidade) dos módulos (Phase 20 / D-06).
 *  Apenas papel==='modulo'; potenciaW ausente conta como 0. */
export function calcularCargaComposicao(composicao: ItemComposicao[] | undefined): number {
  if (!composicao?.length) return 0;
  return composicao
    .filter(c => c.papel === 'modulo')
    .reduce((s, c) => s + (c.potenciaW ?? 0) * c.quantidade, 0);
}
```

3. `recomendarDriver48V` (buckets D-07/D-08, margem ×1.05 via MARGEM_SEGURANCA_DRIVER):
```typescript
export type RecomendacaoDriver48V =
  | { estado: 'sem_carga' }
  | { estado: 'recomendado'; sku: 'LM2343' | 'LM2344'; potenciaW: 100 | 200; potenciaSeguraW: number }
  | { estado: 'excede_200w'; potenciaSeguraW: number };

/** Bucket de driver 48V (Phase 20 / D-07): LM2343 100W até 100W, LM2344 200W até 200W.
 *  > 200W: D-08 — avisa para dividir em N circuitos, NÃO auto-insere. Margem ×1.05. */
export function recomendarDriver48V(cargaTotalW: number): RecomendacaoDriver48V {
  if (cargaTotalW <= 0) return { estado: 'sem_carga' };
  const potenciaSeguraW = Math.round(cargaTotalW * MARGEM_SEGURANCA_DRIVER * 100) / 100;
  if (potenciaSeguraW <= 100) return { estado: 'recomendado', sku: 'LM2343', potenciaW: 100, potenciaSeguraW };
  if (potenciaSeguraW <= 200) return { estado: 'recomendado', sku: 'LM2344', potenciaW: 200, potenciaSeguraW };
  return { estado: 'excede_200w', potenciaSeguraW };
}
```

NÃO criar helper de driver 24V aqui (é query Supabase async — vive no ComposicaoCard, Plano 02, via padrão buscarDriverSugerido). NÃO alterar `analisarMagneto48V` (continua varrendo amb.luminarias — fallback para snapshots antigos).
  </action>
  <verify>
    <automated>cd auraoramentos && npm run test -- orcamento.test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function detectarTipoAncora" auraoramentos/src/types/orcamento.ts` retorna 1 linha
    - `grep -n "export function calcularCargaComposicao" auraoramentos/src/types/orcamento.ts` retorna 1 linha
    - `grep -n "export function recomendarDriver48V" auraoramentos/src/types/orcamento.ts` retorna 1 linha
    - Byte-identical guard (devem retornar EXATAMENTE estas assinaturas, inalteradas):
      - `grep -n "export function calcularDemandaFita(arg: SistemaIluminacao | ItemPerfil): number {" auraoramentos/src/types/orcamento.ts` → 1 match
      - `grep -n "export function calcularConsumoW(arg1: SistemaIluminacao | ItemPerfil, arg2?: ItemFitaLED): number {" auraoramentos/src/types/orcamento.ts` → 1 match
      - `grep -n "export function calcularQtdDrivers(arg1: SistemaIluminacao | ItemPerfil, arg2?: ItemFitaLED, arg3?: ItemDriver): number {" auraoramentos/src/types/orcamento.ts` → 1 match
      - `grep -n "export function calcularSubtotalSistemaSemFita(sistema: SistemaIluminacao): number {" auraoramentos/src/types/orcamento.ts` → 1 match
      - `grep -n "function isSistemaVazio(sis: SistemaIluminacao): boolean {" auraoramentos/src/lib/pdfTemplates/v2.ts` → 1 match (arquivo NÃO foi tocado)
    - `npm run test -- orcamento.test` passa com casos cobrindo: fita-antes-do-fallback, carga só-módulos, bucket LM2343 (carga 90), bucket LM2344 (carga 150), excede_200w (carga 250)
  </acceptance_criteria>
  <done>3 helpers puros exportados, testes verdes, 5 calc sites confirmados byte-idênticos via grep.</done>
</task>

<task type="auto">
  <name>Task 2: filtroSistema em useProdutoSearch para busca escopada de módulos</name>
  <files>src/hooks/useProdutoSearch.ts, src/components/ProdutoAutocomplete.tsx</files>
  <read_first>
    - src/hooks/useProdutoSearch.ts (arquivo completo — ProdutoFiltro:5, assinatura da função:7, query builder:17-43, filtro de driver por voltagem:35)
    - .planning/phases/20-fluxos-magn-ticos/20-RESEARCH.md (Pattern 3 + seção "Voltage Lock por Construção" — escopo de módulos por sistema; NOTA CRÍTICA do alias sistema_magnetico vs coluna 'sistema')
    - .planning/phases/20-fluxos-magn-ticos/20-UI-SPEC.md (seção "Busca escopada de módulos" — tabela de filtros)
  </read_first>
  <action>
Estender `useProdutoSearch` com um parâmetro opcional `filtroSistema?: string` para a busca escopada de módulos por família, SEM quebrar nenhuma chamada existente (todas omitem o novo parâmetro → comportamento idêntico).

Mudanças em `src/hooks/useProdutoSearch.ts`:

1. Assinatura (linha 7) — adicionar 4º parâmetro opcional:
```typescript
export function useProdutoSearch(query: string, filtro: ProdutoFiltro = 'todos', filtroVoltagem?: number, filtroSistema?: string) {
```

2. Dentro do try, APÓS o bloco de filtro de voltagem do driver (após linha 37), adicionar:
```typescript
// Busca escopada de módulos por família (Phase 20 / D-12) — usa a coluna 'sistema'
// (NÃO o alias 'sistema_magnetico'). Exclui trilhos/conectores/drivers/kits para retornar só módulos.
if (filtroSistema) {
  queryBuilder = queryBuilder
    .eq('sistema', filtroSistema)
    .not('tipo_produto', 'in', '("driver","conector","kit_fixacao","perfil")');
}
```

3. Adicionar `filtroSistema` ao array de dependências do useEffect (linha 70):
```typescript
}, [query, filtro, filtroVoltagem, filtroSistema]);
```

IMPORTANTE: usar `.eq('sistema', ...)` — a coluna no banco é `sistema`, o alias `sistema_magnetico` só existe no SELECT do retorno. NÃO confundir. O `.not('tipo_produto','in', ...)` exclui o trilho âncora e componentes (driver/conector/kit) para que a busca de "+ Adicionar módulo" só ofereça módulos.

Propagar o prop em `ProdutoAutocomplete.tsx`: adicionar `filtroSistema?: string` à interface `ProdutoAutocompleteProps` (linha 9), ao destructuring (linha 19), e passá-lo a `useProdutoSearch(query, filtro, filtroVoltagem, filtroSistema)` (linha 22).
  </action>
  <verify>
    <automated>cd auraoramentos && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "filtroSistema" auraoramentos/src/hooks/useProdutoSearch.ts` → ao menos 3 matches (assinatura, bloco .eq, deps array)
    - `grep -n "\.eq('sistema', filtroSistema)" auraoramentos/src/hooks/useProdutoSearch.ts` → 1 match
    - `grep -n "filtroSistema" auraoramentos/src/components/ProdutoAutocomplete.tsx` → ao menos 3 matches (interface, destructuring, passagem ao hook)
    - `grep -c "sistema_magnetico" auraoramentos/src/hooks/useProdutoSearch.ts` → exatamente 1 (só o alias no SELECT, não usado no novo filtro)
    - `npm run build` conclui sem erro de TypeScript
  </acceptance_criteria>
  <done>filtroSistema disponível em useProdutoSearch + ProdutoAutocomplete; build verde; chamadas existentes inalteradas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catálogo (Supabase reads) → cliente | Resultado de busca de produtos entra na composição como snapshot; não confiar cegamente em potenciaW/preço |
| composição em memória → snapshot JSONB | Dados de carga/driver derivados localmente, persistidos como parte de luminarias[].composicao |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | recomendarDriver48V buckets | mitigate | Função pura determinística; testes unitários cobrem fronteiras 100/200W; margem ×1.05 via constante única (paridade edge function) |
| T-20-02 | Information Disclosure | filtroSistema query | accept | Lê tabela `produtos` (catálogo público a autenticados); RLS existente inalterada; sem coluna nova; sem dado sensível |
| T-20-03 | Elevation of Privilege | useProdutoSearch | accept | Nenhuma nova superfície server-side; mesma query autenticada já existente; sem escrita |
| T-20-04 | Denial of Service | calcularCargaComposicao | accept | O(n) sobre array pequeno em memória; sem recursão; sem risco prático |
</threat_model>

<verification>
- `npm run test -- orcamento.test` verde (helpers cobertos)
- `npm run build` verde (filtroSistema typesafe)
- 5 calc sites byte-idênticos confirmados via grep das assinaturas literais
- Nenhuma chamada existente de useProdutoSearch quebrada (parâmetro novo é opcional no fim da lista)
</verification>

<success_criteria>
- detectarTipoAncora, calcularCargaComposicao, recomendarDriver48V exportados de orcamento.ts e testados
- filtroSistema funcional em useProdutoSearch + propagado em ProdutoAutocomplete
- 5 calc sites de Fita Padrão intocados
- Base pronta para Plano 02 consumir (interface-first)
</success_criteria>

<output>
After completion, create `.planning/phases/20-fluxos-magn-ticos/20-01-SUMMARY.md`
</output>
