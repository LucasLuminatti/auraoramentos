---
phase: 20-fluxos-magn-ticos
plan: 03
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified:
  - src/components/AmbienteCard.tsx
autonomous: false
requirements: [SIST-05, SIST-01, SIST-02, COMP-01]
must_haves:
  truths:
    - "O AmbienteCard não tem mais abas Luminárias/Sistemas — há uma busca product-first única acima de uma lista unificada de itens"
    - "Selecionar um produto roteia automaticamente: fita → sistemas[] (Fita Padrão byte-idêntico); magneto_48v/tiny_magneto → luminarias[].composicao[]; outro → item simples; dado sujo → item simples sem diálogo (fallback gracioso)"
    - "Adicionar uma fita abre o fluxo de Fita Padrão atual idêntico — nenhuma regressão de cálculo ou markup"
    - "Itens com composicao definida renderizam via ComposicaoCard; itens sem composicao renderizam como linha simples; sistemas[] renderizam o card de Fita Padrão existente intocado"
    - "Os 5 calc sites de Fita Padrão permanecem byte-idênticos"
  artifacts:
    - path: "src/components/AmbienteCard.tsx"
      provides: "Busca product-first + lista unificada + roteamento de detecção + render de ComposicaoCard; card de Fita Padrão intocado"
      contains: "detectarTipoAncora"
  key_links:
    - from: "src/components/AmbienteCard.tsx (busca product-first)"
      to: "detectarTipoAncora (orcamento.ts)"
      via: "roteamento no handler de seleção global"
      pattern: "detectarTipoAncora\\(produto\\)"
    - from: "src/components/AmbienteCard.tsx"
      to: "ComposicaoCard (Plano 02)"
      via: "render de itens com composicao"
      pattern: "<ComposicaoCard"
    - from: "rota fita"
      to: "addSistema + handleSelectProdutoSistema existentes"
      via: "reuso do fluxo Fita Padrão"
      pattern: "addSistema"
---

<objective>
Reorganizar `src/components/AmbienteCard.tsx`: substituir as abas Luminárias/Sistemas por uma busca product-first única + lista unificada, com roteamento automático de detecção (D-02/D-03), renderizando `ComposicaoCard` (Plano 02) para itens compostos. O card de Fita Padrão e os 5 calc sites permanecem byte-idênticos — só o ponto de entrada muda.

Purpose: SIST-05 (product-first, sem seletor de tipo manual) é o espinho da fase. Esta é a integração final que wira os helpers (Plano 01) e o ComposicaoCard (Plano 02) na UI. Tem checkpoint humano-visual porque é uma mudança de UX de alto impacto no wizard em produção.
Output: AmbienteCard reorganizado, sem regressão de Fita Padrão, com checkpoint de verificação visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-fluxos-magn-ticos/20-CONTEXT.md
@.planning/phases/20-fluxos-magn-ticos/20-RESEARCH.md
@.planning/phases/20-fluxos-magn-ticos/20-UI-SPEC.md

<interfaces>
<!-- Tudo já pronto dos Planos 01/02. NÃO reexplorar. -->

detectarTipoAncora (Plano 01, orcamento.ts):
```typescript
export type TipoAncora = 'luminaria' | 'fita' | 'magneto_48v' | 'tiny_magneto' | 'modular';
export function detectarTipoAncora(produto: Produto): TipoAncora;
```

ComposicaoCard (Plano 02, ./ComposicaoCard):
```typescript
interface ComposicaoCardProps {
  item: ItemLuminaria;
  onChange: (item: ItemLuminaria) => void;
  onRemove: () => void;
  indice: number;
}
```

Handlers existentes a PRESERVAR intactos (AmbienteCard.tsx):
- addSistema():71, updateSistema():77, removeSistema():81, duplicarSistema():85
- handleSelectProdutoSistema():163 (toda a lógica fita/driver/perfil + sugestão automática de driver) — INTOCADA
- buscarDriverSugerido():148, vincularPerfil():311, desvincularPerfil():317 — INTOCADAS
- addLuminaria():59, updateLuminaria():62, removeLuminaria():66 — preservadas (usadas internamente)

A ser REMOVIDO: `activeTab` state (linha 47); `<Tabs>/<TabsList>/<TabsTrigger>/<TabsContent>` (360-647); botões "Adicionar Luminária" (408) e "Novo Sistema" (643) como pontos de entrada primários; o `onRedirectToSistemas={() => setActiveTab('sistemas')}` (382).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Roteamento product-first + handler de seleção global</name>
  <files>src/components/AmbienteCard.tsx</files>
  <read_first>
    - src/components/AmbienteCard.tsx (arquivo completo — handlers 59-90, handleSelectProdutoLuminaria 92-144 com os toasts a preservar, handleSelectProdutoSistema 163)
    - src/types/orcamento.ts (detectarTipoAncora do Plano 01; ItemLuminaria:23; ItemComposicao:43)
    - .planning/phases/20-fluxos-magn-ticos/20-RESEARCH.md (Pattern 1/2; Roteamento de Detecção tabela definitiva; Pitfall 1 fita; Pitfall 4 addSistema síncrono; Pitfall 5 redirect)
    - .planning/phases/20-fluxos-magn-ticos/20-CONTEXT.md (D-01 a D-05)
    - .planning/phases/20-fluxos-magn-ticos/20-UI-SPEC.md (Anatomia: Busca Product-First — tabela de rotas + fallback gracioso)
  </read_first>
  <action>
Adicionar um handler de seleção global `handleSelectProdutoGlobal(produto: Produto)` que roteia via `detectarTipoAncora` (importar do Plano 01). NÃO substituir `handleSelectProdutoLuminaria` (índice-based) — ele permanece para edição inline de itens simples existentes; o novo handler CRIA itens novos a partir da busca global.

Importar `detectarTipoAncora`, `ItemComposicao`, `ComposicaoCard`:
- adicionar `detectarTipoAncora` ao import de `@/types/orcamento` (linha 16)
- `import ComposicaoCard from "./ComposicaoCard";`

```typescript
const handleSelectProdutoGlobal = (produto: Produto) => {
  const tipo = detectarTipoAncora(produto);
  const imgUrl = produto.imagem_url || undefined;
  const preco = Math.round((produto.preco_tabela || 0) * 100) / 100;
  const precoMin = Math.round((produto.preco_minimo || 0) * 100) / 100;

  if (tipo === 'fita') {
    // Rota Fita Padrão — reusar fluxo existente (Pitfall 4: criar sistema pré-populado inline,
    // depois aplicar a fita via handleSelectProdutoSistema na sequência síncrona).
    const novoIndex = ambiente.sistemas.length;
    addSistema();
    // React batcha; aplicar a fita no sistema recém-criado por índice na sequência
    handleSelectProdutoSistema(produto, novoIndex, 'fita');
    return;
  }

  if (tipo === 'magneto_48v' || tipo === 'tiny_magneto') {
    // Inicia composição: ItemLuminaria raiz (trilho âncora) com composicao: [] presente (Pattern 2).
    const tensaoSistema = tipo === 'magneto_48v' ? 48 : 24;
    const novaRaiz: ItemLuminaria = {
      id: uid(),
      codigo: produto.codigo, descricao: produto.descricao, quantidade: 1,
      precoUnitario: preco, precoMinimo: precoMin, imagemUrl: imgUrl,
      sistema: tipo,                       // 'magneto_48v' | 'tiny_magneto'
      potencia_watts: produto.driver_potencia_w ?? null,
      tensao: produto.voltagem ?? tensaoSistema,
      composicao: [],                      // presença (mesmo vazio) ativa o ComposicaoCard
    };
    onChange({ ...ambiente, luminarias: [...ambiente.luminarias, novaRaiz] });
    return;
  }

  // 'modular' (Phase 21) e 'luminaria' e fallback (D-03): item simples, SEM composicao, sem diálogo.
  const novoItem: ItemLuminaria = {
    id: uid(),
    codigo: produto.codigo, descricao: produto.descricao, quantidade: 1,
    precoUnitario: preco, precoMinimo: precoMin, imagemUrl: imgUrl,
    sistema: produto.sistema_magnetico ?? null,
    potencia_watts: produto.driver_potencia_w ?? null,
    tensao: produto.voltagem ?? null,
  };
  onChange({ ...ambiente, luminarias: [...ambiente.luminarias, novoItem] });
};
```

IMPORTANTE para a rota fita (Pitfall 4): `addSistema()` chama `onChange` com o novo sistema; `handleSelectProdutoSistema(produto, novoIndex, 'fita')` lê `ambiente.sistemas[novoIndex]`. Como o `ambiente` em closure ainda é o anterior, garantir que `handleSelectProdutoSistema` opere sobre o índice correto — se necessário, construir o `SistemaIluminacao` pré-populado inline (com a fita já preenchida via mesma lógica de `handleSelectProdutoSistema` component='fita') e fazer um único `onChange`, replicando o cálculo de driver sugerido. Preferir o caminho que NÃO altera `handleSelectProdutoSistema` nem `addSistema`. Documentar a escolha no SUMMARY.

Preservar os toasts de aviso magnético existentes (linhas 96-131) — mover a lógica de toasts para dentro de `handleSelectProdutoGlobal` (ou chamar uma função compartilhada) para que os lembretes continuem aparecendo ao adicionar trilhos/módulos via busca global. NÃO remover os toasts (REGRA #5-#28 são comportamento existente).
  </action>
  <verify>
    <automated>cd auraoramentos && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "detectarTipoAncora(produto)" auraoramentos/src/components/AmbienteCard.tsx` → 1 match
    - `grep -n "import ComposicaoCard" auraoramentos/src/components/AmbienteCard.tsx` → 1 match
    - `grep -n "handleSelectProdutoGlobal" auraoramentos/src/components/AmbienteCard.tsx` → ao menos 2 (definição + uso na busca)
    - `grep -n "composicao: \[\]" auraoramentos/src/components/AmbienteCard.tsx` → 1 match (raiz de composição)
    - Handlers de Fita Padrão preservados: `grep -c "handleSelectProdutoSistema" auraoramentos/src/components/AmbienteCard.tsx` → ao menos 4 (definição + 3 usos fita/perfil/driver); `grep -n "const addSistema" auraoramentos/src/components/AmbienteCard.tsx` → 1 match
    - `npm run build` verde
  </acceptance_criteria>
  <done>handleSelectProdutoGlobal roteia fita/magneto/tiny/simples; toasts preservados; handlers de Fita Padrão intactos; build verde.</done>
</task>

<task type="auto">
  <name>Task 2: Lista unificada — remover tabs, render product-first + ComposicaoCard + Fita Padrão intocado</name>
  <files>src/components/AmbienteCard.tsx</files>
  <read_first>
    - src/components/AmbienteCard.tsx (resultado da Task 1; bloco JSX 358-650 com Tabs; banner analisarMagneto48V 369-378; card de Fita Padrão 416-642 que será PRESERVADO como está)
    - .planning/phases/20-fluxos-magn-ticos/20-UI-SPEC.md (Layout — Estrutura geral; Ordem de renderização da lista unificada; Pattern 10 renderer; Copywriting Contract estado vazio + "Iniciar como sistema composto"; Fita Padrão — Contrato de Não-Regressão)
    - .planning/phases/20-fluxos-magn-ticos/20-RESEARCH.md (Pattern 10 renderer; Pitfall 5; anti-pattern "não mover Fita Padrão")
  </read_first>
  <action>
Substituir o wrapper `<Tabs>...</Tabs>` (linhas ~360-647) por uma lista unificada. Remover o state `activeTab` (linha 47) e o import de `Tabs/TabsContent/TabsList/TabsTrigger` (linha 4).

Nova estrutura dentro de `<CollapsibleContent><div className="border-t p-4">`:

1. **Busca product-first** (UI-SPEC "Anatomia: Busca Product-First"):
```tsx
<div className="space-y-1">
  <span className="text-xs text-muted-foreground">Adicionar ao ambiente</span>
  <ProdutoAutocomplete
    value=""
    onSelect={handleSelectProdutoGlobal}
    placeholder="Buscar produto por código ou descrição..."
    className="w-full"
  />
</div>
```
(filtro omitido → 'todos'; sem `onRedirectToSistemas` — Pitfall 5).

2. **Lista unificada** (`space-y-3 mt-4`) — Pattern 10:
   - Banner `analisarMagneto48V(ambiente)` existente (369-378): MANTER como fallback para luminárias antigas sem composicao (RESEARCH "analisarMagneto48V estado atual"). Renderizar acima da lista.
   - `ambiente.luminarias.map((item, i) => ...)`:
     - se `item.composicao !== undefined` → `<ComposicaoCard key={item.id} item={item} indice={i} onChange={(novo) => updateLuminaria(i, novo)} onRemove={() => removeLuminaria(i)} />`
     - senão → renderizar a linha de item simples (reaproveitar o markup atual das linhas 380-406: ProdutoAutocomplete `filtro="luminaria"` com `onSelect={(p) => handleSelectProdutoLuminaria(p, i)}` para edição inline, descrição readonly, qtd, preço, subtotal, badge tiny_magneto). Adicionar a ação de fallback (D-03): quando `item.sistema` for 'magneto_48v'|'tiny_magneto' MAS `item.composicao` for undefined, mostrar `<Button size="sm" variant="outline">Iniciar como sistema composto</Button>` que seta `composicao: []` no item (converte em raiz de composição). Remover o `onRedirectToSistemas` desse autocomplete.
   - `ambiente.sistemas.map((sis, si) => ...)`: renderizar o card de Fita Padrão EXATAMENTE como hoje (todo o bloco 416-642 — fita, perfil, driver, ValidacaoPanel, duplicar/remover). Byte-identical: NÃO alterar markup, classes, cálculos ou handlers. Apenas mover o bloco para fora do `<TabsContent>`.
   - Estado vazio (Copywriting Contract): quando `ambiente.luminarias.length === 0 && ambiente.sistemas.length === 0`:
```tsx
<p className="text-xs text-muted-foreground text-center py-4">
  Nenhum item adicionado. Use a busca acima para adicionar luminárias ou sistemas.
</p>
```

Os botões "Adicionar Luminária" (408) e "Novo Sistema" (643) como pontos de entrada primários são REMOVIDOS — a entrada é a busca product-first. Os handlers `addLuminaria`/`addSistema` permanecem (chamados internamente).

NÃO tocar: `calcularDemandaFita`, `calcularConsumoW`, `calcularQtdDrivers`, `calcularSubtotalSistemaSemFita` (importados e usados no card de fita) — só são chamados, nunca redefinidos aqui. O card de Fita Padrão renderiza idêntico.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run build 2>&1 | tail -15 && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TabsContent" auraoramentos/src/components/AmbienteCard.tsx` → 0 (tabs removidas)
    - `grep -c "activeTab" auraoramentos/src/components/AmbienteCard.tsx` → 0 (state removido)
    - `grep -n "<ComposicaoCard" auraoramentos/src/components/AmbienteCard.tsx` → 1 match
    - `grep -n "item.composicao !== undefined" auraoramentos/src/components/AmbienteCard.tsx` → 1 match
    - `grep -n "Buscar produto por código ou descrição" auraoramentos/src/components/AmbienteCard.tsx` → 1 match
    - `grep -n "Iniciar como sistema composto" auraoramentos/src/components/AmbienteCard.tsx` → 1 match (fallback D-03)
    - `grep -n "Nenhum item adicionado" auraoramentos/src/components/AmbienteCard.tsx` → 1 match (estado vazio)
    - Fita Padrão intocada (markup ainda presente): `grep -c "ValidacaoPanel" auraoramentos/src/components/AmbienteCard.tsx` → 1; `grep -c "handleSelectProdutoSistema" auraoramentos/src/components/AmbienteCard.tsx` → ao menos 4
    - 5 calc sites byte-idênticos: `grep -n "export function calcularDemandaFita(arg: SistemaIluminacao | ItemPerfil): number {" auraoramentos/src/types/orcamento.ts` → 1; `grep -n "function isSistemaVazio(sis: SistemaIluminacao): boolean {" auraoramentos/src/lib/pdfTemplates/v2.ts` → 1 (arquivos de cálculo NÃO tocados por este plano)
    - `npm run build` verde; `npm run lint` sem erros novos
  </acceptance_criteria>
  <done>Tabs removidas; busca product-first + lista unificada; ComposicaoCard renderizado; Fita Padrão e calc sites intocados; build/lint verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual do fluxo product-first</name>
  <files>src/components/AmbienteCard.tsx, src/components/ComposicaoCard.tsx</files>
  <action>Checkpoint de verificação humana. Rodar code review + Playwright MCP (pipeline padrão do CLAUDE.md global do Lenny) sobre o diff dos Planos 01-03, depois subir o dev server e seguir os passos de verificação abaixo. Pausar para confirmação do usuário antes de fechar a fase.</action>
  <verify>Manual — ver passos em how-to-verify abaixo.</verify>
  <done>Usuário digita "approved" confirmando os 9 cenários sem erros de console nem regressão de Fita Padrão.</done>
  <what-built>
    AmbienteCard reorganizado: busca product-first única (sem abas), lista unificada, roteamento automático de tipo, ComposicaoCard para MAGNETO 48V / TINY 24V (módulos, carga, painel de driver com "Aplicar", checklist), e card de Fita Padrão intocado. Code review + Playwright MCP devem ser executados ANTES deste checkpoint (pipeline padrão do CLAUDE.md global do Lenny).
  </what-built>
  <how-to-verify>
    1. `cd auraoramentos && npm run dev` — abrir o wizard, Step 2, criar um ambiente.
    2. **Fita (não-regressão):** buscar uma fita LED na busca product-first → deve criar um card de Fita Padrão idêntico ao atual, com driver sugerido automaticamente. Conferir demanda/consumo/qtd drivers iguais ao comportamento anterior.
    3. **MAGNETO 48V:** buscar um trilho magneto 48V → deve criar um ComposicaoCard com badge "MAGNETO 48V"; adicionar módulos (busca só mostra módulos 48V) → carga total aparece; painel mostra "Driver recomendado: LM2343/LM2344"; clicar "Aplicar" → driver entra no card; checklist mostra conector LM2338 ausente com "+ Adicionar".
    4. **>200W:** adicionar módulos somando >200W → painel mostra aviso de dividir circuitos, SEM botão "Aplicar".
    5. **TINY 24V:** buscar trilho TINY → badge "TINY 24V"; busca de driver só mostra 24V; checklist aceita LM3168 OU LM3169.
    6. **Embutir:** trilho com "EMBUTIR" na descrição → checklist mostra linha do kit LM2987.
    7. **Item simples:** buscar um spot comum → entra como linha simples.
    8. **Console:** sem erros JS no DevTools.
    9. **Regressão:** abrir um orçamento antigo (rascunho) → renderiza sem quebrar; PDF de orçamento sem compostos continua v2.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados (visual/funcional/console).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| busca product-first (catálogo) → roteamento | tipo detectado a partir de campos não confiáveis do catálogo (sistema/tipo_produto) |
| AmbienteCard → snapshot do orçamento | a estrutura luminarias[]/sistemas[] montada aqui é persistida |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09 | Tampering | roteamento detectarTipoAncora | mitigate | Fallback gracioso (D-03): dado sujo/ausente → item simples, nunca quebra nem perde o produto; arrays luminarias[]/sistemas[] permanecem separados (integridade do snapshot) |
| T-20-10 | Repudiation/Integrity | snapshots antigos | mitigate | composicao opcional; itens sem composicao renderizam como linha simples; Fita Padrão byte-idêntico; PDF v2 condicional inalterado |
| T-20-11 | Elevation of Privilege | nova entrada de UI | accept | Nenhuma nova rota/permissão; mesma sessão autenticada; RLS inalterada; sem coluna/tabela nova |
| T-20-12 | DoS | re-render da lista unificada | accept | Padrão de re-render do wizard existente (Pitfall 6); arrays pequenos; consistente com baseline |
</threat_model>

<verification>
- `npm run build` + `npm run lint` verdes
- Code review + Playwright MCP executados antes do checkpoint (pipeline padrão Lenny)
- 5 calc sites byte-idênticos (grep das assinaturas)
- Fita Padrão sem regressão (markup + cálculo idênticos; só o ponto de entrada mudou)
- Snapshots antigos renderizam; PDF v2 condicional inalterado
- Checkpoint humano confirma os 9 cenários
</verification>

<success_criteria>
- Busca product-first única substitui as abas; sem seletor de tipo manual (SIST-05)
- Roteamento automático fita/magneto/tiny/simples + fallback gracioso (D-02/D-03)
- ComposicaoCard renderizado para itens compostos (SIST-01/02, COMP-01 via card)
- Fita Padrão e 5 calc sites intocados — zero regressão
- Checkpoint visual aprovado
</success_criteria>

<output>
After completion, create `.planning/phases/20-fluxos-magn-ticos/20-03-SUMMARY.md`
</output>
