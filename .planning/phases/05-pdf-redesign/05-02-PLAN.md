---
phase: 05-pdf-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/orcamento.ts
  - src/components/AmbienteCard.tsx
autonomous: true
requirements:
  - PDF-01
must_haves:
  truths:
    - "Tipo SistemaIluminacao tem campo opcional `local: string | null`"
    - "Form de Sistema em AmbienteCard mostra input 'Local (opcional)' com placeholder 'Sanca, Rasgo, Pé-direito...'"
    - "Sistemas existentes sem `local` continuam válidos (campo opcional)"
    - "Snapshot persistido em orcamentos.ambientes (jsonb) inclui `local` quando preenchido pelo usuário"
    - "Sistemas sem `local` ficam com null/undefined no snapshot — não quebra render"
  artifacts:
    - path: src/types/orcamento.ts
      provides: "Type extension SistemaIluminacao com campo `local?: string | null`"
      contains: "local?: string | null"
    - path: src/components/AmbienteCard.tsx
      provides: "Input 'Local (opcional)' no header de cada Sistema"
      contains: "Local (opcional)"
  key_links:
    - from: "src/components/AmbienteCard.tsx (input Local)"
      to: "SistemaIluminacao.local (type)"
      via: "updateSistema(si, { ...sis, local: value })"
      pattern: "local:"
    - from: "src/components/Step3Revisao.tsx (persiste ambientes)"
      to: "orcamentos.ambientes (jsonb)"
      via: "supabase.from('orcamentos').insert({ ambientes: ambientesJson })"
      pattern: "ambientesJson"
---

<objective>
Adicionar campo opcional `local: string | null` ao tipo `SistemaIluminacao` e expor input "Local (opcional)" no formulário do sistema dentro de `AmbienteCard.tsx`. Esse campo permite hierarquia 5-níveis no PDF v2 (Ambiente → Local → Sistema → Componentes — locked decision A4 + B4).

Purpose: A locked decision B4 (CONTEXT.md) define hierarquia visual `Ambiente → Local (Sanca/Rasgo) → Sistema → Componentes`. Hoje o schema só tem Ambiente → Sistema. Sem campo persistido, o template v2 não consegue agrupar. Schema é jsonb (orcamentos.ambientes) — campo novo é aditivo zero-cost (snapshots antigos sem `local` ficam intactos, novos podem ter ou não).
Output: SistemaIluminacao tem campo `local`, UI permite preencher, snapshot persistido aceita o campo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-pdf-redesign/05-CONTEXT.md
@.planning/phases/05-pdf-redesign/05-RESEARCH.md
@src/types/orcamento.ts
@src/components/AmbienteCard.tsx

<interfaces>
<!-- Tipo SistemaIluminacao atual (src/types/orcamento.ts:78-85) -->

```typescript
export interface SistemaIluminacao {
  id: string;
  perfil: ItemPerfil | null;
  fita: ItemFitaLED;
  driver: ItemDriver;
  metragemManual: number | null;
  passadasManual: 1 | 2 | 3;
  // ADICIONAR (esta plan):
  // local?: string | null;  // ex: "Sanca", "Rasgo", "Pé-direito" — opcional, free-form, max ~40 chars (sem validação dura)
}
```

<!-- Local de inserção do input no AmbienteCard.tsx -->
<!-- Linha ~349: header de cada sistema (`<div className="flex items-center justify-between px-4 py-2 bg-muted/40 border-b">`) -->
<!-- Adicionar input "Local" abaixo do header, ANTES de "Fita LED" (linha ~362). -->

<!-- Pattern existente para text inputs: -->
<!-- `<Input value={x} onChange={(e) => updateSistema(si, { ...sis, campo: e.target.value })} placeholder="..." className="w-..." />` -->

<!-- Função updateSistema (linha 68): -->
<!-- `const updateSistema = (index: number, sis: SistemaIluminacao) => { ... }` -->

<!-- Função addSistema (linha 62-67) — ADICIONAR `local: null` no novoSistema: -->
<!-- `const novoSistema: SistemaIluminacao = { id: uid(), perfil: null, fita: novaFita, driver: novoDriver, metragemManual: null, passadasManual: 1, local: null };` -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Estender SistemaIluminacao com campo local</name>
  <files>src/types/orcamento.ts</files>
  <read_first>
    - src/types/orcamento.ts (linhas 78-95 — interfaces SistemaIluminacao e Ambiente)
    - .planning/phases/05-pdf-redesign/05-RESEARCH.md (Open Question 1 — recomendação `local?: string | null`)
  </read_first>
  <action>
    Em `src/types/orcamento.ts`, dentro da interface `SistemaIluminacao` (linhas 78-85), adicionar UMA linha após `passadasManual`:

    ```typescript
    /** Sub-ambiente / agrupamento opcional (ex: "Sanca", "Rasgo", "Pé-direito"). Phase 5 / PDF-01. */
    local?: string | null;
    ```

    Resultado final esperado:

    ```typescript
    export interface SistemaIluminacao {
      id: string;
      perfil: ItemPerfil | null;
      fita: ItemFitaLED;
      driver: ItemDriver;
      metragemManual: number | null;
      passadasManual: 1 | 2 | 3;
      /** Sub-ambiente / agrupamento opcional (ex: "Sanca", "Rasgo", "Pé-direito"). Phase 5 / PDF-01. */
      local?: string | null;
    }
    ```

    NÃO mexer em nenhuma das funções de cálculo (`calcularDemandaFita`, `calcularConsumoW`, etc.) — `local` é puramente metadata visual, não entra em nenhum cálculo.

    Rodar `npm run build` para confirmar que nada quebrou. Como `local` é opcional, todos os call sites existentes que constroem `SistemaIluminacao` sem o campo continuam compilando.
  </action>
  <verify>
    <automated>grep -n "local?: string | null" src/types/orcamento.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/orcamento.ts` contém literal `local?: string | null` exatamente uma vez
    - O literal está dentro do bloco `export interface SistemaIluminacao { ... }` (entre as linhas dessa interface)
    - JSDoc `/** Sub-ambiente / agrupamento opcional...` precede o campo
    - Nenhuma função de cálculo foi modificada (grep `calcularDemandaFita|calcularConsumoW|calcularQtdDrivers` mostra mesma quantidade de matches que antes)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Tipo estendido, build limpo, nenhuma cálculo alterada.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adicionar input "Local (opcional)" no AmbienteCard + inicializar local em addSistema</name>
  <files>src/components/AmbienteCard.tsx</files>
  <read_first>
    - src/components/AmbienteCard.tsx (linhas 60-75 — addSistema/updateSistema)
    - src/components/AmbienteCard.tsx (linhas 338-365 — TabsContent value="sistemas" e header do sistema)
    - src/components/ui/input.tsx (componente Input shadcn — confirmar que aceita `placeholder` e `className`)
  </read_first>
  <action>
    Duas mudanças em `src/components/AmbienteCard.tsx`:

    **Mudança 1 — addSistema (linha ~62-67):** Inicializar `local: null` no novo sistema. Trocar a linha:

    ```typescript
    const novoSistema: SistemaIluminacao = { id: uid(), perfil: null, fita: novaFita, driver: novoDriver, metragemManual: null, passadasManual: 1 };
    ```

    Por:

    ```typescript
    const novoSistema: SistemaIluminacao = { id: uid(), perfil: null, fita: novaFita, driver: novoDriver, metragemManual: null, passadasManual: 1, local: null };
    ```

    **Mudança 2 — Input "Local (opcional)" (logo após o header do sistema, ANTES da seção "Fita LED" em ~linha 361):**

    Localizar o bloco que abre o conteúdo do sistema:

    ```tsx
    <div className="p-4 space-y-3">

      {/* ── FITA LED ── */}
      <div className="space-y-2">
        <span className="text-xs font-semibold text-primary uppercase tracking-wide">Fita LED</span>
    ```

    Inserir IMEDIATAMENTE depois de `<div className="p-4 space-y-3">` e ANTES do comentário `{/* ── FITA LED ── */}`:

    ```tsx
    {/* ── LOCAL (opcional, Phase 5 PDF-01) ── */}
    <div className="space-y-2">
      <div className="flex items-center gap-2">
        <span className="text-xs font-semibold text-muted-foreground uppercase tracking-wide">Local</span>
        <Badge variant="outline" className="text-[10px] px-1.5 py-0">Opcional</Badge>
      </div>
      <Input
        value={sis.local ?? ""}
        onChange={(e) => updateSistema(si, { ...sis, local: e.target.value || null })}
        placeholder="Sanca, Rasgo, Pé-direito... (deixe em branco se não aplicar)"
        maxLength={40}
        className="h-8 text-sm"
      />
    </div>
    ```

    O `Input` e `Badge` já estão importados no topo do arquivo (linhas 1-15) — não precisa adicionar imports.

    Comportamento esperado:
    - Vazio = `null` (sem header de Local no PDF v2 — vai pra pseudo-grupo "Geral")
    - Preenchido = `string` não-vazia (vira header "Local: Sanca" no PDF v2)
    - O `e.target.value || null` garante que string vazia normaliza pra null

    Rodar `npm run build` e `npm run lint` para confirmar que nada quebrou.

    **Pipeline obrigatório (per global CLAUDE.md):** Após esta mudança em UI, rodar code-review skill + Playwright MCP para abrir Index.tsx, criar um ambiente, adicionar um sistema, preencher Local com "Sanca", confirmar que o estado React reflete (re-render mostra "Sanca" no input). Reportar ao final do plan.
  </action>
  <verify>
    <automated>grep -n "Local (opcional, Phase 5 PDF-01)" src/components/AmbienteCard.tsx && grep -n "local: null" src/components/AmbienteCard.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/AmbienteCard.tsx` contém literal `local: null` (no addSistema)
    - Contém literal `Local (opcional, Phase 5 PDF-01)` (no comentário do bloco novo)
    - Contém literal `value={sis.local ?? ""}`
    - Contém literal `onChange={(e) => updateSistema(si, { ...sis, local: e.target.value || null })}`
    - Contém literal `placeholder="Sanca, Rasgo, Pé-direito... (deixe em branco se não aplicar)"`
    - Contém `maxLength={40}`
    - O input está DENTRO do mapeamento `ambiente.sistemas.map((sis, si) => ...)` (verificar: o bloco aparece DEPOIS de `<div className="p-4 space-y-3">` e ANTES de `{/* ── FITA LED ── */}`)
    - `npm run build` exits 0
    - `npm run lint` exits 0
    - Playwright MCP smoke (segundo pipeline global): Index → adicionar ambiente → adicionar sistema → preencher Local "Sanca" → re-render mostra "Sanca". Sem erros no console.
  </acceptance_criteria>
  <done>Input "Local (opcional)" funcional no wizard; novos sistemas começam com local=null; sistemas existentes continuam válidos.</done>
</task>

</tasks>

<verification>
- Type extension não quebra nada (Task 1)
- UI input funciona end-to-end via Playwright (Task 2)
- Snapshot persistido (em Step3 → orcamentos.ambientes) automaticamente inclui `local` quando preenchido — sem mudança no Step3 porque ele já serializa o array `ambientes` inteiro (`ambientes` Json).
</verification>

<success_criteria>
- Campo `local?: string | null` adicionado em SistemaIluminacao
- Input "Local (opcional)" visível e editável em cada Sistema dentro de AmbienteCard
- Sistemas com local preenchido persistem o valor no jsonb `orcamentos.ambientes`
- Sistemas sem local não quebram nada (campo opcional)
- Build, lint e smoke Playwright limpos
</success_criteria>

<output>
Após completion, criar `.planning/phases/05-pdf-redesign/05-02-SUMMARY.md` registrando:
- Diff resumido em orcamento.ts (1 linha + JSDoc)
- Diff resumido em AmbienteCard.tsx (1 linha addSistema + bloco Input ~14 linhas)
- Resultado do code-review + Playwright smoke
</output>
