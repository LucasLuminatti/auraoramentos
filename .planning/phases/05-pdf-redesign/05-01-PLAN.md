---
phase: 05-pdf-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260507000001_add_pdf_template_version.sql
  - src/integrations/supabase/types.ts
autonomous: true
requirements:
  - PDF-05
must_haves:
  truths:
    - "Coluna pdf_template_version existe em public.orcamentos em produção (Supabase jkewlaezvrbuicmncqbj)"
    - "Coluna tem default 2 para inserts novos"
    - "Rows pré-existentes ficam com NULL (tratadas como v1 no leitor via coalesce)"
    - "src/integrations/supabase/types.ts inclui pdf_template_version no type Row/Insert/Update de orcamentos"
  artifacts:
    - path: supabase/migrations/20260507000001_add_pdf_template_version.sql
      provides: "Migration aditiva: ADD COLUMN pdf_template_version int default 2"
      contains: "ALTER TABLE public.orcamentos ADD COLUMN pdf_template_version"
    - path: src/integrations/supabase/types.ts
      provides: "Tipo Database['public']['Tables']['orcamentos'] inclui pdf_template_version"
      contains: "pdf_template_version"
  key_links:
    - from: "supabase/migrations/20260507000001_add_pdf_template_version.sql"
      to: "public.orcamentos (Supabase prod)"
      via: "supabase db push"
      pattern: "ALTER TABLE public.orcamentos"
    - from: "src/integrations/supabase/types.ts"
      to: "public.orcamentos"
      via: "supabase gen types typescript"
      pattern: "pdf_template_version"
---

<objective>
Adicionar coluna `pdf_template_version int default 2` em `public.orcamentos` via migration aditiva, aplicar em produção (Supabase project `jkewlaezvrbuicmncqbj`) e regenerar `types.ts` para que o router v1/v2 (Plan 05) possa ler a versão por orçamento.

Purpose: Plan 05 (router) precisa de um campo persistido que diga qual template usar. Sem essa coluna, snapshots antigos não têm como ser distinguidos dos novos — render quebra (PDF-05). Migration aditiva (default 2 para novos, NULL coerced para 1 nos legados via `coalesce(pdf_template_version, 1)` no leitor).
Output: 1 migration aplicada em prod + types.ts atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-pdf-redesign/05-CONTEXT.md
@.planning/phases/05-pdf-redesign/05-RESEARCH.md
@supabase/migrations/20260504000001_drive_rls_user_id.sql
@supabase/migrations/20260504000002_arquivo_url_nullable.sql

<interfaces>
<!-- Schema atual de orcamentos (de src/integrations/supabase/types.ts:320-388) -->

```typescript
// PRESENTE (não mexer):
orcamentos: {
  Row: {
    ambientes: Json;
    cliente_id: string;
    colaborador_id: string;
    created_at: string;
    data: string;
    fechado_at: string | null;
    id: string;
    motivo_perda: string | null;
    motivo_perda_detalhe: string | null;
    projeto_id: string | null;
    status: string;
    tipo: string | null;
    valor: number;
  }
  // Insert e Update espelham com `?` em campos opcionais
}

// AUSENTE (a adicionar nesta plan):
// pdf_template_version: number | null  (default 2 no Insert)
```

Padrão de migration de referência (Phase 4): `supabase/migrations/20260504000001_drive_rls_user_id.sql` — usa `BEGIN; ... COMMIT;`, comentários `-- =====`, e `COMMENT ON COLUMN`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration aditiva pdf_template_version</name>
  <files>supabase/migrations/20260507000001_add_pdf_template_version.sql</files>
  <read_first>
    - supabase/migrations/20260504000001_drive_rls_user_id.sql (padrão BEGIN/COMMIT + COMMENT ON COLUMN)
    - supabase/migrations/20260504000002_arquivo_url_nullable.sql (padrão de mudança aditiva simples)
    - src/integrations/supabase/types.ts (linhas 320-388 — confirmar nome da tabela e colunas existentes)
    - .planning/phases/05-pdf-redesign/05-RESEARCH.md (seção "Architecture Pattern 1: Template Versioning")
  </read_first>
  <action>
    Criar arquivo `supabase/migrations/20260507000001_add_pdf_template_version.sql` com o conteúdo exato:

```sql
-- Phase 5 / Plan 01: pdf_template_version em orcamentos (PDF-05 compat)
-- Refs: RESEARCH.md "Architecture Pattern 1: Template Versioning"
-- Estratégia: ADD COLUMN nullable com default 2 (snapshots novos pegam v2);
-- snapshots criados ANTES desta migration ficam NULL e o leitor faz
-- coalesce(pdf_template_version, 1) no router gerarPdfHtml.ts (Plan 05).

BEGIN;

-- =========================================================================
-- Bloco 1 — ADD COLUMN pdf_template_version (aditiva, não-destrutiva)
-- =========================================================================
ALTER TABLE public.orcamentos
  ADD COLUMN pdf_template_version integer DEFAULT 2;

COMMENT ON COLUMN public.orcamentos.pdf_template_version IS
  'Versão do template do PDF a ser usado para renderizar este orçamento. NULL ou < 2 = template v1 (legacy, antes da Phase 5). >= 2 = template v2 (editorial, Phase 5+). Default 2 em rows novas; rows pré-existentes ficam NULL e são tratadas como v1 via coalesce no router.';

-- =========================================================================
-- Bloco 2 — Verificação pós-migration
-- Sanity check: confirma que a coluna existe e que rows existentes ficaram NULL
-- (não preencher retroativamente — esse é o comportamento desejado para PDF-05).
-- =========================================================================
DO $$
DECLARE col_count int;
BEGIN
  SELECT count(*) INTO col_count
    FROM information_schema.columns
    WHERE table_schema = 'public'
      AND table_name = 'orcamentos'
      AND column_name = 'pdf_template_version';
  IF col_count = 0 THEN
    RAISE EXCEPTION 'Migration failed: pdf_template_version não foi adicionada';
  END IF;
END $$;

COMMIT;
```

Não usar UPDATE para preencher rows antigos — eles devem ficar NULL para que `coalesce(..., 1)` no router (Plan 05) os trate como v1.
  </action>
  <verify>
    <automated>grep -E "ALTER TABLE public\.orcamentos\s+ADD COLUMN pdf_template_version" supabase/migrations/20260507000001_add_pdf_template_version.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260507000001_add_pdf_template_version.sql` existe
    - Contém literal `ALTER TABLE public.orcamentos`
    - Contém literal `ADD COLUMN pdf_template_version integer DEFAULT 2`
    - Contém `BEGIN;` e `COMMIT;`
    - NÃO contém nenhum `UPDATE public.orcamentos SET pdf_template_version` (rows antigos têm que ficar NULL)
    - Contém `COMMENT ON COLUMN public.orcamentos.pdf_template_version`
  </acceptance_criteria>
  <done>Migration file exists, syntax válida (revisada visualmente), nenhum UPDATE retroativo presente.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar migration em produção via supabase db push</name>
  <files>supabase/migrations/20260507000001_add_pdf_template_version.sql (não editar — só executar)</files>
  <read_first>
    - supabase/migrations/20260507000001_add_pdf_template_version.sql (acabou de ser criada na Task 1)
    - .planning/STATE.md (linha "Migrations aplicadas" — confirmar última migration aplicada foi 20260504000002)
  </read_first>
  <action>
    Aplicar a migration em produção. Esta task é [BLOCKING] — Plan 05 (router) e Plan 06 (call sites) lerão `pdf_template_version` da tabela em prod, então a coluna PRECISA existir lá.

    Executar (PowerShell, do diretório `auraoramentos/`):

    ```powershell
    npx supabase db push
    ```

    Se houver prompt interativo de confirmação, aceitar (a migration é aditiva e segura). Se `SUPABASE_ACCESS_TOKEN` não estiver no ambiente ou o CLI não conseguir autenticar, parar e reportar — não continuar para Task 3.

    Após push bem-sucedido, executar verificação SQL via `supabase` CLI ou via Supabase Studio (uma das duas):

    ```powershell
    npx supabase db remote query "SELECT column_name, data_type, column_default FROM information_schema.columns WHERE table_schema='public' AND table_name='orcamentos' AND column_name='pdf_template_version';"
    ```

    Output esperado: 1 row com `pdf_template_version | integer | 2`. Se 0 rows, push falhou silenciosamente — investigar.
  </action>
  <verify>
    <automated>npx supabase db remote query "SELECT count(*) FROM information_schema.columns WHERE table_schema='public' AND table_name='orcamentos' AND column_name='pdf_template_version';" 2>&1 | grep -E "1$|count"</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db push` retorna exit 0
    - Query `information_schema.columns` retorna 1 row para `pdf_template_version`
    - Coluna tem `data_type = integer` e `column_default = 2`
    - Nenhum erro no output do push (especialmente nada sobre "permission denied", "already exists", "syntax error")
  </acceptance_criteria>
  <done>Coluna `pdf_template_version` confirmada existindo em `public.orcamentos` em produção (project jkewlaezvrbuicmncqbj).</done>
</task>

<task type="auto">
  <name>Task 3: Regenerar types.ts e validar build</name>
  <files>src/integrations/supabase/types.ts</files>
  <read_first>
    - src/integrations/supabase/types.ts (linhas 320-388 — estado atual antes do regen)
    - package.json (confirmar se há script para gen types — provavelmente `supabase gen types typescript --linked` ou similar)
  </read_first>
  <action>
    Regenerar `src/integrations/supabase/types.ts` a partir do schema de produção:

    ```powershell
    npx supabase gen types typescript --linked > src/integrations/supabase/types.ts
    ```

    Se `--linked` não estiver configurado, usar `--project-id jkewlaezvrbuicmncqbj`:

    ```powershell
    npx supabase gen types typescript --project-id jkewlaezvrbuicmncqbj > src/integrations/supabase/types.ts
    ```

    Depois validar:

    1. Abrir `src/integrations/supabase/types.ts` e confirmar que dentro do bloco `orcamentos: { Row: { ... } }` agora aparece `pdf_template_version: number | null`. No bloco `Insert: { ... }` deve aparecer `pdf_template_version?: number | null`. Mesma coisa em `Update`.
    2. Rodar build para garantir que nenhum call site quebrou: `npm run build`. Como nenhum call site existente lê `pdf_template_version` ainda (Plan 05 fará isso), o build deve passar limpo.
    3. Rodar lint: `npm run lint` — também deve passar.

    Não editar `types.ts` à mão — é arquivo gerado. Se a regeneração não trouxe a coluna, voltar à Task 2 e investigar se o push realmente foi aplicado.
  </action>
  <verify>
    <automated>grep -n "pdf_template_version" src/integrations/supabase/types.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/integrations/supabase/types.ts` contém literal `pdf_template_version` em pelo menos 3 ocorrências (Row, Insert, Update do bloco `orcamentos`)
    - Cada ocorrência tem o tipo `number | null` (Row) ou `number | null` opcional (Insert/Update)
    - `npm run build` exits 0
    - `npm run lint` exits 0 (ou apenas warnings, sem errors)
    - `.planning/STATE.md` linha "Migrations aplicadas" pode ser atualizada (Plan 06 closure fará isso)
  </acceptance_criteria>
  <done>Types regenerados com `pdf_template_version`, build e lint limpos.</done>
</task>

</tasks>

<verification>
- Migration file criada com sintaxe correta e sem UPDATE retroativo (Task 1)
- Migration aplicada em prod, coluna existe com default 2 (Task 2)
- types.ts regenerado, build passa (Task 3)
</verification>

<success_criteria>
- Coluna `pdf_template_version` existe em `public.orcamentos` em prod
- Default 2 confirmado para inserts novos
- Rows pré-existentes ficam NULL (tratamento de PDF-05 fica para Plan 05 router)
- Tipo TypeScript reflete a coluna nova
- Build/lint sem regressão
</success_criteria>

<output>
Após completion, criar `.planning/phases/05-pdf-redesign/05-01-SUMMARY.md` registrando:
- Migration aplicada (timestamp + project ID)
- Confirmação da coluna em information_schema
- Versão dos types regenerados
</output>
