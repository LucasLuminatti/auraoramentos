---
phase: 05-pdf-redesign
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-03", "05-04"]
files_modified:
  - src/lib/pdfTemplates/v1.ts
  - src/lib/gerarPdfHtml.ts
  - src/components/Step3Revisao.tsx
  - src/pages/OrcamentoDetalhe.tsx
autonomous: false
requirements:
  - PDF-05
must_haves:
  truths:
    - "Template legacy preservado em src/lib/pdfTemplates/v1.ts (renomeação do conteúdo atual de gerarPdfHtml.ts)"
    - "src/lib/gerarPdfHtml.ts vira router que despacha v1 ou v2 baseado em params.templateVersion"
    - "templateVersion ausente OU < 2 → v1 (legacy)"
    - "templateVersion >= 2 → v2 (novo editorial)"
    - "Step3Revisao (orçamento NOVO) chama gerarOrcamentoHtml com templateVersion=2 e persiste pdf_template_version=2 no insert do Supabase"
    - "OrcamentoDetalhe (Re-emitir PDF) lê orc.pdf_template_version do row e passa para o router; NULL/undefined→1 via coalesce no leitor"
    - "Snapshot antigo (criado antes desta phase, sem coluna preenchida) renderiza com v1 (template legacy) sem crash"
    - "Snapshot antigo ainda funciona porque v1 nunca depende de sis.local"
    - "Step3 e OrcamentoDetalhe carregam @/lib/pdfFonts e chamam ensureFontsReady() ANTES de html2pdf()"
    - "Step3 e OrcamentoDetalhe pre-convertem imagemUrl via inlineImagensSnapshot ANTES de chamar gerarOrcamentoHtml"
    - "pagebreak.mode trocado de ['avoid-all','css','legacy'] para ['css','legacy'] (Pitfall 3)"
  artifacts:
    - path: src/lib/pdfTemplates/v1.ts
      provides: "Template legacy (conteúdo do gerarPdfHtml.ts atual, renomeado)"
      exports: ["gerarOrcamentoHtmlV1", "PdfParamsV1"]
    - path: src/lib/gerarPdfHtml.ts
      provides: "Router público — gerarOrcamentoHtml + PdfParams (assinatura backward-compat)"
      exports: ["gerarOrcamentoHtml", "PdfParams"]
      contains: "templateVersion"
    - path: src/components/Step3Revisao.tsx
      provides: "Geração nova com v2 + persistência de pdf_template_version=2"
      contains: "pdf_template_version: 2"
    - path: src/pages/OrcamentoDetalhe.tsx
      provides: "Re-emitir PDF lendo pdf_template_version do row"
      contains: "pdf_template_version"
  key_links:
    - from: "src/lib/gerarPdfHtml.ts (router)"
      to: "src/lib/pdfTemplates/v1.ts"
      via: "import { gerarOrcamentoHtmlV1 }"
      pattern: "from \"./pdfTemplates/v1\""
    - from: "src/lib/gerarPdfHtml.ts (router)"
      to: "src/lib/pdfTemplates/v2.ts"
      via: "import { gerarOrcamentoHtmlV2 }"
      pattern: "from \"./pdfTemplates/v2\""
    - from: "src/components/Step3Revisao.tsx"
      to: "supabase orcamentos.pdf_template_version"
      via: "insert/update com pdf_template_version: 2"
      pattern: "pdf_template_version: 2"
    - from: "src/pages/OrcamentoDetalhe.tsx"
      to: "orcamento.pdf_template_version (do select)"
      via: "templateVersion: orc.pdf_template_version ?? 1"
      pattern: "pdf_template_version \\?\\? 1"
---

<objective>
Transformar `gerarPdfHtml.ts` em router v1/v2 (preservando assinatura pública), extrair o template atual para `src/lib/pdfTemplates/v1.ts`, e atualizar os 2 call sites (Step3Revisao + OrcamentoDetalhe) para: (a) carregar fontes via pdfFonts, (b) pre-converter imagens via pdfImages, (c) passar `templateVersion` correto, (d) persistir `pdf_template_version: 2` em orçamentos novos, (e) ler `orc.pdf_template_version` em re-emissões. Trocar `pagebreak.mode` para `["css","legacy"]` em ambos call sites.

Purpose: Esse plan finaliza a integração end-to-end: novos orçamentos saem com template v2 + versão persistida; orçamentos antigos (sem coluna preenchida = NULL = coalesce 1) continuam saindo com v1 sem mudança visual. Garante PDF-05 (compat de snapshot). Também aplica os fixes do RESEARCH (fonts ready + imagens base64 + pagebreak mode) que valem para ambos templates.
Output: 1 arquivo novo (v1.ts), 3 arquivos alterados (gerarPdfHtml.ts, Step3Revisao.tsx, OrcamentoDetalhe.tsx).

NÃO autônomo: tem checkpoint de UAT visual no fim (Lenny olha o PDF v2 + v1 lado a lado).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-pdf-redesign/05-CONTEXT.md
@.planning/phases/05-pdf-redesign/05-RESEARCH.md
@src/lib/gerarPdfHtml.ts
@src/lib/pdfTemplates/v2.ts
@src/lib/pdfFonts.ts
@src/lib/pdfImages.ts
@src/components/Step3Revisao.tsx
@src/pages/OrcamentoDetalhe.tsx

<interfaces>
<!-- Tipo público (mantém assinatura existente para não quebrar call sites — só ADD optional templateVersion) -->

```typescript
// src/lib/gerarPdfHtml.ts (router refatorado)
import { gerarOrcamentoHtmlV1 } from "./pdfTemplates/v1";
import { gerarOrcamentoHtmlV2 } from "./pdfTemplates/v2";
import type { Ambiente } from "@/types/orcamento";

export interface PdfParams {
  clienteNome: string;
  projetoNome: string;
  colaborador: string;
  tipo: string;
  ambientes: Ambiente[];
  logoBase64?: string;
  templateVersion?: number;   // NOVO — 1=legacy, 2=editorial, ausente=2 (v1 NUNCA é default — só quando explicitamente passado 1 ou row legacy)
}

export function gerarOrcamentoHtml(params: PdfParams): string;
```

<!-- v1 = template legacy. Renomeação do que está hoje em gerarPdfHtml.ts. -->
<!-- A função exportada vira `gerarOrcamentoHtmlV1` e o tipo `PdfParamsV1`. -->
<!-- ZERO mudança no HTML/CSS gerado por v1 — preservar bytes. -->

<!-- Step3Revisao (geração NOVA): -->
<!-- - persistirOrcamento (linhas ~199-227) precisa adicionar pdf_template_version: 2 no .insert e no .update -->
<!-- - handlePDF (linhas 249-294) precisa: -->
<!--   1. await import("@/lib/pdfFonts").then(m => m.ensureFontsReady()) -->
<!--   2. const ambientesInline = await inlineImagensSnapshot(ambientes); -->
<!--   3. gerarOrcamentoHtml({..., ambientes: ambientesInline, templateVersion: 2}) -->
<!--   4. trocar pagebreak para ["css","legacy"] -->

<!-- OrcamentoDetalhe (Re-emitir): -->
<!-- - select precisa incluir pdf_template_version no .select() do load (linha ~125) -->
<!-- - handleReemitirPdf (linhas 161-208) precisa: -->
<!--   1. ensureFontsReady -->
<!--   2. inlineImagensSnapshot -->
<!--   3. templateVersion: orc.pdf_template_version ?? 1   ← coalesce para LEGACY (PDF-05) -->
<!--   4. trocar pagebreak para ["css","legacy"] -->
```

<!-- Tipo Ambiente vem de @/types/orcamento — incluir local?: string | null já feito no Plan 02. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extrair template legacy para src/lib/pdfTemplates/v1.ts</name>
  <files>src/lib/pdfTemplates/v1.ts</files>
  <read_first>
    - src/lib/gerarPdfHtml.ts (arquivo COMPLETO — vai ser copiado quase 1:1 para v1.ts)
  </read_first>
  <action>
    Criar `src/lib/pdfTemplates/v1.ts` copiando o conteúdo INTEIRO do `src/lib/gerarPdfHtml.ts` atual, com 2 ajustes mínimos:

    1. Renomear a função exportada de `gerarOrcamentoHtml` → `gerarOrcamentoHtmlV1`.
    2. Renomear a interface exportada de `PdfParams` → `PdfParamsV1`.

    Tudo mais (helpers `pad`, `splitMoeda`, `formatarData`, `imgCell`, `tabelaLuminarias`, `tabelaSistemas`, `tabelaResumoFitas`, todo o HTML/CSS, Outfit + Playfair via Google Fonts CDN, info-grid com 4 caixas, terms-section com bullets, etc.) — preservar BYTE A BYTE. Esse é o template congelado para snapshots antigos. NÃO refatorar, NÃO "melhorar".

    No topo do arquivo adicionar comentário:

    ```typescript
    /**
     * Template v1 — LEGACY. Preservado para snapshots criados antes da Phase 5
     * (orcamentos.pdf_template_version IS NULL ou < 2). NÃO modificar — qualquer
     * mudança aqui afeta orçamentos históricos. Para o template novo, veja v2.ts.
     *
     * Movido de src/lib/gerarPdfHtml.ts em Phase 5 / Plan 05.
     */
    ```

    Depois rodar `npm run build`. Vai falhar porque o `src/lib/gerarPdfHtml.ts` ainda referencia ele mesmo, mas vamos refatorar na Task 2. Se falhar AGORA com erro do tipo "duplicate identifier" porque ambos arquivos exportam `gerarOrcamentoHtml`, isso significa que a renomeação para `gerarOrcamentoHtmlV1` não foi feita corretamente — corrigir.

    Atualmente `gerarPdfHtml.ts` tem `export function gerarOrcamentoHtml` (linha 151) e `export interface PdfParams` (linha 15). Em `v1.ts` esses dois precisam virar `gerarOrcamentoHtmlV1` e `PdfParamsV1`.
  </action>
  <verify>
    <automated>grep -E "export function gerarOrcamentoHtmlV1|export interface PdfParamsV1" src/lib/pdfTemplates/v1.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/pdfTemplates/v1.ts` existe
    - Contém `export function gerarOrcamentoHtmlV1(params: PdfParamsV1): string`
    - Contém `export interface PdfParamsV1 {`
    - Contém `Outfit` (template legacy usa Outfit — preservar)
    - Contém `info-grid` (legacy tem as 4 caixas — preservar)
    - Contém Google Fonts CDN `<link href="https://fonts.googleapis.com`
    - NÃO foi "modernizado" — diff vs gerarPdfHtml.ts atual deve ser pequeno (só renomeações)
    - O total geral em v1 ainda usa fundo escuro `var(--gray-900)` (legacy preservado)
    - Comentário top-of-file menciona "LEGACY" e "Phase 5 / Plan 05"
  </acceptance_criteria>
  <done>v1.ts é cópia fiel do template atual com 2 renomeações; preserva visual histórico.</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar src/lib/gerarPdfHtml.ts para router v1/v2</name>
  <files>src/lib/gerarPdfHtml.ts</files>
  <read_first>
    - src/lib/gerarPdfHtml.ts (estado atual antes de virar router)
    - src/lib/pdfTemplates/v1.ts (existe agora — exporta V1)
    - src/lib/pdfTemplates/v2.ts (Plan 04 — exporta V2)
    - .planning/phases/05-pdf-redesign/05-RESEARCH.md (Code Examples > "Router v1/v2 em gerarPdfHtml.ts")
  </read_first>
  <action>
    Substituir TODO o conteúdo de `src/lib/gerarPdfHtml.ts` pelo router enxuto. O arquivo passa de ~400 linhas para ~30 linhas. Conteúdo exato:

    ```typescript
    /**
     * Router de templates de PDF. Phase 5 / Plan 05.
     *
     * Mantém a assinatura pública `gerarOrcamentoHtml(params)` para preservar
     * todos os call sites (Step3Revisao + OrcamentoDetalhe). Internamente
     * dispatcha para v1 (legacy) ou v2 (editorial) baseado em
     * `params.templateVersion`.
     *
     * Regra de roteamento:
     * - templateVersion >= 2 → v2 (template editorial novo)
     * - templateVersion < 2 ou ausente → v1 (template legacy)
     *
     * Como `pdf_template_version` é NULL em rows criadas antes da Phase 5
     * (migration aditiva, default 2 só para INSERTs novos), o leitor deve
     * fazer `orc.pdf_template_version ?? 1` para preservar render legacy.
     * Step3Revisao sempre passa `templateVersion: 2`.
     */

    import type { Ambiente } from "@/types/orcamento";
    import { gerarOrcamentoHtmlV1 } from "./pdfTemplates/v1";
    import { gerarOrcamentoHtmlV2 } from "./pdfTemplates/v2";

    export interface PdfParams {
      clienteNome: string;
      projetoNome: string;
      colaborador: string;
      tipo: string;
      ambientes: Ambiente[];
      logoBase64?: string;
      /**
       * Versão do template do PDF. >=2 = v2 editorial (Phase 5+).
       * <2 ou ausente = v1 legacy. PDF-05.
       */
      templateVersion?: number;
    }

    export function gerarOrcamentoHtml(params: PdfParams): string {
      const v = params.templateVersion ?? 2;
      if (v >= 2) {
        return gerarOrcamentoHtmlV2(params);
      }
      return gerarOrcamentoHtmlV1(params);
    }
    ```

    Decisão de design importante:

    - O **default** dentro do router é `2` (`?? 2`). Isso significa: se um call site chama `gerarOrcamentoHtml(params)` sem passar `templateVersion`, sai v2.
    - O **coalesce para legacy (1)** acontece no LEITOR (OrcamentoDetalhe lê `orc.pdf_template_version ?? 1`). Esse é o ponto crítico do PDF-05: rows antigas têm NULL → leitor coage para 1 → router recebe 1 → sai v1.
    - Step3Revisao SEMPRE passa `templateVersion: 2` explicitamente (orçamentos novos sempre saem com v2).

    Validação:
    - `npm run build` deve passar.
    - `npm run lint` deve passar.
    - Os 2 call sites EXISTENTES (Step3Revisao e OrcamentoDetalhe) continuam compilando porque PdfParams ganhou só um campo opcional. A próxima task ajusta cada um para passar templateVersion corretamente.
  </action>
  <verify>
    <automated>grep -E "templateVersion\?: number|gerarOrcamentoHtmlV1|gerarOrcamentoHtmlV2" src/lib/gerarPdfHtml.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/gerarPdfHtml.ts` tem ≤ 50 linhas (router enxuto, NÃO tem mais o HTML monolítico)
    - Importa `gerarOrcamentoHtmlV1` de `./pdfTemplates/v1`
    - Importa `gerarOrcamentoHtmlV2` de `./pdfTemplates/v2`
    - Exporta `PdfParams` com campo `templateVersion?: number`
    - Exporta `gerarOrcamentoHtml` que faz `params.templateVersion ?? 2` e despacha
    - NÃO contém mais `Outfit`, `info-grid`, `Playfair Display` (esses ficaram em v1.ts e v2.ts)
    - NÃO contém `<style>` nem `<!DOCTYPE html>` (router não constrói HTML)
    - `npm run build` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Router em vigor; v1 e v2 acessíveis via `gerarOrcamentoHtml`; assinatura pública preservada.</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar Step3Revisao.tsx — fontes, imagens base64, templateVersion=2, persist pdf_template_version</name>
  <files>src/components/Step3Revisao.tsx</files>
  <read_first>
    - src/components/Step3Revisao.tsx (linhas 195-294 — persistirOrcamento + carregarLogoBase64 + handlePDF)
    - src/lib/pdfFonts.ts (Plan 03 — confirmar export ensureFontsReady)
    - src/lib/pdfImages.ts (Plan 03 — confirmar export inlineImagensSnapshot)
    - .planning/phases/05-pdf-redesign/05-RESEARCH.md (Pitfall 1, 2, 3 — fonts ready, base64, pagebreak)
  </read_first>
  <action>
    Três mudanças em `src/components/Step3Revisao.tsx`:

    **Mudança 1 — persistirOrcamento (linhas 199-211 update e 212-227 insert):** Adicionar `pdf_template_version: 2` em ambos os casos.

    No bloco do UPDATE (linhas ~201-208):

    ```typescript
    .update({
      tipo: dados.tipo || null,
      ambientes: ambientesJson,
      valor: totalGeral,
      pdf_template_version: 2,   // ← ADICIONAR esta linha (Phase 5 PDF-05)
    })
    ```

    No bloco do INSERT (linhas ~213-222):

    ```typescript
    .insert({
      cliente_id: clienteId,
      colaborador_id: colaborador.id,
      projeto_id: projetoId || null,
      tipo: dados.tipo || null,
      ambientes: ambientesJson,
      valor: totalGeral,
      status: "rascunho" satisfies StatusOrcamento,
      pdf_template_version: 2,   // ← ADICIONAR esta linha (Phase 5 PDF-05)
    })
    ```

    **Mudança 2 — handlePDF (linhas 249-294):** Refatorar para:
    1. Carregar fontes via dynamic import + ensureFontsReady
    2. Pre-converter imagens via inlineImagensSnapshot
    3. Passar `templateVersion: 2`
    4. Trocar pagebreak

    O bloco atual:

    ```typescript
    const [, logoBase64] = await Promise.all([
      persistirOrcamento(),
      carregarLogoBase64(),
    ]);
    const html = gerarOrcamentoHtml({ clienteNome, projetoNome, colaborador: dados.colaborador, tipo: dados.tipo, ambientes, logoBase64 });
    ```

    Vira:

    ```typescript
    // Phase 5: pre-resolver fontes + imagens antes de rasterizar (Pitfalls 1 e 2 do RESEARCH)
    const { ensureFontsReady } = await import("@/lib/pdfFonts");
    const { inlineImagensSnapshot } = await import("@/lib/pdfImages");
    const [, logoBase64, ambientesInline] = await Promise.all([
      persistirOrcamento(),
      carregarLogoBase64(),
      inlineImagensSnapshot(ambientes),
    ]);
    await ensureFontsReady();

    const html = gerarOrcamentoHtml({
      clienteNome,
      projetoNome,
      colaborador: dados.colaborador,
      tipo: dados.tipo,
      ambientes: ambientesInline,
      logoBase64,
      templateVersion: 2,
    });
    ```

    E o bloco `pagebreak: { mode: ["avoid-all", "css", "legacy"] }` (linha ~281) vira:

    ```typescript
    pagebreak: { mode: ["css", "legacy"] },
    ```

    **Mudança 3 — REMOVER o helper local imageToBase64 (linhas 43-57):** Não é mais necessário — `inlineImagensSnapshot` substitui. PORÉM, `carregarLogoBase64` (linha 237-244) ainda usa `imageToBase64(logoModule.default)`. Decisão:
    - **Manter** `imageToBase64` — ele é usado para a logo (asset interno via `import logo`), e essa via é diferente do fetch-de-URL que `pdfImages.ts` faz. Manter como está.
    - Reformular comentário acima dele para deixar claro: `// Helper local para converter asset Vite-imported (logo) — fetch de URL externa usa src/lib/pdfImages.ts.`

    Final:
    - `npm run build`, `npm run lint` limpos
    - Pipeline obrigatório (per global CLAUDE.md): code-review skill + Playwright MCP. Abrir wizard → criar orçamento dummy com 1 ambiente, 1 luminária, 1 sistema (com Local "Sanca") → clicar "Gerar PDF" → verificar:
      - Console sem erros
      - PDF baixa
      - Abrir PDF: layout editorial Apple-like, header laranja em "AMBIENTE", local "Sanca" em italic, sistema 1 com fita+driver, total geral com faixa laranja, bloco de termos no fim com 4 headers em uppercase laranja
      - 4 caixas info-grid AUSENTES
      - Sem emojis no documento
  </action>
  <verify>
    <automated>grep -c "pdf_template_version: 2" src/components/Step3Revisao.tsx && grep -E "ensureFontsReady|inlineImagensSnapshot|templateVersion: 2" src/components/Step3Revisao.tsx && ! grep "avoid-all" src/components/Step3Revisao.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Step3Revisao.tsx` contém literal `pdf_template_version: 2` exatamente 2 vezes (insert + update)
    - Contém `await import("@/lib/pdfFonts")` + `ensureFontsReady`
    - Contém `await import("@/lib/pdfImages")` + `inlineImagensSnapshot`
    - Contém `inlineImagensSnapshot(ambientes)` numa Promise.all com persistirOrcamento + carregarLogoBase64
    - Passa `ambientes: ambientesInline` (não `ambientes: ambientes`) para `gerarOrcamentoHtml`
    - Passa `templateVersion: 2` explicitamente
    - NÃO contém `"avoid-all"` (trocado para `["css","legacy"]`)
    - `imageToBase64` LOCAL ainda existe (usado para logo) — não foi deletado
    - `npm run build` exits 0
    - `npm run lint` exits 0
    - Playwright smoke: PDF gerado mostra novo template, sem console errors
  </acceptance_criteria>
  <done>Step3 gera com v2, persiste versão, fontes carregadas, imagens base64 inline, pagebreak fixed.</done>
</task>

<task type="auto">
  <name>Task 4: Atualizar OrcamentoDetalhe.tsx — ler pdf_template_version do row + passar para router</name>
  <files>src/pages/OrcamentoDetalhe.tsx</files>
  <read_first>
    - src/pages/OrcamentoDetalhe.tsx (estado atual — atenção ao select da linha ~125 e ao handleReemitirPdf 161-208)
    - src/lib/pdfFonts.ts e src/lib/pdfImages.ts (helpers Plan 03)
  </read_first>
  <action>
    Três mudanças em `src/pages/OrcamentoDetalhe.tsx`:

    **Mudança 1 — Select do orçamento (linha ~120-128 onde lê o orcamento por id):** Adicionar `pdf_template_version` na string de SELECT.

    Localizar o bloco que faz `supabase.from("orcamentos").select(...)` e GARANTIR que `pdf_template_version` está na lista de campos retornados. Se o select usa `*` ou um string com vírgulas, adicionar `pdf_template_version` à lista.

    Se o tipo `OrcamentoRow` (interface local, provavelmente declarada perto do topo do arquivo) lista as colunas explicitamente, adicionar lá também: `pdf_template_version: number | null`.

    **Mudança 2 — handleReemitirPdf (linhas 161-208):** Adicionar fontes + imagens + templateVersion:

    O bloco atual:

    ```typescript
    const params: PdfParams = {
      clienteNome: orc.clientes?.nome ?? "—",
      projetoNome: orc.projetos?.nome ?? "—",
      colaborador: orc.colaboradores?.nome ?? "—",
      tipo: orc.tipo,
      ambientes: orc.ambientes ?? [],
      logoBase64: logoBase64 || undefined,
    };
    const html = gerarOrcamentoHtml(params);
    ```

    Vira:

    ```typescript
    // Phase 5: pre-resolver fontes + imagens antes de rasterizar (Pitfalls 1 e 2 do RESEARCH).
    const { ensureFontsReady } = await import("@/lib/pdfFonts");
    const { inlineImagensSnapshot } = await import("@/lib/pdfImages");
    const ambientesInline = await inlineImagensSnapshot(orc.ambientes ?? []);
    await ensureFontsReady();

    const params: PdfParams = {
      clienteNome: orc.clientes?.nome ?? "—",
      projetoNome: orc.projetos?.nome ?? "—",
      colaborador: orc.colaboradores?.nome ?? "—",
      tipo: orc.tipo,
      ambientes: ambientesInline,
      logoBase64: logoBase64 || undefined,
      // PDF-05: rows criadas antes da Phase 5 têm pdf_template_version NULL — coage para 1 (legacy).
      // Rows criadas pela Phase 5 em diante têm 2 explicitamente persistido em Step3Revisao.
      templateVersion: orc.pdf_template_version ?? 1,
    };
    const html = gerarOrcamentoHtml(params);
    ```

    **Mudança 3 — pagebreak (linha ~196):** Trocar `["avoid-all", "css", "legacy"]` para `["css", "legacy"]`.

    Validação:
    - `npm run build`, `npm run lint`
    - Pipeline obrigatório (per global CLAUDE.md): code-review + Playwright MCP. Acessar `/admin/orcamento/{id}` de um orçamento EXISTENTE (criado antes da Phase 5) → clicar "Re-emitir PDF" → confirmar que PDF baixa com layout LEGACY (Outfit, info-grid, fundo escuro do total) — porque `pdf_template_version` é NULL → coalesce 1 → v1.
    - Se também existir um orçamento criado APÓS o Plan 05 ser aplicado, abrir e re-emitir — deve sair com layout EDITORIAL (v2).
    - Se não existir orçamento novo ainda, criar um via wizard primeiro (Step3) e validar a re-emissão depois.
  </action>
  <verify>
    <automated>grep -E "pdf_template_version \?\? 1|inlineImagensSnapshot|ensureFontsReady" src/pages/OrcamentoDetalhe.tsx && ! grep "avoid-all" src/pages/OrcamentoDetalhe.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/OrcamentoDetalhe.tsx` SELECT inclui `pdf_template_version` (verificável: grep `pdf_template_version` no arquivo)
    - Tipo `OrcamentoRow` (ou similar local) tem `pdf_template_version: number | null` se lista colunas explicitamente
    - Contém `await import("@/lib/pdfFonts")` + `ensureFontsReady`
    - Contém `await import("@/lib/pdfImages")` + `inlineImagensSnapshot`
    - Contém literal `templateVersion: orc.pdf_template_version ?? 1`
    - Passa `ambientes: ambientesInline`
    - NÃO contém `"avoid-all"`
    - `npm run build` exits 0
    - `npm run lint` exits 0
    - Playwright UAT visual confirma: orçamento legacy → PDF v1; orçamento novo → PDF v2
  </acceptance_criteria>
  <done>OrcamentoDetalhe lê versão do row, despacha v1 ou v2 corretamente, fontes/imagens prontas, pagebreak fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: UAT visual com Lenny — comparar v1 (snapshot antigo) vs v2 (orçamento novo)</name>
  <what-built>
    - Router gerarPdfHtml.ts dispatcha v1/v2 baseado em pdf_template_version
    - v1 preserva visual histórico (Outfit, info-grid, fundo escuro do total)
    - v2 implementa locked decisions: Apple-like, Playfair+Inter, paleta neutro+laranja, hierarquia 5-níveis com Local, tabela híbrida + chips, thumbs 48×48, 4 caixas removidas e substituídas por bloco prose, total geral redesenhado
    - Fontes via @fontsource (sem CDN race), imagens base64 (sem CORS race), pagebreak ["css","legacy"]
  </what-built>
  <how-to-verify>
    Em `npm run dev` + browser:

    1. **Snapshot antigo (compatibilidade PDF-05):**
       - Acessar `/admin/orcamento/:id` de qualquer orçamento criado antes desta phase
       - Clicar "Re-emitir PDF"
       - Abrir o PDF gerado
       - Esperado: layout LEGACY (font Outfit, 4 caixas info-grid Prazo/Garantia/Pagamento/Observações abaixo do total, total com fundo escuro/azul)
       - Confirmar: NÃO houve crash; campos antigos renderizam normal

    2. **Orçamento novo (template v2 — locked decisions):**
       - No wizard `/`, criar orçamento dummy: 1 cliente, 1 projeto, 1 ambiente "SALA", duas luminárias com thumbnails, 1 sistema com Local="Sanca" (fita+driver+perfil), 1 sistema com Local="Rasgo" (fita+driver), 1 sistema sem Local
       - Avançar para Step3 → Gerar PDF
       - Abrir o PDF gerado
       - Verificar visualmente:
         - [ ] Doc title "Proposta Comercial" em Playfair 32px
         - [ ] Header "SALA" em uppercase laranja com linha gradient à direita
         - [ ] Local "Sanca" em Playfair italic 18px cinza
         - [ ] Sistema 1 abaixo de "Sanca" com fita (chip W/m laranja) + driver (chips neutros)
         - [ ] Local "Rasgo" em Playfair italic 18px cinza
         - [ ] Sistema sem Local aparece sob (sem header de Local — pseudo-grupo "Geral")
         - [ ] Cada item tem thumbnail 48×48 quando imagem existe
         - [ ] 4 colunas: Qtd+un, W/V, Preço Un., SKU
         - [ ] Specs secundárias como chips abaixo da linha
         - [ ] TOTAL GERAL: faixa laranja 4px à esquerda, valor em Playfair grande, sem fundo escuro
         - [ ] Bloco "Termos e Condições" no fim com 4 headers em uppercase laranja (Prazo, Garantia, Pagamento, Observações) como parágrafos contínuos — NÃO como cards
         - [ ] AUSENTE: as 4 caixas info-grid abaixo do total
         - [ ] AUSENTE: emojis (📦 🛡 💳 📋 ⚡)
         - [ ] AUSENTE: cor azul/blue dominante (paleta neutro+laranja)

    3. **Re-emitir o orçamento NOVO:**
       - Após o passo 2, ir em `/admin/orcamento/:id` do orçamento que acabou de criar
       - Clicar "Re-emitir PDF"
       - Esperado: SAI O MESMO LAYOUT V2 (não v1) — porque `pdf_template_version=2` foi persistido no insert.

    4. **Console limpo:** abrir DevTools, gerar PDF, conferir que não há erros JS no console.
  </how-to-verify>
  <resume-signal>Lenny digita "approved" se tudo bater com expectativa, ou descreve issues específicas (ex: "thumbnail muito grande", "cor do Local muito clara") que serão corrigidas em iteração antes de seguir para Plan 06 (closure).</resume-signal>
</task>

</tasks>

<verification>
- v1.ts preserva template legacy intacto
- gerarPdfHtml.ts vira router 30-linhas
- Step3 persiste pdf_template_version=2 + chama v2 + helpers prontos
- OrcamentoDetalhe lê pdf_template_version do row + dispatcha
- pagebreak corrigido nos 2 call sites
- UAT visual confirma v1 vs v2 funcionando
</verification>

<success_criteria>
- Snapshots antigos renderizam com v1 (compat PDF-05 garantido)
- Orçamentos novos renderizam com v2 (PDF-01..04 atendidos)
- Re-emissão respeita pdf_template_version do row
- Lenny aprova UAT
</success_criteria>

<output>
Após completion + UAT approved, criar `.planning/phases/05-pdf-redesign/05-05-SUMMARY.md`:
- Diff resumido dos 4 arquivos
- Resultado do UAT (passed / issues encontradas e endereçadas)
- Confirmação que pdf_template_version está sendo persistido em prod
</output>
