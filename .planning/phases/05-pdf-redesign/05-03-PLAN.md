---
phase: 05-pdf-redesign
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/pdfFonts.ts
  - src/lib/pdfImages.ts
autonomous: true
requirements:
  - PDF-01
must_haves:
  truths:
    - "@fontsource/inter@5.x está em dependencies do package.json"
    - "@fontsource/playfair-display@5.x está em dependencies do package.json"
    - "src/lib/pdfFonts.ts existe e exporta ensureFontsReady()"
    - "src/lib/pdfImages.ts existe e exporta inlineImagensSnapshot()"
    - "ensureFontsReady aguarda document.fonts.ready (espera todas as @font-face declaradas)"
    - "inlineImagensSnapshot retorna deep-clone de Ambiente[] com imagemUrl trocado por base64 quando possível"
  artifacts:
    - path: src/lib/pdfFonts.ts
      provides: "ensureFontsReady() helper que aguarda todas as @font-face do document"
      exports: ["ensureFontsReady"]
      min_lines: 15
    - path: src/lib/pdfImages.ts
      provides: "inlineImagensSnapshot(Ambiente[]) — converte URLs externas para base64 em paralelo"
      exports: ["inlineImagensSnapshot"]
      min_lines: 30
    - path: package.json
      provides: "deps @fontsource/inter e @fontsource/playfair-display"
      contains: "@fontsource/inter"
  key_links:
    - from: "src/lib/pdfFonts.ts"
      to: "document.fonts.ready"
      via: "browser CSS Font Loading API"
      pattern: "document\\.fonts\\.ready"
    - from: "src/lib/pdfImages.ts"
      to: "fetch + FileReader"
      via: "URL → blob → readAsDataURL"
      pattern: "readAsDataURL"
---

<objective>
Criar 2 helpers (`pdfFonts.ts` e `pdfImages.ts`) consumidos pelo template v2 (Plan 04) e pelos call sites (Plan 05). Instalar `@fontsource/inter` e `@fontsource/playfair-display` como deps. Esses helpers resolvem dois pitfalls documentados no RESEARCH (Pitfall 1: race com Google Fonts CDN; Pitfall 2: thumbnails com CORS).

Purpose: html2canvas rasteriza o DOM "como está naquele instante" — se a fonte ainda não carregou, sai Helvetica fallback; se a imagem tem CORS quebrado, sai espaço em branco. Pre-resolver fonts e imagens antes de chamar html2pdf é regra de ouro do RESEARCH (Don't Hand-Roll). Helpers ficam isolados em arquivos próprios para serem reutilizados nos 2 call sites (Step3Revisao + OrcamentoDetalhe) sem duplicação.
Output: 2 arquivos de helpers + 2 deps instaladas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-pdf-redesign/05-RESEARCH.md
@src/types/orcamento.ts
@src/components/Step3Revisao.tsx

<interfaces>
<!-- Tipos consumidos pelo helper (de src/types/orcamento.ts) -->

```typescript
export interface ItemLuminaria {
  // ... campos
  imagemUrl?: string;
}
export interface ItemPerfil {
  imagemUrl?: string;
}
export interface ItemFitaLED {
  imagemUrl?: string;
}
export interface ItemDriver {
  imagemUrl?: string;
}
export interface SistemaIluminacao {
  perfil: ItemPerfil | null;
  fita: ItemFitaLED;
  driver: ItemDriver;
  // local?: string | null; (Plan 02 — pode ou não estar presente quando Plan 03 rodar; tudo bem, helper não toca em local)
}
export interface Ambiente {
  id: string;
  nome: string;
  luminarias: ItemLuminaria[];
  sistemas: SistemaIluminacao[];
}
```

<!-- Padrão imageToBase64 EXISTENTE (src/components/Step3Revisao.tsx:43-57) — usa <img> + canvas: -->
<!-- A nova implementação em pdfImages.ts usa fetch + FileReader (mais robusta para Supabase signed URLs). -->
<!-- O exemplo está no RESEARCH.md "Code Examples > Pre-converter thumbnails para base64". -->

<!-- Vite chunking: imports `@fontsource/.../*.css` viram chunks separados. -->
<!-- Não precisa importar em main.tsx — o Plan 06 fará dynamic import nos call sites. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar @fontsource/inter e @fontsource/playfair-display</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (confirmar versões atuais e que html2pdf.js@0.14.0 está presente)
    - .planning/phases/05-pdf-redesign/05-RESEARCH.md (seção "Standard Stack" — versões 5.2.8 confirmadas via npm view)
  </read_first>
  <action>
    Do diretório `auraoramentos/`:

    ```powershell
    npm install @fontsource/inter@5.2.8 @fontsource/playfair-display@5.2.8 --save
    ```

    Não usar `^5.2.8` ou `~5.2.8` aqui — fixar `5.2.8` (a versão verificada pelo RESEARCH em 2026-05-07). Se depois Lenny quiser bumpar, é uma operação manual.

    Verificar:

    1. `package.json` em `dependencies` agora lista `"@fontsource/inter": "5.2.8"` e `"@fontsource/playfair-display": "5.2.8"`
    2. `package-lock.json` foi atualizado
    3. `npm run build` passa (a instalação não deve quebrar nada — pacotes só ficam no bundle quando algo importa)
  </action>
  <verify>
    <automated># Run via Bash tool (not PowerShell)
grep -q '"@fontsource/inter": "5.2.8"' package.json && grep -q '"@fontsource/playfair-display": "5.2.8"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contém `"@fontsource/inter": "5.2.8"` em `dependencies`
    - `package.json` contém `"@fontsource/playfair-display": "5.2.8"` em `dependencies`
    - `node_modules/@fontsource/inter` e `node_modules/@fontsource/playfair-display` existem (`ls node_modules/@fontsource/`)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Ambas deps fixadas em 5.2.8, build limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Criar src/lib/pdfFonts.ts (carrega fontes + ensureFontsReady)</name>
  <files>src/lib/pdfFonts.ts</files>
  <read_first>
    - .planning/phases/05-pdf-redesign/05-RESEARCH.md (Code Example "Embedar fontes via @fontsource")
    - package.json (confirmar Task 1 completou)
    - node_modules/@fontsource/inter/ (confirmar que .css files existem em pesos 300/400/500/600)
    - node_modules/@fontsource/playfair-display/ (confirmar 400/500/400-italic)
  </read_first>
  <action>
    Criar arquivo `src/lib/pdfFonts.ts` com o conteúdo exato:

    ```typescript
    /**
     * Carrega Inter + Playfair Display via @fontsource (bundle local Vite — sem race com CDN).
     * Cada import roda os @font-face uma vez no document. Pesos baseados no template v2 do PDF.
     *
     * Phase 5 / PDF-01. Resolve Pitfall 1 do RESEARCH (Google Fonts CDN race condition em html2canvas).
     */

    import "@fontsource/inter/300.css";
    import "@fontsource/inter/400.css";
    import "@fontsource/inter/500.css";
    import "@fontsource/inter/600.css";
    import "@fontsource/inter/700.css";
    import "@fontsource/playfair-display/400.css";
    import "@fontsource/playfair-display/500.css";
    import "@fontsource/playfair-display/400-italic.css";

    /**
     * Aguarda todas as @font-face declaradas no document terminarem de carregar.
     * Use ANTES de chamar html2pdf() para garantir que html2canvas captura a fonte
     * correta em vez do fallback (Helvetica/Times). Cross-browser desde 2018.
     */
    export async function ensureFontsReady(): Promise<void> {
      if (typeof document === "undefined" || !document.fonts) return;
      await document.fonts.ready;
    }
    ```

    Notas:
    - Inter 700 é incluído mesmo não sendo no exemplo do RESEARCH — usado em headers de Ambiente (template v2, Plan 04 — `font-weight: 700`).
    - Não importar pesos que não vão ser usados — cada import carrega ~5-15KB de woff2.
    - O check `typeof document === "undefined"` é defensivo (Vitest com jsdom tem `document` mas não tem fonts API completa — o `!document.fonts` cobre isso).
  </action>
  <verify>
    <automated>grep -E "@fontsource/inter/700\.css|export async function ensureFontsReady" src/lib/pdfFonts.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/pdfFonts.ts` existe
    - Contém os 8 imports de @fontsource (inter 300/400/500/600/700 + playfair 400/500/400-italic)
    - Exporta `ensureFontsReady` como `async function` retornando `Promise<void>`
    - Body de `ensureFontsReady` tem o guard `typeof document === "undefined" || !document.fonts`
    - Body chama `await document.fonts.ready`
    - `npm run build` exits 0 (Vite consegue resolver os @fontsource imports)
  </acceptance_criteria>
  <done>Arquivo criado, fontes registradas no bundle, helper exportado.</done>
</task>

<task type="auto">
  <name>Task 3: Criar src/lib/pdfImages.ts (inlineImagensSnapshot — pre-base64 thumbnails)</name>
  <files>src/lib/pdfImages.ts</files>
  <read_first>
    - .planning/phases/05-pdf-redesign/05-RESEARCH.md (Code Example "Pre-converter thumbnails para base64")
    - src/types/orcamento.ts (interfaces Ambiente, ItemLuminaria, ItemPerfil, ItemFitaLED, ItemDriver — campos imagemUrl)
    - src/components/Step3Revisao.tsx:43-57 (padrão imageToBase64 atual — referência mas vamos usar fetch+FileReader, mais robusto pra signed URLs)
  </read_first>
  <action>
    Criar `src/lib/pdfImages.ts` com o conteúdo exato:

    ```typescript
    /**
     * Pre-resolve URLs de thumbnails de produto (luminária / fita / driver / perfil) para
     * data: URLs base64 antes de gerar o PDF.
     *
     * Phase 5 / PDF-01. Resolve Pitfall 2 do RESEARCH:
     * html2canvas + URL externa com CORS frágil (signed URL Supabase) = espaço em branco silencioso.
     * Pre-converter para base64 evita totalmente esse risco.
     */

    import type { Ambiente } from "@/types/orcamento";

    /** Faz fetch de uma URL e retorna data:URL base64. Retorna null em caso de erro. */
    async function urlToBase64(url: string): Promise<string | null> {
      try {
        const res = await fetch(url, { credentials: "omit" });
        if (!res.ok) return null;
        const blob = await res.blob();
        return await new Promise<string>((resolve, reject) => {
          const reader = new FileReader();
          reader.onloadend = () => resolve(reader.result as string);
          reader.onerror = () => reject(new Error("FileReader error"));
          reader.readAsDataURL(blob);
        });
      } catch {
        return null;
      }
    }

    /**
     * Pre-resolve TODOS os imagemUrl de um snapshot Ambiente[] em paralelo.
     * Retorna um deep-clone com imagemUrl trocado por base64 quando o fetch funcionou;
     * mantém URL original quando falhou (template v2 lida graciosamente com URL que vira espaço em branco
     * no fallback — mas a maioria dos casos vai converter com sucesso).
     *
     * Não muta o input. Não fetcha URLs já em data: (idempotente).
     */
    export async function inlineImagensSnapshot(ambientes: Ambiente[]): Promise<Ambiente[]> {
      // Coleta URLs únicas
      const urls = new Map<string, Promise<string | null>>();
      const enqueue = (url?: string) => {
        if (!url || url.startsWith("data:")) return;
        if (!urls.has(url)) urls.set(url, urlToBase64(url));
      };
      for (const amb of ambientes) {
        for (const l of amb.luminarias) enqueue(l.imagemUrl);
        for (const s of amb.sistemas) {
          enqueue(s.fita.imagemUrl);
          enqueue(s.driver.imagemUrl);
          if (s.perfil) enqueue(s.perfil.imagemUrl);
        }
      }

      // Aguarda todos em paralelo
      const resolved = new Map<string, string>();
      await Promise.all(
        Array.from(urls.entries()).map(async ([url, p]) => {
          const v = await p;
          if (v) resolved.set(url, v);
        }),
      );

      // Deep-clone trocando imagemUrl pelos base64 quando disponíveis
      const swap = (url?: string): string | undefined => (url && resolved.get(url)) || url;
      return ambientes.map((amb) => ({
        ...amb,
        luminarias: amb.luminarias.map((l) => ({ ...l, imagemUrl: swap(l.imagemUrl) })),
        sistemas: amb.sistemas.map((s) => ({
          ...s,
          fita: { ...s.fita, imagemUrl: swap(s.fita.imagemUrl) },
          driver: { ...s.driver, imagemUrl: swap(s.driver.imagemUrl) },
          perfil: s.perfil ? { ...s.perfil, imagemUrl: swap(s.perfil.imagemUrl) } : null,
        })),
      }));
    }
    ```

    Notas:
    - `credentials: "omit"` é deliberado — signed URLs do Supabase Storage não dependem de cookies e cookies podem confundir o servidor.
    - Idempotente: se a URL já é `data:...`, é skipped.
    - Não copia ItemLuminaria.imagemUrl quando a fita/driver compartilham — cada item é um objeto diferente, OK.
    - Falhas individuais não abortam — só preserva URL original e segue.
  </action>
  <verify>
    <automated>grep -E "export async function inlineImagensSnapshot|readAsDataURL" src/lib/pdfImages.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/pdfImages.ts` existe
    - Importa `Ambiente` de `@/types/orcamento` (não usa caminho relativo)
    - Exporta `inlineImagensSnapshot` async retornando `Promise<Ambiente[]>`
    - Tem helper interno `urlToBase64` que usa `fetch` + `FileReader.readAsDataURL`
    - `urlToBase64` envolve em try/catch e retorna `null` em erro
    - `inlineImagensSnapshot` itera `luminarias`, `sistemas` (fita + driver + perfil opcional) e enfileira todos os `imagemUrl`
    - Skipa URLs que já começam com `data:`
    - Roda fetch em paralelo com `Promise.all`
    - Retorna deep-clone (não muta input) — verificável por inspeção: usa `ambientes.map(amb => ({ ...amb, luminarias: amb.luminarias.map(l => ({ ...l, imagemUrl: swap(...) })), sistemas: ... }))`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Helper criado, exportado, build limpo.</done>
</task>

</tasks>

<verification>
- Deps instaladas e fixadas em 5.2.8 (Task 1)
- pdfFonts.ts existe com ensureFontsReady (Task 2)
- pdfImages.ts existe com inlineImagensSnapshot (Task 3)
- Build passa em todas
</verification>

<success_criteria>
- 2 deps em package.json
- 2 helpers em src/lib/ prontos para serem importados pelo Plan 04 (template v2) e Plan 05 (router/call sites)
- Build limpo
</success_criteria>

<output>
Após completion, criar `.planning/phases/05-pdf-redesign/05-03-SUMMARY.md` com:
- Versão exata das deps instaladas
- Lista dos 8 .css imports do @fontsource em pdfFonts.ts
- Pseudocódigo de 5 linhas resumindo inlineImagensSnapshot
</output>
