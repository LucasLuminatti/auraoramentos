---
phase: 06-filtros-smoke
plan: 05
type: execute
wave: 5
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - .planning/phases/06-filtros-smoke/06-WRAP-UAT.md
autonomous: false
requirements: [WRAP-01]
must_haves:
  truths:
    - "Existe arquivo .planning/phases/06-filtros-smoke/06-WRAP-UAT.md com 8 itens de smoke (D-09 1..8)"
    - "Cada item tem campos: numeração, descrição, expected, result (pending/passed/failed), observações"
    - "Itens automatizáveis via Playwright MCP foram executados por mim e marcados (com evidência: screenshot ou trecho de log)"
    - "Itens manuais (signup novo, Drive isolado entre 2 contas) ficam pending para Lenny executar e marcar"
    - "Há precondição documentada para item 5 (snapshot legacy): SQL ou identificação do orçamento pré-2026-05-07 com pdf_template_version IS NULL — se nenhum existir, instrução de PATCH para criar"
    - "Critério de fechamento (D-10) está explícito: 0 itens com result=failed que sejam regressão real; cosméticos viram TODO via gsd-add-todo"
    - "Smoke item 8 cobre filtros Phase 6 (Clientes/Produtos/Pedidos isolados + combinação Pedidos)"
  artifacts:
    - path: ".planning/phases/06-filtros-smoke/06-WRAP-UAT.md"
      provides: "Checklist UAT-style do smoke do Marco 1"
      min_lines: 80
      contains: "Smoke #5"
  key_links:
    - from: "WRAP-UAT.md"
      to: "Lenny executa em produção (auraoramentos-kappa.vercel.app)"
      via: "checklist preenchido manualmente após Playwright auto-marcar items 2/3/4/6/8 onde aplicável"
      pattern: "result: \\[pending|passed|failed\\]"
---

<objective>
Materializar WRAP-01: criar `06-WRAP-UAT.md` com checklist de 8 itens (D-09) cobrindo todos os fluxos do Marco 1, executar via Playwright MCP os itens automatizáveis (criar cliente com arquiteto, criar orçamento com PDF v2, importar CSV, filtros Phase 6), deixar pending os itens manuais (signup novo via provider externo, Drive isolado entre 2 contas), e documentar precondição do item 5 (snapshot legacy).

Purpose: Marco 1 fecha quando o smoke passa sem regressão visível (D-10). Este plan cria o artifact + executa o que dá pra automatizar + entrega para Lenny os itens manuais.
Output: arquivo `06-WRAP-UAT.md` com itens automatizados marcados e itens manuais pending.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-filtros-smoke/06-CONTEXT.md
@.planning/phases/06-filtros-smoke/06-01-SUMMARY.md
@.planning/phases/06-filtros-smoke/06-02-SUMMARY.md
@.planning/phases/06-filtros-smoke/06-03-SUMMARY.md
@.planning/phases/06-filtros-smoke/06-04-SUMMARY.md
@.planning/phases/05-pdf-redesign/05-05-PLAN.md
@.planning/phases/05-pdf-redesign/05-05-SUMMARY.md

<interfaces>
<!-- Template UAT-style das phases anteriores (referência: Phase 5 UAT). -->
<!-- Cada item tem: -->
<!--   ## Smoke #N: <descrição curta> -->
<!--   - **Expected:** <comportamento esperado, frase única> -->
<!--   - **Steps:** numerados -->
<!--   - **Result:** [pending|passed|failed] -->
<!--   - **Notes:** observações + evidência (screenshot, log) -->

<!-- Itens (D-09 — copiar verbatim): -->
<!-- 1. Signup novo  → MANUAL (precisa Resend + RLS allowed_users + admin aprovar) -->
<!-- 2. Cliente novo com arquiteto → AUTO via Playwright -->
<!-- 3. Orçamento completo com PDF v2 → AUTO via Playwright -->
<!-- 4. PDF re-emit (orçamento de 3) → AUTO via Playwright -->
<!-- 5. Snapshot legacy → AUTO via Playwright (precondição: existir 1 orçamento pré-2026-05-07 com pdf_template_version IS NULL) -->
<!-- 6. Importar CSV → AUTO via Playwright (Admin > Cadastros > Produtos > ... > ImportMaster ou ImportProdutos) -->
<!-- 7. Drive isolado → MANUAL (precisa 2 contas distintas autenticadas) -->
<!-- 8. Filtros (FIL-01..04) → AUTO via Playwright -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar 06-WRAP-UAT.md com 8 smoke items + precondição legacy snapshot</name>
  <files>.planning/phases/06-filtros-smoke/06-WRAP-UAT.md</files>
  <read_first>
    - .planning/phases/06-filtros-smoke/06-CONTEXT.md §Decisions D-08, D-09, D-10 (escopo + cobertura + critério de fechamento — copiar verbatim)
    - .planning/phases/06-filtros-smoke/06-CONTEXT.md §Specifics (precondição smoke #5)
    - .planning/phases/05-pdf-redesign/05-05-PLAN.md (último UAT da phase anterior — replicar formato)
    - .planning/phases/05-pdf-redesign/05-05-SUMMARY.md (achados PDF v1/v2 — informa o item 5 sobre o que esperar do legacy)
  </read_first>
  <action>
    1. Criar `.planning/phases/06-filtros-smoke/06-WRAP-UAT.md` com a estrutura abaixo:

    ```markdown
    # Phase 6 — WRAP-UAT (Smoke Test Marco 1)

    **Created:** 2026-05-07
    **Executor (manual items):** Lenny
    **Executor (automated items):** Claude (via Playwright MCP)
    **Environment:** produção — https://auraoramentos-kappa.vercel.app
    **Closure criterion (D-10):** 0 items com `result: failed` que sejam regressão real (bug visível). Regressões cosméticas viram TODO via `/gsd-add-todo`.

    ---

    ## Precondição obrigatória — Smoke #5 (Snapshot legacy)

    Smoke #5 verifica que orçamentos PRÉ-Phase 5 (sem `pdf_template_version`) ainda renderizam o PDF v1.
    Antes de executar o smoke, confirmar via SQL que existe pelo menos 1 row em `orcamentos` com:
    - `created_at < '2026-05-07'`
    - `pdf_template_version IS NULL`

    SQL de verificação (rodar no SQL Editor do Supabase):
    ```sql
    select id, created_at, pdf_template_version
    from orcamentos
    where pdf_template_version is null
      and created_at < '2026-05-07'
    order by created_at desc
    limit 5;
    ```

    Se 0 rows: criar 1 manualmente via PATCH (SQL Editor):
    ```sql
    update orcamentos
    set pdf_template_version = null
    where id = '<id-de-um-orçamento-antigo-conhecido>';
    ```
    Anotar o `id` aqui: `_____________________________`

    ---

    ## Smoke #1: Signup novo  [MANUAL — Lenny]

    - **Expected:** Aba anônima → solicitar acesso via fluxo de signup (CPF/telefone/setor obrigatórios) → admin aprova em `allowed_users` → novo user faz signup completo → login OK + redireciona para `/`.
    - **Steps:**
      1. Abrir aba anônima em https://auraoramentos-kappa.vercel.app
      2. Solicitar acesso (`request-access` flow) com email novo
      3. Em outra aba (admin logado), aprovar o email em `allowed_users`
      4. Voltar para aba anônima → /auth → preencher email + senha forte (8+ chars, maiúscula, minúscula, número, especial) + CPF válido + telefone BR + setor (escolher um dos 4 enums)
      5. Confirmar email se Resend exigir (verificar inbox)
      6. Login → redireciona para `/`
    - **Result:** [pending]
    - **Notes:**

    ---

    ## Smoke #2: Cliente novo com arquiteto  [AUTO — Playwright]

    - **Expected:** Form de criar cliente aceita Contato + CPF/CNPJ + Arquiteto via autocomplete (Phase 2). Cliente salva, aparece na lista com arquiteto vinculado.
    - **Steps:**
      1. Login como admin (ou colaborador autorizado).
      2. Navegar `/admin?tab=cadastros&sub=clientes` → "+ Novo Cliente".
      3. Preencher: nome, email, telefone, contato, CPF, e selecionar arquiteto via autocomplete.
      4. Salvar → toast sucesso.
      5. Confirmar que cliente aparece na lista com nome do arquiteto preenchido na coluna Arquiteto.
    - **Result:** [pending — preencher pós-execução Playwright]
    - **Notes:**

    ---

    ## Smoke #3: Orçamento completo + PDF v2  [AUTO — Playwright]

    - **Expected:** Wizard 3 passos cria orçamento com 1+ ambiente, sistema com `local`, luminárias com imagem → Step3 → Gerar PDF → PDF v2 baixa OK (Playfair + Inter, sem 4 caixas, prose final formatado).
    - **Steps:**
      1. Login.
      2. `/` → Step1: selecionar colaborador + tipo "Primeiro Orçamento".
      3. Step2: criar 1 ambiente, adicionar 1 sistema com Local preenchido + 1+ luminária com imagem.
      4. Step3: revisar; clicar "Gerar PDF".
      5. Confirmar download OK e abrir o PDF: tipografia Playfair+Inter, total geral redesenhado, prose final no fim, sem 4 caixas.
    - **Result:** [pending]
    - **Notes:**

    ---

    ## Smoke #4: PDF re-emit do orçamento de #3  [AUTO — Playwright]

    - **Expected:** Abrir orçamento criado em #3 via `/admin/orcamento/:id` → "Re-emitir PDF" → sai v2 idêntico ao do Step3.
    - **Steps:**
      1. Em outra aba: `/admin?tab=pedidos`.
      2. Clicar na linha do orçamento criado em #3.
      3. Em `/admin/orcamento/:id`, clicar "Re-emitir PDF".
      4. PDF baixa; comparar visualmente com o de #3 — devem ser idênticos.
    - **Result:** [pending]
    - **Notes:**

    ---

    ## Smoke #5: Snapshot legacy renderiza PDF v1  [AUTO — Playwright + precondição manual]

    - **Expected:** Orçamento criado antes de 2026-05-07 (com `pdf_template_version IS NULL`) → "Re-emitir PDF" → sai v1 (Outfit + info-grid + total escuro + 4 caixas).
    - **Precondition:** SQL acima confirmou existência (ou criou) 1 orçamento legacy. ID anotado.
    - **Steps:**
      1. `/admin/orcamento/<id-legacy>`.
      2. "Re-emitir PDF".
      3. PDF baixa; abrir e confirmar visualmente: fonte Outfit, info-grid no topo, card escuro do total, 4 caixas (Prazo/Garantia/Pagamento/Observações) presentes.
    - **Result:** [pending]
    - **Notes:**

    ---

    ## Smoke #6: Importar CSV  [AUTO — Playwright]

    - **Expected:** Admin > Preços > Importação > Produtos (CSV) ou Master → upload CSV de teste → preview mostra created/updated/erros → confirmar → produtos aparecem em `/admin?tab=cadastros&sub=produtos`.
    - **Steps:**
      1. Preparar CSV de teste (2 linhas: 1 para criar, 1 com SKU existente para update). Pode usar template baixável da própria UI.
      2. `/admin?tab=precos&sub=importacao` → escolher "Produtos (CSV)".
      3. Upload do CSV.
      4. Preview: confirmar contadores OK (1 created + 1 updated, 0 erros).
      5. Confirmar import.
      6. `/admin?tab=cadastros&sub=produtos` → buscar SKU criado → presente.
    - **Result:** [pending]
    - **Notes:**

    ---

    ## Smoke #7: Drive isolado por colaborador  [MANUAL — Lenny]

    - **Expected:** RLS Phase 4 (D-02 errata: `user_id` em vez de `colaborador_id`) garante que colaborador A não vê arquivos de colaborador B. Admin vê todos.
    - **Steps:**
      1. Login como colaborador A → /drive → upload 1 arquivo "smoke-A.txt".
      2. Sair → login como colaborador B → /drive → confirmar que `smoke-A.txt` NÃO aparece.
      3. Como colaborador B, fazer upload "smoke-B.txt".
      4. Sair → login como admin → /drive → confirmar que VÊ os 2 arquivos.
      5. Limpar (deletar smoke-A e smoke-B no final).
    - **Result:** [pending]
    - **Notes:** Requer 2 contas distintas. Não dá para automatizar trivialmente via Playwright sem config de 2 sessions paralelas.

    ---

    ## Smoke #8: Filtros Phase 6 (FIL-01..04)  [AUTO — Playwright]

    - **Expected:** Filtros funcionam isolados e combinados conforme Plans 02/03/04.
    - **Steps:**
      1. `/admin?tab=cadastros&sub=clientes&arq_clientes=<uuid-arquiteto-X>` → confirmar lista mostra APENAS clientes do arquiteto X.
      2. `/admin?tab=cadastros&sub=clientes&arq_clientes=none` → confirmar lista mostra APENAS clientes sem arquiteto.
      3. `/admin?tab=cadastros&sub=produtos&arq_produtos=<uuid-arquiteto-Y>` → confirmar tabela só mostra produtos do Y; combinar com busca por código → AND-chained.
      4. `/admin?tab=pedidos&arq_pedidos=<uuid>&status_pedidos=enviado&data_de=2026-04-01&data_ate=2026-05-07` → confirmar lista bate com 4 critérios simultâneos.
      5. Em `/admin?tab=pedidos`, abrir Popover de filtros (resize <640px) → confirmar badge contador aparece quando há ao menos 1 filtro ativo, e clicar "Limpar filtros" zera URL.
      6. Bookmark de URL filtrada → abrir em nova aba → estado preservado.
      7. Empty state filtrado: usar UUID inexistente → confirmar mensagens "Nenhum X vinculado a este arquiteto" / "Nenhum pedido bate com os filtros".
    - **Result:** [pending]
    - **Notes:**

    ---

    ## Closure summary

    - **Items passed:** _ / 8
    - **Items failed:** _ / 8
    - **Items pending (manual):** _
    - **Regressões reais encontradas:** [listar — se vazio, "nenhuma"]
    - **Cosméticos virados TODO:** [listar com `gsd-add-todo` IDs]
    - **Marco 1 — fechamento:** [aprovado | bloqueado por falha em #N]

    *Generated by /gsd-execute-phase 06 — Plan 05*
    ```

    2. **Após criar o arquivo, executar Playwright MCP nos itens automatizáveis** (#2, #3, #4, #5 — só a parte de re-emitir, #6, #8):
       - Para cada item, ao final, atualizar `result: [pending]` para `passed` ou `failed` com observações + screenshot path em `Notes`.
       - Para o item 5: rodar antes o SQL de verificação via Supabase MCP (se disponível) ou pedir explicitamente ao Lenny para confirmar/criar a row legacy ANTES de executar o item Playwright.
       - Itens 1 e 7 ficam `pending` (manuais).

    3. **Após execução, atualizar Closure Summary** com contagens reais.

    4. **Reportar a Lenny** ao final desta task: lista de itens marcados como passed por mim, lista de pending para ele executar, eventuais failures encontradas durante Playwright (com proposta de fix ou TODO).
  </action>
  <verify>
    <automated>test -f .planning/phases/06-filtros-smoke/06-WRAP-UAT.md && grep -c "^## Smoke #" .planning/phases/06-filtros-smoke/06-WRAP-UAT.md | grep -q "^8$"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/06-filtros-smoke/06-WRAP-UAT.md` exit 0.
    - `grep -c "^## Smoke #" .planning/phases/06-filtros-smoke/06-WRAP-UAT.md` retorna exatamente 8.
    - `grep "Precondição" .planning/phases/06-filtros-smoke/06-WRAP-UAT.md` retorna match (smoke #5 preflight).
    - `grep "pdf_template_version IS NULL" .planning/phases/06-filtros-smoke/06-WRAP-UAT.md` retorna match.
    - `grep "auraoramentos-kappa.vercel.app" .planning/phases/06-filtros-smoke/06-WRAP-UAT.md` retorna match.
    - `grep "Closure criterion" .planning/phases/06-filtros-smoke/06-WRAP-UAT.md` retorna match (D-10 explícito).
    - Os 6 itens automatizáveis (#2, #3, #4, #5, #6, #8) têm `result: passed` OU `result: failed` (com nota detalhada) — nenhum continua `pending` após a execução Playwright. (Item 5 pode ficar pending SE Lenny não confirmou precondição; nesse caso anotar bloqueio em Notes.)
    - Os 2 itens manuais (#1, #7) permanecem `result: [pending]` com `[MANUAL — Lenny]` na descrição.
    - "Closure summary" tem contagens preenchidas (não fica `_ / 8`).
    - Reportar a Lenny inclui: link do arquivo, items passed por mim, items pending para ele, qualquer falha (com TODO sugerido via `/gsd-add-todo` ou fix proposal).
  </acceptance_criteria>
  <done>
    `06-WRAP-UAT.md` criado, 6 items rodados via Playwright MCP e marcados, 2 items pending para Lenny, Closure summary preenchido, Lenny informado do que falta.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Lenny executa items #1 e #7 e confirma fechamento do Marco 1</name>
  <files>.planning/phases/06-filtros-smoke/06-WRAP-UAT.md</files>
  <what-built>
    - 5 plans da Phase 6 entregues (filtros nas 3 listas + WRAP-UAT).
    - 06-WRAP-UAT.md com 6 items já validados via Playwright MCP por mim.
    - 2 items manuais aguardando: signup novo (#1) e Drive isolado entre 2 contas (#7).
  </what-built>
  <action>
    Lenny executa manualmente os 2 items que não dão para automatizar (#1 signup novo, #7 Drive entre 2 contas distintas), preenche `result:` e `Notes:` no `06-WRAP-UAT.md`, atualiza "Closure summary", e responde via resume-signal indicando aprovação, aprovação-com-TODOs, ou regressão para gap-closure.
  </action>
  <verify>
    <automated>grep -E "^- \*\*Result:\*\* \[pending\]" .planning/phases/06-filtros-smoke/06-WRAP-UAT.md | wc -l | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>
    Items #1 e #7 com `result:` preenchido (passed/failed), Closure summary final atualizado, Marco 1 fechado ou regressão escalada para gap-closure.
  </done>
  <how-to-verify>
    1. Abrir `.planning/phases/06-filtros-smoke/06-WRAP-UAT.md`.
    2. Executar Smoke #1 (signup novo) seguindo os steps; preencher `result:` e `Notes`.
    3. Executar Smoke #7 (Drive isolado entre 2 contas) seguindo os steps; preencher `result:` e `Notes`.
    4. Atualizar "Closure summary" com contagens finais.
    5. Decidir fechamento (D-10): se 0 failures reais → "Marco 1 — fechamento: aprovado". Se houver failures cosméticas → criar TODO via `/gsd-add-todo` e marcar como aprovado mesmo. Se houver regressão real → marcar bloqueado e abrir gap-closure plan via `/gsd-plan-phase 06 --gaps`.
    6. Comitar a versão final do `06-WRAP-UAT.md`.
  </how-to-verify>
  <resume-signal>
    Responder com:
    - "smoke aprovado" → Marco 1 fechado, prosseguir para fase de retrospectiva/closure;
    - "smoke aprovado com TODOs: <lista>" → idem, eu crio os TODOs;
    - "regressão em #N: <descrição>" → eu inicio gap-closure plan.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke executa em produção (auraoramentos-kappa.vercel.app) | Toda interação cria/lê dados reais. |
| Item #5 PATCH SQL em produção | Setar `pdf_template_version = null` em row antiga. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | PATCH SQL em `orcamentos` para preparar smoke #5 | mitigate | Lenny ou eu (via Supabase MCP se disponível) executa o UPDATE com WHERE id explícito. ID é confirmado antes via SELECT. Sem WHERE genérico. |
| T-06-05-02 | Information Disclosure | Smoke #2/#3 cria cliente + orçamento de teste com dados reais | accept | Dado de teste fica em produção; convencionar prefixo "smoke-" no nome do cliente para identificar e limpar depois (TODO opcional). Sem leak externo. |
| T-06-05-03 | Repudiation | Mudanças durante smoke não rastreadas | accept | created_at + colaborador_id já são gravados pelo schema. Sem audit log adicional necessário neste marco. |
| T-06-05-04 | Tampering | Playwright cria rows reais que ficam em prod | mitigate | Smoke #2/#3/#6 cria entidades. Anotar IDs nas Notes do UAT para limpeza opcional. Não bloqueia fechamento. |
</threat_model>

<verification>
1. Arquivo `06-WRAP-UAT.md` criado e válido (8 items, precondição, closure criterion).
2. 6 items automatizáveis executados via Playwright MCP — `result` preenchido em cada um com evidência.
3. Lenny executa items #1 e #7 e atualiza closure summary.
4. Marco 1 fechado se 0 failures reais (D-10).
</verification>

<success_criteria>
- WRAP-01 entregue: smoke executado, sem regressão visível, ou regressões registradas + corrigidas.
- 0 items `failed` reais OU items failed têm gap-closure plan iniciado.
- Marco 1 fechado.
</success_criteria>

<output>
After completion, create `.planning/phases/06-filtros-smoke/06-05-SUMMARY.md` documentando: results finais por item, evidências, decisões de fechamento (TODOs criados, regressões corrigidas inline, etc).
</output>
