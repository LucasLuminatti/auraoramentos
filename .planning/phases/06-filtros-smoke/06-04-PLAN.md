---
phase: 06-filtros-smoke
plan: 04
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - src/pages/Admin.tsx
  - src/components/ClienteFilterAutocomplete.tsx
autonomous: true
requirements: [FIL-03, FIL-04]
must_haves:
  truths:
    - "Em Admin > Pedidos existe uma linha de filtros: [Arquiteto] [Cliente] [Período: De / Até] [Status]"
    - "Filtro Arquiteto refiltra orçamentos via JOIN clientes!inner(arquiteto_id) — sem nova migration, sem coluna em orcamentos"
    - "Filtro Cliente é um autocomplete contra clientes (top 10, debounce 300ms, ilike), retorna UUID; sentinel especial não necessário (apenas selecionar ou limpar)"
    - "Filtros de período usam orcamentos.data: data_de via .gte('data', ...) e data_ate via .lte('data', ...)"
    - "Filtro Status é um Select com options ['Todos', 'rascunho', 'enviado', 'aprovado', 'fechado', 'perdido'] e quando ≠ 'Todos' aplica .eq('status', X)"
    - "Combinações são AND (todos os filtros aplicados simultaneamente)"
    - "URL search params: arq_pedidos, cli_pedidos, data_de, data_ate, status_pedidos — todos opcionais; ausência = sem filtro"
    - "Sentinel `none` em arq_pedidos significa orçamentos cujo cliente.arquiteto_id IS NULL (filtra via clientes!inner com arquiteto_id IS NULL)"
    - "Em mobile (<sm), os filtros adicionais (Cliente, Período, Status) ficam dentro de um Popover com ícone de funil + badge mostrando quantos filtros ativos. Arquiteto sempre visível"
    - "Empty state diferenciado quando há ao menos 1 filtro ativo: 'Nenhum pedido bate com os filtros aplicados' vs 'Nenhum orçamento' atual"
    - "Linha clicável (ADM-01) preservada — filtro não quebra navegação para /admin/orcamento/:id"
  artifacts:
    - path: "src/components/ClienteFilterAutocomplete.tsx"
      provides: "Autocomplete genérico de clientes (analógo a ArquitetoAutocomplete) para filtro em Pedidos"
      min_lines: 60
    - path: "src/pages/Admin.tsx"
      provides: "Bloco de filtros em Pedidos + fetchOrcamentos refatorado com JOIN inner + 4 critérios + empty state"
      contains: "clientes!inner"
  key_links:
    - from: "fetchOrcamentos(filters)"
      to: "supabase.from('orcamentos').select('*, clientes!inner(...)')"
      via: "JOIN inner para filtrar por arquiteto"
      pattern: "clientes!inner"
    - from: "Admin.tsx tab Pedidos"
      to: "ArquitetoAutocomplete (mode='filter') + ClienteFilterAutocomplete + Input type=date + Select status"
      via: "render condicional inline-on-desktop / popover-on-mobile"
      pattern: "<ArquitetoAutocomplete mode=\"filter\""
    - from: "URL params arq_pedidos, cli_pedidos, data_de, data_ate, status_pedidos"
      to: "useSearchParams"
      via: "leitura combinada"
      pattern: "searchParams\\.get\\(['\"](arq_pedidos|cli_pedidos|data_de|data_ate|status_pedidos)['\"]"
---

<objective>
Adicionar filtros combináveis em Admin > Pedidos cobrindo arquiteto, cliente, período (data_de/data_ate), e status — todos AND-chained na query Supabase, sem nova migration. Filtro de arquiteto é feito via JOIN `clientes!inner(arquiteto_id)` (D-11). Layout: 1 linha em desktop, popover com badge contador em mobile (D-06).

Purpose: entrega FIL-03 (lista de pedidos filtrável por arquiteto) + FIL-04 (combinações onde fazem sentido — D-05 selecionou arquiteto + cliente + período + status para Pedidos).
Output: Admin.tsx com bloco de filtros + ClienteFilterAutocomplete novo + fetchOrcamentos refatorado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-filtros-smoke/06-CONTEXT.md
@.planning/phases/06-filtros-smoke/06-01-PLAN.md
@.planning/phases/06-filtros-smoke/06-02-PLAN.md
@.planning/phases/06-filtros-smoke/06-03-PLAN.md
@src/pages/Admin.tsx
@src/components/ArquitetoAutocomplete.tsx

<interfaces>
<!-- Schema relevante (types.ts ~linha 320 — orcamentos): -->
```ts
orcamentos.Row = {
  ambientes: Json; cliente_id: string; colaborador_id: string;
  created_at: string; data: string;  // ISO date string
  id: string; pdf_template_version: number | null; projeto_id: string | null;
  status: string;  // valores observados: rascunho|enviado|aprovado|fechado|perdido
  tipo: string | null; valor: number;
  ...
}
```

<!-- Query atual (Admin.tsx linha 174–179): -->
```tsx
supabase.from("orcamentos")
  .select("*, clientes(nome), colaboradores(nome), projetos(nome)")
  .order("created_at", { ascending: false });
```

<!-- Query nova (D-11 — arquiteto via JOIN inner; cliente/período/status na própria orcamentos): -->
```tsx
let q = supabase.from("orcamentos").select(`
  *,
  clientes!inner(nome, arquiteto_id),
  colaboradores(nome),
  projetos(nome)
`);
if (arqFilter === "none") q = q.is("clientes.arquiteto_id", null);
else if (arqFilter && arqFilter !== "none") q = q.eq("clientes.arquiteto_id", arqFilter);
if (cliFilter) q = q.eq("cliente_id", cliFilter);
if (dataDe)  q = q.gte("data", dataDe);
if (dataAte) q = q.lte("data", dataAte);
if (statusFilter && statusFilter !== "all") q = q.eq("status", statusFilter);
const { data } = await q.order("created_at", { ascending: false });
```

<!-- IMPORTANTE: PostgREST permite filtrar por colunas de tabela embedded com `clientes!inner(...)` + `.eq("clientes.arquiteto_id", ...)`. O `!inner` força INNER JOIN (orçamentos sem cliente são excluídos — comportamento OK pois cliente_id é NOT NULL no schema). -->

<!-- ClienteFilterAutocomplete (componente novo) — analógo ao ArquitetoAutocomplete mas sobre `clientes`, sem opção "Nenhum cliente" (FK NOT NULL): -->
```ts
interface ClienteOption { id: string; nome: string; }
interface Props {
  value: string;
  onSelect: (cli: ClienteOption | null, kind?: 'cliente' | 'all') => void;
  placeholder?: string;
  className?: string;
}
// Sempre mode='filter'-like: prepend [Todos]; sem [Nenhum] (cliente é obrigatório no orçamento).
```

<!-- Status options derivados de Admin.tsx statusLabel (linha 253): -->
```ts
const STATUS_OPTIONS = [
  { value: 'all', label: 'Todos' },
  { value: 'rascunho', label: 'Rascunho' },
  { value: 'enviado', label: 'Enviado' },
  { value: 'aprovado', label: 'Aprovado' },
  { value: 'fechado', label: 'Fechado' },
  { value: 'perdido', label: 'Perdido' },
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar ClienteFilterAutocomplete (autocomplete genérico de clientes para filtros)</name>
  <files>src/components/ClienteFilterAutocomplete.tsx</files>
  <read_first>
    - src/components/ArquitetoAutocomplete.tsx (modelo a replicar — adaptar para tabela `clientes`, omitir [Nenhum arquiteto] equivalente, manter [Todos])
    - .planning/phases/06-filtros-smoke/06-CONTEXT.md §Decisions D-05 (Pedidos ganha filtro de cliente)
    - src/integrations/supabase/types.ts ~linha 200 (schema clientes — campos id, nome)
  </read_first>
  <action>
    1. Criar `src/components/ClienteFilterAutocomplete.tsx` baseado em `ArquitetoAutocomplete.tsx` (estrutura idêntica: useState + debounce 300ms + click-outside + dropdown com botão).
    2. Diferenças em relação ao ArquitetoAutocomplete:
       - Query: `supabase.from("clientes").select("id, nome").order("nome").limit(10)`; quando query > 0: `.ilike("nome", \`%${query}%\`)`.
       - Dropdown: SOMENTE [Todos] no topo (com `ListFilter` icon) seguido da lista de clientes. NÃO incluir "Nenhum cliente" (cliente_id é NOT NULL em orcamentos — não faria sentido).
       - Callback: `onSelect: (cli: ClienteOption | null, kind?: 'cliente' | 'all') => void`.
       - Export default.
    3. Tipos:
       ```tsx
       export interface ClienteOption { id: string; nome: string; }
       interface ClienteFilterAutocompleteProps {
         value: string;
         onSelect: (cli: ClienteOption | null, kind?: 'cliente' | 'all') => void;
         placeholder?: string;
         className?: string;
       }
       ```
    4. Placeholder default: `"Filtrar por cliente..."`.
  </action>
  <verify>
    <automated>cd auraoramentos && npx tsc --noEmit -p tsconfig.app.json && test -f src/components/ClienteFilterAutocomplete.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/ClienteFilterAutocomplete.tsx` exit 0.
    - `grep "from(\"clientes\")" src/components/ClienteFilterAutocomplete.tsx` retorna match.
    - `grep "Todos" src/components/ClienteFilterAutocomplete.tsx` retorna match.
    - `grep "onSelect(null, 'all')" src/components/ClienteFilterAutocomplete.tsx` retorna match.
    - `grep "onSelect(c, 'cliente')" src/components/ClienteFilterAutocomplete.tsx` retorna match.
    - `! grep "Nenhum cliente" src/components/ClienteFilterAutocomplete.tsx` (não inclui sentinel — D-04 só Arquiteto tem 'none').
    - `grep "export interface ClienteOption" src/components/ClienteFilterAutocomplete.tsx` retorna match.
    - `npx tsc --noEmit -p tsconfig.app.json` exit 0.
  </acceptance_criteria>
  <done>
    Componente standalone pronto para a Task 2 importar e usar em Pedidos.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bloco de filtros em Pedidos + fetchOrcamentos com JOIN clientes!inner + URL state + popover mobile</name>
  <files>src/pages/Admin.tsx</files>
  <read_first>
    - src/pages/Admin.tsx (estado pós Plans 02 + 03 — entender padrões existentes de URL params arq_clientes/arq_produtos para replicar)
    - src/integrations/supabase/types.ts linhas 320–392 (schema orcamentos — campos `data`, `status`, `cliente_id`)
    - src/components/ClienteFilterAutocomplete.tsx (criado na Task 1)
    - src/components/ui/select.tsx (verificar se shadcn Select existe — se não, usar `<select>` nativo com classes Tailwind)
    - src/components/ui/popover.tsx (verificar se Popover existe — provável; senão, usar Dialog)
    - .planning/phases/06-filtros-smoke/06-CONTEXT.md §Decisions D-04, D-05, D-06, D-07, D-11 (sentinel none, layout, AND, JOIN inner, sem migration)
  </read_first>
  <action>
    1. **Imports adicionais:**
       ```tsx
       import ArquitetoAutocomplete from "@/components/ArquitetoAutocomplete";
       import ClienteFilterAutocomplete from "@/components/ClienteFilterAutocomplete";
       import { Filter } from "lucide-react";
       // shadcn Select (verificar existência); se ausente, fallback nativo
       import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
       import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover";
       import { Label } from "@/components/ui/label";
       ```
       Se `select` ou `popover` shadcn não existir no projeto, instalar via shadcn CLI (`npx shadcn-ui@latest add select popover`) ANTES de seguir; se shadcn CLI não estiver configurada, usar `<select>` HTML nativo + um Dialog para "popover" mobile (degraded path documentado).

    2. **Ler URL params (junto aos arq_clientes/arq_produtos existentes):**
       ```tsx
       const arqPedidosParam   = searchParams.get("arq_pedidos");
       const cliPedidosParam   = searchParams.get("cli_pedidos");
       const dataDeParam       = searchParams.get("data_de");
       const dataAteParam      = searchParams.get("data_ate");
       const statusPedidosParam = searchParams.get("status_pedidos");
       ```

    3. **State local para nomes (autocomplete value):**
       ```tsx
       const [arqPedidosNome, setArqPedidosNome] = useState("");
       const [cliPedidosNome, setCliPedidosNome] = useState("");
       ```
       + 2 effects sincronizando: `arqPedidosNome` lê de `arquitetosMap[arqPedidosParam]` (igual aos outros plans); `cliPedidosNome` precisa de uma busca pontual quando carrega URL com `?cli_pedidos=<uuid>` — fazer um fetch direto ao montar o componente / quando o param muda:
       ```tsx
       useEffect(() => {
         if (!cliPedidosParam) { setCliPedidosNome(""); return; }
         (async () => {
           const { data } = await supabase.from("clientes").select("nome").eq("id", cliPedidosParam).maybeSingle();
           setCliPedidosNome(data?.nome ?? "");
         })();
       }, [cliPedidosParam]);
       ```

    4. **Refatorar `fetchOrcamentos`** (linha 174):
       ```tsx
       const fetchOrcamentos = async (filters?: {
         arq?: string | null; cli?: string | null;
         dataDe?: string | null; dataAte?: string | null;
         status?: string | null;
       }) => {
         const f = filters ?? {};
         let q = supabase.from("orcamentos").select(`
           *,
           clientes!inner(nome, arquiteto_id),
           colaboradores(nome),
           projetos(nome)
         `);
         if (f.arq === "none") q = q.is("clientes.arquiteto_id", null);
         else if (f.arq && f.arq !== "none") q = q.eq("clientes.arquiteto_id", f.arq);
         if (f.cli) q = q.eq("cliente_id", f.cli);
         if (f.dataDe)  q = q.gte("data", f.dataDe);
         if (f.dataAte) q = q.lte("data", f.dataAte);
         if (f.status && f.status !== "all") q = q.eq("status", f.status);
         const { data, error } = await q.order("created_at", { ascending: false });
         if (error) { toast.error("Erro ao carregar pedidos"); return; }
         setOrcamentos(data || []);
       };
       ```
       NOTA: o `clientes!inner` força INNER JOIN — só retorna orçamentos cujo cliente existe. Como `cliente_id` é NOT NULL no schema, isso não muda o resultado em produção (todo orçamento tem cliente). Aceitar.

    5. **Effect que dispara fetchOrcamentos quando QUALQUER filtro muda:**
       ```tsx
       useEffect(() => {
         fetchOrcamentos({
           arq: arqPedidosParam,
           cli: cliPedidosParam,
           dataDe: dataDeParam,
           dataAte: dataAteParam,
           status: statusPedidosParam,
         });
       }, [arqPedidosParam, cliPedidosParam, dataDeParam, dataAteParam, statusPedidosParam]);
       ```
       Remover a chamada inicial de `fetchOrcamentos()` do useEffect-mount (linha 145).

    6. **Atualizar `EncerrarNegociacaoModal onSuccess` (linha 681):** trocar `onSuccess={fetchOrcamentos}` por `onSuccess={() => fetchOrcamentos({ arq: arqPedidosParam, cli: cliPedidosParam, dataDe: dataDeParam, dataAte: dataAteParam, status: statusPedidosParam })}`.

    7. **Helper genérico para alterar 1 param:**
       ```tsx
       const setUrlParam = (key: string, next: string | null) => {
         const params = new URLSearchParams(searchParams);
         if (next === null || next === "") params.delete(key);
         else params.set(key, next);
         setSearchParams(params, { replace: true });
       };
       ```
       (Pode substituir `setArqClientesParam` e `setArqProdutosParam` introduzidos nos Plans 02/03 por chamadas ao `setUrlParam("arq_clientes", x)` se quiser unificar — opcional, sem regressão.)

    8. **Contador de filtros ativos (badge mobile):**
       ```tsx
       const pedidosFilterCount = [arqPedidosParam, cliPedidosParam, dataDeParam, dataAteParam,
         (statusPedidosParam && statusPedidosParam !== "all") ? statusPedidosParam : null]
         .filter(Boolean).length;
       ```

    9. **STATUS_OPTIONS const:** declarar fora do componente:
       ```tsx
       const STATUS_OPTIONS = [
         { value: "all", label: "Todos" },
         { value: "rascunho", label: "Rascunho" },
         { value: "enviado", label: "Enviado" },
         { value: "aprovado", label: "Aprovado" },
         { value: "fechado", label: "Fechado" },
         { value: "perdido", label: "Perdido" },
       ] as const;
       ```

    10. **UI — bloco de filtros antes da tabela em `<TabsContent value="pedidos">` (linha 537):**
        - Desktop (sm+): 1 linha flex `[Arquiteto] [Cliente] [Período: De / Até] [Status] [Limpar filtros (se algum ativo)]`.
        - Mobile (<sm): só `[Arquiteto]` visível + botão `[Funil + badge count]` que abre Popover com Cliente / Período / Status.

        Estrutura sugerida (adaptar a tokens existentes):
        ```tsx
        <div className="mb-4 space-y-3">
          {/* Desktop */}
          <div className="hidden sm:flex sm:items-end sm:gap-3 sm:flex-wrap">
            <div className="w-56">
              <Label className="text-xs text-muted-foreground">Arquiteto</Label>
              <ArquitetoAutocomplete
                mode="filter"
                value={arqPedidosNome}
                onSelect={(arq, kind) => {
                  if (kind === 'all') { setUrlParam("arq_pedidos", null); setArqPedidosNome(""); }
                  else if (kind === 'none') { setUrlParam("arq_pedidos", "none"); setArqPedidosNome("Nenhum arquiteto"); }
                  else if (arq) { setUrlParam("arq_pedidos", arq.id); setArqPedidosNome(arq.nome); }
                }}
                placeholder="Filtrar por arquiteto..."
              />
            </div>
            <div className="w-56">
              <Label className="text-xs text-muted-foreground">Cliente</Label>
              <ClienteFilterAutocomplete
                value={cliPedidosNome}
                onSelect={(cli, kind) => {
                  if (kind === 'all') { setUrlParam("cli_pedidos", null); setCliPedidosNome(""); }
                  else if (cli) { setUrlParam("cli_pedidos", cli.id); setCliPedidosNome(cli.nome); }
                }}
                placeholder="Filtrar por cliente..."
              />
            </div>
            <div className="w-36">
              <Label className="text-xs text-muted-foreground">De</Label>
              <Input type="date" value={dataDeParam ?? ""} onChange={(e) => setUrlParam("data_de", e.target.value || null)} />
            </div>
            <div className="w-36">
              <Label className="text-xs text-muted-foreground">Até</Label>
              <Input type="date" value={dataAteParam ?? ""} onChange={(e) => setUrlParam("data_ate", e.target.value || null)} />
            </div>
            <div className="w-40">
              <Label className="text-xs text-muted-foreground">Status</Label>
              <Select value={statusPedidosParam ?? "all"} onValueChange={(v) => setUrlParam("status_pedidos", v === "all" ? null : v)}>
                <SelectTrigger><SelectValue /></SelectTrigger>
                <SelectContent>
                  {STATUS_OPTIONS.map(s => <SelectItem key={s.value} value={s.value}>{s.label}</SelectItem>)}
                </SelectContent>
              </Select>
            </div>
            {pedidosFilterCount > 0 && (
              <Button
                variant="ghost"
                size="sm"
                onClick={() => {
                  const p = new URLSearchParams(searchParams);
                  ["arq_pedidos", "cli_pedidos", "data_de", "data_ate", "status_pedidos"].forEach(k => p.delete(k));
                  setSearchParams(p, { replace: true });
                  setArqPedidosNome(""); setCliPedidosNome("");
                }}
              >
                Limpar filtros
              </Button>
            )}
          </div>

          {/* Mobile */}
          <div className="flex sm:hidden items-center gap-2">
            <div className="flex-1">
              <ArquitetoAutocomplete mode="filter" value={arqPedidosNome} onSelect={/* idem desktop */} placeholder="Arquiteto..." />
            </div>
            <Popover>
              <PopoverTrigger asChild>
                <Button variant="outline" size="icon" className="relative">
                  <Filter className="h-4 w-4" />
                  {pedidosFilterCount > 0 && (
                    <span className="absolute -top-1 -right-1 flex h-4 w-4 items-center justify-center rounded-full bg-primary text-[10px] font-semibold text-primary-foreground">
                      {pedidosFilterCount}
                    </span>
                  )}
                </Button>
              </PopoverTrigger>
              <PopoverContent className="w-80 space-y-3">
                {/* Cliente / De / Até / Status reuse — mesma estrutura compactada */}
              </PopoverContent>
            </Popover>
          </div>
        </div>
        ```

    11. **Empty state diferenciado** (linha 585):
        ```tsx
        <TableRow><TableCell colSpan={7} className="text-center text-muted-foreground py-8">
          {pedidosFilterCount > 0
            ? "Nenhum pedido bate com os filtros aplicados"
            : "Nenhum orçamento"}
        </TableCell></TableRow>
        ```

    12. **Verificar que linha clicável (ADM-01) ainda funciona** — não tocar nas linhas 552–582; apenas o cabeçalho/empty state mudam.

    13. **Pipeline obrigatório (D-12):**
        - code-review do diff completo.
        - Playwright MCP cobrindo: (a) URL `?tab=pedidos&arq_pedidos=<uuid>` filtra; (b) URL `?tab=pedidos&arq_pedidos=none` filtra orçamentos cujo cliente.arquiteto_id IS NULL; (c) combinação arq + status=enviado; (d) combinação cliente + período (data_de + data_ate); (e) combinação 4 filtros simultâneos; (f) badge count em mobile (resize viewport <640px); (g) clicar linha vai para /admin/orcamento/:id; (h) "Limpar filtros" zera tudo na URL.
        - Console JS sem erros.
  </action>
  <verify>
    <automated>cd auraoramentos && npx tsc --noEmit -p tsconfig.app.json && npm run lint -- src/pages/Admin.tsx src/components/ClienteFilterAutocomplete.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "arq_pedidos" src/pages/Admin.tsx` retorna >= 3 matches.
    - `grep "cli_pedidos" src/pages/Admin.tsx` retorna >= 3 matches.
    - `grep "data_de" src/pages/Admin.tsx` retorna >= 3 matches.
    - `grep "data_ate" src/pages/Admin.tsx` retorna >= 3 matches.
    - `grep "status_pedidos" src/pages/Admin.tsx` retorna >= 3 matches.
    - `grep "clientes!inner" src/pages/Admin.tsx` retorna match (JOIN inner — D-11).
    - `grep -E "\.is\(['\"]clientes\.arquiteto_id['\"], null\)" src/pages/Admin.tsx` retorna match.
    - `grep -E "\.eq\(['\"]clientes\.arquiteto_id['\"]" src/pages/Admin.tsx` retorna match.
    - `grep -E "\.gte\(['\"]data['\"]" src/pages/Admin.tsx` retorna match.
    - `grep -E "\.lte\(['\"]data['\"]" src/pages/Admin.tsx` retorna match.
    - `grep -E "\.eq\(['\"]status['\"]" src/pages/Admin.tsx` retorna match (filtro status).
    - `grep "Nenhum pedido bate com os filtros aplicados" src/pages/Admin.tsx` retorna match.
    - `grep "Limpar filtros" src/pages/Admin.tsx` retorna match.
    - `grep -c "<ArquitetoAutocomplete[^>]*mode=\"filter\"" src/pages/Admin.tsx` retorna >= 3 (Clientes + Produtos + Pedidos desktop; +1 se duplicado em mobile = 4).
    - `grep "ClienteFilterAutocomplete" src/pages/Admin.tsx` retorna >= 2 matches (import + uso, ou desktop+mobile).
    - `grep "STATUS_OPTIONS" src/pages/Admin.tsx` retorna >= 2 matches.
    - `grep "pedidosFilterCount" src/pages/Admin.tsx` retorna >= 2 matches (declaração + uso badge).
    - Playwright MCP report: 8 cenários (a–h) PASS, sem erros JS.
    - `npx tsc --noEmit -p tsconfig.app.json` exit 0.
    - `npm run lint` exit 0.
  </acceptance_criteria>
  <done>
    Pedidos filtra por arquiteto + cliente + período + status com AND, sem nova migration (JOIN clientes!inner), URL preserva tudo, mobile responsivo via popover, ADM-01 (linha clicável) intacta.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params (5 vetores untrusted) → Supabase query encadeada | arq_pedidos, cli_pedidos, data_de, data_ate, status_pedidos. |
| Filtro arquiteto via JOIN inner em `clientes` | Se RLS de clientes filtrar, JOIN inner exclui orçamentos cujo cliente o admin não veria. |
| Status enum livre (`string` no schema) | Não há CHECK constraint visível; status_pedidos vindo da URL pode ser arbitrário. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | URL `arq_pedidos`/`cli_pedidos` injetado em `.eq()` | mitigate | Supabase JS parametriza. UUID inválido → erro Postgres 22P02 → fetchOrcamentos retorna erro tratado com toast. |
| T-06-04-02 | Tampering | URL `data_de`/`data_ate` em `.gte/.lte('data', X)` | mitigate | Postgres rejeita string não-ISO com erro tipado. Input `<input type="date">` no UI normaliza para ISO. Aceitar que valor manual via URL inválido vire toast. |
| T-06-04-03 | Tampering | URL `status_pedidos` arbitrário | mitigate | Status fora do enum esperado simplesmente não bate com nenhum row → lista vazia. Sem leak. Validar client-side `if (!STATUS_OPTIONS.some(s => s.value === param)) ignore` é melhoria opcional — anotar como follow-up se virar dor. |
| T-06-04-04 | Information Disclosure | JOIN clientes!inner expõe coluna arquiteto_id no payload | accept | `arquiteto_id` já é visível em outras tabs (Cadastros > Clientes). Admin pode ler. RLS de clientes (Phase 4 errata D-02) baseia-se em user_id de cliente_arquivos, não bloqueia leitura de clientes em si. Sem regressão de privacidade. |
| T-06-04-05 | DoS | Refetch a cada onChange dos inputs date | accept | Inputs date só disparam onChange ao confirmar (calendário browser); usuário não consegue spam. Sem debounce extra. |
</threat_model>

<verification>
1. `npx tsc --noEmit` e `npm run lint` passam.
2. Pipeline pre-test (D-12): code-review + Playwright MCP nos 8 cenários listados em action.13.
3. Smoke regressão: Cadastros > Clientes (Plan 02) e Cadastros > Produtos (Plan 03) ainda funcionam; linha clicável de pedido (ADM-01) navega para `/admin/orcamento/:id`; encerrar negociação modal preserva filtros após sucesso.
4. Verificar via Playwright que fetch retorna corretamente quando combinação dá 0 rows (empty state filtrado aparece).
</verification>

<success_criteria>
- FIL-03 entregue: Pedidos filtrável por arquiteto.
- FIL-04 entregue: combinações arquiteto + cliente + período + status, AND.
- ZERO migrations novas (D-11).
- URL preserva todos os filtros + bookmarkável + sobrevive refresh.
- Mobile responsivo (popover + badge).
- Pipeline pre-test executado.
</success_criteria>

<output>
After completion, create `.planning/phases/06-filtros-smoke/06-04-SUMMARY.md` documentando: estrutura final do bloco de filtros, query Supabase com JOIN inner usada, achados Playwright, eventuais ajustes pós-code-review.
</output>
