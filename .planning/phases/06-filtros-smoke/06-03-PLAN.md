---
phase: 06-filtros-smoke
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/pages/Admin.tsx
autonomous: true
requirements: [FIL-02]
must_haves:
  truths:
    - "Em Admin > Cadastros > Produtos existe um filtro por arquiteto no header da tabela, à esquerda do botão '+ Novo Produto', mantendo o input de busca por código/descrição existente"
    - "Selecionar arquiteto X faz a tabela mostrar APENAS variants onde product_variants.arquiteto_id = X (refetch via Supabase, mantendo busca por texto se houver)"
    - "Selecionar 'Nenhum arquiteto' mostra APENAS variants com arquiteto_id IS NULL"
    - "Selecionar 'Todos' / clear → URL sem param, lista volta a mostrar todos"
    - "URL search param `arq_produtos` armazena o estado e sobrevive a refresh"
    - "Empty state diferenciado quando filtro está ativo: 'Nenhum produto vinculado a este arquiteto' vs 'Nenhum produto encontrado' do search atual"
    - "Filtro de arquiteto e busca por código/descrição se combinam (AND-chained na query)"
  artifacts:
    - path: "src/pages/Admin.tsx"
      provides: "Filtro arquiteto na tab Produtos + estado URL `arq_produtos` + fetchProdutos aceitando 2 critérios"
      contains: "arq_produtos"
  key_links:
    - from: "src/pages/Admin.tsx (tab Produtos header)"
      to: "src/components/ArquitetoAutocomplete.tsx (mode='filter')"
      via: "import já existente — nova instância"
      pattern: "<ArquitetoAutocomplete mode=\"filter\""
    - from: "fetchProdutos(search, arqId)"
      to: "supabase.from('product_variants')"
      via: ".or() para search + .eq()/.is() para arquiteto"
      pattern: "\\.eq\\(['\"]arquiteto_id['\"]"
    - from: "URL ?arq_produtos=<uuid|none>"
      to: "useSearchParams"
      via: "leitura"
      pattern: "searchParams\\.get\\(['\"]arq_produtos['\"]\\)"
---

<objective>
Adicionar filtro por arquiteto na lista de Produtos (Admin > Cadastros > Produtos), mesmo padrão do Plan 02 mas combinando com a busca por código/descrição já existente. URL search param `arq_produtos`, sentinel `none`, ausência = "Todos". Refetch Supabase-side com `.or()` (search) AND `.eq()`/`.is()` (arquiteto).

Purpose: entregar FIL-02. Reusa o pattern do Plan 02 + adapta para a tabela `product_variants` que já tem `arquiteto_id`.
Output: Admin.tsx com filtro funcional na tab Produtos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-filtros-smoke/06-CONTEXT.md
@.planning/phases/06-filtros-smoke/06-01-PLAN.md
@.planning/phases/06-filtros-smoke/06-02-PLAN.md
@.planning/phases/06-filtros-smoke/06-02-SUMMARY.md
@src/pages/Admin.tsx

<interfaces>
<!-- Padrão estabelecido pelo Plan 02 — replicar para Produtos -->

```tsx
// fetchClientes virou fetchClientes(arqFilter) — replicar no fetchProdutos:
const fetchProdutos = async (search: string, arqFilter?: string | null) => {
  setLoadingProdutos(true);
  let query = supabase.from("product_variants").select(
    "id, codigo, descricao, nome, preco_tabela, preco_minimo, arquiteto_id, imagem_url, created_at",
    { count: "exact" }
  );
  if (search.trim().length >= 2) {
    query = query.or(`codigo.ilike.%${search}%,descricao.ilike.%${search}%`);
  }
  if (arqFilter === "none") query = query.is("arquiteto_id", null);
  else if (arqFilter && arqFilter !== "none") query = query.eq("arquiteto_id", arqFilter);
  const { data, count } = await query.order("codigo").limit(100);
  setProdutos(data || []);
  setTotalProdutos(count || 0);
  setLoadingProdutos(false);
};
```

<!-- Schema (types.ts ~linha 449 e ~linha 731): product_variants.arquiteto_id: string | null + produtos.arquiteto_id: string | null -->
<!-- Admin.tsx atualmente lê de `product_variants` (linha 159) — não mudar a tabela. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar filtro arquiteto na tab Produtos com URL state arq_produtos + combinar com search existente</name>
  <files>src/pages/Admin.tsx</files>
  <read_first>
    - src/pages/Admin.tsx (estado pós Plan 02 — entender padrão estabelecido para arq_clientes e replicar)
    - .planning/phases/06-filtros-smoke/06-02-SUMMARY.md (pattern documentado)
    - .planning/phases/06-filtros-smoke/06-CONTEXT.md §Decisions D-04 (URL params nomeados por tab; `arq_produtos`)
    - .planning/phases/06-filtros-smoke/06-CONTEXT.md §Specifics (empty state filtrado)
  </read_first>
  <action>
    1. Ler `arq_produtos` do URL (logo após `arqClientesParam` introduzido no Plan 02):
       ```tsx
       const arqProdutosParam = searchParams.get("arq_produtos");
       ```
    2. Adicionar state local para nome:
       ```tsx
       const [arqProdutosNome, setArqProdutosNome] = useState("");
       ```
    3. Effect de sync nome quando param ou arquitetosMap mudam (analógo ao do Plan 02):
       ```tsx
       useEffect(() => {
         if (!arqProdutosParam) { setArqProdutosNome(""); return; }
         if (arqProdutosParam === "none") { setArqProdutosNome("Nenhum arquiteto"); return; }
         setArqProdutosNome(arquitetosMap[arqProdutosParam] || "");
       }, [arqProdutosParam, arquitetosMap]);
       ```
    4. Refatorar `fetchProdutos` (linha 157) para aceitar 2º param `arqFilter`:
       - Manter assinatura backward (chamadas antigas com 1 arg precisam ser atualizadas).
       - Após o `if (search.trim().length >= 2)`, adicionar:
         ```tsx
         if (arqFilter === "none") query = query.is("arquiteto_id", null);
         else if (arqFilter && arqFilter !== "none") query = query.eq("arquiteto_id", arqFilter);
         ```
    5. Atualizar o effect de debounce do search (linha 150):
       ```tsx
       useEffect(() => {
         const timer = setTimeout(() => {
           fetchProdutos(produtoSearch, arqProdutosParam);
         }, 300);
         return () => clearTimeout(timer);
       }, [produtoSearch, arqProdutosParam]);
       ```
       Esse effect agora cobre tanto search quanto arquiteto — remover a chamada inicial `fetchProdutos("")` do useEffect-mount (linha 143) para evitar fetch duplicado, OU deixar e o debounce dispara depois (custo aceitável). Escolher: REMOVER do mount (mantém um único fetch quando a página abre).
    6. Atualizar `onSuccess` dos `<ProdutoEditDialog ...>` (linhas 706 + 713): ambos `onSuccess={() => fetchProdutos(produtoSearch, arqProdutosParam)}`.
    7. Helper para alterar URL (analógo ao do Plan 02):
       ```tsx
       const setArqProdutosParam = (next: string | null) => {
         const params = new URLSearchParams(searchParams);
         if (next === null) params.delete("arq_produtos");
         else params.set("arq_produtos", next);
         setSearchParams(params, { replace: true });
       };
       ```
    8. UI: na tab Produtos (linha 329, dentro de `<div className="space-y-3">`), o header atual (linha 331: `<div className="flex items-center justify-between gap-3">`) já tem search à esquerda + botão à direita. Inserir o filtro entre eles, mantendo flex layout. Substituir bloco por:
       ```tsx
       <div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
         <div className="flex items-center gap-3 flex-1">
           <Search className="h-4 w-4 text-muted-foreground" />
           <Input placeholder="Buscar produto por código ou descrição..." value={produtoSearch} onChange={(e) => setProdutoSearch(e.target.value)} className="max-w-sm" />
         </div>
         <div className="flex items-center gap-3">
           <div className="w-full sm:w-64">
             <ArquitetoAutocomplete
               mode="filter"
               value={arqProdutosNome}
               onSelect={(arq, kind) => {
                 if (kind === 'all') { setArqProdutosParam(null); setArqProdutosNome(""); }
                 else if (kind === 'none') { setArqProdutosParam("none"); setArqProdutosNome("Nenhum arquiteto"); }
                 else if (arq) { setArqProdutosParam(arq.id); setArqProdutosNome(arq.nome); }
               }}
               placeholder="Filtrar por arquiteto..."
             />
           </div>
           <Button onClick={() => setProdutoCreateOpen(true)} className="gap-2 whitespace-nowrap">
             <Plus className="h-4 w-4" /> Novo Produto
           </Button>
         </div>
       </div>
       ```
    9. Empty state (linha 356) diferenciado:
       ```tsx
       <TableRow><TableCell colSpan={6} className="text-center text-muted-foreground py-8">
         {arqProdutosParam
           ? (arqProdutosParam === "none"
               ? (produtoSearch ? "Nenhum produto sem arquiteto bate com a busca" : "Nenhum produto sem arquiteto")
               : (produtoSearch ? "Nenhum produto deste arquiteto bate com a busca" : "Nenhum produto vinculado a este arquiteto"))
           : "Nenhum produto encontrado"}
       </TableCell></TableRow>
       ```
    10. NÃO mexer em outras tabs (Plan 04 cobre Pedidos).
    11. Pipeline obrigatório (D-12): code-review + Playwright MCP testando: (a) filtrar por arquiteto X sem search; (b) filtrar por arquiteto X + digitar SKU AU0 — combinação AND; (c) "Nenhum arquiteto"; (d) clear (Todos); (e) URL `?tab=cadastros&sub=produtos&arq_produtos=<uuid>` carrega filtrado; (f) editar produto e confirmar `onSuccess` preserva o filtro.
  </action>
  <verify>
    <automated>cd auraoramentos && npx tsc --noEmit -p tsconfig.app.json && npm run lint -- src/pages/Admin.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "arq_produtos" src/pages/Admin.tsx` retorna >= 3 matches.
    - `grep -c "<ArquitetoAutocomplete[^>]*mode=\"filter\"" src/pages/Admin.tsx` retorna 2 (Clientes + Produtos).
    - `grep "Nenhum produto vinculado a este arquiteto" src/pages/Admin.tsx` retorna match.
    - `grep "Nenhum produto sem arquiteto" src/pages/Admin.tsx` retorna match.
    - `grep "Nenhum produto encontrado" src/pages/Admin.tsx` retorna match (preservado).
    - `grep -E "fetchProdutos\(produtoSearch, arqProdutosParam\)" src/pages/Admin.tsx` retorna >= 2 matches (effect + onSuccess).
    - `grep "fetchProdutos = async (search: string, arqFilter" src/pages/Admin.tsx` retorna match (assinatura nova).
    - `npx tsc --noEmit -p tsconfig.app.json` exit code 0.
    - `npm run lint` exit code 0.
    - Playwright MCP: confirma combinação search + arquiteto retorna intersecção; URL preservada; sem regressão em Cadastros > Clientes (Plan 02 ainda funcional).
  </acceptance_criteria>
  <done>
    Admin > Cadastros > Produtos filtra por arquiteto + busca textual em conjunto, URL state preservado, sem regressão.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params `arq_produtos` + `produtoSearch` (untrusted) → Supabase | Mesmo modelo do Plan 02; agora com 2 vetores combinados. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | URL `arq_produtos` injetado em `.eq("arquiteto_id", X)` | mitigate | Supabase JS parametriza; Postgres rejeita UUID inválido com erro tipado → toast/lista vazia. Sem SQL injection. |
| T-06-03-02 | Tampering | `produtoSearch` interpolado em `.or(\`codigo.ilike.%${search}%,...\`)` — pré-existente, não foi introduzido por este plan | accept | Já existia desde Phase 3 (linha 161). Supabase `.or()` aceita string PostgREST e escapa internamente; admin é usuário confiável (RLS + role check). Fora do escopo deste plan refatorar. Anotar como follow-up se virar dor. |
| T-06-03-03 | Information Disclosure | Combinar arquiteto + search vazaria produto restrito | accept | RLS de `product_variants` (Phase 3) já regula leitura. Filtro só refina visíveis. |
</threat_model>

<verification>
1. `npx tsc --noEmit` e `npm run lint` passam.
2. Pipeline pre-test (D-12): code-review + Playwright MCP cobrindo combinação search+arquiteto + URL preservation.
3. Smoke regressão: edit/create produto via dialog continua funcional; tab Clientes (Plan 02) ainda funciona.
</verification>

<success_criteria>
- FIL-02 entregue: Produtos filtrável por arquiteto, combinável com busca textual.
- URL `?tab=cadastros&sub=produtos&arq_produtos=<uuid>` funcional + bookmark-friendly.
- Empty states diferenciados (4 combinações: filtro+search / filtro só / search só / nenhum).
- Pipeline pre-test executado.
</success_criteria>

<output>
After completion, create `.planning/phases/06-filtros-smoke/06-03-SUMMARY.md` documentando: pattern de combinação search+filter (model para Pedidos no Plan 04), achados Playwright.
</output>
