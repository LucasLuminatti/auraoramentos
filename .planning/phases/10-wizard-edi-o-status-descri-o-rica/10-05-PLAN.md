---
phase: 10-wizard-edi-o-status-descri-o-rica
plan: 05
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - src/lib/produtoDescricao.ts
  - src/lib/__tests__/produtoDescricao.test.ts
  - src/components/Step3Revisao.tsx
  - src/lib/pdfTemplates/v2.ts
  - .planning/phases/10-wizard-edi-o-status-descri-o-rica/10-05-SMOKE.md
  - .planning/phases/10-wizard-edi-o-status-descri-o-rica/10-05-SUMMARY.md
autonomous: false
requirements: [WIZ-05]
must_haves:
  truths:
    - "Existe `src/lib/produtoDescricao.ts` exportando `construirDescricaoRica` puro com formato `Nome | <Tk>K | <W>W | IRC <irc> | <nicho>`"
    - "Atributo ausente é suprimido (não aparece `—`); snapshot sem atributos retorna nome cru"
    - "Step 3 (Step3Revisao.tsx) renderiza a descrição rica via construirDescricaoRica para luminárias, fitas, perfis e drivers — usando atributos resolvidos via TanStack Query batch lookup por código"
    - "PDF v2 (src/lib/pdfTemplates/v2.ts) renderiza a descrição rica nas 4 row functions (rowLuminaria, rowFita, rowPerfil, rowDriver) — recebe atributosMap como argumento"
    - "Step 2 (AmbienteCard.tsx) e PDF v1 (pdfTemplates/v1.ts) PERMANECEM intactos — D-21: continuam com nome cru"
    - "Lookup TanStack Query: chave estável via useMemo([ambientes]), batch único WHERE codigo IN (...), staleTime 5min"
    - "Snapshot antigo sem campos atributos: re-resolve via batch lookup; se produto sumiu, usa item.descricao puro (D-22)"
    - "Testes unitários do construirDescricaoRica passam: caso completo, atributos parciais, snapshot antigo"
    - "Smoke manual em prod cobre 5 fluxos da Phase 10 (D-30): editar Step 3, marcar perdido, reabrir rascunho, snapshot antigo, marcar aprovado (irreversível)"
  artifacts:
    - path: "src/lib/produtoDescricao.ts"
      provides: "Função pura construirDescricaoRica + interface AtributosRicos"
      contains: "export function construirDescricaoRica"
    - path: "src/lib/__tests__/produtoDescricao.test.ts"
      provides: "Testes unitários Vitest (formato completo, suppress-on-missing, nome cru)"
      contains: "describe"
    - path: ".planning/phases/10-wizard-edi-o-status-descri-o-rica/10-05-SMOKE.md"
      provides: "Checklist smoke manual em prod cobrindo os 5 fluxos D-30"
      contains: "Smoke 1"
  key_links:
    - from: "Step3Revisao.tsx"
      to: "supabase product_variants atributos + potencia_watts"
      via: "useQuery batch WHERE codigo IN (...)"
      pattern: "useQuery"
    - from: "Step3Revisao.tsx"
      to: "construirDescricaoRica"
      via: "import + render em luminária/fita/perfil/driver desc"
      pattern: "construirDescricaoRica\\("
    - from: "pdfTemplates/v2.ts row functions"
      to: "construirDescricaoRica"
      via: "atributosMap arg + import"
      pattern: "construirDescricaoRica\\("
    - from: "gerarPdfHtml.ts (caller do v2)"
      to: "atributosMap"
      via: "argumento adicional na chamada do v2 builder"
      pattern: "atributosMap"
---

<objective>
Implementar **WIZ-05** (descrição rica) — criar builder puro `construirDescricaoRica`, integrar no Step 3 e PDF v2 com batch lookup TanStack Query por código (D-23) e fallback para snapshot puro quando produto sumiu do master (D-22). AmbienteCard (Step 2) e PDF v1 ficam intactos (D-21).

Após a integração, executar o **smoke manual em prod** cobrindo os 5 fluxos críticos da Phase 10 (D-30): (a) editar qty/preço no Step 3 + gerar PDF; (b) marcar rascunho como `perdido`; (c) reabrir rascunho via Pedidos com prefill; (d) abrir orçamento antigo (pré-v1.1) sem crash; (e) marcar rascunho como `aprovado` confirmando irreversibilidade.

Purpose: WIZ-05 success criterion 5 do ROADMAP + closure técnico da Phase 10 (smoke = porta para Plan 10-VERIFICATION).

Output: 2 arquivos novos + 2 arquivos modificados + smoke checklist + summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-CONTEXT.md
@.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-RESEARCH.md
@.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-03-SUMMARY.md
@.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-04-SUMMARY.md
@src/lib/productAttributes.ts
@src/lib/pdfTemplates/v2.ts
@src/lib/gerarPdfHtml.ts
@src/components/Step3Revisao.tsx
@src/components/PrecosBatch.tsx

<interfaces>
<!-- Schema do JSONB `atributos` em product_variants (src/lib/productAttributes.ts:70-95): -->
<!--   { -->
<!--     instalacao, tipo, cor_peca, temperatura_k, cor_iluminacao, lumens, eficiencia, -->
<!--     irc, angulo, tensao_raw, fator_potencia, frequencia, material, cabo, -->
<!--     driver_incluso, dimensao, nicho, variante, familia, pagina, observacoes -->
<!--   } -->
<!-- Phase 10 consome apenas: temperatura_k, irc, nicho (D-24). potencia_watts é coluna typed, NÃO JSONB. -->

<!-- Builder esperado (D-19): -->
<!--   `Nome | <temperatura_k>K | <potencia_watts>W | IRC <irc> | <nicho>` -->
<!--   Atributo ausente = suprimido (não mostra `—`), não placeholder (D-20). -->

<!-- TanStack Query 5.x setup (já em App.tsx, primeiro uso real na Phase 10): -->
<!--   import { useQuery } from "@tanstack/react-query"; -->
<!--   const { data } = useQuery({ queryKey: [...], queryFn: async () => {...}, staleTime: 5*60*1000, enabled: ... }); -->

<!-- Pitfall 3 do RESEARCH: queryKey precisa de array estável (useMemo) — recreate dispara refetch. -->

<!-- pdfTemplates/v2.ts row functions (l.82-172): -->
<!--   rowLuminaria(item: ItemLuminaria): string         ← linha 91: <div class="desc-name">${esc(item.descricao || "—")}</div> -->
<!--   rowFita(sis: SistemaIluminacao): string           ← linha 115: <div class="desc-name"><span class="comp-tag comp-fita">Fita</span> ${esc(sis.fita.descricao || "—")}</div> -->
<!--   rowPerfil(sis: SistemaIluminacao): string         ← linha 140: idem com sis.perfil.descricao -->
<!--   rowDriver(sis: SistemaIluminacao): string         ← linha 163: idem com sis.driver.descricao -->
<!-- Estas 4 funções precisam ganhar argumento `atributosMap` e usar construirDescricaoRica. -->

<!-- gerarPdfHtml.ts é o caller que invoca o builder v2. Phase 10 adiciona um STEP NOVO: -->
<!--   1. Antes de chamar v2 builder, fazer um SELECT WHERE codigo IN (...) para popular atributosMap. -->
<!--   2. Passar atributosMap como argumento para o v2 builder. -->
<!--   3. v2 builder passa para cada row function. -->

<!-- AmbienteCard (Step 2) e PDF v1 NAO MUDAM (D-21). Não tocar src/components/AmbienteCard.tsx nem src/lib/pdfTemplates/v1.ts (se houver). -->

<!-- Aviso de worktree (lição Phase 09): git add caminhos explícitos. -->

<!-- Plan 10-05 é a porta final da Phase 10 — antes do checkpoint:human-action precisa: -->
<!--   - 10-01 mergeado (RLS aplicada em prod) -->
<!--   - 10-02 mergeado (types sync) -->
<!--   - 10-03 mergeado (inputs inline) -->
<!--   - 10-04 mergeado (status dropdown + reabrir rascunho) -->
<!-- Plan 10-05 depends_on=[10-03] no frontmatter (porque mexe em Step3Revisao.tsx — single owner) mas o SMOKE manual final pressupõe TODAS as plans entregues. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar builder puro construirDescricaoRica + testes Vitest</name>
  <files>src/lib/produtoDescricao.ts, src/lib/__tests__/produtoDescricao.test.ts</files>
  <behavior>
    - Função pura, sem side effects
    - Input: { nome: string, atributos?: Record<string, unknown> | null, potenciaWatts?: number | null }
    - Output: string no formato `Nome | <Tk>K | <W>W | IRC <irc> | <nicho>` com partes ausentes suprimidas
    - Snapshot antigo (sem atributos nem potenciaWatts) retorna o nome puro
    - Tests:
      - Test 1 (caso completo): { nome: "Plafon X", atributos: { temperatura_k: 4000, irc: 90, nicho: "embutir" }, potenciaWatts: 12 } → "Plafon X | 4000K | 12W | IRC 90 | embutir"
      - Test 2 (parcial — só temperatura): retorna "Plafon X | 4000K"
      - Test 3 (snapshot antigo): { nome: "Plafon X" } → "Plafon X"
      - Test 4 (atributos null): { nome: "Plafon X", atributos: null } → "Plafon X"
      - Test 5 (potencia=0): NÃO suprime (0W é um valor válido para algumas peças, e D-24 só fala em "atributo ausente"); valor 0 não-null retorna "Plafon X | 0W". Justificativa: distinguir "ausência" de "valor zero" via null vs 0 — usar `!= null` (loose null/undefined check)
      - Test 6 (string vazia em atributo): trata como ausente — `if (v != null && v !== "")` para campos string
  </behavior>
  <action>
**(a) Criar `src/lib/produtoDescricao.ts`:**

```typescript
/**
 * Phase 10 — WIZ-05 (D-19, D-20, D-24)
 *
 * Builder puro de descrição rica de produto para Step 3 + PDF v2.
 * Formato: `Nome | <temperatura_k>K | <potencia_watts>W | IRC <irc> | <nicho>`
 *
 * Atributo ausente é suprimido (não aparece `—` nem placeholder). Distinção entre
 * "ausência" (null/undefined/string vazia) e "valor zero" — apenas o primeiro é suprimido.
 *
 * Step 2 (AmbienteCard) e PDF v1 NÃO consomem este builder — mantêm descrição crua (D-21).
 */

export interface AtributosRicos {
  nome: string;
  atributos?: Record<string, unknown> | null;
  potenciaWatts?: number | null;
}

function asNumberOrNull(v: unknown): number | null {
  if (v == null || v === "") return null;
  const n = typeof v === "number" ? v : parseFloat(String(v));
  return Number.isFinite(n) ? n : null;
}

function asStringOrNull(v: unknown): string | null {
  if (v == null) return null;
  const s = String(v).trim();
  return s.length > 0 ? s : null;
}

/**
 * Constrói a descrição rica suprimindo atributos ausentes.
 *
 * @example
 *   construirDescricaoRica({ nome: "Plafon X", atributos: { temperatura_k: 4000, irc: 90, nicho: "embutir" }, potenciaWatts: 12 })
 *   // → "Plafon X | 4000K | 12W | IRC 90 | embutir"
 *
 *   construirDescricaoRica({ nome: "Plafon X" })
 *   // → "Plafon X"
 */
export function construirDescricaoRica({ nome, atributos, potenciaWatts }: AtributosRicos): string {
  const partes: string[] = [nome.trim()];

  const tempK = asNumberOrNull(atributos?.temperatura_k);
  if (tempK != null) partes.push(`${tempK}K`);

  if (potenciaWatts != null) partes.push(`${potenciaWatts}W`);

  const irc = asNumberOrNull(atributos?.irc);
  if (irc != null) partes.push(`IRC ${irc}`);

  const nicho = asStringOrNull(atributos?.nicho);
  if (nicho != null) partes.push(nicho);

  return partes.join(" | ");
}
```

**(b) Criar `src/lib/__tests__/produtoDescricao.test.ts`:**

```typescript
import { describe, it, expect } from "vitest";
import { construirDescricaoRica } from "@/lib/produtoDescricao";

describe("construirDescricaoRica", () => {
  it("formato completo: nome + temperatura + potência + IRC + nicho", () => {
    const result = construirDescricaoRica({
      nome: "Plafon X",
      atributos: { temperatura_k: 4000, irc: 90, nicho: "embutir" },
      potenciaWatts: 12,
    });
    expect(result).toBe("Plafon X | 4000K | 12W | IRC 90 | embutir");
  });

  it("parcial: só nome + temperatura_k", () => {
    const result = construirDescricaoRica({
      nome: "Plafon X",
      atributos: { temperatura_k: 4000 },
    });
    expect(result).toBe("Plafon X | 4000K");
  });

  it("snapshot antigo: só nome (sem atributos)", () => {
    const result = construirDescricaoRica({ nome: "Plafon X" });
    expect(result).toBe("Plafon X");
  });

  it("atributos explicitamente null: retorna só o nome", () => {
    const result = construirDescricaoRica({ nome: "Plafon X", atributos: null });
    expect(result).toBe("Plafon X");
  });

  it("potencia=0 NÃO é suprimida (0W é valor válido, distinto de ausente)", () => {
    const result = construirDescricaoRica({ nome: "Plafon X", potenciaWatts: 0 });
    expect(result).toBe("Plafon X | 0W");
  });

  it("string vazia em nicho é tratada como ausente", () => {
    const result = construirDescricaoRica({
      nome: "Plafon X",
      atributos: { nicho: "" },
    });
    expect(result).toBe("Plafon X");
  });

  it("potencia null + atributos sem irc: suprime W e IRC", () => {
    const result = construirDescricaoRica({
      nome: "Plafon X",
      atributos: { temperatura_k: 3000, nicho: "sobrepor" },
      potenciaWatts: null,
    });
    expect(result).toBe("Plafon X | 3000K | sobrepor");
  });

  it("atributos com valores stringificados (vindo de JSONB Postgres): coerce para number", () => {
    const result = construirDescricaoRica({
      nome: "Plafon X",
      atributos: { temperatura_k: "4000", irc: "90" },
      potenciaWatts: 12,
    });
    expect(result).toBe("Plafon X | 4000K | 12W | IRC 90");
  });
});
```

**(c) Validar:**
```bash
npm run test -- --run src/lib/__tests__/produtoDescricao.test.ts
```
Esperado: 8 tests pass.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path 'src/lib/produtoDescricao.ts')) { Write-Error 'produtoDescricao.ts missing'; exit 1 }; if (-not (Test-Path 'src/lib/__tests__/produtoDescricao.test.ts')) { Write-Error 'test file missing'; exit 1 }; $src = Get-Content -Raw 'src/lib/produtoDescricao.ts'; if ($src -notmatch 'export function construirDescricaoRica' -or $src -notmatch 'partes.push\\(`\\$\\{tempK\\}K`\\)' -or $src -notmatch 'partes.push\\(`\\$\\{potenciaWatts\\}W`\\)' -or $src -notmatch 'partes.push\\(`IRC \\$\\{irc\\}`\\)') { Write-Error 'builder shape incomplete'; exit 1 }; cd auraoramentos; npm run test -- --run src/lib/__tests__/produtoDescricao.test.ts 2>&1 | Out-Null; if ($LASTEXITCODE -ne 0) { Write-Error 'tests failed'; exit 1 }; Write-Host 'OK: builder + tests green'"</automated>
  </verify>
  <done>`src/lib/produtoDescricao.ts` exporta `construirDescricaoRica` puro com a forma especificada. `src/lib/__tests__/produtoDescricao.test.ts` tem 8 testes (formato completo, parcial, snapshot antigo, null, potencia=0, string vazia, parcial, JSONB stringificado) e passa.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integrar batch lookup TanStack Query + construirDescricaoRica em Step3Revisao</name>
  <files>src/components/Step3Revisao.tsx</files>
  <behavior>
    - useMemo collect de todos os códigos distintos das luminárias/fitas/perfis/drivers
    - useQuery faz SELECT WHERE codigo IN (...) único, retorna map por código (atributosMap)
    - staleTime 5min, enabled=allCodigos.length>0
    - QueryKey estável (useMemo de allCodigos)
    - Render: descrição rica via construirDescricaoRica({ nome: item.descricao, atributos: atributosMap[item.codigo]?.atributos, potenciaWatts: atributosMap[item.codigo]?.potencia_watts ?? item.potencia_watts })
    - Fallback: se atributosMap[codigo] = undefined (produto sumiu), construirDescricaoRica ainda recebe `{ nome: item.descricao }` → retorna nome cru (D-22)
  </behavior>
  <action>
**(a) Adicionar imports em `src/components/Step3Revisao.tsx`:**

```typescript
import { useQuery } from "@tanstack/react-query";
import { construirDescricaoRica } from "@/lib/produtoDescricao";
```

**(b) Adicionar useMemo + useQuery dentro do componente (próximo aos outros useMemo, antes do return JSX):**

```typescript
// WIZ-05 (D-23): coleta de códigos distintos para batch lookup
const allCodigos = useMemo(() => {
  const set = new Set<string>();
  for (const amb of ambientes) {
    for (const l of amb.luminarias) if (l.codigo) set.add(l.codigo);
    for (const sis of amb.sistemas) {
      if (sis.fita?.codigo) set.add(sis.fita.codigo);
      if (sis.driver?.codigo) set.add(sis.driver.codigo);
      if (sis.perfil?.codigo) set.add(sis.perfil.codigo);
    }
  }
  return Array.from(set).sort(); // sort fixa ordem para estabilidade do queryKey
}, [ambientes]);

// WIZ-05 (D-22, D-23): re-resolução de atributos por código
const { data: atributosMap = {} } = useQuery({
  queryKey: ["produtoAtributos", allCodigos],
  queryFn: async () => {
    if (allCodigos.length === 0) return {};
    const { data, error } = await supabase
      .from("product_variants")
      .select("codigo, atributos, potencia_watts")
      .in("codigo", allCodigos);
    if (error || !data) return {};
    const map: Record<string, { atributos: Record<string, unknown> | null; potencia_watts: number | null }> = {};
    for (const row of data) {
      if (row.codigo) {
        map[row.codigo] = {
          atributos: (row.atributos as Record<string, unknown> | null) ?? null,
          potencia_watts: row.potencia_watts ?? null,
        };
      }
    }
    return map;
  },
  staleTime: 5 * 60 * 1000,
  enabled: allCodigos.length > 0,
});

// Helper local para resolver descrição rica de qualquer item com codigo
const descricaoRica = (codigo: string, descricaoSnapshot: string, potenciaSnapshot?: number | null): string => {
  const lookup = atributosMap[codigo];
  return construirDescricaoRica({
    nome: descricaoSnapshot || codigo || "—",
    atributos: lookup?.atributos ?? null,
    potenciaWatts: lookup?.potencia_watts ?? potenciaSnapshot ?? null,
  });
};
```

**(c) Substituir as referências a `.descricao` cru nas TableCells:**

Localizar as 4 TableCells de descrição (linhas aprox 389, 427, 437, 447 — pós-Plan-10-03):

- Luminária (l.389 — antes do plan 10-03 era `{item.descricao}`):
  ```tsx
  <TableCell>{descricaoRica(item.codigo, item.descricao, (item as any).potencia_watts)}</TableCell>
  ```

- Fita (l.427):
  ```tsx
  <TableCell>{descricaoRica(sis.fita.codigo, sis.fita.descricao)}</TableCell>
  ```

- Perfil (l.437):
  ```tsx
  <TableCell>{descricaoRica(sis.perfil.codigo, sis.perfil.descricao)}</TableCell>
  ```

- Driver (l.447):
  ```tsx
  <TableCell>{descricaoRica(sis.driver.codigo, sis.driver.descricao)}</TableCell>
  ```

**(d) Build + test:**
```bash
npm run build
npm run test
```

NOTAS:
- `item.potencia_watts` em luminária pode ou não existir no snapshot (depende da versão); fallback `(item as any).potencia_watts` é tolerante. Se o lookup encontrar o produto no master, a `potencia_watts` da row no banco prevalece (mais atualizada que snapshot).
- Para fita/perfil/driver não passamos potência do snapshot — eles têm `wm` (fita) ou `potencia` (driver) que são campos diferentes; o `potenciaWatts` neste builder é o atributo "potência elétrica em W" e geralmente só faz sentido para luminária. Mas o builder suprime quando null, então não dá problema.
- `as any` é tolerado (tsconfig: strict: false) e segue padrão do projeto para snapshots heterogêneos.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw 'src/components/Step3Revisao.tsx'; $checks = @('import \\{ useQuery \\} from \"@tanstack/react-query\"', 'import \\{ construirDescricaoRica \\} from \"@/lib/produtoDescricao\"', 'const allCodigos = useMemo', '\\.from\\(\"product_variants\"\\)', 'select\\(\"codigo, atributos, potencia_watts\"\\)', 'descricaoRica\\(item.codigo', 'descricaoRica\\(sis.fita.codigo', 'descricaoRica\\(sis.driver.codigo', 'staleTime: 5 \\* 60 \\* 1000'); foreach ($p in $checks) { if ($c -notmatch $p) { Write-Error \"missing: $p\"; exit 1 } }; cd auraoramentos; npm run build 2>&1 | Out-Null; if ($LASTEXITCODE -ne 0) { Write-Error 'build failed'; exit 1 }; npm run test -- --run 2>&1 | Out-Null; if ($LASTEXITCODE -ne 0) { Write-Error 'tests failed'; exit 1 }; Write-Host 'OK: Step3 descrição rica integrated + build green'"</automated>
  </verify>
  <done>Step3Revisao tem useMemo allCodigos (estável), useQuery batch lookup com staleTime 5min, helper descricaoRica, e as 4 TableCells de descrição (luminária/fita/perfil/driver) usam o helper. Build + tests verde.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Integrar construirDescricaoRica em pdfTemplates/v2.ts + propagar atributosMap a partir de gerarPdfHtml.ts</name>
  <files>src/lib/pdfTemplates/v2.ts, src/lib/gerarPdfHtml.ts</files>
  <behavior>
    - v2.ts: as 4 row functions (rowLuminaria, rowFita, rowPerfil, rowDriver) ganham parâmetro atributosMap; renderizam construirDescricaoRica na `<div class="desc-name">` em vez de item.descricao cru
    - PDF v1 (se existir) permanece intacto
    - gerarPdfHtml.ts (router): antes de chamar o builder v2, executa o mesmo batch lookup feito no Step3Revisao para popular atributosMap; passa como argumento ao v2 builder
  </behavior>
  <action>
**(a) Editar `src/lib/pdfTemplates/v2.ts` — adicionar `atributosMap` como argumento e usar construirDescricaoRica:**

No topo, adicionar import:
```typescript
import { construirDescricaoRica } from "@/lib/produtoDescricao";
```

Definir o tipo do map (perto dos outros tipos no arquivo):
```typescript
export type AtributosMap = Record<string, { atributos: Record<string, unknown> | null; potencia_watts: number | null }>;
```

**Modificar as 4 row functions para aceitar `atributosMap`:**

- `function rowLuminaria(item: ItemLuminaria, atributosMap: AtributosMap): string`
  - Substituir `${esc(item.descricao || "—")}` na linha `<div class="desc-name">...</div>` (l.91) por:
    ```javascript
    ${esc(construirDescricaoRica({
      nome: item.descricao || "—",
      atributos: atributosMap[item.codigo]?.atributos ?? null,
      potenciaWatts: atributosMap[item.codigo]?.potencia_watts ?? item.potencia_watts ?? null,
    }))}
    ```

- `function rowFita(sis: SistemaIluminacao, atributosMap: AtributosMap): string` — mesma transformação aplicada em `${esc(sis.fita.descricao || "—")}` (l.115), usando `sis.fita.codigo` e `sis.fita.descricao`. potenciaWatts default null (fita usa W/m, não potência absoluta).

- `function rowPerfil(sis: SistemaIluminacao, atributosMap: AtributosMap): string` — substituir `${esc(sis.perfil.descricao || "—")}` (l.140) usando `sis.perfil.codigo`.

- `function rowDriver(sis: SistemaIluminacao, atributosMap: AtributosMap): string` — substituir `${esc(sis.driver.descricao || "—")}` (l.163) usando `sis.driver.codigo`.

**Propagar atributosMap nos callers internos:**

A função `blocoSistema(sis, indexNoLocal)` (l.175) chama `rowFita(sis)` / `rowPerfil(sis)` / `rowDriver(sis)`. Ajustar para aceitar e propagar `atributosMap`:
```typescript
function blocoSistema(sis: SistemaIluminacao, indexNoLocal: number, atributosMap: AtributosMap): string {
  // ... usa rowFita(sis, atributosMap), rowPerfil(sis, atributosMap), rowDriver(sis, atributosMap)
}
```

E o renderizador de ambiente / orçamento (a função top-level exportada do v2.ts — geralmente `gerarOrcamentoHtmlV2` ou similar) deve receber `atributosMap` como argumento e propagar para os helpers.

**(b) Editar `src/lib/gerarPdfHtml.ts` — fazer o batch lookup antes de chamar v2 builder:**

Localizar onde v2 é invocado (provavelmente um switch sobre `pdf_template_version`). Antes da chamada do v2 builder:

```typescript
// WIZ-05 (D-23): batch lookup de atributos por código para descrição rica no PDF v2
async function buildAtributosMap(ambientes: Ambiente[]): Promise<AtributosMap> {
  const codigos = new Set<string>();
  for (const amb of ambientes) {
    for (const l of amb.luminarias) if (l.codigo) codigos.add(l.codigo);
    for (const sis of amb.sistemas) {
      if (sis.fita?.codigo) codigos.add(sis.fita.codigo);
      if (sis.driver?.codigo) codigos.add(sis.driver.codigo);
      if (sis.perfil?.codigo) codigos.add(sis.perfil.codigo);
    }
  }
  if (codigos.size === 0) return {};
  const { data, error } = await supabase
    .from("product_variants")
    .select("codigo, atributos, potencia_watts")
    .in("codigo", Array.from(codigos));
  if (error || !data) return {};
  const map: AtributosMap = {};
  for (const row of data) {
    if (row.codigo) {
      map[row.codigo] = {
        atributos: (row.atributos as Record<string, unknown> | null) ?? null,
        potencia_watts: row.potencia_watts ?? null,
      };
    }
  }
  return map;
}
```

E na linha de chamada do v2 (algo como `gerarOrcamentoHtmlV2(...)`), prepender com:
```typescript
const atributosMap = await buildAtributosMap(orcamento.ambientes);
// ... gerarOrcamentoHtmlV2(orcamento, ..., atributosMap)
```

**Importante:**
- PDF v1 (se houver) **não muda** — D-21 mantém PDF v1 com descrição crua.
- `gerarOrcamentoHtml` (router) só faz o lookup quando o template é v2 — economiza query desnecessária em casos legacy.

**(c) Build + test:**
```bash
npm run build
npm run test
```
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$v2 = Get-Content -Raw 'src/lib/pdfTemplates/v2.ts'; if ($v2 -notmatch 'import \\{ construirDescricaoRica \\}') { Write-Error 'v2: import missing'; exit 1 }; if ($v2 -notmatch 'AtributosMap') { Write-Error 'v2: AtributosMap type missing'; exit 1 }; if (($v2 | Select-String 'construirDescricaoRica\\(' -AllMatches).Matches.Count -lt 4) { Write-Error 'v2: construirDescricaoRica usage count < 4 (need 4: lum/fita/perfil/driver)'; exit 1 }; $caller = Get-Content -Raw 'src/lib/gerarPdfHtml.ts'; if ($caller -notmatch 'product_variants') { Write-Error 'gerarPdfHtml: batch lookup missing'; exit 1 }; cd auraoramentos; npm run build 2>&1 | Out-Null; if ($LASTEXITCODE -ne 0) { Write-Error 'build failed'; exit 1 }; npm run test -- --run 2>&1 | Out-Null; if ($LASTEXITCODE -ne 0) { Write-Error 'tests failed'; exit 1 }; Write-Host 'OK: v2 + caller integrated + build green'"</automated>
  </verify>
  <done>v2.ts: as 4 row functions usam construirDescricaoRica para a descrição; blocoSistema e função top-level propagam atributosMap. gerarPdfHtml.ts: buildAtributosMap async helper + chamada inserida antes do v2 builder. v1 intacto. Build + test verde.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Criar checklist de smoke manual em prod (10-05-SMOKE.md) cobrindo os 5 fluxos D-30</name>
  <files>.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-05-SMOKE.md</files>
  <action>
Criar `10-05-SMOKE.md` com a estrutura abaixo. Este arquivo será preenchido pelo executor da Task 5 (checkpoint humano com Lenny pilotando o browser).

```markdown
# 10-05 Smoke — Phase 10 Production Validation

**Phase:** 10-wizard-edi-o-status-descri-o-rica
**Target:** https://orcamentosaura.com.br (prod)
**Cobertura:** 5 fluxos D-30 do CONTEXT.md
**Executor:** Lenny + Claude (Playwright MCP se possível, manual se não)

---  <!-- HR -->

## Pré-flight (antes de começar)

- [ ] Plan 10-01 mergeado (RLS UPDATE em prod aplicada + verificada via 10-01-PUSH-LOG.md POST-PUSH)
- [ ] Plan 10-02 mergeado (TS sync + EncerrarNegociacaoModal deletado)
- [ ] Plan 10-03 mergeado (inputs inline Step3)
- [ ] Plan 10-04 mergeado (status dropdown + reabrir rascunho)
- [ ] Plan 10-05 mergeado (descrição rica)
- [ ] Build local exit 0 (`npm run build`)
- [ ] Deploy Vercel concluído (último commit aparece em https://vercel.com/...)
- [ ] Login em prod com conta Lenny + outra conta colab (se quiser exercitar permissões — opcional)

---  <!-- HR -->

## Smoke 1 — Edição inline qty + preço no Step 3 (WIZ-01 + WIZ-02)

**Setup:** Criar um rascunho novo via wizard, do zero. Ir até o Step 3.

**Steps:**
1. Editar quantidade de uma luminária — digitar valor diferente e dar Tab/Enter
2. Confirmar que o subtotal da linha atualiza
3. Editar preço unitário da mesma luminária para um valor abaixo do preço mínimo
4. Confirmar que aparece o badge de violação (ícone laranja/triângulo) e o botão "Solicitar exceção"
5. Editar preço unitário acima do mínimo
6. Confirmar que badge de violação some
7. Gerar PDF
8. Abrir PDF gerado — confirmar valores editados aparecem (não os originais)

**Esperado:** Recalcs visíveis após blur/Enter. Violação detectada quando < precoMinimo. PDF reflete edits.

**Observed:** _(preencher)_

**Status:** PASS / FAIL

---  <!-- HR -->

## Smoke 2 — Marcar rascunho como "perdido" (WIZ-04, transição livre)

**Setup:** Usar o rascunho criado em Smoke 1 (já existe na tab Pedidos).

**Steps:**
1. Ir em /admin?tab=pedidos
2. Localizar o orçamento criado em Smoke 1
3. Na coluna Status, abrir o dropdown
4. Selecionar "Perdido"
5. NÃO deve aparecer AlertDialog (transição livre)
6. Confirmar toast.success "Status atualizado para perdido"
7. Confirmar badge muda para vermelho

**Esperado:** Mudança instantânea, sem confirmação.

**Observed:** _(preencher)_

**Status:** PASS / FAIL

---  <!-- HR -->

## Smoke 3 — Reabrir rascunho (WIZ-03, prefill + edit + save)

**Setup:** Criar um novo rascunho (cliente Y, ambientes, sistemas) e SAIR DO WIZARD sem gerar PDF. Status fica `rascunho`.

**Steps:**
1. Ir em /admin?tab=pedidos
2. Localizar o orçamento rascunho recém-criado
3. Hover sobre a linha → confirmar tooltip "Continuar este rascunho"
4. Clicar na linha (em qualquer lugar fora do dropdown)
5. Confirmar redirect para "/" (Index) e que o wizard abre no **Step 1** com o cliente/projeto/tipo pré-preenchidos
6. Avançar para Step 2 → confirmar ambientes/sistemas pré-preenchidos
7. Avançar para Step 3 → editar preço de algum item
8. Gerar PDF
9. Voltar para /admin?tab=pedidos → confirmar que **NÃO há orçamento duplicado** (mesmo id, agora com valor atualizado)

**Esperado:** Prefill completo no Step 1, edits persistem no mesmo registro, sem duplicata.

**Observed:** _(preencher)_

**Status:** PASS / FAIL

---  <!-- HR -->

## Smoke 4 — Snapshot antigo (pré-v1.1) renderiza sem crash

**Setup:** Localizar um orçamento antigo em prod (criado antes de 2026-05-14) — o JSONB `ambientes` provavelmente NÃO tem os campos de descrição rica.

**Steps:**
1. Ir em /admin?tab=pedidos
2. Localizar orçamento antigo (data anterior a 2026-05-14 ou `pdf_template_version=1`)
3. Se status='rascunho' → clicar pra reabrir (Smoke 3 caminho)
4. Se status != 'rascunho' → clicar pra abrir a página de detalhe `/admin/orcamento/:id`
5. Console DevTools: verificar zero erros JS
6. Se reabriu o wizard: avançar até Step 3 → confirmar que a descrição **aparece como nome cru** (não há crash, não há "undefined K", não há `—`)
7. Se há produto no snapshot que não existe mais no master atual: confirmar que a linha ainda renderiza com snapshot puro

**Esperado:** Sem crash JS; descrição aparece em formato compatível (rica se o produto existe no master atual, crua se snapshot antigo sem dados ou produto sumiu).

**Observed:** _(preencher)_

**Status:** PASS / FAIL

---  <!-- HR -->

## Smoke 5 — Marcar como "aprovado" (WIZ-04, one-way irreversível)

**Setup:** Criar (ou reusar) um rascunho novo simples.

**Steps:**
1. Ir em /admin?tab=pedidos
2. Localizar o orçamento
3. Na coluna Status, abrir o dropdown
4. Selecionar "Aprovado"
5. Confirmar que aparece **AlertDialog "Marcar como aprovado?"** com texto "Esta ação é irreversível..."
6. Clicar "Cancelar" — confirmar que status **NÃO muda**
7. Repetir: selecionar "Aprovado" no dropdown
8. Clicar "Confirmar"
9. Confirmar toast.success
10. Confirmar badge muda para verde
11. **Tentar reverter:** abrir o dropdown novamente — deve estar **disabled** (ou ao tentar trocar, retornar erro de RLS)
12. Confirmação server-side via Supabase MCP:
    ```sql
    SELECT id, status FROM public.orcamentos WHERE id = '<id-do-orcamento>';
    -- esperado: status = 'aprovado'
    ```
13. Tentar UPDATE direto via SQL (caso queira testar RLS):
    ```sql
    UPDATE public.orcamentos SET status = 'perdido' WHERE id = '<id-do-orcamento>' RETURNING id, status;
    -- esperado (como admin Lenny): 0 rows updated (RLS Plan 10-01 bloqueia)
    ```

**Esperado:** AlertDialog aparece para aprovado; cancelar não muda nada; confirmar grava; dropdown disabled após; RLS server-side bloqueia reverter mesmo via SQL direto.

**Observed:** _(preencher)_

**Status:** PASS / FAIL

---  <!-- HR -->

## Summary

| Smoke | Cobertura | Status |
|-------|-----------|--------|
| 1 — Edit qty+preço Step 3 | WIZ-01 + WIZ-02 | |
| 2 — Marcar perdido | WIZ-04 (transição livre) | |
| 3 — Reabrir rascunho | WIZ-03 | |
| 4 — Snapshot antigo | WIZ-05 (D-22 backward-compat) | |
| 5 — Marcar aprovado | WIZ-04 (one-way D-16) | |

**Overall:** _(N/5 PASS)_

**Bugs encontrados:** _(listar com gravidade alta/média/baixa; alta = bloqueia closure da phase)_
```

NÃO executar smoke ainda — preencher OBSERVED só na Task 5 (checkpoint humano).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path '.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-05-SMOKE.md')) { Write-Error 'SMOKE.md missing'; exit 1 }; $c = Get-Content -Raw '.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-05-SMOKE.md'; $smokes = @('Smoke 1', 'Smoke 2', 'Smoke 3', 'Smoke 4', 'Smoke 5'); foreach ($s in $smokes) { if ($c -notmatch $s) { Write-Error \"missing $s\"; exit 1 } }; if ($c -notmatch 'WIZ-01' -or $c -notmatch 'WIZ-05' -or $c -notmatch 'Pré-flight') { Write-Error 'sections incomplete'; exit 1 }; Write-Host 'OK: smoke checklist created'"</automated>
  </verify>
  <done>`10-05-SMOKE.md` existe cobrindo os 5 fluxos D-30 com pré-flight checks, steps numerados, esperados, slot Observed em branco, e tabela de Summary. Pronto para Task 5 preencher.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Executar smoke manual em prod e preencher 10-05-SMOKE.md</name>
  <files>.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-05-SMOKE.md</files>
  <what-built>
Todas as 4 plans anteriores mergeadas + Plan 10-05 tasks 1-4 entregues (builder + integração Step3 + integração PDF v2 + smoke checklist). Falta exercitar os 5 fluxos em produção real e preencher os campos Observed/Status do `10-05-SMOKE.md`.
  </what-built>
  <how-to-verify>
**Lenny, este é o checkpoint final da Phase 10 — smoke em prod.**

Pré-requisitos:
1. Confirmar que TODAS as plans 10-01 a 10-04 estão em prod (deploy Vercel concluído após o último merge)
2. Confirmar `npm run build` local exit 0 + `npm run test` passa
3. Confirmar via Supabase MCP que as policies da Plan 10-01 estão ativas em prod (snapshot POST-PUSH)

Execução (caminhos possíveis):
- **A:** Playwright MCP automatizando os 5 fluxos no browser real (preferido — Lenny acompanha a tela)
- **B:** Lenny pilota o browser manualmente, Claude observa via screenshots/relatos e preenche o SMOKE.md
- **C:** Híbrido — Playwright para os fluxos 1, 2, 5 (form interaction direto), manual para o 3, 4 (cenários precisam de orçamento "antigo" real)

Para CADA smoke do checklist (1-5):
1. Executar os steps numerados
2. Comparar com o "Esperado"
3. Capturar payload literal no campo "Observed:" (ex: console output, status da row, screenshot path)
4. Marcar `Status: PASS` ou `Status: FAIL`
5. Se FAIL: anotar bug com gravidade (alta/média/baixa)

Final do smoke:
- Preencher tabela Summary com N/5 PASS
- Se algum FAIL alta: criar plan de hotfix (10-06-PLAN ou retorno ao plan original) antes de fechar a phase
- Se todos PASS ou apenas FAILs baixa/média: marcar Phase 10 como elegível para `/gsd-verify-work` no próximo step

**Resume signal:** Digite "smoke ok N/5" (ex: "smoke ok 5/5" ou "smoke ok 4/5 com FAIL baixa em descricao rica de snapshot raro") ou "FAIL bloqueante: <descrição>" se houver bug alta-gravidade.
  </how-to-verify>
  <resume-signal>Digite "smoke ok N/5" para fechar (com nota opcional de FAILs não-bloqueantes), ou "FAIL bloqueante: bug" para abrir hotfix antes de fechar.</resume-signal>
  <action>Checkpoint humano — Lenny pilota o browser (manual ou via Playwright MCP), Claude observa, ambos preenchem 10-05-SMOKE.md com Observed/Status para cada um dos 5 smokes; ao final preenchem o Summary com N/5 PASS.</action>
  <verify><automated>powershell -NoProfile -Command "Get-Content .planning/phases/10-wizard-edi-o-status-descri-o-rica/10-05-SMOKE.md | Select-String Status:.*PASS,Status:.*FAIL"</automated></verify>
  <done>10-05-SMOKE.md tem os 5 smokes com Status PASS ou FAIL preenchido; tabela Summary marca Overall N/5; bugs encontrados (se houver) catalogados por gravidade; phase pronta para /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| product_variants (master) → orcamentos.ambientes (snapshot) | Snapshot é cópia congelada do master no momento da criação; ao re-renderizar, podemos OPCIONALMENTE re-resolver o master para enriquecer display (read-only enrich, sem reescrita) |
| Build TS → PDF runtime | construirDescricaoRica é pura — sem dependência de DB; segura em browser e em server-side (caso PDF migre) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-19 | Tampering (data integrity) | Reescrever snapshot `orcamentos.ambientes` com atributos ricos (perde fidelidade histórica) | mitigate | D-22 explícito: NÃO reescrever. construirDescricaoRica é só read-time. Snapshot original fica intacto. |
| T-10-20 | DoS (UI) | N queries 1-por-item (em vez de batch) ao renderizar Step 3 | mitigate | useMemo + useQuery batch único `WHERE codigo IN (...)` com staleTime 5min. Pitfall 3 do RESEARCH (queryKey estável). |
| T-10-21 | Information Disclosure | construirDescricaoRica expor atributos sensíveis | accept | Atributos consumidos (temperatura_k, irc, nicho, potencia_watts) são dados técnicos não-PII. RLS de SELECT em product_variants já controla acesso (admin/colab vê tudo no master — comportamento atual desde v1.0). |
| T-10-22 | Tampering | Atributo malicioso (ex: temperatura_k = "<script>") via JSONB ataque | mitigate | PDF v2 já passa por `esc()` (escape HTML, vide v2.ts l.91). Step 3 renderiza como text node (React escapes auto). Builder é puro string concat. |
| T-10-23 | DoS | Lookup do PDF v2 (gerarPdfHtml.buildAtributosMap) bloqueia geração do PDF se rede lenta | accept | Query é único batch; mesmo com 100 códigos é fast. Worst-case: PDF demora 1-2s a mais. Se virar problema, vira phase futura com cache server-side. |
</threat_model>

<verification>
- [ ] `src/lib/produtoDescricao.ts` existe e exporta builder puro
- [ ] `src/lib/__tests__/produtoDescricao.test.ts` tem 8 testes e passa
- [ ] Step3Revisao usa useQuery batch + descricaoRica helper nas 4 TableCells
- [ ] pdfTemplates/v2.ts: 4 row functions aceitam atributosMap e usam construirDescricaoRica
- [ ] gerarPdfHtml.ts: buildAtributosMap chamado antes do v2 builder
- [ ] AmbienteCard (Step 2) NÃO modificado (D-21)
- [ ] PDF v1 (se existir) NÃO modificado (D-21)
- [ ] `10-05-SMOKE.md` existe com checklist dos 5 fluxos
- [ ] `npm run build` + `npm run test` exit 0
- [ ] Smoke manual em prod executado e Status preenchido (Task 5)
</verification>

<success_criteria>
1. `construirDescricaoRica({ nome, atributos, potenciaWatts })` é função pura testada (8 testes pass) que retorna `Nome | <Tk>K | <W>W | IRC <irc> | <nicho>` com partes ausentes suprimidas
2. Step 3 renderiza descrição rica para luminárias/fitas/perfis/drivers via batch lookup TanStack Query (1 query única, staleTime 5min)
3. PDF v2 renderiza descrição rica nas 4 row functions, recebe atributosMap via argumento; gerarPdfHtml.ts faz o batch lookup async antes da geração
4. Snapshot antigo (sem campos atributos): re-resolve via batch lookup; se produto sumiu do master, fallback para snapshot puro (nome cru) — D-22 preservado
5. AmbienteCard (Step 2) + PDF v1 intactos — D-21
6. Smoke manual em prod cobrindo os 5 fluxos D-30: registrado em `10-05-SMOKE.md` com PASS/FAIL por smoke
</success_criteria>

<output>
Após completion, criar `.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-05-SUMMARY.md` listando:
- Arquivos novos (2): produtoDescricao.ts + test
- Arquivos modificados (2): Step3Revisao.tsx + pdfTemplates/v2.ts + gerarPdfHtml.ts
- Confirmação AmbienteCard + PDF v1 NÃO tocados
- Resultado do smoke (N/5)
- Bugs encontrados (se houver)
- Decisões aplicadas: D-19, D-20, D-21, D-22, D-23, D-24, D-30
- Próximo passo: `/gsd-verify-work 10` para verificação da phase
</output>
