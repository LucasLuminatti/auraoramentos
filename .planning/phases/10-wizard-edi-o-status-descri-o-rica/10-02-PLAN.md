---
phase: 10-wizard-edi-o-status-descri-o-rica
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - src/types/orcamento.ts
  - src/integrations/supabase/types.ts
  - src/components/AdminDashboard.tsx
  - src/components/ClienteList.tsx
  - src/components/EncerrarNegociacaoModal.tsx
  - src/pages/OrcamentoDetalhe.tsx
  - .planning/phases/10-wizard-edi-o-status-descri-o-rica/10-02-SUMMARY.md
autonomous: true
requirements: [WIZ-04]
must_haves:
  truths:
    - "Tipo `StatusOrcamento` em `src/types/orcamento.ts` é o union `'rascunho' | 'aprovado' | 'perdido' | 'pendente'` (refletindo a CHECK constraint Phase 7)"
    - "Nenhuma ocorrência do string literal `'fechado'` ou `\"fechado\"` permanece em `src/` (search exaustivo retorna 0 matches)"
    - "Arquivo `src/components/EncerrarNegociacaoModal.tsx` foi REMOVIDO (D-31) e nenhum import dele permanece"
    - "`npm run build` exit 0 (TypeScript compila sem erros após o rename de tipo)"
    - "Tipo gerado em `src/integrations/supabase/types.ts` para `orcamentos.status` reflete os 4 valores do enum (regenerado via supabase CLI)"
  artifacts:
    - path: "src/types/orcamento.ts"
      provides: "StatusOrcamento union com 4 valores (rascunho/aprovado/perdido/pendente)"
      contains: "type StatusOrcamento ="
    - path: "src/integrations/supabase/types.ts"
      provides: "Tipo auto-gerado refletindo o schema atual do banco"
      contains: "orcamentos"
  key_links:
    - from: "AdminDashboard, Admin, ClienteList, OrcamentoDetalhe"
      to: "StatusOrcamento"
      via: "switch cases / filter predicates"
      pattern: "(rascunho|aprovado|perdido|pendente)"
    - from: "EncerrarNegociacaoModal (DELETED)"
      to: "(nothing — file removed, imports removed)"
      via: "delete"
      pattern: "EncerrarNegociacaoModal"
---

<objective>
Sincronizar TypeScript com a realidade do schema (Phase 7 já trocou `'fechado'` → `'aprovado'` em prod e adicionou `'pendente'`). Atualizar o tipo `StatusOrcamento` (D-25), regenerar `supabase/types.ts` (D-26), eliminar as 11 ocorrências hardcoded de `'fechado'` em 5 arquivos (D-27, D-33), e remover completamente `EncerrarNegociacaoModal` (D-31 — componente bugado que grava `'fechado'` quebrando a CHECK constraint).

Purpose: WIZ-04 cleanup obrigatório — sem este plan o app continua referenciando um valor que não existe mais no banco, e o modal de "encerrar negociação" continua quebrado em prod (bug ativo). Após este plan, qualquer função que toca status tem o tipo correto e o dropdown da Plan 10-04 pode ser construído com base sólida.

Output: 5 arquivos atualizados + 1 arquivo deletado + types regenerados + build verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-CONTEXT.md
@.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-RESEARCH.md
@.planning/phases/07-schema-prep-v1-1/07-CONTEXT.md
@src/types/orcamento.ts
@src/pages/Admin.tsx
@src/components/AdminDashboard.tsx
@src/components/ClienteList.tsx
@src/components/EncerrarNegociacaoModal.tsx
@src/pages/OrcamentoDetalhe.tsx

<interfaces>
<!-- Tipo atual (src/types/orcamento.ts:109) — DESATUALIZADO desde Phase 7: -->
<!--   export type StatusOrcamento = 'rascunho' | 'fechado' | 'perdido'; -->

<!-- Tipo correto (que este plan grava): -->
<!--   export type StatusOrcamento = 'rascunho' | 'aprovado' | 'perdido' | 'pendente'; -->

<!-- 11 ocorrências hardcoded de "fechado" confirmadas via grep: -->
<!--   src/pages/Admin.tsx:56     STATUS_OPTIONS array: { value: "fechado", label: "Fechado" } -->
<!--   src/pages/Admin.tsx:430    statusLabel switch case "fechado": return "Fechado" -->
<!--   src/pages/Admin.tsx:440    statusClass switch case "fechado": return "bg-green-100 text-green-800" -->
<!--   src/components/AdminDashboard.tsx:60   .filter((o) => o.status === "fechado") — variável local "fechados" -->
<!--   src/components/AdminDashboard.tsx:109  .filter((o) => o.status === "fechado") — soma de receita efetiva -->
<!--   src/components/AdminDashboard.tsx:138  fechado: inRange.filter((o) => o.status === "fechado").reduce(...) — dado do BarChart -->
<!--   src/components/AdminDashboard.tsx:210  Tooltip formatter — label "Fechado" -->
<!--   src/components/AdminDashboard.tsx:211  Legend formatter — label "Fechado" -->
<!--   src/components/AdminDashboard.tsx:213  <Bar dataKey="fechado" fill="hsl(142, 71%, 45%)" -->
<!--   src/components/ClienteList.tsx:145  statusLabel case "fechado" -->
<!--   src/components/ClienteList.tsx:155  statusClass case "fechado" -->
<!--   src/components/EncerrarNegociacaoModal.tsx:46  status: resultado === "ganho" ? "fechado" : "perdido"  ← componente vai ser DELETADO -->
<!--   src/pages/OrcamentoDetalhe.tsx:67   statusLabel case "fechado" -->
<!--   src/pages/OrcamentoDetalhe.tsx:77   statusClass case "fechado" -->

<!-- Atenção: Admin.tsx é também tocado pelo Plan 10-04 (wave 3) para o dropdown novo. Este plan (10-02, wave 2) toca apenas: -->
<!--   - STATUS_OPTIONS array (l.51-58): remover 'enviado' e 'fechado', manter rascunho/aprovado/perdido/pendente + Todos -->
<!--   - statusLabel switch (l.425-434): remover 'enviado' e 'fechado', adicionar 'pendente' -->
<!--   - statusClass switch (l.436-443): remover 'enviado' e 'fechado', adicionar 'pendente' (cor âmbar) e 'aprovado' (verde) -->
<!--   - canEncerrar (l.446) + setEncerrarOpen/setEncerrarOrcId state + import de EncerrarNegociacaoModal + uso dele: REMOVER tudo, pois D-31 deleta o modal -->
<!--   - Botão Flag inline (l.1015-1027): REMOVER pois ele invoca o modal deletado — coluna "Ações" fica vazia para a Plan 10-04 popular com o dropdown -->

<!-- Aviso de worktree (lição de Phase 09): NUNCA usar `git add -A` cego (apaga files de outros worktrees). -->
<!-- Usar sempre `git add <caminho-explícito>` quando commitar este plan. -->

<!-- Regen de types: -->
<!--   npx supabase gen types typescript --project-id jkewlaezvrbuicmncqbj > src/integrations/supabase/types.ts -->
<!--   (CLI já autenticado desde Phase 7 — apply_migration MCP usa o mesmo backend) -->
<!--   Se CLI não disponível, alternativa: editar manualmente a linha do `status` em orcamentos no types.ts -->
<!--   para refletir o union 'rascunho' | 'aprovado' | 'perdido' | 'pendente' — vide o pattern do enum já existente no arquivo. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar StatusOrcamento type + regenerar types.ts do Supabase</name>
  <files>src/types/orcamento.ts, src/integrations/supabase/types.ts</files>
  <action>
**(a) `src/types/orcamento.ts` linha 109:**

Trocar:
```typescript
export type StatusOrcamento = 'rascunho' | 'fechado' | 'perdido';
```
por:
```typescript
export type StatusOrcamento = 'rascunho' | 'aprovado' | 'perdido' | 'pendente';
```

Mantém o JSDoc style do projeto (se houver linha de comentário acima, atualizar texto para "Status do orçamento — alinhado com CHECK constraint da Phase 7 (D-25)").

**(b) `src/integrations/supabase/types.ts`:**

Regenerar via CLI (preferido):
```
npx supabase gen types typescript --project-id jkewlaezvrbuicmncqbj > src/integrations/supabase/types.ts
```

Se o CLI não estiver disponível no ambiente, **fallback manual**: localizar a definição de `orcamentos` no arquivo, encontrar a propriedade `status` (aparece em `Row`, `Insert`, `Update`) e garantir que o tipo dela seja `string` (PostgREST não exporta CHECK constraints como union — então geralmente fica `string`). Se já estiver como `string`, nenhuma edição manual é necessária no types.ts. **Caso o tipo seja outra coisa (ex: literal de 'fechado'), substituir por `string` manualmente.**

Importante: o `types.ts` é auto-gerado; preferência absoluta pelo CLI. Diff esperado após regen: pode haver pequenas mudanças em outras tabelas (ex: novos campos da Phase 8) — isso é OK e deve ser commitado junto.

Após este task: rodar `npm run build` localmente — se quebrar por causa de algum import inesperado, é sinal de que alguma outra parte do código usa `'fechado'` via inferência de tipo. Continuar tasks 2-4 antes de tentar `npm run build` final na Task 5.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$content = Get-Content -Raw 'src/types/orcamento.ts'; if ($content -notmatch \"export type StatusOrcamento = 'rascunho' \| 'aprovado' \| 'perdido' \| 'pendente'\") { Write-Error 'StatusOrcamento type not updated'; exit 1 }; if ($content -match \"'fechado'\") { Write-Error 'fechado still in orcamento.ts'; exit 1 }; if (-not (Test-Path 'src/integrations/supabase/types.ts')) { Write-Error 'types.ts missing'; exit 1 }; Write-Host 'OK: StatusOrcamento updated'"</automated>
  </verify>
  <done>`StatusOrcamento` em `src/types/orcamento.ts` reflete o union dos 4 valores; `src/integrations/supabase/types.ts` regenerado (ou validado se já estava como `string`). Zero ocorrência de `'fechado'` em `src/types/orcamento.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: Substituir todas as 11 ocorrências hardcoded de "fechado" em AdminDashboard/ClienteList/OrcamentoDetalhe (e ajustar Admin.tsx STATUS_OPTIONS/statusLabel/statusClass)</name>
  <files>src/components/AdminDashboard.tsx, src/components/ClienteList.tsx, src/pages/OrcamentoDetalhe.tsx, src/pages/Admin.tsx</files>
  <action>
Para cada arquivo abaixo, fazer substituições explícitas (não regex global — preservar contexto):

**(a) `src/components/AdminDashboard.tsx` — 6 ocorrências:**
- l.60: `.filter((o) => o.status === "fechado")` → `.filter((o) => o.status === "aprovado")`. Renomear a variável local `fechados` para `aprovados` se ela aparecer logo após (pode haver `const fechados =` que vira `const aprovados =`); ajustar todos os usos da variável dentro da função.
- l.109: `.filter((o) => o.status === "fechado")` → `.filter((o) => o.status === "aprovado")` (cálculo de receita efetiva)
- l.138: `fechado: inRange.filter((o) => o.status === "fechado").reduce(...)` → `aprovado: inRange.filter((o) => o.status === "aprovado").reduce(...)` (renomear chave do objeto)
- l.210: `[formatCurrency(v), name === "fechado" ? "Fechado" : "Aprovado"]` → simplificar para `[formatCurrency(v), "Aprovado"]` (só há um valor agora)
- l.211: `(v) => (v === "fechado" ? "Fechado" : "Aprovado")` → `(v) => "Aprovado"` (idem)
- l.213: `<Bar dataKey="fechado" fill="hsl(142, 71%, 45%)" ...` → `<Bar dataKey="aprovado" fill="hsl(142, 71%, 45%)" ...`

**(b) `src/components/ClienteList.tsx` — 2 ocorrências:**
- l.145: `case "fechado": return "Fechado";` → REMOVER essa linha inteira; adicionar `case "aprovado": return "Aprovado";` e `case "pendente": return "Pendente";` (se ainda não houver)
- l.155: `case "fechado": return "bg-green-100 text-green-800";` → REMOVER; adicionar `case "aprovado": return "bg-green-100 text-green-800";` e `case "pendente": return "bg-yellow-100 text-yellow-800";`

**(c) `src/pages/OrcamentoDetalhe.tsx` — 2 ocorrências:**
- l.67: `case "fechado": return "Fechado";` → REMOVER; adicionar `case "aprovado": return "Aprovado";` e `case "pendente": return "Pendente";`
- l.77: `case "fechado": return "bg-green-100 text-green-800";` → REMOVER; adicionar `case "aprovado": return "bg-green-100 text-green-800";` e `case "pendente": return "bg-yellow-100 text-yellow-800";`

**(d) `src/pages/Admin.tsx` — 3 ocorrências de "fechado" + cleanup do `enviado` obsoleto:**

- l.51-58 (`STATUS_OPTIONS`): substituir o array todo por:
```typescript
const STATUS_OPTIONS = [
  { value: "all", label: "Todos" },
  { value: "rascunho", label: "Rascunho" },
  { value: "pendente", label: "Pendente" },
  { value: "aprovado", label: "Aprovado" },
  { value: "perdido", label: "Perdido" },
] as const;
```

- l.425-434 (`statusLabel` switch): substituir o switch inteiro por:
```typescript
const statusLabel = (s: string) => {
  switch (s) {
    case "rascunho": return "Rascunho";
    case "pendente": return "Pendente";
    case "aprovado": return "Aprovado";
    case "perdido": return "Perdido";
    default: return s;
  }
};
```

- l.436-443 (`statusClass` switch): substituir por:
```typescript
const statusClass = (s: string) => {
  switch (s) {
    case "rascunho": return "bg-muted text-muted-foreground";
    case "pendente": return "bg-yellow-100 text-yellow-800";
    case "aprovado": return "bg-emerald-100 text-emerald-800";
    case "perdido": return "bg-red-100 text-red-800";
    default: return "bg-muted text-muted-foreground";
  }
};
```

(Paleta: cinza/âmbar/verde/vermelho conforme D-17. Reaproveita tokens shadcn existentes.)

NÃO MEXER em outras partes de Admin.tsx neste plan — particularmente:
- NÃO mexer na TableRow do orcamento (l.999-1030) — Plan 10-04 vai mexer ali
- NÃO mexer no `<span className={...statusClass...}>` da coluna Status (l.1011-1013) — Plan 10-04 substitui pelo dropdown

**Atenção worktree:** ao terminar o plan, commitar com `git add <caminhos-explícitos>` — NUNCA `git add -A` (lição de Phase 09 — apaga files de outros worktrees).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$files = @('src/components/AdminDashboard.tsx','src/components/ClienteList.tsx','src/pages/OrcamentoDetalhe.tsx','src/pages/Admin.tsx'); foreach ($f in $files) { $c = Get-Content -Raw $f; if ($c -match '\"fechado\"' -or $c -match \"'fechado'\") { Write-Error \"fechado still in $f\"; exit 1 } }; $admin = Get-Content -Raw 'src/pages/Admin.tsx'; if ($admin -notmatch 'value: \"pendente\"' -or $admin -notmatch 'value: \"aprovado\"') { Write-Error 'STATUS_OPTIONS missing pendente/aprovado'; exit 1 }; if ($admin -match 'value: \"enviado\"') { Write-Error 'enviado still present in Admin.tsx STATUS_OPTIONS'; exit 1 }; Write-Host 'OK: fechado removed from 4 files'"</automated>
  </verify>
  <done>Zero `'fechado'` ou `"fechado"` em AdminDashboard.tsx, ClienteList.tsx, OrcamentoDetalhe.tsx, Admin.tsx. `STATUS_OPTIONS` em Admin.tsx reflete os 4 valores válidos + "all". `statusLabel`/`statusClass` cobrem `pendente`/`aprovado`/`rascunho`/`perdido`.</done>
</task>

<task type="auto">
  <name>Task 3: Deletar EncerrarNegociacaoModal e remover todos os usos (D-31)</name>
  <files>src/components/EncerrarNegociacaoModal.tsx, src/pages/Admin.tsx, src/components/ClienteList.tsx</files>
  <action>
D-31: `EncerrarNegociacaoModal` está bugado (grava `"fechado"` que a CHECK constraint da Phase 7 rejeita — erro silencioso em prod) E vira redundante com o dropdown novo (Plan 10-04). Deletar completamente.

**(a) Deletar o arquivo:**
- `src/components/EncerrarNegociacaoModal.tsx` → REMOVER do filesystem
- Após o delete, garantir `git add` explícito do delete (não `git rm` se preferir, mas commit precisa refletir a deleção).

**(b) Remover usos em `src/pages/Admin.tsx`:**
- Remover o import: `import EncerrarNegociacaoModal from "@/components/EncerrarNegociacaoModal";` (ou similar; localizar via grep)
- Remover o state local `encerrarOpen` / `encerrarOrcId` (e qualquer setter): `useState<boolean>` + `useState<string | null>`
- Remover a função `canEncerrar` (l.446) — não tem mais consumidor
- Remover o JSX que renderiza o modal (provavelmente perto do fim do componente: `<EncerrarNegociacaoModal open={encerrarOpen} ... />`)
- Remover o botão Flag inline (l.1015-1027) — toda a coluna "Ações" fica VAZIA (a Plan 10-04 vai popular com o dropdown de status):

  Substituir o bloco:
  ```jsx
  <TableCell>
    {canEncerrar(o.status) && (
      <button ...> <Flag .../> </button>
    )}
  </TableCell>
  ```
  por:
  ```jsx
  <TableCell>{/* Plan 10-04 popula com dropdown de status */}</TableCell>
  ```

- Remover o import do icone `Flag` do `lucide-react` se não houver outro uso no arquivo.

**(c) Verificar `src/components/ClienteList.tsx`:**
- Procurar por qualquer import ou uso de `EncerrarNegociacaoModal` neste arquivo (grep). Se houver, remover idem ao item (b). Se não houver, nenhuma mudança.

**(d) Grep final de sanity:**
Após as edições, `grep -r "EncerrarNegociacaoModal" src/` deve retornar **zero matches**.

**Atenção:** este task é destrutivo de arquivo — usar `git add <caminho>` explícito para o delete + as edições. Nunca `git add -A`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (Test-Path 'src/components/EncerrarNegociacaoModal.tsx') { Write-Error 'EncerrarNegociacaoModal.tsx ainda existe'; exit 1 }; $matches = Select-String -Path 'src\*' -Pattern 'EncerrarNegociacaoModal' -Recurse -ErrorAction SilentlyContinue; if ($matches) { Write-Error \"Referências remanescentes a EncerrarNegociacaoModal: $($matches | Out-String)\"; exit 1 }; Write-Host 'OK: EncerrarNegociacaoModal deleted and all references removed'"</automated>
  </verify>
  <done>`src/components/EncerrarNegociacaoModal.tsx` não existe mais. Zero `grep` matches de `EncerrarNegociacaoModal` em `src/`. Admin.tsx não tem mais state/import/JSX/canEncerrar relacionados ao modal. Coluna "Ações" da TableRow está vazia, pronta para Plan 10-04.</done>
</task>

<task type="auto">
  <name>Task 4: Validar build TypeScript + lint + testes</name>
  <files>(no file edits — verification only)</files>
  <action>
Rodar a cadeia completa de validação para confirmar que o rename de `StatusOrcamento` + as substituições não quebraram a tipagem nem nenhum lugar do código:

```bash
cd auraoramentos
npm run build    # tsc + vite build; deve exit 0
npm run lint     # eslint; warnings OK, errors devem manter o baseline pre-existente (754 errs do baseline Phase 9 podem permanecer mas NENHUM erro NOVO deve aparecer relacionado a fechado/StatusOrcamento)
npm run test     # vitest; suite deve passar (nenhum teste depende de "fechado")
```

Se `npm run build` falhar:
- TS2367 (comparação literal incompatível): localizar o arquivo na mensagem de erro e procurar por uso implícito de `'fechado'`. Substituir conforme padrão de Task 2.
- TS2322 (incompatibilidade de tipo): provavelmente algum lugar passa um string literal que não está mais no union. Ajustar.

Se `npm run test` falhar:
- Verificar se algum teste passa uma fixture com `status: 'fechado'`. Substituir por `'aprovado'`. Se houver poucos, atualizar; se for refactor maior, registrar como follow-up.

Não prosseguir para o commit (Task 5 do SUMMARY) até build verde.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "cd auraoramentos; npm run build 2>&1 | Tee-Object -Variable buildOut; if ($LASTEXITCODE -ne 0) { Write-Error 'build failed'; exit 1 }; npm run test 2>&1 | Tee-Object -Variable testOut; if ($LASTEXITCODE -ne 0) { Write-Error 'tests failed'; exit 1 }; Write-Host 'OK: build + tests green'"</automated>
  </verify>
  <done>`npm run build` exit 0. `npm run test` passa. Lint pode ter warnings/baseline-erros pré-existentes — NÃO bloquear se for o caso, apenas registrar no SUMMARY o delta vs baseline (esperado: 0 erros novos).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TypeScript compile-time → Runtime | Type mismatch entre client (StatusOrcamento) e DB (CHECK constraint) causa erro silencioso em runtime (UPDATE com `'fechado'` retorna 400) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering (data integrity) | EncerrarNegociacaoModal grava `'fechado'` quebrando CHECK | mitigate | Delete completo do componente (D-31); UPDATE com `'fechado'` impossibilita-se porque o código que faria isso não existe mais |
| T-10-07 | Repudiation | UI mostra "Fechado" mas DB tem `'aprovado'` (state divergente) | mitigate | Rename StatusOrcamento + atualização dos switches `statusLabel`/`statusClass` em todos os 4 arquivos consumidores garante consistência UI ↔ DB |
| T-10-08 | DoS (compile-time) | tsc falha após rename, app não builda | mitigate | Task 4 valida `npm run build` antes de commitar; rollback trivial se quebrar |
</threat_model>

<verification>
- [ ] `grep -r "'fechado'" src/` ou `grep -r '"fechado"' src/` retornam 0 matches
- [ ] `src/components/EncerrarNegociacaoModal.tsx` não existe
- [ ] `npm run build` exit 0
- [ ] `npm run test` passa
- [ ] `src/types/orcamento.ts:109` linha exata: `export type StatusOrcamento = 'rascunho' | 'aprovado' | 'perdido' | 'pendente';`
</verification>

<success_criteria>
1. `StatusOrcamento` em `src/types/orcamento.ts` é union dos 4 valores válidos (D-25)
2. `src/integrations/supabase/types.ts` regenerado refletindo schema atual (D-26)
3. Zero ocorrências de `'fechado'` em qualquer arquivo `.ts`/`.tsx` em `src/` (D-27, D-33)
4. `EncerrarNegociacaoModal.tsx` deletado + nenhuma referência permanece (D-31)
5. `npm run build` + `npm run test` passam (validação de regressão)
</success_criteria>

<output>
Após completion, criar `.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-02-SUMMARY.md` listando:
- Arquivos modificados (5) e deletado (1)
- Diff resumo de StatusOrcamento
- Confirmação de build + test verde
- Baseline lint vs current (deve ser igual — 0 erros novos)
- Decisões aplicadas: D-25, D-26, D-27, D-31, D-33
</output>
