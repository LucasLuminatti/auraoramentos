---
phase: 10-wizard-edi-o-status-descri-o-rica
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src/components/Step3Revisao.tsx
  - .planning/phases/10-wizard-edi-o-status-descri-o-rica/10-03-SUMMARY.md
autonomous: true
requirements: [WIZ-01, WIZ-02]
must_haves:
  truths:
    - "No Step 3, cada linha de luminária mostra dois inputs editáveis: quantidade (integer) e preço unitário (decimal)"
    - "Cada sistema (fita, perfil, driver) mostra inputs editáveis para preço unitário (e quantidade no perfil); fita/driver não têm input de quantidade (quantidade é derivada do cálculo do sistema)"
    - "Edição usa estado local de string + flush para o domain state apenas no blur ou Enter (pattern PrecosBatch.tsx, D-34) — zero recalc durante digitação"
    - "Após blur/Enter, totais (subtotal por linha, agrupamento de rolos de fita, total geral) atualizam refletindo o valor editado"
    - "Editar preço abaixo do preco_minimo dispara o fluxo de violação existente (ExceptionChat) — fluxo de exception não regrediu"
    - "Edição salva no snapshot via salvarOrcamento (UPDATE em orcamentos.ambientes) — product_variants (master) não é tocado"
  artifacts:
    - path: "src/components/Step3Revisao.tsx"
      provides: "Step 3 com inputs inline editáveis para qty e preço, padrão PrecosBatch"
      contains: "handleEditQuantidade"
      contains_alt: "handleEditPrecoUnitario"
  key_links:
    - from: "<Input> inline na TableCell"
      to: "handleEditQuantidade / handleEditPrecoUnitario"
      via: "onBlur + onKeyDown(Enter)"
      pattern: "onBlur"
    - from: "handleEditQuantidade / handleEditPrecoUnitario"
      to: "onUpdateAmbientes(updated)"
      via: "imutável map sobre ambientes"
      pattern: "onUpdateAmbientes"
    - from: "Step3Revisao.persistirOrcamento"
      to: "orcamentos.ambientes (jsonb) UPDATE"
      via: "supabase.from('orcamentos').update({ ambientes })"
      pattern: "update\\(\\{[\\s\\S]*ambientes"
---

<objective>
Permitir que o colaborador edite **quantidade** e **preço unitário** de cada item (luminárias + componentes de sistemas) direto na tabela do Step 3, via input inline no padrão `PrecosBatch.tsx` (D-34). Edição usa estado local de string + flush no blur/Enter, evitando o jank de recalc por keystroke. Edição altera apenas o snapshot em memória → salva no `orcamentos.ambientes` quando o user gera o PDF (D-04). O `product_variants` (master) não é tocado.

Purpose: WIZ-01 (editar preço) + WIZ-02 (editar quantidade). Atende success criteria 1 e 2 do ROADMAP Phase 10. Integra com fluxo de violação/ExceptionChat já existente (D-03) — preço abaixo do mínimo continua sendo flagado e o user pode ajustar para o mínimo ou solicitar exceção.

Output: `Step3Revisao.tsx` atualizado com inputs inline + handlers + sub-componente reutilizável.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-CONTEXT.md
@.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-RESEARCH.md
@src/components/Step3Revisao.tsx
@src/components/PrecosBatch.tsx
@src/types/orcamento.ts

<interfaces>
<!-- Step3Props (já existente, src/components/Step3Revisao.tsx:22-30): -->
<!--   interface Step3Props { -->
<!--     orcamento: Orcamento; -->
<!--     onPrev: () => void; -->
<!--     clienteId?: string; -->
<!--     clienteNome: string; -->
<!--     projetoNome: string; -->
<!--     projetoId?: string; -->
<!--     onUpdateAmbientes: (ambientes: Ambiente[]) => void;   ← já existe, já usado em handleAjustarPreco -->
<!--   } -->

<!-- Tipos relevantes (src/types/orcamento.ts): -->
<!--   ItemLuminaria { id, codigo, descricao, quantidade: number, precoUnitario: number, precoMinimo: number, ... } -->
<!--   ItemFitaLED { id, codigo, descricao, precoUnitario: number, precoMinimo: number, wm: number, ... }    ← sem quantidade direta -->
<!--   ItemPerfil { id, codigo, descricao, quantidade: number, precoUnitario: number, precoMinimo: number, comprimentoPeca: number, passadas: number } -->
<!--   ItemDriver { id, codigo, descricao, precoUnitario: number, precoMinimo: number, potencia: number, voltagem: number, ... }   ← sem quantidade direta (derivada) -->

<!-- Padrão PrecosBatch.tsx l.309-336 (input inline na TableCell, reference exata D-34): -->
<!--   <Input -->
<!--     type="number" step="0.01" min="0" -->
<!--     value={localValue}                      ← estado local string -->
<!--     onChange={(e) => setLocalValue(e.target.value)}    ← só atualiza string local -->
<!--     onBlur={() => { /* parse + clamp + flush ao onUpdateAmbientes */ }} -->
<!--     onKeyDown={(e) => { if (e.key === 'Enter') e.currentTarget.blur(); }} -->
<!--     className="w-24 text-right ml-auto" -->
<!--   /> -->

<!-- Detector de violação JÁ existente (Step3Revisao.tsx:79-95) → mantém-se intacto: -->
<!--   const isViolacao = (codigo, precoUnitario, precoMinimo) => -->
<!--     precoMinimo > 0 && precoUnitario < precoMinimo && !approvedExceptions.has(`${codigo}-${precoUnitario}`); -->
<!--   ↑ Recomputado a cada render via useMemo `violacoes` (l.79); edição inline atualiza ambientes → recomputa automaticamente -->

<!-- persistirOrcamento JÁ existente (l.189-239): faz UPDATE ou INSERT em orcamentos.ambientes -->
<!--   Edição inline NAO precisa mexer aqui — só atualiza `ambientes` no parent state, e quando user clica "Gerar PDF" o salvar pega o ambientes atual. -->

<!-- Locais exatos a substituir (src/components/Step3Revisao.tsx): -->
<!-- - l.390: <TableCell className="text-right">{item.quantidade}</TableCell>                       ← Luminária qty -->
<!-- - l.391: <TableCell className="text-right">{formatarMoeda(item.precoUnitario)}...              ← Luminária preço -->
<!-- - l.429: <TableCell className="text-right">{formatarMoeda(sis.fita.precoUnitario)}...          ← Fita preço -->
<!-- - l.438: <TableCell ...>{sis.perfil.comprimentoPeca}m × {sis.perfil.quantidade} = ...           ← Perfil quantidade INLINE no detalhe (manter visual, editar separado) -->
<!-- - l.439: <TableCell className="text-right">{formatarMoeda(sis.perfil.precoUnitario)}...        ← Perfil preço -->
<!-- - l.449: <TableCell className="text-right">{formatarMoeda(sis.driver.precoUnitario)}...        ← Driver preço -->

<!-- Sobre quantidade do perfil (D-05 + caveat de Open Question #3 do RESEARCH): -->
<!--   sis.perfil.quantidade é editável (campo INT >= 1). O texto "{Xm × Y = Zm}" na l.438 é um detalhe de cálculo derivado — manter como display, -->
<!--   adicionar input de quantidade APENAS na coluna correspondente (Detalhe ou nova) ou aceitar que o user edita do Step 2. -->
<!--   DECISAO LOCAL: D-06 diz que Step 3 = negociação, Step 2 = estrutura. Perfil.quantidade é "estrutura" (numero de peças do perfil), não "negociação". -->
<!--   → Para WIZ-02 (quantidade), só edita quantidade de LUMINARIA. Sistemas (fita/driver/perfil) só editam PREÇO. -->
<!--   Quantidade derivada de fita/driver é calculada (calcularDemandaFita, calcularQtdDrivers) e não cabe edit. -->
<!--   Perfil.quantidade FICA editável SÓ no Step 2 (AmbienteCard). Step 3 mostra como display calculado, sem input. -->

<!-- Aviso de worktree: usar git add com caminhos explícitos (lição Phase 09). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar sub-componente EditableNumericCell (estado local + flush on-blur, padrão PrecosBatch)</name>
  <files>src/components/Step3Revisao.tsx</files>
  <behavior>
    - Input controlado com estado local de string
    - onChange atualiza só o estado local (sem flush)
    - onBlur faz parse + clamp + chama o callback de flush
    - onKeyDown com Enter → currentTarget.blur() (dispara o flush)
    - Sincroniza com prop value quando ela muda externamente (ex: handleAjustarPreco do botão de violação)
  </behavior>
  <action>
Adicionar um sub-componente local em `src/components/Step3Revisao.tsx` (antes da declaração do `Step3Revisao`):

```typescript
interface EditableNumericCellProps {
  value: number;
  onCommit: (next: number) => void;
  mode: "integer" | "decimal";
  min?: number;       // default: integer=1, decimal=0
  className?: string; // default: "w-20 text-right ml-auto"
  ariaLabel?: string;
}

function EditableNumericCell({ value, onCommit, mode, min, className, ariaLabel }: EditableNumericCellProps) {
  const [local, setLocal] = useState<string>(String(value));

  // Sync external mutations (e.g. handleAjustarPreco) para o input
  useEffect(() => { setLocal(String(value)); }, [value]);

  const handleBlur = () => {
    const raw = mode === "integer" ? parseInt(local, 10) : parseFloat(local);
    const fallback = mode === "integer" ? (min ?? 1) : (min ?? 0);
    const clampMin = min ?? (mode === "integer" ? 1 : 0);
    const next = Number.isFinite(raw) ? Math.max(clampMin, raw) : fallback;
    if (next !== value) onCommit(next);
    setLocal(String(next)); // normaliza display
  };

  return (
    <Input
      type="number"
      step={mode === "integer" ? "1" : "0.01"}
      min={min ?? (mode === "integer" ? 1 : 0)}
      value={local}
      onChange={(e) => setLocal(e.target.value)}
      onBlur={handleBlur}
      onKeyDown={(e) => { if (e.key === "Enter") e.currentTarget.blur(); }}
      onFocus={(e) => e.target.select()}
      className={className ?? "w-20 text-right ml-auto"}
      aria-label={ariaLabel}
    />
  );
}
```

Imports necessários (adicionar/garantir):
- `import { Input } from "@/components/ui/input";` (no topo do arquivo)
- `useEffect` já está importado de `react`

Notas:
- `mode="integer"` para quantidade, `mode="decimal"` para preço.
- `onFocus={(e) => e.target.select()}` é qualidade de vida (selects o conteúdo ao focar) — pattern usado em UIs de planilha.
- O `useEffect` que sincroniza com `value` externo é CRÍTICO: sem ele, quando o user clica em "Ajustar para R$X" (handleAjustarPreco), o input continua mostrando o valor antigo. Esse pitfall não está coberto explicitamente no RESEARCH mas é detectável pelo cenário de violação.

NÃO criar arquivo separado — manter inline como helper local em `Step3Revisao.tsx` (segue convenção do projeto, componentes pequenos co-localizados).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw 'src/components/Step3Revisao.tsx'; $checks = @('function EditableNumericCell', 'mode: \"integer\" \| \"decimal\"', 'onBlur=\\{handleBlur\\}', 'onKeyDown=\\{\\(e\\) => \\{ if \\(e.key === \"Enter\"\\)', 'useEffect\\(\\(\\) => \\{ setLocal'); foreach ($p in $checks) { if ($c -notmatch $p) { Write-Error \"missing: $p\"; exit 1 } }; Write-Host 'OK: EditableNumericCell present'"</automated>
  </verify>
  <done>Componente `EditableNumericCell` definido em `Step3Revisao.tsx` com props {value, onCommit, mode, min?, className?, ariaLabel?}, estado local de string, flush no blur, Enter → blur, sincronização via useEffect quando `value` muda externamente.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar handlers de edição (handleEditQuantidade, handleEditPrecoLuminaria, handleEditPrecoFita, handleEditPrecoPerfil, handleEditPrecoDriver)</name>
  <files>src/components/Step3Revisao.tsx</files>
  <behavior>
    - Recebem (ambienteId, itemId, novoValor) e produzem array `ambientes` atualizado imutavelmente
    - Chamam `onUpdateAmbientes(updated)` — single source of truth pra prop change
    - Não tocam state interno do componente — recálculo de violacoes/totais via useMemo cuida disso automaticamente
    - Não tocam `product_variants` (D-04: persistência só no snapshot via salvarOrcamento)
  </behavior>
  <action>
Adicionar 5 handlers dentro do componente `Step3Revisao` (próximos ao `handleAjustarPreco` existente para reaproveitar padrão de map imutável):

```typescript
// WIZ-02 — Edita quantidade de uma luminária
const handleEditQuantidade = (ambienteId: string, itemId: string, nova: number) => {
  const updated = ambientes.map((amb) => {
    if (amb.id !== ambienteId) return amb;
    return {
      ...amb,
      luminarias: amb.luminarias.map((l) =>
        l.id === itemId ? { ...l, quantidade: Math.max(1, Math.floor(nova)) } : l
      ),
    };
  });
  onUpdateAmbientes(updated);
};

// WIZ-01 — Edita preço unitário de uma luminária
const handleEditPrecoLuminaria = (ambienteId: string, itemId: string, novo: number) => {
  const updated = ambientes.map((amb) => {
    if (amb.id !== ambienteId) return amb;
    return {
      ...amb,
      luminarias: amb.luminarias.map((l) =>
        l.id === itemId ? { ...l, precoUnitario: Math.max(0, novo) } : l
      ),
    };
  });
  onUpdateAmbientes(updated);
};

// WIZ-01 — Edita preço unitário da fita de um sistema
const handleEditPrecoFita = (ambienteId: string, sistemaId: string, novo: number) => {
  const updated = ambientes.map((amb) => {
    if (amb.id !== ambienteId) return amb;
    return {
      ...amb,
      sistemas: amb.sistemas.map((sis) =>
        sis.id === sistemaId
          ? { ...sis, fita: { ...sis.fita, precoUnitario: Math.max(0, novo) } }
          : sis
      ),
    };
  });
  onUpdateAmbientes(updated);
};

// WIZ-01 — Edita preço unitário do perfil de um sistema
const handleEditPrecoPerfil = (ambienteId: string, sistemaId: string, novo: number) => {
  const updated = ambientes.map((amb) => {
    if (amb.id !== ambienteId) return amb;
    return {
      ...amb,
      sistemas: amb.sistemas.map((sis) => {
        if (sis.id !== sistemaId || !sis.perfil) return sis;
        return { ...sis, perfil: { ...sis.perfil, precoUnitario: Math.max(0, novo) } };
      }),
    };
  });
  onUpdateAmbientes(updated);
};

// WIZ-01 — Edita preço unitário do driver de um sistema
const handleEditPrecoDriver = (ambienteId: string, sistemaId: string, novo: number) => {
  const updated = ambientes.map((amb) => {
    if (amb.id !== ambienteId) return amb;
    return {
      ...amb,
      sistemas: amb.sistemas.map((sis) =>
        sis.id === sistemaId
          ? { ...sis, driver: { ...sis.driver, precoUnitario: Math.max(0, novo) } }
          : sis
      ),
    };
  });
  onUpdateAmbientes(updated);
};
```

Notas críticas:
- Luminária aceita edição de QUANTIDADE (WIZ-02). Sistemas (fita/driver/perfil) NÃO têm input de quantidade no Step 3 — `sis.perfil.quantidade` continua só editável no Step 2 (D-06: Step 3 = negociação, Step 2 = estrutura). Para sistemas, só preço é editável.
- `Math.max` faz o clamp (qty >= 1, preço >= 0) — D-05.
- `Math.floor` na quantidade garante integer mesmo se o parse retornar decimal (defensivo).
- Imutável map sobre array — padrão idêntico ao `handleAjustarPreco` existente (l.147-165).
- A recomputação de `violacoes` (l.79-95), `gruposFita` (l.74), `resumoDrivers` (l.75), `totalGeral` (l.76) é via `useMemo([ambientes])` — automática após `onUpdateAmbientes`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw 'src/components/Step3Revisao.tsx'; $names = @('handleEditQuantidade', 'handleEditPrecoLuminaria', 'handleEditPrecoFita', 'handleEditPrecoPerfil', 'handleEditPrecoDriver'); foreach ($n in $names) { if ($c -notmatch \"const $n = \") { Write-Error \"missing handler: $n\"; exit 1 } }; if (($c | Select-String 'onUpdateAmbientes\\(updated\\)' -AllMatches).Matches.Count -lt 6) { Write-Error 'onUpdateAmbientes call count < 6 (5 novos + 1 do handleAjustarPreco existente)'; exit 1 }; Write-Host 'OK: 5 handlers created'"</automated>
  </verify>
  <done>5 handlers (`handleEditQuantidade`, `handleEditPrecoLuminaria`, `handleEditPrecoFita`, `handleEditPrecoPerfil`, `handleEditPrecoDriver`) definidos dentro do componente Step3Revisao, todos usando map imutável e chamando `onUpdateAmbientes`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Substituir as 5 TableCells estáticas por EditableNumericCell + build verde</name>
  <files>src/components/Step3Revisao.tsx</files>
  <behavior>
    - Cada TableCell de qty ou preço passa a renderizar `<EditableNumericCell>` em vez do texto puro
    - O badge de violação (`violacaoIndicator`) que vinha junto da preço-cell continua aparecendo do lado do input
    - npm run build + npm run test passam
  </behavior>
  <action>
Substituir as TableCells estáticas em `Step3Revisao.tsx`:

**(a) Luminária — qty (l.390):**

Antes:
```tsx
<TableCell className="text-right">{item.quantidade}</TableCell>
```

Depois:
```tsx
<TableCell className="text-right">
  <EditableNumericCell
    value={item.quantidade}
    onCommit={(v) => handleEditQuantidade(amb.id, item.id, v)}
    mode="integer"
    ariaLabel={`Quantidade ${item.codigo}`}
  />
</TableCell>
```

**(b) Luminária — preço (l.391):**

Antes:
```tsx
<TableCell className="text-right">{formatarMoeda(item.precoUnitario)}{violacaoIndicator(item.codigo, item.precoUnitario, item.precoMinimo)}</TableCell>
```

Depois:
```tsx
<TableCell className="text-right">
  <div className="flex items-center justify-end gap-1">
    <EditableNumericCell
      value={item.precoUnitario}
      onCommit={(v) => handleEditPrecoLuminaria(amb.id, item.id, v)}
      mode="decimal"
      ariaLabel={`Preço unitário ${item.codigo}`}
    />
    {violacaoIndicator(item.codigo, item.precoUnitario, item.precoMinimo)}
  </div>
</TableCell>
```

**(c) Fita — preço (l.429):**

Antes:
```tsx
<TableCell className="text-right">{formatarMoeda(sis.fita.precoUnitario)}{violacaoIndicator(sis.fita.codigo, sis.fita.precoUnitario, sis.fita.precoMinimo)}</TableCell>
```

Depois:
```tsx
<TableCell className="text-right">
  <div className="flex items-center justify-end gap-1">
    <EditableNumericCell
      value={sis.fita.precoUnitario}
      onCommit={(v) => handleEditPrecoFita(amb.id, sis.id, v)}
      mode="decimal"
      ariaLabel={`Preço unitário fita ${sis.fita.codigo}`}
    />
    {violacaoIndicator(sis.fita.codigo, sis.fita.precoUnitario, sis.fita.precoMinimo)}
  </div>
</TableCell>
```

**(d) Perfil — preço (l.439):**

Antes:
```tsx
<TableCell className="text-right">{formatarMoeda(sis.perfil.precoUnitario)}{violacaoIndicator(sis.perfil.codigo, sis.perfil.precoUnitario, sis.perfil.precoMinimo)}</TableCell>
```

Depois:
```tsx
<TableCell className="text-right">
  <div className="flex items-center justify-end gap-1">
    <EditableNumericCell
      value={sis.perfil.precoUnitario}
      onCommit={(v) => handleEditPrecoPerfil(amb.id, sis.id, v)}
      mode="decimal"
      ariaLabel={`Preço unitário perfil ${sis.perfil.codigo}`}
    />
    {violacaoIndicator(sis.perfil.codigo, sis.perfil.precoUnitario, sis.perfil.precoMinimo)}
  </div>
</TableCell>
```

**(e) Driver — preço (l.449):**

Antes:
```tsx
<TableCell className="text-right">{formatarMoeda(sis.driver.precoUnitario)}{violacaoIndicator(sis.driver.codigo, sis.driver.precoUnitario, sis.driver.precoMinimo)}</TableCell>
```

Depois:
```tsx
<TableCell className="text-right">
  <div className="flex items-center justify-end gap-1">
    <EditableNumericCell
      value={sis.driver.precoUnitario}
      onCommit={(v) => handleEditPrecoDriver(amb.id, sis.id, v)}
      mode="decimal"
      ariaLabel={`Preço unitário driver ${sis.driver.codigo}`}
    />
    {violacaoIndicator(sis.driver.codigo, sis.driver.precoUnitario, sis.driver.precoMinimo)}
  </div>
</TableCell>
```

**NÃO MEXER:**
- Linha 438 do perfil (`{sis.perfil.comprimentoPeca}m × {sis.perfil.quantidade} = ...`) permanece como detalhe display.
- Subtotal cells (`calcularSubtotalLuminaria(item)`, etc) permanecem com `formatarMoeda` — apenas recalculam via useMemo após edição.
- A coluna "Fita - Subtotal" mostra "Global →" (l.430) — não muda.
- Header da tabela (l.376-383) não muda.

**Validação final:**
```bash
npm run build   # tsc + vite build — exit 0 obrigatório
npm run test    # vitest — passa obrigatório
```

Se o build quebrar:
- "Cannot find name 'amb'" — verificar que cada substituição está DENTRO do map `amb.luminarias.map((item) => ...)` ou `amb.sistemas.map((sis) => ...)` — o `amb` referenciado vem do nível externo.
- "Property 'id' does not exist on perfil" — confirmar que o `sis.perfil` está dentro do bloco `{sis.perfil && (...)}` (já é o caso na l.433-442).

**Atenção worktree:** `git add` com caminhos explícitos.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw 'src/components/Step3Revisao.tsx'; $cells = @('handleEditQuantidade\\(amb.id, item.id, v\\)', 'handleEditPrecoLuminaria\\(amb.id, item.id, v\\)', 'handleEditPrecoFita\\(amb.id, sis.id, v\\)', 'handleEditPrecoPerfil\\(amb.id, sis.id, v\\)', 'handleEditPrecoDriver\\(amb.id, sis.id, v\\)'); foreach ($p in $cells) { if ($c -notmatch $p) { Write-Error \"missing usage: $p\"; exit 1 } }; cd auraoramentos; npm run build 2>&1 | Out-Null; if ($LASTEXITCODE -ne 0) { Write-Error 'build failed'; exit 1 }; npm run test -- --run 2>&1 | Out-Null; if ($LASTEXITCODE -ne 0) { Write-Error 'tests failed'; exit 1 }; Write-Host 'OK: edits + build + tests green'"</automated>
  </verify>
  <done>As 5 TableCells alvo (luminária qty + 4 preços: luminária/fita/perfil/driver) renderizam `<EditableNumericCell>`, com `violacaoIndicator` agora dentro de um `<div className="flex items-center justify-end gap-1">` ao lado do input. `npm run build` exit 0 + `npm run test` passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → React state | Input numérico do user vai pro state local; antes do flush para domain state, valor é parsed e clamped |
| React state → orcamentos.ambientes (jsonb) | Quando user clica "Gerar PDF", o snapshot atual de `ambientes` é serializado e gravado via UPDATE (server-side: RLS UPDATE de Plan 10-01 protege contra colab alheio) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Tampering | User digita preço negativo no input | mitigate | `<Input min="0">` (HTML hint) + `Math.max(0, v)` no handler (clamp efetivo) |
| T-10-10 | Tampering | User digita quantidade zero/negativa | mitigate | `<Input min="1">` + `Math.max(1, Math.floor(v))` no handler |
| T-10-11 | Tampering | User contorna o floor preco_minimo via edit inline | accept | Detecção continua via `isViolacao` no useMemo `violacoes`; fluxo ExceptionChat existente trata (D-03). Preço abaixo do mínimo NÃO é bloqueado no input — é flagado e requer exception aprovada |
| T-10-12 | DoS (UI jank) | Recalc rodando a cada keystroke | mitigate | Estado local de string + flush só no blur/Enter (D-34, pattern PrecosBatch) — recalc roda 1×/edição completa |
</threat_model>

<verification>
- [ ] `EditableNumericCell` definido com estado local + flush on-blur + Enter blur + sync useEffect
- [ ] 5 handlers de edição (qty luminária + 4 preços) imutáveis chamando `onUpdateAmbientes`
- [ ] 5 TableCells substituídas usando os handlers
- [ ] `npm run build` exit 0
- [ ] `npm run test` passa
- [ ] Fluxo de violação preservado (visual: badge de violação continua aparecendo do lado do input quando preço < mínimo)
</verification>

<success_criteria>
1. Edição de quantidade de luminária dispara recalc de subtotal + totalGeral on-blur (WIZ-02)
2. Edição de preço unitário em luminária/fita/perfil/driver dispara recalc + violação se < precoMinimo (WIZ-01 + D-03)
3. Recalc não acontece durante digitação (apenas no blur/Enter) — confirmado por code review do EditableNumericCell (estado local)
4. Edição não toca `product_variants` (D-04) — confirmado: nenhum handler chama Supabase, só `onUpdateAmbientes`
5. Edição persiste quando user gera PDF: o `salvarOrcamento` existente já passa `ambientes` (atual) para o UPDATE em `orcamentos.ambientes`
</success_criteria>

<output>
Após completion, criar `.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-03-SUMMARY.md` listando:
- Sub-componente `EditableNumericCell` criado (linhas adicionadas)
- 5 handlers criados
- 5 TableCells substituídas
- Build + test status
- Decisões aplicadas: D-01, D-02, D-03, D-04, D-05, D-06, D-34
</output>
