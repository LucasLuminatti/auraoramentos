---
phase: 10-wizard-edi-o-status-descri-o-rica
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260514000002_orcamentos_status_rls.sql
  - .planning/phases/10-wizard-edi-o-status-descri-o-rica/10-01-PUSH-LOG.md
  - .planning/phases/10-wizard-edi-o-status-descri-o-rica/10-01-SUMMARY.md
autonomous: false
requirements: [WIZ-04]
must_haves:
  truths:
    - "Policy permissiva 'Authenticated users can update orcamentos' (USING (true)) foi removida em prod"
    - "Existe policy de UPDATE em `orcamentos` que filtra por `colaborador_id = (SELECT id FROM colaboradores WHERE user_id = auth.uid())` AND `status != 'aprovado'`"
    - "Existe policy de UPDATE em `orcamentos` que filtra por `has_role(auth.uid(), 'admin'::app_role)` AND `status != 'aprovado'`"
    - "Tentativa de UPDATE em `orcamentos` com `status='aprovado'` (mudar de aprovado pra qualquer outra coisa) retorna 0 rows updated"
    - "WITH CHECK garante que apenas valores do enum (`rascunho|aprovado|perdido|pendente`) podem ser gravados"
  artifacts:
    - path: "supabase/migrations/20260514000002_orcamentos_status_rls.sql"
      provides: "Migration RLS UPDATE one-way aprovado + colab dono + admin"
      contains: "DROP POLICY \"Authenticated users can update orcamentos\""
    - path: ".planning/phases/10-wizard-edi-o-status-descri-o-rica/10-01-PUSH-LOG.md"
      provides: "Snapshot PRE-PUSH e POST-PUSH das policies em prod + smoke SQL"
      contains: "POST-PUSH"
  key_links:
    - from: "supabase/migrations/20260514000002_orcamentos_status_rls.sql"
      to: "prod database (jkewlaezvrbuicmncqbj)"
      via: "supabase MCP apply_migration (ou supabase db push)"
      pattern: "Colab update own orcamentos non-aprovado"
    - from: "policy USING clause"
      to: "public.colaboradores.user_id"
      via: "subquery SELECT id FROM colaboradores WHERE user_id = auth.uid()"
      pattern: "colaborador_id = \\(SELECT id FROM"
---

<objective>
Substituir a policy de UPDATE permissiva atual (`USING (true)`) em `public.orcamentos` por policies que (a) restringem UPDATE ao colaborador dono OU admin e (b) bloqueiam server-side qualquer UPDATE em rows onde `status='aprovado'` (one-way irreversível por D-16). Camada de defesa server-side para WIZ-04 — a UI de dropdown da Plan 10-04 já vai mostrar o estado correto, mas a policy garante o invariante mesmo se o frontend vazar.

Purpose: WIZ-04 (D-15 + D-16) — sem essa policy, qualquer usuário autenticado pode alterar qualquer orçamento + reverter aprovado via SQL direto. É a única migration da Phase 10 (sem mexer em colunas).

Output: 1 migration aplicada em prod + push-log + summary com snapshot before/after.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-CONTEXT.md
@.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-RESEARCH.md
@.planning/phases/07-schema-prep-v1-1/07-CONTEXT.md
@.planning/phases/09-multi-tenancy-rls/09-VERIFICATION.md
@supabase/migrations/20260511000003_orcamentos_status_enum.sql
@supabase/migrations/20260514000001_arquitetos_clientes_rls.sql

<interfaces>
<!-- Schema atual de `public.orcamentos` em prod (verificar via list_tables antes do apply): -->
<!-- - id UUID PK -->
<!-- - cliente_id UUID FK clientes(id) -->
<!-- - colaborador_id UUID FK colaboradores(id)   ← KEY: subquery resolve user_id → colaborador.id -->
<!-- - status TEXT NOT NULL DEFAULT 'rascunho' CHECK (status IN ('rascunho','aprovado','perdido','pendente'))  ← Phase 7 -->
<!-- - ambientes JSONB -->
<!-- - valor NUMERIC -->
<!-- - tipo TEXT -->
<!-- - pdf_template_version INTEGER -->
<!-- - created_at, updated_at TIMESTAMP -->

<!-- Policies atuais em `public.orcamentos` (a substituir): -->
<!--   SELECT: "Anyone can read orcamentos" USING (true)         ← fica intacto (fora de escopo Phase 10, ver CONTEXT D-32) -->
<!--   INSERT: existente                                          ← fica intacto -->
<!--   UPDATE: "Authenticated users can update orcamentos" USING (true)  ← DROPAR -->
<!--   DELETE: existente                                          ← fica intacto -->

<!-- Função helper já existente (validada em Phase 9): -->
<!--   public.has_role(_user_id UUID, _role app_role) RETURNS BOOLEAN SECURITY DEFINER -->

<!-- Padrão Drive D-02 / Phase 9 (replicar a estrutura SQL): -->
<!--   ver supabase/migrations/20260514000001_arquitetos_clientes_rls.sql -->
<!--   BEGIN; DROP POLICY IF EXISTS ...; CREATE POLICY ... USING (...) WITH CHECK (...); COMMENT ON POLICY ...; COMMIT; -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar arquivo da migration SQL com policies UPDATE colab-dono + admin + block-aprovado</name>
  <files>supabase/migrations/20260514000002_orcamentos_status_rls.sql</files>
  <action>
Criar o arquivo `supabase/migrations/20260514000002_orcamentos_status_rls.sql` espelhando o padrão da Phase 9 (`20260514000001_arquitetos_clientes_rls.sql`) — atomicidade via BEGIN/COMMIT, DROP POLICY IF EXISTS, CREATE POLICY, COMMENT ON POLICY com referência à fase/decisão.

Conteúdo exato:

```sql
-- Phase 10 / WIZ-04 (D-15, D-16): RLS UPDATE em orcamentos
--
-- Substitui a policy permissiva atual ("Authenticated users can update orcamentos"
-- USING (true)) por policies que:
--   1. Restringem UPDATE ao colaborador dono OU admin (has_role)
--   2. Bloqueiam server-side qualquer UPDATE em rows onde status='aprovado'
--      (camada de defesa para o invariante one-way de WIZ-04)
--
-- SELECT continua aberto (USING (true)) — fora de escopo Phase 10 (D-32).
-- Padrão replicado de supabase/migrations/20260514000001_arquitetos_clientes_rls.sql

BEGIN;

-- Drop policy permissiva legada (se existir com qualquer um dos nomes possíveis)
DROP POLICY IF EXISTS "Authenticated users can update orcamentos" ON public.orcamentos;
DROP POLICY IF EXISTS "Colab can update own orcamentos non-aprovado" ON public.orcamentos;
DROP POLICY IF EXISTS "Admin can update orcamentos non-aprovado" ON public.orcamentos;

-- Policy: colab dono pode atualizar seus orcamentos, mas nao pode alterar rows ja aprovadas
CREATE POLICY "Colab can update own orcamentos non-aprovado"
  ON public.orcamentos
  FOR UPDATE
  TO authenticated
  USING (
    colaborador_id = (SELECT id FROM public.colaboradores WHERE user_id = auth.uid())
    AND status != 'aprovado'
  )
  WITH CHECK (
    status IN ('rascunho', 'aprovado', 'perdido', 'pendente')
  );

COMMENT ON POLICY "Colab can update own orcamentos non-aprovado" ON public.orcamentos IS
  'Phase 10 / WIZ-04 D-15 + D-16: colab dono pode atualizar seus orcamentos (incluindo marcar como aprovado), mas nao pode alterar row ja aprovada (one-way irreversivel).';

-- Policy: admin pode atualizar qualquer orcamento, mas tambem nao pode alterar rows ja aprovadas
CREATE POLICY "Admin can update orcamentos non-aprovado"
  ON public.orcamentos
  FOR UPDATE
  TO authenticated
  USING (
    public.has_role(auth.uid(), 'admin'::app_role)
    AND status != 'aprovado'
  )
  WITH CHECK (
    status IN ('rascunho', 'aprovado', 'perdido', 'pendente')
  );

COMMENT ON POLICY "Admin can update orcamentos non-aprovado" ON public.orcamentos IS
  'Phase 10 / WIZ-04 D-15 + D-16: admin pode atualizar qualquer orcamento, mas tambem nao pode alterar row ja aprovada (one-way irreversivel mesmo para admin).';

COMMIT;
```

**Pontos críticos:**
- `BEGIN`/`COMMIT` torna o conjunto atômico (se uma policy falha, rollback completo).
- `DROP POLICY IF EXISTS` cobre os 3 nomes possíveis (atual + futuros, defensivo).
- Subquery `(SELECT id FROM public.colaboradores WHERE user_id = auth.uid())` é o padrão de Phase 9 — coluna `colaboradores.user_id` existe desde Phase 7 D-01.
- `'admin'::app_role` — cast explícito ao enum `app_role` (mesmo padrão Phase 9 e Drive v1.0).
- `WITH CHECK status IN (...)` é segurança adicional contra valores inválidos (a CHECK constraint já trava no banco, mas RLS WITH CHECK é o ponto idiomático).
- NÃO criar policy para SELECT — D-32 mantém SELECT aberto fora do escopo Phase 10.
- NÃO criar policy para INSERT/DELETE — fora de escopo.
- Nome dos arquivos: timestamp `20260514000002_` (após `20260514000001_` da Phase 9, mesma data).

NÃO executar `supabase db push` ainda — apenas criar o arquivo. O apply em prod é Task 3 (checkpoint).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path 'supabase\migrations\20260514000002_orcamentos_status_rls.sql')) { Write-Error 'Migration file missing'; exit 1 }; $content = Get-Content -Raw 'supabase\migrations\20260514000002_orcamentos_status_rls.sql'; $checks = @('DROP POLICY IF EXISTS \"Authenticated users can update orcamentos\"', 'CREATE POLICY \"Colab can update own orcamentos non-aprovado\"', 'CREATE POLICY \"Admin can update orcamentos non-aprovado\"', 'status != ''aprovado''', 'colaborador_id = (SELECT id FROM public.colaboradores WHERE user_id = auth.uid())', 'public.has_role(auth.uid(), ''admin''::app_role)', 'WITH CHECK', 'BEGIN;', 'COMMIT;'); foreach ($p in $checks) { if ($content -notmatch [regex]::Escape($p)) { Write-Error \"Missing pattern: $p\"; exit 1 } }; Write-Host 'OK: migration file structure complete'"</automated>
  </verify>
  <done>Arquivo `supabase/migrations/20260514000002_orcamentos_status_rls.sql` existe contendo BEGIN/COMMIT, DROP da policy legada, 2 CREATE POLICY (colab + admin) com `status != 'aprovado'` na USING clause e WITH CHECK do enum dos 4 status, e 2 COMMENT ON POLICY referenciando Phase 10 / WIZ-04 D-15+D-16.</done>
</task>

<task type="auto">
  <name>Task 2: Capturar PRE-PUSH snapshot das policies atuais em prod</name>
  <files>.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-01-PUSH-LOG.md</files>
  <action>
Criar `10-01-PUSH-LOG.md` com a seção PRE-PUSH preenchida. Usar Supabase MCP (`mcp__plugin_supabase__execute_sql`) com a project_id `jkewlaezvrbuicmncqbj` para capturar:

1. **Lista de policies atuais em `orcamentos`:**
```sql
SELECT polname, polcmd, pg_get_expr(polqual, polrelid) AS using_expr, pg_get_expr(polwithcheck, polrelid) AS with_check_expr
FROM pg_policy
WHERE polrelid = 'public.orcamentos'::regclass
ORDER BY polname;
```

2. **Count de orcamentos por status (baseline de integridade):**
```sql
SELECT status, count(*) FROM public.orcamentos GROUP BY status ORDER BY status;
```

3. **Verify que coluna `colaboradores.user_id` existe (a subquery na policy depende disso):**
```sql
SELECT column_name, data_type, is_nullable
FROM information_schema.columns
WHERE table_schema='public' AND table_name='colaboradores' AND column_name='user_id';
```

4. **Verify que função `has_role` existe:**
```sql
SELECT proname, prosecdef FROM pg_proc WHERE proname='has_role' AND pronamespace=(SELECT oid FROM pg_namespace WHERE nspname='public');
```

Formato do arquivo:

```markdown
# 10-01 PUSH LOG — Migration RLS UPDATE orcamentos

**Plan:** 10-01
**Migration:** `supabase/migrations/20260514000002_orcamentos_status_rls.sql`
**Target:** prod (jkewlaezvrbuicmncqbj, sa-east-1)

---  <!-- HR -->

## PRE-PUSH Snapshot

**Captured:** <timestamp ISO>

### Policies atuais em `public.orcamentos`

| polname | polcmd | USING | WITH CHECK |
|---------|--------|-------|------------|
| <linha por policy> | ... | ... | ... |

### Count por status (integridade pre-push)

| status | count |
|--------|-------|
| ... | ... |
| **TOTAL** | <soma> |

### Pre-flight: dependencies

- `colaboradores.user_id`: <exists/missing — data_type, is_nullable>
- `public.has_role(uuid, app_role)`: <exists/missing — SECURITY DEFINER yes/no>

### Pre-flight assertions
- [x/ ] Policy "Authenticated users can update orcamentos" PRESENTE no PRE-PUSH (precisa estar pra Task 1 dar DROP)
- [x/ ] `colaboradores.user_id` EXISTE
- [x/ ] `has_role` EXISTE como SECURITY DEFINER

---  <!-- HR -->

## Apply

(preenchido na Task 3)

---  <!-- HR -->

## POST-PUSH Snapshot

(preenchido na Task 3)

---  <!-- HR -->

## Smoke SQL

(preenchido na Task 3)
```

**Atenção:** Se a policy "Authenticated users can update orcamentos" NÃO estiver presente no PRE-PUSH, marcar a checkbox como `[ ]` e adicionar nota — não é blocker (o DROP IF EXISTS lida graciosamente), mas é sinal de que alguém já tocou o RLS desta tabela.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path '.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-01-PUSH-LOG.md')) { Write-Error 'PUSH-LOG missing'; exit 1 }; $content = Get-Content -Raw '.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-01-PUSH-LOG.md'; if ($content -notmatch 'PRE-PUSH Snapshot' -or $content -notmatch 'Policies atuais em' -or $content -notmatch 'Count por status' -or $content -notmatch 'colaboradores.user_id') { Write-Error 'PUSH-LOG sections incomplete'; exit 1 }; Write-Host 'OK: PRE-PUSH section present'"</automated>
  </verify>
  <done>`10-01-PUSH-LOG.md` existe com seção PRE-PUSH preenchida com dados literais de prod (policies, counts, pre-flight checks). Placeholders Apply/POST-PUSH/Smoke ainda vazios (Task 3 preenche).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Aplicar migration em prod + capturar POST-PUSH snapshot + smoke SQL</name>
  <files>.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-01-PUSH-LOG.md</files>
  <what-built>
Migration `20260514000002_orcamentos_status_rls.sql` criada localmente (Task 1) + PRE-PUSH snapshot capturado em `10-01-PUSH-LOG.md` (Task 2). Falta: aplicar em prod e validar via smoke SQL.
  </what-built>
  <how-to-verify>
**Lenny, esta tarefa requer sua aprovação antes do apply em prod** (mexe em RLS de produção; rollback possível mas custoso).

Pré-confirmação:
1. Conferir o conteúdo de `supabase/migrations/20260514000002_orcamentos_status_rls.sql` (Task 1)
2. Conferir o PRE-PUSH snapshot em `10-01-PUSH-LOG.md` — em particular se a policy "Authenticated users can update orcamentos" REALMENTE existe hoje, e se há rows com status fora do enum dos 4 valores (não deve haver — Phase 7 garantiu)
3. Aprovar com "go" no chat

Apply (executar via Supabase MCP `mcp__plugin_supabase__apply_migration` com `project_id='jkewlaezvrbuicmncqbj'`, `name='20260514000002_orcamentos_status_rls'`, query = conteúdo SQL da Task 1 sem o cabeçalho de comentários — ou via `supabase db push` se preferir CLI; ambos os caminhos validados em Phase 9).

POST-PUSH (rodar IMEDIATAMENTE após o apply via `mcp__plugin_supabase__execute_sql`):

```sql
-- (1) policies novas presentes
SELECT polname, polcmd, pg_get_expr(polqual, polrelid) AS using_expr
FROM pg_policy
WHERE polrelid = 'public.orcamentos'::regclass
ORDER BY polname;
-- ESPERADO: incluir "Colab can update own orcamentos non-aprovado" e "Admin can update orcamentos non-aprovado"; NAO incluir "Authenticated users can update orcamentos"

-- (2) count por status NAO mudou (sanity)
SELECT status, count(*) FROM public.orcamentos GROUP BY status ORDER BY status;
-- ESPERADO: counts identicos ao PRE-PUSH

-- (3) smoke: tentar UPDATE em row 'aprovada' (se houver) via admin role simulada — deve retornar 0 rows
-- (rodar como o próprio Lenny logado como admin — apenas observar o RETURNING vazio)
-- Pular este teste se nao houver row 'aprovada' em prod hoje; nesse caso registrar "n/a — sem row aprovada para testar".

-- (4) build local nao quebra (separado, fora do MCP):
--   npm run build  → exit 0 esperado
--   npm run test   → suite passa
```

Preencher seções Apply, POST-PUSH e Smoke SQL do `10-01-PUSH-LOG.md` com payloads literais. Marcar `**Overall:** PASS` se as 4 verificações batem.

Se algo divergir (ex: count de orcamentos mudou; policy nao aparece), **parar e reportar** antes de prosseguir.
  </how-to-verify>
  <resume-signal>Digite "go" para aprovar o apply em prod, ou descreva ajustes (ex: "ajustar texto da policy", "rollback agora").</resume-signal>
  <action>Checkpoint humano — Lenny aprova com "go", Claude aplica a migration via Supabase MCP apply_migration, executa POST-PUSH SQL queries, preenche as seções Apply/POST-PUSH/Smoke SQL em 10-01-PUSH-LOG.md com payloads literais.</action>
  <verify><automated>powershell -NoProfile -Command " = Get-Content -Raw '.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-01-PUSH-LOG.md'; if ( -notmatch 'POST-PUSH Snapshot' -or  -notmatch 'Apply' -or  -notmatch 'Overall:.*PASS') { Write-Error 'PUSH-LOG sections nao preenchidas ou Overall != PASS'; exit 1 }; Write-Host 'OK'"</automated></verify>
  <done>Migration aplicada em prod com sucesso; POST-PUSH snapshot mostra 2 policies novas e a permissiva removida; counts por status identicos PRE/POST; PUSH-LOG.md marcado **Overall: PASS**.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated user → public.orcamentos UPDATE | Usuário autenticado (colab ou admin) submete UPDATE via PostgREST; RLS filtra antes do storage tocar a row |
| Application UI → Supabase REST | Frontend pode ser bypassado (Postman, curl direto com token); RLS é a única linha de defesa server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation of Privilege | UPDATE em `orcamentos` por colab não-dono | mitigate | Policy `USING (colaborador_id = (SELECT id FROM colaboradores WHERE user_id = auth.uid()))` filtra antes do UPDATE chegar à row |
| T-10-02 | Tampering | Reversão de `status='aprovado'` via SQL direto / REST | mitigate | Policy `USING (status != 'aprovado')` impede UPDATE em rows já aprovadas — invariante one-way (D-16) |
| T-10-03 | Tampering | Gravação de status fora do enum (`'fechado'`, `'finalizado'`, etc.) | mitigate | `WITH CHECK (status IN ('rascunho', 'aprovado', 'perdido', 'pendente'))` na policy + CHECK constraint Phase 7 como redundância em camadas |
| T-10-04 | Elevation of Privilege | Admin tentando reverter aprovado | mitigate | Policy "Admin can update orcamentos non-aprovado" também tem `status != 'aprovado'` — admin não tem bypass do invariante one-way |
| T-10-05 | Information Disclosure | Colab B vendo orçamentos de colab A | accept | Fora de escopo Phase 10 (D-32). SELECT continua `USING (true)`; vira phase futura se virar requisito v1.2 |
</threat_model>

<verification>
- [ ] Arquivo da migration existe localmente (Task 1)
- [ ] PRE-PUSH snapshot capturado com policy legada presente (Task 2)
- [ ] Apply em prod com sucesso (sem erro SQL) (Task 3)
- [ ] POST-PUSH mostra 2 novas policies + ausência da policy legada (Task 3)
- [ ] Count por status pré ↔ pós idêntico (Task 3)
- [ ] `npm run build` exit 0 após Task 3 (sanity local)
</verification>

<success_criteria>
1. `supabase/migrations/20260514000002_orcamentos_status_rls.sql` aplicada em prod com sucesso atômico (BEGIN/COMMIT)
2. Policy permissiva `"Authenticated users can update orcamentos" USING (true)` foi removida em prod
3. As 2 policies novas (`"Colab can update own orcamentos non-aprovado"` + `"Admin can update orcamentos non-aprovado"`) estão ativas em prod com a expressão USING contendo `status != 'aprovado'`
4. Tentativa de UPDATE em row com `status='aprovado'` retorna 0 linhas afetadas (server-side block) — ou registrar "n/a" se não houver row aprovada em prod hoje
5. PUSH-LOG.md com seções PRE-PUSH, Apply, POST-PUSH e Smoke SQL preenchidas com payloads literais (não placeholders)
</success_criteria>

<output>
Após completion, criar `.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-01-SUMMARY.md` referenciando o PUSH-LOG, listando policies criadas/removidas e marcando WIZ-04 (server-side defense) como DELIVERED.
</output>
