---
phase: 10-wizard-edi-o-status-descri-o-rica
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - src/pages/Admin.tsx
  - src/pages/Index.tsx
  - src/components/Step3Revisao.tsx
  - .planning/phases/10-wizard-edi-o-status-descri-o-rica/10-04-SUMMARY.md
autonomous: true
requirements: [WIZ-03, WIZ-04]
must_haves:
  truths:
    - "Na tab Pedidos (Admin.tsx), a coluna Status renderiza badge colorido + dropdown shadcn Select com 4 opções (rascunho, pendente, aprovado, perdido)"
    - "Mudar o status para qualquer valor != 'aprovado' faz UPDATE direto via Supabase, exibe toast.success e atualiza state local sem refetch"
    - "Mudar o status para 'aprovado' abre AlertDialog 'Marcar como aprovado é irreversível' — só após confirmar, faz UPDATE"
    - "Card de orçamento com status='rascunho' é clicável (cursor pointer + tooltip 'Continuar este rascunho') e navega para '/' com state.orcamentoId"
    - "Index.tsx detecta location.state.orcamentoId, faz fetch do orçamento, popula dados/ambientes/cliente/projeto e entra no modo create no Step 1"
    - "Snapshot órfã (cliente removido): toast.error + redirect para Pedidos sem abrir wizard"
    - "Snapshot com produto removido do master: wizard abre normal e exibe (no Step 2/3) os itens com snapshot puro — badge amarelo no item é OUT OF SCOPE deste plan (entra em Plan 10-05 ao lado da descrição rica) — este plan SÓ precisa abrir o wizard sem crash"
    - "salvarOrcamento de rascunho reaberto faz UPDATE no mesmo orcamentoId (não cria duplicata)"
  artifacts:
    - path: "src/pages/Admin.tsx"
      provides: "Coluna Status com dropdown + card clicável quando rascunho"
      contains: "StatusBadgeSelect"
      contains_alt: "AlertDialog"
    - path: "src/pages/Index.tsx"
      provides: "Detecção de location.state.orcamentoId + fetch + populate wizard"
      contains: "useLocation"
    - path: "src/components/Step3Revisao.tsx"
      provides: "Prop initialOrcamentoId para evitar duplicate INSERT em rascunho reaberto"
      contains: "initialOrcamentoId"
  key_links:
    - from: "Admin.tsx TableRow (rascunho)"
      to: "Index.tsx via navigate('/', { state: { orcamentoId } })"
      via: "useNavigate + useLocation"
      pattern: "navigate\\(\"/\", \\{ state:"
    - from: "Index.tsx useEffect[orcamentoParaReabrir]"
      to: "supabase.from('orcamentos').select(...).eq('id', ...).single()"
      via: "Supabase SDK (RLS aplica)"
      pattern: "orcamentos[\\s\\S]*\\.eq\\(\"id\""
    - from: "Step3Revisao.tsx persistirOrcamento"
      to: "UPDATE em orcamentos com mesmo id"
      via: "orcamentoId state inicializado via initialOrcamentoId"
      pattern: "initialOrcamentoId"
    - from: "Status Select onValueChange='aprovado'"
      to: "AlertDialog de confirmação"
      via: "state setConfirmAprovarOpen"
      pattern: "AlertDialog"
---

<objective>
Entregar **WIZ-04** (dropdown de status no card de Pedidos com AlertDialog one-way para `aprovado`) e **WIZ-03** (reabrir rascunho clicando no card → wizard no Step 1 com prefill completo) — ambos modificam o card de orçamento em `Admin.tsx`, então agrupados no mesmo plan para evitar conflito de arquivo.

Camadas de defesa em camadas:
- Cliente (UI): dropdown disabilita reverter aprovado + AlertDialog de confirmação
- Servidor (Plan 10-01): RLS UPDATE bloqueia `status != 'aprovado'` mesmo via SQL direto

Purpose: atende success criteria 3 (reabrir rascunho) + 4 (status dropdown one-way) do ROADMAP Phase 10.

Output: `Admin.tsx` + `Index.tsx` + `Step3Revisao.tsx` ajustados para fluxo round-trip + status edit pós-criação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-CONTEXT.md
@.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-RESEARCH.md
@.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-01-SUMMARY.md
@.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-02-SUMMARY.md
@.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-03-SUMMARY.md
@src/pages/Admin.tsx
@src/pages/Index.tsx
@src/components/Step3Revisao.tsx
@src/components/CompletarCadastroBanner.tsx

<interfaces>
<!-- Estado de Admin.tsx APÓS Plan 10-02 (já mergeado se Wave 2 entregou): -->
<!--   - STATUS_OPTIONS limpo (rascunho/pendente/aprovado/perdido + "all" para o filtro) -->
<!--   - statusLabel/statusClass cobrem os 4 valores -->
<!--   - canEncerrar removido, EncerrarNegociacaoModal removido, botão Flag inline removido -->
<!--   - Coluna "Ações" da TableRow está VAZIA pronta para o novo control -->

<!-- shadcn primitives já importados em Admin.tsx (l.27-30): -->
<!--   AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger -->
<!--   Select, SelectContent, SelectItem, SelectTrigger, SelectValue -->

<!-- Padrão CompletarCadastroBanner.tsx usa useLocation — já validado: -->
<!--   import { useLocation } from "react-router-dom"; -->
<!--   const location = useLocation(); -->
<!--   const state = location.state as { ... } | null; -->

<!-- Schema orcamentos para fetch (verificar coluna names em supabase types.ts): -->
<!--   orcamentos { id, cliente_id, colaborador_id, projeto_id, tipo, ambientes (jsonb), status, valor, pdf_template_version, created_at } -->
<!--   Joins disponíveis: clientes!inner (nome, id), projetos!inner (nome, id), colaboradores (nome, id) -->

<!-- Step3Revisao state hoje (l.70): -->
<!--   const [orcamentoId, setOrcamentoId] = useState<string | null>(null); -->
<!--   ↑ se for null → INSERT; se for set → UPDATE -->
<!--   Para rascunho reaberto, precisa inicializar com o id existente — adicionar prop initialOrcamentoId + useEffect -->

<!-- RLS UPDATE em prod (após Plan 10-01): -->
<!--   - "Colab can update own orcamentos non-aprovado" — USING colaborador_id = (SELECT id FROM colaboradores WHERE user_id = auth.uid()) AND status != 'aprovado' -->
<!--   - "Admin can update orcamentos non-aprovado" — USING has_role(...) AND status != 'aprovado' -->
<!--   ↑ Significa que se um colab tentar mudar status de orçamento de outro colab, o UPDATE retorna 0 rows. -->
<!--   ↑ O dropdown do admin funciona em qualquer card; do colab só nos próprios. -->
<!--   ↑ Dropdown deve ser DISABLED visualmente quando status atual já é 'aprovado' (UI hint do invariante one-way). -->

<!-- Wave 3 — este plan roda DEPOIS de 10-02 (Admin.tsx limpo) e 10-03 (Step3Revisao já mudou inputs inline). -->
<!-- Coordenação: Step3Revisao já está com handlers da Plan 10-03; aqui adicionamos apenas a prop initialOrcamentoId + useEffect. -->

<!-- Aviso de worktree (lição Phase 09): git add caminhos explícitos. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar componente StatusBadgeSelect (dropdown + AlertDialog one-way aprovado) em Admin.tsx</name>
  <files>src/pages/Admin.tsx</files>
  <behavior>
    - Renderiza badge (statusClass + statusLabel) + Select shadcn ao lado
    - Mudança para qualquer valor != 'aprovado' faz UPDATE direto + toast + state update local
    - Mudança para 'aprovado' abre AlertDialog; só após confirmar, UPDATE
    - Se status atual já é 'aprovado', Select fica disabled (UI hint do invariante one-way)
    - Erro do Supabase (RLS bloqueio) é capturado e exibido como toast.error com mensagem amigável
  </behavior>
  <action>
Adicionar dentro de `src/pages/Admin.tsx` (próximo ao topo, antes do `const Admin = () => {` ou inline como helper local, igual ao padrão da base de código):

```typescript
interface StatusBadgeSelectProps {
  orcamentoId: string;
  currentStatus: string;
  onStatusChange: (orcamentoId: string, novo: string) => Promise<void>;
}

function StatusBadgeSelect({ orcamentoId, currentStatus, onStatusChange }: StatusBadgeSelectProps) {
  const [confirmAprovarOpen, setConfirmAprovarOpen] = useState(false);
  const [pendingValue, setPendingValue] = useState<string | null>(null);
  const isAprovado = currentStatus === "aprovado";

  const handleChange = (next: string) => {
    if (next === currentStatus) return;
    if (next === "aprovado") {
      setPendingValue(next);
      setConfirmAprovarOpen(true);
      return;
    }
    void onStatusChange(orcamentoId, next);
  };

  const handleConfirmAprovado = async () => {
    if (pendingValue) await onStatusChange(orcamentoId, pendingValue);
    setConfirmAprovarOpen(false);
    setPendingValue(null);
  };

  return (
    <>
      <Select value={currentStatus} onValueChange={handleChange} disabled={isAprovado}>
        <SelectTrigger className="w-[140px] h-8" onClick={(e) => e.stopPropagation()}>
          <SelectValue />
        </SelectTrigger>
        <SelectContent onClick={(e) => e.stopPropagation()}>
          <SelectItem value="rascunho">Rascunho</SelectItem>
          <SelectItem value="pendente">Pendente</SelectItem>
          <SelectItem value="aprovado">Aprovado</SelectItem>
          <SelectItem value="perdido">Perdido</SelectItem>
        </SelectContent>
      </Select>

      <AlertDialog open={confirmAprovarOpen} onOpenChange={setConfirmAprovarOpen}>
        <AlertDialogContent onClick={(e) => e.stopPropagation()}>
          <AlertDialogHeader>
            <AlertDialogTitle>Marcar como aprovado?</AlertDialogTitle>
            <AlertDialogDescription>
              Esta ação é irreversível. Depois de aprovado, o orçamento não pode ser revertido para outro status. Tem certeza?
            </AlertDialogDescription>
          </AlertDialogHeader>
          <AlertDialogFooter>
            <AlertDialogCancel onClick={() => setPendingValue(null)}>Cancelar</AlertDialogCancel>
            <AlertDialogAction onClick={handleConfirmAprovado}>Confirmar</AlertDialogAction>
          </AlertDialogFooter>
        </AlertDialogContent>
      </AlertDialog>
    </>
  );
}
```

E a função que orquestra o UPDATE no nível de `Admin` (próximo aos outros handlers como `fetchOrcamentos`):

```typescript
const handleStatusChange = async (id: string, novo: string) => {
  const { error } = await supabase
    .from("orcamentos")
    .update({ status: novo })
    .eq("id", id);

  if (error) {
    // RLS bloqueio (ex: tentar reverter aprovado) cai aqui
    toast.error(
      error.message?.includes("policy")
        ? "Você não tem permissão para alterar este orçamento (ou ele já está aprovado)."
        : `Erro ao atualizar status: ${error.message}`
    );
    return;
  }

  // Otimistic update do state local — evita refetch completo
  setOrcamentos((prev) => prev.map((o) => (o.id === id ? { ...o, status: novo } : o)));
  toast.success(`Status atualizado para ${novo}`);
};
```

Notas importantes:
- `onClick={(e) => e.stopPropagation()}` em SelectTrigger/SelectContent/AlertDialogContent é CRÍTICO — a TableRow é `role="button"` com `onClick={navigate(...)}` (l.1002-1004). Sem o stopPropagation, clicar no dropdown também dispara o navigate (UX broken).
- `disabled={isAprovado}` na Select faz o invariante one-way visível na UI. Combinado com a RLS server-side (Plan 10-01), defesa em camadas.
- `toast.error` com tratamento da mensagem de RLS — quando colab tenta mudar status de outro colab, ou tentar reverter aprovado, mensagem amigável em vez do raw error.
- O fluxo `setOrcamentos(prev => prev.map(...))` faz o otimistic update — necessário porque `orcamentos` state local não é refetched após cada UPDATE (refetch completo seria desperdício).

Imports adicionais a garantir no topo de Admin.tsx:
- `import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";` (verificar se já presente — confirmar via grep)
- `import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle } from "@/components/ui/alert-dialog";` (idem)
- `toast` de `sonner` (já presente)
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw 'src/pages/Admin.tsx'; $checks = @('function StatusBadgeSelect', 'AlertDialogTitle>Marcar como aprovado', 'disabled=\\{isAprovado\\}', 'handleStatusChange', 'onClick=\\{\\(e\\) => e.stopPropagation\\(\\)\\}', 'toast.success\\(.Status atualizado'); foreach ($p in $checks) { if ($c -notmatch $p) { Write-Error \"missing: $p\"; exit 1 } }; Write-Host 'OK: StatusBadgeSelect + handleStatusChange present'"</automated>
  </verify>
  <done>`StatusBadgeSelect` definido em Admin.tsx com Select shadcn + AlertDialog para aprovado + disabled quando isAprovado + stopPropagation nos elementos clicáveis. `handleStatusChange` faz UPDATE com tratamento de erro RLS amigável e otimistic state update.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tornar TableRow do orçamento (Pedidos) clicável para reabrir rascunho + plugar StatusBadgeSelect na coluna Status</name>
  <files>src/pages/Admin.tsx</files>
  <behavior>
    - Coluna "Status" renderiza `<StatusBadgeSelect>` com o status atual
    - TableRow com status='rascunho': cursor pointer + tooltip "Continuar este rascunho" + onClick navigate('/', { state: { orcamentoId: o.id } }) (em vez do navigate atual para /admin/orcamento/{id})
    - TableRow com qualquer outro status: mantém comportamento atual (navigate para /admin/orcamento/{id})
    - Coluna "Ações" fica vazia (Plan 10-02 já limpou o botão Flag) — ou removida do header se for o caso
  </behavior>
  <action>
Em `src/pages/Admin.tsx`, localizar o `<TableRow>` do mapper de orcamentos (l.999-1030 do estado pré-Plan-10-02; após 10-02 está sem o botão Flag).

**(a) Wrapper de exibição: badge + dropdown na coluna Status**

Substituir:
```jsx
<TableCell>
  <span className={`rounded px-2 py-0.5 text-xs font-medium ${statusClass(o.status)}`}>{statusLabel(o.status)}</span>
</TableCell>
```

por:
```jsx
<TableCell>
  <div className="flex items-center gap-2" onClick={(e) => e.stopPropagation()}>
    <span className={`rounded px-2 py-0.5 text-xs font-medium ${statusClass(o.status)}`}>{statusLabel(o.status)}</span>
    <StatusBadgeSelect
      orcamentoId={o.id}
      currentStatus={o.status}
      onStatusChange={handleStatusChange}
    />
  </div>
</TableCell>
```

Mantém o badge visual (rápido de ler) + adiciona o dropdown. `stopPropagation` no wrapper duplica a defesa contra o navigate da row.

**(b) TableRow clicável para rascunho:**

Localizar (l.1000-1004):
```jsx
<TableRow
  key={o.id}
  role="button"
  className="cursor-pointer hover:bg-muted/50"
  onClick={() => navigate(`/admin/orcamento/${o.id}`)}
>
```

Substituir por:
```jsx
<TableRow
  key={o.id}
  role="button"
  className="cursor-pointer hover:bg-muted/50"
  title={o.status === "rascunho" ? "Continuar este rascunho" : undefined}
  onClick={() => {
    if (o.status === "rascunho") {
      navigate("/", { state: { orcamentoId: o.id } });
    } else {
      navigate(`/admin/orcamento/${o.id}`);
    }
  }}
>
```

Notas:
- Status `rascunho` → reabrir wizard via Index.
- Status `pendente|aprovado|perdido` → comportamento atual (página de detalhe somente leitura).
- Tooltip via attr `title` (nativo do HTML, simples; segue padrão do projeto).

**(c) Coluna "Ações" — opcional**

Após Plan 10-02 a coluna ficou vazia. Decisões possíveis:
- Manter a coluna vazia (header "Ações") como placeholder para futuro
- Remover a coluna do header e do TableRow

**Decisão:** REMOVER a coluna "Ações" completamente do TableHeader e TableRow neste plan — o dropdown na coluna Status já cobre o caso de uso anterior do botão Flag, e tabela mais limpa. Atualizar também o `colSpan={7}` do empty state (linha ~1033) para `colSpan={6}`.

Antes:
```jsx
<TableHead>Status</TableHead>
<TableHead className="w-20">Ações</TableHead>
```
Depois:
```jsx
<TableHead>Status</TableHead>
```

Antes:
```jsx
<TableCell>{/* Plan 10-04 popula */ }</TableCell>
```
Depois: REMOVER a TableCell.

Antes (empty state):
```jsx
<TableCell colSpan={7} ...>
```
Depois:
```jsx
<TableCell colSpan={6} ...>
```
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw 'src/pages/Admin.tsx'; if ($c -notmatch '<StatusBadgeSelect') { Write-Error 'StatusBadgeSelect not used in TableRow'; exit 1 }; if ($c -notmatch 'navigate\\(\"/\", \\{ state: \\{ orcamentoId: o.id \\} \\}\\)') { Write-Error 'navigate to rascunho not added'; exit 1 }; if ($c -notmatch 'title=\\{o.status === \"rascunho\"') { Write-Error 'tooltip missing'; exit 1 }; if ($c -match '<TableHead className=\"w-20\">Ações</TableHead>') { Write-Error 'Coluna Acoes ainda presente no header'; exit 1 }; if ($c -match 'colSpan=\\{7\\}') { Write-Error 'colSpan ainda 7 — deveria ser 6'; exit 1 }; cd auraoramentos; npm run build 2>&1 | Out-Null; if ($LASTEXITCODE -ne 0) { Write-Error 'build failed'; exit 1 }; Write-Host 'OK: TableRow updated + Coluna Acoes removida + build green'"</automated>
  </verify>
  <done>TableRow do orcamento: rascunho navega para "/" com state.orcamentoId; outros status mantêm `/admin/orcamento/:id`; coluna Status mostra StatusBadgeSelect; coluna "Ações" removida do header + body; colSpan do empty state ajustado para 6; build verde.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Index.tsx detecta location.state.orcamentoId e popula wizard — com tratamento de órfã</name>
  <files>src/pages/Index.tsx</files>
  <behavior>
    - useLocation detecta state.orcamentoId
    - useEffect dispara fetch do orçamento + JOIN cliente + projeto
    - Se ok: popula dados (colaborador, tipo), ambientes, currentClienteId, currentClienteNome, currentProjetoId, currentProjetoNome + setStep(1) + setMode("create")
    - Se cliente removido (cliente_id existe mas join clientes retorna null): toast.error + redirect para /admin?tab=pedidos
    - Se orçamento não existe ou RLS bloqueia: toast.error + redirect para /admin?tab=pedidos
    - Step3Revisao recebe novo prop initialOrcamentoId para evitar duplicate INSERT
  </behavior>
  <action>
**(a) Imports e detecção em Index.tsx:**

No topo de `src/pages/Index.tsx`, adicionar `useLocation`:
```typescript
import { useNavigate, useLocation } from "react-router-dom";
```

Dentro do componente `Index`, adicionar (próximo aos outros hooks):
```typescript
const location = useLocation();
const orcamentoParaReabrir = (location.state as { orcamentoId?: string } | null)?.orcamentoId ?? null;
const [reopenedOrcamentoId, setReopenedOrcamentoId] = useState<string | null>(null);
const [reopening, setReopening] = useState(false);
```

E adicionar o `useEffect` de reabertura:
```typescript
useEffect(() => {
  if (!orcamentoParaReabrir) return;
  let cancelled = false;

  async function reabrir() {
    setReopening(true);
    try {
      const { data, error } = await supabase
        .from("orcamentos")
        .select("id, cliente_id, colaborador_id, projeto_id, tipo, ambientes, status, clientes:cliente_id(id, nome), projetos:projeto_id(id, nome)")
        .eq("id", orcamentoParaReabrir!)
        .maybeSingle();

      if (cancelled) return;
      if (error || !data) {
        toast.error("Orçamento não encontrado ou inacessível.");
        navigate("/admin?tab=pedidos", { replace: true });
        return;
      }

      // Cliente removido (FK RESTRICT em prod deveria impedir, mas defesa em camadas — D-10)
      if (!data.clientes) {
        toast.error("Este orçamento referencia um cliente removido — não é possível continuar.");
        navigate("/admin?tab=pedidos", { replace: true });
        return;
      }

      // Popula state do wizard
      setDados({ colaborador: colaborador?.nome ?? "", tipo: (data.tipo as DadosOrcamento["tipo"]) ?? "" });
      setAmbientes((data.ambientes as unknown as Ambiente[]) ?? []);
      setCurrentClienteId(data.cliente_id);
      setCurrentClienteNome((data.clientes as any).nome ?? "");
      setCurrentProjetoId(data.projeto_id);
      setCurrentProjetoNome((data.projetos as any)?.nome ?? "");
      setReopenedOrcamentoId(data.id);
      setStep(1); // D-08: sempre reabre no Step 1
      setMode("create");

      // Limpar location.state para evitar re-fetch em refresh — usa replace
      navigate("/", { replace: true, state: null });
    } finally {
      if (!cancelled) setReopening(false);
    }
  }

  void reabrir();
  return () => { cancelled = true; };
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [orcamentoParaReabrir]);
```

Imports adicionais a garantir:
- `import { toast } from "sonner";`
- `import { supabase } from "@/integrations/supabase/client";`

**(b) handleNovoOrcamento — também limpar reopenedOrcamentoId:**

No handler existente `handleNovoOrcamento` (l.41-50), adicionar `setReopenedOrcamentoId(null)` após `setAmbientes([])` — garante que iniciar um novo orçamento não herda o id do rascunho anterior.

E no `handleConfirmarVoltar` (l.56-66), idem: adicionar `setReopenedOrcamentoId(null)`.

**(c) Passar initialOrcamentoId para Step3Revisao:**

Localizar o `<Step3Revisao ... />` no JSX (provavelmente um render condicional `{step === 3 && ...}`). Adicionar a prop:

```jsx
<Step3Revisao
  // ... props existentes (orcamento, onPrev, clienteId, clienteNome, projetoNome, projetoId, onUpdateAmbientes)
  initialOrcamentoId={reopenedOrcamentoId ?? undefined}
/>
```

**Atenção:** Index.tsx tem `Step3Revisao` renderizado em algum lugar — confirmar via grep antes de editar. Se a estrutura do return tiver um wizard switch, adicionar ali.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw 'src/pages/Index.tsx'; $checks = @('useLocation', 'orcamentoParaReabrir', 'reopenedOrcamentoId', 'Este orçamento referencia um cliente removido', 'setStep\\(1\\)', 'initialOrcamentoId='); foreach ($p in $checks) { if ($c -notmatch $p) { Write-Error \"missing: $p\"; exit 1 } }; Write-Host 'OK: Index.tsx reopen flow present'"</automated>
  </verify>
  <done>Index.tsx detecta location.state.orcamentoId, faz fetch com JOIN, trata 4 caminhos: (1) sucesso → popula wizard no Step 1, (2) orçamento não encontrado/RLS → toast + redirect, (3) cliente órfão → toast específico + redirect, (4) navega para "/" com replace para limpar state e evitar re-fetch em refresh. `Step3Revisao` recebe `initialOrcamentoId`. `handleNovoOrcamento` e `handleConfirmarVoltar` zeram `reopenedOrcamentoId`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Step3Revisao — aceitar initialOrcamentoId via prop e inicializar state interno para evitar duplicate INSERT</name>
  <files>src/components/Step3Revisao.tsx</files>
  <behavior>
    - Step3Props ganha prop opcional initialOrcamentoId?: string
    - Em mount, useEffect inicializa `orcamentoId` state com `initialOrcamentoId` se passado
    - persistirOrcamento continua decidindo UPDATE vs INSERT pelo valor de `orcamentoId` — não muda
    - Quando o user volta do wizard reaberto e gera o PDF, o UPDATE é em cima do mesmo id (sem duplicata)
  </behavior>
  <action>
Em `src/components/Step3Revisao.tsx`:

**(a) Adicionar prop em Step3Props (l.22-30):**

Antes:
```typescript
interface Step3Props {
  orcamento: Orcamento;
  onPrev: () => void;
  clienteId?: string;
  clienteNome: string;
  projetoNome: string;
  projetoId?: string;
  onUpdateAmbientes: (ambientes: Ambiente[]) => void;
}
```

Depois:
```typescript
interface Step3Props {
  orcamento: Orcamento;
  onPrev: () => void;
  clienteId?: string;
  clienteNome: string;
  projetoNome: string;
  projetoId?: string;
  onUpdateAmbientes: (ambientes: Ambiente[]) => void;
  /** Phase 10 WIZ-03 (D-08): inicializa orcamentoId state quando o wizard é reaberto a partir de um rascunho — evita duplicate INSERT. */
  initialOrcamentoId?: string;
}
```

**(b) Aceitar prop na função:**

Antes:
```typescript
const Step3Revisao = ({ orcamento, onPrev, clienteId, clienteNome, projetoNome, projetoId, onUpdateAmbientes }: Step3Props) => {
```

Depois:
```typescript
const Step3Revisao = ({ orcamento, onPrev, clienteId, clienteNome, projetoNome, projetoId, onUpdateAmbientes, initialOrcamentoId }: Step3Props) => {
```

**(c) useEffect de inicialização (logo após o useState do orcamentoId, l.70):**

```typescript
useEffect(() => {
  if (initialOrcamentoId) setOrcamentoId(initialOrcamentoId);
  // Intencional: rodar apenas no mount. Mudança subsequente de initialOrcamentoId é cobertura de edge case (parent reabriria outro rascunho sem desmontar — não acontece no fluxo atual).
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, []);
```

**Não mexer em mais nada** em `persistirOrcamento` — ele já tem o switch `if (orcamentoId)` na l.202 que decide UPDATE vs INSERT. Com `initialOrcamentoId` setado, esse branch funciona corretamente.

**(d) Validar build:**
```
npm run build      # exit 0
npm run test       # passa
```
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw 'src/components/Step3Revisao.tsx'; if ($c -notmatch 'initialOrcamentoId\\?: string') { Write-Error 'initialOrcamentoId prop missing'; exit 1 }; if ($c -notmatch 'if \\(initialOrcamentoId\\) setOrcamentoId\\(initialOrcamentoId\\)') { Write-Error 'init useEffect missing'; exit 1 }; cd auraoramentos; npm run build 2>&1 | Out-Null; if ($LASTEXITCODE -ne 0) { Write-Error 'build failed'; exit 1 }; Write-Host 'OK: Step3 initialOrcamentoId integrated + build green'"</automated>
  </verify>
  <done>`Step3Props` tem prop opcional `initialOrcamentoId`. `Step3Revisao` aceita e inicializa o state interno via useEffect no mount. `persistirOrcamento` permanece intacto (branch UPDATE já funcionava — só faltava setar o id em rascunho reaberto). Build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser UI → Supabase REST UPDATE status | Colab/admin clica no dropdown; UPDATE viaja via PostgREST com JWT; RLS server-side filtra antes de tocar a row |
| location.state (in-memory) → /admin/orcamento/:id | orcamentoId carregado via state efêmero (não URL) → fetch via Supabase SDK → RLS SELECT aplica |
| Snapshot ambientes (jsonb) → Step3Revisao | Snapshot pode estar inconsistente (cliente removido, produto removido); UI precisa tolerar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Tampering | Colab tenta reverter `aprovado` via UI (digitando direto na URL ou DevTools) | mitigate | UI: dropdown disabled quando isAprovado + AlertDialog antes de aprovar. Server: RLS policy Plan 10-01 bloqueia `status != 'aprovado'` na clause USING. Defesa em camadas. |
| T-10-14 | Elevation of Privilege | Colab B tenta mudar status do orcamento de Colab A via Supabase SDK direto | mitigate | RLS UPDATE policy filtra por `colaborador_id = (SELECT id FROM colaboradores WHERE user_id = auth.uid())` — UPDATE retorna 0 rows. UI handler captura o erro e exibe toast.error amigável. |
| T-10-15 | Information Disclosure | Colab B reabre orcamento de Colab A via URL state manipulation | accept | SELECT em orcamentos é `USING (true)` (D-32, fora de escopo Phase 10). Fetch retorna o orçamento mesmo se for de outro colab. Risco aceito; vira phase futura se sensível. |
| T-10-16 | Tampering | Snapshot reaberto com cliente removido (FK RESTRICT impede em prod mas defesa em camadas) | mitigate | useEffect check `!data.clientes` → toast.error + redirect (D-10). Não abre wizard com state inválido. |
| T-10-17 | Tampering | Duplicate INSERT ao gerar PDF de rascunho reaberto (Step3 cria novo orcamento em vez de UPDATE) | mitigate | Prop `initialOrcamentoId` + useEffect de mount setam `orcamentoId` state → branch UPDATE em `persistirOrcamento` (l.202) é tomado corretamente. |
| T-10-18 | DoS / UX | Clique no Select dispara navigate da TableRow (event bubbling) | mitigate | `onClick={(e) => e.stopPropagation()}` em SelectTrigger, SelectContent, AlertDialogContent e no `<div>` wrapper da TableCell de status. |
</threat_model>

<verification>
- [ ] StatusBadgeSelect renderiza badge + Select disabled-when-aprovado
- [ ] AlertDialog para aprovado aparece e só após confirmar dispara UPDATE
- [ ] handleStatusChange faz UPDATE + toast + state local sem refetch
- [ ] TableRow rascunho navega para "/" com state.orcamentoId; outros status mantêm /admin/orcamento/:id
- [ ] Index.tsx fetcha orçamento + JOIN clientes; trata cliente órfão; popula wizard no Step 1
- [ ] Step3Revisao aceita initialOrcamentoId + useEffect inicializa o state
- [ ] `npm run build` exit 0 + `npm run test` passa
- [ ] Coluna "Ações" removida da tab Pedidos; colSpan empty state ajustado
</verification>

<success_criteria>
1. Dropdown shadcn Select com 4 opções no card de orçamento de Pedidos; UPDATE imediato exceto para `aprovado` (AlertDialog confirma)
2. Status `aprovado` → Select disabled (one-way visual); RLS server-side garante o invariante mesmo se UI vazar (cobertura Plan 10-01)
3. Card com `status='rascunho'` é clicável → wizard abre no Step 1 com prefill completo (cliente, projeto, colaborador, ambientes)
4. Cliente órfão → toast.error + redirect, sem abrir wizard com state inválido
5. Salvar rascunho reaberto após gerar PDF faz UPDATE no mesmo id (sem duplicata) — confirmado pela presença de `initialOrcamentoId` + useEffect de mount
</success_criteria>

<output>
Após completion, criar `.planning/phases/10-wizard-edi-o-status-descri-o-rica/10-04-SUMMARY.md` listando:
- Componente `StatusBadgeSelect` criado (linhas, sub-componente, AlertDialog)
- Handler `handleStatusChange` criado com tratamento de erro RLS
- TableRow do orçamento: navegação dual (rascunho ↔ outros)
- Coluna "Ações" removida
- Index.tsx: useLocation + useEffect de reabertura + tratamento de órfã
- Step3Revisao: prop initialOrcamentoId + useEffect de mount
- Decisões aplicadas: D-07, D-08, D-09, D-10, D-11, D-13, D-14, D-15, D-16, D-17, D-18
</output>
