---
phase: 08-cadastros-opcionalizar-imagens-manuais
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ClienteDialog.tsx
autonomous: true
requirements: [FORM-01]
must_haves:
  truths:
    - "Form de criar cliente aceita submit sem Contato, sem CPF/CNPJ e sem Arquiteto (só Nome obrigatório)"
    - "Labels dos 3 campos opcionais exibem '(opcional)' inline em cinza"
    - "Validação de formato CPF/CNPJ permanece intacta quando o campo é preenchido"
  artifacts:
    - path: "src/components/ClienteDialog.tsx"
      provides: "ClienteDialog sem required={true} nos 3 campos opcionais; labels com hint '(opcional)'"
      contains: "(opcional)"
  key_links:
    - from: "ClienteDialog.tsx form submit"
      to: "supabase.from('clientes').insert"
      via: "payload com contato/cpf_cnpj/arquiteto_id já null-safe (linhas 71-76)"
      pattern: "contato.trim\\(\\) \\|\\| null"
---

<objective>
Tornar Contato, CPF/CNPJ e Arquiteto OPCIONAIS no formulário de cliente (criar e editar), removendo o atributo `required` do JSX e sinalizando "(opcional)" nos labels. **Nenhuma mudança de schema, nenhuma mudança de backend** — o payload já é null-safe (linhas 71-76 de ClienteDialog.tsx). Esta plan é puramente UX.

Purpose: FORM-01 — colaborador não pode mais ser bloqueado por campos não-essenciais.
Output: ClienteDialog.tsx editado em ~4 pontos pontuais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-cadastros-opcionalizar-imagens-manuais/08-CONTEXT.md

<interfaces>
<!-- Estado atual confirmado por leitura direta de src/components/ClienteDialog.tsx -->

ClienteRow (exported, NÃO mudar):
```typescript
export interface ClienteRow {
  id: string;
  nome: string;
  contato: string | null;
  cpf_cnpj: string | null;
  arquiteto_id: string | null;
}
```

Payload de submit já null-safe (ClienteDialog.tsx:71-76 — NÃO MUDAR):
```typescript
const payload = {
  nome: nome.trim(),
  contato: contato.trim() || null,
  cpf_cnpj: cpfCnpj.trim() ? unmask(cpfCnpj) : null,
  arquiteto_id: arquitetoId,
};
```

Pontos exatos de mudança (linhas verificadas em leitura prévia):
- Linha 109: `required` no Input do Nome → MANTER (Nome continua obrigatório)
- Linha 113: `<Label htmlFor="cli-contato">Contato</Label>` → adicionar hint "(opcional)"
- Linhas 114-119: Input contato → JÁ não tem `required` (confirmado), só ajustar label
- Linha 122: `<Label htmlFor="cli-cpf">CPF/CNPJ</Label>` → adicionar hint "(opcional)"
- Linhas 123-128: Input CPF → JÁ não tem `required` (confirmado), só ajustar label
- Linha 131: `<Label>Arquiteto</Label>` → adicionar hint "(opcional)"
- Linhas 132-145: ArquitetoAutocomplete → JÁ permite limpar (onSelect null), só ajustar label
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sinalizar Contato/CPF/Arquiteto como opcionais nos labels do ClienteDialog</name>
  <files>src/components/ClienteDialog.tsx</files>
  <read_first>
    - src/components/ClienteDialog.tsx (arquivo completo — confirmar linhas 113, 122, 131 e estado dos inputs)
    - .planning/phases/08-cadastros-opcionalizar-imagens-manuais/08-CONTEXT.md §D-14, §D-15, §D-16, §D-17 (decisões lockadas)
  </read_first>
  <action>
    Em `src/components/ClienteDialog.tsx`, fazer as 3 substituições EXATAS abaixo (e nada além disso):

    1) Substituir literal:
       ```
       <Label htmlFor="cli-contato">Contato</Label>
       ```
       por:
       ```
       <Label htmlFor="cli-contato">Contato <span className="text-muted-foreground text-xs font-normal">(opcional)</span></Label>
       ```

    2) Substituir literal:
       ```
       <Label htmlFor="cli-cpf">CPF/CNPJ</Label>
       ```
       por:
       ```
       <Label htmlFor="cli-cpf">CPF/CNPJ <span className="text-muted-foreground text-xs font-normal">(opcional)</span></Label>
       ```

    3) Substituir literal:
       ```
       <Label>Arquiteto</Label>
       ```
       por:
       ```
       <Label>Arquiteto <span className="text-muted-foreground text-xs font-normal">(opcional)</span></Label>
       ```

    **NÃO mexer em:**
    - O `required` da linha 109 (Nome continua obrigatório — D-14)
    - O `disabled={saving || !nome.trim()}` do botão Salvar (linha 150) — `!nome.trim()` ainda é a única gate; já está correto
    - O `formatCpfCnpj`/`unmask` (D-16: formato continua mascarando quando preenchido)
    - O payload (D-17: já null-safe)
    - A interface `ClienteRow` exportada
    - O `useEffect` de hydration (modo edit precisa continuar carregando dados sem erro quando vierem null)

    Confirmar via grep que após as 3 substituições, o arquivo contém exatamente 3 ocorrências do literal `(opcional)`.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run lint -- src/components/ClienteDialog.tsx && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ClienteDialog.tsx` contém exatamente 3 ocorrências do literal `(opcional)` (uma para cada um: Contato, CPF/CNPJ, Arquiteto)
    - Arquivo contém literal `text-muted-foreground text-xs font-normal` exatamente 3 vezes
    - Linha do Input de Nome ainda contém atributo `required` (Nome continua obrigatório)
    - Inputs de contato (id="cli-contato") e CPF (id="cli-cpf") **não** contêm o atributo `required` no JSX
    - `npm run lint -- src/components/ClienteDialog.tsx` exit 0
    - `npx tsc --noEmit -p tsconfig.app.json` exit 0 (zero erros TS)
    - `git diff --stat src/components/ClienteDialog.tsx` mostra apenas modificação (nenhum arquivo extra)
  </acceptance_criteria>
  <done>ClienteDialog visualmente sinaliza "(opcional)" nos 3 campos; lint + TS passa; ready para smoke (08-05) confirmar que submit sem os 3 campos funciona em prod.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form input → Supabase clientes.insert | Input do usuário (já validado em formato CPF/CNPJ via mask quando preenchido) atravessa pro DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | T (Tampering) | ClienteDialog payload | accept | Mudança é apenas remover `required` HTML; validação de formato CPF/CNPJ (`formatCpfCnpj`/`unmask`) permanece quando campo é preenchido. Nome continua obrigatório (server e client) |
| T-08-05 | I (Information disclosure) | clientes table sem campos obrigatórios | accept | Phase 9 ativará RLS em `clientes` — colaborador só verá os seus. Vazamento entre colabs já será bloqueado no nível do DB |
</threat_model>

<verification>
- Lint passa em ClienteDialog.tsx
- TypeScript compila zero erros
- 3 labels com "(opcional)" inline em cinza
- Nome continua required
</verification>

<success_criteria>
ClienteDialog opcionaliza Contato, CPF/CNPJ e Arquiteto via UX sutil "(opcional)" sem mudar backend nem schema. Smoke da plan 08-05 valida o submit em prod.
</success_criteria>

<output>
Após completar, criar `.planning/phases/08-cadastros-opcionalizar-imagens-manuais/08-02-SUMMARY.md` documentando as 3 substituições aplicadas e confirmando que o payload e a interface ClienteRow permanecem inalterados.
</output>
