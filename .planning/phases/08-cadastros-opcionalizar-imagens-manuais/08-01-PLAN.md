---
phase: 08-cadastros-opcionalizar-imagens-manuais
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260512000001_arquitetos_expand_fields.sql
autonomous: true
requirements: [FORM-02]
must_haves:
  truths:
    - "Arquivo de migration existe no path canônico com 7 colunas aditivas + 1 index"
    - "SQL é puramente aditivo (sem DROP, sem NOT NULL, sem default destrutivo)"
    - "Cada ADD COLUMN tem COMMENT citando 'Phase 8 FORM-02'"
  artifacts:
    - path: "supabase/migrations/20260512000001_arquitetos_expand_fields.sql"
      provides: "ALTER TABLE arquitetos com 7 colunas + index BTREE em data_nascimento"
      contains: "ADD COLUMN data_nascimento"
  key_links:
    - from: "arquitetos.data_nascimento"
      to: "idx_arquitetos_data_nascimento"
      via: "CREATE INDEX BTREE"
      pattern: "CREATE INDEX idx_arquitetos_data_nascimento"
---

<objective>
Criar o arquivo SQL da migration aditiva para expandir a tabela `arquitetos` com 7 colunas opcionais (data_nascimento + endereço + 5 colunas bancárias) e um index BTREE em `data_nascimento`. **Esta plan NÃO aplica a migration em prod** — apenas cria o arquivo. O push ocorre na plan 08-04 (BLOCKING).

Purpose: Habilitar FORM-02 (ficha completa do arquiteto) com schema aditivo que respeita constraint perpétua "schema sempre aditivo" do PROJECT.md.
Output: 1 arquivo SQL em `supabase/migrations/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cadastros-opcionalizar-imagens-manuais/08-CONTEXT.md

<interfaces>
<!-- Template estrutural canônico — replicar BEGIN/COMMIT + ADD COLUMN + INDEX + COMMENT -->

From supabase/migrations/20260511000002_clientes_data_nascimento.sql:
```sql
BEGIN;

ALTER TABLE public.clientes
  ADD COLUMN data_nascimento DATE;

CREATE INDEX idx_clientes_data_nascimento
  ON public.clientes(data_nascimento);

COMMENT ON COLUMN public.clientes.data_nascimento IS
  'Data de nascimento do cliente (opcional). Preenchida pelo admin via FORM-02 (Phase 8). Consumida pelo cron de aniversário (Phase 12). Phase 7 AUTO-03.';

COMMIT;
```

Colunas a adicionar em `public.arquitetos` (todas NULL, sem default):
- `data_nascimento DATE` — D-03 (replica Phase 7 D-07/D-08)
- `endereco TEXT` — D-01 (free-form, sem ViaCEP)
- `banco TEXT` — D-02
- `agencia TEXT` — D-02
- `conta TEXT` — D-02
- `tipo_conta TEXT` — D-02 (valores livres tipo 'corrente'/'poupança' — sem CHECK)
- `pix TEXT` — D-02 (string genérica, aceita CPF/email/telefone/aleatória)

Index obrigatório:
- `idx_arquitetos_data_nascimento` BTREE em `arquitetos(data_nascimento)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration SQL aditiva para 7 colunas + 1 index em arquitetos</name>
  <files>supabase/migrations/20260512000001_arquitetos_expand_fields.sql</files>
  <read_first>
    - supabase/migrations/20260511000002_clientes_data_nascimento.sql (template estrutural canônico — BEGIN/COMMIT, padrão de COMMENT, sem default destrutivo)
    - supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql (última migration que tocou em `arquitetos` — confirmar que `user_id` já existe e que não há conflito de nome de coluna)
    - .planning/phases/08-cadastros-opcionalizar-imagens-manuais/08-CONTEXT.md §D-01..D-05 (decisões lockadas sobre colunas e tipos)
  </read_first>
  <action>
    Criar `supabase/migrations/20260512000001_arquitetos_expand_fields.sql` com o conteúdo exato abaixo (preserve a estrutura BEGIN/COMMIT, comentários em pt-BR, header citando D-01..D-05):

    ```sql
    -- Phase 8 / Plan 01: arquitetos.expand_fields — FORM-02 (ficha completa do escritório)
    -- Refs: FORM-02 — desbloqueia ArquitetoDialog expandido (Plan 08-05)
    -- Decisões:
    --   D-01 endereco TEXT NULL (free-form, sem ViaCEP)
    --   D-02 dados bancários em 5 colunas typed (sem JSONB)
    --   D-03 data_nascimento DATE NULL + index BTREE (replica Phase 7 D-07/D-08)
    --   D-05 7 colunas em uma única migration aditiva
    -- Aditivo puro: sem NOT NULL, sem default, sem backfill, sem pre-flight.

    BEGIN;

    -- =========================================================================
    -- Bloco 1 — ADD COLUMN x 7 (todas NULL, sem default — schema sempre aditivo)
    -- =========================================================================
    ALTER TABLE public.arquitetos
      ADD COLUMN data_nascimento DATE,
      ADD COLUMN endereco        TEXT,
      ADD COLUMN banco           TEXT,
      ADD COLUMN agencia         TEXT,
      ADD COLUMN conta           TEXT,
      ADD COLUMN tipo_conta      TEXT,
      ADD COLUMN pix             TEXT;

    -- =========================================================================
    -- Bloco 2 — Index BTREE em data_nascimento (D-03)
    -- Replica padrão Phase 7 D-08 (clientes.data_nascimento). Volume atual baixo;
    -- index funcional MONTH/DAY pode entrar depois se cron de aniversário do
    -- arquiteto for ativado (out of scope v1.1).
    -- =========================================================================
    CREATE INDEX idx_arquitetos_data_nascimento
      ON public.arquitetos(data_nascimento);

    -- =========================================================================
    -- Bloco 3 — COMMENT ON COLUMN x 7 (citando Phase 8 FORM-02 em cada uma)
    -- =========================================================================
    COMMENT ON COLUMN public.arquitetos.data_nascimento IS
      'Data de nascimento do arquiteto (opcional). Phase 8 FORM-02. Preparado para eventual cron de aniversário (Phase 12+).';
    COMMENT ON COLUMN public.arquitetos.endereco IS
      'Endereço do escritório do arquiteto (string única free-form, sem estrutura CEP/UF/cidade). Phase 8 FORM-02 D-01.';
    COMMENT ON COLUMN public.arquitetos.banco IS
      'Nome do banco para pagamento de comissão. Phase 8 FORM-02 D-02.';
    COMMENT ON COLUMN public.arquitetos.agencia IS
      'Número da agência bancária. Phase 8 FORM-02 D-02.';
    COMMENT ON COLUMN public.arquitetos.conta IS
      'Número da conta bancária. Phase 8 FORM-02 D-02.';
    COMMENT ON COLUMN public.arquitetos.tipo_conta IS
      'Tipo de conta (corrente/poupança/etc — string livre, sem CHECK). Phase 8 FORM-02 D-02.';
    COMMENT ON COLUMN public.arquitetos.pix IS
      'Chave Pix do arquiteto (CPF/email/telefone/aleatória — string genérica). Phase 8 FORM-02 D-02.';

    COMMIT;
    ```

    Garantir que o arquivo termina com newline. **Não aplicar `supabase db push` aqui** — a aplicação em prod é responsabilidade exclusiva da plan 08-04 (que também regenera os types TS).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('supabase/migrations/20260512000001_arquitetos_expand_fields.sql','utf8');const need=['ADD COLUMN data_nascimento DATE','ADD COLUMN endereco        TEXT','ADD COLUMN banco           TEXT','ADD COLUMN agencia         TEXT','ADD COLUMN conta           TEXT','ADD COLUMN tipo_conta      TEXT','ADD COLUMN pix             TEXT','CREATE INDEX idx_arquitetos_data_nascimento','COMMENT ON COLUMN public.arquitetos.data_nascimento','COMMENT ON COLUMN public.arquitetos.endereco','COMMENT ON COLUMN public.arquitetos.banco','COMMENT ON COLUMN public.arquitetos.agencia','COMMENT ON COLUMN public.arquitetos.conta','COMMENT ON COLUMN public.arquitetos.tipo_conta','COMMENT ON COLUMN public.arquitetos.pix','BEGIN;','COMMIT;'];const miss=need.filter(s=>!t.includes(s));if(miss.length){console.error('MISSING:',miss);process.exit(1)}if(/DROP|NOT NULL|DEFAULT/i.test(t)){console.error('Detected destructive keyword');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260512000001_arquitetos_expand_fields.sql` existe
    - Contém literal `ALTER TABLE public.arquitetos` exatamente 1 vez
    - Contém 7 substrings `ADD COLUMN` (uma por linha do bloco ALTER)
    - Contém literal `CREATE INDEX idx_arquitetos_data_nascimento`
    - Contém 7 ocorrências do literal `COMMENT ON COLUMN public.arquitetos.`
    - Não contém os literais `DROP `, `NOT NULL`, `DEFAULT` (case-insensitive — schema aditivo puro)
    - Começa com `BEGIN;` e termina com `COMMIT;` (transação atômica)
    - Comando de verify acima sai com exit 0 e imprime `OK`
  </acceptance_criteria>
  <done>Arquivo SQL aditivo criado, validado pelo script de verify, pronto para `supabase db push` ser executado na plan 08-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → prod DB | SQL aditivo será aplicado em produção via `supabase db push` (plan 08-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T (Tampering) | migration file SQL | mitigate | Transação atômica `BEGIN/COMMIT` garante rollback se qualquer ALTER falhar; verify script bloqueia DROP/NOT NULL/DEFAULT por regex |
| T-08-02 | I (Information disclosure) | arquitetos.banco/agencia/conta/pix (PII bancária) | accept | Phase 9 ativará RLS em `arquitetos`; até lá, dependemos do controle de acesso por role (admin + colab dono) implementado na UI (D-04). Risco aceito pelo curto janelamento (Phase 8 → Phase 9 são back-to-back) |
| T-08-03 | D (Denial of service) | index BTREE em data_nascimento | accept | Volume atual de `arquitetos` é dezenas de linhas; CREATE INDEX é instantâneo. Sem risco operacional |
</threat_model>

<verification>
- Arquivo SQL existe no path canônico
- Estrutura BEGIN/COMMIT preservada
- Sem keywords destrutivas
- 7 ADD COLUMN + 1 CREATE INDEX + 7 COMMENT presentes
</verification>

<success_criteria>
SQL aditivo escrito, validado por script automatizado, pronto para push na plan 08-04. Zero side-effect em prod nesta plan.
</success_criteria>

<output>
Após completar, criar `.planning/phases/08-cadastros-opcionalizar-imagens-manuais/08-01-SUMMARY.md` documentando o path do arquivo criado e confirmando que a aplicação em prod está pendente para 08-04.
</output>
