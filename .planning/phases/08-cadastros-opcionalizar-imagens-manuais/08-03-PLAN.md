---
phase: 08-cadastros-opcionalizar-imagens-manuais
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Admin.tsx
autonomous: true
requirements: [FORM-03, FORM-04]
must_haves:
  truths:
    - "Cada linha da tabela de produtos (tab Cadastros > Produtos) exibe um botão de imagem inline (ícone ImageIcon) ao lado do botão Pencil"
    - "Clicar no botão de imagem abre o ProdutoEditDialog em modo edit normal (mesmo dialog do Pencil)"
    - "Coringas AU001..AU016 aparecem na lista de produtos e podem ser editados (descrição + imagem) sem bloqueio"
  artifacts:
    - path: "src/pages/Admin.tsx"
      provides: "Botão de imagem inline adicionado ao TableCell de ações da tab Cadastros > Produtos"
      contains: "ImageIcon className=\"h-4 w-4\""
  key_links:
    - from: "Botão ImageIcon inline (Admin.tsx ~linha 590)"
      to: "ProdutoEditDialog (já renderizado em Admin.tsx mais abaixo)"
      via: "setProdutoEditTarget + setProdutoEditOpen(true)"
      pattern: "setProdutoEditOpen\\(true\\)"
---

<objective>
Implementar FORM-03 + FORM-04: adicionar botão de imagem inline (`ImageIcon`) em cada linha da tabela de produtos na tab Cadastros > Produtos do Admin, que abre o ProdutoEditDialog existente em modo edit normal. Isto cobre **FORM-04** (anexar/trocar imagem inline em qualquer SKU) e **FORM-03** (coringas AU001..AU016 — que já são editáveis tecnicamente — ganham visibilidade equivalente aos outros SKUs).

Purpose: Admin pode anexar/trocar imagem de qualquer produto (master/coringa/manual/legado) sem precisar abrir o dialog completo via Pencil e percorrer todos os campos. ImportImagens em massa permanece intocado.
Output: 1 botão extra no TableCell de ações + reuso 100% do ProdutoEditDialog existente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-cadastros-opcionalizar-imagens-manuais/08-CONTEXT.md

<interfaces>
<!-- Estado atual confirmado por leitura direta de src/pages/Admin.tsx -->

Imports já existem em Admin.tsx:12:
```typescript
import { ArrowLeft, Loader2, Trash2, Search, FileSpreadsheet, DollarSign, ImageIcon, Flag, Plus, Pencil, Filter } from "lucide-react";
```

Estado do ProdutoEditDialog já gerenciado em Admin.tsx (procurar `produtoEditTarget` e `produtoEditOpen` — já existem; setter usados nas linhas 578-588).

TableRow atual (linhas 566-595 de Admin.tsx) — o TableCell de ações termina em:
```tsx
<TableCell className="text-right">
  <Button
    variant="ghost"
    size="sm"
    onClick={() => {
      setProdutoEditTarget({
        id: p.id,
        codigo: p.codigo,
        descricao: p.descricao,
        nome: p.nome ?? null,
        preco_tabela: p.preco_tabela,
        preco_minimo: p.preco_minimo,
        arquiteto_id: p.arquiteto_id ?? null,
        imagem_url: p.imagem_url ?? null,
      });
      setProdutoEditOpen(true);
    }}
  >
    <Pencil className="h-4 w-4" />
  </Button>
</TableCell>
```

ProdutoEditDialog já implementa upload de imagem internamente (CONTEXT §"Reusable Assets" — ProdutoEditDialog 361 linhas suporta create/edit com upload completo). Nenhuma mudança no dialog.

D-11 (locked): reuso total do dialog, sem criar modo "focused-image".
D-12 (locked): toast "Imagem atualizada" já existe no fluxo do dialog — confirmar via leitura, não alterar.
D-13 (locked): funciona pra qualquer SKU uniform — não filtrar por origem.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar botão ImageIcon inline no TableCell de ações da tab Cadastros > Produtos</name>
  <files>src/pages/Admin.tsx</files>
  <read_first>
    - src/pages/Admin.tsx (linhas 12 confirmar import ImageIcon + linhas 560-600 confirmar estrutura do TableCell de ações)
    - src/components/ProdutoEditDialog.tsx (confirmar que o dialog em modo edit suporta upload de imagem sem mudança; D-11 não altera o dialog)
    - .planning/phases/08-cadastros-opcionalizar-imagens-manuais/08-CONTEXT.md §D-07..D-13 (decisões lockadas FORM-03 + FORM-04)
  </read_first>
  <action>
    Em `src/pages/Admin.tsx`, localizar o TableCell de ações da tab Cadastros > Produtos (já confirmado nas linhas 573-593 — termina com `</TableCell>` na linha 593 após o Button do Pencil).

    Substituir o bloco atual:
    ```tsx
    <TableCell className="text-right">
      <Button
        variant="ghost"
        size="sm"
        onClick={() => {
          setProdutoEditTarget({
            id: p.id,
            codigo: p.codigo,
            descricao: p.descricao,
            nome: p.nome ?? null,
            preco_tabela: p.preco_tabela,
            preco_minimo: p.preco_minimo,
            arquiteto_id: p.arquiteto_id ?? null,
            imagem_url: p.imagem_url ?? null,
          });
          setProdutoEditOpen(true);
        }}
      >
        <Pencil className="h-4 w-4" />
      </Button>
    </TableCell>
    ```

    Pelo bloco a seguir (adiciona um Button ImageIcon ANTES do Pencil, reusando o mesmo onClick — D-11):
    ```tsx
    <TableCell className="text-right">
      <div className="inline-flex gap-1">
        <Button
          variant="ghost"
          size="sm"
          title="Anexar/trocar imagem"
          onClick={() => {
            setProdutoEditTarget({
              id: p.id,
              codigo: p.codigo,
              descricao: p.descricao,
              nome: p.nome ?? null,
              preco_tabela: p.preco_tabela,
              preco_minimo: p.preco_minimo,
              arquiteto_id: p.arquiteto_id ?? null,
              imagem_url: p.imagem_url ?? null,
            });
            setProdutoEditOpen(true);
          }}
        >
          <ImageIcon className="h-4 w-4" />
        </Button>
        <Button
          variant="ghost"
          size="sm"
          title="Editar produto"
          onClick={() => {
            setProdutoEditTarget({
              id: p.id,
              codigo: p.codigo,
              descricao: p.descricao,
              nome: p.nome ?? null,
              preco_tabela: p.preco_tabela,
              preco_minimo: p.preco_minimo,
              arquiteto_id: p.arquiteto_id ?? null,
              imagem_url: p.imagem_url ?? null,
            });
            setProdutoEditOpen(true);
          }}
        >
          <Pencil className="h-4 w-4" />
        </Button>
      </div>
    </TableCell>
    ```

    **NÃO mexer em:**
    - Outras tabelas Pencil (Admin.tsx:631 = Arquitetos, Admin.tsx:711 = Colaboradores) — escopo é apenas a tabela de Produtos
    - `setProdutoEditTarget`/`setProdutoEditOpen` declarações ou ProdutoEditDialog (D-11: reuso 100%)
    - `reconcileProducts.ts` (D-09 + CONTEXT §"Reusable Assets" §"reconcileProducts.ts": NÃO TOCAR — coringa já protegido por D-10)
    - ImportImagens (complementar, não substituir)
    - Bucket / upload (D-08: continua singular `produtos-imagens` conforme `uploadProdutoImagem.ts:16`; mesmo path `<codigo>.<ext>`)

    O onClick é intencionalmente duplicado (mesma ação) — visualmente o admin vê 2 botões com semântica clara (imagem vs editar tudo); funcionalmente abrem o mesmo dialog. D-11 prioriza simplicidade total sobre DRY local.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run lint -- src/pages/Admin.tsx && npx tsc --noEmit -p tsconfig.app.json && node -e "const t=require('fs').readFileSync('src/pages/Admin.tsx','utf8');const imgCount=(t.match(/<ImageIcon className=\"h-4 w-4\"/g)||[]).length;const pencilCount=(t.match(/<Pencil className=\"h-4 w-4\"/g)||[]).length;if(imgCount<1){console.error('Expected ImageIcon h-4 inline button, got',imgCount);process.exit(1)}if(pencilCount<3){console.error('Pencil count regressed:',pencilCount);process.exit(1)}console.log('OK img='+imgCount+' pencil='+pencilCount)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/Admin.tsx` contém pelo menos 1 ocorrência do literal `<ImageIcon className="h-4 w-4"` em contexto de Button (inline na tabela)
    - `src/pages/Admin.tsx` mantém pelo menos 3 ocorrências de `<Pencil className="h-4 w-4"` (Produtos + Arquitetos + Colaboradores — confirma que nenhum Pencil foi removido por engano)
    - O Button ImageIcon contém atributo `title="Anexar/trocar imagem"`
    - O onClick do Button ImageIcon chama `setProdutoEditOpen(true)`
    - `npm run lint -- src/pages/Admin.tsx` exit 0
    - `npx tsc --noEmit -p tsconfig.app.json` exit 0
    - Script de verify acima imprime `OK img=1 pencil=3` (ou mais)
    - Arquivos `src/components/ProdutoEditDialog.tsx` e `src/lib/uploadProdutoImagem.ts` **não** estão em `git diff` (D-11 reuso 100%)
    - Arquivo `src/lib/reconcileProducts.ts` **não** está em `git diff` (D-09: coringa não é remarcado)
  </acceptance_criteria>
  <done>Botão de imagem inline funcional em todas as linhas da tabela Cadastros > Produtos, incluindo os 16 coringas AU001..AU016. Smoke da plan 08-05 valida click → upload → persistência da imagem em prod.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File upload (admin browser) → bucket produtos-imagens | Upload de imagem arbitrária pro bucket Supabase Storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | T (Tampering / path traversal) | uploadProdutoImagem.ts path derivation | mitigate | `uploadProdutoImagem.ts:46-52` já valida `codigo` via regex `/^[A-Za-z0-9_-]+$/` — path traversal bloqueado. Esta plan NÃO altera o helper; mitigação herdada |
| T-08-07 | E (Elevation of privilege) | Botão admin inline acessível sem checagem de role | accept | Tab Cadastros > Produtos já está dentro de `<AdminRoute>` (Admin.tsx é admin-only). Botão extra herda o guard; sem mudança de superfície de ataque |
| T-08-08 | D (Denial of service) | Upload de arquivos grandes | mitigate | `uploadProdutoImagem.ts:65-70` já enforce MAX_SIZE 2MB. Esta plan herda a validação sem alterar |
</threat_model>

<verification>
- Lint + TS passa
- ImageIcon inline presente na tabela de Produtos
- Pencil em todas as 3 tabelas preservado (regressão zero)
- ProdutoEditDialog e uploadProdutoImagem.ts NÃO modificados
</verification>

<success_criteria>
FORM-03 + FORM-04 entregues via reuso total do dialog existente. Admin tem entrypoint visual claro pra imagem em qualquer SKU (master/coringa/manual/legado) sem path crítico de imports em massa.
</success_criteria>

<output>
Após completar, criar `.planning/phases/08-cadastros-opcionalizar-imagens-manuais/08-03-SUMMARY.md` documentando o diff aplicado em Admin.tsx, confirmando que ProdutoEditDialog/uploadProdutoImagem/reconcileProducts permanecem intocados, e que coringas seguem com `origem='coringa'` (D-09).
</output>
