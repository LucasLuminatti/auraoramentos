---
phase: 08-cadastros-opcionalizar-imagens-manuais
plan: 05
type: execute
wave: 3
depends_on: [08-01, 08-02, 08-03, 08-04]
files_modified:
  - src/components/ArquitetoDialog.tsx
  - .planning/phases/08-cadastros-opcionalizar-imagens-manuais/SMOKE-RESULTS.md
autonomous: false
requirements: [FORM-01, FORM-02, FORM-03, FORM-04]
must_haves:
  truths:
    - "ArquitetoDialog expõe 7 campos novos: data_nascimento (date), endereco (textarea), banco, agencia, conta, tipo_conta, pix"
    - "Modo edit hydrata todos os 7 campos novos a partir do ArquitetoRow"
    - "Payload de submit envia os 7 campos novos como null quando vazios (string.trim() || null; date string ou null)"
    - "Submit com apenas o Nome preenchido funciona (todos os 7 campos opcionais)"
    - "Smoke prod manual confirma FORM-01, FORM-02, FORM-03, FORM-04 funcionando end-to-end (documento em SMOKE-RESULTS.md)"
  artifacts:
    - path: "src/components/ArquitetoDialog.tsx"
      provides: "ArquitetoDialog expandido com 7 campos opcionais + ArquitetoRow estendida com os mesmos campos"
      contains: "data_nascimento"
    - path: ".planning/phases/08-cadastros-opcionalizar-imagens-manuais/SMOKE-RESULTS.md"
      provides: "Resultados do smoke manual cobrindo FORM-01..04 em prod"
  key_links:
    - from: "ArquitetoDialog form submit"
      to: "supabase.from('arquitetos').insert / update"
      via: "payload com 8 campos (nome + 7 novos)"
      pattern: "supabase\\.from\\(\"arquitetos\"\\)"
---

<objective>
Expandir `ArquitetoDialog.tsx` (hoje 101 linhas, 2 campos) para suportar os 7 campos novos de FORM-02 (data_nascimento + endereco + 5 campos bancários), todos opcionais, com hydration em modo edit e payload null-safe. Após isso, executar smoke manual em prod cobrindo todos os 4 requirements da phase (FORM-01..04) e documentar em SMOKE-RESULTS.md.

Purpose: Fecha FORM-02 (UI + persistência) e valida a phase inteira via smoke pós-push no padrão D-18.
Output: ArquitetoDialog estendido + SMOKE-RESULTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-cadastros-opcionalizar-imagens-manuais/08-CONTEXT.md
@.planning/phases/08-cadastros-opcionalizar-imagens-manuais/PUSH-LOG.md

<interfaces>
<!-- Estado atual confirmado por leitura direta de src/components/ArquitetoDialog.tsx -->

Interface atual (ArquitetoDialog.tsx:9-13) — EXPANDIR (não substituir):
```typescript
export interface ArquitetoRow {
  id: string;
  nome: string;
  contato: string | null;
}
```

Pós-Plan 08-04, types.ts contém em `Database['public']['Tables']['arquitetos']['Row']`:
```typescript
{
  id: string;
  nome: string;
  contato: string | null;
  user_id: string;             // adicionado em Phase 7
  data_nascimento: string | null;  // novo — date vem como ISO string YYYY-MM-DD
  endereco: string | null;     // novo
  banco: string | null;        // novo
  agencia: string | null;      // novo
  conta: string | null;        // novo
  tipo_conta: string | null;   // novo
  pix: string | null;          // novo
}
```

Padrão de payload null-safe (replicar do ClienteDialog.tsx:71-76):
```typescript
const payload = {
  nome: nome.trim(),
  contato: contato.trim() || null,
  data_nascimento: dataNascimento || null,  // input type="date" devolve "" ou "YYYY-MM-DD"
  endereco: endereco.trim() || null,
  banco: banco.trim() || null,
  agencia: agencia.trim() || null,
  conta: conta.trim() || null,
  tipo_conta: tipoConta.trim() || null,
  pix: pix.trim() || null,
};
```

D-04 (lockada): permissão admin + colab dono. **Esta plan NÃO implementa o gate de role** (Phase 9 fará RLS); apenas garante que o form funciona pros 2 cenários quando o usuário consegue abrir o dialog. Sem `useUserRole` check inline aqui — adicionar abre escopo de regressão.

Ordem dos campos no JSX (Claude's discretion no CONTEXT — escolha: Nome → Contato → Data Nascimento → Endereço → bloco "Dados Bancários" agrupado por seção visual com `<Separator>` ou heading):
```
Nome *
Contato
Data de Nascimento
Endereço
─── Dados Bancários (opcional) ───
Banco | Agência | Conta | Tipo de Conta | Pix
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expandir ArquitetoDialog com 7 campos opcionais + hydration + payload null-safe</name>
  <files>src/components/ArquitetoDialog.tsx</files>
  <read_first>
    - src/components/ArquitetoDialog.tsx (arquivo completo, 101 linhas — confirmar estrutura atual antes de expandir)
    - src/components/ClienteDialog.tsx (padrão de payload null-safe nas linhas 71-76 — replicar)
    - src/integrations/supabase/types.ts (confirmar que pós-Plan 08-04 os 7 campos novos estão tipados como `string | null` em arquitetos Row/Insert/Update)
    - .planning/phases/08-cadastros-opcionalizar-imagens-manuais/PUSH-LOG.md (gate da Plan 08-04 — se não existir ou não marcar "Migration aplicada", abortar essa task)
    - .planning/phases/08-cadastros-opcionalizar-imagens-manuais/08-CONTEXT.md §D-01..D-05 (decisões lockadas)
  </read_first>
  <action>
    Reescrever `src/components/ArquitetoDialog.tsx` mantendo a assinatura externa (default export, props `ArquitetoDialogProps`) mas estendendo:

    **1) Estender `ArquitetoRow` (exported interface):**
    ```typescript
    export interface ArquitetoRow {
      id: string;
      nome: string;
      contato: string | null;
      data_nascimento: string | null;
      endereco: string | null;
      banco: string | null;
      agencia: string | null;
      conta: string | null;
      tipo_conta: string | null;
      pix: string | null;
    }
    ```

    **2) Adicionar 7 useState junto aos 2 existentes:**
    ```typescript
    const [nome, setNome] = useState("");
    const [contato, setContato] = useState("");
    const [dataNascimento, setDataNascimento] = useState("");  // "" ou "YYYY-MM-DD"
    const [endereco, setEndereco] = useState("");
    const [banco, setBanco] = useState("");
    const [agencia, setAgencia] = useState("");
    const [conta, setConta] = useState("");
    const [tipoConta, setTipoConta] = useState("");
    const [pix, setPix] = useState("");
    const [saving, setSaving] = useState(false);
    ```

    **3) Estender o `useEffect` de hydration (atualmente linhas 28-33):**
    Para modo edit: hydratar TODOS os 9 campos a partir de `arquiteto` (cada `null` vira `""` na UI). Para modo create (ou quando `!open`): zerar todos os 9.
    ```typescript
    useEffect(() => {
      if (open) {
        const a = mode === "edit" ? arquiteto : null;
        setNome(a?.nome ?? "");
        setContato(a?.contato ?? "");
        setDataNascimento(a?.data_nascimento ?? "");
        setEndereco(a?.endereco ?? "");
        setBanco(a?.banco ?? "");
        setAgencia(a?.agencia ?? "");
        setConta(a?.conta ?? "");
        setTipoConta(a?.tipo_conta ?? "");
        setPix(a?.pix ?? "");
      }
    }, [open, mode, arquiteto]);
    ```

    **4) Estender o payload no `handleSubmit` (atualmente linha 42):**
    ```typescript
    const payload = {
      nome: nome.trim(),
      contato: contato.trim() || null,
      data_nascimento: dataNascimento || null,
      endereco: endereco.trim() || null,
      banco: banco.trim() || null,
      agencia: agencia.trim() || null,
      conta: conta.trim() || null,
      tipo_conta: tipoConta.trim() || null,
      pix: pix.trim() || null,
    };
    ```
    Manter o resto do `handleSubmit` idêntico (insert/update branches, error handling, toast, onSuccess).

    **5) Expandir JSX dentro do `<form>` na ordem visual abaixo, sempre dentro de DialogContent:**
    - Aumentar `DialogContent` para `sm:max-w-2xl` (mais campos cabem).
    - Manter o campo Nome com `required` (D-14 análogo — Nome continua obrigatório aqui também).
    - Marcar os demais labels com hint `<span className="text-muted-foreground text-xs font-normal">(opcional)</span>` exatamente como na Plan 08-02.
    - Bloco bancário separado por um título visual `<div className="pt-2 text-sm font-medium border-t mt-2">Dados Bancários</div>`.
    - Endereço usa `<Textarea>` (importar de `@/components/ui/textarea`) com `rows={2}` para acomodar endereço multi-linha.
    - Data de nascimento usa `<Input type="date" />`.
    - Os 5 campos bancários em grid 2 colunas (`grid grid-cols-1 sm:grid-cols-2 gap-3`).
    - IDs dos inputs no padrão `arq-{campo}` (ex: `arq-data-nascimento`, `arq-endereco`, `arq-banco`).

    **6) NÃO mexer em:**
    - Assinatura/exports do componente (mantém default export + named export `ArquitetoRow`)
    - Props `ArquitetoDialogProps` (apenas o tipo `arquiteto` é estendido implicitamente pela mudança em `ArquitetoRow`)
    - Lógica de toast/error (mantém `"Erro ao salvar arquiteto: "` e success com `"Arquiteto criado!"`/`"Arquiteto atualizado!"`)
    - O botão Salvar com `disabled={saving || !nome.trim()}` (Nome continua única gate)

    **7) Verificar consumidores de `ArquitetoRow`:**
    Após editar, rodar:
    ```
    grep -rn "ArquitetoRow" src/
    ```
    Em cada arquivo que importa `ArquitetoRow`, confirmar que os usos atuais (provavelmente apenas `id`, `nome`, `contato`) continuam compilando — os 7 campos novos são adições opcionais. Se algum consumidor faz `Pick<ArquitetoRow, ...>` ou destructuring restritivo, ajustar para incluir os novos campos somente onde necessário. **Não introduzir uso novo** dos campos fora deste dialog (FORM-02 escopa só o cadastro; consumo em outros lugares fica fora do escopo).
  </action>
  <verify>
    <automated>cd auraoramentos && npm run lint -- src/components/ArquitetoDialog.tsx && npx tsc --noEmit -p tsconfig.app.json && node -e "const t=require('fs').readFileSync('src/components/ArquitetoDialog.tsx','utf8');const need=['data_nascimento','endereco','banco','agencia','conta','tipo_conta','pix','arq-data-nascimento','arq-endereco','arq-banco','arq-agencia','arq-conta','arq-tipo-conta','arq-pix','Dados Bancários','type=\"date\"','Textarea'];const miss=need.filter(s=>!t.includes(s));if(miss.length){console.error('MISSING:',miss);process.exit(1)}const optCount=(t.match(/\\(opcional\\)/g)||[]).length;if(optCount<7){console.error('Expected >=7 (opcional) hints, got',optCount);process.exit(1)}console.log('OK opts='+optCount)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ArquitetoDialog.tsx` contém os 7 literais: `data_nascimento`, `endereco`, `banco`, `agencia`, `conta`, `tipo_conta`, `pix` (cada um pelo menos 2x — interface + state + payload)
    - Contém os 7 IDs de input: `arq-data-nascimento`, `arq-endereco`, `arq-banco`, `arq-agencia`, `arq-conta`, `arq-tipo-conta`, `arq-pix`
    - Contém o literal `Dados Bancários` (título visual da seção)
    - Contém `type="date"` (campo data_nascimento) e `Textarea` (campo endereço)
    - Contém pelo menos 7 ocorrências do literal `(opcional)` nos labels
    - Interface exportada `ArquitetoRow` contém os 7 novos campos como `string | null`
    - Payload no `handleSubmit` envia os 7 campos com `|| null` (nunca string vazia)
    - Botão Salvar continua com `disabled={saving || !nome.trim()}` (Nome única gate)
    - `npm run lint -- src/components/ArquitetoDialog.tsx` exit 0
    - `npx tsc --noEmit -p tsconfig.app.json` exit 0 (consumidores de ArquitetoRow compilam com os campos novos)
    - Toast strings `"Arquiteto criado!"`, `"Arquiteto atualizado!"`, `"Erro ao salvar arquiteto: "` preservados literalmente
  </acceptance_criteria>
  <done>ArquitetoDialog persiste 7 campos novos com hydration correta em modo edit; build verde; pronto pra smoke manual da Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke prod manual (D-18) cobrindo FORM-01..04 + documentar SMOKE-RESULTS.md</name>
  <files>.planning/phases/08-cadastros-opcionalizar-imagens-manuais/SMOKE-RESULTS.md</files>
  <read_first>
    - .planning/phases/08-cadastros-opcionalizar-imagens-manuais/08-CONTEXT.md §D-18 (smoke específico)
    - .planning/phases/07-schema-prep-v1-1/SMOKE-RESULTS.md (template estrutural — replicar layout: tabela com check + observações)
  </read_first>
  <what-built>
    - Migration `arquitetos_expand_fields` em prod (Plan 08-04)
    - ClienteDialog com Contato/CPF/Arquiteto opcionais (Plan 08-02 → FORM-01)
    - ArquitetoDialog com 7 campos novos (Task 1 desta plan → FORM-02)
    - Botão ImageIcon inline em Cadastros > Produtos (Plan 08-03 → FORM-03 + FORM-04)
  </what-built>
  <how-to-verify>
    Antes de aprovar, executar os 4 checks abaixo em **produção (https://orcamentosaura.com.br)** com conta admin, e registrar resultado em `.planning/phases/08-cadastros-opcionalizar-imagens-manuais/SMOKE-RESULTS.md`. O pipeline automático global (CLAUDE.md) ativa Playwright MCP para os 3 primeiros (UI); o 4º é SQL via mcp__plugin_supabase.

    **Check 1 (FORM-01):** Tab Cadastros > Clientes > "Novo Cliente". Preencher APENAS o campo Nome (ex: `Smoke Cliente FORM-01`). Deixar Contato, CPF/CNPJ e Arquiteto em branco. Salvar. Esperado: toast "Cliente criado!", cliente aparece na lista sem os 3 campos opcionais. Labels devem mostrar "(opcional)" em cinza.

    **Check 2 (FORM-02):** Tab Cadastros > Arquitetos > escolher um arquiteto existente > "Editar". Preencher:
    - Data de Nascimento: data futura ou passada qualquer
    - Endereço: `Rua de Teste 123, Sala 4`
    - Banco: `Bradesco`
    - Agência: `1234`
    - Conta: `567890-1`
    - Tipo de Conta: `corrente`
    - Pix: `arquiteto@teste.com`
    Salvar. Reabrir o mesmo arquiteto em edit. Esperado: TODOS os 7 campos pré-preenchidos com os valores salvos.

    **Check 3 (FORM-03):** Tab Cadastros > Produtos. Filtrar/buscar por `AU001`. Clicar no botão ImageIcon (ou Pencil) na linha do AU001. No dialog: alterar a descrição para algo descritivo (ex: `Driver 24V 100W IP20 — coringa AU001`) e fazer upload de uma imagem qualquer (JPG/PNG < 2MB). Salvar. Esperado: toast de sucesso, lista atualiza com a nova descrição e a imagem aparece ao reabrir o dialog.

    **Check 4 (FORM-04):** Tab Cadastros > Produtos. Escolher um SKU master qualquer (não-coringa, ex: qualquer LM*). Clicar no botão ImageIcon inline (NÃO o Pencil). Confirmar: dialog abre em modo edit. Fazer upload de imagem nova. Salvar. Esperado: imagem aparece no row do produto após reload.

    **Check 5 (SQL persistência FORM-02):** Via mcp__plugin_supabase, executar:
    ```sql
    SELECT id, nome, data_nascimento, endereco, banco, agencia, conta, tipo_conta, pix
    FROM public.arquitetos
    WHERE banco IS NOT NULL OR endereco IS NOT NULL OR data_nascimento IS NOT NULL
    ORDER BY updated_at DESC NULLS LAST
    LIMIT 5;
    ```
    Esperado: pelo menos a linha do arquiteto editado no Check 2 aparece com todos os 7 campos preenchidos.

    Após os 5 checks, criar `SMOKE-RESULTS.md` com tabela:
    ```markdown
    # Phase 8 — Smoke Results

    **Executado em:** {YYYY-MM-DD HH:MM} em https://orcamentosaura.com.br
    **Operador:** Lenny (admin) + Claude Code (Playwright MCP + Supabase MCP)

    | # | Requirement | Check | Resultado | Observações |
    |---|-------------|-------|-----------|-------------|
    | 1 | FORM-01 | Cliente só com Nome | ✅/❌ | ... |
    | 2 | FORM-02 | Arquiteto + 7 campos hydratam | ✅/❌ | ... |
    | 3 | FORM-03 | AU001 descrição + imagem editáveis | ✅/❌ | ... |
    | 4 | FORM-04 | ImageIcon inline em SKU master | ✅/❌ | ... |
    | 5 | FORM-02 SQL | Persistência confirmada via DB | ✅/❌ | output do SELECT |

    ## Regressões verificadas (sem quebra)
    - Wizard 3 passos: criar orçamento end-to-end ainda funciona
    - Drive RLS: admin vê tudo, colab vê só o seu (não tocado, mas spot check rápido)

    ## Bugs encontrados
    - {lista ou "nenhum"}

    ## Decisão
    - [x] Phase 8 aprovada para fechamento
    OU
    - [ ] Bugs bloqueantes — abrir tasks de fix antes de fechar
    ```
  </how-to-verify>
  <resume-signal>
    Lenny: digite "aprovado" se os 5 checks passaram e SMOKE-RESULTS.md está documentado, ou descreva os bugs encontrados.
  </resume-signal>
  <acceptance_criteria>
    - `.planning/phases/08-cadastros-opcionalizar-imagens-manuais/SMOKE-RESULTS.md` existe
    - Contém tabela com 5 linhas (FORM-01, FORM-02 UI, FORM-03, FORM-04, FORM-02 SQL)
    - Cada linha tem resultado explícito (✅ ou ❌)
    - Se algum ❌, contém seção "Bugs encontrados" listando o problema
    - Decisão final está marcada (aprovado ou bloqueado)
  </acceptance_criteria>
  <done>Phase 8 validada em prod ou bugs documentados pra correção.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ArquitetoDialog form → arquitetos.insert/update | Input do admin atravessa pra DB com 7 campos novos (incluindo dados bancários — PII) |
| Smoke action → prod DB | Operações reais em produção durante smoke |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | I (Information disclosure) | arquitetos.banco/agencia/conta/pix em prod sem RLS | accept | Phase 9 ativará RLS imediatamente depois (back-to-back). Até lá, Admin.tsx só é acessível via AdminRoute; ArquitetoDialog em modo edit também só admin abre na lista da tab Cadastros. Risco aceito por curta janela e por controle de acesso UI |
| T-08-13 | T (Tampering) | payload null-safe | mitigate | `trim() || null` em todos os 7 campos textuais + `dataNascimento || null` para date. Vazio nunca persistido como string vazia (regressão SQL) |
| T-08-14 | I (Information disclosure) | Smoke vai criar dado real de teste em prod | mitigate | Smoke usa nomes prefixados `Smoke` para fácil cleanup posterior; arquiteto existente é editado com dados realistas mas removíveis. SMOKE-RESULTS documenta IDs criados para auditoria |
</threat_model>

<verification>
- Build verde após expansão do ArquitetoDialog
- 5 checks de smoke documentados em SMOKE-RESULTS.md
- Persistência DB confirmada via SQL direto
- Sem regressão no wizard / Drive
</verification>

<success_criteria>
Phase 8 entregue ponta-a-ponta: schema em prod + 4 mudanças UI + smoke pós-push. SMOKE-RESULTS.md serve como gate de fechamento da phase. Pronto pra Phase 9 (RLS) começar.
</success_criteria>

<output>
Após completar, criar `.planning/phases/08-cadastros-opcionalizar-imagens-manuais/08-05-SUMMARY.md` consolidando:
- Diff de ArquitetoDialog (linhas finais vs 101 originais)
- Link pro SMOKE-RESULTS.md
- Status FORM-01..04 cada (✅ entregue / ❌ bloqueado)
- Próximos passos (Phase 9 RLS desbloqueada)
</output>
