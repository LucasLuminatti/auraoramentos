---
phase: 08-cadastros-opcionalizar-imagens-manuais
plan: 04
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/integrations/supabase/types.ts
  - .planning/phases/08-cadastros-opcionalizar-imagens-manuais/PUSH-LOG.md
autonomous: false
requirements: [FORM-02]
must_haves:
  truths:
    - "Migration 20260512000001_arquitetos_expand_fields.sql aplicada em prod via supabase db push"
    - "Tabela public.arquitetos em prod tem 7 colunas novas + index idx_arquitetos_data_nascimento"
    - "src/integrations/supabase/types.ts foi regenerado e reflete as 7 colunas novas em Database['public']['Tables']['arquitetos']['Row']"
    - "PUSH-LOG.md documenta counts pré/pós push (rows em arquitetos antes e depois — devem ser idênticos)"
  artifacts:
    - path: "src/integrations/supabase/types.ts"
      provides: "Types regenerados pós-push com 7 campos novos em arquitetos Row/Insert/Update"
      contains: "data_nascimento"
    - path: ".planning/phases/08-cadastros-opcionalizar-imagens-manuais/PUSH-LOG.md"
      provides: "Log do push com timestamp, output do CLI, counts pré/pós, smoke SQL"
  key_links:
    - from: "supabase db push"
      to: "public.arquitetos (prod DB)"
      via: "CLI Supabase com SUPABASE_ACCESS_TOKEN"
      pattern: "Applied migration"
---

<objective>
**[BLOCKING — gate de prod]** Aplicar a migration `20260512000001_arquitetos_expand_fields.sql` em produção via `supabase db push`, regenerar `src/integrations/supabase/types.ts` para refletir as 7 colunas novas, e documentar o push em `PUSH-LOG.md` com counts pré/pós (mesmo padrão da Phase 7 Plan 07-04).

Purpose: Sem esta plan, FORM-02 (Plan 08-05) não compila — os campos `data_nascimento`, `endereco`, `banco`, `agencia`, `conta`, `tipo_conta`, `pix` não existem no tipo `Database['public']['Tables']['arquitetos']` até que types sejam regenerados a partir do schema live. Build/TS passam falsamente sem este push (types vêm da config local, não da live DB), criando verification false-positive.
Output: types.ts atualizado + PUSH-LOG.md + smoke SQL confirmando colunas em prod.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-cadastros-opcionalizar-imagens-manuais/08-CONTEXT.md
@.planning/phases/07-schema-prep-v1-1/07-04-PLAN.md
@.planning/phases/07-schema-prep-v1-1/PUSH-LOG.md

<interfaces>
<!-- Padrão de PUSH-LOG estabelecido na Phase 7 07-04 — replicar estrutura -->

Comandos canônicos:
- Push: `supabase db push` (PowerShell ou bash; precisa de `SUPABASE_ACCESS_TOKEN` em env ou login prévio)
- Types regen: `npx supabase gen types typescript --linked > src/integrations/supabase/types.ts`
- Smoke SQL (via mcp__plugin_supabase ou via Supabase Dashboard SQL Editor):
  ```sql
  SELECT column_name, data_type, is_nullable
  FROM information_schema.columns
  WHERE table_schema='public' AND table_name='arquitetos'
    AND column_name IN ('data_nascimento','endereco','banco','agencia','conta','tipo_conta','pix')
  ORDER BY column_name;
  ```
  Esperado: 7 linhas, todas `is_nullable=YES`, `data_nascimento` é `date`, demais são `text`.
- Smoke SQL para index:
  ```sql
  SELECT indexname FROM pg_indexes
  WHERE schemaname='public' AND tablename='arquitetos'
    AND indexname='idx_arquitetos_data_nascimento';
  ```
  Esperado: 1 linha.
- Counts pré/pós (proteção contra surpresa de dados):
  ```sql
  SELECT COUNT(*) FROM public.arquitetos;
  ```
  Roda antes e depois do push — devem ser idênticos (migration é aditiva, zero data churn).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] Aplicar migration em prod via supabase db push + regenerar types + documentar PUSH-LOG</name>
  <files>
    src/integrations/supabase/types.ts
    .planning/phases/08-cadastros-opcionalizar-imagens-manuais/PUSH-LOG.md
  </files>
  <read_first>
    - supabase/migrations/20260512000001_arquitetos_expand_fields.sql (a migration criada na Plan 08-01 — confirmar que existe ANTES de tentar push)
    - .planning/phases/07-schema-prep-v1-1/PUSH-LOG.md (template estrutural — replicar layout: timestamp, count pré, comando, output, count pós, smoke results)
    - .planning/phases/07-schema-prep-v1-1/07-04-PLAN.md (referência do padrão de smoke pós-push)
  </read_first>
  <action>
    Executar a sequência abaixo em ordem (NÃO pular passos — cada passo gera evidência pro PUSH-LOG):

    **Passo 1 — Pre-flight (count pré):**
    Via `mcp__plugin_supabase` (preferido — projeto já linkado) OU via Supabase Dashboard SQL Editor, executar:
    ```sql
    SELECT COUNT(*) AS arquitetos_count_pre FROM public.arquitetos;
    ```
    Anotar o número (será comparado no Passo 5).

    **Passo 2 — Push da migration:**
    Da pasta `auraoramentos/`, executar:
    ```
    supabase db push
    ```
    Em PowerShell, garantir que `$env:SUPABASE_ACCESS_TOKEN` está setado (se houver erro de auth, abortar e pedir ao Lenny pra rodar `supabase login` manualmente).

    Capturar o output completo (especialmente a linha `Applied migration 20260512000001_arquitetos_expand_fields.sql`).

    Se push falhar com erro de schema (ex: coluna já existe, conflito), abortar e marcar a plan como FAILED com o erro completo no PUSH-LOG — não tentar workaround sem ler o erro completo.

    **Passo 3 — Regenerar types TypeScript:**
    ```
    npx supabase gen types typescript --linked > src/integrations/supabase/types.ts
    ```
    Verificar via grep que o arquivo contém a string `data_nascimento` dentro do bloco `arquitetos: {` (use leitura ou `grep -n "data_nascimento" src/integrations/supabase/types.ts`). Esperado: pelo menos 6 ocorrências (Row + Insert + Update, para `data_nascimento`, `endereco`, `banco`, `agencia`, `conta`, `tipo_conta`, `pix` — múltiplas por seção).

    **Passo 4 — Smoke SQL pós-push:**
    Executar via mcp__plugin_supabase ou Dashboard:
    ```sql
    SELECT column_name, data_type, is_nullable
    FROM information_schema.columns
    WHERE table_schema='public' AND table_name='arquitetos'
      AND column_name IN ('data_nascimento','endereco','banco','agencia','conta','tipo_conta','pix')
    ORDER BY column_name;
    ```
    Esperado: 7 linhas. `data_nascimento` deve ter `data_type='date'`; as demais 6 devem ter `data_type='text'`. Todas com `is_nullable='YES'`.

    Em seguida:
    ```sql
    SELECT indexname FROM pg_indexes
    WHERE schemaname='public' AND tablename='arquitetos'
      AND indexname='idx_arquitetos_data_nascimento';
    ```
    Esperado: 1 linha.

    **Passo 5 — Count pós (verificar zero data churn):**
    ```sql
    SELECT COUNT(*) AS arquitetos_count_pos FROM public.arquitetos;
    ```
    Deve ser IGUAL ao count do Passo 1. Se diferir, abortar e investigar.

    **Passo 6 — Lint + TS check com types novos:**
    ```
    npm run lint
    npx tsc --noEmit -p tsconfig.app.json
    ```
    Devem passar (ArquitetoDialog ainda não foi atualizado — Plan 08-05 — mas o TS deve compilar pois as colunas novas são opcionais no Row e Insert).

    **Passo 7 — Documentar em PUSH-LOG.md:**
    Criar `.planning/phases/08-cadastros-opcionalizar-imagens-manuais/PUSH-LOG.md` com:
    ```markdown
    # Phase 8 — Push Log

    **Migration:** 20260512000001_arquitetos_expand_fields.sql
    **Aplicada em:** {YYYY-MM-DD HH:MM:SS America/Sao_Paulo}
    **Operador:** Claude Code (gsd-execute-phase) + Lenny

    ## Pré-flight
    - `SELECT COUNT(*) FROM public.arquitetos` → {N}

    ## Push CLI
    ```
    {output completo do `supabase db push`, incluindo a linha "Applied migration 20260512000001..."}
    ```

    ## Smoke SQL pós-push
    | column_name | data_type | is_nullable |
    |-------------|-----------|-------------|
    | agencia | text | YES |
    | banco | text | YES |
    | conta | text | YES |
    | data_nascimento | date | YES |
    | endereco | text | YES |
    | pix | text | YES |
    | tipo_conta | text | YES |

    Index `idx_arquitetos_data_nascimento`: ✅ presente

    ## Pós-flight
    - `SELECT COUNT(*) FROM public.arquitetos` → {N} (idêntico ao pré — zero data churn)

    ## Types regen
    - `npx supabase gen types typescript --linked > src/integrations/supabase/types.ts` executado
    - 7 colunas novas presentes em `Database['public']['Tables']['arquitetos']`

    ## Gate
    - [x] Migration aplicada
    - [x] Types regenerados
    - [x] Smoke SQL OK
    - [x] Lint + TS OK
    ```

    **NÃO mexer em:**
    - Outras migrations (não rodar `db reset` em hipótese alguma)
    - Schema de outras tabelas
    - Arquivo da migration (não editar pós-push)
  </action>
  <verify>
    <automated>cd auraoramentos && grep -q "data_nascimento" src/integrations/supabase/types.ts && grep -q "endereco" src/integrations/supabase/types.ts && grep -q "pix" src/integrations/supabase/types.ts && grep -q "tipo_conta" src/integrations/supabase/types.ts && test -f .planning/phases/08-cadastros-opcionalizar-imagens-manuais/PUSH-LOG.md && grep -q "Applied migration" .planning/phases/08-cadastros-opcionalizar-imagens-manuais/PUSH-LOG.md && npm run lint && npx tsc --noEmit -p tsconfig.app.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` executou com sucesso e output contém `Applied migration 20260512000001_arquitetos_expand_fields.sql` (literal copiado no PUSH-LOG)
    - `src/integrations/supabase/types.ts` contém literal `data_nascimento` (pelo menos 3 ocorrências — Row/Insert/Update do arquitetos)
    - `src/integrations/supabase/types.ts` contém literais `endereco`, `banco`, `agencia`, `conta`, `tipo_conta`, `pix` (cada um pelo menos 3x)
    - `.planning/phases/08-cadastros-opcionalizar-imagens-manuais/PUSH-LOG.md` existe e contém: timestamp ISO, count pré, output do CLI, tabela smoke SQL com as 7 colunas, count pós (idêntico ao pré), checklist do gate marcado
    - Smoke SQL pós-push retornou exatamente 7 linhas com data_types corretos (date para data_nascimento, text para as demais)
    - Smoke SQL retornou index `idx_arquitetos_data_nascimento` presente
    - `npm run lint` exit 0 com types regenerados
    - `npx tsc --noEmit -p tsconfig.app.json` exit 0 com types regenerados
    - `SELECT COUNT(*) FROM public.arquitetos` é idêntico antes e depois do push (zero data churn)
  </acceptance_criteria>
  <done>Migration em prod. Types regenerados. PUSH-LOG escrito. Plan 08-05 (ArquitetoDialog FORM-02 UI) liberada para começar — agora os campos novos existem no tipo TS e o submit do dialog vai persistir corretamente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supabase CLI (local) → prod DB | DDL atravessa pra produção via push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | T (Tampering) | prod schema durante push | mitigate | Migration é transação atômica BEGIN/COMMIT (Plan 08-01); falha em qualquer ALTER reverte tudo. Count pré/pós valida zero data churn |
| T-08-10 | R (Repudiation) | push sem audit trail | mitigate | PUSH-LOG.md captura timestamp, operador, output completo do CLI, smoke SQL — auditável post-mortem |
| T-08-11 | E (Elevation of privilege) | SUPABASE_ACCESS_TOKEN | accept | Token é do owner (Lenny) — risco aceito; rotação fora do escopo da phase |
</threat_model>

<verification>
- Migration aplicada em prod (output CLI confirma)
- 7 colunas + 1 index presentes via smoke SQL
- types.ts regenerado e contém os 7 campos
- PUSH-LOG.md documenta evidência completa
- Lint + TS verde
- Zero data churn (count idêntico pré/pós)
</verification>

<success_criteria>
Schema FORM-02 em prod. Types TS refletem o schema. Plan 08-05 pode implementar a UI sem `any`/cast. PUSH-LOG documenta tudo no padrão Phase 7.
</success_criteria>

<output>
Após completar, criar `.planning/phases/08-cadastros-opcionalizar-imagens-manuais/08-04-SUMMARY.md` referenciando o PUSH-LOG.md e confirmando os 6 critérios do gate (migration / types / smoke colunas / smoke index / lint / TS).
</output>
