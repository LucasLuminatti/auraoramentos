---
phase: 16-c-lculo-metragem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260611000001_sync_passadas_padrao.sql
autonomous: false
requirements: [CALC-03]
user_setup:
  - service: supabase
    why: "Aplicar UPDATE de sync passadas_padrao na base de produção (histórico de migrations diverge — db push é inseguro)"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: ".env.local (já presente) — ou colar SQL no Supabase Studio > SQL Editor"
    dashboard_config:
      - task: "Rodar o UPDATE de sync (SQL Editor) e confirmar contagem de linhas afetadas"
        location: "Supabase Studio > SQL Editor (projeto jkewlaezvrbuicmncqbj, sa-east-1)"

must_haves:
  truths:
    - "Perfis da família light_50 têm passadas_padrao = 3 na base (não 1)"
    - "Perfis de famílias com regra (embutir_sobrepor_30, etc.) têm passadas_padrao alinhado com regras_compatibilidade_perfil"
    - "A migration é idempotente (re-rodar não muda nada) e aditiva (só UPDATE em product_variants)"
  artifacts:
    - path: "supabase/migrations/20260611000001_sync_passadas_padrao.sql"
      provides: "Migration de sync passadas_padrao (CALC-03 prerequisite)"
      contains: "UPDATE public.product_variants"
  key_links:
    - from: "product_variants.passadas_padrao"
      to: "regras_compatibilidade_perfil.passadas_padrao"
      via: "UPDATE ... FROM ... WHERE familia_perfil match"
      pattern: "FROM public.regras_compatibilidade_perfil"
---

<objective>
Criar e aplicar uma migration de sync que alinha `product_variants.passadas_padrao` com os valores corretos em `regras_compatibilidade_perfil` (CALC-03 / D-12). Hoje `passadas_padrao` está no DEFAULT 1 para todas as famílias (a sync nunca foi feita), então perfil `light_50` sugere 1 passada em vez de 3. Esta migration é **[BLOCKING]**: a UI de passadas editáveis (Plano 02) NÃO pode ser entregue sem ela.

Purpose: Sem a sync, o dropdown de passadas (Plano 02) mostraria max=1 para light_50 e o usuário não conseguiria selecionar 3 passadas — o requisito CALC-03 ficaria visualmente entregue mas funcionalmente quebrado.
Output: Arquivo de migration idempotente + UPDATE aplicado na base de produção + histórico reconciliado via `migration repair`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-c-lculo-metragem/16-CONTEXT.md
@.planning/phases/16-c-lculo-metragem/16-RESEARCH.md

<interfaces>
<!-- Tabelas/colunas relevantes — verificadas no RESEARCH.md -->
product_variants.passadas_padrao  -- INTEGER, adicionado com DEFAULT 1 em 20260319000001
product_variants.familia_perfil   -- text, família do perfil
product_variants.tipo_produto     -- 'perfil' | 'driver' | 'fita' | ...
regras_compatibilidade_perfil.familia_perfil    -- chave de join
regras_compatibilidade_perfil.passadas_padrao   -- valor correto (light_50 = 3, várias famílias = 2)

-- Famílias com passadas_padrao > 1 (seed verificado em 20260319000002):
-- light_50=3 | embutir_sobrepor_30=2 | light_nano_30=2 | alojamento=2
-- no_frame_bilateral=2 | no_frame_wide=2 | wall_washer=2 | trik=2 | fk=2 | sanca=2 | pendente=2
-- Todas as demais famílias: 1 (já correto pelo DEFAULT)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever a migration de sync passadas_padrao</name>
  <read_first>
    - .planning/phases/16-c-lculo-metragem/16-RESEARCH.md (§"Risk Area 1" + §"Safe Migration Application" — SQL exato e procedimento)
    - .planning/phases/16-c-lculo-metragem/16-CONTEXT.md (D-12)
    - supabase/migrations/20260319000002_regras_compatibilidade_perfil.sql (seed das regras — fonte da verdade dos valores)
    - supabase/migrations/20260319000001_campos_tecnicos_produtos.sql (ADD COLUMN passadas_padrao DEFAULT 1)
    - supabase/migrations/20260610000001_tipo_produto_correcao_catalogos.sql (estilo da migration mais recente)
  </read_first>
  <files>supabase/migrations/20260611000001_sync_passadas_padrao.sql</files>
  <action>
Criar o arquivo `supabase/migrations/20260611000001_sync_passadas_padrao.sql` com EXATAMENTE este conteúdo (idempotente + aditivo, conforme RESEARCH.md §"Risk Area 1"):

```sql
-- Migration: sincronizar passadas_padrao de product_variants com regras_compatibilidade_perfil
-- Phase 16 / CALC-03 (D-12) — DEVE ser aplicada antes do unlock da UI de passadas (Plano 02)
-- Idempotente (IS DISTINCT FROM) · Aditivo (apenas UPDATE em product_variants)

BEGIN;

UPDATE public.product_variants pv
  SET passadas_padrao = rcp.passadas_padrao
FROM public.regras_compatibilidade_perfil rcp
WHERE pv.familia_perfil = rcp.familia_perfil
  AND pv.tipo_produto = 'perfil'
  AND pv.passadas_padrao IS DISTINCT FROM rcp.passadas_padrao;

COMMIT;
```

NÃO adicionar `ALTER TABLE`, `DROP`, ou recriação de view. NÃO tocar a view `produtos`. O UPDATE cobre TODAS as famílias de perfil (não só light_50) — alinha embutir_sobrepor_30=2, light_nano_30=2, etc., conforme a tabela do RESEARCH.md. O `IS DISTINCT FROM` garante idempotência e segurança contra NULL.
  </action>
  <verify>
  <automated>test -f supabase/migrations/20260611000001_sync_passadas_padrao.sql && grep -q "UPDATE public.product_variants" supabase/migrations/20260611000001_sync_passadas_padrao.sql && grep -q "regras_compatibilidade_perfil" supabase/migrations/20260611000001_sync_passadas_padrao.sql && grep -q "IS DISTINCT FROM" supabase/migrations/20260611000001_sync_passadas_padrao.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260611000001_sync_passadas_padrao.sql` existe
    - Contém a string literal `UPDATE public.product_variants`
    - Contém `FROM public.regras_compatibilidade_perfil`
    - Contém `pv.tipo_produto = 'perfil'`
    - Contém `IS DISTINCT FROM rcp.passadas_padrao`
    - NÃO contém `ALTER TABLE`, `DROP`, nem `CREATE VIEW` / `CREATE OR REPLACE VIEW` (grep -L)
  </acceptance_criteria>
  <done>Migration idempotente e aditiva escrita conforme SQL do RESEARCH.md, sem DDL destrutivo.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Aplicar a migration em produção + reconciliar histórico</name>
  <read_first>
    - .planning/phases/16-c-lculo-metragem/16-RESEARCH.md (§"Safe Migration Application" — procedimento aprovado: SQL Editor OU service role + migration repair)
    - supabase/migrations/20260611000001_sync_passadas_padrao.sql (o SQL a aplicar)
  </read_first>
  <files>supabase/migrations/20260611000001_sync_passadas_padrao.sql</files>
  <what-built>
Migration de sync `passadas_padrao` (Task 1). O histórico de migrations do projeto DIVERGE do repo — `supabase db push` é inseguro pois re-aplicaria migrations locais já presentes em prod (ex.: 20260602000001 product_variants_ativo, que recria a view `produtos` com coluna `ativo` e quebraria `useProdutoSearch`). Por isso a aplicação é manual + `migration repair`, conforme procedimento aprovado pelo Lenny na Phase 14.
  </what-built>
  <action>
Esta é uma tarefa de aplicação manual em produção (checkpoint:human-action) — o executor NÃO deve rodar `supabase db push`. Conduzir o operador pelos passos abaixo e aguardar confirmação:

PASSO 1 — Aplicar o UPDATE (escolha um caminho):

  Caminho A (recomendado, zero risco): Supabase Studio > SQL Editor > colar e rodar:
    UPDATE public.product_variants pv
      SET passadas_padrao = rcp.passadas_padrao
    FROM public.regras_compatibilidade_perfil rcp
    WHERE pv.familia_perfil = rcp.familia_perfil
      AND pv.tipo_produto = 'perfil'
      AND pv.passadas_padrao IS DISTINCT FROM rcp.passadas_padrao;

  Caminho B (service role via CLI): usar SUPABASE_SERVICE_ROLE_KEY de .env.local com psql apontando para o projeto jkewlaezvrbuicmncqbj.

PASSO 2 — Confirmar o resultado no SQL Editor:
    SELECT familia_perfil, passadas_padrao, COUNT(*)
    FROM public.product_variants
    WHERE tipo_produto = 'perfil' AND familia_perfil = 'light_50'
    GROUP BY familia_perfil, passadas_padrao;
  Esperado: light_50 → passadas_padrao = 3 (não 1).

PASSO 3 — Reconciliar histórico local (NÃO usar `supabase db push`):
    supabase migration repair --status applied 20260611000001
  </action>
  <how-to-verify>
Rodar o PASSO 2 (SELECT) no SQL Editor e confirmar `light_50 → passadas_padrao = 3`. Reportar quantas linhas o UPDATE afetou e o passadas_padrao de light_50 após rodar.
  </how-to-verify>
  <verify>
  <automated>MISSING — verificação é manual em produção (SQL Editor SELECT confirma light_50 → passadas_padrao = 3); aplicação requer service-role/Studio que o executor não pode automatizar com segurança dada a divergência de histórico</automated>
  </verify>
  <acceptance_criteria>
    - UPDATE executado em produção (Caminho A ou B)
    - SELECT de verificação retorna `light_50 → passadas_padrao = 3`
    - `supabase migration repair --status applied 20260611000001` executado com sucesso
    - `supabase db push` NÃO foi executado
  </acceptance_criteria>
  <done>UPDATE aplicado em prod, light_50 com passadas_padrao=3 confirmado por SELECT, histórico reconciliado via migration repair, sem db push.</done>
  <resume-signal>Digite "aplicado" com a contagem de linhas afetadas e o passadas_padrao de light_50, ou descreva o erro.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → banco de produção | UPDATE direto via service role / SQL Editor cruza para dados de produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | UPDATE em product_variants | mitigate | WHERE escopa por `tipo_produto = 'perfil'` + match de familia_perfil; nenhuma outra coluna tocada; `IS DISTINCT FROM` evita writes desnecessários; transação BEGIN/COMMIT |
| T-16-02 | Denial of Service | perda/corrupção de dados na sync | mitigate | Migration é aditiva (só atualiza passadas_padrao, não deleta linhas); idempotente; verificação pós-aplicação por SELECT antes de seguir |
| T-16-03 | Elevation/Info | service-role key (.env.local) | accept | Key já existente no ambiente local do Lenny; não commitada; uso pontual; alternativa SQL Editor não expõe a key |
| T-16-04 | Tampering | divergência de histórico ao aplicar | mitigate | Proibido `supabase db push`; aplicação isolada + `migration repair --status applied` conforme procedimento aprovado (Phase 14) |
</threat_model>

<verification>
- `npm run build` continua passando (migration é SQL puro, não afeta build/types).
- SELECT em prod confirma `light_50.passadas_padrao = 3`.
- Migration idempotente: re-rodar o UPDATE afeta 0 linhas.
</verification>

<success_criteria>
- Migration `20260611000001_sync_passadas_padrao.sql` existe, idempotente e aditiva.
- Em produção, perfis `light_50` têm `passadas_padrao = 3` (e demais famílias alinhadas com a regra).
- Histórico reconciliado via `migration repair`, sem `db push`.
- Pré-requisito CALC-03 satisfeito antes do Plano 02 (UI de passadas).
</success_criteria>

<output>
After completion, create `.planning/phases/16-c-lculo-metragem/16-01-SUMMARY.md`
</output>
