---
phase: 16-c-lculo-metragem
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/types/orcamento.ts
  - src/lib/pdfTemplates/v2.ts
  - src/components/Step2Ambientes.tsx
  - src/components/AmbienteCard.tsx
autonomous: true
requirements: [CALC-01, CALC-02, CALC-03]

must_haves:
  truths:
    - "Sistema com fita e sem perfil cuja metragemManual é null ou 0 NÃO avança do Step 2 para o Step 3 — mostra aviso e bloqueia"
    - "Sistema totalmente vazio (sem fita, sem driver, sem perfil) é avisado e removido ao avançar, sem bloquear"
    - "Ao informar/alterar comprimento ou quantidade do perfil, a metragem aparece na descrição no formato ' — 2,5m' (vírgula decimal)"
    - "O sufixo de metragem não duplica em edições sucessivas e preserva o texto manual da descrição"
    - "Passadas é um Select editável restrito a [1..passadasPadrao] da família, pré-selecionando o padrão; light_50 oferece até 3"
    - "Rascunhos antigos (metragemManual null, perfil null, sem passadasPadrao) abrem sem crash e recebem o bloqueio no gate"
  artifacts:
    - path: "src/types/orcamento.ts"
      provides: "Campo passadasPadrao no ItemPerfil + helper aplicarSufixoMetragem"
      contains: "passadasPadrao"
    - path: "src/components/Step2Ambientes.tsx"
      provides: "Gate CALC-01 (bloqueio metragem) + remoção de sistema vazio (D-06) no handleNext"
      contains: "metragemManual"
    - path: "src/components/AmbienteCard.tsx"
      provides: "Sufixo na descrição (3 sites) + Select de passadas restrito por família"
      contains: "aplicarSufixoMetragem"
  key_links:
    - from: "src/components/Step2Ambientes.tsx handleNext"
      to: "onNext (setStep(3))"
      via: "guard que bloqueia antes de chamar onNext quando há metragem inválida"
      pattern: "metragemManual"
    - from: "src/components/AmbienteCard.tsx (3 onChange/select)"
      to: "sis.perfil.descricao"
      via: "aplicarSufixoMetragem(descricao, comprimentoPeca, quantidade)"
      pattern: "aplicarSufixoMetragem"
    - from: "src/components/AmbienteCard.tsx Select passadas"
      to: "sis.perfil.passadasPadrao"
      via: "[1,2,3].filter(n => n <= passadasPadrao)"
      pattern: "passadasPadrao"
---

<objective>
Patch atômico (D-14) que entrega CALC-01, CALC-02 e CALC-03 num único concern, sem dividir os 5 sites de cálculo. Inclui: (1) gate de bloqueio de metragem inválida + remoção de sistema vazio no Step 2; (2) sufixo de metragem gerenciado na descrição do perfil em 3 call-sites do AmbienteCard via helper idempotente; (3) Select de passadas editável restrito por família, com novo campo opcional `passadasPadrao` no `ItemPerfil`. Revisar os 5 sites de cálculo no mesmo commit para confirmar que o comportamento pós-gate não mudou (o `|| 0` permanece intacto; `isSistemaVazio` NÃO muda).

Purpose: Garantir que nenhuma fita/perfil suma do orçamento como R$0 silencioso, que a metragem apareça automaticamente na descrição, e que as passadas respeitem a regra por família — tudo de forma backwards-compatible com rascunhos antigos.
Output: 4 arquivos modificados, build verde, sem regressão no wizard/PDF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-c-lculo-metragem/16-CONTEXT.md
@.planning/phases/16-c-lculo-metragem/16-RESEARCH.md

<interfaces>
<!-- Contratos verificados nesta sessão — usar diretamente, sem explorar o codebase -->

From src/types/orcamento.ts:
```typescript
export interface ItemPerfil {
  id: string; codigo: string; descricao: string;
  comprimentoPeca: 1 | 2 | 3;
  quantidade: number;
  passadas: 1 | 2 | 3;
  precoUnitario: number; precoMinimo: number;
  imagemUrl?: string;
  familia_perfil?: string | null;
  // ... (driver_restr_*, somente_baby)
}
export interface SistemaIluminacao {
  id: string;
  perfil: ItemPerfil | null;
  fita: ItemFitaLED;       // fita.codigo: string ('' quando não selecionada)
  driver: ItemDriver;      // driver.codigo: string ('' quando não selecionado)
  metragemManual: number | null;  // usado quando perfil = null
  passadasManual: 1 | 2 | 3;
  local?: string | null;
}
export function calcularMetragemTotal(perfil: ItemPerfil): number; // comprimentoPeca * quantidade
export function calcularDemandaFita(sistema: SistemaIluminacao): number; // sem perfil: (metragemManual||0)*(passadasManual||1)
```

From src/components/Step2Ambientes.tsx (gate point, L34-40):
```typescript
const handleNext = () => {
  if (ambientes.length === 0) { toast.error("Adicione pelo menos um ambiente"); return; }
  onNext();  // ← Index.tsx:192 onNext={() => setStep(3)}; gate entra ANTES daqui
};
```

From src/components/AmbienteCard.tsx:
```typescript
const updateSistema = (index: number, sis: SistemaIluminacao) => { /* ... */ };  // L75
// perfil select handler: handleSelectProdutoSistema(produto, sistemaIndex, 'perfil')  L201
// passadasAuto = (produto.passadas ?? base.passadas) as 1|2|3   L203 (produto.passadas = passadas_padrao após sync)
// comprimento Select onChange: L500 | quantidade Input onChange: L511 | Badge passadas (a substituir): L515
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tipo passadasPadrao + helper aplicarSufixoMetragem + revisão dos 5 sites de cálculo</name>
  <read_first>
    - src/types/orcamento.ts (tipos L36-87, cálculos L121-236)
    - src/lib/pdfTemplates/v2.ts (isSistemaVazio L89-93)
    - .planning/phases/16-c-lculo-metragem/16-RESEARCH.md (§"Risk Area 2" 5 sites; §"Risk Area 3" sufixo; §"Pitfall 3/4")
    - .planning/phases/16-c-lculo-metragem/16-CONTEXT.md (D-08, D-09, D-10, D-14)
  </read_first>
  <files>src/types/orcamento.ts, src/lib/pdfTemplates/v2.ts</files>
  <action>
Em `src/types/orcamento.ts`:

1. Adicionar ao `interface ItemPerfil` (após `familia_perfil`) o campo opcional:
   `passadasPadrao?: 1 | 2 | 3;`
   (opcional → backwards-compatible com snapshots antigos; per RESEARCH.md §"Pitfall 4" / D-11).

2. Adicionar e EXPORTAR o helper `aplicarSufixoMetragem` (per RESEARCH.md §"Risk Area 3", D-09/D-10). Colocar perto de `calcularMetragemTotal`:
```typescript
/**
 * Aplica/regenera o sufixo de metragem na descrição do perfil (CALC-02 / D-09).
 * Remove qualquer sufixo " — N,Nm" anterior antes de re-anexar, preservando o texto manual.
 * Formato pt-BR: " — 2,5m" (travessão + espaço, vírgula decimal, sem espaço antes de "m").
 */
export function aplicarSufixoMetragem(descricaoBase: string, comprimentoPeca: number, quantidade: number): string {
  const baseStripped = descricaoBase.replace(/ — \d+(,\d+)?m$/, '').trimEnd();
  const metragem = comprimentoPeca * quantidade;
  const metragemFormatada = metragem % 1 === 0
    ? `${metragem}m`
    : `${metragem.toString().replace('.', ',')}m`;
  return `${baseStripped} — ${metragemFormatada}`;
}
```
   ATENÇÃO regex: o travessão é U+2014 (—, "em dash"), não hífen. O strip e o append devem usar o MESMO caractere para a idempotência funcionar.

3. Revisão atômica dos 5 sites (D-14) — CONFIRMAR (não alterar a matemática):
   - `calcularDemandaFita` (L126): manter `(sis.metragemManual || 0) * (sis.passadasManual || 1)` no caminho sem perfil. NÃO trocar por throw/early-return (RESEARCH §"Pitfall 1") — o fix é o gate (Task 2), não o cálculo.
   - `calcularConsumoW` (L143), `calcularQtdDrivers` (L156), `calcularSubtotalSistemaSemFita` (L234): sem mudança de lógica — derivam de calcularDemandaFita. Apenas confirmar que continuam coerentes.

Em `src/lib/pdfTemplates/v2.ts`:
   - `isSistemaVazio` (L89-93): NÃO ALTERAR. Per RESEARCH.md §"Pitfall 2" e D-07, ele retorna true para metragem null/0; o gate do Step 2 impede esses sistemas de chegarem ao PDF upstream — a consistência D-06/D-07 é garantida pela cadeia lógica, não por mudar esta função. Deixar o comentário/código intactos. (Este site é listado em D-14 só para revisão dentro do commit atômico, confirmando que não precisa mudar.)
  </action>
  <verify>
  <automated>npx tsc --noEmit -p tsconfig.app.json && grep -q "passadasPadrao" src/types/orcamento.ts && grep -q "export function aplicarSufixoMetragem" src/types/orcamento.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/orcamento.ts` contém `passadasPadrao?: 1 | 2 | 3;` dentro de `interface ItemPerfil`
    - `src/types/orcamento.ts` contém `export function aplicarSufixoMetragem(`
    - O helper contém o regex `/ — \d+(,\d+)?m$/` (travessão em dash)
    - `calcularDemandaFita` ainda contém `(sis.metragemManual || 0)` (não foi trocado por throw/return)
    - `src/lib/pdfTemplates/v2.ts` `isSistemaVazio` permanece com `calcularDemandaFita(sis) === 0` (inalterado)
    - `npx tsc --noEmit -p tsconfig.app.json` passa sem erros
  </acceptance_criteria>
  <done>Tipo estendido (campo opcional), helper exportado e idempotente, 5 sites revisados sem mudança de matemática. Type-check verde.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Gate CALC-01 (bloqueio metragem inválida) + remoção de sistema vazio no Step2Ambientes</name>
  <read_first>
    - src/components/Step2Ambientes.tsx (handleNext L34-40; addAmbiente/updateAmbiente/removeAmbiente; Step2Props)
    - src/pages/Index.tsx (L192 onNext={() => setStep(3)})
    - .planning/phases/16-c-lculo-metragem/16-RESEARCH.md (§"Risk Area 2" — algoritmo do gate, isSistemaCompletamenteVazio)
    - .planning/phases/16-c-lculo-metragem/16-CONTEXT.md (D-01..D-07)
  </read_first>
  <files>src/components/Step2Ambientes.tsx</files>
  <action>
Estender `handleNext` em `src/components/Step2Ambientes.tsx` (mantendo o guard existente de `ambientes.length === 0`). Inserir a lógica ANTES de `onNext()`:

```typescript
const handleNext = () => {
  if (ambientes.length === 0) {
    toast.error("Adicione pelo menos um ambiente");
    return;
  }

  // CALC-01 (D-01..D-05): bloquear sistemas com fita, sem perfil, metragem null/0
  const sistemasInvalidos: string[] = [];
  // D-06/D-07: detectar sistemas totalmente vazios para remover (não bloqueia)
  const ambientesLimpos = ambientes.map((amb) => {
    amb.sistemas.forEach((sis, idx) => {
      const totalmenteVazio = !sis.fita.codigo && !sis.driver.codigo && !sis.perfil;
      if (totalmenteVazio) return; // removido abaixo
      if (sis.fita.codigo && !sis.perfil) {
        const metragemInvalida = !sis.metragemManual || sis.metragemManual <= 0;
        if (metragemInvalida) {
          sistemasInvalidos.push(`${amb.nome} — Sistema ${idx + 1}`);
        }
      }
    });
    return {
      ...amb,
      sistemas: amb.sistemas.filter(
        (sis) => !(!sis.fita.codigo && !sis.driver.codigo && !sis.perfil)
      ),
    };
  });

  if (sistemasInvalidos.length > 0) {
    toast.error("Informe uma metragem válida para este sistema antes de continuar.", {
      description: sistemasInvalidos.join(" · "),
    });
    return; // BLOQUEIO (D-02)
  }

  const removidos =
    ambientes.reduce((acc, a) => acc + a.sistemas.length, 0) -
    ambientesLimpos.reduce((acc, a) => acc + a.sistemas.length, 0);

  if (removidos > 0) {
    onChange(ambientesLimpos); // remove vazios (D-06)
    toast.info(
      removidos === 1
        ? "1 sistema vazio foi removido do orçamento."
        : `${removidos} sistemas vazios foram removidos do orçamento.`
    );
  }

  onNext(); // não bloqueia para vazios
};
```

Notas:
- `null` e `0` tratados igual via `!sis.metragemManual || sis.metragemManual <= 0` (D-03). NÃO expor a diferença ao usuário.
- Mesmo comportamento para rascunho antigo e dado novo (D-04) — a lógica não distingue origem; rascunho antigo com `metragemManual: null` cai em `metragemInvalida`.
- A mensagem de bloqueio usa a copy literal do D-05.
- `onChange` já é a prop existente (`Step2Props.onChange`); não criar novo mecanismo.
- A ordem importa: bloqueio de metragem ANTES da remoção de vazios — se há sistema inválido, não avança e não remove nada.
  </action>
  <verify>
  <automated>npx tsc --noEmit -p tsconfig.app.json && grep -q "Informe uma metragem válida para este sistema antes de continuar" src/components/Step2Ambientes.tsx && grep -q "totalmenteVazio" src/components/Step2Ambientes.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `handleNext` mantém o guard `ambientes.length === 0`
    - Contém a copy literal `Informe uma metragem válida para este sistema antes de continuar.`
    - Bloqueia (`return` sem `onNext()`) quando `sis.fita.codigo && !sis.perfil && (!sis.metragemManual || sis.metragemManual <= 0)`
    - Remove sistemas onde `!fita.codigo && !driver.codigo && !perfil` via `onChange`, com toast informativo, SEM bloquear
    - `npx tsc --noEmit -p tsconfig.app.json` passa
  </acceptance_criteria>
  <done>Gate bloqueia metragem null/0 (fita sem perfil) e remove sistemas vazios sem bloquear; rascunho antigo recebe o bloqueio sem crash.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sufixo de metragem (3 sites) + marcador inline + Select de passadas no AmbienteCard</name>
  <read_first>
    - src/components/AmbienteCard.tsx (handleSelectProdutoSistema perfil L201-218; comprimento Select L500; quantidade Input L511; Badge passadas L513-516; metragem L519)
    - src/types/orcamento.ts (aplicarSufixoMetragem + ItemPerfil.passadasPadrao — Task 1)
    - .planning/phases/16-c-lculo-metragem/16-RESEARCH.md (§"Risk Area 3" sufixo; §"Code Examples" badge inline + dropdown passadas; §"Pitfall 4")
    - .planning/phases/16-c-lculo-metragem/16-CONTEXT.md (D-08..D-13)
  </read_first>
  <files>src/components/AmbienteCard.tsx</files>
  <action>
Importar `aplicarSufixoMetragem` de `@/types/orcamento` no topo (junto dos outros imports de orcamento).

CALC-02 — sufixo em 3 sites (D-08/D-09/D-10):

1. Seleção do perfil (L201-218, dentro de `handleSelectProdutoSistema`, `component === 'perfil'`):
   - Computar `passadasAuto` (já existe).
   - Setar `passadasPadrao: passadasAuto` no objeto perfil (novo campo — alimenta o Select).
   - Trocar `descricao: produto.descricao` por:
     `descricao: aplicarSufixoMetragem(produto.descricao, base.comprimentoPeca, base.quantidade),`

2. Comprimento (Select onChange, L500): ao mudar `comprimentoPeca`, regenerar a descrição:
```typescript
onValueChange={(v) => {
  const novoComp = Number(v) as 1 | 2 | 3;
  updateSistema(si, { ...sis, perfil: { ...sis.perfil!, comprimentoPeca: novoComp,
    descricao: aplicarSufixoMetragem(sis.perfil!.descricao, novoComp, sis.perfil!.quantidade) } });
}}
```

3. Quantidade (Input onChange, L511): ao mudar `quantidade`, regenerar:
```typescript
onChange={(e) => {
  const raw = e.target.value;
  const qtd = raw === "" ? 0 : (parseInt(raw) || 0);
  updateSistema(si, { ...sis, perfil: { ...sis.perfil!, quantidade: qtd,
    descricao: aplicarSufixoMetragem(sis.perfil!.descricao, sis.perfil!.comprimentoPeca, qtd) } });
}}
```
   O Input de descrição (`readOnly`, L496) continua exibindo `sis.perfil.descricao` — o sufixo aparece automaticamente e flui para Resumo/PDF sem mudança extra.

CALC-03 — Select de passadas (D-11/D-12), substituir o Badge readonly em L515:
```tsx
<Select
  value={String(sis.perfil.passadas)}
  onValueChange={(v) => updateSistema(si, { ...sis, perfil: { ...sis.perfil!, passadas: Number(v) as 1 | 2 | 3 } })}
>
  <SelectTrigger className="w-20 h-8"><SelectValue /></SelectTrigger>
  <SelectContent>
    {[1, 2, 3]
      .filter((n) => n <= (sis.perfil!.passadasPadrao ?? 3))
      .map((n) => <SelectItem key={n} value={String(n)}>{n}</SelectItem>)}
  </SelectContent>
</Select>
```
   Fallback `?? 3` para snapshots antigos sem `passadasPadrao` (degradação segura — mostra todas as opções, não bloqueia). Após a sync do Plano 01, light_50 chega com `passadasPadrao = 3`.

CALC-01 — marcador inline (D-02), no header do sistema (mesma posição do badge de voltagem divergente, ~L411). Adicionar um Badge condicional:
```tsx
{(() => {
  const semPerfilEInvalido = !!sis.fita.codigo && !sis.perfil && (!sis.metragemManual || sis.metragemManual <= 0);
  return semPerfilEInvalido ? (
    <Badge variant="destructive" className="text-[10px] px-1.5 py-0">⚠ Metragem obrigatória</Badge>
  ) : null;
})()}
```
   Reativo (sem esperar o clique em "Próximo"), atrelado ao mesmo critério do gate (Task 2).
  </action>
  <verify>
  <automated>npx tsc --noEmit -p tsconfig.app.json && grep -q "aplicarSufixoMetragem" src/components/AmbienteCard.tsx && grep -q "passadasPadrao" src/components/AmbienteCard.tsx && grep -q "Metragem obrigatória" src/components/AmbienteCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `aplicarSufixoMetragem` importado e chamado em 3 locais (seleção de perfil, onChange de comprimento, onChange de quantidade)
    - Objeto perfil na seleção seta `passadasPadrao: passadasAuto`
    - O Badge readonly `{sis.perfil.passadas}× (auto)` foi substituído por um `<Select>` filtrado por `n <= (sis.perfil.passadasPadrao ?? 3)`
    - Badge inline `⚠ Metragem obrigatória` presente, condicionado a `fita.codigo && !perfil && metragem null/0`
    - `npx tsc --noEmit -p tsconfig.app.json` passa
  </acceptance_criteria>
  <done>Descrição reflete metragem (sufixo idempotente), passadas editável restrita por família, marcador inline de metragem inválida visível no card.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| colaborador → cálculo do orçamento | inputs numéricos (metragemManual, comprimentoPeca, quantidade, passadas) alimentam cálculos e descrição |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Tampering | metragemManual = 0/null silencioso | mitigate | Gate em handleNext bloqueia avanço (Step 2 → 3) quando fita sem perfil e metragem inválida; marcador inline reativo no card |
| T-16-06 | Tampering | passadas fora do limite da família | mitigate | Select restrito a `[1..passadasPadrao]` (server-truth via sync DB do Plano 01); valor inválido nem aparece como opção |
| T-16-07 | Tampering | sufixo de metragem cumulativo/injetado | mitigate | `aplicarSufixoMetragem` faz strip do sufixo anterior via regex ancorado (`$`) antes de re-anexar; formato controlado pelo sistema |
| T-16-08 | Info Disclosure | sistema com fita+metragem 0 sumindo do PDF | mitigate | Gate impede que cheguem ao Step 3/PDF; `isSistemaVazio` inalterado (consistência por cadeia lógica, D-07) |
| T-16-09 | DoS | crash ao abrir rascunho antigo (sem passadasPadrao/metragem) | mitigate | Campo `passadasPadrao` opcional + fallback `?? 3`; gate trata null/0 sem fix silencioso (D-04) |
</threat_model>

<verification>
- `npm run build` passa (type-check + bundle).
- `npx tsc --noEmit -p tsconfig.app.json` sem erros.
- `npm run lint` sem novos erros.
- Wizard manual (Playwright na Fase de validação): fita sem perfil + metragem vazia → bloqueia com a copy do D-05; perfil com comprimento 2,5 → descrição mostra ` — 2,5m`; light_50 → Select de passadas oferece 1/2/3; sistema vazio → aviso de remoção ao avançar.
</verification>

<success_criteria>
- CALC-01: sistema fita-sem-perfil com metragem null/0 bloqueia o avanço; vazio é removido sem bloquear.
- CALC-02: metragem aparece na descrição (` — 2,5m`), sem duplicar e preservando texto manual.
- CALC-03: passadas editável restrita por família, pré-selecionando o padrão; light_50 até 3.
- Backwards-compat: rascunhos antigos abrem sem crash, sem fix silencioso, e recebem o bloqueio no gate.
- Patch atômico (D-14): 5 sites revisados num único concern; `isSistemaVazio` e o `|| 0` intactos.
</success_criteria>

<output>
After completion, create `.planning/phases/16-c-lculo-metragem/16-02-SUMMARY.md`
</output>
