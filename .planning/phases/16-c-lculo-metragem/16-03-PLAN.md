---
phase: 16-c-lculo-metragem
plan: 03
type: tdd
wave: 3
depends_on: ["16-02"]
files_modified:
  - src/types/__tests__/sufixoMetragem.test.ts
  - src/components/__tests__/Step2Gate.test.ts
  - src/components/__tests__/AmbienteCardPassadas.test.tsx
autonomous: true
requirements: [CALC-01, CALC-02, CALC-03]

must_haves:
  truths:
    - "Teste prova que metragemManual null E 0 ambos bloqueiam o avanço (fita sem perfil)"
    - "Teste prova que rascunho antigo (metragemManual null, perfil null) é detectado como inválido pelo critério do gate"
    - "Teste prova que aplicarSufixoMetragem é idempotente e preserva o texto manual"
    - "Teste prova que o range de passadas é restrito por passadasPadrao da família"
    - "Teste prova que sistema totalmente vazio é detectado para remoção (não bloqueia)"
  artifacts:
    - path: "src/types/__tests__/sufixoMetragem.test.ts"
      provides: "Cobertura CALC-02 (helper aplicarSufixoMetragem)"
      contains: "aplicarSufixoMetragem"
    - path: "src/components/__tests__/Step2Gate.test.ts"
      provides: "Cobertura CALC-01 (lógica do gate: null/0/old-draft/empty)"
      contains: "metragemManual"
    - path: "src/components/__tests__/AmbienteCardPassadas.test.tsx"
      provides: "Cobertura CALC-03 (range de passadas por passadasPadrao)"
      contains: "passadasPadrao"
  key_links:
    - from: "tests"
      to: "src/types/orcamento.ts + src/components/Step2Ambientes.tsx"
      via: "npm run test"
      pattern: "aplicarSufixoMetragem|metragemManual|passadasPadrao"
---

<objective>
Suíte Vitest cobrindo os três requisitos da fase em seus pontos de maior risco (RESEARCH.md §"Validation Architecture" → Test Map). Foco: a lógica pura testável — o helper de sufixo (idempotência + pt-BR), o critério do gate (null/0/old-draft/empty tratados conforme D-03/D-04/D-06), e a derivação do range de passadas por família (D-11/D-12).

Purpose: Travar os comportamentos críticos antes de regressões — especialmente o sufixo cumulativo (Pitfall 3), a equivalência null≡0 (D-03), e o range de passadas (Pitfall 4). Sem isso, a verificação seria só manual e frágil.
Output: 3 arquivos de teste, `npm run test` verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-c-lculo-metragem/16-RESEARCH.md
@.planning/phases/16-c-lculo-metragem/16-CONTEXT.md

<interfaces>
From src/types/orcamento.ts (após Plano 02):
```typescript
export function aplicarSufixoMetragem(descricaoBase: string, comprimentoPeca: number, quantidade: number): string;
export interface ItemPerfil { /* ... */ passadasPadrao?: 1 | 2 | 3; }
```

Test setup existente: Vitest 3.2.4, jsdom, @testing-library/react. Alias `@/` → `src/` (configurado em vitest.config.ts).
Exemplos de testes existentes para estilo: src/lib/__tests__/produtoDescricao.test.ts, src/components/__tests__/PrecosBatch.test.tsx.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes do helper aplicarSufixoMetragem (CALC-02)</name>
  <read_first>
    - src/types/orcamento.ts (aplicarSufixoMetragem)
    - src/lib/__tests__/produtoDescricao.test.ts (estilo de teste de string puro)
    - .planning/phases/16-c-lculo-metragem/16-RESEARCH.md (§"Pitfall 3"; §Test Map CALC-02)
  </read_first>
  <files>src/types/__tests__/sufixoMetragem.test.ts</files>
  <behavior>
    - `aplicarSufixoMetragem("PERFIL X", 1, 2)` → `"PERFIL X — 2m"` (inteiro, sem decimal)
    - `aplicarSufixoMetragem("PERFIL X", 1, 1)` com comprimento 2.5 simulado → valor com vírgula: ex. `comprimentoPeca` não é fracionário no tipo (1|2|3), então testar metragem fracionária via `(1, 2.5)` → `"PERFIL X — 2,5m"` (vírgula decimal)
    - Idempotência: aplicar duas vezes não duplica → `aplicarSufixoMetragem(aplicarSufixoMetragem("PERFIL X", 2, 1), 3, 1)` === `"PERFIL X — 3m"`
    - Preserva texto manual: `aplicarSufixoMetragem("PERFIL EMBUTIR SALA — 2m", 2, 2)` → `"PERFIL EMBUTIR SALA — 4m"` (substitui sufixo, mantém "EMBUTIR SALA")
    - Sem sufixo prévio + metragem inteira não deixa vírgula
  </behavior>
  <action>
Criar `src/types/__tests__/sufixoMetragem.test.ts` importando `aplicarSufixoMetragem` de `@/types/orcamento`. Cobrir os casos do bloco <behavior>. Usar o caractere travessão em dash (—, U+2014) literal nas asserções — deve casar com o usado no helper. Incluir um caso explícito de re-aplicação para provar a idempotência (anti-Pitfall 3) e um caso com texto manual preservado.
  </action>
  <verify>
  <automated>npm run test -- --run sufixoMetragem</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `src/types/__tests__/sufixoMetragem.test.ts` existe e importa de `@/types/orcamento`
    - Cobre: metragem inteira (`— 2m`), fracionária com vírgula (`— 2,5m`), idempotência (re-aplicação), preservação de texto manual
    - `npm run test -- --run sufixoMetragem` passa (todos verdes)
  </acceptance_criteria>
  <done>Helper de sufixo coberto: pt-BR, idempotência e preservação de texto manual provadas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes da lógica do gate CALC-01 (null/0/old-draft/empty)</name>
  <read_first>
    - src/components/Step2Ambientes.tsx (handleNext — critério metragemInvalida + totalmenteVazio)
    - src/types/orcamento.ts (SistemaIluminacao, Ambiente)
    - .planning/phases/16-c-lculo-metragem/16-RESEARCH.md (§Test Map CALC-01; §"Risk Area 2" algoritmo)
    - .planning/phases/16-c-lculo-metragem/16-CONTEXT.md (D-03, D-04, D-06)
  </read_first>
  <files>src/components/__tests__/Step2Gate.test.ts</files>
  <behavior>
    - Sistema com `fita.codigo` preenchido, `perfil = null`, `metragemManual = null` → metragem inválida (bloqueia)
    - Mesmo sistema com `metragemManual = 0` → metragem inválida (bloqueia) — null ≡ 0 (D-03)
    - Mesmo sistema com `metragemManual = 12` → válido (não bloqueia)
    - Sistema com perfil presente e `metragemManual = null` → NÃO bloqueia (metragem só é exigida sem perfil)
    - Sistema totalmente vazio (`fita.codigo=''`, `driver.codigo=''`, `perfil=null`) → detectado para remoção, não conta como inválido (D-06)
    - Rascunho antigo (metragemManual null, perfil null, fita.codigo preenchido) → cai em inválido sem crash (D-04)
  </behavior>
  <action>
Criar `src/components/__tests__/Step2Gate.test.ts`. Como `handleNext` é um closure interno do componente, extrair os PREDICADOS testáveis em vez de renderizar:

Opção preferida — testar predicados puros: replicar (ou, se o executor preferir e for trivial, exportar do componente) os critérios:
```typescript
const metragemInvalida = (sis) => !!sis.fita.codigo && !sis.perfil && (!sis.metragemManual || sis.metragemManual <= 0);
const totalmenteVazio  = (sis) => !sis.fita.codigo && !sis.driver.codigo && !sis.perfil;
```
Se for trivial exportar esses dois helpers de `Step2Ambientes.tsx` (export nomeado), faça-o e importe no teste — preferível a duplicar a lógica. Caso contrário, replicar os predicados no teste com um comentário apontando para o arquivo fonte.

Construir fixtures de `SistemaIluminacao` mínimos (fita/driver com `codigo` string, metragemManual variando) e asserir o resultado de cada predicado conforme o bloco <behavior>. Incluir explicitamente o caso null e o caso 0 como dois testes separados provando a equivalência (D-03), e o caso rascunho-antigo.
  </action>
  <verify>
  <automated>npm run test -- --run Step2Gate</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `src/components/__tests__/Step2Gate.test.ts` existe
    - Testes separados provam metragemManual=null E metragemManual=0 ambos inválidos (D-03)
    - Teste prova que metragemManual>0 é válido
    - Teste prova que sistema com perfil não exige metragemManual
    - Teste prova detecção de sistema totalmente vazio (D-06) distinto de inválido
    - `npm run test -- --run Step2Gate` passa
  </acceptance_criteria>
  <done>Critérios do gate cobertos: null≡0, válido, perfil-presente, vazio, rascunho antigo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes do range de passadas por família (CALC-03)</name>
  <read_first>
    - src/components/AmbienteCard.tsx (filtro `[1,2,3].filter(n => n <= (passadasPadrao ?? 3))`)
    - src/types/orcamento.ts (ItemPerfil.passadasPadrao)
    - .planning/phases/16-c-lculo-metragem/16-RESEARCH.md (§Test Map CALC-03; §"Pitfall 4")
    - .planning/phases/16-c-lculo-metragem/16-CONTEXT.md (D-11, D-12)
  </read_first>
  <files>src/components/__tests__/AmbienteCardPassadas.test.tsx</files>
  <behavior>
    - `passadasPadrao = 3` (light_50) → opções `[1, 2, 3]`
    - `passadasPadrao = 2` → opções `[1, 2]` (3 excluído)
    - `passadasPadrao = 1` → opções `[1]`
    - `passadasPadrao` undefined (rascunho antigo) → fallback mostra `[1, 2, 3]` (degradação segura, não bloqueia)
  </behavior>
  <action>
Criar `src/components/__tests__/AmbienteCardPassadas.test.tsx`. Testar a derivação do range (a expressão `[1,2,3].filter(n => n <= (passadasPadrao ?? 3))`) como função pura:
```typescript
const opcoesPassadas = (passadasPadrao?: number) => [1, 2, 3].filter((n) => n <= (passadasPadrao ?? 3));
```
Asserir cada caso do bloco <behavior>. Se o executor preferir testar via render do AmbienteCard, é aceitável, mas o teste de função pura é suficiente e mais robusto — o ponto é provar que o range respeita o limite da família e degrada com segurança para snapshots antigos. Documentar no topo do teste que a expressão espelha o filtro em AmbienteCard.tsx.
  </action>
  <verify>
  <automated>npm run test -- --run AmbienteCardPassadas</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `src/components/__tests__/AmbienteCardPassadas.test.tsx` existe
    - Casos: passadasPadrao=3 → [1,2,3]; =2 → [1,2]; =1 → [1]; undefined → [1,2,3]
    - `npm run test -- --run AmbienteCardPassadas` passa
  </acceptance_criteria>
  <done>Range de passadas restrito por família coberto, incluindo fallback de snapshot antigo.</done>
</task>

<task type="auto">
  <name>Task 4: Rodar a suíte completa e confirmar verde</name>
  <read_first>
    - vitest.config.ts
    - package.json (script test)
  </read_first>
  <files>(nenhum — verificação)</files>
  <action>
Rodar `npm run test` (suíte inteira) e confirmar que os 3 novos arquivos passam e que nenhuma suíte existente (PrecosBatch, produtoDescricao) regrediu. Se algum teste novo falhar por desalinhamento com a implementação do Plano 02 (ex.: travessão diferente, critério do gate), corrigir o TESTE para refletir a implementação real verificada — não reescrever a implementação aqui (este é o plano de testes; mudanças de produção pertencem ao Plano 02).
  </action>
  <verify>
  <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` retorna exit 0
    - Os 3 arquivos novos aparecem como passed
    - Nenhuma suíte preexistente regrediu
  </acceptance_criteria>
  <done>Suíte completa verde com os novos testes de CALC-01/02/03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Plano só adiciona testes; não introduz superfície de ataque |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Tampering | regressão futura em sufixo/gate/passadas | mitigate | Testes travam os comportamentos críticos (null≡0, idempotência, range por família) — CI/`npm run test` pega regressões |
| T-16-11 | (n/a) | testes não tocam dados/produção | accept | Funções puras + fixtures em memória; sem rede, sem DB |
</threat_model>

<verification>
- `npm run test` verde (3 novos arquivos + suíte preexistente).
- Cobertura do Test Map do RESEARCH.md: null/0 bloqueados, old-draft, suffix idempotência, passadas por família, empty removal.
</verification>

<success_criteria>
- CALC-01: testes provam null≡0 bloqueiam, válido passa, vazio detectado, rascunho antigo sem crash.
- CALC-02: testes provam sufixo pt-BR, idempotência, preservação de texto manual.
- CALC-03: testes provam range de passadas por família + fallback de snapshot antigo.
- Suíte completa verde, sem regressão.
</success_criteria>

<output>
After completion, create `.planning/phases/16-c-lculo-metragem/16-03-SUMMARY.md`
</output>
