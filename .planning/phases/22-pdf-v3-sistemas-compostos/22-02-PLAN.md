---
phase: 22-pdf-v3-sistemas-compostos
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - src/components/Step3Revisao.tsx
  - src/pages/OrcamentoDetalhe.tsx
  - src/lib/pdfTemplateVersion.ts
  - src/lib/__tests__/pdfTemplateVersion.test.ts
autonomous: false
requirements: [PDF-03]
tags: [pdf, router, wiring]
must_haves:
  truths:
    - "Ao gerar/salvar um orçamento que contém ao menos um sistema composto, pdf_template_version persiste 3 e o PDF sai no template v3"
    - "Ao gerar/salvar um orçamento sem nenhum composto (só Fita Padrão e/ou luminárias avulsas), pdf_template_version persiste 2 e o PDF sai no template v2 inalterado"
    - "Ao reemitir um orçamento antigo, OrcamentoDetalhe lê pdf_template_version do snapshot (NULL→1, 2→v2, 3→v3) e renderiza a versão correta sem alteração de v1/v2"
    - "A condição de composto é única e compartilhada: ambientes.some(a => a.luminarias.some(l => l.composicao?.length))"
  artifacts:
    - path: "src/lib/pdfTemplateVersion.ts"
      provides: "Helper puro temPlatformComposto/resolverTemplateVersion compartilhado por writer e testes"
      contains: "composicao?.length"
      min_lines: 10
    - path: "src/components/Step3Revisao.tsx"
      provides: "Writer condicional de pdf_template_version (2 ou 3) + templateVersion na chamada de geração"
      contains: "resolverTemplateVersion"
    - path: "src/pages/OrcamentoDetalhe.tsx"
      provides: "Reader que passa pdf_template_version (NULL→1) para o router — já aditivo para 3"
      contains: "pdf_template_version"
  key_links:
    - from: "src/components/Step3Revisao.tsx"
      to: "src/lib/pdfTemplateVersion.ts"
      via: "resolverTemplateVersion(ambientes) → persiste e passa para gerarOrcamentoHtml"
      pattern: "resolverTemplateVersion"
    - from: "src/pages/OrcamentoDetalhe.tsx"
      to: "src/lib/gerarPdfHtml.ts"
      via: "templateVersion: orc.pdf_template_version ?? 1 (router já roteia 3→v3)"
      pattern: "pdf_template_version \\?\\? 1"
---

<objective>
Cabear o **disparo condicional** do template v3: o WRITER (`Step3Revisao`) persiste `pdf_template_version: 3` e gera o PDF v3 **apenas** quando o orçamento contém ao menos um sistema composto — caso contrário persiste `2` e gera v2 inalterado (SC #2). O READER (`OrcamentoDetalhe`) já lê `pdf_template_version ?? 1` e o router (Plan 01) roteia `3 → v3`, `2 → v2`, `NULL → v1`, mantendo snapshots antigos inalterados (SC #3). A condição de composto é centralizada num helper puro e testável, reusando a expressão travada do CONTEXT.md.

Purpose: Fechar PDF-03 — orçamentos com composto saem em v3; tudo o mais permanece v2/v1 sem regressão.
Output: helper `src/lib/pdfTemplateVersion.ts`, writer e reader cabeados, testes do helper, checkpoint visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-pdf-v3-sistemas-compostos/22-CONTEXT.md
@.planning/phases/22-pdf-v3-sistemas-compostos/22-01-PLAN.md

<interfaces>
De src/lib/gerarPdfHtml.ts (após Plan 01 — já roteia 3→v3):
```typescript
export interface PdfParams { /* ... */ templateVersion?: number; }
export async function gerarOrcamentoHtml(params: PdfParams): Promise<string>;
// dispatch: v>=3 → v3; v>=2 → v2; else → v1
```

De src/types/orcamento.ts:
```typescript
export interface Ambiente { id; nome; luminarias: ItemLuminaria[]; sistemas: SistemaIluminacao[]; }
export interface ItemLuminaria { /* ... */ composicao?: ItemComposicao[]; }
```

Call site WRITER (src/components/Step3Revisao.tsx):
- persistirOrcamento() faz UPDATE (~398) e INSERT (~414) com `pdf_template_version: 2` HARDCODED em ambos.
- handlePDF() (~458) chama `gerarOrcamentoHtml({ ..., templateVersion: 2 })` HARDCODED.

Call site READER (src/pages/OrcamentoDetalhe.tsx):
- handleReemitirPdf() (~182) já passa `templateVersion: orc.pdf_template_version ?? 1` — NÃO precisa de mudança lógica (o router do Plan 01 já roteia 3→v3). Confirmar apenas que `pdf_template_version` está no select da query de orçamento.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper resolverTemplateVersion + cabear writer (Step3Revisao) + reader (OrcamentoDetalhe)</name>
  <files>src/lib/pdfTemplateVersion.ts, src/components/Step3Revisao.tsx, src/pages/OrcamentoDetalhe.tsx</files>
  <read_first>
    - src/components/Step3Revisao.tsx (persistirOrcamento ~385-428 com os dois pontos de `pdf_template_version: 2`; handlePDF ~442-466 com `templateVersion: 2`)
    - src/pages/OrcamentoDetalhe.tsx (handleReemitirPdf ~161-184; confirmar que a query de orçamento seleciona pdf_template_version)
    - src/lib/gerarPdfHtml.ts (router pós-Plan 01 — confirmar branch v3)
    - src/types/orcamento.ts (Ambiente / ItemLuminaria.composicao)
    - .planning/phases/22-pdf-v3-sistemas-compostos/22-CONTEXT.md (SC #2 — expressão condicional exata)
  </read_first>
  <action>
**(A) Criar `src/lib/pdfTemplateVersion.ts` (helper puro, fonte única da condição):**
```typescript
import type { Ambiente } from "@/types/orcamento";

/** True quando o orçamento contém ao menos um sistema composto.
 *  Expressão travada (Phase 22 / SC #2): composto = luminária com composicao?.length. */
export function temSistemaComposto(ambientes: Ambiente[]): boolean {
  return ambientes.some(a => a.luminarias.some(l => !!l.composicao?.length));
}

/** Resolve a versão do template do PDF para um orçamento.
 *  3 = tem composto (v3); 2 = sem composto (v2 editorial). Nunca retorna 1 (writer novo). */
export function resolverTemplateVersion(ambientes: Ambiente[]): 2 | 3 {
  return temSistemaComposto(ambientes) ? 3 : 2;
}
```
Imports via alias `@/`.

**(B) Cabear o WRITER em src/components/Step3Revisao.tsx:**
  - Importar `resolverTemplateVersion` de `@/lib/pdfTemplateVersion`.
  - Em `persistirOrcamento()`: computar `const templateVersion = resolverTemplateVersion(ambientes);` UMA vez no topo da função (após o guard inicial) e substituir AMBOS os literais `pdf_template_version: 2` (no UPDATE ~398 e no INSERT ~414) por `pdf_template_version: templateVersion`.
  - Em `handlePDF()`: substituir `templateVersion: 2` (~465) na chamada de `gerarOrcamentoHtml` por `templateVersion: resolverTemplateVersion(ambientes)` (ou reutilizar uma const computada no início de handlePDF). Garantir que o valor passado para gerar o PDF É O MESMO persistido (consistência writer↔render).
  - NÃO mudar mais nada no fluxo (inlineImagensSnapshot, fontes, html2pdf permanecem).

**(C) Confirmar/ajustar o READER em src/pages/OrcamentoDetalhe.tsx:**
  - A linha `templateVersion: orc.pdf_template_version ?? 1` (~182) já é correta e aditiva — o router do Plan 01 roteia `3 → v3`. NÃO alterar a lógica.
  - VERIFICAR que a query Supabase que carrega `orc` inclui a coluna `pdf_template_version` no `.select(...)`. Se NÃO incluir (vier undefined), adicionar `pdf_template_version` à lista de colunas selecionadas para que o reader receba 3 corretamente. Se já incluir (ex: `select("*")`), nenhuma mudança.

**Restrições:** não tocar em v1.ts/v2.ts; não alterar o wizard de 3 passos; manter compat de snapshots antigos (reader NULL→1).
  </action>
  <verify>
    <automated>cd auraoramentos && npx tsc --noEmit -p tsconfig.app.json 2>&1 | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/pdfTemplateVersion.ts` existe e contém `composicao?.length` e `resolverTemplateVersion`
    - `grep -c "pdf_template_version: 2" src/components/Step3Revisao.tsx` retorna 0 (ambos literais substituídos)
    - `grep -n "resolverTemplateVersion" src/components/Step3Revisao.tsx` retorna o import + uso no persist + uso no handlePDF
    - `grep -n "pdf_template_version" src/pages/OrcamentoDetalhe.tsx` confirma reader presente; query de orçamento inclui a coluna (select("*") ou lista com pdf_template_version)
    - `npx tsc --noEmit -p tsconfig.app.json` sem erro novo
  </acceptance_criteria>
  <done>Helper resolverTemplateVersion criado; writer persiste 3 quando há composto e 2 caso contrário (ambos literais substituídos) e passa a mesma versão para a geração; reader lê pdf_template_version (NULL→1) e a coluna está no select.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes do helper de versão (composto→3, sem composto→2, snapshot antigo)</name>
  <files>src/lib/__tests__/pdfTemplateVersion.test.ts</files>
  <read_first>
    - src/lib/pdfTemplateVersion.ts (recém-criado na Task 1)
    - src/types/__tests__/composicao.test.ts (shape literal de Ambiente com composicao para reusar nos fixtures)
    - src/lib/__tests__/produtoDescricao.test.ts (padrão Vitest de lib pura)
  </read_first>
  <behavior>
    - Ambiente com 1 luminária contendo `composicao: [{...}]` → `temSistemaComposto` true, `resolverTemplateVersion` === 3
    - Ambiente só com Fita Padrão em `sistemas[]` e/ou luminárias avulsas SEM composicao → false, `resolverTemplateVersion` === 2
    - Ambiente com `composicao: []` (array vazio) → false → 2 (length 0 é falsy — não é composto)
    - Luminária com `composicao: undefined` (snapshot antigo) → false → 2
    - Mistura: 1 ambiente sem composto + 1 ambiente com composto → true → 3 (some atravessa todos os ambientes)
  </behavior>
  <action>
Criar `src/lib/__tests__/pdfTemplateVersion.test.ts` (Vitest, padrão `describe/it/expect`). Montar fixtures `Ambiente[]` mínimos (cast `as unknown as Ambiente` quando conveniente, como no padrão de composicao.test.ts) cobrindo os 5 casos do bloco `<behavior>`. Asserir `temSistemaComposto` e `resolverTemplateVersion` para cada caso. Reusar o shape de `ItemComposicao` da Phase 19 (papel/quantidade/precoUnitario etc.) para o fixture com composto.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run test -- pdfTemplateVersion 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/__tests__/pdfTemplateVersion.test.ts` existe e cobre os 5 casos do `<behavior>`
    - `npm run test -- pdfTemplateVersion` passa todos os testes
    - `npm run build` sai 0
  </acceptance_criteria>
  <done>Suíte do helper verde provando composto→3, sem-composto→2, array vazio→2, undefined→2, mistura→3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint visual — PDF v3 (com composto) e v2 (sem composto)</name>
  <files>(nenhum — verificação visual)</files>
  <action>Verificação humana assistida por Playwright (pipeline padrão do projeto). Não modifica código. Siga os passos de <how-to-verify> abaixo para validar os 3 cenários (v3 com composto, v2 sem composto, snapshot antigo) e o console limpo.</action>
  <what-built>
    Router v3 condicional cabeado: orçamentos com sistema composto geram PDF v3 (bloco estruturado inline "Sistema Composto N — TIPO" com trilho, módulos, fita modular, driver, acessórios, preço por linha e subtotal do sistema); orçamentos sem composto continuam gerando PDF v2 inalterado. Snapshots antigos (v1/v2) inalterados.
  </what-built>
  <how-to-verify>
    Pré: `npm run dev` rodando (porta 8080). Validação via Playwright MCP (pipeline padrão do projeto — code review + Playwright antes de reportar). Caminhos:
    1. **PDF v3 (com composto):** Montar/abrir um orçamento que tenha um sistema composto (MAGNETO 48V, TINY 24V ou SYSTEM MOLD — montados nas Phases 20/21). No Step 3, gerar o PDF. CONFERIR no PDF: existe um bloco "Sistema Composto N — {TIPO}" dentro do ambiente; o trilho/perfil aparece no topo com SKU+qtd; os módulos aparecem como sub-linhas com SKU/qtd; o driver aparece com qtd+SKU; acessórios obrigatórios (conector/kit) aparecem; cada linha tem preço; há um "Subtotal do sistema"; há o resumo "{W}W total" (e "fita {m}m" se SYSTEM MOLD). A fita modular NÃO aparece no "Resumo de Fitas" global.
    2. **PDF v2 (sem composto):** Abrir/gerar um orçamento só com Fita Padrão e/ou luminárias avulsas (sem composto). CONFERIR: o PDF sai idêntico ao v2 atual (sem bloco composto), o "Resumo de Fitas" aparece normalmente, e o `pdf_template_version` persistido é 2.
    3. **Snapshot antigo:** Reemitir um orçamento antigo (v2 ou pré-Phase-5) em OrcamentoDetalhe — renderiza sem erro e inalterado.
    4. Console do navegador sem erros JS durante as gerações.
  </how-to-verify>
  <resume-signal>Digite "aprovado" se os 3 PDFs saem corretos (v3 com bloco estruturado completo, v2 inalterado, antigo inalterado) e console limpo; ou descreva os problemas observados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente (Step3/Detalhe) → Supabase (orcamentos.pdf_template_version) | gravação de inteiro de versão derivado do conteúdo do orçamento (sem input de usuário livre) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | Tampering | persist de pdf_template_version (2/3) | accept | Valor derivado deterministicamente do snapshot via `resolverTemplateVersion` (sem entrada de usuário); pior caso de manipulação direta no DB = render no template errado (sem impacto de segurança, sem vazamento). RLS de orcamentos já vigente. |
| T-22-05 | Information Disclosure | reader OrcamentoDetalhe (select de pdf_template_version) | accept | Coluna já existente, não-sensível (inteiro de versão); leitura sob a mesma RLS do orçamento que o usuário já acessa. |

Nenhuma ameaça HIGH. Wiring de roteamento client-side, sem novas superfícies de input.
</threat_model>

<verification>
- `npm run build` sai 0.
- `npm run lint` limpo em `Step3Revisao.tsx`, `OrcamentoDetalhe.tsx`, `pdfTemplateVersion.ts`.
- `npm run test -- pdfTemplateVersion` verde (5 casos).
- `grep "pdf_template_version: 2" src/components/Step3Revisao.tsx` vazio (literais substituídos pela versão resolvida).
- Checkpoint visual (Playwright + revisão humana): PDF v3 com bloco completo para orçamento com composto; PDF v2 inalterado para orçamento sem composto; snapshot antigo inalterado; console limpo.
</verification>

<success_criteria>
- Orçamento com ≥1 composto persiste `pdf_template_version: 3` e gera PDF v3 com o bloco estruturado completo (SC #1 fechado fim-a-fim).
- Orçamento sem composto persiste `pdf_template_version: 2` e gera v2 inalterado — disparo condicional via `ambientes.some(a => a.luminarias.some(l => l.composicao?.length))` (SC #2).
- Reemissão de snapshot antigo (NULL→1, 2→v2) renderiza inalterado; router v1/v2/v3 aditivo (SC #3).
</success_criteria>

<output>
Após completar, criar `.planning/phases/22-pdf-v3-sistemas-compostos/22-02-SUMMARY.md` documentando: onde a condição de composto foi centralizada, se a query do reader precisou incluir a coluna, e o resultado do checkpoint visual (v3/v2/antigo).
</output>
