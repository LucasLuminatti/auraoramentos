---
phase: 22-pdf-v3-sistemas-compostos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/pdfTemplates/v3.ts
  - src/lib/gerarPdfHtml.ts
  - src/lib/pdfTemplates/__tests__/v3.test.ts
autonomous: true
requirements: [PDF-03]
tags: [pdf, template, compostos]
must_haves:
  truths:
    - "Um ambiente com luminária que tem composicao?.length renderiza um bloco 'Sistema Composto N — {TIPO}' com trilho/perfil no topo e sub-linhas (módulos, fita modular, driver, acessórios)"
    - "Cada componente do composto mostra preço unitário por linha e o bloco mostra um subtotal do sistema ao fim"
    - "O router gerarOrcamentoHtml dispara o template v3 quando templateVersion >= 3, reusando os helpers/CSS do v2"
    - "buildAtributosMap inclui os códigos de composicao[] no batch lookup de atributos"
    - "O blocoResumoFitas do v2 NÃO foi alterado e continua iterando só sistemas[].fita (fita modular fica fora do resumo global)"
  artifacts:
    - path: "src/lib/pdfTemplates/v3.ts"
      provides: "Template v3 — gerarOrcamentoHtmlV3 + blocoComposto reusando helpers do v2"
      contains: "export function gerarOrcamentoHtmlV3"
      min_lines: 80
    - path: "src/lib/gerarPdfHtml.ts"
      provides: "Router com branch v3 + buildAtributosMap estendido para composicao[]"
      contains: "gerarOrcamentoHtmlV3"
    - path: "src/lib/pdfTemplates/__tests__/v3.test.ts"
      provides: "Testes do bloco composto + guard de v2 inalterado"
      contains: "gerarOrcamentoHtmlV3"
  key_links:
    - from: "src/lib/gerarPdfHtml.ts"
      to: "src/lib/pdfTemplates/v3.ts"
      via: "import gerarOrcamentoHtmlV3 + branch v >= 3"
      pattern: "gerarOrcamentoHtmlV3"
    - from: "src/lib/pdfTemplates/v3.ts"
      to: "src/lib/pdfTemplates/v2.ts"
      via: "reuso de helpers (esc, chip, thumb, agruparPorLocal) e/ou re-export do builder v2 para os blocos não-compostos"
      pattern: "from \"./v2\""
---

<objective>
Criar o template **PDF v3** como camada aditiva que renderiza **sistemas compostos** (MAGNETO 48V / TINY 24V / SYSTEM MOLD) como **bloco inline dentro do ambiente** — trilho/perfil no topo, sub-linhas dos componentes (módulos, fita modular, driver, acessórios), preço por linha e subtotal do sistema (D-01, D-02, D-03, D-04). O v3 reusa os helpers e o CSS do v2 e estende o router `gerarOrcamentoHtml` com um terceiro branch + estende `buildAtributosMap` para os códigos de `composicao[]`.

**CRÍTICO (compatibilidade travada):** o v2 (`src/lib/pdfTemplates/v2.ts`) e o v1 (`src/lib/pdfTemplates/v1.ts`) **NÃO podem ser alterados**. Orçamentos sem composto continuam saindo idênticos no v2; snapshots antigos v1/v2 renderizam inalterados. O `blocoResumoFitas` do v2 NÃO pode passar a varrer `composicao[]` — a fita modular aparece SÓ dentro do bloco composto (D-04).

Purpose: Entregar a apresentação ao cliente dos sistemas compostos montados nas Phases 19-21 (PDF-03), sem regredir o PDF estável.
Output: `src/lib/pdfTemplates/v3.ts` (novo), `src/lib/gerarPdfHtml.ts` (router estendido), testes do bloco composto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-pdf-v3-sistemas-compostos/22-CONTEXT.md

<interfaces>
<!-- Contratos que o executor usa diretamente — não precisa explorar o codebase. -->

De src/types/orcamento.ts:
```typescript
export interface ItemLuminaria {
  id: string; codigo: string; descricao: string; quantidade: number;
  precoUnitario: number; precoMinimo: number; imagemUrl?: string;
  sistema?: string | null;            // 'magneto_48v' | 'tiny_magneto' | 's_mode' | null — fonte do rótulo de TIPO do bloco
  potencia_watts?: number | null; tensao?: number | null;
  composicao?: ItemComposicao[];      // presença => luminária é âncora de sistema composto
}

export interface ItemComposicao {
  id: string; codigo: string; descricao: string; quantidade: number;
  precoUnitario: number; precoMinimo: number; imagemUrl?: string;
  papel: 'modulo' | 'driver_recomendado' | 'driver_obrigatorio' | 'conector_energia'
       | 'kit_fixacao' | 'acessorio_opcional' | 'fita_modular';
  obrigatorio: boolean;
  comprimento?: number;   // metros — módulo difuso (SYSTEM MOLD)
  potenciaW?: number;     // watts individuais — módulo magnético
}

// Funções de cálculo já existentes (importar de @/types/orcamento):
export function calcularSubtotalComposicao(item: ItemLuminaria): number;          // Σ precoUnitario*qtd dos componentes
export function calcularCargaComposicao(c: ItemComposicao[] | undefined): number; // Σ potenciaW*qtd dos papel==='modulo'
export function calcularMetragemModulosDifusos(c: ItemComposicao[] | undefined): number; // Σ comprimento*qtd
export function formatarMoeda(v: number): string;
```

De src/lib/pdfTemplates/v2.ts (helpers a REUSAR — NÃO duplicar):
```typescript
export type AtributosMap = Record<string, { atributos: Record<string, unknown> | null; potencia_watts: number | null }>;
export function gerarOrcamentoHtmlV2(params: PdfParamsV2): string;
// internos do v2 (esc, chip, thumb, agruparPorLocal, construirDescricaoRica via @/lib/produtoDescricao):
//   NÃO são exportados hoje. Ver <action> da Task 1 para a estratégia de reuso permitida.
```

De src/lib/gerarPdfHtml.ts (router atual):
```typescript
export interface PdfParams { clienteNome; projetoNome; colaborador; tipo; ambientes: Ambiente[]; logoBase64?; templateVersion?: number; }
export async function gerarOrcamentoHtml(params: PdfParams): Promise<string>;
// hoje: const v = params.templateVersion ?? 2; if (v >= 2) v2; else v1;
// buildAtributosMap hoje varre: l.codigo (luminarias), sis.fita/driver/perfil.codigo — NÃO varre composicao[]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar src/lib/pdfTemplates/v3.ts — template v3 com bloco composto inline</name>
  <files>src/lib/pdfTemplates/v3.ts</files>
  <read_first>
    - src/lib/pdfTemplates/v2.ts (LEITURA OBRIGATÓRIA INTEIRA — é a base de estilo/estrutura: helpers esc/chip/thumb/agruparPorLocal, rowLuminaria ~100, blocoSistema ~217, blocoAmbiente ~244, blocoResumoFitas ~266, blocoTotal ~302, blocoTermos ~320, gerarOrcamentoHtmlV2 ~358, CSS ~376)
    - src/types/orcamento.ts (linhas 23-57 ItemLuminaria/ItemComposicao; ~183 calcularCargaComposicao; ~192 calcularMetragemModulosDifusos; ~346 calcularSubtotalComposicao; ~340 calcularSubtotalLuminaria; formatarMoeda)
    - src/lib/produtoDescricao.ts (construirDescricaoRica — usado pelo v2 nas rows)
    - .planning/phases/22-pdf-v3-sistemas-compostos/22-CONTEXT.md (D-01 layout inline, D-02 preço por linha + subtotal, D-03 atributo técnico por papel, D-04 fita modular dentro do bloco)
  </read_first>
  <action>
Criar `src/lib/pdfTemplates/v3.ts` que exporta `export function gerarOrcamentoHtmlV3(params: PdfParamsV2): string` (reusar o tipo `PdfParamsV2` e `AtributosMap` do v2 via `import { gerarOrcamentoHtmlV2, type PdfParamsV2, type AtributosMap } from "./v2"`).

**Estratégia de reuso (decisão técnica do planner — NÃO alterar v2.ts):**
Os helpers `esc`, `chip`, `thumb`, `agruparPorLocal` hoje NÃO são exportados de v2.ts. Como **NÃO podemos modificar v2.ts** para orçamentos sem composto, mas PODEMOS adicionar exports nomeados a ele SEM mudar comportamento de render, a abordagem permitida é UMA das duas (escolher a primeira que mantém v2 byte-idêntico em render):
  - **(Preferida)** Adicionar a v2.ts apenas linhas `export` aos helpers já existentes (`export function esc`, `export function chip`, `export function thumb`, `export function agruparPorLocal`) — isso NÃO altera o HTML gerado pelo v2 (mudança puramente de visibilidade de símbolo). Então importá-los no v3. Se escolher esta, a Task 2 do guard valida que `gerarOrcamentoHtmlV2` e o HTML do v2 não mudaram.
  - **(Alternativa)** Reimplementar `esc`/`chip`/`thumb` localmente em v3.ts (cópia byte-idêntica dos helpers do v2) e não tocar em v2.ts. Use esta se preferir zero-toque absoluto no v2.

`gerarOrcamentoHtmlV3` deve reusar a estrutura geral do v2 (header, total geral, termos, CSS) — a forma mais limpa: copiar o corpo de `gerarOrcamentoHtmlV2` como base e substituir o `blocoAmbiente` por uma versão v3 que detecta compostos. NÃO chamar `gerarOrcamentoHtmlV2` aninhado (ele não conhece compostos). Reuse o MESMO bloco `<style>...</style>` do v2 + adicione APENAS as classes CSS novas do bloco composto (ex: `.composto-block`, `.composto-label`, `.composto-resumo`, `.composto-subtotal`, `.comp-sub-row`) seguindo a linguagem visual existente (Inter/Playfair, laranja #E68601, chips).

**Render condicional do ambiente (integration point):** o `blocoAmbiente` do v3 itera `amb.luminarias`. Para cada luminária:
  - se `l.composicao?.length` for truthy → renderizar `blocoComposto(l, atributosMap)` (bloco estruturado)
  - senão → renderizar a linha simples (reusar a mesma lógica de `rowLuminaria` do v2 dentro da tabela de luminárias)
Os `amb.sistemas` (Fita Padrão) continuam renderizados EXATAMENTE como no v2 (agruparPorLocal → blocoSistema → rowFita/rowPerfil/rowDriver) — não tocar nessa parte. O `blocoResumoFitas` global continua iterando SÓ `sistemas[].fita` (D-04 — NÃO varrer composicao[]).

**`blocoComposto(item: ItemLuminaria, atributosMap)` (D-01/D-02/D-03):**
  - Rótulo de tipo (Claude's Discretion aprovado): mapear `item.sistema` → label legível:
    `'magneto_48v' → 'MAGNETO 48V'`, `'tiny_magneto' → 'TINY 24V'`, `'s_mode' → 'SYSTEM MOLD'`, qualquer outro/null → 'SISTEMA COMPOSTO'. Header do bloco: `Sistema Composto {N} — {LABEL}` (N = índice 1-based do composto dentro do ambiente).
  - Resumo do sistema (D-03, topo ou rodapé do bloco): carga total via `calcularCargaComposicao(item.composicao)` → `"{W}W total"`; metragem derivada via `calcularMetragemModulosDifusos(item.composicao)` → quando > 0, `"fita {m}m"`. Ex: `"36W total · fita 0,8m"`. Omitir cada parte quando 0.
  - **Trilho/perfil no topo** = a própria luminária âncora (`item`): SKU `RV{item.codigo}`, qtd, preço unitário, chip de comprimento/tipo quando disponível.
  - **Sub-linhas dos componentes** (ordem sugerida D-03: módulos → fita modular → driver → acessórios), iterar `item.composicao`. Por papel, escolher o chip técnico relevante (D-03):
    - `papel==='modulo'` → chip de `potenciaW`W e/ou `comprimento`m
    - `papel==='fita_modular'` → chips W/m + voltagem + metragem derivada (usar atributos do catálogo se disponíveis em atributosMap[codigo]; metragem = `comprimento` do item)
    - `papel==='driver_recomendado' | 'driver_obrigatorio'` → chip potência + voltagem
    - `papel==='conector_energia' | 'kit_fixacao' | 'acessorio_opcional'` → só descrição (sem chip técnico)
    - cada sub-linha mostra SKU `RV{c.codigo}`, qtd `c.quantidade`, preço `formatarMoeda(c.precoUnitario)` (D-02 — preço por componente)
  - **Subtotal do sistema** ao fim do bloco (D-02): `calcularSubtotalLuminaria(item) + calcularSubtotalComposicao(item)` formatado com `formatarMoeda`, destacado (ex: linha `Subtotal do sistema: {valor}`).
  - **SEGURANÇA (HTML injection):** TODA string derivada de catálogo/snapshot (descricao, codigo, labels) DEVE passar por `esc()` antes de entrar no HTML — exatamente como o v2 faz. NUNCA interpolar `c.descricao`, `item.descricao`, `item.codigo` crus no template.

Imports SEMPRE via alias `@/` para tipos/funções de domínio (`@/types/orcamento`, `@/lib/produtoDescricao`); import relativo `./v2` apenas para reuso interno do mesmo diretório (segue o padrão do router que já usa `./pdfTemplates/v2`).
  </action>
  <verify>
    <automated>cd auraoramentos && npx tsc --noEmit -p tsconfig.app.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/pdfTemplates/v3.ts` existe e contém `export function gerarOrcamentoHtmlV3`
    - `grep -n "function blocoComposto" src/lib/pdfTemplates/v3.ts` retorna ≥1 linha
    - `grep -n "calcularCargaComposicao\|calcularMetragemModulosDifusos\|calcularSubtotalComposicao" src/lib/pdfTemplates/v3.ts` retorna as 3 funções usadas
    - `grep -n "SYSTEM MOLD\|MAGNETO 48V\|TINY 24V" src/lib/pdfTemplates/v3.ts` retorna o mapa de labels
    - `grep -nc "esc(" src/lib/pdfTemplates/v3.ts` retorna ≥4 (toda string de catálogo escapada)
    - `npx tsc --noEmit -p tsconfig.app.json` não emite erro novo em v3.ts
  </acceptance_criteria>
  <done>v3.ts exporta gerarOrcamentoHtmlV3; blocoComposto renderiza trilho no topo + sub-linhas por papel com chip técnico + preço por linha + subtotal do sistema + resumo carga/metragem; todas as strings de catálogo passam por esc().</done>
</task>

<task type="auto">
  <name>Task 2: Branch v3 no router + buildAtributosMap estendido + testes + guard v2 intocado</name>
  <files>src/lib/gerarPdfHtml.ts, src/lib/pdfTemplates/__tests__/v3.test.ts</files>
  <read_first>
    - src/lib/gerarPdfHtml.ts (INTEIRO — router gerarOrcamentoHtml ~75, buildAtributosMap ~43; hoje varre l.codigo + sis.fita/driver/perfil.codigo, NÃO composicao[])
    - src/lib/pdfTemplates/v3.ts (recém-criado na Task 1 — assinatura de gerarOrcamentoHtmlV3)
    - src/lib/pdfTemplates/v2.ts (NÃO MODIFICAR — referência para o guard: blocoResumoFitas ~266 itera só sistemas[].fita)
    - src/types/__tests__/composicao.test.ts (padrão de teste com snapshot literal de composicao — reusar o shape)
    - src/lib/__tests__/produtoDescricao.test.ts (padrão de teste de lib pura)
  </read_first>
  <action>
**(A) Router — branch v3 em `gerarOrcamentoHtml` (src/lib/gerarPdfHtml.ts):**
Importar `gerarOrcamentoHtmlV3` de `./pdfTemplates/v3`. Estender o dispatch para 3 branches mantendo o padrão aditivo:
```typescript
const v = params.templateVersion ?? 2;
if (v >= 3) {
  const atributosMap = await buildAtributosMap(params.ambientes);
  return gerarOrcamentoHtmlV3({ ...params, atributosMap });
}
if (v >= 2) {
  const atributosMap = await buildAtributosMap(params.ambientes);
  return gerarOrcamentoHtmlV2({ ...params, atributosMap });
}
return gerarOrcamentoHtmlV1(params);
```
NÃO mudar o comportamento dos branches v1/v2. NÃO bumpar a default (continua `?? 2`) — a decisão de quando usar 3 é do WRITER (Plan 02).

**(B) buildAtributosMap estendido para composicao[] (mesmo arquivo):**
No loop que coleta `codigos`, ADICIONAR a varredura dos códigos de composição:
```typescript
for (const l of amb.luminarias) {
  if (l.codigo) codigos.add(l.codigo);
  for (const c of l.composicao ?? []) if (c.codigo) codigos.add(c.codigo);
}
```
Manter a varredura existente de `sis.fita/driver/perfil`. Guard `?? []` garante backward-compat (snapshots sem composicao → loop vazio).

**(C) Testes — criar `src/lib/pdfTemplates/__tests__/v3.test.ts` (Vitest):**
  - Helper que monta um `Ambiente` com uma luminária-âncora `sistema: 's_mode'` + `composicao: [{papel:'modulo', comprimento:0.4, quantidade:2, ...}, {papel:'fita_modular', comprimento:0.8, ...}, {papel:'driver_recomendado', ...}]`.
  - Teste 1: `gerarOrcamentoHtmlV3({ ..., atributosMap:{} })` retorna string contendo `Sistema Composto 1 — SYSTEM MOLD`, o SKU do trilho (`RV{codigo}`), o SKU de cada componente, e `Subtotal do sistema`.
  - Teste 2 (rótulos): âncora `sistema:'magneto_48v'` → contém `MAGNETO 48V`; `sistema:'tiny_magneto'` → contém `TINY 24V`.
  - Teste 3 (resumo): carga = `calcularCargaComposicao` aparece como `"{W}W total"` no HTML; metragem derivada aparece quando >0.
  - Teste 4 (esc / HTML injection): luminária com `descricao: 'Trilho <script>x</script> & cia'` → o HTML NÃO contém `<script>` cru (contém `&lt;script&gt;`).
  - Teste 5 (GUARD v2 inalterado / D-04): importar `gerarOrcamentoHtmlV2`; gerar com um ambiente que tem fita modular em composicao[] mas NENHUM sistemas[].fita → o HTML do v2 NÃO contém `RESUMO DE FITAS` (blocoResumoFitas não varre composicao[]). E asserir que `gerarOrcamentoHtmlV2` para um orçamento só-Fita-Padrão é idêntico antes/depois (snapshot de referência inline).
  - Teste 6 (router condicional — em src/lib não há acesso a supabase nos testes; testar a seleção de branch via mock OU via teste separado que verifica que templateVersion 3 chama o builder v3): se mockar supabase for custoso, ao menos asserir que `gerarOrcamentoHtmlV3` é exportado e produz HTML distinto do v2 para o mesmo ambiente com composto.

**(D) GUARD — v2.ts não varre composicao[]:**
Confirmar via grep que `src/lib/pdfTemplates/v2.ts` NÃO contém a string `composicao` (a menos que a Task 1 tenha escolhido a abordagem "Preferida" de só adicionar `export` aos helpers — nesse caso v2.ts pode ter sido tocado SÓ para visibilidade de símbolos, jamais para varrer composicao). O critério é: `grep -n "composicao" src/lib/pdfTemplates/v2.ts` deve retornar VAZIO.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run test -- v3 2>&1 | tail -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "v >= 3\|v>=3" src/lib/gerarPdfHtml.ts` retorna o branch v3
    - `grep -n "gerarOrcamentoHtmlV3" src/lib/gerarPdfHtml.ts` retorna o import e a chamada
    - `grep -n "l.composicao ?? \[\]\|l.composicao ??" src/lib/gerarPdfHtml.ts` retorna a varredura de composicao em buildAtributosMap
    - `grep -n "composicao" src/lib/pdfTemplates/v2.ts` retorna VAZIO (guard D-04)
    - `src/lib/pdfTemplates/__tests__/v3.test.ts` existe; `npm run test -- v3` passa todos os testes
    - `npm run build` sai com código 0
  </acceptance_criteria>
  <done>Router tem 3 branches aditivos (default ?? 2 inalterada); buildAtributosMap inclui códigos de composicao[]; testes v3 verdes cobrindo rótulos, resumo, esc, e guard de v2 inalterado; build verde; v2.ts não varre composicao[].</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot do orçamento (jsonb) → string HTML do PDF | dados de catálogo/cliente (descrições, códigos, nomes de local) atravessam para o DOM renderizado off-screen e rasterizado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering/XSS | blocoComposto em v3.ts (interpolação de descricao/codigo de catálogo no HTML) | mitigate | TODA string derivada de catálogo/snapshot passa por `esc()` (o mesmo helper do v2) antes de entrar no template; Teste 4 prova que `<script>` é escapado. Nenhuma interpolação crua de `descricao`/`codigo`. |
| T-22-02 | Information Disclosure | buildAtributosMap estendido (query a product_variants por códigos de composicao[]) | accept | Mesma query/colunas já usadas para luminarias/fita/driver (codigo, atributos, potencia_watts); nenhum dado novo exposto, nenhum campo sensível; só amplia o conjunto de códigos consultados. RLS de product_variants já vigente. |
| T-22-03 | Denial of Service | render do bloco composto para orçamentos grandes | accept | Geração é client-side, sob controle do próprio colaborador; sem amplificação remota. Peso/perf do PDF é tratado no backlog 999.1 (PDF vetorial), fora deste escopo. |

Nenhuma ameaça HIGH. Superfície aditiva client-side sem novas escritas em DB/rede.
</threat_model>

<verification>
- `npm run build` sai 0 (TypeScript + Vite OK).
- `npm run lint` limpo nos arquivos alterados (`src/lib/pdfTemplates/v3.ts`, `src/lib/gerarPdfHtml.ts`).
- `npm run test -- v3` verde: rótulos de tipo, resumo carga/metragem, escape de HTML, guard de v2 inalterado.
- Guard D-04: `grep "composicao" src/lib/pdfTemplates/v2.ts` vazio.
- O router preserva default `?? 2` e os branches v1/v2 inalterados (revisão de diff).
</verification>

<success_criteria>
- `gerarOrcamentoHtmlV3` renderiza, para cada luminária com `composicao?.length`, um bloco "Sistema Composto N — {TIPO}" com trilho no topo, sub-linhas por papel com chip técnico relevante, preço por linha e subtotal do sistema (SC #1).
- O router `gerarOrcamentoHtml` dispara v3 quando `templateVersion >= 3`, mantendo v1/v2 aditivos e a default `?? 2` (base para SC #2/#3 — o disparo condicional é cabeado no Plan 02).
- v2.ts e v1.ts intocados em comportamento de render; `blocoResumoFitas` não varre `composicao[]` (D-04, base de SC #3).
</success_criteria>

<output>
Após completar, criar `.planning/phases/22-pdf-v3-sistemas-compostos/22-01-SUMMARY.md` documentando: estratégia de reuso de helpers escolhida (export em v2 vs cópia local), classes CSS novas do bloco composto, e confirmação do guard D-04.
</output>
