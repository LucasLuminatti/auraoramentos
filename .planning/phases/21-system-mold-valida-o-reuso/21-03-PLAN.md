---
phase: 21-system-mold-valida-o-reuso
plan: 03
type: execute
wave: 3
depends_on: ["21-01", "21-02"]
files_modified:
  - src/components/Step2Ambientes.tsx
  - src/components/AmbienteCard.tsx
  - tests/advisory-compostos.test.ts
autonomous: false
requirements: [VAL-01, DUP-01]
must_haves:
  truths:
    - "Ao avançar do Step 2, um composto magnético sem driver aplicado gera aviso não-bloqueante"
    - "Ao avançar, um composto sem o conector obrigatório da família gera aviso não-bloqueante"
    - "Ao avançar, um SYSTEM MOLD com metragem derivada > 0 mas sem fita gera aviso não-bloqueante"
    - "O colaborador pode continuar mesmo com avisos (não-bloqueante)"
    - "Clicar Duplicar no card abre seletor de ambiente destino e clona o composto com novos UUIDs em toda a árvore"
    - "O clone aparece no ambiente escolhido e soma corretamente no Step 3"
  artifacts:
    - path: "src/components/Step2Ambientes.tsx"
      provides: "3 novos AdvisoryItem de composto + orquestração de duplicação com seletor de destino"
      contains: "composto-sem-driver"
    - path: "src/components/AmbienteCard.tsx"
      provides: "thread do callback onDuplicarComposto até o ComposicaoCard"
      contains: "onDuplicarComposto"
    - path: "tests/advisory-compostos.test.ts"
      provides: "testes das 3 condições D-03"
      contains: "modular-sem-fita"
  key_links:
    - from: "Step2Ambientes.handleNext loop de compostos"
      to: "itensIncompletos com 3 novos tipos"
      via: "amb.luminarias onde l.composicao?.length"
      pattern: "composto-sem-conector"
    - from: "ComposicaoCard botão Duplicar (onDuplicate)"
      to: "Step2Ambientes seletor de destino + clonarItemLuminaria"
      via: "AmbienteCard onDuplicarComposto → Step2"
      pattern: "clonarItemLuminaria"
---

<objective>
As duas frentes que vivem no dono dos ambientes: **VAL-01** estende o advisory não-bloqueante existente em `Step2Ambientes.handleNext` com 3 condições de composto incompleto (D-03), e **DUP-01** orquestra a duplicação — o botão Duplicar do card (criado no Plan 02) dispara um seletor de ambiente destino no `Step2Ambientes`, que clona o composto via `clonarItemLuminaria` (Plan 01) com novos UUIDs em toda a árvore. Inclui o threading do callback através do `AmbienteCard` e um checkpoint visual humano (Playwright + olho do Lenny) por ser fim de fase com UI nova.

Purpose: fecha as 3 success criteria da fase; entrega aviso e reuso entre ambientes sem remontar.
Output: advisory estendido + duplicação ambiente-a-ambiente funcionando + testes da lógica de aviso.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-system-mold-valida-o-reuso/21-CONTEXT.md
@.planning/phases/21-system-mold-valida-o-reuso/21-RESEARCH.md
@.planning/phases/21-system-mold-valida-o-reuso/21-01-SUMMARY.md
@.planning/phases/21-system-mold-valida-o-reuso/21-02-SUMMARY.md

<interfaces>
<!-- Contratos das Waves 1 e 2 — usar diretamente. -->

De src/types/orcamento.ts (Plan 01):
```typescript
export function calcularMetragemModulosDifusos(composicao: ItemComposicao[] | undefined): number;
export function clonarItemLuminaria(item: ItemLuminaria): ItemLuminaria;
export const REGRAS_COMPOSICAO: Record<string, { conectoresObrigatorios: string[]; ... }>;
// chaves: 'magneto_48v' (LM2338), 'tiny_magneto' (LM3168, LM3169). NÃO tem 's_mode'.
```

AdvisoryItem atual (src/components/Step2Ambientes.tsx:20):
```typescript
interface AdvisoryItem {
  ambienteNome: string;
  tipo: 'fita-sem-driver' | 'driver-sem-fita' | 'perfil-sem-fita' | 'peca-sem-lampada';
  descricao: string;
}
const ADVISORY_LABELS: Record<AdvisoryItem['tipo'], string> = { ... };
```

Loop advisory atual (Step2Ambientes.tsx:120-140) percorre amb.sistemas (Fita Padrão) e amb.luminarias (lâmpada). NÃO percorre composicao hoje.

ComposicaoCardProps.onDuplicate (Plan 02) — callback opcional já recebido pelo card.
AmbienteCardProps atual (src/components/AmbienteCard.tsx:18): { ambiente, onChange, onRemove, onDuplicate? }.
AmbienteCard renderiza ComposicaoCard em src/components/AmbienteCard.tsx:516 com onChange/onRemove/indice.
Step2Ambientes renderiza AmbienteCard em src/components/Step2Ambientes.tsx:165 com onChange/onRemove/onDuplicate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VAL-01 — 3 novos AdvisoryItem de composto incompleto</name>
  <read_first>
    - src/components/Step2Ambientes.tsx (AdvisoryItem:20; ADVISORY_LABELS:33; handleNext:70; loop advisory:120-140; imports:17)
    - src/types/orcamento.ts (REGRAS_COMPOSICAO, calcularMetragemModulosDifusos, ItemComposicao.papel)
    - .planning/phases/21-system-mold-valida-o-reuso/21-RESEARCH.md (Existing Code Map — "Step2Ambientes.handleNext — advisory loop" com o snippet verbatim das 3 condições)
    - tests/ (estrutura de testes existente, se houver; senão criar)
  </read_first>
  <files>src/components/Step2Ambientes.tsx, tests/advisory-compostos.test.ts</files>
  <behavior>
    - Composto com sistema='magneto_48v', módulos presentes, sem item papel='driver_recomendado' → gera 'composto-sem-driver'
    - Composto com sistema='tiny_magneto' sem nenhum dos conectores LM3168/LM3169 → gera 'composto-sem-conector'
    - Composto com sistema='magneto_48v' COM LM2338 presente → NÃO gera 'composto-sem-conector'
    - Composto sistema='s_mode' com metragem derivada > 0 e sem item papel='fita_modular' → gera 'modular-sem-fita'
    - Composto sistema='s_mode' com fita_modular presente → NÃO gera 'modular-sem-fita'
    - Luminaria sem composicao (composicao undefined) → não gera nenhum aviso de composto
    - NÃO gera aviso por "embutir sem kit LM2987" (D-03 — evita ruído)
  </behavior>
  <action>
Em src/components/Step2Ambientes.tsx:

1. Estenda o union `AdvisoryItem.tipo` (linha 22) com os 3 novos tipos:
```typescript
tipo: 'fita-sem-driver' | 'driver-sem-fita' | 'perfil-sem-fita' | 'peca-sem-lampada'
    | 'composto-sem-driver' | 'composto-sem-conector' | 'modular-sem-fita';
```

2. Adicione os labels em ADVISORY_LABELS (linha 33):
```typescript
'composto-sem-driver': 'Sistema composto sem driver aplicado',
'composto-sem-conector': 'Sistema composto sem o conector obrigatório da família',
'modular-sem-fita': 'SYSTEM MOLD sem fita adicionada',
```

3. Importe os helpers (linha 17):
```typescript
import { ..., REGRAS_COMPOSICAO, calcularMetragemModulosDifusos } from "@/types/orcamento";
```

4. No loop de advisory (após o `for (const sis of amb.sistemas)` existente, dentro do `for (const amb of ambientesLimpos)`, ~linha 140), adicione o loop de compostos (snippet verbatim do RESEARCH):
```typescript
for (const lum of amb.luminarias) {
  if (!lum.composicao?.length) continue;
  const comp = lum.composicao;
  const sistema = lum.sistema ?? '';

  // D-03.1: composto magnético sem driver aplicado
  if ((sistema === 'magneto_48v' || sistema === 'tiny_magneto') &&
      !comp.some(c => c.papel === 'driver_recomendado')) {
    itensIncompletos.push({ ambienteNome: amb.nome, tipo: 'composto-sem-driver', descricao: lum.descricao });
  }

  // D-03.2: conector obrigatório da família ausente
  const regras = REGRAS_COMPOSICAO[sistema];
  if (regras && !regras.conectoresObrigatorios.some(sku => comp.some(c => c.codigo === sku))) {
    itensIncompletos.push({ ambienteNome: amb.nome, tipo: 'composto-sem-conector', descricao: lum.descricao });
  }

  // D-03.3: SYSTEM MOLD sem fita
  if (sistema === 's_mode') {
    const metragem = calcularMetragemModulosDifusos(comp);
    const temFita = comp.some(c => c.papel === 'fita_modular');
    if (metragem > 0 && !temFita) {
      itensIncompletos.push({ ambienteNome: amb.nome, tipo: 'modular-sem-fita', descricao: lum.descricao });
    }
  }
}
```
Permanece NÃO-bloqueante — o dialog existente já tem botão de continuar; nada muda no fluxo de decisão (D-02). NÃO usar REGRAS_COMPOSICAO['s_mode'] (não existe — Deferred).

5. Extraia a lógica de detecção para uma função pura exportável para teste, OU teste via uma função helper. Recomendado: crie `tests/advisory-compostos.test.ts` que importa uma função pura `detectarAvisosComposto(amb: Ambiente): AdvisoryItem[]` extraída de Step2Ambientes (mova o loop de compostos para essa função exportada e chame-a no handleNext), cobrindo cada item de `<behavior>`. Isso mantém o loop testável sem montar o componente.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run test -- --run tests/advisory-compostos.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "composto-sem-driver" src/components/Step2Ambientes.tsx`
    - `grep -q "composto-sem-conector" src/components/Step2Ambientes.tsx`
    - `grep -q "modular-sem-fita" src/components/Step2Ambientes.tsx`
    - `grep -q "calcularMetragemModulosDifusos" src/components/Step2Ambientes.tsx`
    - NÃO referencia `REGRAS_COMPOSICAO['s_mode']` nem aviso de kit LM2987
    - `npm run test -- --run tests/advisory-compostos.test.ts` verde cobrindo as 7 behaviors
    - O loop de compostos é adicionado SEM remover/alterar os pushes existentes (fita-sem-driver etc. intactos)
  </acceptance_criteria>
  <done>handleNext gera os 3 avisos de composto, não-bloqueante; testes verdes; avisos legados preservados.</done>
</task>

<task type="auto">
  <name>Task 2: DUP-01 — orquestração da duplicação com seletor de ambiente destino</name>
  <read_first>
    - src/components/Step2Ambientes.tsx (props Step2:26; render AmbienteCard:165; duplicarAmbiente:63 como padrão; estado/dialogs)
    - src/components/AmbienteCard.tsx (AmbienteCardProps:18; render ComposicaoCard:516; map de luminarias:513)
    - src/types/orcamento.ts (clonarItemLuminaria — Plan 01)
    - .planning/phases/21-system-mold-valida-o-reuso/21-RESEARCH.md (Open Question 3 — Pattern 3 + "DUP-01 orchestration"; recomendação: AmbienteCard fires onDuplicarComposto(item) upward)
    - src/components/ui/select.tsx ou dialog.tsx (primitivo para o seletor)
  </read_first>
  <files>src/components/AmbienteCard.tsx, src/components/Step2Ambientes.tsx</files>
  <action>
**Threading do callback (AmbienteCard):**
1. Estenda AmbienteCardProps (linha 18) com:
```typescript
onDuplicarComposto?: (item: ItemLuminaria) => void;   // Phase 21 / DUP-01 (D-05)
```
e adicione ao destructuring (linha 43).

2. No render do ComposicaoCard (linha 516), passe `onDuplicate` que dispara o callback com o item atual:
```tsx
<ComposicaoCard
  key={item.id}
  item={item}
  indice={i}
  onChange={(novo) => updateLuminaria(i, novo)}
  onRemove={() => removeLuminaria(i)}
  onDuplicate={onDuplicarComposto ? () => onDuplicarComposto(item) : undefined}
/>
```

**Orquestração (Step2Ambientes):**
3. No render de AmbienteCard (linha 165), passe o callback:
```tsx
onDuplicarComposto={(item) => iniciarDuplicacaoComposto(item, i)}
```
(`i` é o índice do ambiente de origem.)

4. Implemente o estado + handlers. Se `ambientes.length === 1`, clona direto no ambiente atual (D-05). Se houver múltiplos, abra um seletor de destino (Dialog com shadcn Select, ou RadioGroup listando `ambientes.map(a => a.nome)`):
```typescript
const [dupState, setDupState] = useState<{ item: ItemLuminaria; origemIdx: number } | null>(null);

const iniciarDuplicacaoComposto = (item: ItemLuminaria, origemIdx: number) => {
  if (ambientes.length === 1) {
    inserirCompostoEm(0, item);
    return;
  }
  setDupState({ item, origemIdx });   // abre o seletor
};

const inserirCompostoEm = (destinoIdx: number, item: ItemLuminaria) => {
  const clone = clonarItemLuminaria(item);   // novos UUIDs em toda a árvore (D-06)
  const arr = ambientes.map((a, idx) =>
    idx === destinoIdx ? { ...a, luminarias: [...a.luminarias, clone] } : a
  );
  onChange(arr);
  setDupState(null);
  toast.success(`Sistema duplicado para "${ambientes[destinoIdx].nome}".`);
};
```
Importe `clonarItemLuminaria` e `ItemLuminaria` de `@/types/orcamento`.

5. Renderize o Dialog de seleção quando `dupState` não for null — lista os ambientes (por nome + índice) e ao confirmar chama `inserirCompostoEm(destinoIdx, dupState.item)`. Use primitivos de `@/components/ui/` (Dialog + Select/RadioGroup + Button), design tokens, copy em pt-BR (ex.: título "Duplicar sistema para qual ambiente?").

O cálculo do Step 3 agrupa por código, não por id (decisão v1.2), então o clone soma corretamente.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "onDuplicarComposto" src/components/AmbienteCard.tsx` e `src/components/Step2Ambientes.tsx`
    - `grep -q "clonarItemLuminaria" src/components/Step2Ambientes.tsx`
    - ComposicaoCard recebe `onDuplicate` ligado a `onDuplicarComposto(item)`
    - O Dialog/seletor de destino é renderizado quando há múltiplos ambientes; ambientes.length===1 insere direto
    - inserirCompostoEm usa `clonarItemLuminaria` (não spread raso)
    - `npm run build` passa
  </acceptance_criteria>
  <done>Botão Duplicar → seletor de destino (ou inserção direta se 1 ambiente) → clone com novos UUIDs no ambiente escolhido.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — verificação visual do fluxo SYSTEM MOLD + advisory + duplicação</name>
  <what-built>
    Fluxo SYSTEM MOLD completo (montagem de perfil modular + difusos + fita derivada + driver advisory), aviso não-bloqueante no Step 2→3 para compostos incompletos, e duplicação de composto entre ambientes. Antes do checkpoint, execute o pipeline padrão: code-review do diff + Playwright MCP no fluxo alterado (caminho feliz + edge cases + console limpo + regressão de Fita Padrão).
  </what-built>
  <action>Checkpoint de verificacao humana — sem implementacao. Execute o pipeline padrao (code-review do diff + Playwright MCP) e entao conduza o usuario pelos passos de how-to-verify, pausando para aprovacao.</action>
  <files>(nenhum — verificacao)</files>
  <verify>Verificacao manual pelo usuario conforme how-to-verify.</verify>
  <done>Usuario digita "aprovado" ou lista problemas a corrigir.</done>
  <how-to-verify>
    1. `npm run dev` e abra o wizard (Step 2). Busque "SYSTEM MOLD PERFIL NOFRAME MODULAR 1M BRANCO" → confirme que abre o card de composição modular (badge MODULAR), não item simples.
    2. Adicione 2-3 módulos difusos (busca "DIFUSO") com quantidades → confirme que "Fita necessária: X m" reflete Σ(comprimento × qtd).
    3. Clique "Adicionar fita" → busque e escolha uma fita 24V → confirme que a fita entra com a metragem pré-preenchida e que o painel de driver advisory aparece com botão "Aplicar".
    4. Sem adicionar a fita em outro SYSTEM MOLD, clique "Próximo" → confirme o aviso "SYSTEM MOLD sem fita adicionada" (não-bloqueante — consegue continuar). Teste também composto magnético sem driver e sem conector.
    5. Clique "Duplicar" no header do card → se houver 2+ ambientes, escolha o destino → confirme que o clone aparece no ambiente escolhido e que o Step 3 soma corretamente (sem chaves React duplicadas no console).
    6. REGRESSÃO: monte uma Fita Padrão e um MAGNETO 48V e confirme que funcionam idênticos ao anterior.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clone composto → ambiente destino | árvore de IDs replicada entre ambientes do mesmo orçamento |
| advisory loop → estado client | leitura de composicao para gerar avisos (sem mutação) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | Tampering | clone de composto entre ambientes | mitigate | `clonarItemLuminaria` gera `crypto.randomUUID()` na raiz E em cada ItemComposicao (Plan 01, testado) — sem colisão de id entre origem e destino, evita corromper o ambiente de origem ao editar o clone. Cálculo agrupa por código, não por id. |
| T-21-07 | Information Disclosure | duplicação cross-ambiente | accept | Destino restrito aos ambientes do MESMO orçamento (array local `ambientes`); não cruza orçamentos nem RLS. Sem vazamento. |

Nenhuma ameaça high-severity — operações client-side sobre estado do orçamento em edição; advisory é read-only; clone isolado por UUIDs novos.
</threat_model>

<verification>
- `npm run test -- --run tests/advisory-compostos.test.ts` verde
- `npm run build` verde
- Pipeline: code-review do diff + Playwright MCP (fluxo SYSTEM MOLD + advisory + duplicação + regressão Fita Padrão, console limpo)
- Checkpoint humano aprovado
</verification>

<success_criteria>
- VAL-01: as 3 condições de composto incompleto geram aviso não-bloqueante no Step 2→3
- DUP-01: composto duplicável para ambiente escolhido, novos UUIDs em toda a árvore, soma correta no Step 3
- Nenhuma regressão nos avisos legados nem em Fita Padrão / magnéticos
</success_criteria>

<output>
Após completar, crie `.planning/phases/21-system-mold-valida-o-reuso/21-03-SUMMARY.md`.
</output>
