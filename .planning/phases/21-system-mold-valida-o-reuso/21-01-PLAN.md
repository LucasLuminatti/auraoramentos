---
phase: 21-system-mold-valida-o-reuso
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/orcamento.ts
  - src/types/orcamento.test.ts
  - src/hooks/useProdutoSearch.ts
  - supabase/migrations/20260616000001_sistema_s_mode_system_mold.sql
autonomous: true
requirements: [SIST-03, DUP-01]
must_haves:
  truths:
    - "Buscar um perfil SYSTEM MOLD (LM1998 etc.) retorna o produto com sistema='s_mode', e detectarTipoAncora retorna 'modular'"
    - "A busca de módulos difusos (filtro='modulo_difuso') retorna os 15 difusos PARA FITA LED"
    - "calcularMetragemModulosDifusos soma comprimento × qtd dos módulos com papel='modulo'"
    - "clonarItemLuminaria gera UUID novo na raiz e em cada item de composicao[]"
    - "clonarAmbiente deep-clona composicao[] (não compartilha mais referências)"
    - "Os 5 calc sites de Fita Padrão permanecem byte-idênticos"
  artifacts:
    - path: "src/types/orcamento.ts"
      provides: "calcularMetragemModulosDifusos, parsearComprimentoModulo, clonarItemLuminaria, papel 'fita_modular'"
      contains: "export function calcularMetragemModulosDifusos"
    - path: "src/hooks/useProdutoSearch.ts"
      provides: "filtro 'modulo_difuso'"
      contains: "modulo_difuso"
    - path: "supabase/migrations/20260616000001_sistema_s_mode_system_mold.sql"
      provides: "marca sistema='s_mode' em 12 perfis-âncora + 15 difusos"
      contains: "SET sistema = 's_mode'"
    - path: "src/types/orcamento.test.ts"
      provides: "testes unitários dos novos helpers"
      contains: "calcularMetragemModulosDifusos"
  key_links:
    - from: "useProdutoSearch.ts filtro='modulo_difuso'"
      to: "product_variants.sistema='s_mode' + tipo_produto='acessorio'"
      via: ".eq query"
      pattern: "modulo_difuso"
    - from: "detectarTipoAncora"
      to: "produto.sistema_magnetico === 's_mode'"
      via: "alias sistema_magnetico:sistema"
      pattern: "s_mode"
---

<objective>
Fundação técnica do SYSTEM MOLD e do reuso de compostos: a migration que marca os perfis-âncora e os módulos difusos com `sistema='s_mode'` (sem isso a detecção do fluxo modular não funciona — todos os 87 produtos têm `sistema=NULL` hoje), os helpers puros novos em `orcamento.ts` (metragem derivada, parse de comprimento, clone deep), o fix do bug de clone raso em `clonarAmbiente`, e o novo filtro de busca `'modulo_difuso'`.

Purpose: desbloqueia os fluxos de UI das Waves 2/3 (SIST-03, DUP-01) com lógica pura testável e dados de catálogo corretos, sem tocar em UI nem nos 5 calc sites de Fita Padrão.
Output: 1 migration aplicada, helpers exportados de `orcamento.ts`, `ProdutoFiltro` estendido, suíte de testes verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-system-mold-valida-o-reuso/21-CONTEXT.md
@.planning/phases/21-system-mold-valida-o-reuso/21-RESEARCH.md

<interfaces>
<!-- Contratos existentes que o executor usa diretamente. Extraídos do codebase. -->

ItemComposicao (src/types/orcamento.ts:43) — campo comprimento? JÁ existe (forward-complete Phase 19):
```typescript
export interface ItemComposicao {
  id: string;
  codigo: string;
  descricao: string;
  quantidade: number;
  precoUnitario: number;
  precoMinimo: number;
  imagemUrl?: string;
  papel: 'modulo' | 'driver_recomendado' | 'driver_obrigatorio' | 'conector_energia' | 'kit_fixacao' | 'acessorio_opcional';
  obrigatorio: boolean;
  comprimento?: number;   // metros — usado por SYSTEM MOLD
  potenciaW?: number;
}
```

Padrão de referência — calcularCargaComposicao (src/types/orcamento.ts:183):
```typescript
export function calcularCargaComposicao(composicao: ItemComposicao[] | undefined): number {
  if (!composicao?.length) return 0;
  return composicao
    .filter(c => c.papel === 'modulo')
    .reduce((s, c) => s + (c.potenciaW ?? 0) * c.quantidade, 0);
}
```

Padrão de referência — clonarSistema/clonarAmbiente (src/types/orcamento.ts:591/614):
```typescript
export function clonarAmbiente(amb: Ambiente): Ambiente {
  return {
    ...amb,
    id: crypto.randomUUID(),
    nome: `${amb.nome} (cópia)`,
    luminarias: amb.luminarias.map((l) => ({ ...l, id: crypto.randomUUID() })),  // BUG: composicao[] compartilhada
    sistemas: amb.sistemas.map((sis) => clonarSistemaParaAmbiente(sis)),
  };
}
```

useProdutoSearch — ProdutoFiltro e branch de filtro (src/hooks/useProdutoSearch.ts:5,28):
```typescript
export type ProdutoFiltro = 'fita' | 'driver' | 'perfil' | 'conector' | 'kit_fixacao' | 'luminaria' | 'todos';
// branch atual:
if (filtro === 'fita' || filtro === 'driver' || filtro === 'perfil' || filtro === 'conector' || filtro === 'kit_fixacao') {
  queryBuilder = queryBuilder.eq('tipo_produto', filtro);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration sistema='s_mode' nos perfis-âncora + difusos SYSTEM MOLD</name>
  <read_first>
    - .planning/phases/21-system-mold-valida-o-reuso/21-RESEARCH.md (Open Question 1 — SQL verbatim, Pitfall 3, processo de aplicação)
    - supabase/migrations/20260612000001_cat03_tipo_produto_conector_kit.sql (template CAT-03 da Phase 19)
    - Memory project_aura_migration_divergence (NÃO usar `supabase db push`)
  </read_first>
  <files>supabase/migrations/20260616000001_sistema_s_mode_system_mold.sql</files>
  <action>
Crie o arquivo de migration com o SQL EXATO do RESEARCH (Open Question 1). Copie verbatim — o filtro foi verificado contra o DB ao vivo (12 perfis + 15 difusos):

```sql
-- Migration: 20260616000001_sistema_s_mode_system_mold.sql
-- Marca perfis-âncora SYSTEM MOLD e módulos difusos com sistema='s_mode' (Phase 21 / SIST-03).
-- Padrão CAT-03 (Phase 19). Aplicar via MCP apply_migration + migration repair (NÃO supabase db push).
-- Tabela: product_variants (view 'produtos' é alias). Idempotente via IS DISTINCT FROM.

BEGIN;

-- 1. Perfis MODULAR âncora (NOFRAME + EMBUTIR) — esperado: 12 linhas (LM1998–LM2003 + LM2109–LM2114)
UPDATE public.product_variants
SET sistema = 's_mode'
WHERE descricao ILIKE '%SYSTEM MOLD%'
  AND (
    descricao ILIKE '%PERFIL NOFRAME MODULAR%'
    OR descricao ILIKE '%PERFIL DE EMBUTIR MODULAR%'
  )
  AND sistema IS DISTINCT FROM 's_mode';

-- 2. Módulos difusos (PARA FITA LED) — esperado: 15 linhas
UPDATE public.product_variants
SET sistema = 's_mode'
WHERE descricao ILIKE '%SYSTEM MOLD%'
  AND descricao ILIKE '%DIFUSO%'
  AND tipo_produto = 'acessorio'
  AND sistema IS DISTINCT FROM 's_mode';

COMMIT;
```

ANTES de aplicar: verifique se a coluna `sistema` tem CHECK constraint que bloqueie o valor 's_mode' (Assumption A1 do RESEARCH):
```sql
SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint
WHERE conrelid='public.product_variants'::regclass AND contype='c';
```
Se houver um CHECK restringindo `sistema`, adicione um `ALTER TABLE ... DROP CONSTRAINT / ADD CONSTRAINT` incluindo 's_mode' ANTES dos UPDATEs (mesmo padrão do `kit_fixacao` em CAT-03). Se não houver (esperado — só 'magneto_48v'/'tiny_magneto' existem como valores, sem constraint), prossiga.

APLICAÇÃO (caminho seguro deste projeto — histórico de migrations divergente): use o MCP Supabase `apply_migration` (projeto `jkewlaezvrbuicmncqbj`) com o conteúdo acima, depois `migration repair` para reconciliar o histórico. NÃO rode `supabase db push`.

Após aplicar, confirme as contagens com:
```sql
SELECT count(*) FROM public.product_variants WHERE sistema='s_mode' AND descricao ILIKE '%PERFIL%MODULAR%'; -- 12
SELECT count(*) FROM public.product_variants WHERE sistema='s_mode' AND tipo_produto='acessorio' AND descricao ILIKE '%DIFUSO%'; -- 15
```
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260616000001_sistema_s_mode_system_mold.sql && grep -q "SET sistema = 's_mode'" supabase/migrations/20260616000001_sistema_s_mode_system_mold.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo supabase/migrations/20260616000001_sistema_s_mode_system_mold.sql existe
    - Contém `descricao ILIKE '%PERFIL NOFRAME MODULAR%'` E `descricao ILIKE '%PERFIL DE EMBUTIR MODULAR%'` (NÃO um genérico `%PERFIL%` — Pitfall 3 pegaria TAMPAS)
    - Contém `tipo_produto = 'acessorio'` no segundo UPDATE
    - Contém `IS DISTINCT FROM 's_mode'` (idempotência) nos dois UPDATEs
    - Query de verificação retorna exatamente 12 perfis e 15 difusos com sistema='s_mode'
    - NÃO contém `supabase db push` em nenhum lugar
  </acceptance_criteria>
  <done>Migration criada e aplicada via MCP apply_migration; 12 perfis + 15 difusos com sistema='s_mode' confirmados; coluna sem constraint bloqueante (ou constraint estendida).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Helpers puros novos em orcamento.ts + fix clonarAmbiente</name>
  <read_first>
    - src/types/orcamento.ts (estado atual — ItemComposicao:43, calcularCargaComposicao:183, clonarSistema:591, clonarAmbiente:614, os 5 calc sites: calcularDemandaFita:228, calcularConsumoW, calcularQtdDrivers, calcularSubtotalSistemaSemFita)
    - src/types/orcamento.test.ts (estilo dos testes existentes)
    - .planning/phases/21-system-mold-valida-o-reuso/21-RESEARCH.md (Code Examples — assinaturas verbatim; Pitfall 2 e 4)
  </read_first>
  <files>src/types/orcamento.ts, src/types/orcamento.test.ts</files>
  <behavior>
    - calcularMetragemModulosDifusos([{papel:'modulo',comprimento:0.264,quantidade:2},{papel:'modulo',comprimento:1.0,quantidade:1}]) === 1.528
    - calcularMetragemModulosDifusos(undefined) === 0 e calcularMetragemModulosDifusos([]) === 0
    - calcularMetragemModulosDifusos ignora itens com papel !== 'modulo' e itens com comprimento == null
    - parsearComprimentoModulo("...FITA LED 264MM...") === 0.264; "...FITA LED 132MM..." === 0.132; "...FITA LED 1MT..." === 1.0; "...FITA LED 2MT..." === 2.0; sem match → undefined
    - clonarItemLuminaria: root.id !== original.id; cada composicao[i].id !== original.composicao[i].id; demais campos preservados
    - clonarItemLuminaria com composicao undefined → resultado.composicao permanece undefined (backward-compat)
    - clonarAmbiente: nenhum id de ItemComposicao no clone colide com id do original (regressão Pitfall 2)
  </behavior>
  <action>
Em src/types/orcamento.ts, ADICIONE (sem tocar nos 5 calc sites protegidos):

1. Estenda o union `papel` em `ItemComposicao` (linha 51) adicionando `'fita_modular'` ao final:
```typescript
papel: 'modulo' | 'driver_recomendado' | 'driver_obrigatorio' | 'conector_energia' | 'kit_fixacao' | 'acessorio_opcional' | 'fita_modular';
```
(Apenas TS — a CHECK constraint de produto_composicao não se aplica a snapshots jsonb em orcamentos.ambientes, conforme Assumption A2 / Pitfall 4.)

2. Novo helper logo após `calcularCargaComposicao` (~linha 188):
```typescript
/** Metragem de fita derivada dos módulos difusos de um SYSTEM MOLD (Phase 21 / D-01).
 *  = Σ(comprimento × quantidade) dos itens papel==='modulo' com comprimento definido. */
export function calcularMetragemModulosDifusos(composicao: ItemComposicao[] | undefined): number {
  if (!composicao?.length) return 0;
  return composicao
    .filter(c => c.papel === 'modulo' && c.comprimento != null)
    .reduce((s, c) => s + (c.comprimento ?? 0) * c.quantidade, 0);
}

/** Parse do comprimento (m) do módulo difuso a partir da descrição. Snapshot no add-time.
 *  Verificado contra os 15 difusos: 132MM→0.132 ... 660MM→0.66, 1MT→1.0, 2MT→2.0. Phase 21 / D-01. */
export function parsearComprimentoModulo(descricao: string): number | undefined {
  const mmMatch = descricao.match(/FITA LED\s+(\d+(?:[,.]\d+)?)\s*MM/i);
  if (mmMatch) return parseFloat(mmMatch[1].replace(',', '.')) / 1000;
  const mtMatch = descricao.match(/FITA LED\s+(\d+(?:[,.]\d+)?)\s*MT/i);
  if (mtMatch) return parseFloat(mtMatch[1].replace(',', '.'));
  return undefined;
}
```

3. Novo helper de clone deep, logo antes de `clonarAmbiente` (~linha 613):
```typescript
/** Clona um ItemLuminaria com novos UUIDs em TODA a árvore (raiz + composicao[]).
 *  composicao ausente → permanece undefined (backward-compat). Phase 21 / D-06. */
export function clonarItemLuminaria(item: ItemLuminaria): ItemLuminaria {
  return {
    ...item,
    id: crypto.randomUUID(),
    composicao: item.composicao?.map(c => ({ ...c, id: crypto.randomUUID() })),
  };
}
```

4. Corrija `clonarAmbiente` (linha 619) para usar o helper — fix do bug de clone raso (Pitfall 2):
```typescript
luminarias: amb.luminarias.map(clonarItemLuminaria),
```
(troca `amb.luminarias.map((l) => ({ ...l, id: crypto.randomUUID() }))`).

Em src/types/orcamento.test.ts, adicione `describe` blocks cobrindo cada item de `<behavior>`. Importe os novos exports via `@/types/orcamento`. Para o teste de clonarAmbiente, monte um Ambiente com uma luminaria que tenha composicao com 2 itens e prove que os ids dos ItemComposicao do clone são todos diferentes dos do original.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run test -- --run src/types/orcamento.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function calcularMetragemModulosDifusos" src/types/orcamento.ts`
    - `grep -q "export function parsearComprimentoModulo" src/types/orcamento.ts`
    - `grep -q "export function clonarItemLuminaria" src/types/orcamento.ts`
    - `grep -q "luminarias: amb.luminarias.map(clonarItemLuminaria)" src/types/orcamento.ts`
    - `grep -q "fita_modular" src/types/orcamento.ts`
    - REGRESSÃO: `git diff src/types/orcamento.ts` mostra ZERO alterações dentro dos corpos de calcularDemandaFita, calcularConsumoW, calcularQtdDrivers, calcularSubtotalSistemaSemFita (apenas adições novas + a 1 linha de clonarAmbiente)
    - `npm run test -- --run src/types/orcamento.test.ts` passa, incluindo os novos describe blocks
  </acceptance_criteria>
  <done>4 mudanças aplicadas em orcamento.ts; testes novos verdes; 5 calc sites de Fita Padrão intocados (diff vazio nos corpos).</done>
</task>

<task type="auto">
  <name>Task 3: Filtro 'modulo_difuso' em useProdutoSearch</name>
  <read_first>
    - src/hooks/useProdutoSearch.ts (estado atual — ProdutoFiltro:5, branch de filtro:28, lição Postgres NULL NOT IN do Phase 20)
    - .planning/phases/21-system-mold-valida-o-reuso/21-RESEARCH.md (Open Question 1 — "filtroSistema gap for difusos"; Pitfall 1)
  </read_first>
  <files>src/hooks/useProdutoSearch.ts</files>
  <action>
Em src/hooks/useProdutoSearch.ts:

1. Estenda o type `ProdutoFiltro` (linha 5) adicionando `'modulo_difuso'`:
```typescript
export type ProdutoFiltro = 'fita' | 'driver' | 'perfil' | 'conector' | 'kit_fixacao' | 'luminaria' | 'todos' | 'modulo_difuso';
```

2. Adicione um branch novo no bloco de filtro (após o `else if (filtro === 'luminaria')` da linha 30-32). NÃO reusar o caminho `filtroSistema` — ele tem `.is('tipo_produto', null)` que EXCLUI os difusos (que têm `tipo_produto='acessorio'`, Pitfall 1):
```typescript
} else if (filtro === 'modulo_difuso') {
  queryBuilder = queryBuilder
    .eq('tipo_produto', 'acessorio')
    .eq('sistema', 's_mode');
}
```
(Usa a coluna `sistema` direto, não o alias `sistema_magnetico`. Não usar `.not(...in...)` — `NULL NOT IN` em Postgres é falsy.)
  </action>
  <verify>
    <automated>cd auraoramentos && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "modulo_difuso" src/hooks/useProdutoSearch.ts` (no type union E no branch)
    - O branch contém `.eq('tipo_produto', 'acessorio')` e `.eq('sistema', 's_mode')`
    - NÃO usa `.is('tipo_produto', null)` no branch de modulo_difuso
    - `npm run build` passa (type union estendido sem erro de TS)
  </acceptance_criteria>
  <done>ProdutoFiltro inclui 'modulo_difuso'; branch retorna difusos s_mode; build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → product_variants | UPDATE de massa na tabela de catálogo (dados de produção) |
| client filtro → Supabase | input de busca cruza para query (já mitigado por SDK parametrizado) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | migration UPDATE | mitigate | WHERE delimitado por `PERFIL NOFRAME MODULAR`/`PERFIL DE EMBUTIR MODULAR` + `tipo_produto='acessorio'` — verificado contra DB ao vivo (12+15 linhas exatas). `IS DISTINCT FROM 's_mode'` torna idempotente. Não toca produtos magnéticos (`magneto_48v`/`tiny_magneto`) nem Fita Padrão. Contagens verificadas pós-aplicação. |
| T-21-02 | Denial of Service | migration mal-delimitada | accept | Pitfall 3 documenta o risco de pegar TAMPAS/spots; mitigado pelo filtro preciso. Rollback SQL disponível no RESEARCH. Baixo risco — UPDATE escopado e reversível. |
| T-21-03 | Information Disclosure | filtro 'modulo_difuso' | accept | Query escopada a catálogo público (sem PII, sem RLS sensível); produtos são visíveis a todos os autenticados por design. |

Nenhuma ameaça high-severity nesta fundação — migration escopada/idempotente/reversível e busca client-side sobre dados de catálogo já públicos.
</threat_model>

<verification>
- `npm run test -- --run src/types/orcamento.test.ts` verde (novos helpers + regressão clone)
- `npm run build` verde (type unions estendidos)
- Query Supabase confirma 12 perfis + 15 difusos com `sistema='s_mode'`
- `git diff src/types/orcamento.ts` não altera os corpos dos 5 calc sites de Fita Padrão
</verification>

<success_criteria>
- Migration aplicada via apply_migration; detectarTipoAncora retorna 'modular' para os perfis-âncora
- useProdutoSearch com filtro='modulo_difuso' retorna os 15 difusos
- calcularMetragemModulosDifusos, parsearComprimentoModulo, clonarItemLuminaria exportados e testados
- clonarAmbiente deep-clona composicao[] (bug corrigido)
- 5 calc sites de Fita Padrão intocados
</success_criteria>

<output>
Após completar, crie `.planning/phases/21-system-mold-valida-o-reuso/21-01-SUMMARY.md`.
</output>
