---
phase: 21-system-mold-valida-o-reuso
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/components/ComposicaoCard.tsx
  - src/components/AmbienteCard.tsx
autonomous: true
requirements: [SIST-03, DUP-01]
must_haves:
  truths:
    - "Buscar e selecionar um perfil SYSTEM MOLD inicia uma composição modular (ComposicaoCard) em vez de item simples"
    - "No card modular o colaborador adiciona N módulos difusos (SKU + qtd) e o comprimento é gravado como snapshot"
    - "O card exibe a metragem derivada Σ(comprimento × qtd) dos difusos"
    - "Botão 'Adicionar fita' abre a busca de fita; o colaborador escolhe o SKU; a fita entra com metragem pré-preenchida"
    - "Adicionar a fita dispara recomendação advisory de driver (botão Aplicar), nunca silenciosa"
    - "O header do card de composição tem um botão Duplicar ao lado da lixeira"
  artifacts:
    - path: "src/components/ComposicaoCard.tsx"
      provides: "painel modular (difusos + fita derivada + driver advisory) + botão duplicar"
      contains: "isModular"
    - path: "src/components/AmbienteCard.tsx"
      provides: "rota 'modular' que inicia composicao: []"
      contains: "tipo === 'modular'"
  key_links:
    - from: "AmbienteCard.handleSelectProdutoGlobal rota 'modular'"
      to: "ItemLuminaria com sistema='s_mode' + composicao: []"
      via: "onChange"
      pattern: "sistema: 's_mode'"
    - from: "ComposicaoCard 'Adicionar fita'"
      to: "ItemComposicao papel='fita_modular' com comprimento=metragem"
      via: "ProdutoAutocomplete filtro='fita'"
      pattern: "fita_modular"
    - from: "ComposicaoCard add difuso"
      to: "ItemComposicao papel='modulo' com comprimento snapshot"
      via: "parsearComprimentoModulo"
      pattern: "parsearComprimentoModulo"
---

<objective>
A UI de montagem do SYSTEM MOLD (SIST-03): a rota `'modular'` em `AmbienteCard.handleSelectProdutoGlobal` passa a iniciar uma composição (hoje cai em item simples), e o `ComposicaoCard` ganha o ramo modular — busca de módulos difusos, metragem derivada exibida, botão "Adicionar fita" (vendedor escolhe o SKU, nunca auto-escolhido) com metragem pré-preenchida, e recomendação advisory de driver reaproveitando o padrão da busca 24V já presente no card. Inclui também o botão "Duplicar" no header do card (parte visual de DUP-01; a orquestração de destino fica no Plan 03).

Purpose: entrega o terceiro fluxo composto ao lado de MAGNETO 48V / TINY 24V, com a mesma arquitetura `luminarias[].composicao?`.
Output: ComposicaoCard com ramo modular funcional; AmbienteCard roteando 'modular' para composição; botão duplicar no header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-system-mold-valida-o-reuso/21-CONTEXT.md
@.planning/phases/21-system-mold-valida-o-reuso/21-RESEARCH.md
@.planning/phases/21-system-mold-valida-o-reuso/21-01-SUMMARY.md

<interfaces>
<!-- Contratos do Plan 01 (Wave 1) — usar diretamente, sem reexplorar. -->

De src/types/orcamento.ts (criados no Plan 01):
```typescript
export function calcularMetragemModulosDifusos(composicao: ItemComposicao[] | undefined): number;
export function parsearComprimentoModulo(descricao: string): number | undefined;
// papel agora inclui 'fita_modular'
```

ComposicaoCardProps atual (src/components/ComposicaoCard.tsx:49):
```typescript
interface ComposicaoCardProps {
  item: ItemLuminaria;
  onChange: (item: ItemLuminaria) => void;
  onRemove: () => void;
  indice: number;
}
```

Padrão existente no card — flags de sistema (ComposicaoCard.tsx:69) e busca de driver 24V async com cancelled flag (linha 100-154). itemRef.current usado em callbacks async (Pitfall 5).

Rota magnética de referência em AmbienteCard.handleSelectProdutoGlobal (AmbienteCard.tsx:407-417):
```typescript
const novaRaiz: ItemLuminaria = {
  id: uid(), codigo: produto.codigo, descricao: produto.descricao, quantidade: 1,
  precoUnitario: preco, precoMinimo: precoMin, imagemUrl: imgUrl,
  sistema: tipo, potencia_watts: produto.driver_potencia_w ?? null,
  tensao: produto.voltagem ?? (...), composicao: [],
};
onChange({ ...ambiente, luminarias: [...ambiente.luminarias, novaRaiz] });
```

Renderização condicional já existe (AmbienteCard.tsx:514): `if (item.composicao !== undefined) return <ComposicaoCard .../>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rota 'modular' em AmbienteCard inicia composição</name>
  <read_first>
    - src/components/AmbienteCard.tsx (handleSelectProdutoGlobal:324-447; comentário da rota modular:420; rota magnética:391-417; uid helper; render ComposicaoCard:514-523)
    - src/types/orcamento.ts (detectarTipoAncora:173 — já retorna 'modular' para s_mode)
    - .planning/phases/21-system-mold-valida-o-reuso/21-RESEARCH.md (Existing Code Map — "AmbienteCard.handleSelectProdutoGlobal — 'modular' route")
  </read_first>
  <files>src/components/AmbienteCard.tsx</files>
  <action>
Em `handleSelectProdutoGlobal` (AmbienteCard.tsx), ANTES do bloco de item simples/fallback (linha 420 — o comentário "'modular' (Phase 21), 'luminaria' e fallback..."), adicione uma rota dedicada para `tipo === 'modular'` que inicia uma composição (como as rotas magnéticas fazem na linha 391):

```typescript
if (tipo === 'modular') {
  const novaRaiz: ItemLuminaria = {
    id: uid(),
    codigo: produto.codigo, descricao: produto.descricao, quantidade: 1,
    precoUnitario: preco, precoMinimo: precoMin, imagemUrl: imgUrl,
    sistema: 's_mode',
    potencia_watts: null,            // perfil modular não tem potencia
    tensao: produto.voltagem ?? null,
    composicao: [],                  // presença ativa o ComposicaoCard
  };
  onChange({ ...ambiente, luminarias: [...ambiente.luminarias, novaRaiz] });
  return;
}
```

Atualize o comentário da linha 420 para remover "modular" da lista de fallback (agora 'luminaria' e fallback apenas). NÃO altere a rota 'fita' (linha 330) nem as rotas magnéticas — elas ficam byte-idênticas. A renderização condicional `item.composicao !== undefined → <ComposicaoCard>` (linha 514) já existe e cobre o novo item automaticamente.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "tipo === 'modular'" src/components/AmbienteCard.tsx`
    - O bloco modular contém `sistema: 's_mode'` e `composicao: []`
    - O comentário da rota fallback NÃO lista mais 'modular'
    - `git diff` mostra rotas 'fita', 'magneto_48v', 'tiny_magneto' inalteradas (Fita Padrão intocada)
    - `npm run build` passa
  </acceptance_criteria>
  <done>Selecionar um perfil SYSTEM MOLD cria ItemLuminaria com sistema='s_mode' e composicao:[], renderizando o ComposicaoCard.</done>
</task>

<task type="auto">
  <name>Task 2: Ramo modular no ComposicaoCard (difusos + fita derivada + driver advisory + botão duplicar)</name>
  <read_first>
    - src/components/ComposicaoCard.tsx (estrutura inteira: flags is48V/is24V:69; busca driver 24V async:100-154; atualizarComposicaoItem:158; header:584-619; corpo/módulos:621-714; imports:1-17)
    - src/components/ProdutoAutocomplete.tsx (assinatura do prop filtro e onSelect)
    - src/types/orcamento.ts (calcularMetragemModulosDifusos, parsearComprimentoModulo, MARGEM_SEGURANCA_DRIVER, ItemComposicao papel='fita_modular')
    - .planning/phases/21-system-mold-valida-o-reuso/21-RESEARCH.md (Open Question 2 — reuso buscarDriverSugerido inline; Pattern 2 e 3; Pitfall 4 e 5)
  </read_first>
  <files>src/components/ComposicaoCard.tsx</files>
  <action>
Estenda ComposicaoCardProps com um callback opcional de duplicação (consumido pelo Plan 03):
```typescript
interface ComposicaoCardProps {
  item: ItemLuminaria;
  onChange: (item: ItemLuminaria) => void;
  onRemove: () => void;
  onDuplicate?: () => void;   // Phase 21 / DUP-01 (D-04)
  indice: number;
}
```
E adicione `onDuplicate` ao destructuring do componente (linha 68).

**1. Flag modular** — após a linha 70 (`is24V`):
```typescript
const isModular = item.sistema === "s_mode";
```

**2. Imports** — adicione ao import de `@/types/orcamento` (linha 10-17): `calcularMetragemModulosDifusos`, `parsearComprimentoModulo`. Adicione `Copy` ao import de `lucide-react` (linha 5).

**3. Derivações modulares** — junto às derivações (linha 90-94):
```typescript
const metragemDerivada = isModular ? calcularMetragemModulosDifusos(item.composicao) : 0;
const fitaModular = composicao.find((c) => c.papel === "fita_modular");
```

**4. Botão Duplicar no header** — no header (linha 611-618), ANTES do botão Trash2, renderize condicionalmente:
```tsx
{onDuplicate && (
  <Button size="icon" variant="ghost" className="h-7 w-7 text-muted-foreground"
    title="Duplicar" onClick={onDuplicate}>
    <Copy className="h-3.5 w-3.5" />
  </Button>
)}
```

**5. Badge MODULAR no header** — junto aos badges is48V/is24V (linha 586-601):
```tsx
{isModular && (
  <Badge variant="outline" className="text-[10px] px-1 py-0 font-semibold border-sky-400 text-sky-700 bg-sky-50">
    MODULAR
  </Badge>
)}
```

**6. Busca de módulo difuso** — o card já tem `mostrarBuscaModulo` + `ProdutoAutocomplete`. Quando `isModular`, a busca de módulo deve usar `filtro="modulo_difuso"` (não a busca magnética). No handler que adiciona o módulo selecionado, quando isModular, grave o comprimento como snapshot:
```typescript
const comprimento = isModular ? parsearComprimentoModulo(produto.descricao) : undefined;
// no ItemComposicao novo: papel: 'modulo', obrigatorio: false, comprimento, potenciaW: undefined
```
(Reaproveite o handler de adição de módulo existente; só ramifique o `filtro` do ProdutoAutocomplete e o cálculo de `comprimento` por isModular.)

**7. Painel de fita derivada** (após a lista de módulos, ~linha 714) — renderizar só quando `isModular`:
```tsx
{isModular && (
  <div className="rounded-md border border-sky-300/50 bg-sky-50/50 px-3 py-2 space-y-2">
    <p className="text-xs font-semibold text-sky-900">
      Fita necessária: {metragemDerivada > 0 ? `${metragemDerivada.toFixed(3).replace(/\.?0+$/,'').replace('.',',')} m` : "—"}
      <span className="font-normal text-sky-700"> (Σ comprimento × qtd dos difusos)</span>
    </p>
    {fitaModular ? (
      /* mostra fita escolhida: codigo, descricao readOnly + qtd metragem editável + preço + botão remover (papel fita_modular) */
    ) : (
      metragemDerivada > 0 && (
        <Button size="sm" variant="outline" className="h-7 text-xs gap-1" onClick={() => setMostrarBuscaFita(true)}>
          <Plus className="h-3 w-3" /> Adicionar fita
        </Button>
      )
    )}
    {mostrarBuscaFita && (
      <ProdutoAutocomplete filtro="fita" placeholder="Buscar fita LED..."
        onSelect={(p) => handleAdicionarFitaModular(p)} value="" />
    )}
  </div>
)}
```
Adicione estado `const [mostrarBuscaFita, setMostrarBuscaFita] = useState(false);`.

**8. handleAdicionarFitaModular** — adiciona a fita como ItemComposicao papel='fita_modular' com `comprimento = metragemDerivada` pré-preenchido (NUNCA auto-escolhe SKU — o vendedor já escolheu no autocomplete), depois dispara a busca advisory de driver:
```typescript
const handleAdicionarFitaModular = (produto: Produto) => {
  const metragem = calcularMetragemModulosDifusos(itemRef.current.composicao);
  const novaFita: ItemComposicao = {
    id: crypto.randomUUID(),
    codigo: produto.codigo, descricao: produto.descricao, quantidade: 1,
    precoUnitario: Math.round((produto.preco_tabela || 0) * 100) / 100,
    precoMinimo: Math.round((produto.preco_minimo || 0) * 100) / 100,
    imagemUrl: produto.imagem_url || undefined,
    papel: 'fita_modular', obrigatorio: false,
    comprimento: metragem,                 // metragem pré-preenchida (D-01)
  };
  const nova = [...(itemRef.current.composicao ?? []), novaFita];
  onChange({ ...itemRef.current, composicao: nova });
  setMostrarBuscaFita(false);
  // Dispara recomendação advisory de driver (não-bloqueante)
  buscarDriverModular(produto.voltagem ?? 24, produto.wm ?? 0, metragem);
};
```

**9. buscarDriverModular** — replique a query de driver inline (mesmo padrão da busca 24V já no card, linha 117-127; researcher confirmou que buscarDriverSugerido de AmbienteCard é closure local não-importável). Use a voltagem da fita escolhida (tipicamente 24V), aplique MARGEM_SEGURANCA_DRIVER, e exiba o resultado no MESMO painel advisory do driver com botão "Aplicar" (reaproveite o estado/render de sugestão de driver existente). O driver aplicado entra como ItemComposicao papel='driver_recomendado'. Use `itemRef.current` e flag `cancelled` (Pitfall 5):
```typescript
const buscarDriverModular = async (voltagem: number, wm: number, metragem: number) => {
  const metragemEf = metragem > 0 ? metragem : 5;
  const consumo = wm * metragemEf * MARGEM_SEGURANCA_DRIVER;
  const { data } = await supabase.from("produtos")
    .select("id, codigo, descricao, preco_tabela, preco_minimo, driver_potencia_w:potencia_watts")
    .eq("tipo_produto", "driver").eq("tensao", voltagem)
    .gte("potencia_watts", consumo)
    .not("descricao", "ilike", "%DESCONTINUAR%")
    .order("potencia_watts", { ascending: true }).limit(1);
  // setar a sugestão no mesmo painel advisory (estado de sugestão), botão Aplicar adiciona papel='driver_recomendado'
};
```
A recomendação permanece ADVISORY: o vendedor clica "Aplicar" para inserir; pode revisar/sobrescrever. NÃO inserir o driver automaticamente.

**NÃO** adicionar checklist para s_mode (Deferred — REGRAS_COMPOSICAO não tem 's_mode'). Módulos concentrados (spots) fora de escopo.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "isModular" src/components/ComposicaoCard.tsx`
    - `grep -q "calcularMetragemModulosDifusos" src/components/ComposicaoCard.tsx`
    - `grep -q "parsearComprimentoModulo" src/components/ComposicaoCard.tsx`
    - `grep -q "fita_modular" src/components/ComposicaoCard.tsx`
    - `grep -q 'filtro="modulo_difuso"' src/components/ComposicaoCard.tsx`
    - `grep -q 'filtro="fita"' src/components/ComposicaoCard.tsx` (busca de fita — vendedor escolhe SKU)
    - `grep -q "onDuplicate" src/components/ComposicaoCard.tsx` e botão com `title="Duplicar"`
    - `grep -q "driver_recomendado" src/components/ComposicaoCard.tsx` (driver advisory via Aplicar)
    - O driver NÃO é inserido sem clique em Aplicar (advisory) — confirmar por leitura do handler
    - `npm run build` passa
  </acceptance_criteria>
  <done>Card modular: adiciona difusos (comprimento snapshot), exibe metragem derivada, "Adicionar fita" abre busca (SKU escolhido pelo vendedor) com metragem pré-preenchida, dispara driver advisory com Aplicar; header tem botão Duplicar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| busca de fita/difuso → composicao snapshot | input de catálogo vira item congelado em estado client |
| async driver busca → itemRef | reconciliação pós-await pode divergir do estado atual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering | reconciliação async fita+driver | mitigate | Usar `itemRef.current` (não `item`) dentro de callbacks async + flag `cancelled` no padrão já estabelecido no card (Pitfall 5). Evita sobrescrever edições feitas durante o await. |
| T-21-05 | Repudiation | escolha de SKU de fita | accept | Decisão de projeto exige que o vendedor escolha o SKU (nunca auto). Snapshot congela a escolha no item — auditável no orçamento salvo. |

Nenhuma ameaça high-severity — fluxo é client-side, sobre dados de catálogo públicos, sem novos endpoints. RLS de orçamentos inalterada.
</threat_model>

<verification>
- `npm run build` verde
- Playwright (manual no checkpoint do Plan 03 ou aqui): buscar "SYSTEM MOLD PERFIL NOFRAME MODULAR" → card modular abre; adicionar difuso → metragem aparece; "Adicionar fita" → escolher SKU → fita entra com metragem; painel de driver advisory aparece com Aplicar
- Fita Padrão e fluxos magnéticos inalterados (git diff das rotas)
</verification>

<success_criteria>
- SIST-03: SYSTEM MOLD montável (perfil + difusos), metragem derivada de Σ(comprimento × qtd), fita adicionada com SKU escolhido pelo vendedor e metragem pré-preenchida, driver advisory
- Botão Duplicar presente no header do card (wiring de destino no Plan 03)
- Nenhuma regressão em Fita Padrão / MAGNETO 48V / TINY 24V
</success_criteria>

<output>
Após completar, crie `.planning/phases/21-system-mold-valida-o-reuso/21-02-SUMMARY.md`.
</output>
