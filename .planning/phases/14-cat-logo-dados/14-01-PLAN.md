---
phase: 14-cat-logo-dados
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-cat-logo-dados/14-DIAGNOSTICO.md
autonomous: false
requirements: [CAT-01, CAT-02]
must_haves:
  truths:
    - "A lista completa de produtos com tipo_produto nulo/inválido existe, agrupada por família/categoria"
    - "WALL WASHER, CANTONEIRA, LM3475, LM3291 aparecem na varredura com seu tipo_produto atual real (esperado NULL)"
    - "O valor real da coluna sistema do produto MAGNETO 48V está documentado (decide se CAT-02 é dado, código ou ambos)"
    - "Lenny aprovou as regras de correção por grupo (não SKU a SKU)"
  artifacts:
    - path: ".planning/phases/14-cat-logo-dados/14-DIAGNOSTICO.md"
      provides: "Baseline de contagem (Query A), grupos a corrigir (Query B), confirmação famílias (Query C), causa-raiz MAGNETO (Query D), lista explícita de SKUs aprovada"
      contains: "Query A"
  key_links:
    - from: "Query B output"
      to: "lista explícita de SKUs por grupo aprovado"
      via: "decisão de Lenny no checkpoint"
      pattern: "WHERE codigo IN"
---

<objective>
Diagnosticar o estado real do catálogo (`product_variants`) na base de produção Supabase
`jkewlaezvrbuicmncqbj` e obter aprovação de Lenny sobre as REGRAS DE CORREÇÃO POR GRUPO antes
de qualquer migration. Este plano NÃO altera dado nem código — apenas lê, agrupa e documenta.

Implementa as decisões D-01 (varredura ampla com revisão prévia), D-02 (aprovação por grupos),
D-04 (baseline de contagem) e a fase de diagnóstico de D-03 (causa-raiz MAGNETO antes de corrigir).

Purpose: A migration do Plano 02 só pode ser determinística e auditável se a lista de SKUs nascer
de grupos que Lenny aprovou. Pular esta varredura = Pitfall 5 (catálogo continua quebrado para
famílias não listadas).
Output: `14-DIAGNOSTICO.md` com saídas das Queries A-D + tabela de grupos + lista de SKUs aprovada + decisão CAT-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-cat-logo-dados/14-CONTEXT.md
@.planning/phases/14-cat-logo-dados/14-RESEARCH.md

<interfaces>
Valores válidos do CHECK constraint `check_tipo_produto` (fonte: 20260319000001_campos_tecnicos_produtos.sql):
  'fita', 'driver', 'perfil', 'spot', 'lampada', 'acessorio', 'conector', 'suporte'  (ou NULL)

Valores válidos do CHECK constraint `check_sistema` (coluna `sistema`, aliased como `sistema_magnetico`):
  'padrao', 'tiny_magneto', 'magneto_48v', 's_mode', 'trilha'  (ou NULL)

DML deve mirar a TABELA `public.product_variants`, NUNCA a view `public.produtos` (view não-atualizável).
O hook `useProdutoSearch.ts:26-29` filtra perfil/driver via `.eq('tipo_produto', filtro)` — produto
com tipo_produto NULL/errado some do seletor.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rodar varredura diagnóstica (Queries A-D) e montar tabela de grupos</name>
  <read_first>
    - .planning/phases/14-cat-logo-dados/14-RESEARCH.md (seção "Diagnostic SQL" — Queries A, B, C, D literais)
    - src/hooks/useProdutoSearch.ts (confirmar que filtro perfil/driver usa .eq('tipo_produto', filtro))
    - supabase/migrations/20260319000001_campos_tecnicos_produtos.sql (valores válidos do CHECK)
  </read_first>
  <action>
    Conectar ao projeto Supabase de produção `jkewlaezvrbuicmncqbj` (via Supabase SQL editor ou
    `supabase db` CLI / MCP Supabase se disponível). Rodar EXATAMENTE as 4 queries de RESEARCH.md,
    todas contra a TABELA `public.product_variants` (não a view `produtos`):

    Query A (baseline D-04):
      SELECT tipo_produto, COUNT(*) AS total FROM public.product_variants
      GROUP BY tipo_produto ORDER BY tipo_produto NULLS FIRST;

    Query B (varredura ampla D-01) — agrupa null/inválidos por (categoria, familia_perfil, tipo_atual):
      SELECT COALESCE(p.categoria,'(sem categoria)') AS categoria,
             COALESCE(pv.familia_perfil,'(sem familia)') AS familia_perfil,
             pv.tipo_produto AS tipo_atual, COUNT(*) AS qtd,
             MIN(pv.codigo) AS exemplo_codigo, MIN(pv.descricao) AS exemplo_descricao
      FROM public.product_variants pv
      LEFT JOIN public.products p ON pv.product_id = p.id
      WHERE pv.tipo_produto IS NULL
         OR pv.tipo_produto NOT IN ('fita','driver','perfil','spot','lampada','acessorio','conector','suporte')
      GROUP BY p.categoria, pv.familia_perfil, pv.tipo_produto ORDER BY qtd DESC;

    Query C (confirmar famílias conhecidas do UAT):
      SELECT codigo, descricao, tipo_produto, familia_perfil, sistema FROM public.product_variants
      WHERE descricao ILIKE ANY (ARRAY['%WALL WASHER%','%CANTONEIRA%','%LM3475%','%LM3291%'])
      ORDER BY descricao;

    Query D (causa-raiz MAGNETO, CAT-02 / D-03):
      SELECT codigo, descricao, tipo_produto, sistema, familia_perfil FROM public.product_variants
      WHERE descricao ILIKE '%MAGNETO%' OR sistema IN ('magneto_48v','tiny_magneto')
      ORDER BY sistema, descricao;

    Criar `.planning/phases/14-cat-logo-dados/14-DIAGNOSTICO.md` colando a saída CRUA de cada query.
    Depois montar a tabela de proposta de grupos a partir da Query B, uma linha por grupo, colunas:
    `categoria | familia_perfil | tipo_atual | qtd | exemplo_codigo | exemplo_descricao | tipo_produto ALVO proposto`.
    O ALVO proposto deve usar APENAS valores válidos do CHECK (perfil/driver/spot/lampada/acessorio/conector/suporte).
    Regra fixa por D-02b: qualquer grupo WALL WASHER → ALVO = 'perfil'. Para os demais grupos, propor
    o alvo com base em familia_perfil/descricao (ex.: grupos de perfil → 'perfil'; grupos de fonte/driver → 'driver').

    Para CAT-02, na seção MAGNETO do DIAGNOSTICO.md, classificar o produto MAGNETO 48V real conforme
    a tabela de decisão de RESEARCH.md:
      - sistema='tiny_magneto' num MAGNETO 48V → fix de DADO (migration muda sistema p/ 'magneto_48v')
      - sistema='magneto_48v' → sem fix
      - sistema=NULL e descrição NÃO contém "MAGNETO22" → fix de DADO + CÓDIGO (broaden regex em AmbienteCard.tsx:81)
      - sistema=NULL e descrição contém "MAGNETO22" → sem fix
    Registrar a decisão explícita (dado / código / ambos / nenhum) com o codigo+descricao+sistema reais.
  </action>
  <verify>
    <automated>MISSING — diagnóstico é leitura de DB de produção, não automatizável por teste. Verificação: arquivo 14-DIAGNOSTICO.md existe e contém as 4 saídas + tabela de grupos + decisão MAGNETO.</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `.planning/phases/14-cat-logo-dados/14-DIAGNOSTICO.md` existe
    - Contém as strings "Query A", "Query B", "Query C", "Query D" com saída crua de cada
    - Contém uma tabela de grupos com coluna "tipo_produto ALVO" usando apenas valores válidos do CHECK
    - Contém uma seção MAGNETO com codigo+descricao+sistema reais e a decisão (dado/código/ambos/nenhum)
    - Nenhum arquivo .sql foi criado neste plano; nenhuma linha de product_variants foi alterada (este plano é read-only)
  </acceptance_criteria>
  <done>14-DIAGNOSTICO.md tem baseline (A), grupos (B), confirmação famílias (C), causa-raiz MAGNETO (D) e a decisão de onde corrigir CAT-02.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Aprovação das regras de correção por grupo (D-02)</name>
  <decision>Quais grupos de produtos recebem qual tipo_produto, e onde corrigir o MAGNETO (CAT-02)</decision>
  <context>
    D-02 exige aprovação por GRUPOS/REGRAS, não SKU a SKU. Apresentar a Lenny a tabela de grupos da
    Task 1 (Query B) com contagem + tipo_produto ALVO proposto por grupo, mais a decisão CAT-02 da Query D.
    Após aprovação, materializar a lista EXPLÍCITA de `codigo` por grupo aprovado (rodando um SELECT
    codigo de cada grupo) e gravá-la em 14-DIAGNOSTICO.md — é dessa lista que a migration do Plano 02
    nasce (`WHERE codigo IN (...)`), garantindo determinismo e auditabilidade.
  </context>
  <options>
    <option id="approve-as-proposed">
      <name>Aprovar grupos como propostos</name>
      <pros>Resolve CAT-01 de forma definitiva (toda a base consistente); migration determinística</pros>
      <cons>Nenhum — alvos seguem o CHECK e a regra D-02b</cons>
    </option>
    <option id="adjust-groups">
      <name>Ajustar alvos de grupos específicos antes de aprovar</name>
      <pros>Lenny corrige qualquer grupo cujo ALVO proposto esteja errado (ex.: grupo que deveria ser 'acessorio' e não 'perfil')</pros>
      <cons>Requer nova rodada de revisão da tabela</cons>
    </option>
  </options>
  <read_first>
    - .planning/phases/14-cat-logo-dados/14-DIAGNOSTICO.md (tabela de grupos + seção MAGNETO geradas na Task 1)
  </read_first>
  <action>
    Apresentar a tabela de grupos e a decisão CAT-02 a Lenny. Após ele aprovar (ou ajustar), rodar
    `SELECT codigo FROM public.product_variants WHERE ...` para cada grupo aprovado e gravar a LISTA
    EXPLÍCITA de codigos por grupo em 14-DIAGNOSTICO.md, sob um heading "## SKUs aprovados (para migration)".
    Para WALL WASHER, registrar se será endereçado por lista explícita de codigo ou por `descricao ILIKE
    '%WALL WASHER%'` (D-02 prefere lista explícita; usar ILIKE só se Query C confirmar que descrição é o
    único identificador). Marcar claramente os codigos do MAGNETO cujo `sistema` será corrigido (se a
    decisão for fix de dado).
  </action>
  <acceptance_criteria>
    - 14-DIAGNOSTICO.md contém heading "## SKUs aprovados (para migration)" com lista explícita de codigos por grupo
    - Cada grupo aprovado tem tipo_produto alvo definido (valor válido do CHECK)
    - A decisão CAT-02 (dado/código/ambos/nenhum) está marcada como APROVADA com os codigos MAGNETO afetados
  </acceptance_criteria>
  <resume-signal>Lenny aprova os grupos (e a decisão CAT-02) ou pede ajuste; ao aprovar, a lista de SKUs fica gravada</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor → Supabase prod (read) | Queries de leitura contra product_variants em produção; sem escrita neste plano |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | Query B/C/D output em 14-DIAGNOSTICO.md | accept | Dados de catálogo (códigos/descrições/preços) não são PII; arquivo fica no repo de planning já versionado |
| T-14-02 | Tampering | product_variants (escrita acidental) | mitigate | Plano é read-only por contrato; nenhum .sql criado, nenhum UPDATE rodado (acceptance criteria garante) |
</threat_model>

<verification>
- 14-DIAGNOSTICO.md existe com saídas das Queries A-D
- Lista explícita de SKUs por grupo aprovado gravada
- Decisão CAT-02 documentada e aprovada
- Nenhuma alteração em product_variants nem em código nesta fase
</verification>

<success_criteria>
- Varredura ampla concluída (D-01): todos os grupos null/inválidos identificados, não só os 4 do UAT
- Baseline de contagem registrado (D-04 — Query A)
- Causa-raiz MAGNETO determinada (D-03 — Query D)
- Lenny aprovou as regras por grupo (D-02) e a lista explícita de SKUs existe
</success_criteria>

<output>
After completion, create `.planning/phases/14-cat-logo-dados/14-01-SUMMARY.md`
</output>
