---
phase: 14-cat-logo-dados
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - supabase/migrations/20260610000001_tipo_produto_correcao_catalogos.sql
  - src/components/AmbienteCard.tsx
autonomous: false
requirements: [CAT-01, CAT-02]
must_haves:
  truths:
    - "A migration corrige tipo_produto de todos os grupos aprovados via UPDATE ... WHERE codigo IN (...)"
    - "WALL WASHER recebe tipo_produto = 'perfil' (não 'wall_washer', que é inválido no CHECK)"
    - "A migration é idempotente (guarda IS DISTINCT FROM) e tem nota de rollback no header"
    - "Se o diagnóstico apontou, o sistema do MAGNETO 48V é corrigido no dado e/ou o regex em AmbienteCard.tsx é ampliado"
    - "A migration está aplicada na base de produção (supabase db push), não só no disco"
    - "A contagem por tipo_produto pós-migration difere da baseline pelo número exato de SKUs corrigidos (D-04)"
  artifacts:
    - path: "supabase/migrations/20260610000001_tipo_produto_correcao_catalogos.sql"
      provides: "UPDATE idempotente de tipo_produto + (se necessário) sistema do MAGNETO, em BEGIN/COMMIT, com header de rollback"
      contains: "WHERE codigo IN"
  key_links:
    - from: "supabase/migrations/20260610000001_tipo_produto_correcao_catalogos.sql"
      to: "public.product_variants"
      via: "UPDATE ... SET tipo_produto"
      pattern: "UPDATE public.product_variants"
    - from: "AmbienteCard.tsx branch MAGNETO (L81)"
      to: "toast MAGNETO 48V (não TINY)"
      via: "condição sistema_magnetico==='magneto_48v' avaliada antes do branch TINY (L89)"
      pattern: "magneto_48v"
---

<objective>
Materializar a migration SQL aditiva e idempotente que corrige `tipo_produto` (CAT-01) a partir da
lista de SKUs aprovada no Plano 01, aplicá-la na base de produção, e corrigir a causa-raiz da dica
MAGNETO (CAT-02) no dado e/ou no código conforme o diagnóstico.

Implementa D-02 (migration nasce dos grupos aprovados, `WHERE codigo IN`), D-02b (WALL WASHER →
'perfil'), D-03 (corrigir CAT-02 na causa-raiz, sem workaround), D-04 (contagem antes/depois) e
D-07 (idempotente + nota de rollback).

Purpose: É o coração da fase — sem a migration aplicada na DB viva, os seletores continuam vazios e
os critérios de sucesso 1-3 do roadmap NÃO passam (arquivo no disco não basta).
Output: migration aplicada + (se necessário) patch em AmbienteCard.tsx + delta de contagem registrado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-cat-logo-dados/14-CONTEXT.md
@.planning/phases/14-cat-logo-dados/14-RESEARCH.md
@.planning/phases/14-cat-logo-dados/14-DIAGNOSTICO.md

<interfaces>
Padrão de migration idempotente do projeto (fonte: 20260602000001_product_variants_ativo.sql):
  BEGIN;
  UPDATE public.product_variants SET tipo_produto = 'perfil'
    WHERE codigo IN ('LM3475','LM3291',...)            -- lista explícita do 14-DIAGNOSTICO.md
      AND tipo_produto IS DISTINCT FROM 'perfil';        -- guarda idempotente
  COMMIT;

Valores válidos tipo_produto: fita, driver, perfil, spot, lampada, acessorio, conector, suporte (ou NULL)
Valores válidos sistema:      padrao, tiny_magneto, magneto_48v, s_mode, trilha (ou NULL)

DML mira a TABELA public.product_variants — NUNCA a view public.produtos (Pitfall 1/3).

AmbienteCard.tsx:81 (regex atual, frágil):
  if (produto.sistema_magnetico === 'magneto_48v' || /MAGNETO22/.test(d)) {
AmbienteCard.tsx:89 (branch TINY, captura indevidamente se o de cima falha):
  } else if (produto.sistema_magnetico === 'tiny_magneto' || /TINY\s+MAG/.test(d)) {
Substituição segura (se o diagnóstico exigir broaden do regex):
  if (produto.sistema_magnetico === 'magneto_48v' || (/MAGNETO/.test(d) && !/TINY/.test(d))) {
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever migration idempotente de tipo_produto + (se preciso) sistema MAGNETO</name>
  <read_first>
    - .planning/phases/14-cat-logo-dados/14-DIAGNOSTICO.md (lista "## SKUs aprovados (para migration)" + decisão CAT-02)
    - .planning/phases/14-cat-logo-dados/14-RESEARCH.md (seção "Code Examples" — estrutura de migration; "Migration File Naming")
    - supabase/migrations/20260602000001_product_variants_ativo.sql (padrão BEGIN/COMMIT idempotente do projeto)
    - supabase/migrations/20260319000001_campos_tecnicos_produtos.sql (CHECK constraints — valores válidos)
  </read_first>
  <action>
    Criar `supabase/migrations/20260610000001_tipo_produto_correcao_catalogos.sql` (ajustar o sufixo
    numérico se já existir migration com essa data; usar a próxima sequência disponível).

    Estrutura obrigatória:
    1. Header comment com título, "Phase 14 / Plan 02", e bloco "-- ROLLBACK:" listando os UPDATEs
       inversos (restaurar valores anteriores conforme baseline da Query A / valores reais por SKU).
    2. `BEGIN;` ... `COMMIT;` envolvendo TODO o DML.
    3. Um UPDATE por grupo aprovado, mirando `public.product_variants`, com `WHERE codigo IN (...lista
       explícita do 14-DIAGNOSTICO.md...)` AND guarda idempotente `tipo_produto IS DISTINCT FROM '<alvo>'`.
       Cada alvo deve ser um valor válido do CHECK (perfil/driver/spot/lampada/acessorio/conector/suporte).
    4. WALL WASHER (D-02b): `SET tipo_produto='perfil'` — usar lista explícita de codigo se o
       DIAGNOSTICO a fornecer; caso o DIAGNOSTICO tenha aprovado por descrição, usar
       `WHERE descricao ILIKE '%WALL WASHER%' AND tipo_produto IS DISTINCT FROM 'perfil'`.
    5. CAT-02 (somente se o DIAGNOSTICO marcou fix de DADO): UPDATE
       `SET sistema='magneto_48v' WHERE codigo IN (<codigos MAGNETO 48V do DIAGNOSTICO>) AND sistema IS DISTINCT FROM 'magneto_48v'`.

    NÃO incluir nenhum UPDATE sem WHERE. NÃO mirar a view `produtos`. NÃO usar o literal inválido
    'wall_washer' como valor de tipo_produto.
  </action>
  <verify>
    <automated>MISSING — migration SQL não tem unit test; verificação por inspeção de conteúdo + aplicação (Task 2) + Playwright (Plano 03).</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo supabase/migrations/20260610000001_tipo_produto_correcao_catalogos.sql existe (ou com sufixo de sequência ajustado)
    - Contém "BEGIN;" e "COMMIT;"
    - Contém ao menos um "WHERE codigo IN"
    - Contém o comentário "-- ROLLBACK:" no header
    - Todo UPDATE mira "UPDATE public.product_variants" (nenhum "UPDATE public.produtos")
    - Todo UPDATE tem guarda idempotente "IS DISTINCT FROM"
    - tipo_produto WALL WASHER setado para 'perfil' (string 'wall_washer' NÃO aparece como valor de SET)
    - Se CAT-02 = fix de dado: existe UPDATE SET sistema = 'magneto_48v' com guarda IS DISTINCT FROM
  </acceptance_criteria>
  <done>Migration idempotente, transacional, com rollback documentado, materializando a lista aprovada do Plano 01.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: [BLOCKING] Aplicar a migration na base de produção (supabase db push) + delta de contagem</name>
  <read_first>
    - supabase/migrations/20260610000001_tipo_produto_correcao_catalogos.sql (a migration da Task 1)
    - .planning/phases/14-cat-logo-dados/14-DIAGNOSTICO.md (baseline Query A para calcular delta)
  </read_first>
  <action>
    Aplicar a migration na base de produção `jkewlaezvrbuicmncqbj`. Garantir `SUPABASE_ACCESS_TOKEN`
    no ambiente, então rodar `supabase db push` a partir de `auraoramentos/`. Se o `supabase db push`
    exigir confirmação interativa não suprimível neste ambiente non-TTY, PARAR e pedir a Lenny que
    rode o push manualmente (por isso este plano é autonomous: false) — não tentar aplicar via caminho
    alternativo que burle o histórico de migrations.

    Após aplicar, re-rodar a Query A (contagem por tipo_produto) e comparar com a baseline gravada no
    14-DIAGNOSTICO.md. Registrar o DELTA (quantos produtos mudaram por tipo) em 14-DIAGNOSTICO.md sob
    "## Delta pós-migration (D-04)". O número total de SKUs que mudaram deve bater com o tamanho da
    lista aprovada.
  </action>
  <verify>
    <automated>supabase migration list (ou consulta à tabela supabase_migrations.schema_migrations) mostra a versão 20260610000001 aplicada</automated>
  </verify>
  <acceptance_criteria>
    - A migration 20260610000001 consta como aplicada na base de produção (supabase migration list)
    - SELECT tipo_produto, COUNT(*) ... pós-migration mostra aumento em 'perfil'/'driver' coerente com a lista aprovada
    - 14-DIAGNOSTICO.md contém heading "## Delta pós-migration (D-04)" com a contagem antes vs depois
    - Re-rodar a migration não altera mais nenhuma linha (idempotência confirmada — guardas IS DISTINCT FROM)
  </acceptance_criteria>
  <done>Migration aplicada na DB viva; delta de contagem registrado e coerente com a lista aprovada (D-04).</done>
</task>

<task type="auto">
  <name>Task 3: Corrigir o regex/branch MAGNETO em AmbienteCard.tsx (somente se o diagnóstico exigir código)</name>
  <read_first>
    - .planning/phases/14-cat-logo-dados/14-DIAGNOSTICO.md (decisão CAT-02 — se inclui "código")
    - src/components/AmbienteCard.tsx (linhas 76-128, handleSelectProdutoLuminaria)
    - .planning/phases/14-cat-logo-dados/14-RESEARCH.md (seção "Code fix for CAT-02")
  </read_first>
  <action>
    Se o 14-DIAGNOSTICO.md marcou a decisão CAT-02 como "dado apenas" ou "nenhum": NÃO alterar código —
    pular esta task e registrar no SUMMARY que o fix foi 100% no dado (Task 1). Não introduzir mudança
    de código desnecessária.

    Se marcou "código" ou "ambos": em `src/components/AmbienteCard.tsx`, substituir a condição da L81
      DE:  if (produto.sistema_magnetico === 'magneto_48v' || /MAGNETO22/.test(d)) {
      PARA: if (produto.sistema_magnetico === 'magneto_48v' || (/MAGNETO/.test(d) && !/TINY/.test(d))) {
    Isso garante que um produto descrito como "MAGNETO 48V..." (sem o literal "MAGNETO22") caia no
    branch correto e que o branch TINY (L89) só capture quando NÃO houver match de MAGNETO 48V acima.
    Não tocar no branch TINY (L89) nem na lógica de toast subsequente. Confirmar que o branch MAGNETO
    48V (L81) continua sendo avaliado ANTES do branch TINY (L89).
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
    - Se decisão = dado/nenhum: AmbienteCard.tsx permanece inalterado (git diff vazio para o arquivo)
    - Se decisão = código/ambos: a L81 contém `(/MAGNETO/.test(d) && !/TINY/.test(d))` e NÃO contém mais `/MAGNETO22/`
    - O branch `=== 'magneto_48v'` (L81) aparece textualmente ANTES do branch `=== 'tiny_magneto'` (L89) no arquivo
    - `npm run test` passa (suíte Vitest sem regressão)
    - `npm run lint` sem novos erros no arquivo alterado
  </acceptance_criteria>
  <done>Causa-raiz CAT-02 corrigida na camada certa (dado e/ou código), sem mascarar o problema (D-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → product_variants (escrita) | DML de UPDATE em produção; roda como role postgres (fora do RLS) |
| Catálogo recategorizado → orçamentos salvos | Snapshot jsonb autocontido; sem FK — recategorização não propaga |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | UPDATE amplo sem guarda re-executado corrompe dado | mitigate | Todo UPDATE tem `WHERE codigo IN (...)` explícito + guarda `IS DISTINCT FROM '<alvo>'` (D-07); BEGIN/COMMIT garante atomicidade |
| T-14-04 | Tampering | DML mira a view `produtos` (não-atualizável) e falha/silencia | mitigate | Acceptance criteria força "UPDATE public.product_variants"; nenhum "UPDATE public.produtos" (Pitfall 1/3) |
| T-14-05 | Tampering | Recategorização muta orçamentos existentes | accept | Snapshot jsonb em orcamentos.ambientes é denormalizado, sem FK para product_variants (D-06, VERIFICADO em RESEARCH.md); validado no Plano 03 |
| T-14-06 | Elevation of Privilege | Migration precisa de privilégio admin | accept | Migrations Supabase rodam como postgres fora do RLS; nenhuma policy de product_variants alterada (sem regressão de RLS) |
| T-14-07 | Denial of Service | Push falha mid-way deixando DB inconsistente | mitigate | BEGIN/COMMIT envolve todo o DML — falha = rollback total; task [BLOCKING] verifica aplicação via supabase migration list |
</threat_model>

<verification>
- Migration aplicada na produção (supabase migration list mostra 20260610000001)
- Delta de contagem por tipo_produto coerente com a lista aprovada (D-04)
- Re-execução da migration não altera linhas (idempotência D-07)
- Nenhuma policy/RLS de product_variants alterada
- AmbienteCard.tsx só alterado se o diagnóstico exigiu código (D-03)
</verification>

<success_criteria>
- CAT-01: tipo_produto corrigido para todos os grupos aprovados, WALL WASHER='perfil' (D-02, D-02b)
- CAT-02: causa-raiz corrigida na camada certa (dado/código/ambos), sem workaround (D-03)
- Migration idempotente, transacional, com rollback documentado (D-07)
- Migration efetivamente aplicada na DB de produção (não só no disco)
</success_criteria>

<output>
After completion, create `.planning/phases/14-cat-logo-dados/14-02-SUMMARY.md`
</output>
