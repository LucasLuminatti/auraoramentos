---
phase: 14-cat-logo-dados
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - e2e/catalogo.spec.ts
autonomous: false
requirements: [CAT-01, CAT-02]
must_haves:
  truths:
    - "WALL WASHER aparece na busca do seletor de perfil"
    - "CANTONEIRA, LM3475, LM3291 aparecem no seletor correto (perfil/driver)"
    - "Ao adicionar produto MAGNETO 48V, o toast descreve o MAGNETO (Trilho magnético 48V), não o TINY (24V)"
    - "Um orçamento antigo continua abrindo intacto e renderiza seus itens do snapshot jsonb (D-06)"
  artifacts:
    - path: "e2e/catalogo.spec.ts"
      provides: "Teste Playwright: busca de perfil/driver encontra as famílias corrigidas + toast MAGNETO correto"
      contains: "WALL WASHER"
  key_links:
    - from: "e2e/catalogo.spec.ts"
      to: "seletor de perfil/driver no wizard Step 2"
      via: "page.getByRole textbox Código + assert opção visível"
      pattern: "getByRole"
---

<objective>
Validar — automaticamente (Playwright) e manualmente — que as correções dos Planos 01-02 surtiram
efeito no app rodando contra a base de produção: as famílias antes invisíveis aparecem nos seletores
corretos, a dica do MAGNETO está certa, e orçamentos antigos continuam intactos.

Implementa D-05 (validação Playwright + manual de WALL WASHER/CANTONEIRA/LM3475/LM3291 e da dica
MAGNETO) e D-06 (snapshot antigo intacto). Cobre os 4 Success Criteria do roadmap Phase 14.

Purpose: É o gate da fase — sem esta validação não há prova de que o catálogo está corrigido para o
colaborador. Os critérios 1-3 do roadmap dependem da migration aplicada (Plano 02) E confirmada aqui.
Output: e2e/catalogo.spec.ts passando + checklist manual confirmado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-cat-logo-dados/14-CONTEXT.md
@.planning/phases/14-cat-logo-dados/14-RESEARCH.md
@.planning/phases/14-cat-logo-dados/14-DIAGNOSTICO.md

<interfaces>
Padrão de teste Playwright do projeto (fonte: e2e/wizard.spec.ts) — roda contra PROD, limpa via service role:
  - navega "/", abre cliente → projeto → "Novo Orçamento" → Step 1 (combobox tipo) → "Próximo"
  - Step 2: "Adicionar Ambiente" → "Adicionar Luminária" / botões de sistema
  - autocomplete: page.getByRole("textbox", { name: /Código do item/i }).first().fill(CODIGO)
  - opção: page.getByRole("button", { name: new RegExp(`${CODIGO}.*DESCR`, "i") })
  - afterEach apaga o rascunho de teste via e2e/helpers/supabaseAdmin.ts (snapshotOrcamentoIds / deleteOrcamentoDeTeste)
  - comando: npm run test:e2e

O seletor de perfil/driver é um campo de busca separado por componente do sistema (perfil|fita|driver),
ver AmbienteCard.tsx handleSelectProdutoSistema. A dica MAGNETO dispara via toast (sonner) ao selecionar
luminária MAGNETO 48V — texto correto contém "48V" / "Trilho magnético 48V"; o ERRADO (TINY) contém "24V" / "Tiny Mag".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever teste Playwright de catálogo (seletores + dica MAGNETO)</name>
  <files>e2e/catalogo.spec.ts</files>
  <read_first>
    - e2e/wizard.spec.ts (padrão completo de fluxo, fingerprint cleanup, seletores getByRole)
    - e2e/helpers/supabaseAdmin.ts (helpers de cleanup snapshotOrcamentoIds/deleteOrcamentoDeTeste)
    - .planning/phases/14-cat-logo-dados/14-DIAGNOSTICO.md (codigos reais confirmados de WALL WASHER, CANTONEIRA, LM3475, LM3291 e do MAGNETO 48V)
    - src/components/AmbienteCard.tsx (linhas 80-95 — texto exato do toast MAGNETO 48V vs TINY)
  </read_first>
  <action>
    Criar `e2e/catalogo.spec.ts` seguindo o padrão de wizard.spec.ts (mesma estrutura de beforeEach/
    afterEach para fingerprint + cleanup via service role; rodar contra PROD).

    Teste 1 — seletores de perfil/driver encontram as famílias corrigidas:
      Navegar até o Step 2, abrir um sistema de iluminação, e no campo de busca do componente correto
      (perfil para WALL WASHER/CANTONEIRA/LM3475 conforme o ALVO aprovado; driver se algum foi
      classificado como driver), digitar o codigo real (do DIAGNOSTICO) e asserir que a opção
      correspondente fica visível (page.getByRole("button", { name: new RegExp(codigo, "i") })
      .toBeVisible). Cobrir os 4 itens do roadmap: WALL WASHER, CANTONEIRA, LM3475, LM3291, cada um no
      seletor que recebeu o tipo_produto alvo aprovado.

    Teste 2 — dica MAGNETO correta:
      Adicionar uma luminária, buscar o codigo real do MAGNETO 48V, selecionar, e asserir que aparece
      um toast contendo "48V" (ou "Trilho magnético 48V") e que NÃO aparece toast contendo "Tiny Mag"
      / "24V". Usar page.getByText com regex.

    Reaproveitar os helpers de cleanup; não deixar rascunho de teste em PROD.
  </action>
  <verify>
    <automated>npm run test:e2e -- catalogo</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo e2e/catalogo.spec.ts existe e contém as strings "WALL WASHER", "CANTONEIRA", "LM3475", "LM3291", "MAGNETO"
    - Importa os helpers de e2e/helpers/supabaseAdmin.ts e tem afterEach de cleanup (não deixa lixo em PROD)
    - Teste 1: asserts de visibilidade da opção para cada uma das 4 famílias no seletor correto
    - Teste 2: assert positivo de toast com "48V" e assert negativo de "Tiny Mag"/"24V"
    - `npm run test:e2e -- catalogo` passa
  </acceptance_criteria>
  <done>e2e/catalogo.spec.ts cobre os 4 itens de busca + a dica MAGNETO e passa contra PROD.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação manual — seletores, dica MAGNETO e orçamento antigo intacto (D-05, D-06)</name>
  <what-built>
    Migration de tipo_produto aplicada (Plano 02) + (se preciso) fix de regex MAGNETO + teste Playwright
    de catálogo. Esta verificação confirma o comportamento no app real e a integridade de orçamentos antigos.
  </what-built>
  <action>
    Após o teste Playwright (Task 1) passar, executar manualmente os 5 passos de how-to-verify no app
    apontado para a MESMA base de produção que recebeu a migration, e registrar o resultado de cada
    passo (aprovado / falha + descrição) no 14-03-SUMMARY.md.
  </action>
  <how-to-verify>
    1. Abrir o app (PROD: auraoramentos-kappa.vercel.app, ou dev em localhost:8080 apontando para a mesma DB).
    2. Iniciar um orçamento → Step 2 → adicionar um sistema de iluminação.
    3. No seletor de PERFIL, buscar "WALL WASHER" → o produto DEVE aparecer. Repetir com "CANTONEIRA",
       "LM3475", "LM3291" nos seletores correspondentes (perfil/driver conforme o alvo aprovado) → todos DEVEM aparecer.
    4. Adicionar uma luminária e selecionar um produto MAGNETO 48V → a dica/toast DEVE descrever o
       MAGNETO ("Trilho magnético 48V", driver 100W/200W LM2343/LM2344) e NÃO o TINY (24V / LM3168/LM3169).
    5. Abrir um orçamento ANTIGO já salvo (de antes desta fase) → DEVE abrir normalmente, com todos os
       itens renderizando do snapshot, sem erro e sem alteração de categorização nos itens já salvos (D-06).
  </how-to-verify>
  <read_first>
    - .planning/phases/14-cat-logo-dados/14-DIAGNOSTICO.md (codigos reais para buscar)
    - .planning/ROADMAP.md (Phase 14 Success Criteria 1-4)
  </read_first>
  <acceptance_criteria>
    - Os 5 passos executados e o resultado de cada um registrado no 14-03-SUMMARY.md
    - Passos 3 e 4 aprovados (4 famílias visíveis + dica MAGNETO correta)
    - Passo 5 aprovado (orçamento antigo abre intacto, sem mutação dos itens salvos)
  </acceptance_criteria>
  <resume-signal>Type "approved" ou descreva quais itens não apareceram / dica errada / orçamento antigo quebrado</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Teste E2E → PROD (escrita temporária) | Cria rascunho de orçamento em produção, apagado no afterEach via service role |
| Catálogo recategorizado → orçamento antigo | Leitura do snapshot jsonb; verificação de que não houve mutação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Tampering | Teste E2E deixa rascunho de teste em PROD | mitigate | afterEach com fingerprint (id novo + tipo + valor + status) apaga só a linha de teste; assert toBeLessThanOrEqual(1) (padrão wizard.spec.ts) |
| T-14-09 | Information Disclosure | Orçamento antigo expõe dado recategorizado erroneamente | accept | Snapshot jsonb é autocontido; Task 2 passo 5 confirma que itens salvos não mudaram (D-06) |
</threat_model>

<verification>
- e2e/catalogo.spec.ts passa (4 famílias visíveis + dica MAGNETO correta)
- Verificação manual confirma os 4 Success Criteria do roadmap
- Orçamento antigo abre intacto (D-06)
</verification>

<success_criteria>
- Roadmap Success Criteria 1: WALL WASHER aparece no seletor de perfil
- Roadmap Success Criteria 2: CANTONEIRA, LM3475, LM3291 aparecem no seletor correto
- Roadmap Success Criteria 3: dica do MAGNETO descreve o MAGNETO (não o TINY)
- Roadmap Success Criteria 4: orçamentos antigos continuam abrindo sem perder dados (D-06)
</success_criteria>

<output>
After completion, create `.planning/phases/14-cat-logo-dados/14-03-SUMMARY.md`
</output>
