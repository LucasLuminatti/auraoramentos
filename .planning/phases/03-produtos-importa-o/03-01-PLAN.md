---
phase: 03-produtos-importa-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260501000001_products_and_variants.sql
  - supabase/migrations/20260501000002_storage_bucket_produtos_imagens.sql
  - src/integrations/supabase/types.ts
autonomous: true
requirements: [PROD-01, PROD-02, IMP-01, IMP-02, IMP-03, IMP-04, IMP-05, IMP-06]
tags: [schema, migration, storage, rls, foundation]
user_setup: []

must_haves:
  truths:
    - "Tabela `products` existe em prod com colunas (id, codigo_pai, nome, categoria, tipologia, created_at)"
    - "Tabela `product_variants` existe em prod (renomeada de `produtos`, todas as 4646 linhas preservadas com mesmos UUIDs e codigos)"
    - "Toda variant existente tem product_id apontando para products.codigo_pai='P-LEGADO'"
    - "View `produtos` retorna SELECT idêntico ao da tabela antiga (5 leituras client-side e FK vinculos_spot_lampada continuam funcionais)"
    - "Bucket `produtos-imagens` existe no Supabase Storage com leitura pública e escrita restrita a admin"
    - "src/integrations/supabase/types.ts tem `products`, `product_variants` e `produtos` (Views) tipados"
    - "RLS habilitado em products + product_variants com policies (read autenticado / write admin)"
  artifacts:
    - path: "supabase/migrations/20260501000001_products_and_variants.sql"
      provides: "Migration aditiva: cria products, renomeia produtos→product_variants, adiciona colunas, cria view produtos, RLS"
      contains: "ALTER TABLE public.produtos RENAME TO product_variants"
    - path: "supabase/migrations/20260501000002_storage_bucket_produtos_imagens.sql"
      provides: "Bucket Storage produtos-imagens com policies"
      contains: "INSERT INTO storage.buckets"
    - path: "src/integrations/supabase/types.ts"
      provides: "Tipos auto-gerados pós-migration (products, product_variants, produtos View)"
      contains: "product_variants:"
  key_links:
    - from: "supabase/migrations/20260501000001_products_and_variants.sql"
      to: "produção (Supabase jkewlaezvrbuicmncqbj)"
      via: "supabase db push (BLOCKING task)"
      pattern: "supabase db push"
    - from: "view produtos"
      to: "product_variants"
      via: "SELECT em colunas mapeadas"
      pattern: "CREATE VIEW public.produtos AS"
    - from: "vinculos_spot_lampada FKs"
      to: "product_variants.codigo"
      via: "FK preservada via RENAME (não destrutivo)"
      pattern: "RENAME TO product_variants"
---

<objective>
Criar a base de schema da Phase 3: tabela `products` (pais), renomear `produtos` → `product_variants` (não-destrutivo, preserva FKs e UUIDs), adicionar colunas (product_id, origem, editado_manualmente, atributos jsonb, nome), criar view `produtos` para backward-compat, habilitar RLS, criar bucket Supabase Storage `produtos-imagens` com policies. Aplicar via `supabase db push` e regenerar types.

Purpose: foundation que destrava todos os outros plans da Phase 3. Sem ela, reconcile() não tem onde escrever, AU coringa não tem destino, ProdutoEditDialog estendido não tem coluna `editado_manualmente`, e bulk image upload não tem bucket. Migration usa estratégia de RENAME (não DROP+CREATE) para preservar FK `vinculos_spot_lampada` e os UUIDs já presentes em snapshots.

Output: 2 migrations SQL aplicadas em prod + types.ts regenerado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-produtos-importa-o/03-CONTEXT.md
@.planning/phases/03-produtos-importa-o/03-RESEARCH.md
@CLAUDE.md
@supabase/migrations/20260319000003_vinculos_spot_lampada.sql
@supabase/migrations/20260423000001_create_arquitetos.sql
@supabase/migrations/20260423000003_produtos_arquiteto.sql
@src/integrations/supabase/types.ts

<interfaces>
<!-- Schema atual de produtos (target da migração — vai ser renomeado para product_variants) -->
<!-- Da inspeção em src/integrations/supabase/types.ts:438-528 -->

Tabela `produtos` (atual, 4646 linhas):
- id UUID PK
- codigo TEXT UNIQUE NOT NULL  ← chave de negócio, REFERENCED por vinculos_spot_lampada.codigo_spot e codigo_lampada
- descricao TEXT NOT NULL
- preco_tabela NUMERIC NOT NULL DEFAULT 0
- preco_minimo NUMERIC NOT NULL DEFAULT 0
- imagem_url TEXT NULL
- arquiteto_id UUID NULL REFERENCES arquitetos(id) ON DELETE SET NULL  ← Phase 2
- created_at TIMESTAMPTZ NOT NULL DEFAULT now()
- (campos técnicos opcionais): tensao INT, watts_por_metro NUMERIC, largura_mm INT, tipo_produto TEXT, subtipo TEXT, sistema TEXT, familia_perfil TEXT, passadas_padrao INT, largura_canal_mm INT, driver_max_watts INT, driver_tipo_permitido TEXT, somente_baby BOOLEAN, tamanho_rolo_m INT, fator_spot NUMERIC, potencia_watts NUMERIC, cor TEXT, aplicacao TEXT

CHECK constraints existentes (preservar via RENAME):
- check_tensao: tensao IS NULL OR tensao IN (12, 24, 48)
- check_tamanho_rolo: tamanho_rolo_m IS NULL OR tamanho_rolo_m IN (5, 10, 15)
- (outros, ver migration 20260319000001)

FKs externas que apontam para produtos.codigo (vão sobreviver ao rename):
- vinculos_spot_lampada.codigo_spot REFERENCES produtos(codigo) ON DELETE CASCADE
- vinculos_spot_lampada.codigo_lampada REFERENCES produtos(codigo) ON DELETE RESTRICT

Função RBAC já existente:
- public.has_role(auth.uid() UUID, 'admin'::TEXT) RETURNS BOOLEAN

Bucket Storage atual:
- 'produto-imagens' (singular) — usado em ImportImagens.tsx:141. Phase 3 cria 'produtos-imagens' (plural, conforme D-14).

5 leituras client-side de `produtos` que devem continuar funcionando via view de compat:
- src/pages/Admin.tsx:96 — supabase.from("produtos").select("*", { count: "exact" })
- src/components/ProdutoEditDialog.tsx:74 — supabase.from("produtos").update(...) [WRITE — incompatível com view, será migrado para product_variants no Plan 03]
- src/components/ImportImagens.tsx:144 — supabase.from("produtos").update({ imagem_url }) [WRITE — incompatível com view, será migrado no Plan 04]
- supabase/functions/import-produtos/index.ts — UPSERT (WRITE — será migrado no Plan 04)
- src/hooks/useProdutoSearch.ts — SELECT (compatível com view)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration 20260501000001_products_and_variants.sql</name>
  <files>supabase/migrations/20260501000001_products_and_variants.sql</files>
  <read_first>
    - supabase/migrations/20260319000003_vinculos_spot_lampada.sql (pattern has_role + RLS + FK em produtos.codigo)
    - supabase/migrations/20260423000001_create_arquitetos.sql (pattern de criação de tabela aditiva com RLS)
    - supabase/migrations/20260423000003_produtos_arquiteto.sql (último ALTER em produtos antes desta phase)
    - supabase/migrations/20260319000001_campos_tecnicos_produtos.sql (CHECK constraints existentes, não derrubar)
    - src/integrations/supabase/types.ts (linhas 438-528: schema atual de produtos)
  </read_first>
  <action>
Criar arquivo `supabase/migrations/20260501000001_products_and_variants.sql` com EXATAMENTE este conteúdo (SQL ipsis litteris — NÃO simplificar, NÃO renomear colunas, NÃO usar v1):

```sql
-- Phase 3 / Plan 01: cria tabelas products + product_variants (rename de produtos)
-- Estratégia: ALTER TABLE produtos RENAME TO product_variants preserva TODAS as FKs
-- (vinculos_spot_lampada.codigo_spot/codigo_lampada continuam válidas — só o nome da
--  tabela base muda; o constraint move com ela). View `produtos` recriada para backward-compat.
-- Refs: D-01, D-02, D-03, D-04 (CONTEXT.md Phase 3) + Pattern 1 (RESEARCH.md)

BEGIN;

-- 1. Cria tabela products (pais)
CREATE TABLE public.products (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  codigo_pai TEXT UNIQUE NOT NULL,
  nome TEXT NOT NULL,
  categoria TEXT,
  tipologia TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_products_codigo_pai ON public.products(codigo_pai);
CREATE INDEX idx_products_categoria ON public.products(categoria);

COMMENT ON TABLE public.products IS 'Produto pai (master 2026 + dummy P-LEGADO + AU coringa). Phase 3.';

-- 2. Cria pai dummy P-LEGADO (D-04: recebe FK de TODOS os SKUs legados)
INSERT INTO public.products (codigo_pai, nome, categoria, tipologia)
  VALUES ('P-LEGADO', 'Produtos Legados', NULL, NULL);

-- 3. Renomeia produtos -> product_variants (preserva TODAS as FKs externas e UUIDs)
ALTER TABLE public.produtos RENAME TO product_variants;

-- 4. Adiciona colunas novas em product_variants
ALTER TABLE public.product_variants
  ADD COLUMN product_id UUID REFERENCES public.products(id),
  ADD COLUMN origem TEXT NOT NULL DEFAULT 'legado'
    CHECK (origem IN ('master', 'legado', 'coringa', 'manual')),
  ADD COLUMN editado_manualmente BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN atributos JSONB NOT NULL DEFAULT '{}'::jsonb,
  ADD COLUMN nome TEXT;

-- 5. Backfill: vincula TODOS os SKUs existentes a P-LEGADO (D-04 / D-06)
UPDATE public.product_variants
  SET product_id = (SELECT id FROM public.products WHERE codigo_pai = 'P-LEGADO');

-- 6. NOT NULL após backfill
ALTER TABLE public.product_variants
  ALTER COLUMN product_id SET NOT NULL;

CREATE INDEX idx_product_variants_product_id ON public.product_variants(product_id);
CREATE INDEX idx_product_variants_origem ON public.product_variants(origem);
CREATE INDEX idx_product_variants_editado ON public.product_variants(editado_manualmente);

COMMENT ON TABLE public.product_variants IS 'SKU. FK para products. Phase 3 (renomeada de produtos). Não DROP/RECREATE — preserva UUIDs e FKs externas.';
COMMENT ON COLUMN public.product_variants.origem IS 'master=veio da planilha base; legado=já estava no DB sem master; coringa=AU001..16; manual=criado pelo admin via UI';
COMMENT ON COLUMN public.product_variants.editado_manualmente IS 'Setado TRUE quando admin edita via ProdutoEditDialog (D-08). Master subsequente respeita (D-05).';
COMMENT ON COLUMN public.product_variants.atributos IS 'Specs variáveis da master (Lumens, IRC, Material, Cabo, Dimensao, etc.) — D-02';
COMMENT ON COLUMN public.product_variants.nome IS 'Nome da variante (ex: "VISION 5W"). Diferente de products.nome (família, ex: "Arandela VISION").';

-- 7. RLS em products (replica padrão de produtos antigo)
ALTER TABLE public.products ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Anyone authenticated can read products"
  ON public.products FOR SELECT TO authenticated USING (true);

CREATE POLICY "Admins manage products"
  ON public.products FOR ALL TO authenticated
  USING (public.has_role(auth.uid(), 'admin'))
  WITH CHECK (public.has_role(auth.uid(), 'admin'));

-- 8. RLS em product_variants (RLS já estava em produtos; reaplicar policies usando o novo nome)
ALTER TABLE public.product_variants ENABLE ROW LEVEL SECURITY;

-- Drop possíveis policies antigas com o nome de "produtos" (se existirem) e recriar
DROP POLICY IF EXISTS "Anyone authenticated can read produtos" ON public.product_variants;
DROP POLICY IF EXISTS "Admins manage produtos" ON public.product_variants;

CREATE POLICY "Anyone authenticated can read product_variants"
  ON public.product_variants FOR SELECT TO authenticated USING (true);

CREATE POLICY "Admins manage product_variants"
  ON public.product_variants FOR ALL TO authenticated
  USING (public.has_role(auth.uid(), 'admin'))
  WITH CHECK (public.has_role(auth.uid(), 'admin'));

-- 9. View de compatibilidade `produtos` (backward-compat — D-03 + Pattern 1 RESEARCH)
-- Expõe MESMAS colunas que a tabela antiga tinha. Leitura idêntica.
-- WRITES (UPDATE/INSERT/DELETE) NÃO funcionam direto na view — devem ir para product_variants.
-- Plans 03 e 04 migram os 3 writes existentes (ProdutoEditDialog, ImportImagens, edge fn).
CREATE VIEW public.produtos AS
  SELECT
    pv.id,
    pv.codigo,
    pv.descricao,
    pv.preco_tabela,
    pv.preco_minimo,
    pv.imagem_url,
    pv.tensao,
    pv.watts_por_metro,
    pv.largura_mm,
    pv.tipo_produto,
    pv.subtipo,
    pv.sistema,
    pv.familia_perfil,
    pv.passadas_padrao,
    pv.largura_canal_mm,
    pv.driver_max_watts,
    pv.driver_tipo_permitido,
    pv.somente_baby,
    pv.tamanho_rolo_m,
    pv.fator_spot,
    pv.potencia_watts,
    pv.cor,
    pv.aplicacao,
    pv.arquiteto_id,
    pv.created_at
  FROM public.product_variants pv;

COMMENT ON VIEW public.produtos IS 'View de compatibilidade backward-compat. Lê de product_variants. Não use para INSERT/UPDATE/DELETE — vá direto em product_variants.';

-- 10. Permitir leitura da view ao role authenticated (views herdam permissions da base + grants)
GRANT SELECT ON public.produtos TO authenticated;
GRANT SELECT ON public.produtos TO anon;

COMMIT;
```

**Notas críticas (NÃO altere):**
- D-04 explicitamente locka `P-LEGADO` como pai dummy de TODOS os SKUs existentes. NÃO criar pai sem nome ou pular o backfill.
- ALTER TABLE RENAME preserva todas as FK (`vinculos_spot_lampada` continua válida). NÃO usar CREATE+COPY+DROP.
- View tem que listar TODAS as colunas que `produtos` tinha (lista exata em src/integrations/supabase/types.ts:438-528). Não pode esquecer arquiteto_id, todos os campos técnicos, etc.
- CHECK constraint do origem inclui 'manual' (resolvido em RESEARCH Open Question 1).
- Migration NÃO toca preco_tabela/preco_minimo (D-19 — IMP-02 deferido, schema reservado).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260501000001_products_and_variants.sql && grep -q "ALTER TABLE public.produtos RENAME TO product_variants" supabase/migrations/20260501000001_products_and_variants.sql && grep -q "CREATE VIEW public.produtos AS" supabase/migrations/20260501000001_products_and_variants.sql && grep -q "P-LEGADO" supabase/migrations/20260501000001_products_and_variants.sql && grep -q "CHECK (origem IN" supabase/migrations/20260501000001_products_and_variants.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260501000001_products_and_variants.sql` existe
    - Contém literalmente `CREATE TABLE public.products` (cria pai)
    - Contém literalmente `ALTER TABLE public.produtos RENAME TO product_variants` (não DROP)
    - Contém literalmente `INSERT INTO public.products (codigo_pai, nome, categoria, tipologia) VALUES ('P-LEGADO'` (cria pai dummy)
    - Contém literalmente `UPDATE public.product_variants SET product_id = (SELECT id FROM public.products WHERE codigo_pai = 'P-LEGADO')` (backfill)
    - Contém `ALTER COLUMN product_id SET NOT NULL` (após backfill)
    - Contém `CHECK (origem IN ('master', 'legado', 'coringa', 'manual'))` (4 valores)
    - Contém `editado_manualmente BOOLEAN NOT NULL DEFAULT false`
    - Contém `atributos JSONB NOT NULL DEFAULT '{}'::jsonb`
    - Contém `CREATE VIEW public.produtos AS` com SELECT de pelo menos 24 colunas (id, codigo, descricao, preco_tabela, preco_minimo, imagem_url, tensao, watts_por_metro, largura_mm, tipo_produto, subtipo, sistema, familia_perfil, passadas_padrao, largura_canal_mm, driver_max_watts, driver_tipo_permitido, somente_baby, tamanho_rolo_m, fator_spot, potencia_watts, cor, aplicacao, arquiteto_id, created_at)
    - Contém `ENABLE ROW LEVEL SECURITY` em ambas tabelas products e product_variants
    - Contém `public.has_role(auth.uid(), 'admin')` em policies de write
    - Contém `GRANT SELECT ON public.produtos TO authenticated`
    - Começa com `BEGIN;` e termina com `COMMIT;`
  </acceptance_criteria>
  <done>Arquivo de migration criado com todos os elementos acima e estrutura DDL válida.</done>
</task>

<task type="auto">
  <name>Task 2: Criar migration 20260501000002_storage_bucket_produtos_imagens.sql</name>
  <files>supabase/migrations/20260501000002_storage_bucket_produtos_imagens.sql</files>
  <read_first>
    - supabase/migrations/20260319000003_vinculos_spot_lampada.sql (pattern has_role)
    - src/components/ImportImagens.tsx (linhas 141-143: uso atual do bucket 'produto-imagens' singular)
    - .planning/phases/03-produtos-importa-o/03-RESEARCH.md (Pattern 3 + Pitfall 7)
  </read_first>
  <action>
Criar arquivo `supabase/migrations/20260501000002_storage_bucket_produtos_imagens.sql` com EXATAMENTE este conteúdo:

```sql
-- Phase 3 / Plan 01: cria bucket Supabase Storage 'produtos-imagens' (plural, conforme D-14)
-- Bucket antigo 'produto-imagens' (singular) usado em ImportImagens.tsx:141 NÃO é tocado nesta migration.
-- Plan 04 será responsável por migrar referências e (se houver dados) migrar arquivos via UPDATE em massa
-- de imagem_url usando REPLACE('produto-imagens', 'produtos-imagens'), ou por script de migração de objetos.
-- Refs: D-14, Pattern 3 (RESEARCH), Pitfall 7 (RESEARCH).

BEGIN;

-- 1. Cria bucket público (leitura aberta para PDFs e admin)
INSERT INTO storage.buckets (id, name, public)
  VALUES ('produtos-imagens', 'produtos-imagens', true)
  ON CONFLICT (id) DO NOTHING;

-- 2. Policy: leitura pública (imagens aparecem em PDFs, autocomplete, lista admin)
DROP POLICY IF EXISTS "Anyone can read produtos-imagens" ON storage.objects;
CREATE POLICY "Anyone can read produtos-imagens"
  ON storage.objects FOR SELECT
  USING (bucket_id = 'produtos-imagens');

-- 3. Policy: insert/update/delete restrito a role admin
DROP POLICY IF EXISTS "Admins can manage produtos-imagens" ON storage.objects;
CREATE POLICY "Admins can manage produtos-imagens"
  ON storage.objects FOR ALL
  TO authenticated
  USING (bucket_id = 'produtos-imagens' AND public.has_role(auth.uid(), 'admin'))
  WITH CHECK (bucket_id = 'produtos-imagens' AND public.has_role(auth.uid(), 'admin'));

COMMIT;
```

**Notas:**
- D-14 locka o nome `produtos-imagens` (plural). NÃO usar singular.
- Bucket é público (leitura aberta) porque PDFs e listas precisam de URL pública (Pattern 3 RESEARCH).
- Escrita restrita via `has_role(auth.uid(), 'admin')` — mesmo padrão de `vinculos_spot_lampada` migration.
- NÃO deletar bucket antigo `produto-imagens` aqui (Pitfall 7 — Plan 04 cuida da decisão).
- Se `INSERT INTO storage.buckets` falhar em alguma versão do Supabase (RESEARCH A2), task tem fallback: criar via Dashboard UI (1 clique) com mesmo nome + policies. Documente no SUMMARY se cair nesse caminho.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260501000002_storage_bucket_produtos_imagens.sql && grep -q "INSERT INTO storage.buckets" supabase/migrations/20260501000002_storage_bucket_produtos_imagens.sql && grep -q "produtos-imagens" supabase/migrations/20260501000002_storage_bucket_produtos_imagens.sql && grep -q "has_role(auth.uid(), 'admin')" supabase/migrations/20260501000002_storage_bucket_produtos_imagens.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260501000002_storage_bucket_produtos_imagens.sql` existe
    - Contém `INSERT INTO storage.buckets (id, name, public) VALUES ('produtos-imagens', 'produtos-imagens', true)`
    - Contém `ON CONFLICT (id) DO NOTHING`
    - Contém policy `"Anyone can read produtos-imagens"` com `FOR SELECT USING (bucket_id = 'produtos-imagens')`
    - Contém policy `"Admins can manage produtos-imagens"` com `FOR ALL TO authenticated` e `has_role(auth.uid(), 'admin')`
    - Contém `WITH CHECK (bucket_id = 'produtos-imagens' AND public.has_role(auth.uid(), 'admin'))`
    - Não contém referência a bucket `produto-imagens` (singular — não toca)
  </acceptance_criteria>
  <done>Migration de bucket criada, com leitura pública e escrita restrita a admin.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar migrations em produção via supabase db push</name>
  <files>(nenhum arquivo modificado — efeito é em prod Supabase)</files>
  <read_first>
    - supabase/migrations/20260501000001_products_and_variants.sql (verificar que existe)
    - supabase/migrations/20260501000002_storage_bucket_produtos_imagens.sql (verificar que existe)
    - .planning/STATE.md (Phase 1 SUMMARY confirma supabase CLI já em uso)
  </read_first>
  <action>
**[BLOCKING]** — Esta task é mandatória. Sem ela, types regen falha e plans seguintes não conseguem usar product_variants/products tipados.

Aplicar as duas migrations em produção via Supabase CLI:

```bash
cd C:\Users\lenny\Desktop\Luminatti\automa_aura\auraoramentos
supabase db push
```

**Se `supabase db push` requer autenticação interativa que não pode ser suprimida:**
- Tente: `SUPABASE_ACCESS_TOKEN=<token> supabase db push` (token do user, se setado)
- Se falhar mesmo com env var: marcar task como `autonomous: false` no execute, pedir ao user para rodar `supabase db push` manualmente e reportar saída
- Como último recurso: aplicar migrations manualmente via Supabase Dashboard SQL Editor (cole conteúdo das duas migrations em ordem, executar)

**Verificar resultado em produção:**

Rodar pré-flight (antes de migration aplicar — só pra ter contagem-base):
```bash
# Antes (já rodado em CONTEXT — DB tem 4646 SKUs em produtos)
echo "Antes: 4646 produtos esperados"
```

Rodar pós-aplicação (smoke check, via `supabase db remote sql` ou Dashboard SQL Editor):
```sql
-- 1. Tabela products existe e tem ao menos 1 row (P-LEGADO)
SELECT count(*) FROM public.products WHERE codigo_pai = 'P-LEGADO';
-- esperado: 1

-- 2. product_variants existe com 4646 rows e todas têm product_id NOT NULL apontando para P-LEGADO
SELECT count(*) FROM public.product_variants
  WHERE product_id = (SELECT id FROM public.products WHERE codigo_pai = 'P-LEGADO');
-- esperado: 4646 (todas vinculadas a P-LEGADO inicialmente)

-- 3. View produtos retorna mesma contagem
SELECT count(*) FROM public.produtos;
-- esperado: 4646

-- 4. UUID preservado: pegue qualquer SKU e confirme que UUID em product_variants é o mesmo que estava em produtos antes
SELECT id, codigo FROM public.product_variants LIMIT 1;
-- esperado: UUID válido + codigo (ex: LM2847)

-- 5. FKs externas continuam válidas
SELECT count(*) FROM public.vinculos_spot_lampada;
-- esperado: contagem atual (não pode ter caído pra zero — significa que CASCADE rodou inadvertidamente)

-- 6. Bucket existe
SELECT id, name, public FROM storage.buckets WHERE id = 'produtos-imagens';
-- esperado: 1 row, public=true

-- 7. Origem default = 'legado' em todas as 4646 rows
SELECT origem, count(*) FROM public.product_variants GROUP BY origem;
-- esperado: 'legado' = 4646
```

Se qualquer um dos checks falhar:
- (a) Salvar log do erro;
- (b) NÃO prosseguir com Task 4 — investigar/reverter primeiro;
- (c) Reverter via `supabase db reset` (cuidado em prod) ou criar migration `*_revert_phase_3_plan_01.sql` que reverte ALTER TABLE rename + DROP VIEW + DROP TABLE products + DROP COLUMNS adicionados.

Após sucesso, commitar referência da aplicação no SUMMARY (timestamp + log resumido do output do `supabase db push`).
  </action>
  <verify>
    <automated>supabase migration list 2>&1 | grep -E "20260501000001|20260501000002" | wc -l | grep -q "^2$"</automated>
  </verify>
  <acceptance_criteria>
    - Comando `supabase db push` retornou exit 0
    - `supabase migration list` mostra `20260501000001_products_and_variants` e `20260501000002_storage_bucket_produtos_imagens` aplicadas em remote
    - SQL pós-flight (via Dashboard ou `supabase db remote sql`): `SELECT count(*) FROM public.product_variants` retorna 4646 (mesmo valor que `produtos` tinha antes)
    - SQL pós-flight: `SELECT count(*) FROM public.products WHERE codigo_pai = 'P-LEGADO'` retorna 1
    - SQL pós-flight: `SELECT count(*) FROM public.produtos` (a view) retorna 4646
    - SQL pós-flight: `SELECT count(*) FROM public.vinculos_spot_lampada` retorna a mesma contagem que tinha pre-migration (FKs preservadas — sem CASCADE inadvertido)
    - SQL pós-flight: `SELECT id, name, public FROM storage.buckets WHERE id = 'produtos-imagens'` retorna 1 row, public=true
    - SQL pós-flight: `SELECT origem, count(*) FROM public.product_variants GROUP BY origem` retorna apenas linha 'legado' = 4646 (nenhuma 'master', 'coringa' ou 'manual' ainda)
  </acceptance_criteria>
  <done>Migrations aplicadas em produção. Tabela products + view produtos + bucket existem. UUIDs e FKs preservados.</done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Regenerar src/integrations/supabase/types.ts</name>
  <files>src/integrations/supabase/types.ts</files>
  <read_first>
    - src/integrations/supabase/types.ts (ver shape atual com `produtos` em Tables)
  </read_first>
  <action>
**[BLOCKING]** — Sem types regenerados, TypeScript não enxerga `product_variants` nem `products`, e qualquer chamada `supabase.from("product_variants")` no Plan 02/03/04 dá erro de compilação.

Regenerar types via Supabase CLI:

```bash
cd C:\Users\lenny\Desktop\Luminatti\automa_aura\auraoramentos
npx supabase gen types typescript --project-id jkewlaezvrbuicmncqbj > src/integrations/supabase/types.ts
```

**Sanity check após regeneração:**

```bash
# 1. Confirma que types.ts tem product_variants
grep -q "product_variants:" src/integrations/supabase/types.ts && echo "OK: product_variants typed"

# 2. Confirma que types.ts tem products
grep -q "      products:" src/integrations/supabase/types.ts && echo "OK: products typed"

# 3. Confirma que types.ts ainda tem produtos (agora como Views, não Tables)
grep -q "produtos:" src/integrations/supabase/types.ts && echo "OK: produtos still typed (now via Views)"

# 4. Confirma que tipa colunas novas
grep -q "editado_manualmente" src/integrations/supabase/types.ts && echo "OK: editado_manualmente column visible"
grep -q "origem" src/integrations/supabase/types.ts && echo "OK: origem column visible"
grep -q "atributos" src/integrations/supabase/types.ts && echo "OK: atributos jsonb column visible"

# 5. TS ainda compila
npx tsc --noEmit 2>&1 | tee /tmp/tsc.log
# esperado: exit 0 (zero erros). Pré-existentes em Admin.tsx etc. com `any[]` não impedem build.
# Se aparecer erro novo do tipo "Property 'product_variants' does not exist on type", types não regerou.
```

Se `npx supabase gen types` falhar (auth ou rede):
- Fallback: copiar types via Supabase Dashboard → API → TypeScript types → cole em `src/integrations/supabase/types.ts` (substituindo todo conteúdo)
- Confirmar mesmos sanity checks acima

**IMPORTANTE:** types.ts é arquivo gerado — não editar manualmente. Sempre regenerar via CLI.
  </action>
  <verify>
    <automated>grep -q "product_variants:" src/integrations/supabase/types.ts && grep -q "      products:" src/integrations/supabase/types.ts && grep -q "editado_manualmente" src/integrations/supabase/types.ts && grep -q "atributos" src/integrations/supabase/types.ts && npx tsc --noEmit 2>&1 | grep -v "node_modules" | grep -E "error TS" | wc -l | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `src/integrations/supabase/types.ts` foi sobrescrito (timestamp recente)
    - `grep -q "product_variants:" src/integrations/supabase/types.ts` retorna match
    - `grep -q "      products:" src/integrations/supabase/types.ts` retorna match (com 6 espaços de indent — confirma que está como Table no shape)
    - `grep -q "editado_manualmente" src/integrations/supabase/types.ts` retorna match
    - `grep -q "origem" src/integrations/supabase/types.ts` retorna match
    - `grep -q "atributos" src/integrations/supabase/types.ts` retorna match (coluna jsonb)
    - `grep -q "produtos:" src/integrations/supabase/types.ts` retorna match (agora exposta como View)
    - `npx tsc --noEmit` retorna exit 0 (sem erros TS novos — pré-existentes em `any[]` em Admin.tsx não contam pois `strict: false` no tsconfig)
  </acceptance_criteria>
  <done>types.ts regenerado, compila limpo, expõe product_variants + products + view produtos com colunas novas (origem, editado_manualmente, atributos).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → Supabase Postgres (DDL) | Migration aplicada via `supabase db push` (service_role). Bypassa RLS por design — só admin do projeto Supabase pode rodar |
| Authenticated user → product_variants/products | RLS deve restringir writes a admin via `has_role(auth.uid(), 'admin')` |
| Authenticated user → bucket produtos-imagens | Storage RLS deve restringir uploads a admin |
| Anon user → produtos-imagens (public read) | Bucket público — qualquer um lê via URL. Por design (PDFs e listas precisam de URL pública) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Migration scripts in CI | mitigate | Migration usa BEGIN/COMMIT (transação atômica) — falha em qualquer step reverte tudo. Tests pós-aplicação (Task 3 acceptance criteria) confirmam invariantes. |
| T-03-02 | Tampering | RENAME TO produtos→product_variants quebra FK externas | mitigate | RESEARCH Pattern 1 + Assumption A3 confirma `ALTER TABLE RENAME` preserva FKs. Acceptance criteria de Task 3 inclui SQL check em vinculos_spot_lampada count para validar isso na execução. |
| T-03-03 | Information Disclosure | Bucket público expõe imagens não autorizadas | accept | D-14 explicita bucket público. Imagens de produtos são catálogo aberto (mesmo sem login user vê em PDF). Sem PII. Risco aceitável. |
| T-03-04 | Elevation of Privilege | User não-admin escreve em product_variants/products via SDK | mitigate | RLS policy `FOR ALL TO authenticated USING/WITH CHECK has_role(auth.uid(), 'admin')` — replicado em Task 1 lines 7-8 da estrutura. |
| T-03-05 | Elevation of Privilege | User não-admin faz upload no bucket | mitigate | Storage policy `FOR ALL TO authenticated USING bucket_id='produtos-imagens' AND has_role` — Task 2. |
| T-03-06 | Tampering | View `produtos` permite UPDATE inadvertido | mitigate | Postgres views simples bloqueiam writes por padrão (sem INSTEAD OF triggers). Plans 03/04 migram os 3 writes existentes para `product_variants`. Erro "cannot update view" no console é warning sign documentado em Pitfall 1 (RESEARCH). |
| T-03-07 | Denial of Service | Migration trava em prod por table lock prolongado em UPDATE 4646 rows | accept | 4646 rows é pequeno. UPDATE backfill (`SET product_id = ...`) deve completar em < 1s. Migration roda em transação — se demorar > 30s, abort manual via `pg_terminate_backend()` no Dashboard. |
| T-03-08 | Repudiation | Mudança aplicada sem registro auditável | mitigate | Migrations versionadas em git (`supabase/migrations/`) + STATE.md atualizado pós-phase. `supabase migration list` mostra timestamp de aplicação. |
| T-03-09 | Information Disclosure | Bucket antigo `produto-imagens` ainda existe e é público | accept | Bucket existente; conteúdo já era público pelo design original. Plan 04 decide se migra arquivos ou deleta. Não bloqueia esta migration. |
| T-03-10 | Tampering | INSTEAD OF trigger mal-implementado na view permite escape de RLS | accept | Não criamos INSTEAD OF triggers. View é só leitura. Risco não existe nesta phase. |
</threat_model>

<verification>
**Pós-aplicação (manual + automated):**

1. `npx tsc --noEmit` exit 0
2. `npm run build` exit 0 (warnings pré-existentes ok)
3. `npm run lint` count idêntico antes/depois (51 problemas — não introduzir novos)
4. SQL no Dashboard: `SELECT count(*) FROM public.product_variants` = 4646
5. SQL no Dashboard: `SELECT count(*) FROM public.produtos` = 4646 (view)
6. SQL no Dashboard: `SELECT count(*) FROM public.vinculos_spot_lampada` igual a pré-migration (FKs ok)
7. SQL no Dashboard: bucket produtos-imagens existe com public=true
8. Smoke web: abrir Vercel kappa, logar como admin, abrir aba Produtos → tabela ainda lista produtos (5 leituras client-side via view ok)
9. Smoke web: abrir wizard de orçamento → autocomplete de produto ainda retorna SKUs (useProdutoSearch usa view ok)
</verification>

<success_criteria>
- Migrations aplicadas em prod com 4646 rows preservadas em product_variants (mesmos UUIDs e codigos)
- Tabela products + pai dummy P-LEGADO existem
- View `produtos` retorna shape idêntico ao da tabela antiga (5 leituras client-side ok via view)
- FK `vinculos_spot_lampada.codigo_spot/codigo_lampada` continuam válidas
- Bucket `produtos-imagens` (plural) existe com policies (read público, write admin)
- types.ts regenerado expõe product_variants + products tipados + view produtos com colunas novas (origem, editado_manualmente, atributos)
- `npx tsc --noEmit` exit 0
- Wizard de orçamento e Admin.tsx Produtos tab continuam funcionando em prod (smoke manual passa)
</success_criteria>

<output>
After completion, create `.planning/phases/03-produtos-importa-o/03-01-SUMMARY.md` with:
- Migrations aplicadas (timestamps + diff resumido)
- Smoke pós-aplicação: counts de products, product_variants, view produtos, vinculos_spot_lampada, bucket produtos-imagens
- types.ts regen confirmado (timestamp + linhas com product_variants/products/produtos)
- Decisões executadas: D-01 a D-04, D-14
- Notas de qualquer fallback usado (e.g., bucket criado via Dashboard se SQL falhou)
</output>
