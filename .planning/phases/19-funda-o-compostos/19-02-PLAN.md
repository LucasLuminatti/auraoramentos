---
phase: 19-funda-o-compostos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useProdutoSearch.ts
  - supabase/migrations/20260612000001_cat03_tipo_produto_conector_kit.sql
autonomous: true
requirements: [CAT-03]
tags: [catalogo, busca, migration, conector, kit_fixacao]

must_haves:
  truths:
    - "ProdutoFiltro inclui 'conector' e 'kit_fixacao' como valores válidos"
    - "useProdutoSearch com filtro='conector' filtra .eq('tipo_produto','conector') e com filtro='kit_fixacao' filtra .eq('tipo_produto','kit_fixacao')"
    - "Existe uma migration de UPDATE aditiva que corrige tipo_produto dos SKUs de conector (LM2338/LM3168/LM3169) e kit (LM2987) auditados, escopada por lista explícita de codigo"
    - "A migration não usa UPDATE em massa sem WHERE de codigo; só toca SKUs explicitamente listados"
  artifacts:
    - path: "src/hooks/useProdutoSearch.ts"
      provides: "Filtros 'conector' e 'kit_fixacao' na busca do catálogo"
      contains: "kit_fixacao"
    - path: "supabase/migrations/20260612000001_cat03_tipo_produto_conector_kit.sql"
      provides: "UPDATE aditivo de tipo_produto para conectores/kits"
      contains: "UPDATE public.product_variants"
  key_links:
    - from: "useProdutoSearch query builder"
      to: "product_variants.tipo_produto"
      via: ".eq('tipo_produto', filtro)"
      pattern: "\\.eq\\('tipo_produto', filtro\\)"
---

<objective>
Fix de catálogo CAT-03: conectores e kits de fixação aparecem na busca de componentes do sistema. Duas partes: (1) código — adicionar `'conector'` e `'kit_fixacao'` ao type `ProdutoFiltro` e à query builder de `useProdutoSearch`; (2) dados — migration de UPDATE aditiva corrigindo `tipo_produto` no `product_variants` para os SKUs auditados (template = Phase 14).

IMPORTANTE: este plano ESCREVE o arquivo SQL da migration mas NÃO o aplica ao banco. A aplicação das migrations (esta + a do Plan 03) acontece no Plan 03, task `[BLOCKING]` via service role.

Purpose: Sem este fix, conectores (LM2338, LM3168, LM3169) e kit (LM2987) ficam invisíveis no seletor — bloqueando todo o fluxo de montagem da Phase 20 (checklist de componentes obrigatórios).

Output: `src/hooks/useProdutoSearch.ts` + `supabase/migrations/20260612000001_cat03_tipo_produto_conector_kit.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-funda-o-compostos/19-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@supabase/migrations/20260610000001_tipo_produto_correcao_catalogos.sql

<interfaces>
<!-- Estado atual de useProdutoSearch.ts. Extraído do código — não explorar. -->

ProdutoFiltro atual (src/hooks/useProdutoSearch.ts:5):
```typescript
export type ProdutoFiltro = 'fita' | 'driver' | 'perfil' | 'luminaria' | 'todos';
```

Query builder atual (linhas 28-32):
```typescript
if (filtro === 'fita' || filtro === 'driver' || filtro === 'perfil') {
  queryBuilder = queryBuilder.eq('tipo_produto', filtro);
} else if (filtro === 'luminaria') {
  queryBuilder = queryBuilder.or('tipo_produto.is.null,tipo_produto.in.(spot,lampada,acessorio,conector,suporte)');
}
```

NOTA: o CHECK constraint `check_tipo_produto` no DB permite hoje: 'fita','driver','perfil','spot','lampada','acessorio','conector','suporte' (PITFALLS N-2). 'conector' JÁ existe no enum. 'kit_fixacao' NÃO existe — a auditoria do DB (acceptance) deve confirmar se o constraint precisa ser estendido; se sim, a migration o estende ANTES do UPDATE.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar filtros 'conector' e 'kit_fixacao' ao ProdutoFiltro e à query builder</name>
  <read_first>
    - src/hooks/useProdutoSearch.ts (ESTADO ATUAL — ProdutoFiltro:5, query builder:28-32)
    - .planning/phases/19-funda-o-compostos/19-CONTEXT.md (D-08 — código: adicionar 'conector'/'kit_fixacao' ao type e à query)
  </read_first>
  <action>
Editar APENAS `src/hooks/useProdutoSearch.ts`:

**1. Estender o type `ProdutoFiltro`** (linha 5). Substituir por:
```typescript
export type ProdutoFiltro = 'fita' | 'driver' | 'perfil' | 'conector' | 'kit_fixacao' | 'luminaria' | 'todos';
```

**2. Estender a condição da query builder** (linha 28). Substituir a condição existente por uma que inclua os dois novos filtros como `.eq('tipo_produto', filtro)`:
```typescript
        if (filtro === 'fita' || filtro === 'driver' || filtro === 'perfil' || filtro === 'conector' || filtro === 'kit_fixacao') {
          queryBuilder = queryBuilder.eq('tipo_produto', filtro);
        } else if (filtro === 'luminaria') {
          queryBuilder = queryBuilder.or('tipo_produto.is.null,tipo_produto.in.(spot,lampada,acessorio,conector,suporte)');
        }
```
NÃO alterar o bloco `filtro === 'luminaria'` (mantém 'conector' no OR para compat com a busca de luminária), nem o pré-filtro de voltagem do driver, nem o fallback UX-01.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useProdutoSearch.ts` ProdutoFiltro union contains `'conector'` AND `'kit_fixacao'`
    - `src/hooks/useProdutoSearch.ts` contains `filtro === 'conector' || filtro === 'kit_fixacao'`
    - `src/hooks/useProdutoSearch.ts` still contains `.eq('tipo_produto', filtro)`
    - `npm run build` exits 0
    - `npm run lint` reports no NEW errors on src/hooks/useProdutoSearch.ts
  </acceptance_criteria>
  <done>useProdutoSearch aceita filtro='conector' e filtro='kit_fixacao', ambos roteando para .eq('tipo_produto', filtro); build verde.</done>
</task>

<task type="auto">
  <name>Task 2: Escrever a migration de UPDATE aditiva de tipo_produto (CAT-03) — SKUs auditados contra o DB</name>
  <read_first>
    - supabase/migrations/20260610000001_tipo_produto_correcao_catalogos.sql (TEMPLATE DIRETO — estrutura BEGIN/UPDATE/COMMIT, guarda IS DISTINCT FROM, comentário de escopo + rollback)
    - .planning/research/PITFALLS.md (C-1 escopo por codigo, nunca blanket; N-2 check_tipo_produto constraint — 'conector' existe, 'kit_fixacao' NÃO)
    - .planning/phases/19-funda-o-compostos/19-CONTEXT.md (D-08 — lista autoritativa auditada contra o DB; sementes LM2338/LM3168/LM3169/LM2987)
  </read_first>
  <action>
**AUDITAR PRIMEIRO contra o DB ao vivo.** Antes de escrever a lista final, consultar o `product_variants` para confirmar o estado real dos SKUs-semente. Rodar (via service-role / SQL console do Supabase, ou pedir a Lenny se sem credencial):
```sql
SELECT codigo, descricao, tipo_produto, sistema
FROM public.product_variants
WHERE codigo IN ('LM2338','LM3168','LM3169','LM2987')
   OR descricao ILIKE '%CONECTOR%MAGNETIC%'
   OR descricao ILIKE '%CONECTOR%TINY%'
   OR descricao ILIKE '%KIT%FIXA%'
ORDER BY codigo;
```
Registrar o resultado no SUMMARY. Classificar cada SKU: (a) existe com tipo_produto errado/null → entra no UPDATE; (b) já está correto → NÃO entra (idempotência via `IS DISTINCT FROM` cobre); (c) ausente do DB → registrar como "ausente, não há o que corrigir". Expandir a lista de conectores/kits com quaisquer SKUs da mesma família que a query revelar com tipo_produto divergente (ex: variantes de cor, embutir).

Se a auditoria não puder rodar agora (sem credencial), escrever a migration com a lista-semente conhecida (LM2338, LM3168, LM3169 → 'conector'; LM2987 → 'kit_fixacao') e marcar no SUMMARY que a lista deve ser confirmada/expandida na task `[BLOCKING]` do Plan 03 antes de aplicar.

**Criar `supabase/migrations/20260612000001_cat03_tipo_produto_conector_kit.sql`** seguindo o template da Phase 14. Estrutura:
```sql
-- Migration: CAT-03 — corrigir tipo_produto de CONECTORES e KITS DE FIXAÇÃO de sistemas compostos (Phase 19 / Plan 02)
-- Motivo: conectores (LM2338 MAGNETO 48V; LM3168/LM3169 TINY 24V) e kit de fixação (LM2987)
--   precisam de tipo_produto='conector'/'kit_fixacao' para aparecer na busca de componentes
--   (useProdutoSearch.ts agora roteia filtro='conector'/'kit_fixacao' via .eq('tipo_produto', filtro)).
-- Escopo: lista EXPLÍCITA de codigo auditada contra o DB (D-08). NUNCA UPDATE em massa (PITFALLS C-1).
-- Aditiva, idempotente (guarda IS DISTINCT FROM), transacional. Mira a TABELA product_variants (não a view produtos).
-- NÃO toca snapshots em orcamentos.ambientes (jsonb autocontido — padrão Phase 14 CAT-01).
--
-- AUDITORIA DB (resultado registrado no 19-02-SUMMARY.md):
--   <colar aqui o resultado da query de auditoria: codigo | tipo_produto atual | ação>
--
-- ROLLBACK:
--   BEGIN;
--   UPDATE public.product_variants SET tipo_produto = NULL WHERE codigo IN (<lista>) AND tipo_produto IN ('conector','kit_fixacao');
--   COMMIT;

BEGIN;

-- check_tipo_produto: 'conector' JÁ está no enum; 'kit_fixacao' precisa ser adicionado ao CHECK antes do UPDATE.
-- (PITFALLS N-2). Estender o constraint de forma aditiva — só se a auditoria confirmar que 'kit_fixacao' não é aceito.
ALTER TABLE public.product_variants DROP CONSTRAINT IF EXISTS check_tipo_produto;
ALTER TABLE public.product_variants ADD CONSTRAINT check_tipo_produto
  CHECK (tipo_produto IS NULL OR tipo_produto IN (
    'fita','driver','perfil','spot','lampada','acessorio','conector','suporte','kit_fixacao'
  ));

-- CONECTORES → 'conector'
UPDATE public.product_variants
  SET tipo_produto = 'conector'
  WHERE codigo IN ('LM2338', 'LM3168', 'LM3169')  -- + SKUs adicionais que a auditoria revelar
  AND tipo_produto IS DISTINCT FROM 'conector';

-- KIT DE FIXAÇÃO → 'kit_fixacao'
UPDATE public.product_variants
  SET tipo_produto = 'kit_fixacao'
  WHERE codigo IN ('LM2987')  -- + SKUs adicionais que a auditoria revelar
  AND tipo_produto IS DISTINCT FROM 'kit_fixacao';

COMMIT;
```
ATENÇÃO ao constraint: confirmar na auditoria o NOME exato do constraint (`check_tipo_produto`) consultando o catálogo `pg_constraint`/`information_schema` se houver dúvida; se o nome real divergir, ajustar o DROP/ADD. Se a auditoria mostrar que `kit_fixacao` JÁ é aceito, manter o ALTER mesmo assim (idempotente — re-declara o mesmo conjunto + 'kit_fixacao').

NÃO aplicar a migration agora. Apenas escrever o arquivo.
  </action>
  <verify>
    <automated>cd auraoramentos && test -f supabase/migrations/20260612000001_cat03_tipo_produto_conector_kit.sql && grep -c "UPDATE public.product_variants" supabase/migrations/20260612000001_cat03_tipo_produto_conector_kit.sql</automated>
  </verify>
  <acceptance_criteria>
    - file `supabase/migrations/20260612000001_cat03_tipo_produto_conector_kit.sql` exists
    - migration contains `BEGIN;` AND `COMMIT;`
    - migration contains `UPDATE public.product_variants` (at least 2 occurrences — conector + kit)
    - migration contains `tipo_produto = 'conector'` AND `tipo_produto = 'kit_fixacao'`
    - migration contains `'LM2338'` AND `'LM3168'` AND `'LM3169'` AND `'LM2987'`
    - migration contains `IS DISTINCT FROM` (idempotência)
    - migration contains `WHERE codigo IN (` (escopo explícito — NUNCA blanket UPDATE)
    - migration does NOT contain an UPDATE without a `WHERE codigo IN` clause: `grep -n 'UPDATE public.product_variants' followed by no 'WHERE codigo' is absent`
    - 19-02-SUMMARY.md records the DB audit result (or explicitly states audit deferred to Plan 03 [BLOCKING] task)
  </acceptance_criteria>
  <done>Migration SQL escrita com lista de SKUs escopada por codigo (auditada ou marcada para confirmar no apply), idempotente, constraint estendido para kit_fixacao; arquivo presente mas NÃO aplicado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → product_variants (dados de catálogo em prod) | UPDATE de dados de produção; risco de corromper tipo_produto de SKUs não-alvo. |
| browser (colaborador autenticado) → Supabase query | Busca de catálogo via useProdutoSearch; já protegida por RLS existente de product_variants/produtos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering | UPDATE migration de tipo_produto | mitigate | UPDATE escopado por `WHERE codigo IN (<lista explícita>)` — NUNCA blanket. Guarda `IS DISTINCT FROM` idempotente. Acceptance criteria proíbe UPDATE sem WHERE codigo. Auditoria contra DB ao vivo antes de finalizar a lista (PITFALLS C-1). Snapshots jsonb não são tocados. |
| T-19-04 | Tampering | check_tipo_produto constraint | mitigate | ALTER aditivo re-declara o conjunto completo + 'kit_fixacao'; auditar nome exato do constraint via pg_constraint antes do DROP/ADD para não dropar o constraint errado. |
| T-19-05 | Information Disclosure | busca de catálogo via filtro novo | accept | 'conector'/'kit_fixacao' são dados de catálogo público (não-sensíveis); RLS de product_variants/produtos já vigente cobre o acesso; nenhum dado de cliente exposto. |

Sem threat S/R/D/E de alta severidade neste plano.
</threat_model>

<verification>
- `npm run build` exits 0
- migration file existe, escopada por codigo, idempotente, com constraint estendido
- (verificação ao vivo da query de catálogo acontece no Plan 03 após o apply: filtro='conector' retorna LM2338, filtro='kit_fixacao' retorna LM2987)
</verification>

<success_criteria>
- ProdutoFiltro + query builder aceitam 'conector' e 'kit_fixacao' (Success Criteria #1, parte código)
- Migration de UPDATE aditiva escrita, escopada e idempotente (Success Criteria #1, parte dados — aplicação no Plan 03)
- CAT-03 endereçado (requirement do plano)
</success_criteria>

<output>
After completion, create `.planning/phases/19-funda-o-compostos/19-02-SUMMARY.md` (incluir o resultado da auditoria de SKUs do DB ou a nota de auditoria deferida).
</output>
