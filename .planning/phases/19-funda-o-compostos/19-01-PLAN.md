---
phase: 19-funda-o-compostos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/orcamento.ts
  - src/types/__tests__/composicao.test.ts
autonomous: true
requirements: []
tags: [data-model, calc, typescript]

must_haves:
  truths:
    - "ItemComposicao existe como interface exportada forward-complete (id, codigo, descricao, quantidade, precoUnitario, precoMinimo, imagemUrl?, papel, obrigatorio, comprimento?, potenciaW?)"
    - "ItemLuminaria carrega campo opcional composicao?: ItemComposicao[] sem quebrar snapshots antigos"
    - "calcularSubtotalComposicao retorna 0 para composicao undefined/vazia e Σ(qtd×precoUnitario) quando populada"
    - "calcularTotalAmbienteSemFita inclui o subtotal de composicao por luminária, mas o total de um ambiente sem composicao é byte-idêntico ao anterior"
    - "REGRAS_COMPOSICAO existe como constante keyed por sistema (magneto_48v→LM2338, tiny_magneto→LM3168/LM3169, embutir→LM2987)"
    - "Os 4 calc sites de orcamento.ts (calcularDemandaFita, calcularConsumoW, calcularQtdDrivers, calcularSubtotalSistemaSemFita) permanecem byte-idênticos"
  artifacts:
    - path: "src/types/orcamento.ts"
      provides: "ItemComposicao, composicao? em ItemLuminaria, calcularSubtotalComposicao, calcularTotalAmbienteSemFita estendido, REGRAS_COMPOSICAO"
      contains: "export interface ItemComposicao"
    - path: "src/types/__tests__/composicao.test.ts"
      provides: "Testes de backward-compat e subtotal de composição"
      contains: "calcularSubtotalComposicao"
  key_links:
    - from: "calcularTotalAmbienteSemFita"
      to: "calcularSubtotalComposicao"
      via: "soma por luminária com guard ?.length"
      pattern: "calcularSubtotalComposicao"
---

<objective>
Modelo de dados aditivo e camada de cálculo dos sistemas compostos, 100% em TypeScript, sem tocar DB nem UI. Entrega o `ItemComposicao` forward-complete (D-02), o campo opcional `composicao?` em `ItemLuminaria` (D-01), a função folha `calcularSubtotalComposicao`, a extensão backward-safe de `calcularTotalAmbienteSemFita`, a constante `REGRAS_COMPOSICAO` (D-07) e os testes unitários de compatibilidade.

Purpose: Pagar o custo de modelagem uma única vez na fundação para que as Phases 20/21 não reabram o tipo nem migrem snapshots. Backward-compat é a superfície de maior risco — orçamentos antigos sem `composicao` precisam abrir e somar exatamente igual a antes.

Output: `src/types/orcamento.ts` estendido + `src/types/__tests__/composicao.test.ts`.

INVARIANTE (Success Criteria #4): NÃO alterar `calcularDemandaFita`, `calcularConsumoW`, `calcularQtdDrivers`, `calcularSubtotalSistemaSemFita`. Apenas ADICIONAR código novo e modificar APENAS o corpo de `calcularTotalAmbienteSemFita`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-funda-o-compostos/19-CONTEXT.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- Estado atual de orcamento.ts que o executor estende. Extraído do código — não explorar. -->

ItemLuminaria atual (src/types/orcamento.ts:23-34) — ESTENDER com composicao?:
```typescript
export interface ItemLuminaria {
  id: string;
  codigo: string;
  descricao: string;
  quantidade: number;
  precoUnitario: number;
  precoMinimo: number;
  imagemUrl?: string;
  sistema?: string | null;
  potencia_watts?: number | null;
  tensao?: number | null;
}
```

calcularSubtotalLuminaria atual (orcamento.ts:236) — NÃO alterar, usar como base:
```typescript
export function calcularSubtotalLuminaria(item: ItemLuminaria): number {
  return item.precoUnitario * item.quantidade;
}
```

calcularTotalAmbienteSemFita atual (orcamento.ts:473-477) — ÚNICA função a MODIFICAR:
```typescript
export function calcularTotalAmbienteSemFita(amb: Ambiente): number {
  const totalLum = amb.luminarias.reduce((s, i) => s + calcularSubtotalLuminaria(i), 0);
  const totalSistemas = amb.sistemas.reduce((s, sis) => s + calcularSubtotalSistemaSemFita(sis), 0);
  return totalLum + totalSistemas;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ItemComposicao + composicao? em ItemLuminaria + calcularSubtotalComposicao + REGRAS_COMPOSICAO</name>
  <read_first>
    - src/types/orcamento.ts (ESTADO ATUAL — ItemLuminaria:23, calcularSubtotalLuminaria:236, calcularTotalAmbienteSemFita:473)
    - .planning/research/ARCHITECTURE.md (§ "(b) Snapshot Representation" — shape do ItemComposicao linhas 129-147 + calc layer linhas 216-230)
    - .planning/phases/19-funda-o-compostos/19-CONTEXT.md (D-02 shape forward-complete; D-05 union de 6 papéis; D-07 REGRAS_COMPOSICAO)
  </read_first>
  <behavior>
    - calcularSubtotalComposicao(item) com item.composicao === undefined → 0
    - calcularSubtotalComposicao(item) com item.composicao === [] → 0
    - calcularSubtotalComposicao(item) com composicao [{qtd:2, precoUnitario:50},{qtd:1, precoUnitario:100}] → 200
    - ItemComposicao aceita comprimento e potenciaW opcionais (ambos podem estar ausentes)
    - REGRAS_COMPOSICAO['magneto_48v'] inclui o código LM2338; REGRAS_COMPOSICAO['tiny_magneto'] inclui LM3168 e LM3169
  </behavior>
  <action>
Editar APENAS `src/types/orcamento.ts`. Três edições aditivas + uma reescrita de função:

**1. Adicionar `composicao?` ao `ItemLuminaria`** (campo aditivo opcional — structural subtyping ignora ausência em snapshots antigos). Logo após `tensao?: number | null;` e antes do `}` de fechamento da interface (linha ~33), adicionar:
```typescript
  /** Sub-itens de um sistema composto (MAGNETO/TINY/MODULAR). Phase 19 / D-01.
   *  Opcional — snapshots antigos têm undefined e continuam funcionando. */
  composicao?: ItemComposicao[];
```

**2. Adicionar a interface `ItemComposicao`** (forward-complete, D-02 + D-05) imediatamente APÓS o fechamento de `ItemLuminaria` (depois da linha 34). Copiar VERBATIM:
```typescript
/** Sub-item de um sistema composto (módulo, driver, conector, kit, acessório).
 *  Forward-complete (Phase 19 / D-02): inclui comprimento (SYSTEM MOLD, Phase 21)
 *  e potenciaW (auto-load magnético, Phase 20), ambos opcionais → zero quebra.
 *  Campos técnicos e preço são SNAPSHOT do catálogo no add-time (D-03). */
export interface ItemComposicao {
  id: string;
  codigo: string;
  descricao: string;
  quantidade: number;
  precoUnitario: number;
  precoMinimo: number;
  imagemUrl?: string;
  papel: 'modulo' | 'driver_recomendado' | 'driver_obrigatorio' | 'conector_energia' | 'kit_fixacao' | 'acessorio_opcional';
  obrigatorio: boolean;
  /** Comprimento em metros do módulo (SYSTEM MOLD deriva fita de Σ(comprimento × qtd)). Phase 21. */
  comprimento?: number;
  /** Potência individual em watts do módulo (auto-load magnético deriva carga total). Phase 20. */
  potenciaW?: number;
}
```

**3. Adicionar a constante `REGRAS_COMPOSICAO`** (D-07 — regra estrutural de família, mora no código, NÃO na tabela). Colocar logo após `MARGEM_SEGURANCA_DRIVER` (linha ~121). Copiar VERBATIM:
```typescript
/** Regras de conector/kit obrigatório por família de sistema composto (Phase 19 / D-07).
 *  Vive no código (3 famílias fixas, regra estrutural estável), NÃO na tabela produto_composicao.
 *  A produto_composicao fica reservada para sugestões SKU↔SKU. Consumido pelo validador da Phase 20 (COMP-01).
 *  `sistema` corresponde a product_variants.sistema ('magneto_48v' | 'tiny_magneto' | 's_mode'). */
export const REGRAS_COMPOSICAO: Record<string, {
  conectoresObrigatorios: string[];
  kitFixacaoEmbutir?: string;
  descricao: string;
}> = {
  magneto_48v: {
    conectoresObrigatorios: ['LM2338'],
    kitFixacaoEmbutir: 'LM2987',
    descricao: 'MAGNETO 48V — conector de energia direcional LM2338; versão embutir requer kit LM2987.',
  },
  tiny_magneto: {
    conectoresObrigatorios: ['LM3168', 'LM3169'],
    kitFixacaoEmbutir: 'LM2987',
    descricao: 'TINY MAGNETO 24V — conector LM3168 (preto) ou LM3169 (branco); versão embutir requer kit LM2987.',
  },
};
```

**4. Adicionar a função folha `calcularSubtotalComposicao`** logo após `calcularSubtotalLuminaria` (depois da linha 238). Copiar VERBATIM:
```typescript
/** Subtotal dos sub-itens de composição de uma luminária (Phase 19 / D-01).
 *  Guard ?.length → retorna 0 para snapshots antigos sem composicao (backward-compat). */
export function calcularSubtotalComposicao(item: ItemLuminaria): number {
  if (!item.composicao?.length) return 0;
  return item.composicao.reduce((s, c) => s + c.precoUnitario * c.quantidade, 0);
}
```

NÃO tocar em `calcularDemandaFita`, `calcularConsumoW`, `calcularQtdDrivers`, `calcularSubtotalSistemaSemFita`, `analisarMagneto48V`, nem em `calcularTotalGeral`. Essa é a regra do success criteria #4.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/orcamento.ts` contains `export interface ItemComposicao`
    - `src/types/orcamento.ts` contains `composicao?: ItemComposicao[]`
    - `src/types/orcamento.ts` contains `comprimento?: number` AND `potenciaW?: number`
    - `src/types/orcamento.ts` contains `'modulo' | 'driver_recomendado' | 'driver_obrigatorio' | 'conector_energia' | 'kit_fixacao' | 'acessorio_opcional'`
    - `src/types/orcamento.ts` contains `export const REGRAS_COMPOSICAO`
    - `src/types/orcamento.ts` contains `export function calcularSubtotalComposicao`
    - `src/types/orcamento.ts` contains both `'LM2338'` and `'LM3168'` and `'LM3169'` and `'LM2987'`
    - `npm run build` exits 0
    - `npm run lint` reports no NEW errors on src/types/orcamento.ts
  </acceptance_criteria>
  <done>ItemComposicao, composicao?, REGRAS_COMPOSICAO e calcularSubtotalComposicao existem e compilam; nenhum dos 4 calc sites de orcamento.ts foi tocado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Estender calcularTotalAmbienteSemFita (backward-safe) + testes unitários</name>
  <read_first>
    - src/types/orcamento.ts (calcularTotalAmbienteSemFita:473 — APÓS Task 1, já com calcularSubtotalComposicao definido)
    - src/types/__tests__/clonagem.test.ts (PADRÃO de fixtures e estilo de teste Vitest deste projeto — makeFita/makeDriver/makeLuminaria/describe/it/expect)
    - .planning/research/ARCHITECTURE.md (calc layer modificado, linhas 216-230)
  </read_first>
  <behavior>
    - Ambiente com luminárias SEM composicao → calcularTotalAmbienteSemFita retorna EXATAMENTE o mesmo valor de antes (Σ subtotalLuminaria + Σ subtotalSistemaSemFita)
    - Ambiente com 1 luminária composta (root preço X qtd Q + composicao Σc) → total = X*Q + Σc + sistemas
    - Snapshot antigo (objeto luminária literal sem a chave composicao) não quebra e soma só o root
  </behavior>
  <action>
**1. Modificar APENAS o corpo de `calcularTotalAmbienteSemFita`** em `src/types/orcamento.ts` (linha ~473). Substituir a linha `const totalLum = amb.luminarias.reduce((s, i) => s + calcularSubtotalLuminaria(i), 0);` por:
```typescript
  const totalLum = amb.luminarias.reduce(
    (s, i) => s + calcularSubtotalLuminaria(i) + calcularSubtotalComposicao(i),
    0
  );
```
Manter `totalSistemas` e o `return` EXATAMENTE como estão. Não tocar `calcularTotalGeral` nem `calcularTotalFitasGlobal`.

**2. Criar `src/types/__tests__/composicao.test.ts`** seguindo o estilo de `clonagem.test.ts` (imports via `@/types/orcamento`, fixtures locais, `describe/it/expect` do vitest). Cobrir explicitamente backward-compat (maior risco). Copiar VERBATIM:
```typescript
import { describe, it, expect } from 'vitest';
import {
  calcularSubtotalComposicao,
  calcularTotalAmbienteSemFita,
  REGRAS_COMPOSICAO,
} from '@/types/orcamento';
import type { ItemLuminaria, ItemComposicao, Ambiente } from '@/types/orcamento';

function makeLuminaria(overrides?: Partial<ItemLuminaria>): ItemLuminaria {
  return { id: 'lum-1', codigo: 'LM0300', descricao: 'Spot GU10', quantidade: 2, precoUnitario: 50, precoMinimo: 40, ...overrides };
}

function makeComp(overrides?: Partial<ItemComposicao>): ItemComposicao {
  return { id: 'c-1', codigo: 'LM0001', descricao: 'Módulo', quantidade: 1, precoUnitario: 100, precoMinimo: 80, papel: 'modulo', obrigatorio: false, ...overrides };
}

function makeAmbiente(luminarias: ItemLuminaria[]): Ambiente {
  return { id: 'amb-1', nome: 'Sala', luminarias, sistemas: [] };
}

describe('calcularSubtotalComposicao', () => {
  it('retorna 0 quando composicao é undefined (snapshot antigo)', () => {
    expect(calcularSubtotalComposicao(makeLuminaria())).toBe(0);
  });
  it('retorna 0 quando composicao é array vazio', () => {
    expect(calcularSubtotalComposicao(makeLuminaria({ composicao: [] }))).toBe(0);
  });
  it('soma Σ(qtd × precoUnitario) dos sub-itens', () => {
    const item = makeLuminaria({ composicao: [makeComp({ quantidade: 2, precoUnitario: 50 }), makeComp({ id: 'c-2', quantidade: 1, precoUnitario: 100 })] });
    expect(calcularSubtotalComposicao(item)).toBe(200);
  });
});

describe('calcularTotalAmbienteSemFita — backward-compat', () => {
  it('ambiente sem composicao soma exatamente subtotal das luminárias (igual a antes)', () => {
    const amb = makeAmbiente([makeLuminaria({ quantidade: 2, precoUnitario: 50 }), makeLuminaria({ id: 'lum-2', quantidade: 1, precoUnitario: 30 })]);
    expect(calcularTotalAmbienteSemFita(amb)).toBe(2 * 50 + 1 * 30);
  });
  it('snapshot literal sem a chave composicao não quebra', () => {
    const snapshotAntigo = { id: 'l', codigo: 'X', descricao: 'd', quantidade: 3, precoUnitario: 10, precoMinimo: 8 } as ItemLuminaria;
    expect(calcularTotalAmbienteSemFita(makeAmbiente([snapshotAntigo]))).toBe(30);
  });
  it('luminária composta adiciona o subtotal da composição', () => {
    const composta = makeLuminaria({ quantidade: 1, precoUnitario: 200, composicao: [makeComp({ quantidade: 2, precoUnitario: 50 })] });
    expect(calcularTotalAmbienteSemFita(makeAmbiente([composta]))).toBe(200 + 100);
  });
});

describe('REGRAS_COMPOSICAO', () => {
  it('magneto_48v exige conector LM2338', () => {
    expect(REGRAS_COMPOSICAO['magneto_48v'].conectoresObrigatorios).toContain('LM2338');
  });
  it('tiny_magneto aceita LM3168 ou LM3169', () => {
    expect(REGRAS_COMPOSICAO['tiny_magneto'].conectoresObrigatorios).toEqual(expect.arrayContaining(['LM3168', 'LM3169']));
  });
  it('versão embutir referencia kit LM2987', () => {
    expect(REGRAS_COMPOSICAO['magneto_48v'].kitFixacaoEmbutir).toBe('LM2987');
  });
});
```
  </action>
  <verify>
    <automated>cd auraoramentos && npm run test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/orcamento.ts` contains `calcularSubtotalLuminaria(i) + calcularSubtotalComposicao(i)`
    - `src/types/__tests__/composicao.test.ts` exists and contains `calcularTotalAmbienteSemFita`
    - `npm run test` exits 0 (todos os testes verdes, incl. os 128 pré-existentes + os novos)
    - git diff de `src/types/orcamento.ts` NÃO contém alterações em `calcularDemandaFita`, `calcularConsumoW`, `calcularQtdDrivers`, `calcularSubtotalSistemaSemFita`, `calcularTotalGeral` (apenas adições + o corpo de calcularTotalAmbienteSemFita): `git diff src/types/orcamento.ts` mostra mudança SÓ na linha do reduce de totalLum dentro de calcularTotalAmbienteSemFita
  </acceptance_criteria>
  <done>calcularTotalAmbienteSemFita inclui composição com backward-compat provada por teste; suíte verde; os 4 calc sites intocados intactos no diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhum novo | Plano puramente client-side TypeScript; nenhuma entrada não confiável cruza fronteira; sem I/O, sem DB, sem rede. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | calcularTotalAmbienteSemFita (cálculo de preço) | mitigate | Backward-compat coberto por teste unitário explícito (snapshot literal sem composicao → mesmo total); guard `?.length` impede NaN/undefined. Risco: total errado em orçamento = preço errado ao cliente. |
| T-19-02 | Tampering | os 4 calc sites de Fita Padrão | mitigate | Acceptance criteria exige git diff provando que calcularDemandaFita/ConsumoW/QtdDrivers/SubtotalSistemaSemFita são byte-idênticos. |

Nenhum threat de I/D/R/E aplicável — sem persistência, sem auth, sem rede neste plano.
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run test` exits 0 (128 pré-existentes + ~9 novos)
- `git diff src/types/orcamento.ts` mostra apenas adições (ItemComposicao, composicao?, REGRAS_COMPOSICAO, calcularSubtotalComposicao) + a única linha modificada dentro de calcularTotalAmbienteSemFita
</verification>

<success_criteria>
- ItemComposicao forward-complete exportado (Success Criteria #2 parte do data model)
- composicao? opcional em ItemLuminaria; orçamentos antigos abrem e somam igual (Success Criteria #2)
- REGRAS_COMPOSICAO no código keyed por sistema (D-07)
- Os 4 calc sites de orcamento.ts intocados (Success Criteria #4, parte 1)
</success_criteria>

<output>
After completion, create `.planning/phases/19-funda-o-compostos/19-01-SUMMARY.md`
</output>
