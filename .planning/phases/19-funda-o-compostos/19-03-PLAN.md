---
phase: 19-funda-o-compostos
plan: 03
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - supabase/migrations/20260612000002_produto_composicao_table.sql
  - .planning/PROJECT.md
autonomous: false
requirements: []
tags: [migration, rls, produto_composicao, documentation]

must_haves:
  truths:
    - "A tabela produto_composicao existe no DB ao vivo (migration aditiva, começa vazia)"
    - "RLS de produto_composicao: SELECT para authenticated; ALL (write) só via has_role(auth.uid(),'admin')"
    - "As DUAS migrations da fase (CAT-03 do Plan 02 + produto_composicao) foram aplicadas via service role e reconciliadas com migration repair"
    - "A query de catálogo ao vivo confirma: filtro='conector' retorna LM2338; filtro='kit_fixacao' retorna LM2987"
    - "A decisão de arquitetura D-01 está documentada em PROJECT.md → Key Decisions, com a verificação dos 5 calc sites intocados"
  artifacts:
    - path: "supabase/migrations/20260612000002_produto_composicao_table.sql"
      provides: "Tabela produto_composicao vazia + índices + RLS"
      contains: "CREATE TABLE public.produto_composicao"
    - path: ".planning/PROJECT.md"
      provides: "Registro D-09 da decisão de arquitetura de compostos"
      contains: "luminarias[].composicao"
  key_links:
    - from: "produto_composicao policy de escrita"
      to: "has_role(auth.uid(), 'admin')"
      via: "RLS FOR ALL USING/WITH CHECK"
      pattern: "has_role\\(auth.uid\\(\\), 'admin'"
---

<objective>
Tabela `produto_composicao` (migration aditiva vazia + RLS), aplicação das DUAS migrations da fase ao DB ao vivo, e documentação da decisão de arquitetura (D-09).

Este plano contém a task `[BLOCKING]` que aplica as migrations. CRÍTICO: neste projeto o histórico de migrations diverge do repo — `supabase db push` é INSEGURO. As migrations são aplicadas via service role (SQL direto contra o DB) + `supabase migration repair --status applied <version>` para reconciliar. Por isso a task de apply é `autonomous: false` (Lenny roda com credencial service-role).

Purpose: A `produto_composicao` é a base reservada para sugestões SKU↔SKU (D-07) — começa vazia, NUNCA é ponto de falha (as regras de família vivem no código, Plan 01). A documentação fecha o Success Criteria #4.

Output: `supabase/migrations/20260612000002_produto_composicao_table.sql` + DB ao vivo com as 2 tabelas/correções aplicadas + `.planning/PROJECT.md` atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-funda-o-compostos/19-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@supabase/migrations/20260515000001_aniversario_envios_table.sql
@supabase/migrations/20260612000001_cat03_tipo_produto_conector_kit.sql

<interfaces>
<!-- Padrão RLS has_role deste projeto. Extraído de 20260515000001 — usar VERBATIM. -->
<!-- ATENÇÃO: a função has_role neste projeto espera o cast app_role: has_role(auth.uid(), 'admin'::app_role). -->
<!-- O blueprint da pesquisa (ARCHITECTURE.md) escreve has_role(..., 'admin') sem cast — USAR a forma do projeto com ::app_role. -->

Schema-alvo (ARCHITECTURE.md §a, linhas 84-107) — adaptar RLS para o idioma ::app_role do projeto:
CREATE TABLE com colunas: id UUID PK, pai_codigo TEXT FK product_variants(codigo), filho_codigo TEXT FK, papel TEXT CHECK (6 papéis), ordem INT default 0, obrigatorio BOOL default false, created_at TIMESTAMPTZ, UNIQUE(pai_codigo, filho_codigo, papel).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever a migration da tabela produto_composicao (vazia + índices + RLS admin/authenticated)</name>
  <read_first>
    - supabase/migrations/20260515000001_aniversario_envios_table.sql (PADRÃO do projeto: ENABLE RLS, CREATE POLICY ... USING (public.has_role(auth.uid(), 'admin'::app_role)), COMMENTs)
    - .planning/research/ARCHITECTURE.md (§a schema produto_composicao, linhas 84-107)
    - .planning/phases/19-funda-o-compostos/19-CONTEXT.md (D-06 schema completo + RLS; D-05 os 6 papéis no CHECK)
  </read_first>
  <files>supabase/migrations/20260612000002_produto_composicao_table.sql</files>
  <action>
Criar `supabase/migrations/20260612000002_produto_composicao_table.sql`. A tabela começa VAZIA. RLS: SELECT para `authenticated`, ALL (write) só admin via `has_role(auth.uid(), 'admin'::app_role)` — usar o cast `::app_role` do PROJETO (NÃO a forma sem cast do blueprint). Copiar VERBATIM:

```sql
-- Migration: tabela produto_composicao — base de sugestões SKU<->SKU de sistemas compostos (Phase 19 / Plan 03 / D-06)
-- Comeca VAZIA. Populada incrementalmente depois (admin/CSV). NUNCA um ponto de falha do fluxo:
--   as regras de conector obrigatorio por FAMILIA vivem no codigo (REGRAS_COMPOSICAO em src/types/orcamento.ts, D-07).
--   Esta tabela e reservada para sugestoes SKU<->SKU de modulos/acessorios compativeis.
-- Aditiva. RLS: leitura para authenticated, escrita so admin (has_role).
-- Padrao has_role cast: 'admin'::app_role (replicado de 20260515000001_aniversario_envios_table.sql).

BEGIN;

CREATE TABLE public.produto_composicao (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  pai_codigo TEXT NOT NULL REFERENCES public.product_variants(codigo),
  filho_codigo TEXT NOT NULL REFERENCES public.product_variants(codigo),
  papel TEXT NOT NULL CHECK (papel IN (
    'modulo', 'driver_recomendado', 'driver_obrigatorio',
    'conector_energia', 'kit_fixacao', 'acessorio_opcional'
  )),
  ordem INTEGER NOT NULL DEFAULT 0,
  obrigatorio BOOLEAN NOT NULL DEFAULT false,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (pai_codigo, filho_codigo, papel)
);

CREATE INDEX idx_composicao_pai ON public.produto_composicao(pai_codigo);
CREATE INDEX idx_composicao_filho ON public.produto_composicao(filho_codigo);

COMMENT ON TABLE public.produto_composicao IS
  'Phase 19 D-06/D-07: sugestoes SKU<->SKU de componentes compativeis (pai->filho com papel). Comeca vazia. Regras de familia obrigatoria NAO vivem aqui (vivem em REGRAS_COMPOSICAO no codigo).';

-- ===== RLS: leitura authenticated, escrita so admin =====

ALTER TABLE public.produto_composicao ENABLE ROW LEVEL SECURITY;

CREATE POLICY "read composicao"
  ON public.produto_composicao
  FOR SELECT
  TO authenticated
  USING (true);

CREATE POLICY "admin write composicao"
  ON public.produto_composicao
  FOR ALL
  TO authenticated
  USING (public.has_role(auth.uid(), 'admin'::app_role))
  WITH CHECK (public.has_role(auth.uid(), 'admin'::app_role));

COMMENT ON POLICY "read composicao" ON public.produto_composicao IS
  'Phase 19 D-06: qualquer usuario autenticado le as sugestoes de composicao (dados de catalogo).';
COMMENT ON POLICY "admin write composicao" ON public.produto_composicao IS
  'Phase 19 D-06: INSERT/UPDATE/DELETE so para admin (has_role). Service role bypass RLS naturalmente.';

COMMIT;
```

NOTA sobre o FK `REFERENCES public.product_variants(codigo)`: confirmar na auditoria (Task 2) que `product_variants.codigo` tem UNIQUE/PK — o FK exige. Se a auditoria revelar que não há constraint UNIQUE em `codigo`, registrar no SUMMARY e ajustar (criar `UNIQUE(codigo)` aditivo antes do FK, OU remover o FK mantendo a coluna TEXT). NÃO aplicar a migration nesta task — só escrever o arquivo.
  </action>
  <verify>
    <automated>cd auraoramentos && test -f supabase/migrations/20260612000002_produto_composicao_table.sql && grep -c "has_role" supabase/migrations/20260612000002_produto_composicao_table.sql</automated>
  </verify>
  <acceptance_criteria>
    - file `supabase/migrations/20260612000002_produto_composicao_table.sql` exists
    - migration contains `CREATE TABLE public.produto_composicao`
    - migration contains `UNIQUE (pai_codigo, filho_codigo, papel)`
    - migration contains all 6 papéis: `'modulo'`, `'driver_recomendado'`, `'driver_obrigatorio'`, `'conector_energia'`, `'kit_fixacao'`, `'acessorio_opcional'`
    - migration contains `CREATE INDEX idx_composicao_pai` AND `idx_composicao_filho`
    - migration contains `FOR SELECT` and `TO authenticated` and `USING (true)`
    - migration contains `FOR ALL` with both `USING (public.has_role(auth.uid(), 'admin'::app_role))` AND `WITH CHECK (public.has_role(auth.uid(), 'admin'::app_role))`
    - migration contains `ENABLE ROW LEVEL SECURITY`
  </acceptance_criteria>
  <done>Arquivo da migration produto_composicao escrito com schema completo (6 papéis, UNIQUE, índices) e RLS authenticated-read/admin-write via has_role(::app_role); NÃO aplicado.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 [BLOCKING]: Auditar SKUs + aplicar as DUAS migrations via service role + migration repair + verificar DB ao vivo</name>
  <read_first>
    - supabase/migrations/20260612000001_cat03_tipo_produto_conector_kit.sql (migration do Plan 02 — aplicar PRIMEIRO)
    - supabase/migrations/20260612000002_produto_composicao_table.sql (migration deste plano — aplicar em seguida)
    - MEMORY: project_aura_migration_divergence (db push inseguro; aplicar via service role + migration repair)
  </read_first>
  <files>supabase/migrations/20260612000001_cat03_tipo_produto_conector_kit.sql, supabase/migrations/20260612000002_produto_composicao_table.sql</files>
  <action>
Checkpoint manual (autonomous:false): Lenny aplica as duas migrations da fase ao DB ao vivo via service role e reconcilia o histórico. Os passos detalhados estão em `<how-to-verify>` (A. auditar SKUs → B. aplicar 0001 e depois 0002 via SQL direto → C. `supabase migration repair --status applied` para ambas → D. verificar ao vivo). Claude NÃO pode executar: sem credencial service-role e `db push` é inseguro neste projeto (histórico diverge). Após o sinal de retomada, registrar os resultados das queries de verificação no SUMMARY.
  </action>
  <what-built>
    Duas migrations aditivas foram ESCRITAS mas não aplicadas: (1) CAT-03 corrige tipo_produto de conectores/kits; (2) cria a tabela produto_composicao vazia com RLS. Neste projeto `supabase db push` é INSEGURO (histórico diverge). É necessário aplicar via service role + reconciliar o histórico com migration repair.
  </what-built>
  <how-to-verify>
    Lenny (com credencial service-role) executa, nesta ordem:

    A. Auditoria de SKUs (confirma a lista do Plan 02 antes de aplicar a CAT-03):
    ```sql
    SELECT codigo, descricao, tipo_produto, sistema
    FROM public.product_variants
    WHERE codigo IN ('LM2338','LM3168','LM3169','LM2987')
       OR descricao ILIKE '%CONECTOR%MAGNETIC%'
       OR descricao ILIKE '%CONECTOR%TINY%'
       OR descricao ILIKE '%KIT%FIXA%'
    ORDER BY codigo;
    ```
    Confirmar que `product_variants.codigo` tem UNIQUE/PK (necessário para o FK da produto_composicao):
    ```sql
    SELECT conname, contype FROM pg_constraint
    WHERE conrelid = 'public.product_variants'::regclass AND contype IN ('p','u');
    ```
    Confirmar o nome real do constraint de tipo_produto antes do DROP/ADD da CAT-03:
    ```sql
    SELECT conname FROM pg_constraint
    WHERE conrelid = 'public.product_variants'::regclass AND conname ILIKE '%tipo_produto%';
    ```
    Se a auditoria revelar SKUs adicionais com tipo_produto divergente OU um nome de constraint diferente, ajustar `20260612000001_cat03_tipo_produto_conector_kit.sql` ANTES de aplicar.

    B. Aplicar as migrations via service role (SQL direto, NÃO `db push`), na ordem 0001 -> 0002:
    - Rodar o conteúdo de `20260612000001_cat03_tipo_produto_conector_kit.sql`
    - Rodar o conteúdo de `20260612000002_produto_composicao_table.sql`

    C. Reconciliar o histórico:
    ```bash
    supabase migration repair --status applied 20260612000001
    supabase migration repair --status applied 20260612000002
    ```

    D. Verificar o DB ao vivo (provar que aplicou — não assumir):
    ```sql
    SELECT to_regclass('public.produto_composicao');
    SELECT relrowsecurity FROM pg_class WHERE oid = 'public.produto_composicao'::regclass;
    SELECT polname, cmd FROM pg_policies WHERE tablename = 'produto_composicao';
    SELECT codigo, tipo_produto FROM public.product_variants WHERE codigo IN ('LM2338','LM3168','LM3169','LM2987') ORDER BY codigo;
    ```
    Esperado: to_regclass != NULL; relrowsecurity = t; duas policies (SELECT read + ALL admin write); LM2338/LM3168/LM3169 -> 'conector', LM2987 -> 'kit_fixacao' (para os SKUs presentes). Colar os resultados no resumo. Se a tabela não existir ou as policies faltarem, NÃO marcar como aplicado.
  </how-to-verify>
  <verify>
    <automated>MISSING — verificação manual ao vivo (autonomous:false); o executor cola os resultados das queries D no SUMMARY e o gate só libera com o sinal "aplicado".</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT to_regclass('public.produto_composicao')` retorna não-NULL (tabela existe ao vivo)
    - `pg_class.relrowsecurity` da produto_composicao = true
    - `pg_policies` para produto_composicao lista uma policy SELECT (read) e uma ALL (admin write)
    - `product_variants` ao vivo: LM2338/LM3168/LM3169 com tipo_produto='conector' e LM2987 com 'kit_fixacao' (para os SKUs presentes no DB; ausentes registrados no resumo)
    - `supabase migration repair --status applied` rodado para 20260612000001 e 20260612000002 (histórico reconciliado)
  </acceptance_criteria>
  <done>Ambas as migrations aplicadas ao DB ao vivo via service role, histórico reconciliado, tabela + RLS + CAT-03 verificados por query (não assumidos).</done>
  <resume-signal>Cole os resultados das queries de verificação (to_regclass, pg_policies, tipo_produto dos 4 SKUs) e digite "aplicado" — ou descreva o que falhou.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Documentar a decisão de arquitetura D-01 no PROJECT.md (D-09) com verificação dos 5 calc sites</name>
  <read_first>
    - .planning/PROJECT.md (§ "Key Decisions" — tabela markdown, linhas 126-154; adicionar 1 linha)
    - .planning/phases/19-funda-o-compostos/19-CONTEXT.md (D-01 anchor da arquitetura; D-09 exigência de documentação)
    - src/types/orcamento.ts + src/lib/pdfTemplates/v2.ts (confirmar que os 5 calc sites estão intocados antes de documentar)
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
1. Verificar primeiro que os 5 calc sites estão byte-idênticos ao baseline (via git diff contra o último commit pré-fase). Rodar:
```bash
git diff HEAD -- src/types/orcamento.ts src/lib/pdfTemplates/v2.ts
```
Confirmar que `calcularDemandaFita`, `calcularConsumoW`, `calcularQtdDrivers`, `calcularSubtotalSistemaSemFita` (orcamento.ts) e `isSistemaVazio` (v2.ts) NÃO aparecem alterados no diff — apenas adições novas e a única linha de `calcularTotalAmbienteSemFita`. Se algum dos 5 aparecer alterado, PARAR e reverter a alteração antes de documentar.

2. Adicionar uma linha à tabela "Key Decisions" do PROJECT.md (após a última linha da tabela, ~linha 154). Copiar VERBATIM:
```markdown
| Compostos em `luminarias[].composicao?`, não em `sistemas[]` (D-01) | Opção mais conservadora (pesquisa HIGH); evita guards nas funções de cálculo de fita; snapshot-compat via campo opcional undefined; `sistemas[]` permanece exclusivo para Fita Padrão | ✓ Validated v1.3 (Phase 19, D-01) — os 5 calc sites (calcularDemandaFita/ConsumoW/QtdDrivers/SubtotalSistemaSemFita + isSistemaVazio no v2.ts) confirmados intocados via git diff; ItemComposicao forward-complete + calcularSubtotalComposicao folha + REGRAS_COMPOSICAO no código (D-07) |
```

3. Atualizar o rodapé `*Last updated:*` do PROJECT.md mencionando que a Phase 19 (fundação de compostos) registrou D-01.

NÃO tocar em nenhum outro arquivo.
  </action>
  <verify>
    <automated>cd auraoramentos && grep -c "luminarias\[\].composicao" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains `luminarias[].composicao`
    - `.planning/PROJECT.md` Key Decisions table contains a row referencing `(Phase 19, D-01)`
    - `git diff HEAD -- src/types/orcamento.ts src/lib/pdfTemplates/v2.ts` shows NO removed/changed lines inside the function bodies of calcularDemandaFita, calcularConsumoW, calcularQtdDrivers, calcularSubtotalSistemaSemFita, isSistemaVazio (only additions + the single totalLum reduce line)
  </acceptance_criteria>
  <done>D-01 documentado em PROJECT.md Key Decisions; verificação git diff prova os 5 calc sites byte-idênticos; rodapé atualizado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (colaborador/admin autenticado) → produto_composicao | Leitura/escrita da nova tabela; RLS é a fronteira que separa admin (write) de colaborador (read-only). |
| service role → DB | Aplicação das migrations bypassa RLS; ação manual confiável (Lenny). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Elevation of Privilege | produto_composicao write policy | mitigate | Policy `FOR ALL` com `USING` + `WITH CHECK` `has_role(auth.uid(),'admin'::app_role)` — colaborador não-admin não consegue INSERT/UPDATE/DELETE. Acceptance verifica via pg_policies ao vivo que a policy ALL existe e é admin-gated. Risco: não-admin alterar regras de composição do catálogo. |
| T-19-07 | Information Disclosure | produto_composicao read policy | mitigate | SELECT restrito a `TO authenticated` (não `anon`) — leitura de dados de catálogo só para usuários logados. Sem PII; dados são de produto. |
| T-19-08 | Tampering | aplicação das migrations (db push inseguro) | mitigate | Apply via service role + `migration repair` (não `db push`), task `[BLOCKING]` autonomous:false. Verificação ao vivo (to_regclass, pg_policies, relrowsecurity) prova o estado real do DB — não assume sucesso do push. |
| T-19-09 | Tampering | os 5 calc sites de Fita Padrão | mitigate | Task 3 exige git diff provando que os 5 sites são byte-idênticos antes de documentar; PARAR se algum mudou. |

Sem threat S/R/D de alta severidade aberto.
</threat_model>

<verification>
- Migration produto_composicao escrita com 6 papéis, UNIQUE, índices, RLS admin/authenticated
- DB ao vivo: tabela existe, RLS habilitada, 2 policies (SELECT read + ALL admin write), CAT-03 aplicado (4 SKUs categorizados) — provado por query, não assumido
- Histórico reconciliado via migration repair
- PROJECT.md documenta D-01 com verificação dos 5 calc sites intocados
</verification>

<success_criteria>
- Tabela produto_composicao existe (vazia) com RLS leitura-authenticated / escrita-admin (Success Criteria #3)
- CAT-03 aplicado e verificado ao vivo: conectores/kit aparecem na busca (Success Criteria #1, parte dados)
- D-01 documentado em PROJECT.md + 5 calc sites confirmados intocados (Success Criteria #4)
</success_criteria>

<output>
After completion, create `.planning/phases/19-funda-o-compostos/19-03-SUMMARY.md` (incluir resultados das queries de verificação ao vivo da task [BLOCKING]).
</output>
