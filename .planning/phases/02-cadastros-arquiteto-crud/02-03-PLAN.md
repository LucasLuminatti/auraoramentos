---
phase: 02-cadastros-arquiteto-crud
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
files_modified:
  - src/components/ArquitetoAutocomplete.tsx
  - src/components/ArquitetoDialog.tsx
  - src/pages/Admin.tsx
autonomous: true
requirements:
  - ARQ-02
threat_model_summary: "CRUD de arquitetos restrito a admin via RLS já em prod (Phase 1). Mitigations: AlertDialog protege contra delete acidental (mensagem clara sobre ON DELETE SET NULL), inputs sanitizados pelo Supabase SDK."
must_haves:
  truths:
    - "Admin tem 8ª aba 'Arquitetos' (URL /admin?tab=arquitetos) com Table listando todos os arquitetos por nome ASC"
    - "Botão '+ Novo Arquiteto' abre Dialog com form [Nome*, Contato] que insere em arquitetos"
    - "Cada linha tem botões Editar (abre mesmo Dialog em mode='edit' pré-populado) e Excluir (abre AlertDialog)"
    - "AlertDialog de excluir tem mensagem sobre ON DELETE SET NULL: 'Clientes/produtos vinculados ficarão sem arquiteto'"
    - "Listagem ORDER BY nome ASC, sem paginação, sem busca neste marco"
    - "Componente reusável ArquitetoAutocomplete.tsx existe (busca ilike '%termo%' em arquitetos.nome, limit 10, opção 'Nenhum arquiteto' no topo, sem criar inline)"
  artifacts:
    - path: "src/components/ArquitetoAutocomplete.tsx"
      provides: "Combobox para selecionar arquiteto (consumido por Plan 02-04)"
      contains: "export default"
    - path: "src/components/ArquitetoDialog.tsx"
      provides: "Dialog reusável create/edit (mode prop)"
      contains: "mode"
    - path: "src/pages/Admin.tsx"
      provides: "Aba Arquitetos com Table + Dialog + AlertDialog"
      contains: "arquitetos"
  key_links:
    - from: "src/pages/Admin.tsx aba arquitetos"
      to: "supabase.from('arquitetos')"
      via: "select/insert/update/delete via SDK"
      pattern: "arquitetos.*select|insert|update|delete"
    - from: "src/components/ArquitetoAutocomplete.tsx"
      to: "supabase.from('arquitetos').select('id,nome').ilike('nome', '%...%').limit(10)"
      via: "busca async on input change"
      pattern: "ilike.*nome"
---

<objective>
Entregar CRUD completo de arquitetos no admin (ARQ-02) e criar `ArquitetoAutocomplete` para reuso na Plan 04.

Purpose: Sem CRUD de arquitetos, admin não consegue popular a base. Sem o autocomplete, cliente form (Plan 04) não consegue selecionar arquiteto. Esta plan entrega ambos.

Output:
- `src/components/ArquitetoAutocomplete.tsx` (novo) — combobox espelhando ProdutoAutocomplete (D-16, D-17).
- `src/components/ArquitetoDialog.tsx` (novo) — Dialog create/edit com prop `mode` (D-20).
- `src/pages/Admin.tsx` — adiciona 8ª aba "Arquitetos" com Table + integração com ArquitetoDialog + AlertDialog de delete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-cadastros-arquiteto-crud/02-CONTEXT.md
@.planning/phases/02-cadastros-arquiteto-crud/02-01-PLAN.md

@CLAUDE.md
@auraoramentos/CLAUDE.md

@src/components/ProdutoAutocomplete.tsx
@src/pages/Admin.tsx
@src/components/ui/dialog.tsx
@src/components/ui/alert-dialog.tsx
@supabase/migrations/20260423000001_create_arquitetos.sql

<interfaces>
<!-- Schema da tabela arquitetos (Phase 1, em prod) -->

From src/integrations/supabase/types.ts:
```typescript
arquitetos: {
  Row: { contato: string | null; created_at: string; id: string; nome: string }
  Insert: { contato?: string | null; created_at?: string; id?: string; nome: string }
  Update: { contato?: string | null; created_at?: string; id?: string; nome?: string }
}
```

RLS (de 20260423000001_create_arquitetos.sql):
- SELECT: `USING (true)` — qualquer autenticado lê
- ALL: `USING (has_role(auth.uid(), 'admin'))` — escrita restrita a admin
- FK em clientes/produtos: `ON DELETE SET NULL`
- Index: `idx_arquitetos_nome` (btree em nome)

Pattern do ProdutoAutocomplete (espelhar — D-16):
```typescript
// Props
interface Props {
  value: string;            // valor exibido no input
  onSelect: (item) => void; // callback quando user escolhe
  placeholder?: string;
  className?: string;
}
// Internal
- useState query, open, ref para click-outside
- Busca async via hook (useProdutoSearch faz debounce 300ms)
- Renderiza dropdown absolute z-50 abaixo do input com items hover-able
```

VALID_TABS de Admin.tsx (linha 22):
```typescript
const VALID_TABS = ["dashboard", "excecoes", "importacao", "produtos", "colaboradores", "orcamentos", "clientes"] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar ArquitetoAutocomplete.tsx (combobox espelhando ProdutoAutocomplete)</name>
  <files>src/components/ArquitetoAutocomplete.tsx</files>
  <read_first>
    - src/components/ProdutoAutocomplete.tsx (74 linhas — pattern completo a espelhar)
    - src/hooks/useProdutoSearch.ts (se existir — confirmar pattern de debounce; senão fazer inline)
    - src/integrations/supabase/types.ts (linhas 69-89 — confirmar shape de arquitetos)
    - .planning/phases/02-cadastros-arquiteto-crud/02-CONTEXT.md (D-16, D-17)
  </read_first>
  <action>
    Criar `src/components/ArquitetoAutocomplete.tsx` espelhando o pattern do `ProdutoAutocomplete.tsx`. Diferenças vs Produto:
    - Busca em `arquitetos.nome` (ilike, case-insensitive)
    - Limit 10 (D-17)
    - Item "Nenhum arquiteto" fixo no TOPO da lista (selecionar = `onSelect(null)` para desvincular)
    - SEM busca de código/preço — apenas nome
    - SEM filtro adicional

    ```tsx
    import { useState, useRef, useEffect } from "react";
    import { Input } from "@/components/ui/input";
    import { supabase } from "@/integrations/supabase/client";
    import { cn } from "@/lib/utils";
    import { Loader2, X } from "lucide-react";

    export interface ArquitetoOption {
      id: string;
      nome: string;
    }

    interface ArquitetoAutocompleteProps {
      value: string;                                          // nome atual exibido (ou "")
      onSelect: (arquiteto: ArquitetoOption | null) => void;  // null = desvincular
      placeholder?: string;
      className?: string;
    }

    const ArquitetoAutocomplete = ({
      value,
      onSelect,
      placeholder = "Buscar arquiteto...",
      className,
    }: ArquitetoAutocompleteProps) => {
      const [query, setQuery] = useState(value);
      const [open, setOpen] = useState(false);
      const [results, setResults] = useState<ArquitetoOption[]>([]);
      const [loading, setLoading] = useState(false);
      const ref = useRef<HTMLDivElement>(null);

      useEffect(() => { setQuery(value); }, [value]);

      // Click outside fecha dropdown
      useEffect(() => {
        const handler = (e: MouseEvent) => {
          if (ref.current && !ref.current.contains(e.target as Node)) setOpen(false);
        };
        document.addEventListener("mousedown", handler);
        return () => document.removeEventListener("mousedown", handler);
      }, []);

      // Debounce 300ms (mesmo do ProdutoAutocomplete)
      useEffect(() => {
        if (!open) return;
        const timer = setTimeout(async () => {
          setLoading(true);
          let req = supabase
            .from("arquitetos")
            .select("id, nome")
            .order("nome")
            .limit(10);
          if (query.trim().length > 0) {
            req = req.ilike("nome", `%${query.trim()}%`);
          }
          const { data, error } = await req;
          if (!error && data) setResults(data as ArquitetoOption[]);
          setLoading(false);
        }, 300);
        return () => clearTimeout(timer);
      }, [query, open]);

      return (
        <div ref={ref} className={cn("relative", className)}>
          <div className="relative">
            <Input
              value={query}
              onChange={(e) => { setQuery(e.target.value); setOpen(true); }}
              onFocus={() => setOpen(true)}
              placeholder={placeholder}
            />
            {loading && (
              <Loader2 className="absolute right-3 top-2.5 h-4 w-4 animate-spin text-muted-foreground" />
            )}
          </div>
          {open && (
            <div className="absolute z-50 mt-1 max-h-60 w-full overflow-auto rounded-lg border bg-popover p-1 shadow-lg">
              {/* "Nenhum arquiteto" fixo no topo (D-17) */}
              <button
                type="button"
                className="flex w-full items-center gap-2 rounded-md px-3 py-2 text-left text-sm text-muted-foreground hover:bg-accent transition-colors"
                onClick={() => { onSelect(null); setQuery(""); setOpen(false); }}
              >
                <X className="h-3.5 w-3.5" />
                Nenhum arquiteto
              </button>
              {loading && (
                <div className="flex items-center justify-center py-4">
                  <Loader2 className="h-5 w-5 animate-spin text-muted-foreground" />
                </div>
              )}
              {!loading && results.length === 0 && (
                <p className="py-3 text-center text-sm text-muted-foreground">
                  Nenhum arquiteto encontrado
                </p>
              )}
              {results.map((a) => (
                <button
                  key={a.id}
                  type="button"
                  className="flex w-full items-center gap-3 rounded-md px-3 py-2 text-left text-sm hover:bg-accent hover:text-accent-foreground transition-colors"
                  onClick={() => { onSelect(a); setQuery(a.nome); setOpen(false); }}
                >
                  <span className="font-medium text-foreground">{a.nome}</span>
                </button>
              ))}
            </div>
          )}
        </div>
      );
    };

    export default ArquitetoAutocomplete;
    ```

    Por D-17: SEM criar arquiteto inline. Usuário vai no Admin > Arquitetos.

    Naming: PascalCase pra component, camelCase pros props (CLAUDE.md auraoramentos).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "ArquitetoAutocomplete"; test $? -ne 0 || echo "ts errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ArquitetoAutocomplete.tsx` existe
    - Exporta `ArquitetoOption` interface (`{ id: string; nome: string }`)
    - Exporta default `ArquitetoAutocomplete`
    - Contém `ilike("nome",` (busca case-insensitive)
    - Contém `.limit(10)` (D-17)
    - Contém `onSelect(null)` (opção "Nenhum arquiteto")
    - Contém debounce com `setTimeout(..., 300)`
    - SEM código de criar arquiteto inline (grep "insert" retorna 0 — ou apenas em comentários)
    - `npx tsc --noEmit` passa
  </acceptance_criteria>
  <done>
    Combobox renderiza, busca arquitetos por nome com debounce, oferece "Nenhum arquiteto", não cria inline.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar ArquitetoDialog.tsx (Dialog reusável create/edit com prop mode)</name>
  <files>src/components/ArquitetoDialog.tsx</files>
  <read_first>
    - src/components/ui/dialog.tsx (shadcn Dialog primitives)
    - src/pages/Auth.tsx (pattern de form submit + loading + toast)
    - src/pages/Admin.tsx (linhas 379-392 — Dialog de excluir cliente como referência de estrutura)
    - .planning/phases/02-cadastros-arquiteto-crud/02-CONTEXT.md (D-20)
  </read_first>
  <action>
    Criar `src/components/ArquitetoDialog.tsx` — componente isolado com Dialog shadcn + form `[Nome*, Contato]`. Funciona em dois modes:
    - `mode="create"`: form vazio, submit faz INSERT em arquitetos
    - `mode="edit"`: form pré-populado com `arquiteto` recebido em prop, submit faz UPDATE

    ```tsx
    import { useState, useEffect } from "react";
    import { supabase } from "@/integrations/supabase/client";
    import { Button } from "@/components/ui/button";
    import { Input } from "@/components/ui/input";
    import { Label } from "@/components/ui/label";
    import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter } from "@/components/ui/dialog";
    import { toast } from "sonner";

    export interface ArquitetoRow {
      id: string;
      nome: string;
      contato: string | null;
    }

    interface ArquitetoDialogProps {
      open: boolean;
      onOpenChange: (open: boolean) => void;
      mode: "create" | "edit";
      arquiteto?: ArquitetoRow | null; // Required quando mode='edit'
      onSuccess: () => void;            // refetch after success
    }

    const ArquitetoDialog = ({ open, onOpenChange, mode, arquiteto, onSuccess }: ArquitetoDialogProps) => {
      const [nome, setNome] = useState("");
      const [contato, setContato] = useState("");
      const [saving, setSaving] = useState(false);

      useEffect(() => {
        if (open) {
          setNome(mode === "edit" && arquiteto ? arquiteto.nome : "");
          setContato(mode === "edit" && arquiteto?.contato ? arquiteto.contato : "");
        }
      }, [open, mode, arquiteto]);

      const handleSubmit = async (e: React.FormEvent) => {
        e.preventDefault();
        if (!nome.trim()) {
          toast.error("Nome é obrigatório");
          return;
        }
        setSaving(true);
        const payload = { nome: nome.trim(), contato: contato.trim() || null };
        let error;
        if (mode === "create") {
          const res = await supabase.from("arquitetos").insert(payload);
          error = res.error;
        } else if (mode === "edit" && arquiteto) {
          const res = await supabase.from("arquitetos").update(payload).eq("id", arquiteto.id);
          error = res.error;
        }
        setSaving(false);
        if (error) {
          toast.error("Erro ao salvar arquiteto: " + error.message);
          return;
        }
        toast.success(mode === "create" ? "Arquiteto criado!" : "Arquiteto atualizado!");
        onOpenChange(false);
        onSuccess();
      };

      return (
        <Dialog open={open} onOpenChange={onOpenChange}>
          <DialogContent className="sm:max-w-md">
            <DialogHeader>
              <DialogTitle>{mode === "create" ? "Novo Arquiteto" : "Editar Arquiteto"}</DialogTitle>
            </DialogHeader>
            <form onSubmit={handleSubmit} className="space-y-4 py-2">
              <div className="space-y-2">
                <Label htmlFor="arq-nome">Nome *</Label>
                <Input
                  id="arq-nome"
                  placeholder="Ex: Studio MK27"
                  value={nome}
                  onChange={(e) => setNome(e.target.value)}
                  required
                />
              </div>
              <div className="space-y-2">
                <Label htmlFor="arq-contato">Contato</Label>
                <Input
                  id="arq-contato"
                  placeholder="Ex: contato@studiomk27.com.br"
                  value={contato}
                  onChange={(e) => setContato(e.target.value)}
                />
              </div>
              <DialogFooter>
                <Button type="button" variant="outline" onClick={() => onOpenChange(false)}>
                  Cancelar
                </Button>
                <Button type="submit" disabled={saving || !nome.trim()}>
                  {saving ? "Salvando..." : mode === "create" ? "Criar" : "Salvar"}
                </Button>
              </DialogFooter>
            </form>
          </DialogContent>
        </Dialog>
      );
    };

    export default ArquitetoDialog;
    ```

    Trim do nome antes de salvar. Contato vazio vira `null` (CLI-01 pattern). Submit fechado via `Enter` no form (default HTML).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "ArquitetoDialog"; test $? -ne 0 || echo "ts errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ArquitetoDialog.tsx` existe
    - Exporta interface `ArquitetoRow` (`{ id: string; nome: string; contato: string | null }`)
    - Componente tem prop `mode: "create" | "edit"`
    - Componente tem prop `onSuccess: () => void`
    - Contém `supabase.from("arquitetos").insert(`
    - Contém `supabase.from("arquitetos").update(`
    - Contém `.eq("id", arquiteto.id)` (só atualiza o registro selecionado)
    - Contém `nome.trim()` e `contato.trim() || null`
    - Title condicional: `"Novo Arquiteto"` (create) vs `"Editar Arquiteto"` (edit)
    - `npx tsc --noEmit` passa
  </acceptance_criteria>
  <done>
    Dialog reusável funciona em ambos modes, faz insert/update via Supabase, mostra toast e chama onSuccess.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Adicionar 8ª aba "Arquitetos" em Admin.tsx (Table + Dialog + AlertDialog excluir)</name>
  <files>src/pages/Admin.tsx</files>
  <read_first>
    - src/pages/Admin.tsx (estado atual — VALID_TABS linha 22, TabsList linhas 177-185, fetchClientes linhas 99-102, Dialog excluir cliente linhas 379-392)
    - src/components/ArquitetoDialog.tsx (recém-criado — exports `ArquitetoRow`)
    - src/components/ui/alert-dialog.tsx (shadcn AlertDialog)
    - .planning/phases/02-cadastros-arquiteto-crud/02-CONTEXT.md (D-18, D-19, D-20, D-21, D-22)
  </read_first>
  <action>
    Editar `src/pages/Admin.tsx` para adicionar a aba Arquitetos. Mudanças pontuais:

    **1. Adicionar imports (perto dos imports existentes ~linhas 1-20):**
    ```typescript
    import ArquitetoDialog, { type ArquitetoRow } from "@/components/ArquitetoDialog";
    import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle } from "@/components/ui/alert-dialog";
    import { Plus, Pencil } from "lucide-react"; // (somar a imports de lucide-react existentes; já tem ArrowLeft, Loader2, Trash2, etc.)
    ```

    **2. Atualizar VALID_TABS (linha 22):**
    ```typescript
    const VALID_TABS = ["dashboard", "excecoes", "importacao", "produtos", "colaboradores", "orcamentos", "clientes", "arquitetos"] as const;
    ```

    **3. Adicionar state + fetch para arquitetos (perto dos outros state, ~linhas 49-58):**
    ```typescript
    // Arquitetos tab
    const [arquitetos, setArquitetos] = useState<ArquitetoRow[]>([]);
    const [arquitetoDialogOpen, setArquitetoDialogOpen] = useState(false);
    const [arquitetoDialogMode, setArquitetoDialogMode] = useState<"create" | "edit">("create");
    const [arquitetoEditTarget, setArquitetoEditTarget] = useState<ArquitetoRow | null>(null);
    const [arquitetoDeleteTarget, setArquitetoDeleteTarget] = useState<ArquitetoRow | null>(null);
    const [arquitetoDeleteOpen, setArquitetoDeleteOpen] = useState(false);
    ```

    **4. Adicionar fetch + handlers (perto de fetchClientes, ~linhas 99-115):**
    ```typescript
    const fetchArquitetos = async () => {
      const { data, error } = await supabase.from("arquitetos").select("id, nome, contato").order("nome", { ascending: true });
      if (error) {
        toast.error("Erro ao carregar arquitetos");
        return;
      }
      setArquitetos((data || []) as ArquitetoRow[]);
    };

    const handleDeleteArquiteto = async () => {
      if (!arquitetoDeleteTarget) return;
      const { error } = await supabase.from("arquitetos").delete().eq("id", arquitetoDeleteTarget.id);
      if (error) {
        toast.error("Erro ao excluir arquiteto: " + error.message);
        return;
      }
      toast.success("Arquiteto excluído!");
      setArquitetoDeleteOpen(false);
      setArquitetoDeleteTarget(null);
      fetchArquitetos();
    };

    const openCreateArquiteto = () => {
      setArquitetoDialogMode("create");
      setArquitetoEditTarget(null);
      setArquitetoDialogOpen(true);
    };

    const openEditArquiteto = (arq: ArquitetoRow) => {
      setArquitetoDialogMode("edit");
      setArquitetoEditTarget(arq);
      setArquitetoDialogOpen(true);
    };
    ```

    **5. Chamar `fetchArquitetos()` no useEffect inicial (linhas 60-65):**
    ```typescript
    useEffect(() => {
      fetchProdutos("");
      fetchColaboradores();
      fetchOrcamentos();
      fetchClientes();
      fetchArquitetos();   // NOVO
    }, []);
    ```

    **6. Adicionar TabsTrigger na TabsList (linhas 177-185), depois de "clientes":**
    ```tsx
    <TabsTrigger value="arquitetos">Arquitetos</TabsTrigger>
    ```

    **7. Adicionar TabsContent value="arquitetos" (depois do TabsContent "clientes" — após linha 375, antes do `</Tabs>` linha 376):**

    ```tsx
    <TabsContent value="arquitetos">
      <div className="space-y-3">
        <div className="flex items-center justify-between">
          <h3 className="text-lg font-semibold">Arquitetos</h3>
          <Button size="sm" onClick={openCreateArquiteto} className="gap-1.5">
            <Plus className="h-4 w-4" /> Novo Arquiteto
          </Button>
        </div>
        <div className="rounded-xl border overflow-hidden">
          <Table>
            <TableHeader>
              <TableRow>
                <TableHead>Nome</TableHead>
                <TableHead>Contato</TableHead>
                <TableHead className="w-28 text-right">Ações</TableHead>
              </TableRow>
            </TableHeader>
            <TableBody>
              {arquitetos.map((a) => (
                <TableRow key={a.id}>
                  <TableCell className="font-medium">{a.nome}</TableCell>
                  <TableCell>{a.contato || "—"}</TableCell>
                  <TableCell className="text-right">
                    <Button variant="ghost" size="sm" onClick={() => openEditArquiteto(a)}>
                      <Pencil className="h-4 w-4" />
                    </Button>
                    <Button
                      variant="ghost"
                      size="sm"
                      className="text-destructive hover:text-destructive"
                      onClick={() => { setArquitetoDeleteTarget(a); setArquitetoDeleteOpen(true); }}
                    >
                      <Trash2 className="h-4 w-4" />
                    </Button>
                  </TableCell>
                </TableRow>
              ))}
              {arquitetos.length === 0 && (
                <TableRow>
                  <TableCell colSpan={3} className="text-center text-muted-foreground py-8">
                    Nenhum arquiteto cadastrado
                  </TableCell>
                </TableRow>
              )}
            </TableBody>
          </Table>
        </div>
      </div>
    </TabsContent>
    ```

    **8. Adicionar `<ArquitetoDialog>` e `<AlertDialog>` no final, junto com os outros dialogs (após o Dialog de excluir cliente, ~linha 392):**

    ```tsx
    <ArquitetoDialog
      open={arquitetoDialogOpen}
      onOpenChange={setArquitetoDialogOpen}
      mode={arquitetoDialogMode}
      arquiteto={arquitetoEditTarget}
      onSuccess={fetchArquitetos}
    />

    <AlertDialog open={arquitetoDeleteOpen} onOpenChange={setArquitetoDeleteOpen}>
      <AlertDialogContent>
        <AlertDialogHeader>
          <AlertDialogTitle>Excluir Arquiteto</AlertDialogTitle>
          <AlertDialogDescription>
            Tem certeza que deseja excluir <strong>{arquitetoDeleteTarget?.nome}</strong>?
            Clientes e produtos vinculados ficarão sem arquiteto (não serão deletados).
            Esta ação não pode ser desfeita.
          </AlertDialogDescription>
        </AlertDialogHeader>
        <AlertDialogFooter>
          <AlertDialogCancel>Cancelar</AlertDialogCancel>
          <AlertDialogAction
            onClick={handleDeleteArquiteto}
            className="bg-destructive text-destructive-foreground hover:bg-destructive/90"
          >
            Excluir
          </AlertDialogAction>
        </AlertDialogFooter>
      </AlertDialogContent>
    </AlertDialog>
    ```

    Não tocar em outras tabs nem na lógica de orçamentos/clientes/colaboradores/produtos. Não mudar tabela `<Tabs>` wrapper. Não tocar em CompletarCadastroBanner (instalado pela Plan 02).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "Admin.tsx|ArquitetoDialog"; test $? -ne 0 || echo "ts errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/Admin.tsx` `VALID_TABS` contém `"arquitetos"`
    - Contém `import ArquitetoDialog, { type ArquitetoRow } from "@/components/ArquitetoDialog"`
    - Contém `import { AlertDialog,` (qualquer imports do alert-dialog)
    - Contém `<TabsTrigger value="arquitetos">Arquitetos</TabsTrigger>`
    - Contém `<TabsContent value="arquitetos">`
    - Contém `const fetchArquitetos = async`
    - Contém `.from("arquitetos").select` (linha do fetch)
    - Contém `.from("arquitetos").delete()` (linha do delete handler)
    - Contém `.order("nome", { ascending: true })` (D-22)
    - Contém `<Plus className="h-4 w-4" /> Novo Arquiteto` ou similar (botão criar)
    - Contém mensagem do AlertDialog com "vinculados ficarão sem arquiteto" (D-21)
    - Contém `<ArquitetoDialog` montado
    - Contém `mode={arquitetoDialogMode}`
    - `npx tsc --noEmit` passa
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>
    Aba Arquitetos funcional ponta-a-ponta: lista alfabética, criar via Dialog, editar via mesmo Dialog em mode='edit', excluir via AlertDialog com aviso de FK SET NULL.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (admin) → public.arquitetos | Admin executa CRUD completo. RLS confia em `has_role(auth.uid(), 'admin')`. |
| browser (qualquer autenticado) → public.arquitetos SELECT | Leitura aberta (USING true) — necessário para autocomplete em forms de cliente/produto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Elevation of Privilege | colaborador comum tenta CRUD via API | mitigate | RLS já em prod (Phase 1): policy "Admins can manage arquitetos" ALL FOR `has_role(auth.uid(),'admin')`. UI Admin.tsx só carrega via AdminRoute — defesa em camadas. |
| T-02-12 | Tampering | input nome/contato sem sanitização | mitigate | Supabase SDK escapa via parâmetros. `nome.trim()` e `contato.trim() || null` evitam strings vazias. Renderização React escapa XSS por padrão. Sem `dangerouslySetInnerHTML`. |
| T-02-13 | Denial of Service | excluir arquiteto vinculado a 1000 clientes | accept | FK `ON DELETE SET NULL` é eficiente (índice em arquiteto_id). 200 arquitetos é limite razoável. AlertDialog avisa o usuário. |
| T-02-14 | Information Disclosure | autocomplete expõe lista de todos arquitetos para qualquer autenticado | accept | Lista de arquitetos não é PII. Necessário para form de cliente. RLS SELECT aberta intencional. |
| T-02-15 | Repudiation | exclusão sem audit log | accept | Sem audit neste marco (mesmo de T-02-09). Aceito. |
</threat_model>

<verification>
- `npx tsc --noEmit` passa
- `npm run build` exit 0
- `npm run lint` passa
- Smoke manual (não automatizado):
  1. Logar como admin, ir em /admin?tab=arquitetos
  2. Ver tab Arquitetos com botão "+ Novo Arquiteto"
  3. Clicar Novo, criar "Studio Teste" com contato "teste@test.com" → Dialog fecha, lista atualiza, "Studio Teste" aparece em ordem alfabética
  4. Clicar lápis na linha → Dialog abre em mode='edit' com campos pré-populados
  5. Editar nome para "Studio Teste 2" → Dialog fecha, lista atualiza
  6. Clicar lixeira → AlertDialog abre com mensagem sobre clientes/produtos vinculados ficarem sem arquiteto
  7. Confirmar exclusão → registro some
  8. F5 na URL `/admin?tab=arquitetos` → continua na aba Arquitetos (URL persistence funciona)
  9. Logar como colaborador comum (não admin), tentar `await supabase.from("arquitetos").insert({nome:"x"})` no console → recebe erro RLS (defesa em camadas)
</verification>

<success_criteria>
1. Admin tem 8ª aba "Arquitetos" funcional (URL /admin?tab=arquitetos persiste)
2. Listar: Table com nome + contato, ORDER BY nome ASC, sem paginação
3. Criar: Dialog com nome obrigatório, contato opcional, INSERT em arquitetos
4. Editar: mesmo Dialog em mode='edit' pré-populado, UPDATE
5. Excluir: AlertDialog com mensagem sobre ON DELETE SET NULL, DELETE
6. Componente reusável `ArquitetoAutocomplete` existe para Plan 04 consumir (busca ilike, limit 10, "Nenhum arquiteto" topo, sem inline create)
</success_criteria>

<output>
After completion, create `.planning/phases/02-cadastros-arquiteto-crud/02-03-SUMMARY.md` documenting:
- Files criados/alterados
- Resultado smoke manual passos 1-9
- Confirmação de que ArquitetoAutocomplete e ArquitetoDialog estão prontos para Plan 04 consumir
- Comportamento da policy RLS testado (passo 9): colaborador não-admin recebe erro ao tentar insert
</output>
