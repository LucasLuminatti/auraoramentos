---
phase: 02-cadastros-arquiteto-crud
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/pages/Auth.tsx
  - src/pages/PerfilCompletar.tsx
  - src/components/CompletarCadastroBanner.tsx
  - src/hooks/useColaborador.ts
  - src/App.tsx
  - src/pages/Index.tsx
  - src/pages/Admin.tsx
autonomous: true
requirements:
  - USR-01
  - USR-02
  - USR-03
  - USR-04
threat_model_summary: "Public signup form coleta PII (CPF/telefone). Mitigations: validação algorítmica client-side, submit envia desmascarado, edge function (Plan 01) faz defesa em profundidade. Banner USR-04 não bloqueia uso (CTA proativo, dados não vazam)."
must_haves:
  truths:
    - "Signup pede CPF (validado pelo algoritmo brasileiro), telefone mascarado e setor (Select 4 opções) — não submete sem os três"
    - "Validação inline (texto vermelho + border vermelha) sob cada campo inválido; toast só pra erro de submit"
    - "Submit envia CPF e telefone desmascarados (apenas dígitos) pra edge function"
    - "Colaborador antigo com cpf/telefone/setor null vê banner amber sticky no topo de Index e Admin"
    - "Banner some quando os 3 campos estão preenchidos (sem botão dispensar)"
    - "CTA do banner navega pra /perfil/completar (rota nova, ProtectedRoute)"
    - "Página /perfil/completar tem form dos 3 campos + reusa máscaras/validadores; submit faz UPDATE colaboradores e redireciona pra /"
  artifacts:
    - path: "src/pages/Auth.tsx"
      provides: "Signup expandido com 3 campos novos"
      contains: "validateCPF"
    - path: "src/pages/PerfilCompletar.tsx"
      provides: "Página de backfill USR-04"
      contains: "export default"
    - path: "src/components/CompletarCadastroBanner.tsx"
      provides: "Banner sticky amber"
      contains: "export default"
    - path: "src/hooks/useColaborador.ts"
      provides: "Colaborador interface estendida com cpf/telefone/setor"
      contains: "cpf"
    - path: "src/App.tsx"
      provides: "Rota /perfil/completar registrada"
      contains: "perfil/completar"
  key_links:
    - from: "src/pages/Auth.tsx"
      to: "supabase.functions.invoke('create-colaborador')"
      via: "body com cpf/telefone/setor desmascarados"
      pattern: "create-colaborador.*cpf"
    - from: "src/components/CompletarCadastroBanner.tsx"
      to: "useColaborador().colaborador"
      via: "lê cpf/telefone/setor para condição de exibição"
      pattern: "colaborador.*cpf.*telefone.*setor"
    - from: "src/pages/PerfilCompletar.tsx"
      to: "supabase.from('colaboradores').update"
      via: "UPDATE com cpf/telefone/setor desmascarados"
      pattern: "colaboradores.*update"
---

<objective>
Entregar signup expandido (USR-01, USR-02, USR-03) e backfill para colaboradores antigos (USR-04). Tudo construído em cima das primitivas da Plan 01.

Purpose: Novos cadastros entram com dados completos validados; usuários antigos são proativamente lembrados de completar (sem bloqueio). Marca o ponto de virada: a partir daqui, dados de colaborador estão padronizados.

Output:
- `src/pages/Auth.tsx` — 3 campos novos no form de signup, ordem definida (D-04), Select para setor (D-06), máscaras durante digitação (D-07), validação inline (D-05), submit desmascarado.
- `src/pages/PerfilCompletar.tsx` (novo) — página dedicada para backfill (D-02).
- `src/components/CompletarCadastroBanner.tsx` (novo) — banner amber sticky (D-01).
- `src/hooks/useColaborador.ts` — interface `Colaborador` estendida com cpf/telefone/setor para o banner ler.
- `src/App.tsx` — rota `/perfil/completar` (ProtectedRoute, sem AdminRoute).
- `src/pages/Index.tsx` + `src/pages/Admin.tsx` — montar banner no topo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-cadastros-arquiteto-crud/02-CONTEXT.md
@.planning/phases/02-cadastros-arquiteto-crud/02-01-PLAN.md

@CLAUDE.md
@auraoramentos/CLAUDE.md

@src/pages/Auth.tsx
@src/hooks/useColaborador.ts
@src/App.tsx
@src/pages/Index.tsx
@src/pages/Admin.tsx
@src/components/ui/select.tsx
@src/lib/masks.ts
@src/lib/validators.ts

<interfaces>
<!-- Primitivas da Plan 01 que esta plan vai consumir -->

From src/lib/masks.ts (Plan 02-01):
```typescript
export function formatCPF(value: string): string;       // (00) 00000-0000 progressivo
export function formatTelefone(value: string): string;  // (00) 00000-0000 progressivo
export function formatCpfCnpj(value: string): string;   // não usado nesta plan (cliente form usa)
export function unmask(value: string): string;          // remove tudo que não é dígito
```

From src/lib/validators.ts (Plan 02-01):
```typescript
export function validateCPF(cpf: string): boolean;       // algoritmo BR + rejeita repetidos
export function validateTelefone(tel: string): boolean;  // 11 dígitos, DDD 11-99
```

From src/integrations/supabase/types.ts (já em prod):
```typescript
colaboradores: {
  Row: {
    id: string; nome: string;
    cpf: string | null; telefone: string | null; setor: string | null;
    cargo: string | null; departamento: string | null; user_id: string | null;
    created_at: string;
  }
}
```

From src/components/ui/select.tsx (shadcn — já existente):
```tsx
<Select value={...} onValueChange={...}>
  <SelectTrigger><SelectValue placeholder="..." /></SelectTrigger>
  <SelectContent>
    <SelectItem value="comercial">Comercial</SelectItem>
    ...
  </SelectContent>
</Select>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Estender useColaborador para expor cpf/telefone/setor + criar Banner USR-04</name>
  <files>src/hooks/useColaborador.ts, src/components/CompletarCadastroBanner.tsx</files>
  <read_first>
    - src/hooks/useColaborador.ts (estado atual — interface Colaborador linhas 6-12, fetch linhas 35-48)
    - src/integrations/supabase/types.ts (linhas 244-279 — confirmar colunas cpf/telefone/setor)
    - .planning/phases/02-cadastros-arquiteto-crud/02-CONTEXT.md (D-01, D-02, D-03 — banner spec; specifics §"Banner USR-04 estilo")
    - src/components/ui/button.tsx (Button shadcn)
    - tailwind.config.ts (confirmar tokens amber disponíveis)
  </read_first>
  <action>
    **Parte A — Estender `src/hooks/useColaborador.ts`:**

    Atualizar a interface `Colaborador` (linhas 6-12) para incluir os 3 campos novos:
    ```typescript
    export interface Colaborador {
      id: string;
      nome: string;
      cargo: string | null;
      departamento: string | null;
      user_id: string | null;
      cpf: string | null;        // NOVO
      telefone: string | null;   // NOVO
      setor: string | null;      // NOVO
    }
    ```

    Atualizar os 2 `.select(...)` (linhas 38-42 e 57-61) para incluir as 3 colunas:
    ```typescript
    .select("id, nome, cargo, departamento, user_id, cpf, telefone, setor")
    ```

    Não tocar em outras partes do hook (auto-create, error handling, derivarNomeInicial). Não regredir gap 3 (create-colaborador 401 fora do signup).

    **Parte B — Criar `src/components/CompletarCadastroBanner.tsx`:**

    Componente que lê `useColaborador()`, renderiza banner amber sticky se algum dos 3 campos for null, e some quando todos preenchidos. Sem botão "dispensar" (D-03).

    ```tsx
    import { useColaborador } from "@/hooks/useColaborador";
    import { useNavigate, useLocation } from "react-router-dom";
    import { Button } from "@/components/ui/button";
    import { AlertCircle } from "lucide-react";

    const CompletarCadastroBanner = () => {
      const { colaborador, loading } = useColaborador();
      const navigate = useNavigate();
      const location = useLocation();

      // Não mostrar enquanto carrega, na própria página de completar, ou se já completou.
      if (loading || !colaborador) return null;
      if (location.pathname === "/perfil/completar") return null;

      const incompleto = !colaborador.cpf || !colaborador.telefone || !colaborador.setor;
      if (!incompleto) return null;

      return (
        <div className="sticky top-0 z-40 w-full border-b border-amber-500/30 bg-amber-500/10 backdrop-blur">
          <div className="mx-auto flex max-w-6xl items-center justify-between gap-3 px-4 py-2.5">
            <div className="flex items-center gap-2 text-amber-700 dark:text-amber-400">
              <AlertCircle className="h-4 w-4 shrink-0" />
              <span className="text-sm font-medium">
                Complete seu cadastro com CPF, telefone e setor.
              </span>
            </div>
            <Button
              size="sm"
              variant="outline"
              className="border-amber-600 text-amber-700 hover:bg-amber-500/20 hover:text-amber-800"
              onClick={() => navigate("/perfil/completar")}
            >
              Completar agora
            </Button>
          </div>
        </div>
      );
    };

    export default CompletarCadastroBanner;
    ```

    Use Tailwind tokens existentes (`bg-amber-500/10`, `border-amber-500/30`, `text-amber-700`) — esses são valores semânticos OK pra estado de aviso (CLAUDE.md "hard-coded colors allowed for semantic states not in the token system" — amber é warning).

    Se `tailwindcss-animate` ou `backdrop-blur` falhar visualmente, OK — apenas garantir que renderiza (verificação visual fica pra Plan 02-04 ou smoke).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "(useColaborador|CompletarCadastroBanner)"; test $? -ne 0 || echo "ts errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useColaborador.ts` interface `Colaborador` contém `cpf: string | null`, `telefone: string | null`, `setor: string | null`
    - `useColaborador.ts` `.select("...")` strings (ambas) contêm `cpf, telefone, setor`
    - `src/components/CompletarCadastroBanner.tsx` existe
    - Banner contém `if (loading || !colaborador) return null;`
    - Banner contém `!colaborador.cpf || !colaborador.telefone || !colaborador.setor` (condição de incompleto)
    - Banner contém `navigate("/perfil/completar")` no onClick
    - Banner contém `location.pathname === "/perfil/completar"` (não auto-mostra na própria página)
    - Sem botão "Dispensar" / "X" (grep por "dispensar" ou "Dispensar" retorna 0)
    - `npx tsc --noEmit` passa
  </acceptance_criteria>
  <done>
    Hook expõe 3 campos novos; banner renderiza condicional, navega para /perfil/completar, sem dispensar.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar PerfilCompletar.tsx + registrar rota /perfil/completar</name>
  <files>src/pages/PerfilCompletar.tsx, src/App.tsx</files>
  <read_first>
    - src/App.tsx (estado atual — ProtectedRoute linhas 21-26, rotas linhas 44-53)
    - src/pages/Auth.tsx (pattern de form submit, loading state, error toast)
    - src/lib/masks.ts (formatCPF, formatTelefone, unmask)
    - src/lib/validators.ts (validateCPF, validateTelefone)
    - src/components/ui/select.tsx (já em uso pelo projeto)
    - src/components/ui/card.tsx, label.tsx, input.tsx, button.tsx
    - .planning/phases/02-cadastros-arquiteto-crud/02-CONTEXT.md (D-02, D-06)
  </read_first>
  <action>
    **Parte A — Criar `src/pages/PerfilCompletar.tsx`:**

    Página dedicada com form dos 3 campos. Reusa máscaras/validadores. Submit faz UPDATE em `colaboradores` filtrado por `user_id = auth.uid()` e redireciona pra `/`. Layout simples, mesma vibe do `Auth.tsx`.

    ```tsx
    import { useState, useEffect } from "react";
    import { useNavigate } from "react-router-dom";
    import { supabase } from "@/integrations/supabase/client";
    import { useColaborador } from "@/hooks/useColaborador";
    import { Button } from "@/components/ui/button";
    import { Input } from "@/components/ui/input";
    import { Label } from "@/components/ui/label";
    import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";
    import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
    import { toast } from "sonner";
    import { formatCPF, formatTelefone, unmask } from "@/lib/masks";
    import { validateCPF, validateTelefone } from "@/lib/validators";

    const SETORES = [
      { value: "comercial", label: "Comercial" },
      { value: "projetos", label: "Projetos" },
      { value: "logistica", label: "Logística" },
      { value: "financeiro", label: "Financeiro" },
    ];

    const PerfilCompletar = () => {
      const navigate = useNavigate();
      const { colaborador, loading: loadingColab } = useColaborador();
      const [cpf, setCpf] = useState("");
      const [telefone, setTelefone] = useState("");
      const [setor, setSetor] = useState("");
      const [errors, setErrors] = useState<{ cpf?: string; telefone?: string; setor?: string }>({});
      const [saving, setSaving] = useState(false);

      // Pré-preenche se algum campo já existir.
      useEffect(() => {
        if (colaborador) {
          if (colaborador.cpf) setCpf(formatCPF(colaborador.cpf));
          if (colaborador.telefone) setTelefone(formatTelefone(colaborador.telefone));
          if (colaborador.setor) setSetor(colaborador.setor);
        }
      }, [colaborador]);

      const handleSubmit = async (e: React.FormEvent) => {
        e.preventDefault();
        const newErrors: typeof errors = {};
        if (!validateCPF(cpf)) newErrors.cpf = "CPF inválido";
        if (!validateTelefone(telefone)) newErrors.telefone = "Telefone inválido (use celular com DDD)";
        if (!setor) newErrors.setor = "Selecione um setor";
        setErrors(newErrors);
        if (Object.keys(newErrors).length > 0) return;

        if (!colaborador) {
          toast.error("Colaborador não encontrado");
          return;
        }

        setSaving(true);
        const { error } = await supabase
          .from("colaboradores")
          .update({
            cpf: unmask(cpf),
            telefone: unmask(telefone),
            setor,
          })
          .eq("id", colaborador.id);
        setSaving(false);

        if (error) {
          toast.error("Erro ao salvar: " + error.message);
          return;
        }
        toast.success("Cadastro completo!");
        navigate("/");
      };

      if (loadingColab) {
        return <div className="flex min-h-screen items-center justify-center text-muted-foreground">Carregando...</div>;
      }

      return (
        <div className="flex min-h-screen items-center justify-center bg-background px-4">
          <Card className="w-full max-w-md">
            <CardHeader>
              <CardTitle>Completar cadastro</CardTitle>
              <CardDescription>
                Preencha os dados que faltam pro seu perfil. Não vai te bloquear, mas é necessário pra fechar o cadastro.
              </CardDescription>
            </CardHeader>
            <CardContent>
              <form onSubmit={handleSubmit} className="space-y-4">
                <div className="space-y-2">
                  <Label htmlFor="cpf">CPF *</Label>
                  <Input
                    id="cpf"
                    placeholder="000.000.000-00"
                    value={cpf}
                    onChange={(e) => setCpf(formatCPF(e.target.value))}
                    className={errors.cpf ? "border-destructive" : ""}
                    required
                  />
                  {errors.cpf && <p className="text-xs text-destructive">{errors.cpf}</p>}
                </div>
                <div className="space-y-2">
                  <Label htmlFor="telefone">Telefone *</Label>
                  <Input
                    id="telefone"
                    placeholder="(11) 98765-4321"
                    value={telefone}
                    onChange={(e) => setTelefone(formatTelefone(e.target.value))}
                    className={errors.telefone ? "border-destructive" : ""}
                    required
                  />
                  {errors.telefone && <p className="text-xs text-destructive">{errors.telefone}</p>}
                </div>
                <div className="space-y-2">
                  <Label htmlFor="setor">Setor *</Label>
                  <Select value={setor} onValueChange={setSetor}>
                    <SelectTrigger id="setor" className={errors.setor ? "border-destructive" : ""}>
                      <SelectValue placeholder="Selecione um setor" />
                    </SelectTrigger>
                    <SelectContent>
                      {SETORES.map((s) => (
                        <SelectItem key={s.value} value={s.value}>{s.label}</SelectItem>
                      ))}
                    </SelectContent>
                  </Select>
                  {errors.setor && <p className="text-xs text-destructive">{errors.setor}</p>}
                </div>
                <Button type="submit" className="w-full" disabled={saving}>
                  {saving ? "Salvando..." : "Salvar"}
                </Button>
              </form>
            </CardContent>
          </Card>
        </div>
      );
    };

    export default PerfilCompletar;
    ```

    **Parte B — Registrar rota `/perfil/completar` em `src/App.tsx`:**

    Adicionar import:
    ```typescript
    import PerfilCompletar from "./pages/PerfilCompletar";
    ```

    Adicionar `<Route>` dentro de `<Routes>` (após a rota `/`, antes de `/admin`):
    ```tsx
    <Route path="/perfil/completar" element={<ProtectedRoute><PerfilCompletar /></ProtectedRoute>} />
    ```

    NÃO usar AdminRoute — qualquer colaborador autenticado pode acessar. Mantém as outras rotas e a estrutura BrowserRouter intactas.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "PerfilCompletar"; test $? -ne 0 || echo "ts errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/PerfilCompletar.tsx` existe
    - PerfilCompletar.tsx contém `import { validateCPF, validateTelefone } from "@/lib/validators"`
    - Contém `import { formatCPF, formatTelefone, unmask } from "@/lib/masks"`
    - Contém array `SETORES` com 4 itens (comercial/projetos/logistica/financeiro)
    - Contém `unmask(cpf)` e `unmask(telefone)` no UPDATE (envia desmascarado)
    - Contém `.eq("id", colaborador.id)` (filtro pelo colaborador logado)
    - Contém `navigate("/")` após sucesso
    - `src/App.tsx` contém `<Route path="/perfil/completar"`
    - `src/App.tsx` contém `import PerfilCompletar from "./pages/PerfilCompletar"`
    - `src/App.tsx` rota `/perfil/completar` está dentro de `<ProtectedRoute>` (não AdminRoute)
    - `npx tsc --noEmit` passa
  </acceptance_criteria>
  <done>
    Página de completar cadastro existe, valida 3 campos com algoritmo BR, faz UPDATE com valores desmascarados, redireciona pra /. Rota registrada como ProtectedRoute.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Expandir signup em Auth.tsx (3 campos novos + Select setor + máscaras + validação inline)</name>
  <files>src/pages/Auth.tsx</files>
  <read_first>
    - src/pages/Auth.tsx (estado atual — 369 linhas; signup form linhas 188-220, handleSubmit linhas 54-130)
    - src/lib/masks.ts (formatCPF, formatTelefone, unmask)
    - src/lib/validators.ts (validateCPF, validateTelefone)
    - src/components/ui/select.tsx
    - .planning/phases/02-cadastros-arquiteto-crud/02-CONTEXT.md (D-04, D-05, D-06, D-07)
    - supabase/functions/create-colaborador/index.ts (Plan 01 — confirma campos do body)
  </read_first>
  <action>
    Editar `src/pages/Auth.tsx` para adicionar 3 campos novos no signup (CPF, Telefone, Setor) com máscaras durante digitação e validação inline. Submit envia desmascarado.

    **1. Imports adicionar (perto dos imports existentes, ~linha 10):**
    ```typescript
    import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
    import { formatCPF, formatTelefone, unmask } from "@/lib/masks";
    import { validateCPF, validateTelefone } from "@/lib/validators";
    ```

    **2. Constante `SETORES` no topo do arquivo (após `RULES`, ~linha 35):**
    ```typescript
    const SETORES = [
      { value: "comercial", label: "Comercial" },
      { value: "projetos", label: "Projetos" },
      { value: "logistica", label: "Logística" },
      { value: "financeiro", label: "Financeiro" },
    ];
    ```

    **3. State novo dentro do componente Auth (após `setDepartamento` linha 46):**
    ```typescript
    const [cpf, setCpf] = useState("");
    const [telefone, setTelefone] = useState("");
    const [setor, setSetor] = useState("");
    const [fieldErrors, setFieldErrors] = useState<{
      cpf?: string;
      telefone?: string;
      setor?: string;
    }>({});
    ```

    **4. Adicionar validação inline em `handleSubmit` (dentro do bloco `if (!isLogin) {...}`, ANTES das checagens existentes de email/password):**
    ```typescript
    if (!isLogin) {
      const fe: typeof fieldErrors = {};
      if (!validateCPF(cpf)) fe.cpf = "CPF inválido";
      if (!validateTelefone(telefone)) fe.telefone = "Telefone inválido (celular com DDD)";
      if (!setor) fe.setor = "Selecione um setor";
      setFieldErrors(fe);
      if (Object.keys(fe).length > 0) return; // bloqueia submit, sem toast (D-05)
      // ... existing checks (nome, email, password) abaixo
    }
    ```

    **5. Atualizar a invocação de `create-colaborador` (linha ~117) para enviar 3 campos novos DESMASCARADOS:**
    ```typescript
    const res = await supabase.functions.invoke("create-colaborador", {
      body: {
        nome,
        cargo,
        departamento,
        user_id: userId,
        cpf: unmask(cpf),
        telefone: unmask(telefone),
        setor,
      },
    });
    ```

    **6. Re-ordenar e re-render dos campos no JSX do form (linhas 188-220), seguindo D-04: `[Nome*, Email*, Confirmar email*, CPF*, Telefone*, Setor*, Cargo, Departamento, Senha*, Confirmar senha*]`.**

    Substituir o bloco atual (`{!isLogin && (<>... Nome ... Cargo/Departamento grid ...</>)}`, linhas 188-220) por uma sequência onde Cargo/Departamento descem para depois do Setor. Estrutura final do form (resumida):

    ```tsx
    {!isLogin && (
      <>
        {/* Nome */}
        <div className="space-y-2">
          <Label htmlFor="nome">Nome Completo *</Label>
          <Input id="nome" placeholder="Ex: João Silva" value={nome}
            onChange={(e) => setNome(e.target.value)} required />
        </div>
      </>
    )}

    {/* E-mail */}
    <div className="space-y-2">
      <Label htmlFor="email">E-mail</Label>
      <Input id="email" type="email" placeholder="seu@email.com"
        value={email} onChange={(e) => setEmail(e.target.value)} required />
    </div>

    {/* Confirmar e-mail (existing — só no signup) */}
    {!isLogin && (
      <div className="space-y-2">{/* já existe — manter */}</div>
    )}

    {!isLogin && (
      <>
        {/* CPF */}
        <div className="space-y-2">
          <Label htmlFor="cpf">CPF *</Label>
          <Input
            id="cpf"
            placeholder="000.000.000-00"
            value={cpf}
            onChange={(e) => setCpf(formatCPF(e.target.value))}
            className={fieldErrors.cpf ? "border-destructive" : ""}
            required
          />
          {fieldErrors.cpf && <p className="text-xs text-destructive">{fieldErrors.cpf}</p>}
        </div>

        {/* Telefone */}
        <div className="space-y-2">
          <Label htmlFor="telefone">Telefone *</Label>
          <Input
            id="telefone"
            placeholder="(11) 98765-4321"
            value={telefone}
            onChange={(e) => setTelefone(formatTelefone(e.target.value))}
            className={fieldErrors.telefone ? "border-destructive" : ""}
            required
          />
          {fieldErrors.telefone && <p className="text-xs text-destructive">{fieldErrors.telefone}</p>}
        </div>

        {/* Setor */}
        <div className="space-y-2">
          <Label htmlFor="setor">Setor *</Label>
          <Select value={setor} onValueChange={setSetor}>
            <SelectTrigger id="setor" className={fieldErrors.setor ? "border-destructive" : ""}>
              <SelectValue placeholder="Selecione um setor" />
            </SelectTrigger>
            <SelectContent>
              {SETORES.map((s) => (
                <SelectItem key={s.value} value={s.value}>{s.label}</SelectItem>
              ))}
            </SelectContent>
          </Select>
          {fieldErrors.setor && <p className="text-xs text-destructive">{fieldErrors.setor}</p>}
        </div>

        {/* Cargo + Departamento (grid de 2 colunas — opcionais) */}
        <div className="grid grid-cols-2 gap-3">
          <div className="space-y-2">
            <Label htmlFor="cargo">Cargo</Label>
            <Input id="cargo" placeholder="Ex: Designer" value={cargo}
              onChange={(e) => setCargo(e.target.value)} />
          </div>
          <div className="space-y-2">
            <Label htmlFor="departamento">Departamento</Label>
            <Input id="departamento" placeholder="Ex: Projetos" value={departamento}
              onChange={(e) => setDepartamento(e.target.value)} />
          </div>
        </div>
      </>
    )}

    {/* Senha + indicador + Confirmar Senha — JÁ EXISTEM, manter intactos abaixo */}
    ```

    Atenção: o bloco "Departamento" antigo tinha label "Setor" (linha 211). Trocar para `Departamento` agora que existe campo `Setor` real. O state `departamento` continua existindo (campo opcional do colaborador).

    **7. Atualizar reset de form em signUpSuccess (linhas 150-158):**

    No `onClick` do botão "Voltar para o login", adicionar:
    ```typescript
    setCpf("");
    setTelefone("");
    setSetor("");
    setFieldErrors({});
    ```

    Não tocar em: handleSubmit do login, allowed_users gate, password strength UI, navigation, onPaste handlers, signUpSuccess view, dialog de "verifique seu email", links inferiores.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "auth.tsx"; test $? -ne 0 || echo "ts errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/Auth.tsx` contém `import { formatCPF, formatTelefone, unmask } from "@/lib/masks"`
    - Contém `import { validateCPF, validateTelefone } from "@/lib/validators"`
    - Contém `import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"`
    - Contém `const SETORES = [` com `comercial`, `projetos`, `logistica`, `financeiro`
    - Contém `const [cpf, setCpf] = useState("")`
    - Contém `const [telefone, setTelefone] = useState("")`
    - Contém `const [setor, setSetor] = useState("")`
    - Contém `const [fieldErrors, setFieldErrors]`
    - Contém `validateCPF(cpf)` na lógica de submit
    - Contém `validateTelefone(telefone)` na lógica de submit
    - Contém `cpf: unmask(cpf)` no body da chamada `functions.invoke`
    - Contém `telefone: unmask(telefone)` no body da chamada
    - Contém `setor` (sem unmask — já é enum) no body
    - Contém `formatCPF(e.target.value)` no onChange do Input de CPF
    - Contém `formatTelefone(e.target.value)` no onChange do Input de Telefone
    - Contém renderização condicional de erros: `{fieldErrors.cpf && <p`
    - Sem `toast` para erro de campo individual (apenas inline) — no novo bloco de validação dos 3 campos
    - `npx tsc --noEmit` passa
    - `npm run lint` passa (ou warnings só, sem errors)
  </acceptance_criteria>
  <done>
    Signup form pede 3 campos novos na ordem definida (D-04), aplica máscaras durante digitação, valida inline com algoritmo BR, envia desmascarado pra edge function, sem toast de campo.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Montar CompletarCadastroBanner no topo de Index.tsx e Admin.tsx</name>
  <files>src/pages/Index.tsx, src/pages/Admin.tsx</files>
  <read_first>
    - src/pages/Index.tsx (estado atual — 80 primeiras linhas leitura suficiente; encontrar header/main wrapper)
    - src/pages/Admin.tsx (linhas 156-176 — header + main)
    - src/components/CompletarCadastroBanner.tsx (recém-criado pela Task 1)
    - .planning/phases/02-cadastros-arquiteto-crud/02-CONTEXT.md (D-01)
  </read_first>
  <action>
    **Em `src/pages/Index.tsx`:**

    Adicionar import no topo (após os imports existentes):
    ```typescript
    import CompletarCadastroBanner from "@/components/CompletarCadastroBanner";
    ```

    Inserir `<CompletarCadastroBanner />` como primeiro filho do wrapper de página (`<div className="min-h-screen ...">` ou equivalente), antes do `<header>`. Se Index.tsx renderiza fragmentos múltiplos (modes "list" e "create"), montar o banner uma única vez no nível mais externo possível para que apareça em todos os modes.

    Localização sugerida: ler Index.tsx completo primeiro, identificar o root JSX que envolve toda a página, e montar `<CompletarCadastroBanner />` como primeiro filho. O banner já é `sticky top-0`, então fica fixado mesmo com scroll.

    **Em `src/pages/Admin.tsx`:**

    Mesmo padrão. Adicionar import:
    ```typescript
    import CompletarCadastroBanner from "@/components/CompletarCadastroBanner";
    ```

    Inserir `<CompletarCadastroBanner />` como primeiro filho dentro de `<div className="min-h-screen bg-background">` (linha 157), antes de `<header>` (linha 158).

    Resultado em Admin.tsx (linhas 157-159) ficará:
    ```tsx
    <div className="min-h-screen bg-background">
      <CompletarCadastroBanner />
      <header className="border-b bg-card shadow-sm">
    ```

    Não tocar em mais nada. Não mudar Tabs, header, main, footer, dialogs.
  </action>
  <verify>
    <automated>grep -l "CompletarCadastroBanner" src/pages/Index.tsx src/pages/Admin.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/Index.tsx` contém `import CompletarCadastroBanner from "@/components/CompletarCadastroBanner"`
    - `src/pages/Index.tsx` contém `<CompletarCadastroBanner />` no JSX
    - `src/pages/Admin.tsx` contém `import CompletarCadastroBanner from "@/components/CompletarCadastroBanner"`
    - `src/pages/Admin.tsx` contém `<CompletarCadastroBanner />` (linha 158-159, antes do `<header>`)
    - `npx tsc --noEmit` passa
    - `npm run build` passa (smoke build, sem erro)
  </acceptance_criteria>
  <done>
    Banner aparece automaticamente em / e /admin quando o colaborador logado tem cpf/telefone/setor null. Some quando preenchidos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase Auth (signUp) | Email/senha + criação de user em auth.users. |
| browser → edge function `create-colaborador` | Body com PII (CPF, telefone) + setor enum. |
| browser → public.colaboradores (UPDATE direto via PerfilCompletar) | RLS: colaborador autenticado pode atualizar próprio registro? CONFIRMAR antes do go-live. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | client-side validateCPF / validateTelefone | mitigate | Edge function (Plan 01) faz defesa em profundidade no `setor`. CPF inválido bypass cliente passa, mas DB CHECK não filtra CPF — aceito porque CPF inválido é problema de dado, não de segurança. |
| T-02-07 | Information Disclosure | toast/console com PII | mitigate | Validação inline (texto vermelho) só mostra "CPF inválido" / "Telefone inválido", nunca o valor digitado. Toast de erro só pra falha de network/auth. Sem `console.log(cpf)` ou similar. |
| T-02-08 | Elevation of Privilege | UPDATE direto em colaboradores via PerfilCompletar | mitigate | Filtro `.eq("id", colaborador.id)` onde `colaborador.id` vem do hook `useColaborador()` que filtra por `auth.uid()`. RLS de `colaboradores` (verificar que policy permite UPDATE WHERE user_id = auth.uid() — se não permitir, edge function alternativa precisa ser criada). |
| T-02-09 | Repudiation | mudança de cadastro sem trail | accept | Sem audit log neste marco. `colaboradores.updated_at` não existe no schema atual. Aceito — feature de audit fica pra marco de qualidade. |
| T-02-10 | Information Disclosure | banner expõe estado de cadastro incompleto | accept | Banner só aparece pro próprio usuário. Não vaza pra outros. Risco zero. |

**Pre-flight check para Task 2 / T-02-08:** antes de implementar, executor deve testar:
- Logado como colaborador comum, executar `await supabase.from("colaboradores").update({ cpf: "12345678909" }).eq("id", "<próprio id>")` no console — se retornar erro RLS, criar edge function `update-meu-perfil` em vez de UPDATE direto. Se passar, manter UPDATE direto.
- Esse é o ÚNICO ponto de risco arquitetural desta plan.
</threat_model>

<verification>
- `npx tsc --noEmit` passa
- `npm run build` exit 0
- `npm run lint` passa (warnings OK, erros não)
- Smoke manual (não automatizado, fica pra final do plan):
  1. Abrir `/auth?mode=signup`, ver 3 campos novos (CPF, Telefone, Setor) na ordem D-04
  2. Tentar submit com CPF "111.111.111-11" → mostra erro inline "CPF inválido"
  3. Tentar submit com telefone "(00) 12345-6789" → mostra erro inline "Telefone inválido"
  4. Tentar submit sem selecionar setor → mostra erro inline "Selecione um setor"
  5. Submit com dados válidos → cria conta, edge function recebe `{cpf:"12345678909", telefone:"11987654321", setor:"comercial"}` (desmascarados)
  6. Logar como colaborador antigo (cpf null) → ver banner amber em / e /admin com botão "Completar agora"
  7. Clicar "Completar agora" → vai pra /perfil/completar, preencher 3 campos válidos, submit, redireciona pra / com banner sumido
</verification>

<success_criteria>
1. Signup pede CPF, Telefone, Setor obrigatórios — não submete sem os três (USR-01, USR-02, USR-03)
2. Signup valida CPF pelo algoritmo BR e telefone celular BR
3. Validação inline (texto vermelho + border vermelha) — sem toast de campo
4. Submit envia desmascarado pra edge function (cpf 11 dígitos, telefone 11 dígitos, setor enum)
5. Banner amber aparece em Index.tsx e Admin.tsx para colaborador com cpf/telefone/setor null (USR-04)
6. Banner some quando 3 campos preenchidos
7. /perfil/completar funciona com mesmo validador, faz UPDATE filtrado por colaborador.id e redireciona pra /
</success_criteria>

<output>
After completion, create `.planning/phases/02-cadastros-arquiteto-crud/02-02-SUMMARY.md` documenting:
- Files alterados/criados (7)
- Resultado do smoke manual (passos 1-7 acima)
- Resolução do T-02-08 (UPDATE direto funcionou ou foi necessária edge function?)
- Confirmação de que CPF/telefone NÃO aparecem em logs do navegador (DevTools) durante signup
</output>
