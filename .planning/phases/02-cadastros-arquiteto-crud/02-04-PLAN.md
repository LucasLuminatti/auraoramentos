---
phase: 02-cadastros-arquiteto-crud
plan: 04
type: execute
wave: 4
depends_on:
  - "02-01"
  - "02-03"
files_modified:
  - src/components/ClienteDialog.tsx
  - src/pages/Index.tsx
  - src/pages/Admin.tsx
  - src/components/ProdutoEditDialog.tsx
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - PROD-03
  - PROD-04
threat_model_summary: "Forms de cliente/produto editam dados pertencentes ao tenant. Mitigations: RLS de clientes/produtos já em prod; CPF/CNPJ no cliente é opcional sem validação semântica (locked D-12); UI escapa XSS por padrão."
must_haves:
  truths:
    - "Form de criar cliente aceita Contato (text livre), CPF/CNPJ (mascarado auto-detect, sem validação), Arquiteto (autocomplete) — todos opcionais"
    - "Form de editar cliente expõe os mesmos 3 campos novos pré-populados"
    - "Submit do cliente envia CPF/CNPJ desmascarado (apenas dígitos)"
    - "Admin > Produtos tem Dialog de edição com campo Arquiteto via ArquitetoAutocomplete"
    - "Atribuir arquiteto a produto existente é possível ponta-a-ponta via UI (PROD-03 manual)"
    - "Admin > Produtos lista mostra coluna ou indicação visual de arquiteto vinculado"
  artifacts:
    - path: "src/components/ClienteDialog.tsx"
      provides: "Dialog reusável create/edit cliente com 4 campos (nome + 3 novos)"
      contains: "ArquitetoAutocomplete"
    - path: "src/pages/Index.tsx"
      provides: "Dialog de novo cliente substituído por ClienteDialog"
      contains: "ClienteDialog"
    - path: "src/components/ProdutoEditDialog.tsx"
      provides: "Dialog de edição de produto com campo Arquiteto"
      contains: "ArquitetoAutocomplete"
    - path: "src/pages/Admin.tsx"
      provides: "Aba Produtos: botão editar abre ProdutoEditDialog; aba Clientes: botão editar abre ClienteDialog"
      contains: "ProdutoEditDialog"
  key_links:
    - from: "src/components/ClienteDialog.tsx"
      to: "supabase.from('clientes').insert/update"
      via: "payload com {nome, contato, cpf_cnpj, arquiteto_id}"
      pattern: "arquiteto_id.*cpf_cnpj"
    - from: "src/components/ProdutoEditDialog.tsx"
      to: "supabase.from('produtos').update"
      via: "UPDATE com arquiteto_id"
      pattern: "produtos.*arquiteto_id"
---

<objective>
Fechar o Phase 2 entregando form de cliente expandido (CLI-01, CLI-02, CLI-03) e edição de produto com arquiteto (PROD-03, PROD-04). Consome o ArquitetoAutocomplete da Plan 03 e máscaras da Plan 01.

Purpose: Sem isso, arquiteto criado no admin não tem onde ser vinculado, e contato/CPF do cliente não tem entrada. Esta plan transforma os campos novos do schema (Phase 1) em UX real.

Output:
- `src/components/ClienteDialog.tsx` (novo) — Dialog reusável (create/edit) com 4 campos.
- `src/pages/Index.tsx` — Dialog inline de "Novo Cliente" substituído por `<ClienteDialog>`.
- `src/components/ProdutoEditDialog.tsx` (novo) — Dialog de edição de produto com campo arquiteto.
- `src/pages/Admin.tsx` — aba Clientes ganha botão "Editar" + uso de ClienteDialog; aba Produtos ganha botão "Editar" por linha + ProdutoEditDialog; lista de produtos mostra coluna de arquiteto.
- Documentação SQL alternativa em `02-04-SUMMARY.md` para Lenny vincular produtos em massa via SQL Editor (PROD-03 alternativo, D-24).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-cadastros-arquiteto-crud/02-CONTEXT.md
@.planning/phases/02-cadastros-arquiteto-crud/02-01-PLAN.md
@.planning/phases/02-cadastros-arquiteto-crud/02-03-PLAN.md

@CLAUDE.md
@auraoramentos/CLAUDE.md

@src/pages/Index.tsx
@src/pages/Admin.tsx
@src/components/ClienteList.tsx
@src/lib/masks.ts
@src/components/ArquitetoAutocomplete.tsx

<interfaces>
<!-- Schema dos campos novos em clientes (Phase 1, em prod) -->

From src/integrations/supabase/types.ts (table clientes):
```typescript
clientes: {
  Row: {
    arquiteto_id: string | null   // NOVO (Phase 1)
    contato: string | null         // NOVO (Phase 1)
    cpf_cnpj: string | null        // NOVO (Phase 1)
    created_at: string
    email: string | null
    id: string
    nome: string
    telefone: string | null
  }
  Relationships: [
    {
      foreignKeyName: "clientes_arquiteto_id_fkey"
      referencedRelation: "arquitetos"
      // ...
    }
  ]
}
```

Schema produtos (relevante):
```typescript
produtos: {
  Row: {
    arquiteto_id: string | null  // (Phase 1)
    codigo: string
    descricao: string
    preco_tabela: number
    preco_minimo: number
    // ... outros campos
  }
}
```

From Plan 02-03:
```typescript
// src/components/ArquitetoAutocomplete.tsx
export interface ArquitetoOption { id: string; nome: string }
interface Props { value: string; onSelect: (a: ArquitetoOption | null) => void; placeholder?: string; className?: string }

// src/components/ArquitetoDialog.tsx
export interface ArquitetoRow { id: string; nome: string; contato: string | null }
```

From Plan 02-01:
```typescript
// src/lib/masks.ts
export function formatCpfCnpj(value: string): string;  // auto-detect 11 vs 14 dígitos
export function unmask(value: string): string;
```

Estado atual de "Novo Cliente" em Index.tsx (linhas 32-33, 54-65):
```typescript
const [clienteDialogOpen, setClienteDialogOpen] = useState(false);
const [novoClienteNome, setNovoClienteNome] = useState("");

const handleCriarCliente = async () => {
  if (!novoClienteNome.trim()) return;
  const { error } = await supabase.from("clientes").insert({ nome: novoClienteNome.trim() });
  // ...
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar ClienteDialog.tsx (Dialog reusável create/edit com 4 campos: Nome, Contato, CPF/CNPJ, Arquiteto)</name>
  <files>src/components/ClienteDialog.tsx</files>
  <read_first>
    - src/components/ArquitetoDialog.tsx (Plan 02-03 — pattern do Dialog reusável com prop mode)
    - src/components/ArquitetoAutocomplete.tsx (Plan 02-03 — para acoplar no form)
    - src/lib/masks.ts (formatCpfCnpj, unmask)
    - src/components/ui/dialog.tsx
    - .planning/phases/02-cadastros-arquiteto-crud/02-CONTEXT.md (D-12, D-13, D-14, D-15)
  </read_first>
  <action>
    Criar `src/components/ClienteDialog.tsx`. Pattern espelhado em ArquitetoDialog.tsx mas com 4 campos. Order conforme D-14: `[Nome*, Contato, CPF/CNPJ, Arquiteto]`. Tudo abaixo de Nome é opcional. CPF/CNPJ usa `formatCpfCnpj` (auto-detect 11 vs 14 dígitos) e SEM validação (D-12). Submit envia desmascarado.

    ```tsx
    import { useState, useEffect } from "react";
    import { supabase } from "@/integrations/supabase/client";
    import { Button } from "@/components/ui/button";
    import { Input } from "@/components/ui/input";
    import { Label } from "@/components/ui/label";
    import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter } from "@/components/ui/dialog";
    import { toast } from "sonner";
    import { formatCpfCnpj, unmask } from "@/lib/masks";
    import ArquitetoAutocomplete from "@/components/ArquitetoAutocomplete";

    export interface ClienteRow {
      id: string;
      nome: string;
      contato: string | null;
      cpf_cnpj: string | null;
      arquiteto_id: string | null;
    }

    interface ClienteDialogProps {
      open: boolean;
      onOpenChange: (open: boolean) => void;
      mode: "create" | "edit";
      cliente?: ClienteRow | null;
      onSuccess: () => void;
    }

    const ClienteDialog = ({ open, onOpenChange, mode, cliente, onSuccess }: ClienteDialogProps) => {
      const [nome, setNome] = useState("");
      const [contato, setContato] = useState("");
      const [cpfCnpj, setCpfCnpj] = useState("");
      const [arquitetoId, setArquitetoId] = useState<string | null>(null);
      const [arquitetoNome, setArquitetoNome] = useState(""); // exibição no autocomplete
      const [saving, setSaving] = useState(false);

      // Carrega dados quando abre em modo edit
      useEffect(() => {
        if (!open) return;
        if (mode === "edit" && cliente) {
          setNome(cliente.nome);
          setContato(cliente.contato || "");
          setCpfCnpj(cliente.cpf_cnpj ? formatCpfCnpj(cliente.cpf_cnpj) : "");
          setArquitetoId(cliente.arquiteto_id);
          // Carregar nome do arquiteto se houver
          if (cliente.arquiteto_id) {
            supabase.from("arquitetos").select("nome").eq("id", cliente.arquiteto_id).maybeSingle().then(({ data }) => {
              setArquitetoNome(data?.nome || "");
            });
          } else {
            setArquitetoNome("");
          }
        } else {
          setNome("");
          setContato("");
          setCpfCnpj("");
          setArquitetoId(null);
          setArquitetoNome("");
        }
      }, [open, mode, cliente]);

      const handleSubmit = async (e: React.FormEvent) => {
        e.preventDefault();
        if (!nome.trim()) {
          toast.error("Nome é obrigatório");
          return;
        }
        setSaving(true);
        const payload = {
          nome: nome.trim(),
          contato: contato.trim() || null,
          cpf_cnpj: cpfCnpj.trim() ? unmask(cpfCnpj) : null,
          arquiteto_id: arquitetoId,
        };
        let error;
        if (mode === "create") {
          const res = await supabase.from("clientes").insert(payload);
          error = res.error;
        } else if (mode === "edit" && cliente) {
          const res = await supabase.from("clientes").update(payload).eq("id", cliente.id);
          error = res.error;
        }
        setSaving(false);
        if (error) {
          toast.error("Erro ao salvar cliente: " + error.message);
          return;
        }
        toast.success(mode === "create" ? "Cliente criado!" : "Cliente atualizado!");
        onOpenChange(false);
        onSuccess();
      };

      return (
        <Dialog open={open} onOpenChange={onOpenChange}>
          <DialogContent className="sm:max-w-md">
            <DialogHeader>
              <DialogTitle>{mode === "create" ? "Novo Cliente" : "Editar Cliente"}</DialogTitle>
            </DialogHeader>
            <form onSubmit={handleSubmit} className="space-y-4 py-2">
              <div className="space-y-2">
                <Label htmlFor="cli-nome">Nome *</Label>
                <Input
                  id="cli-nome"
                  placeholder="Ex: Casa João Silva"
                  value={nome}
                  onChange={(e) => setNome(e.target.value)}
                  required
                />
              </div>
              <div className="space-y-2">
                <Label htmlFor="cli-contato">Contato</Label>
                <Input
                  id="cli-contato"
                  placeholder="Ex: João Silva, joao@email.com"
                  value={contato}
                  onChange={(e) => setContato(e.target.value)}
                />
              </div>
              <div className="space-y-2">
                <Label htmlFor="cli-cpf">CPF/CNPJ</Label>
                <Input
                  id="cli-cpf"
                  placeholder="000.000.000-00 ou 00.000.000/0000-00"
                  value={cpfCnpj}
                  onChange={(e) => setCpfCnpj(formatCpfCnpj(e.target.value))}
                />
              </div>
              <div className="space-y-2">
                <Label>Arquiteto</Label>
                <ArquitetoAutocomplete
                  value={arquitetoNome}
                  onSelect={(arq) => {
                    if (arq === null) {
                      setArquitetoId(null);
                      setArquitetoNome("");
                    } else {
                      setArquitetoId(arq.id);
                      setArquitetoNome(arq.nome);
                    }
                  }}
                  placeholder="Buscar arquiteto..."
                />
              </div>
              <DialogFooter>
                <Button type="button" variant="outline" onClick={() => onOpenChange(false)}>
                  Cancelar
                </Button>
                <Button type="submit" disabled={saving || !nome.trim()}>
                  {saving ? "Salvando..." : mode === "create" ? "Criar" : "Salvar"}
                </Button>
              </DialogFooter>
            </form>
          </DialogContent>
        </Dialog>
      );
    };

    export default ClienteDialog;
    ```

    Atenção: SEM `validateCPF` aqui (D-12). Apenas máscara visual + unmask no submit. CPF/CNPJ vazio vira `null`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "ClienteDialog"; test $? -ne 0 || echo "ts errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ClienteDialog.tsx` existe
    - Exporta interface `ClienteRow` com campos `id, nome, contato, cpf_cnpj, arquiteto_id`
    - Componente tem prop `mode: "create" | "edit"`
    - Contém `import ArquitetoAutocomplete`
    - Contém `import { formatCpfCnpj, unmask } from "@/lib/masks"`
    - Contém `formatCpfCnpj(e.target.value)` no onChange do input CPF/CNPJ
    - Contém `unmask(cpfCnpj)` no payload (envia desmascarado)
    - Contém `arquiteto_id: arquitetoId` no payload
    - Contém `<ArquitetoAutocomplete` montado no form
    - SEM `validateCPF` import (D-12 — não valida)
    - 4 campos visíveis: Nome, Contato, CPF/CNPJ, Arquiteto (na ordem D-14)
    - `npx tsc --noEmit` passa
  </acceptance_criteria>
  <done>
    Dialog reusável de cliente funciona em ambos modes, monta ArquitetoAutocomplete, aplica máscara CPF/CNPJ sem validar, envia desmascarado.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Substituir Dialog inline de "Novo Cliente" em Index.tsx por ClienteDialog</name>
  <files>src/pages/Index.tsx</files>
  <read_first>
    - src/pages/Index.tsx (estado atual — state linhas 32-39, handleCriarCliente linhas 54-65, Dialog inline JSX procurar por "novoClienteNome")
    - src/components/ClienteDialog.tsx (Task 1)
  </read_first>
  <action>
    Editar `src/pages/Index.tsx`:

    **1. Adicionar import:**
    ```typescript
    import ClienteDialog from "@/components/ClienteDialog";
    ```

    **2. Remover state desnecessário (linhas 32-33):**
    Apagar:
    ```typescript
    const [clienteDialogOpen, setClienteDialogOpen] = useState(false);
    const [novoClienteNome, setNovoClienteNome] = useState("");
    ```

    Substituir por:
    ```typescript
    const [clienteDialogOpen, setClienteDialogOpen] = useState(false);
    ```

    **3. Remover `handleCriarCliente` (linhas 54-65)** — `ClienteDialog` cuida do INSERT.

    **4. Substituir `<Dialog>` inline (procurar por `clienteDialogOpen` no JSX, e o Dialog que usa `novoClienteNome`)** por:

    ```tsx
    <ClienteDialog
      open={clienteDialogOpen}
      onOpenChange={setClienteDialogOpen}
      mode="create"
      onSuccess={() => setListKey((k) => k + 1)}
    />
    ```

    **5. Garantir que o botão "Novo Cliente" (que abria o Dialog antigo) ainda chama `setClienteDialogOpen(true)`** — isso deve continuar funcionando sem mudanças.

    Não tocar em Step1/2/3, ClienteList, EncerrarNegociacaoModal, dialogs de voltar/projetos. Manter Index.tsx funcional após mudança — wizard 3 passos não pode quebrar.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "index.tsx"; test $? -ne 0 || echo "ts errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/Index.tsx` contém `import ClienteDialog from "@/components/ClienteDialog"`
    - Contém `<ClienteDialog` no JSX
    - NÃO contém mais `novoClienteNome` (state) — `grep "novoClienteNome" src/pages/Index.tsx` retorna 0
    - NÃO contém mais `handleCriarCliente` definido — `grep "handleCriarCliente" src/pages/Index.tsx` retorna 0
    - Contém `mode="create"` no ClienteDialog
    - Contém `onSuccess={() => setListKey` (refetch via key bump)
    - `npx tsc --noEmit` passa
    - `npm run build` exit 0
    - Wizard 3 passos não regrediu (verificação visual no smoke)
  </acceptance_criteria>
  <done>
    Botão "Novo Cliente" abre ClienteDialog em mode='create', usuário pode preencher os 4 campos, submit cria registro com FK arquiteto correta.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Adicionar botão Editar Cliente em Admin.tsx aba Clientes (usando ClienteDialog em mode='edit')</name>
  <files>src/pages/Admin.tsx</files>
  <read_first>
    - src/pages/Admin.tsx (estado atual após Plan 02-03 — aba Clientes linhas 345-375, dialog excluir cliente linhas 379-392)
    - src/components/ClienteDialog.tsx (Task 1 — exports `ClienteRow` interface)
    - .planning/phases/02-cadastros-arquiteto-crud/02-CONTEXT.md (D-15)
  </read_first>
  <action>
    Editar `src/pages/Admin.tsx`:

    **1. Adicionar import:**
    ```typescript
    import ClienteDialog, { type ClienteRow } from "@/components/ClienteDialog";
    ```

    **2. Adicionar state (perto dos outros state de cliente, ~linhas 52-54):**
    ```typescript
    const [clienteEditOpen, setClienteEditOpen] = useState(false);
    const [clienteEditTarget, setClienteEditTarget] = useState<ClienteRow | null>(null);
    const [clienteCreateOpen, setClienteCreateOpen] = useState(false);
    ```

    **3. Atualizar `fetchClientes` (linhas 99-102) para incluir os campos novos:**
    ```typescript
    const fetchClientes = async () => {
      const { data } = await supabase
        .from("clientes")
        .select("id, nome, email, telefone, contato, cpf_cnpj, arquiteto_id")
        .order("nome");
      setClientes(data || []);
    };
    ```

    **4. Adicionar botão "Novo Cliente" no topo da aba Clientes** (linhas 345-375). Antes de `<div className="rounded-xl border overflow-hidden">`, adicionar:
    ```tsx
    <div className="flex justify-end mb-3">
      <Button size="sm" onClick={() => setClienteCreateOpen(true)} className="gap-1.5">
        <Plus className="h-4 w-4" /> Novo Cliente
      </Button>
    </div>
    ```

    (Garantir que `Plus` está importado de `lucide-react` — pode já estar pela Plan 02-03.)

    **5. Adicionar botão Editar (lápis) na linha de cada cliente** (linha 362-366, antes do botão Trash2):

    ```tsx
    <Button
      variant="ghost"
      size="sm"
      onClick={() => {
        setClienteEditTarget(c as ClienteRow);
        setClienteEditOpen(true);
      }}
    >
      <Pencil className="h-4 w-4" />
    </Button>
    ```

    Aumentar `className="w-20"` da TableHead "Ações" para `className="w-28"` para caber 2 botões.

    **6. Mostrar arquiteto na linha** (opcional mas útil pra UAT). Após `<TableHead>Telefone</TableHead>` adicionar `<TableHead>Arquiteto</TableHead>`. Em cada `<TableRow>`, adicionar célula. Como o select não faz join, fazer fetch de arquitetos e mapear. Implementação simples:

    ```typescript
    // Após state, adicionar map<id,nome>:
    const [arquitetosMap, setArquitetosMap] = useState<Record<string, string>>({});

    // Após fetchArquitetos atualizar mapa:
    const fetchArquitetos = async () => {
      const { data, error } = await supabase.from("arquitetos").select("id, nome, contato").order("nome", { ascending: true });
      if (error) { toast.error("Erro ao carregar arquitetos"); return; }
      setArquitetos((data || []) as ArquitetoRow[]);
      const map: Record<string, string> = {};
      for (const a of data || []) map[a.id] = a.nome;
      setArquitetosMap(map);
    };
    ```

    Na célula da linha cliente:
    ```tsx
    <TableCell>{c.arquiteto_id ? (arquitetosMap[c.arquiteto_id] || "—") : "—"}</TableCell>
    ```

    Atualizar `colSpan` do empty-state TableRow de `4` para `5`.

    **7. Montar os dois ClienteDialog (final do componente, perto dos outros dialogs):**
    ```tsx
    <ClienteDialog
      open={clienteCreateOpen}
      onOpenChange={setClienteCreateOpen}
      mode="create"
      onSuccess={fetchClientes}
    />
    <ClienteDialog
      open={clienteEditOpen}
      onOpenChange={setClienteEditOpen}
      mode="edit"
      cliente={clienteEditTarget}
      onSuccess={fetchClientes}
    />
    ```

    Não tocar em outras abas, no AlertDialog/Dialog de excluir cliente (continua valendo), nas outras tabs, em CompletarCadastroBanner. Manter ArquitetoDialog/AlertDialog da Plan 03 intactos.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "Admin.tsx"; test $? -ne 0 || echo "ts errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/Admin.tsx` contém `import ClienteDialog, { type ClienteRow } from "@/components/ClienteDialog"`
    - Contém `setClienteEditOpen`, `setClienteEditTarget`, `setClienteCreateOpen`
    - `fetchClientes` agora seleciona `contato, cpf_cnpj, arquiteto_id` (além dos campos antigos)
    - Aba Clientes contém `<TableHead>Arquiteto</TableHead>`
    - Aba Clientes contém botão `<Pencil` em cada linha
    - Contém botão "+ Novo Cliente" no topo da aba clientes
    - Dois `<ClienteDialog` montados (create e edit)
    - `npx tsc --noEmit` passa
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>
    Aba Clientes do admin permite criar/editar com 4 campos, mostra arquiteto vinculado na lista.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Criar ProdutoEditDialog.tsx + adicionar botão Editar na aba Produtos do Admin</name>
  <files>src/components/ProdutoEditDialog.tsx, src/pages/Admin.tsx</files>
  <read_first>
    - src/pages/Admin.tsx aba Produtos (linhas 221-259)
    - src/components/ClienteDialog.tsx (Task 1 — pattern reusável)
    - src/components/ArquitetoAutocomplete.tsx (Plan 02-03)
    - src/integrations/supabase/types.ts (table produtos — confirmar campos editáveis: codigo, descricao, preco_tabela, preco_minimo, arquiteto_id)
    - .planning/phases/02-cadastros-arquiteto-crud/02-CONTEXT.md (D-23, D-24)
  </read_first>
  <action>
    **Parte A — Criar `src/components/ProdutoEditDialog.tsx`:**

    Dialog focado em EDIÇÃO de produto existente (D-23). Cadastro novo de produto (PROD-01) é Phase 3 — fora de escopo. Aqui editamos: descrição, preço tabela, preço mínimo, arquiteto. Código fica readonly (chave de correlação com snapshots e CSV imports — não mudar).

    ```tsx
    import { useState, useEffect } from "react";
    import { supabase } from "@/integrations/supabase/client";
    import { Button } from "@/components/ui/button";
    import { Input } from "@/components/ui/input";
    import { Label } from "@/components/ui/label";
    import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter } from "@/components/ui/dialog";
    import { toast } from "sonner";
    import ArquitetoAutocomplete from "@/components/ArquitetoAutocomplete";

    export interface ProdutoEditRow {
      id: string;
      codigo: string;
      descricao: string;
      preco_tabela: number | null;
      preco_minimo: number | null;
      arquiteto_id: string | null;
    }

    interface ProdutoEditDialogProps {
      open: boolean;
      onOpenChange: (open: boolean) => void;
      produto: ProdutoEditRow | null;
      onSuccess: () => void;
    }

    const ProdutoEditDialog = ({ open, onOpenChange, produto, onSuccess }: ProdutoEditDialogProps) => {
      const [descricao, setDescricao] = useState("");
      const [precoTabela, setPrecoTabela] = useState("");
      const [precoMinimo, setPrecoMinimo] = useState("");
      const [arquitetoId, setArquitetoId] = useState<string | null>(null);
      const [arquitetoNome, setArquitetoNome] = useState("");
      const [saving, setSaving] = useState(false);

      useEffect(() => {
        if (!open || !produto) return;
        setDescricao(produto.descricao || "");
        setPrecoTabela(produto.preco_tabela != null ? String(produto.preco_tabela) : "");
        setPrecoMinimo(produto.preco_minimo != null ? String(produto.preco_minimo) : "");
        setArquitetoId(produto.arquiteto_id);
        if (produto.arquiteto_id) {
          supabase.from("arquitetos").select("nome").eq("id", produto.arquiteto_id).maybeSingle().then(({ data }) => {
            setArquitetoNome(data?.nome || "");
          });
        } else {
          setArquitetoNome("");
        }
      }, [open, produto]);

      const handleSubmit = async (e: React.FormEvent) => {
        e.preventDefault();
        if (!produto) return;
        if (!descricao.trim()) {
          toast.error("Descrição é obrigatória");
          return;
        }
        const ptNum = precoTabela.trim() ? Number(precoTabela.replace(",", ".")) : null;
        const pmNum = precoMinimo.trim() ? Number(precoMinimo.replace(",", ".")) : null;
        if (ptNum !== null && Number.isNaN(ptNum)) { toast.error("Preço tabela inválido"); return; }
        if (pmNum !== null && Number.isNaN(pmNum)) { toast.error("Preço mínimo inválido"); return; }

        setSaving(true);
        const { error } = await supabase
          .from("produtos")
          .update({
            descricao: descricao.trim(),
            preco_tabela: ptNum,
            preco_minimo: pmNum,
            arquiteto_id: arquitetoId,
          })
          .eq("id", produto.id);
        setSaving(false);
        if (error) {
          toast.error("Erro ao salvar: " + error.message);
          return;
        }
        toast.success("Produto atualizado!");
        onOpenChange(false);
        onSuccess();
      };

      return (
        <Dialog open={open} onOpenChange={onOpenChange}>
          <DialogContent className="sm:max-w-md">
            <DialogHeader>
              <DialogTitle>Editar Produto</DialogTitle>
            </DialogHeader>
            <form onSubmit={handleSubmit} className="space-y-4 py-2">
              <div className="space-y-2">
                <Label>Código</Label>
                <Input value={produto?.codigo || ""} disabled className="font-mono" />
                <p className="text-xs text-muted-foreground">Código é fixo (chave de correlação com importação CSV).</p>
              </div>
              <div className="space-y-2">
                <Label htmlFor="prod-desc">Descrição *</Label>
                <Input
                  id="prod-desc"
                  value={descricao}
                  onChange={(e) => setDescricao(e.target.value)}
                  required
                />
              </div>
              <div className="grid grid-cols-2 gap-3">
                <div className="space-y-2">
                  <Label htmlFor="prod-pt">Preço Tabela</Label>
                  <Input
                    id="prod-pt"
                    inputMode="decimal"
                    placeholder="0.00"
                    value={precoTabela}
                    onChange={(e) => setPrecoTabela(e.target.value)}
                  />
                </div>
                <div className="space-y-2">
                  <Label htmlFor="prod-pm">Preço Mínimo</Label>
                  <Input
                    id="prod-pm"
                    inputMode="decimal"
                    placeholder="0.00"
                    value={precoMinimo}
                    onChange={(e) => setPrecoMinimo(e.target.value)}
                  />
                </div>
              </div>
              <div className="space-y-2">
                <Label>Arquiteto</Label>
                <ArquitetoAutocomplete
                  value={arquitetoNome}
                  onSelect={(arq) => {
                    if (arq === null) {
                      setArquitetoId(null);
                      setArquitetoNome("");
                    } else {
                      setArquitetoId(arq.id);
                      setArquitetoNome(arq.nome);
                    }
                  }}
                  placeholder="Buscar arquiteto..."
                />
              </div>
              <DialogFooter>
                <Button type="button" variant="outline" onClick={() => onOpenChange(false)}>
                  Cancelar
                </Button>
                <Button type="submit" disabled={saving || !descricao.trim()}>
                  {saving ? "Salvando..." : "Salvar"}
                </Button>
              </DialogFooter>
            </form>
          </DialogContent>
        </Dialog>
      );
    };

    export default ProdutoEditDialog;
    ```

    **Parte B — Integrar na aba Produtos do `src/pages/Admin.tsx`:**

    1. Adicionar import:
    ```typescript
    import ProdutoEditDialog, { type ProdutoEditRow } from "@/components/ProdutoEditDialog";
    ```

    2. Adicionar state (perto do state de produtos, linha ~40-44):
    ```typescript
    const [produtoEditOpen, setProdutoEditOpen] = useState(false);
    const [produtoEditTarget, setProdutoEditTarget] = useState<ProdutoEditRow | null>(null);
    ```

    3. Atualizar `fetchProdutos` para incluir `arquiteto_id` (linha ~74-84). Mudar `.select("*", { count: "exact" })` (já tem `*`, então arquiteto_id já vem). Adicionar fetch do nome do arquiteto via map (reusar `arquitetosMap` da Task 3 ou fazer join SQL).

    4. Na Table de produtos (linhas 228-256), adicionar coluna Arquiteto e coluna Ações:

    Substituir `<TableHeader>` por:
    ```tsx
    <TableHeader>
      <TableRow>
        <TableHead>Código</TableHead>
        <TableHead>Descrição</TableHead>
        <TableHead>Arquiteto</TableHead>
        <TableHead className="text-right">Preço Tabela</TableHead>
        <TableHead className="text-right">Preço Mínimo</TableHead>
        <TableHead className="w-20 text-right">Ações</TableHead>
      </TableRow>
    </TableHeader>
    ```

    Em cada `<TableRow>` (linhas 243-250) substituir por:
    ```tsx
    <TableRow key={p.id}>
      <TableCell className="font-mono text-sm">{p.codigo}</TableCell>
      <TableCell>{p.descricao}</TableCell>
      <TableCell>{p.arquiteto_id ? (arquitetosMap[p.arquiteto_id] || "—") : "—"}</TableCell>
      <TableCell className="text-right">{p.preco_tabela ? `R$ ${Number(p.preco_tabela).toFixed(2)}` : "—"}</TableCell>
      <TableCell className="text-right">{p.preco_minimo ? `R$ ${Number(p.preco_minimo).toFixed(2)}` : "—"}</TableCell>
      <TableCell className="text-right">
        <Button
          variant="ghost"
          size="sm"
          onClick={() => {
            setProdutoEditTarget({
              id: p.id,
              codigo: p.codigo,
              descricao: p.descricao,
              preco_tabela: p.preco_tabela,
              preco_minimo: p.preco_minimo,
              arquiteto_id: p.arquiteto_id ?? null,
            });
            setProdutoEditOpen(true);
          }}
        >
          <Pencil className="h-4 w-4" />
        </Button>
      </TableCell>
    </TableRow>
    ```

    Atualizar `colSpan` dos placeholders ("Buscando..." e "Nenhum produto") de 4 para 6.

    5. Montar `<ProdutoEditDialog>` no final (junto dos outros dialogs):
    ```tsx
    <ProdutoEditDialog
      open={produtoEditOpen}
      onOpenChange={setProdutoEditOpen}
      produto={produtoEditTarget}
      onSuccess={() => fetchProdutos(produtoSearch)}
    />
    ```

    Não tocar na busca de produtos, em outras abas, em outros dialogs.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "ProdutoEditDialog|Admin.tsx"; test $? -ne 0 || echo "ts errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ProdutoEditDialog.tsx` existe
    - ProdutoEditDialog contém `import ArquitetoAutocomplete`
    - ProdutoEditDialog contém `<Input value={produto?.codigo || ""} disabled` (código readonly)
    - Contém `arquiteto_id: arquitetoId` no UPDATE payload
    - `src/pages/Admin.tsx` contém `import ProdutoEditDialog, { type ProdutoEditRow } from "@/components/ProdutoEditDialog"`
    - Aba Produtos contém `<TableHead>Arquiteto</TableHead>`
    - Cada linha de produto tem botão `<Pencil` que abre Dialog
    - Dois ColSpan (Buscando + Nenhum produto) atualizados para 6
    - `<ProdutoEditDialog>` montado no final
    - `npx tsc --noEmit` passa
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>
    Admin pode editar qualquer produto e atribuir/alterar arquiteto. Lista mostra arquiteto vinculado. Código permanece readonly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → public.clientes (UPDATE/INSERT) | Colaborador autenticado edita clientes (RLS ampla — same as today). |
| browser → public.produtos (UPDATE) | Admin edita produtos (RLS restringe a admin). |
| ArquitetoAutocomplete → public.arquitetos SELECT | Qualquer autenticado pode ler arquitetos (intencional, Plan 02-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Tampering | CPF/CNPJ no cliente sem validação | accept | Locked D-12 ("sem validação semântica neste marco"). Aceito porque campo é opcional e não vira ID legal nesta fase. |
| T-02-17 | Information Disclosure | toast/console com dados de cliente | mitigate | Toast genérico ("Erro ao salvar cliente: " + error.message). error.message do Supabase pode incluir constraint violation mas não dados crus. Sem console.log de payload. |
| T-02-18 | Elevation of Privilege | colaborador comum tenta UPDATE produto | mitigate | RLS de produtos já restringe escrita a admin (assumido — confirmar no smoke). Aba Produtos só carrega via AdminRoute. |
| T-02-19 | Denial of Service | autocomplete spam de query a cada keystroke | mitigate | Debounce 300ms no ArquitetoAutocomplete (Plan 03). Limit 10. Indice btree em `arquitetos.nome`. |
| T-02-20 | Tampering | preço editado para valor absurdo | accept | Sem validação de range. Decisão de negócio (admin tem responsabilidade total). Marco focado em features, não em business rules de preço. |
</threat_model>

<verification>
- `npx tsc --noEmit` passa
- `npm run build` exit 0
- `npm run lint` passa
- Smoke manual:
  1. Logar como colaborador, ir em / e clicar "Novo Cliente"
  2. Ver dialog com 4 campos (Nome*, Contato, CPF/CNPJ, Arquiteto)
  3. Digitar CPF "12345678901" → mostra "123.456.789-01" (auto-detect CPF)
  4. Digitar CNPJ "12345678000190" → mostra "12.345.678/0001-90" (auto-detect CNPJ)
  5. Buscar arquiteto no autocomplete → ver lista (com "Nenhum arquiteto" no topo)
  6. Selecionar arquiteto → nome aparece no input do autocomplete
  7. Submit → cliente criado com FK correta. Verificar via SQL que `cpf_cnpj` está desmascarado e `arquiteto_id` preenchido
  8. Logar como admin, ir em /admin?tab=clientes
  9. Ver coluna "Arquiteto" com nome do arquiteto na linha do cliente criado
  10. Clicar Editar (lápis) → Dialog abre populated; trocar arquiteto, salvar → linha atualiza
  11. Ir em /admin?tab=produtos, clicar Editar em um produto
  12. Atribuir arquiteto, salvar → coluna Arquiteto na lista atualiza
  13. Wizard 3 passos do orçamento continua funcionando (Step1 → Step2 → Step3 → PDF)
</verification>

<success_criteria>
1. Form de criar cliente aceita Contato, CPF/CNPJ (mascarado, sem validação), Arquiteto (autocomplete) — todos opcionais (CLI-01, CLI-02, CLI-03)
2. Form de editar cliente expõe os mesmos 3 campos pré-populados (D-15)
3. Submit envia CPF/CNPJ desmascarado pra `clientes.cpf_cnpj`
4. Admin > Produtos: edição com Dialog que inclui campo Arquiteto via ArquitetoAutocomplete (PROD-04)
5. PROD-03 viabilizada: admin pode atribuir arquiteto a qualquer produto existente via UI manual
6. Lista de produtos no admin mostra coluna Arquiteto
7. Wizard 3 passos não regrediu (orçamento continua funcionando)
</success_criteria>

<output>
After completion, create `.planning/phases/02-cadastros-arquiteto-crud/02-04-SUMMARY.md` documenting:
- Files criados/alterados
- Resultado smoke manual passos 1-13
- **SQL alternativo para PROD-03 em massa (D-24)** — bloco SQL pronto pra Lenny rodar no Supabase SQL Editor caso queira atribuir arquiteto a 50+ produtos de uma vez. Exemplo:
  ```sql
  -- Vincular todos os produtos com prefixo "MK27" ao arquiteto X
  UPDATE produtos SET arquiteto_id = (SELECT id FROM arquitetos WHERE nome = 'Studio MK27')
  WHERE codigo LIKE 'MK27%';
  ```
- Confirmação de que phase 2 fechou: 10/10 requirements deliverables (USR-01..04, CLI-01..03, ARQ-02, PROD-03, PROD-04)
</output>
