---
phase: 18-ux-transversal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/orcamento.ts
  - src/types/__tests__/clonagem.test.ts
  - src/types/__tests__/checklistDetectors.test.ts
autonomous: true
requirements: [RES-04, UX-04, UX-05]
user_setup: []

must_haves:
  truths:
    - "clonarSistema gera novo UUID no sistema e em fita/driver/perfil e sufixa local com ' (cópia)'"
    - "clonarSistemaParaAmbiente gera novos UUIDs mas preserva o local original sem sufixo"
    - "clonarAmbiente clona a árvore inteira (ambiente + luminárias + sistemas) sem reusar nenhum UUID e sufixa o nome com ' (cópia)'"
    - "detectarChecklistIssues retorna erro bloqueante para fita com 0m e avisos para sem-driver/driver-sem-fita/perfil-sem-fita/voltagem-divergente/peça-sem-lâmpada"
  artifacts:
    - path: "src/types/orcamento.ts"
      provides: "clonarSistema, clonarSistemaParaAmbiente, clonarAmbiente, detectarChecklistIssues, ChecklistIssue, luminariaPrecisaLampada, ambienteTemLampada"
      exports: ["clonarSistema", "clonarSistemaParaAmbiente", "clonarAmbiente", "detectarChecklistIssues", "ChecklistIssue"]
    - path: "src/types/__tests__/clonagem.test.ts"
      provides: "Cobertura de UUID-novo e sufixo das funções de clonagem"
    - path: "src/types/__tests__/checklistDetectors.test.ts"
      provides: "Cobertura dos 6 detectores do checklist"
  key_links:
    - from: "detectarChecklistIssues"
      to: "Ambiente[] runtime data"
      via: "pure function, no async"
      pattern: "export function detectarChecklistIssues"
---

<objective>
Camada de domínio (helpers puros) para a Phase 18: funções de clonagem com novos UUIDs (RES-04 duplicar sistema, UX-04 duplicar ambiente) e detector unificado de itens suspeitos do checklist (UX-05), todos exportados de `src/types/orcamento.ts` com testes Vitest.

Purpose: Centralizar a lógica testável antes que os consumidores (AmbienteCard, Step2Ambientes, Step3Revisao) a importem na Wave 2. Evita lógica inline não-testável e colisão de edição entre plans.
Output: 3 funções de clonagem + 1 função de detecção + tipo `ChecklistIssue` + 2 predicados de lâmpada exportados, com 2 arquivos de teste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-ux-transversal/18-CONTEXT.md
@.planning/phases/18-ux-transversal/18-RESEARCH.md

<interfaces>
<!-- Tipos já existentes em src/types/orcamento.ts (linhas 36-100). Use-os diretamente, não explore o codebase. -->

```typescript
export interface ItemLuminaria { id: string; codigo: string; descricao: string; quantidade: number; precoUnitario: number; precoMinimo: number; imagemUrl?: string; sistema?: string | null; potencia_watts?: number | null; tensao?: number | null; }
export interface ItemPerfil { id: string; codigo: string; descricao: string; comprimentoPeca: 1|2|3; quantidade: number; passadas: 1|2|3; precoUnitario: number; precoMinimo: number; imagemUrl?: string; familia_perfil?: string|null; driver_restr_tipo?: string|null; driver_restr_max_w?: number|null; somente_baby?: boolean|null; passadasPadrao?: 1|2|3; }
export interface ItemFitaLED { id: string; codigo: string; descricao: string; wm: number; voltagem?: 12|24|48; metragemRolo: 5|10|15; precoUnitario: number; precoMinimo: number; imagemUrl?: string; is_baby?: boolean|null; }
export interface ItemDriver { id: string; codigo: string; descricao: string; potencia: number; voltagem: 12|24|48; precoUnitario: number; precoMinimo: number; imagemUrl?: string; driver_tipo?: string|null; }
export interface SistemaIluminacao { id: string; perfil: ItemPerfil | null; fita: ItemFitaLED; driver: ItemDriver; metragemManual: number | null; passadasManual: 1|2|3; local?: string | null; }
export interface Ambiente { id: string; nome: string; luminarias: ItemLuminaria[]; sistemas: SistemaIluminacao[]; }
```
</interfaces>

<reference_predicates>
<!-- Predicados de lâmpada já existentes (inline, NÃO exportados) em Step2Ambientes.tsx linhas 26-37.
     Esta task os MOVE para orcamento.ts como exports. O plan 18-03 trocará o import no Step2Ambientes. -->

```typescript
function luminariaPrecisaLampada(descricao: string): boolean {
  const d = (descricao ?? '').toUpperCase();
  const temBaseLampada = /\b(GU10|E27|MR11|MR16|AR70|AR111|PAR20|PAR30|DICROICA|DICRO)\b/.test(d);
  const temLedIntegrado = /LED\s+INTEGRADO|COM\s+LED/.test(d);
  return temBaseLampada && !temLedIntegrado;
}
function ambienteTemLampada(amb: Ambiente): boolean {
  return amb.luminarias.some((l) => /l[âa]mpada/i.test(l.descricao ?? '') || (l as any).tipo_produto === 'lampada');
}
```
</reference_predicates>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar helpers de clonagem (UUID-novo) em orcamento.ts</name>
  <read_first>
    - src/types/orcamento.ts (linhas 36-100 — interfaces ItemPerfil/ItemFitaLED/ItemDriver/SistemaIluminacao/Ambiente; final do arquivo ~490 para ponto de inserção)
    - src/components/AmbienteCard.tsx (linha 47 — padrão `const uid = () => crypto.randomUUID()`; linhas 69-74 — formato dos itens)
    - src/types/__tests__/sufixoMetragem.test.ts (padrão de teste de função pura exportada de orcamento.ts)
  </read_first>
  <behavior>
    - clonarSistema(sis): novo `id` (≠ original), `fita.id`/`driver.id` novos, `perfil.id` novo se perfil existir (null permanece null), `local` recebe sufixo " (cópia)" (se local vazio/null → "(cópia)"). Demais campos preservados.
    - clonarSistemaParaAmbiente(sis): mesmos UUIDs novos, MAS `local` preservado sem sufixo.
    - clonarAmbiente(amb): novo `id`, `nome` recebe sufixo " (cópia)", cada luminária com novo `id`, cada sistema clonado via clonarSistemaParaAmbiente (local preservado).
    - Nenhum UUID do original é reusado em nenhum nível da árvore.
  </behavior>
  <action>
Adicionar ao FINAL de `src/types/orcamento.ts` (após a última export, ~linha 490) este bloco. Usar `crypto.randomUUID()` (padrão do projeto, AmbienteCard.tsx:47). NÃO usar JSON.parse/stringify.

```typescript
// ─── Clonagem de sistemas e ambientes (Phase 18 — RES-04 / UX-04) ───
// Regenera TODOS os UUIDs da árvore para evitar colisão de key em re-render e snapshot/PDF.

/** Duplica um sistema dentro do MESMO ambiente (RES-04). Novos UUIDs + sufixo " (cópia)" no local. */
export function clonarSistema(sis: SistemaIluminacao): SistemaIluminacao {
  return {
    ...sis,
    id: crypto.randomUUID(),
    local: sis.local ? `${sis.local} (cópia)` : '(cópia)',
    fita: { ...sis.fita, id: crypto.randomUUID() },
    driver: { ...sis.driver, id: crypto.randomUUID() },
    perfil: sis.perfil ? { ...sis.perfil, id: crypto.randomUUID() } : null,
  };
}

/** Duplica um sistema para dentro de um ambiente clonado (UX-04). Novos UUIDs, local PRESERVADO. */
export function clonarSistemaParaAmbiente(sis: SistemaIluminacao): SistemaIluminacao {
  return {
    ...sis,
    id: crypto.randomUUID(),
    fita: { ...sis.fita, id: crypto.randomUUID() },
    driver: { ...sis.driver, id: crypto.randomUUID() },
    perfil: sis.perfil ? { ...sis.perfil, id: crypto.randomUUID() } : null,
  };
}

/** Duplica um ambiente inteiro (UX-04). Novos UUIDs em toda a árvore + sufixo " (cópia)" no nome. */
export function clonarAmbiente(amb: Ambiente): Ambiente {
  return {
    ...amb,
    id: crypto.randomUUID(),
    nome: `${amb.nome} (cópia)`,
    luminarias: amb.luminarias.map((l) => ({ ...l, id: crypto.randomUUID() })),
    sistemas: amb.sistemas.map((sis) => clonarSistemaParaAmbiente(sis)),
  };
}
```

Criar `src/types/__tests__/clonagem.test.ts` (espelhando o padrão de sufixoMetragem.test.ts) com helpers de fixture mínimos e os casos abaixo.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run test -- clonagem</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function clonarSistema\b" src/types/orcamento.ts` retorna 1 match
    - `grep -n "export function clonarSistemaParaAmbiente" src/types/orcamento.ts` retorna 1 match
    - `grep -n "export function clonarAmbiente" src/types/orcamento.ts` retorna 1 match
    - `grep -c "crypto.randomUUID()" src/types/orcamento.ts` retorna ao menos 7 (3 em clonarSistema, 3 em clonarSistemaParaAmbiente, 1+ em clonarAmbiente nas luminárias)
    - Teste clonagem.test.ts cobre: clonarSistema → sis.id/fita.id/driver.id/perfil.id todos ≠ original E local termina com "(cópia)"; clonarSistemaParaAmbiente → IDs ≠ original E local === local original; clonarAmbiente → amb.id ≠ original, nome termina com "(cópia)", nenhum sistema/luminária/fita/driver id presente no conjunto de ids do original
    - `npm run test -- clonagem` passa (verde)
  </acceptance_criteria>
  <done>clonarSistema, clonarSistemaParaAmbiente e clonarAmbiente exportados de orcamento.ts; teste clonagem.test.ts passa cobrindo UUID-novo e sufixos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extrair detectores para detectarChecklistIssues em orcamento.ts</name>
  <read_first>
    - src/components/Step2Ambientes.tsx (linhas 26-37 — predicados luminariaPrecisaLampada/ambienteTemLampada; linhas 127-146 — 4 detectores advisory inline)
    - src/components/AmbienteCard.tsx (linhas 411-417 — predicado voltagem divergente; linhas 419-423 — predicado fita 0m sem perfil)
    - .planning/phases/18-ux-transversal/18-UI-SPEC.md (linhas 272-285 — tabela de levels/triggers/mensagens exatas)
    - src/types/__tests__/sufixoMetragem.test.ts (padrão de teste)
  </read_first>
  <behavior>
    - detectarChecklistIssues([]) → []
    - sistema com fita.codigo, sem perfil, metragemManual null/0 → 1 issue level 'error'
    - sistema com fita.codigo e driver.codigo vazio → 1 issue level 'warning' (sem driver)
    - sistema com driver.codigo e fita.codigo vazio → 1 issue 'warning' (driver sem fita)
    - sistema com perfil e fita.codigo vazio → 1 issue 'warning' (perfil sem fita)
    - fita.voltagem definida ≠ driver.voltagem (ambos com codigo) → 1 issue 'warning' (voltagem divergente)
    - luminária que precisa lâmpada num ambiente sem lâmpada → 1 issue 'warning' (peça sem lâmpada)
    - sistema completo e válido → 0 issues
    - múltiplos ambientes → issues de todos coletados; erros antes de avisos
  </behavior>
  <action>
Primeiro, MOVER `luminariaPrecisaLampada` e `ambienteTemLampada` de Step2Ambientes.tsx para orcamento.ts como funções EXPORTADAS (o plan 18-03 trocará o import no Step2Ambientes — esta task apenas adiciona os exports; NÃO remova ainda de Step2Ambientes para não quebrar o build da Wave 1, deixe a remoção/import-swap para 18-03). Adicionar ao final de orcamento.ts:

```typescript
// ─── Detector unificado de itens suspeitos do checklist (Phase 18 — UX-05) ───

export function luminariaPrecisaLampada(descricao: string): boolean {
  const d = (descricao ?? '').toUpperCase();
  const temBaseLampada = /\b(GU10|E27|MR11|MR16|AR70|AR111|PAR20|PAR30|DICROICA|DICRO)\b/.test(d);
  const temLedIntegrado = /LED\s+INTEGRADO|COM\s+LED/.test(d);
  return temBaseLampada && !temLedIntegrado;
}

export function ambienteTemLampada(amb: Ambiente): boolean {
  return amb.luminarias.some(
    (l) => /l[âa]mpada/i.test(l.descricao ?? '') || (l as any).tipo_produto === 'lampada'
  );
}

export interface ChecklistIssue {
  id: string;
  level: 'error' | 'warning';
  ambienteNome: string;
  mensagem: string;
}

/** Detecta itens suspeitos em todos os ambientes. Pura — sem async, sem Supabase.
 *  Erros (fita 0m) vêm antes dos avisos. Consumido por Step2 (advisory) e Step3 (checklist). */
export function detectarChecklistIssues(ambientes: Ambiente[]): ChecklistIssue[] {
  const erros: ChecklistIssue[] = [];
  const avisos: ChecklistIssue[] = [];
  for (const amb of ambientes) {
    for (const sis of amb.sistemas) {
      const fitaVazia = !sis.fita.codigo;
      const driverVazio = !sis.driver.codigo;
      // 🔴 erro: fita sem perfil com metragem 0m (gate CALC-01)
      if (sis.fita.codigo && !sis.perfil && (!sis.metragemManual || sis.metragemManual <= 0)) {
        erros.push({ id: `${amb.id}-${sis.id}-fita0m`, level: 'error', ambienteNome: amb.nome,
          mensagem: `${amb.nome} — Fita sem metragem (0m): o orçamento ficará R$ 0,00` });
      }
      if (sis.fita.codigo && driverVazio) {
        avisos.push({ id: `${amb.id}-${sis.id}-semdriver`, level: 'warning', ambienteNome: amb.nome,
          mensagem: `${amb.nome} — Sistema sem driver` });
      }
      if (sis.driver.codigo && fitaVazia) {
        avisos.push({ id: `${amb.id}-${sis.id}-driversemfita`, level: 'warning', ambienteNome: amb.nome,
          mensagem: `${amb.nome} — Driver sem fita LED` });
      }
      if (sis.perfil && fitaVazia) {
        avisos.push({ id: `${amb.id}-${sis.id}-perfilsemfita`, level: 'warning', ambienteNome: amb.nome,
          mensagem: `${amb.nome} — Perfil sem fita LED` });
      }
      const fv = sis.fita.voltagem, dv = sis.driver.voltagem;
      if (sis.fita.codigo && sis.driver.codigo && fv !== undefined && fv !== null && fv !== dv) {
        avisos.push({ id: `${amb.id}-${sis.id}-voltagem`, level: 'warning', ambienteNome: amb.nome,
          mensagem: `${amb.nome} — Voltagem divergente: fita ${fv}V × driver ${dv}V` });
      }
    }
    if (!ambienteTemLampada(amb)) {
      for (const lum of amb.luminarias) {
        if (luminariaPrecisaLampada(lum.descricao)) {
          avisos.push({ id: `${amb.id}-${lum.id}-semlampada`, level: 'warning', ambienteNome: amb.nome,
            mensagem: `${amb.nome} — Peça sem lâmpada: ${lum.descricao}` });
        }
      }
    }
  }
  return [...erros, ...avisos];
}
```

Criar `src/types/__tests__/checklistDetectors.test.ts` cobrindo os 8 casos do bloco `<behavior>` com fixtures mínimos.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run test -- checklistDetectors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function detectarChecklistIssues" src/types/orcamento.ts` → 1 match
    - `grep -n "export interface ChecklistIssue" src/types/orcamento.ts` → 1 match
    - `grep -n "export function luminariaPrecisaLampada" src/types/orcamento.ts` → 1 match
    - `grep -n "export function ambienteTemLampada" src/types/orcamento.ts` → 1 match
    - mensagem exata "Fita sem metragem (0m): o orçamento ficará R$ 0,00" presente: `grep -c "Fita sem metragem (0m)" src/types/orcamento.ts` ≥ 1
    - mensagem exata "Voltagem divergente: fita" presente em orcamento.ts
    - Teste cobre: array vazio→[]; fita0m→level error; sem driver→warning; voltagem divergente→warning; peça sem lâmpada→warning; sistema válido→[]; e que erros aparecem antes de avisos no retorno (resultado[0].level === 'error' quando há erro + aviso)
    - `npm run test -- checklistDetectors` passa
  </acceptance_criteria>
  <done>detectarChecklistIssues + ChecklistIssue + predicados de lâmpada exportados de orcamento.ts; teste passa cobrindo os 6 detectores e a ordenação erro-antes-de-aviso.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum) | Plan puramente funcional em memória — sem I/O, sem rede, sem persistência |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | clonarAmbiente/clonarSistema (UUID reuse) | mitigate | Regenerar `crypto.randomUUID()` em todos os níveis evita colisão de id que corromperia reconciliação React/snapshot; coberto por teste de unicidade de id |

Nenhuma outra categoria STRIDE aplicável: funções puras sem entrada não-confiável, sem auth, sem persistência. ASVS L1: N/A (sem superfície de rede/input externo).
</threat_model>

<verification>
- `npm run test -- clonagem checklistDetectors` verde
- `npm run lint` sem novos erros
- `npx tsc --noEmit` (ou `npm run build`) compila — exports não quebram tipos existentes
</verification>

<success_criteria>
- clonarSistema, clonarSistemaParaAmbiente, clonarAmbiente, detectarChecklistIssues, ChecklistIssue, luminariaPrecisaLampada, ambienteTemLampada todos exportados de src/types/orcamento.ts
- 2 arquivos de teste passando cobrindo UUID-novo, sufixos e os 6 detectores
- Build compila; nenhuma assinatura/tipo existente alterado (apenas adições)
</success_criteria>

<output>
After completion, create `.planning/phases/18-ux-transversal/18-01-SUMMARY.md`
</output>
