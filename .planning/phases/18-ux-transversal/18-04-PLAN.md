---
phase: 18-ux-transversal
plan: 04
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/components/Step3Revisao.tsx
autonomous: false
requirements: [UX-05]
user_setup: []

must_haves:
  truths:
    - "O Step 3 exibe, como primeiro elemento, um painel 'Verificação pré-PDF' sempre visível"
    - "O painel lista itens suspeitos com 🔴 (erro: fita 0m) e 🟡 (avisos), cada um com link 'corrigir' que volta ao Step 2"
    - "Quando não há issues, o painel mostra estado de sucesso 'Tudo certo! Nenhum item suspeito encontrado.'"
    - "O botão 'Gerar PDF' fica disabled quando há erro bloqueante (fita 0m); avisos não bloqueiam"
  artifacts:
    - path: "src/components/Step3Revisao.tsx"
      provides: "Painel checklist derivado via useMemo + gate temErroBloqueante no botão Gerar PDF"
      contains: "detectarChecklistIssues"
  key_links:
    - from: "Step3Revisao checklistIssues"
      to: "detectarChecklistIssues (orcamento.ts)"
      via: "useMemo([ambientes])"
      pattern: "detectarChecklistIssues\\(ambientes\\)"
    - from: "checklist corrigir button"
      to: "Step 2"
      via: "onPrev()"
      pattern: "onClick={onPrev}|onClick={() => onPrev"
    - from: "Gerar PDF disabled"
      to: "temErroBloqueante"
      via: "disabled condition"
      pattern: "temErroBloqueante"
---

<objective>
Checklist visual pré-PDF (UX-05) no topo do Step 3: painel sempre visível listando itens suspeitos com classificação 🔴 erro / 🟡 aviso, link "corrigir", e gate aditivo no botão Gerar PDF (fita 0m bloqueia, avisos não).

Purpose: Camada de segurança antes do PDF do cliente — destaca tudo que parece incompleto e impede gerar um orçamento com fita R$ 0,00.
Output: Step3Revisao com `checklistIssues` (useMemo), Card de verificação como primeiro elemento, e `disabled` do botão Gerar PDF estendido com `temErroBloqueante`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-ux-transversal/18-CONTEXT.md
@.planning/phases/18-ux-transversal/18-RESEARCH.md
@.planning/phases/18-ux-transversal/18-UI-SPEC.md
@.planning/phases/17-resumo-apresenta-o/17-04-SUMMARY.md

<interfaces>
<!-- Disponível de src/types/orcamento.ts após 18-01. Importe direto. -->
```typescript
export interface ChecklistIssue { id: string; level: 'error' | 'warning'; ambienteNome: string; mensagem: string; }
export function detectarChecklistIssues(ambientes: Ambiente[]): ChecklistIssue[]; // erros antes de avisos
```

<!-- Estado atual de Step3Revisao.tsx (verificado):
     - linha 109: const { dados, ambientes } = orcamento;  ← ambientes em escopo
     - linha 1: já importa useMemo
     - onPrev em Step3Props (linha 28) já wired
     - return abre em <div className="space-y-6"> (linha 517), heading em <div> (linha 518)
     - botão Gerar PDF (linha 838): disabled={hasUnresolved || savingOrcamento}, ícone FileDown
     - Card/CardHeader/CardContent/CardTitle NÃO estão importados ainda — adicionar de @/components/ui/card
     - CheckCircle2/AlertTriangle: AlertTriangle já importado (linha 9); CheckCircle2 adicionar de lucide-react -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: checklistIssues derivado + painel "Verificação pré-PDF"</name>
  <read_first>
    - src/components/Step3Revisao.tsx (linha 1 imports React; linha 9 imports lucide; linha 109 destructure ambientes; linhas 516-521 abertura do return)
    - .planning/phases/18-ux-transversal/18-UI-SPEC.md (linhas 245-326 — composição completa do painel checklist, estados, ordenação)
    - src/types/orcamento.ts (detectarChecklistIssues / ChecklistIssue — disponíveis após 18-01)
  </read_first>
  <action>
**(a) Imports:** adicionar `import { Card, CardHeader, CardContent, CardTitle } from "@/components/ui/card";`. Adicionar `CheckCircle2` ao import de lucide-react (linha 9). Adicionar `detectarChecklistIssues` (e tipo `ChecklistIssue` se necessário) ao import de `@/types/orcamento` (linhas 13-18).

**(b) Estado derivado:** logo após `const { dados, ambientes } = orcamento;` (linha 109) ou junto aos outros useMemo (linhas 130-132), adicionar:
```tsx
const checklistIssues = useMemo(() => detectarChecklistIssues(ambientes), [ambientes]);
const temErroBloqueante = checklistIssues.some((i) => i.level === 'error');
```

**(c) Painel (UX-05):** inserir como PRIMEIRO elemento dentro de `<div className="space-y-6">` (linha 517), ANTES do `<div>` do heading (linha 518):
```tsx
<Card className="border">
  <CardHeader className="pb-2">
    <CardTitle className="text-sm font-semibold flex items-center gap-2">
      {checklistIssues.length === 0 ? (
        <CheckCircle2 className="h-4 w-4 text-emerald-500" />
      ) : (
        <AlertTriangle className={cn("h-4 w-4", temErroBloqueante ? "text-destructive" : "text-yellow-600")} />
      )}
      Verificação pré-PDF
    </CardTitle>
  </CardHeader>
  <CardContent className="pt-0 space-y-1">
    {checklistIssues.length === 0 ? (
      <p className="text-sm text-muted-foreground">Tudo certo! Nenhum item suspeito encontrado.</p>
    ) : (
      checklistIssues.map((issue) => (
        <div key={issue.id} className="flex items-center justify-between gap-3 py-1">
          <div className="flex items-center gap-2">
            <span>{issue.level === 'error' ? '🔴' : '🟡'}</span>
            <span className="text-sm text-foreground">{issue.mensagem}</span>
          </div>
          <Button variant="link" size="sm" className="h-auto p-0 text-xs text-primary" onClick={onPrev}>
            corrigir
          </Button>
        </div>
      ))
    )}
  </CardContent>
</Card>
```
Importar `cn` de `@/lib/utils` se ainda não importado. O link "corrigir" chama `onPrev` (já em Step3Props, volta ao Step 2 — D-11).
  </action>
  <verify>
    <automated>cd auraoramentos && npx tsc --noEmit 2>&1 | grep -i "Step3Revisao" || echo "tsc OK Step3Revisao"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "detectarChecklistIssues(ambientes)" src/components/Step3Revisao.tsx` → 1 match dentro de useMemo
    - `grep -n "const temErroBloqueante" src/components/Step3Revisao.tsx` → 1 match
    - heading exato: `grep -c "Verificação pré-PDF" src/components/Step3Revisao.tsx` ≥ 1
    - sucesso exato: `grep -c "Tudo certo! Nenhum item suspeito encontrado." src/components/Step3Revisao.tsx` ≥ 1
    - link exato: `grep -c ">corrigir<" src/components/Step3Revisao.tsx` ≥ 1 e o botão chama `onPrev`
    - Card importado de `@/components/ui/card`; CheckCircle2 de lucide-react
    - painel é o primeiro filho de `<div className="space-y-6">` (antes do heading "Revisão do Orçamento")
  </acceptance_criteria>
  <done>Painel "Verificação pré-PDF" sempre visível no topo do Step 3, com estado de sucesso e lista de issues 🔴/🟡 com link "corrigir".</done>
</task>

<task type="auto">
  <name>Task 2: Gate do botão Gerar PDF com temErroBloqueante</name>
  <read_first>
    - src/components/Step3Revisao.tsx (linha 838 — botão Gerar PDF com disabled={hasUnresolved || savingOrcamento})
    - .planning/phases/18-ux-transversal/18-UI-SPEC.md (linhas 297-316 — gate D-12)
  </read_first>
  <action>
Estender o `disabled` do botão Gerar PDF (linha 838) para incluir `temErroBloqueante` (aditivo — preservar `hasUnresolved` e `savingOrcamento`):
```tsx
<Button onClick={handlePDF} className="gap-2 print:hidden"
  disabled={hasUnresolved || savingOrcamento || temErroBloqueante}
  title={temErroBloqueante ? "Corrija a fita sem metragem (0m) antes de gerar o PDF" : (hasUnresolved ? "Resolva as violações de preço antes de gerar o PDF" : "")}>
  <FileDown className="h-4 w-4" /> {savingOrcamento ? "Salvando..." : "Gerar PDF"}
</Button>
```
NÃO remover o gate `hasUnresolved` (violações de preço) — é independente.
  </action>
  <verify>
    <automated>cd auraoramentos && npx tsc --noEmit 2>&1 | grep -i "Step3Revisao" || echo "tsc OK Step3Revisao"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "disabled={hasUnresolved || savingOrcamento || temErroBloqueante}" src/components/Step3Revisao.tsx` → 1 match
    - `grep -c "hasUnresolved" src/components/Step3Revisao.tsx` ≥ 2 (gate de preço preservado)
    - title menciona "fita sem metragem" quando temErroBloqueante
  </acceptance_criteria>
  <done>Botão Gerar PDF fica disabled quando há fita 0m (erro bloqueante); avisos 🟡 não bloqueiam; gate de preço preservado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana — checklist pré-PDF + gate</name>
  <what-built>UX-05 checklist visual pré-PDF no Step 3 + gate de bloqueio do botão Gerar PDF.</what-built>
  <action>Pausar para verificação humana no navegador conforme <how-to-verify>. Não prosseguir até receber o resume-signal.</action>
  <how-to-verify>
    1. `npm run dev`, montar um orçamento e ir ao Step 3.
    2. Caso limpo (sistema completo, fita com metragem): painel "Verificação pré-PDF" no topo mostra ícone verde + "Tudo certo! Nenhum item suspeito encontrado." e o botão "Gerar PDF" está habilitado.
    3. Voltar ao Step 2, criar um sistema com fita SEM driver e outro com voltagem divergente (fita 24V + driver 12V). Voltar ao Step 3: o painel deve listar 🟡 "Sistema sem driver" e 🟡 "Voltagem divergente: fita 24V × driver 12V"; botão Gerar PDF AINDA habilitado (avisos não bloqueiam).
    4. Criar um sistema com fita sem perfil e metragem 0/null (se o gate da Phase 16 permitir chegar ao Step 3 num rascunho antigo): o painel deve mostrar 🔴 "Fita sem metragem (0m): o orçamento ficará R$ 0,00" e o botão Gerar PDF deve ficar DESABILITADO.
    5. Clicar "corrigir" em qualquer linha → volta ao Step 2.
    6. Console do navegador sem erros JS.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum) | Derivação de estado em memória (useMemo sobre ambientes) — sem I/O, sem rede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering | gate temErroBloqueante (bypass do bloqueio fita 0m) | accept | É um gate de UX no cliente, não de segurança; o cálculo de PDF é client-side e o objetivo é evitar erro humano, não ataque. Predicado idêntico ao gate CALC-01 da Phase 16 |

Sem writes novos, sem rede, sem PII. ASVS L1: N/A (sem input externo/superfície de rede neste plan).
</threat_model>

<verification>
- `npx tsc --noEmit` compila
- `npm run lint` sem novos erros
- Checkpoint humano (checklist + gate PDF) aprovado
</verification>

<success_criteria>
- Painel "Verificação pré-PDF" sempre visível no topo do Step 3 com sucesso/issues 🔴🟡 e link "corrigir"
- Gerar PDF bloqueado por fita 0m; avisos não bloqueiam; gate de preço preservado
</success_criteria>

<output>
After completion, create `.planning/phases/18-ux-transversal/18-04-SUMMARY.md`
</output>
