---
phase: 18-ux-transversal
plan: 03
type: execute
wave: 3
depends_on: ["18-01", "18-02"]
files_modified:
  - src/components/Step2Ambientes.tsx
  - src/components/AmbienteCard.tsx
autonomous: false
requirements: [UX-04]
user_setup: []

must_haves:
  truths:
    - "Cada AmbienteCard exibe um botão Duplicar ambiente que clona o ambiente inteiro logo abaixo do original"
    - "O ambiente clonado tem nome sufixado '(cópia)', todos os UUIDs novos, e preserva luminárias e sistemas (local dos sistemas sem sufixo)"
    - "O advisory gate Step2→Step3 continua funcionando, agora consumindo o detector compartilhado de orcamento.ts"
  artifacts:
    - path: "src/components/Step2Ambientes.tsx"
      provides: "duplicarAmbiente + onDuplicate na render loop + import de helpers compartilhados"
      contains: "clonarAmbiente"
    - path: "src/components/AmbienteCard.tsx"
      provides: "prop onDuplicate + botão Duplicar ambiente no header"
      contains: "onDuplicate"
  key_links:
    - from: "Step2Ambientes duplicarAmbiente"
      to: "clonarAmbiente (orcamento.ts)"
      via: "import + splice i+1 + onChange"
      pattern: "clonarAmbiente"
    - from: "Step2Ambientes handleNext advisory"
      to: "luminariaPrecisaLampada (orcamento.ts)"
      via: "import compartilhado"
      pattern: "luminariaPrecisaLampada"
---

<objective>
Duplicar ambiente inteiro (UX-04) em Step2Ambientes + botão no header do AmbienteCard + refatorar o advisory da Phase 17 para consumir os predicados compartilhados de orcamento.ts (evitando duplicação de lógica com o checklist do Step 3).

Purpose: Reduzir retrabalho ao montar quartos/sancas iguais, e unificar a fonte da lógica de detecção de itens incompletos.
Output: Step2Ambientes com handler duplicarAmbiente + prop onDuplicate; AmbienteCard com prop onDuplicate + botão; predicados de lâmpada importados de orcamento.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-ux-transversal/18-CONTEXT.md
@.planning/phases/18-ux-transversal/18-RESEARCH.md
@.planning/phases/18-ux-transversal/18-UI-SPEC.md

<interfaces>
<!-- Disponível de src/types/orcamento.ts após 18-01. Importe direto. -->
```typescript
export function clonarAmbiente(amb: Ambiente): Ambiente; // árvore inteira, novos UUIDs, nome " (cópia)"
export function luminariaPrecisaLampada(descricao: string): boolean;
export function ambienteTemLampada(amb: Ambiente): boolean;
```

<!-- AmbienteCard.tsx já foi editado por 18-02 (Wave 2): Tabs controlado, microcopy, botão duplicar
     sistema, import de Copy de lucide-react e de clonarSistema de @/types/orcamento já presentes.
     Esta task ADICIONA a prop onDuplicate e o botão de duplicar ambiente no header. -->
</interfaces>
</context>

<dependency_note>
Este plan está na Wave 3 e depende de 18-02 (que já editou AmbienteCard.tsx na Wave 2). Antes de editar AmbienteCard.tsx, confirme via `git log --oneline -- src/components/AmbienteCard.tsx` que as mudanças de 18-02 (Tabs controlado, botão duplicar sistema, import de `Copy`) já estão presentes. O `Copy` de lucide-react já deve estar importado por 18-02 — reuse, não duplique o import.
</dependency_note>

<tasks>

<task type="auto">
  <name>Task 1: Refatorar advisory do Step2 para usar predicados compartilhados</name>
  <read_first>
    - src/components/Step2Ambientes.tsx (linhas 1-55 imports + predicados inline; linhas 124-155 advisory em handleNext)
    - src/types/orcamento.ts (luminariaPrecisaLampada / ambienteTemLampada exportados após 18-01)
  </read_first>
  <action>
Remover as definições inline de `luminariaPrecisaLampada` (linhas 26-31) e `ambienteTemLampada` (linhas 33-37) de Step2Ambientes.tsx e importá-las de `@/types/orcamento`. Adicionar ao import de `@/types/orcamento` (ou criar import nomeado): `luminariaPrecisaLampada, ambienteTemLampada, clonarAmbiente`.

NÃO alterar o comportamento do advisory: as 4 detecções inline em handleNext (fita-sem-driver, driver-sem-fita, perfil-sem-fita, peca-sem-lampada) e o AlertDialog permanecem idênticos — apenas os dois predicados agora vêm do import. Isto garante que o advisory da Phase 17 NÃO regride.
  </action>
  <verify>
    <automated>cd auraoramentos && npx tsc --noEmit 2>&1 | grep -i "Step2Ambientes" || echo "tsc OK Step2Ambientes"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function luminariaPrecisaLampada" src/components/Step2Ambientes.tsx` → 0 (definição inline removida)
    - `grep -c "function ambienteTemLampada" src/components/Step2Ambientes.tsx` → 0 (definição inline removida)
    - `grep -n "luminariaPrecisaLampada" src/components/Step2Ambientes.tsx` → ainda usado (no advisory) via import
    - import de `@/types/orcamento` inclui luminariaPrecisaLampada, ambienteTemLampada e clonarAmbiente
    - AlertDialog advisory preservado: `grep -c "Alguns itens parecem incompletos" src/components/Step2Ambientes.tsx` ≥ 1
  </acceptance_criteria>
  <done>Predicados de lâmpada importados de orcamento.ts; advisory da Phase 17 intacto (comportamento idêntico).</done>
</task>

<task type="auto">
  <name>Task 2: duplicarAmbiente + botão Duplicar ambiente no AmbienteCard</name>
  <read_first>
    - src/components/Step2Ambientes.tsx (linhas 57-74 addAmbiente/updateAmbiente/removeAmbiente; linhas 169-178 render loop dos AmbienteCard)
    - src/components/AmbienteCard.tsx (interface AmbienteCardProps; header com botão remover ~linha 337) — já modificado por 18-02
    - .planning/phases/18-ux-transversal/18-UI-SPEC.md (linhas 200-241 — composição botão duplicar ambiente + FLAG sr-only)
  </read_first>
  <action>
**(a) Step2Ambientes — handler:** adicionar perto de updateAmbiente (linha 70):
```tsx
const duplicarAmbiente = (index: number) => {
  const clone = clonarAmbiente(ambientes[index]);
  const arr = [...ambientes];
  arr.splice(index + 1, 0, clone);
  onChange(arr);
};
```

**(b) Step2Ambientes — render loop (linha 171):** adicionar a prop `onDuplicate`:
```tsx
<AmbienteCard
  key={amb.id}
  ambiente={amb}
  onChange={(a) => updateAmbiente(i, a)}
  onRemove={() => removeAmbiente(i)}
  onDuplicate={() => duplicarAmbiente(i)}
/>
```

**(c) AmbienteCard — prop:** adicionar `onDuplicate?: () => void;` à interface `AmbienteCardProps` e ao destructure do componente. `Copy` já está importado de lucide-react por 18-02 — reuse.

**(d) AmbienteCard — botão no header:** no header (~linha 337, junto ao botão remover Trash2), ANTES do botão Trash2, inserir:
```tsx
{onDuplicate && (
  <Button size="icon" variant="ghost" className="h-8 w-8 text-muted-foreground hover:text-foreground" title="Duplicar ambiente" onClick={(e) => { e.stopPropagation(); onDuplicate(); }}>
    <span className="sr-only">Duplicar ambiente</span>
    <Copy className="h-4 w-4" />
  </Button>
)}
```
(FLAG UI-SPEC: `<span className="sr-only">` obrigatório no botão icon-only. `e.stopPropagation()` evita abrir/fechar o collapsible ao clicar.)
  </action>
  <verify>
    <automated>cd auraoramentos && npx tsc --noEmit 2>&1 | grep -iE "Step2Ambientes|AmbienteCard" || echo "tsc OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "const duplicarAmbiente" src/components/Step2Ambientes.tsx` → 1 match, usa `clonarAmbiente` + `splice(index + 1, 0, clone)`
    - `grep -n "onDuplicate={() => duplicarAmbiente(i)}" src/components/Step2Ambientes.tsx` → 1 match
    - `grep -n "onDuplicate?: () => void" src/components/AmbienteCard.tsx` → 1 match (prop adicionada)
    - botão duplicar ambiente: `grep -c "title=\"Duplicar ambiente\"" src/components/AmbienteCard.tsx` ≥ 1
    - `grep -c "Duplicar ambiente" src/components/AmbienteCard.tsx` ≥ 2 (title + sr-only)
    - botão renderizado condicionalmente em `{onDuplicate && (` e usa `e.stopPropagation()`
  </acceptance_criteria>
  <done>Botão Duplicar ambiente no header do AmbienteCard clona o ambiente inteiro logo abaixo via clonarAmbiente.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana — duplicar ambiente + advisory</name>
  <what-built>UX-04 duplicar ambiente inteiro no Step 2 + advisory da Phase 17 refatorado para detector compartilhado.</what-built>
  <action>Pausar para verificação humana no navegador conforme <how-to-verify>. Não prosseguir até receber o resume-signal.</action>
  <how-to-verify>
    1. `npm run dev`, abrir o wizard, Step 2.
    2. Montar um ambiente "Quarto 1" com 1 luminária + 1 sistema (fita+driver, Local "Sanca").
    3. Clicar no ícone Duplicar (Copy) no header do ambiente. Esperado: "Quarto 1 (cópia)" aparece logo abaixo, com a mesma luminária e o mesmo sistema (Local "Sanca", SEM sufixo no local do sistema).
    4. Editar o preço de um item no clone → o original NÃO muda (UUIDs distintos).
    5. Clicar "Próximo" com um sistema incompleto (fita sem driver) → o advisory "Alguns itens parecem incompletos" deve aparecer normalmente (regressão da Phase 17).
    6. Console do navegador sem erros JS.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum) | Mutação de estado React em memória — sem I/O, sem rede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | clonarAmbiente (UUID compartilhado) | mitigate | clonarAmbiente (coberto por teste no 18-01) regenera todos os UUIDs; editar clone não afeta original. Verificado no checkpoint passo 4 |

Sem writes, sem rede, sem PII. ASVS L1: N/A (sem input externo/superfície de rede neste plan).
</threat_model>

<verification>
- `npx tsc --noEmit` compila
- `npm run lint` sem novos erros
- `npm run test -- Step2Gate` ainda passa (advisory não regrediu)
- Checkpoint humano (duplicar ambiente + advisory) aprovado
</verification>

<success_criteria>
- Botão Duplicar ambiente clona a árvore inteira com novos UUIDs e nome "(cópia)"; local dos sistemas preservado
- Advisory da Phase 17 funciona idêntico, agora via predicados compartilhados de orcamento.ts
</success_criteria>

<output>
After completion, create `.planning/phases/18-ux-transversal/18-03-SUMMARY.md`
</output>
