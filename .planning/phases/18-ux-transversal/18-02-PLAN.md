---
phase: 18-ux-transversal
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/hooks/useProdutoSearch.ts
  - src/components/ProdutoAutocomplete.tsx
  - src/components/AmbienteCard.tsx
autonomous: false
requirements: [UX-01, UX-03, RES-04]
user_setup: []

must_haves:
  truths:
    - "Buscar na aba Luminárias um código que é perfil/fita/driver mostra mensagem de redirecionamento em vez de 'Nenhum produto encontrado'"
    - "Botão 'Ir para Sistemas de Iluminação' troca a aba do AmbienteCard de luminarias para sistemas"
    - "Cada aba (Luminárias / Sistemas) tem microcopy inline em text-xs text-muted-foreground explicando o que entra ali"
    - "Botão Duplicar sistema clona o sistema no mesmo ambiente logo abaixo, com novos UUIDs e local sufixado '(cópia)'"
  artifacts:
    - path: "src/hooks/useProdutoSearch.ts"
      provides: "Retorno redirectTipo via fallback query"
      contains: "redirectTipo"
    - path: "src/components/ProdutoAutocomplete.tsx"
      provides: "Empty-state de redirect + prop onRedirectToSistemas"
      contains: "onRedirectToSistemas"
    - path: "src/components/AmbienteCard.tsx"
      provides: "Tabs controlado, microcopy, botão duplicar sistema, wiring do redirect"
      contains: "clonarSistema"
  key_links:
    - from: "ProdutoAutocomplete redirect button"
      to: "AmbienteCard setActiveTab('sistemas')"
      via: "onRedirectToSistemas prop"
      pattern: "onRedirectToSistemas"
    - from: "AmbienteCard duplicarSistema"
      to: "clonarSistema (orcamento.ts)"
      via: "import + onChange splice si+1"
      pattern: "clonarSistema"
---

<objective>
Redirecionamento na busca de Luminária (UX-01), microcopy inline nas abas (UX-03) e botão Duplicar sistema (RES-04) — tudo dentro de useProdutoSearch + ProdutoAutocomplete + AmbienteCard.

Purpose: Tornar a busca difícil de errar (redireciona perfil/fita/driver digitado em Luminárias), explicar o que entra em cada aba, e permitir reuso de sistema sem remontagem.
Output: hook com fallback query, autocomplete com empty-state de redirect, AmbienteCard com Tabs controlado + 2 microcopies + botão duplicar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-ux-transversal/18-CONTEXT.md
@.planning/phases/18-ux-transversal/18-RESEARCH.md
@.planning/phases/18-ux-transversal/18-UI-SPEC.md

<interfaces>
<!-- Disponível de src/types/orcamento.ts após o plan 18-01 (Wave 1). Importe direto. -->
```typescript
export function clonarSistema(sis: SistemaIluminacao): SistemaIluminacao; // novo UUID em tudo + local " (cópia)"
```

<!-- Estado atual de useProdutoSearch.ts (retorna { results, loading }); CHECK válido de tipo_produto:
     'fita','driver','perfil','spot','lampada','acessorio','conector','suporte' (NÃO existe 'luminaria').
     O filtro luminaria usa .or('tipo_produto.is.null,tipo_produto.in.(spot,lampada,acessorio,conector,suporte)'). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fallback query em useProdutoSearch retornando redirectTipo</name>
  <read_first>
    - src/hooks/useProdutoSearch.ts (arquivo inteiro — useEffect com debounce linhas 11-53, retorno linha 55)
    - .planning/phases/18-ux-transversal/18-RESEARCH.md (Pattern 1 + Pitfalls 3, 6, 7 — gating e CHECK values)
  </read_first>
  <action>
Adicionar estado `redirectTipo: string | null` e uma segunda query de fallback DENTRO do mesmo useEffect/debounce, disparada APENAS quando `filtro === 'luminaria'`, a query principal retornou 0 resultados e `query.trim().length >= 2`. A fallback busca SEM filtro de luminária, restrita a `tipo_produto IN ('perfil','fita','driver')`, `.limit(1)`, e seta `redirectTipo` com o `tipo_produto` encontrado (ou null).

Gating obrigatório (Pitfalls 3 e 6): resetar `redirectTipo` para `null` no início de cada execução do efeito ANTES das queries; só rodar a fallback após a principal resolver com 0 resultados. NÃO filtrar por `'luminaria'` na fallback (Pitfall 7 — não é valor CHECK válido).

Implementação dentro do `try`, após `setResults(data || [])`:
```typescript
// UX-01: fallback de detecção de tipo real quando luminária não acha nada
let redirect: string | null = null;
if (filtro === 'luminaria' && (data?.length ?? 0) === 0 && query.trim().length >= 2) {
  const { data: fb } = await supabase
    .from("produtos")
    .select("codigo, tipo_produto")
    .or(`codigo.ilike.%${query}%,descricao.ilike.%${query}%`)
    .in("tipo_produto", ["perfil", "fita", "driver"])
    .order("codigo")
    .limit(1);
  redirect = fb?.[0]?.tipo_produto ?? null;
}
setRedirectTipo(redirect);
```
No início do efeito (antes do try), adicionar `setRedirectTipo(null);`. No `catch`, adicionar `setRedirectTipo(null);`. Adicionar `const [redirectTipo, setRedirectTipo] = useState<string | null>(null);` junto aos demais states. Retornar `{ results, loading, redirectTipo }`.
  </action>
  <verify>
    <automated>cd auraoramentos && npx tsc --noEmit 2>&1 | grep -i "useProdutoSearch" || echo "tsc OK useProdutoSearch"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "redirectTipo" src/hooks/useProdutoSearch.ts` → ao menos 3 matches (state, set no fallback, return)
    - `grep -n "return { results, loading, redirectTipo }" src/hooks/useProdutoSearch.ts` → 1 match
    - `grep -n "\.in(\"tipo_produto\", \[\"perfil\", \"fita\", \"driver\"\])" src/hooks/useProdutoSearch.ts` → 1 match (fallback NÃO usa 'luminaria')
    - `grep -c "setRedirectTipo(null)" src/hooks/useProdutoSearch.ts` ≥ 2 (reset no início + no catch)
    - fallback só dispara dentro do bloco `filtro === 'luminaria' && ... === 0 && query.trim().length >= 2`
  </acceptance_criteria>
  <done>useProdutoSearch retorna redirectTipo derivado de fallback query gated; build compila.</done>
</task>

<task type="auto">
  <name>Task 2: Empty-state de redirect em ProdutoAutocomplete</name>
  <read_first>
    - src/components/ProdutoAutocomplete.tsx (arquivo inteiro — props linhas 8-15, useProdutoSearch linha 20, empty-state linhas 51-53)
    - .planning/phases/18-ux-transversal/18-UI-SPEC.md (linhas 84-122 — composição do bloco de redirect + tabela tipo→display)
  </read_first>
  <action>
Adicionar prop opcional `onRedirectToSistemas?: () => void` à interface. Consumir `redirectTipo` do hook: `const { results, loading, redirectTipo } = useProdutoSearch(query, filtro, filtroVoltagem);`. Importar `ArrowRight` de lucide-react e `Button` de `@/components/ui/button`.

Mapa de display (pt-BR): `{ perfil: 'perfil', fita: 'fita LED', driver: 'driver' }`. Substituir o bloco empty-state atual (linhas 51-53) por um condicional:
```tsx
{!loading && results.length === 0 && redirectTipo && (
  <div className="px-3 py-3 space-y-2">
    <p className="text-sm text-blue-900">
      Este produto é um {({ perfil: 'perfil', fita: 'fita LED', driver: 'driver' } as Record<string,string>)[redirectTipo] ?? redirectTipo} — adicione em Sistemas de Iluminação
    </p>
    {onRedirectToSistemas && (
      <Button variant="outline" size="sm" className="gap-2 w-full text-xs"
        onClick={() => { onRedirectToSistemas(); setOpen(false); }}>
        <ArrowRight className="h-3.5 w-3.5" /> Ir para Sistemas de Iluminação
      </Button>
    )}
  </div>
)}
{!loading && results.length === 0 && !redirectTipo && (
  <p className="py-4 text-center text-sm text-muted-foreground">Nenhum produto encontrado</p>
)}
```
Manter o container/dropdown existente intacto.
  </action>
  <verify>
    <automated>cd auraoramentos && npx tsc --noEmit 2>&1 | grep -i "ProdutoAutocomplete" || echo "tsc OK ProdutoAutocomplete"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "onRedirectToSistemas" src/components/ProdutoAutocomplete.tsx` → ao menos 3 matches (prop type, destructure, onClick)
    - copy exata presente: `grep -c "Este produto é um" src/components/ProdutoAutocomplete.tsx` ≥ 1
    - copy exata presente: `grep -c "Ir para Sistemas de Iluminação" src/components/ProdutoAutocomplete.tsx` ≥ 1
    - empty-state "Nenhum produto encontrado" preservado quando `!redirectTipo`: `grep -c "Nenhum produto encontrado" src/components/ProdutoAutocomplete.tsx` ≥ 1
    - botão usa `variant="outline"` e ícone `ArrowRight` (nunca accent/primary)
  </acceptance_criteria>
  <done>ProdutoAutocomplete renderiza bloco azul de redirect com botão quando redirectTipo detectado; empty-state padrão preservado caso contrário.</done>
</task>

<task type="auto">
  <name>Task 3: AmbienteCard — Tabs controlado, microcopy, duplicar sistema, wiring redirect</name>
  <read_first>
    - src/components/AmbienteCard.tsx (linha 16 import de orcamento; linhas 42-47 estado/uid; linha 75-78 updateSistema; linha 345 Tabs; linhas 352, 366, 398 — TabsContent e ProdutoAutocomplete luminária; linhas 408-431 header do sistema)
    - .planning/phases/18-ux-transversal/18-UI-SPEC.md (linhas 122-198 — composição microcopy + botão duplicar sistema; FLAG sr-only)
    - src/types/orcamento.ts (clonarSistema — disponível após 18-01)
  </read_first>
  <action>
**(a) Import:** adicionar `clonarSistema` ao import existente de `@/types/orcamento` (linha 16). Adicionar `Copy` ao import de lucide-react (linha 8).

**(b) Tabs controlado (UX-01 wiring):** adicionar state `const [activeTab, setActiveTab] = useState<'luminarias' | 'sistemas'>('luminarias');`. Trocar `<Tabs defaultValue="luminarias">` (linha 345) por `<Tabs value={activeTab} onValueChange={(v) => setActiveTab(v as 'luminarias' | 'sistemas')}>`. No `ProdutoAutocomplete` da luminária (linha 366) adicionar `onRedirectToSistemas={() => setActiveTab('sistemas')}`.

**(c) Microcopy (UX-03):** logo após `<TabsContent value="luminarias" ...>` (linha 352), antes do bloco Magneto, inserir:
```tsx
<p className="text-xs text-muted-foreground mb-3">Spots, pendentes, plafons, trilhos e luminárias individuais.</p>
```
Logo após `<TabsContent value="sistemas" ...>` (linha 398), antes do `.map`, inserir:
```tsx
<p className="text-xs text-muted-foreground mb-3">Fitas LED, perfis, drivers e componentes que formam um sistema.</p>
```

**(d) Duplicar sistema (RES-04):** adicionar handler local junto aos demais de sistema (perto da linha 75):
```tsx
const duplicarSistema = (index: number) => {
  const clone = clonarSistema(ambiente.sistemas[index]);
  const arr = [...ambiente.sistemas];
  arr.splice(index + 1, 0, clone);
  onChange({ ...ambiente, sistemas: arr });
};
```
No grupo de ações do header do sistema (`<div className="flex items-center gap-2">` linha 425), ANTES do botão de remover (linha 427), inserir:
```tsx
<Button size="icon" variant="ghost" className="h-7 w-7 text-muted-foreground hover:text-foreground" title="Duplicar sistema" onClick={() => duplicarSistema(si)}>
  <span className="sr-only">Duplicar sistema</span>
  <Copy className="h-3.5 w-3.5" />
</Button>
```
(FLAG UI-SPEC: o `<span className="sr-only">` é obrigatório no botão icon-only para acessibilidade.)
  </action>
  <verify>
    <automated>cd auraoramentos && npx tsc --noEmit 2>&1 | grep -i "AmbienteCard" || echo "tsc OK AmbienteCard"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "value={activeTab} onValueChange" src/components/AmbienteCard.tsx` → 1 match (Tabs controlado)
    - `grep -c "defaultValue=\"luminarias\"" src/components/AmbienteCard.tsx` → 0 (uncontrolled removido)
    - `grep -n "onRedirectToSistemas={() => setActiveTab('sistemas')}" src/components/AmbienteCard.tsx` → 1 match
    - microcopy exata: `grep -c "Spots, pendentes, plafons, trilhos e luminárias individuais." src/components/AmbienteCard.tsx` ≥ 1
    - microcopy exata: `grep -c "Fitas LED, perfis, drivers e componentes que formam um sistema." src/components/AmbienteCard.tsx` ≥ 1
    - `grep -n "const duplicarSistema" src/components/AmbienteCard.tsx` → 1 match e usa `clonarSistema` + `splice(index + 1, 0, clone)`
    - botão duplicar tem `title="Duplicar sistema"` e `<span className="sr-only">Duplicar sistema</span>` (FLAG acessibilidade)
    - botão duplicar usa `variant="ghost"` e ícone `Copy`
  </acceptance_criteria>
  <done>AmbienteCard com Tabs controlado wired ao redirect, 2 microcopies inline, e botão Duplicar sistema (sr-only) funcional via clonarSistema.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificação humana — redirect, microcopy, duplicar sistema</name>
  <what-built>UX-01 redirect na busca de Luminária, UX-03 microcopy inline, RES-04 duplicar sistema — todos no wizard Step 2.</what-built>
  <action>Pausar para verificação humana no navegador conforme <how-to-verify>. Não prosseguir até receber o resume-signal.</action>
  <how-to-verify>
    1. `npm run dev` e abrir o wizard, ir ao Step 2, adicionar um ambiente.
    2. Na aba Luminárias, confirmar o microcopy "Spots, pendentes, plafons, trilhos e luminárias individuais." abaixo do tab.
    3. Digitar na busca de Luminária um código de PERFIL (ex.: LM1370 ou outro perfil conhecido). Esperado: bloco azul "Este produto é um perfil — adicione em Sistemas de Iluminação" + botão "Ir para Sistemas de Iluminação" (não "Nenhum produto encontrado").
    4. Clicar no botão → a aba deve trocar para "Sistemas de Iluminação" (confirmar o microcopy "Fitas LED, perfis, drivers e componentes que formam um sistema.").
    5. Na aba Sistemas, montar um sistema (fita + driver), preencher Local "Sanca". Clicar no ícone Duplicar (Copy) no header do sistema. Esperado: novo "Sistema 2" logo abaixo com Local "Sanca (cópia)".
    6. Editar o preço do clone e confirmar que o original NÃO muda (UUIDs distintos).
    7. Verificar console do navegador sem erros JS.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (read) | Fallback query SELECT em `produtos` (mesma sessão autenticada, mesma RLS da busca existente) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Information Disclosure | fallback query em produtos | accept | Subconjunto da busca de catálogo já existente; mesma RLS/sessão; lê só codigo+tipo_produto (sem PII). Sem nova superfície |
| T-18-03 | Injection | `.ilike.%${query}%` na fallback | accept | Mesmo padrão parametrizado pelo SDK já usado na busca atual (useProdutoSearch linha 38); SDK escapa o binding. ASVS V5: sem novo vetor |

Sem novos writes, sem nova RLS, sem PII. Threat model honesto/minimal conforme escopo client-side.
</threat_model>

<verification>
- `npx tsc --noEmit` compila
- `npm run lint` sem novos erros
- Checkpoint humano (redirect + microcopy + duplicar sistema) aprovado
</verification>

<success_criteria>
- Buscar perfil/fita/driver em Luminárias mostra redirect + botão que troca a aba
- Microcopy visível em ambas as abas
- Duplicar sistema gera clone com novos UUIDs e local "(cópia)" sem afetar o original
</success_criteria>

<output>
After completion, create `.planning/phases/18-ux-transversal/18-02-SUMMARY.md`
</output>
