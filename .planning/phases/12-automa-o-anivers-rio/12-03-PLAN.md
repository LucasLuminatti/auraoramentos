---
phase: 12-automa-o-anivers-rio
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - supabase/migrations/20260515000002_aniversario_cron_schedule.sql
autonomous: false
requirements: [AUTO-01, AUTO-02]
requirements_addressed: [AUTO-01, AUTO-02]
tags: [supabase, migration, pg_cron, pg_net, vault, automation]

must_haves:
  truths:
    - "Extensões pg_cron + pg_net habilitadas em prod (jkewlaezvrbuicmncqbj) via CREATE EXTENSION IF NOT EXISTS"
    - "Vault secret 'service_role_key' criado manualmente em prod (FORA do git) via SELECT vault.create_secret()"
    - "Cron job 'aniversario-diario' agendado pra rodar diariamente às 09:00 UTC (06:00 BR — D-01)"
    - "Cron chama edge function aniversario-clientes via net.http_post com Authorization Bearer lido do vault.decrypted_secrets"
    - "Migration aplicada em prod via supabase db push DEPOIS de Vault secret estar criado"
    - "Smoke pós-deploy: SELECT cron.job retorna 1 row, e simulação manual de execução do command SQL retorna request_id válido"
  artifacts:
    - path: "supabase/migrations/20260515000002_aniversario_cron_schedule.sql"
      provides: "DDL extensions + cron.schedule"
      contains: "CREATE EXTENSION IF NOT EXISTS pg_cron"
  key_links:
    - from: "cron.schedule 'aniversario-diario'"
      to: "net.http_post"
      via: "command SQL"
      pattern: "net\\.http_post"
    - from: "net.http_post"
      to: "edge function aniversario-clientes URL"
      via: "url parameter"
      pattern: "functions/v1/aniversario-clientes"
    - from: "Authorization header"
      to: "vault.decrypted_secrets"
      via: "service_role_key lookup"
      pattern: "vault\\.decrypted_secrets"
---

<objective>
Última peça da Phase 12: agendar pg_cron diário 09:00 UTC (06:00 BR) chamando a edge function via pg_net HTTP POST, com Authorization Bearer lido do Supabase Vault. Migration depende: (a) edge function deployed em prod (Wave 2), (b) Vault secret criado MANUALMENTE em prod antes da migration rodar (não pode estar em git).

Purpose: Phase 12 / AUTO-01/AUTO-02. Sem cron, edge fn só roda quando Lenny curla manual. Cron é o "automático" do requirement. Vault é a única forma documentada/segura de evitar hardcode de service_role_key em git.

Output: 1 migration aplicada em prod + Vault secret existente + cron job ativo + smoke pós-deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-automa-o-anivers-rio/12-CONTEXT.md
@.planning/phases/12-automa-o-anivers-rio/12-RESEARCH.md
@.planning/phases/12-automa-o-anivers-rio/12-01-SUMMARY.md
@.planning/phases/12-automa-o-anivers-rio/12-02-SUMMARY.md

<verified_facts>
Estado das extensões em prod (jkewlaezvrbuicmncqbj) — confirmado via MCP supabase list_extensions:
- supabase_vault: INSTALADO (0.3.1, schema vault) — Wave 3 usa pra ler service_role_key
- pg_cron: DISPONÍVEL 1.6.4, NÃO instalado — esta migration habilita
- pg_net: DISPONÍVEL 0.20.0, NÃO instalado — esta migration habilita

Edge function URL (Wave 2 deployed):
https://jkewlaezvrbuicmncqbj.supabase.co/functions/v1/aniversario-clientes
</verified_facts>

<order_of_operations>
ORDEM CRÍTICA — Wave 3 NÃO pode aplicar migration antes de:
1. Wave 1 aplicada (tabela log + stored functions) — DONE
2. Wave 2 deployed (edge function ativa) — DONE
3. **MANUAL:** Vault secret criado em prod (Task 1 abaixo é checkpoint humano)
4. SOMENTE ENTÃO: aplicar migration #2 (Task 3)

Se aplicar migration #2 antes do Vault secret existir, o primeiro `cron.schedule()` salva o command, mas o primeiro run às 09:00 UTC vai falhar porque a subquery `SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name='service_role_key'` retorna NULL → Authorization header fica `Bearer ` (vazio) → edge fn responde 401.
</order_of_operations>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Lenny cria Vault secret 'service_role_key' em prod (FORA do git)</name>
  <what-built>
Antes de aplicar migration #2, o Vault precisa ter o secret `service_role_key` com o valor real da service role key do projeto. Este passo é manual (não pode estar em git por óbvias razões de segurança) e exige que Lenny rode SQL diretamente no Supabase Dashboard.

Claude NÃO pode automatizar isso — service_role_key não pode aparecer em nenhum arquivo do repo (PLAN, log, summary, migration, .env tracked). Lenny pega do Dashboard e cola no SQL Editor.
  </what-built>
  <how-to-verify>
1. **Pegar SERVICE_ROLE_KEY:**
   - Supabase Dashboard → Project `jkewlaezvrbuicmncqbj` → Settings → API
   - Copiar valor de "service_role" secret (formato JWT longo, começa com `eyJ...`)
   - **NÃO cole em nenhum arquivo, screenshot, mensagem do chat — copie direto no SQL Editor.**

2. **Verificar se Vault está acessível** (deveria estar — confirmado via list_extensions):
   ```sql
   SELECT name, default_version, installed_version
   FROM pg_available_extensions
   WHERE name = 'supabase_vault';
   ```
   Esperado: 1 row, `installed_version='0.3.1'` (não-NULL).

3. **Criar secret no Vault** (Supabase Dashboard → SQL Editor → New query):
   ```sql
   -- COLE A SERVICE_ROLE_KEY NO PRIMEIRO PARÂMETRO, ROTE UMA VEZ, NÃO SALVE
   SELECT vault.create_secret(
     '<COLE_AQUI_O_SERVICE_ROLE_KEY_DO_DASHBOARD>',
     'service_role_key',
     'Phase 12 cron auth — pg_cron lê via vault.decrypted_secrets pra montar Authorization header da edge fn aniversario-clientes'
   );
   ```

   Esperado: retorna 1 UUID (id do secret).

4. **Confirmar secret criado:**
   ```sql
   SELECT name, description, created_at
   FROM vault.secrets
   WHERE name = 'service_role_key';
   ```
   Esperado: 1 row, name='service_role_key'.

5. **Smoke do leitor — confirma que pg_cron vai conseguir ler depois:**
   ```sql
   SELECT length(decrypted_secret) AS key_len
   FROM vault.decrypted_secrets
   WHERE name = 'service_role_key';
   ```
   Esperado: 1 row, key_len entre 200 e 500 (JWT longo). **NÃO faça SELECT do `decrypted_secret` direto sem `length()` — o valor não deve aparecer no histórico do SQL Editor.**

6. **Se secret já existir** (re-execução, troca de key) — primeiro deletar:
   ```sql
   DELETE FROM vault.secrets WHERE name = 'service_role_key';
   -- depois rodar o create_secret de novo
   ```

7. **Documentar SEM EXPOR a key:**
   Em `.planning/phases/12-automa-o-anivers-rio/PUSH-LOG.md`, registrar apenas:
   ```
   2026-05-XX HH:MM — Vault secret 'service_role_key' criado em prod via Dashboard SQL Editor (key_len=NNN). Service role key permanece exclusivamente no Vault + Dashboard Settings.
   ```
  </how-to-verify>
  <resume-signal>Digite "vault-ok" depois de confirmar `SELECT name FROM vault.secrets WHERE name='service_role_key'` retorna 1 row, ou descreva problemas.</resume-signal>
  <acceptance_criteria>
    - `SELECT count(*) FROM vault.secrets WHERE name='service_role_key'` retorna 1
    - `SELECT length(decrypted_secret) FROM vault.decrypted_secrets WHERE name='service_role_key'` retorna valor entre 200 e 500
    - PUSH-LOG.md registra criação SEM expor a key
    - Nenhum arquivo do repo contém a service_role_key (verificar: `grep -r "eyJ" .planning/ supabase/migrations/` não deve achar nada além de exemplos)
  </acceptance_criteria>
  <done>Vault secret 'service_role_key' existe em prod, leitor via vault.decrypted_secrets funciona, key NÃO está em nenhum arquivo do repo, PUSH-LOG registra criação.</done>
</task>

<task type="auto">
  <name>Task 2: Criar migration 20260515000002_aniversario_cron_schedule.sql</name>
  <files>supabase/migrations/20260515000002_aniversario_cron_schedule.sql</files>
  <read_first>
    - .planning/phases/12-automa-o-anivers-rio/12-RESEARCH.md (Code Examples — Migration #2 + Finding 2 + Finding 6)
    - .planning/phases/12-automa-o-anivers-rio/12-CONTEXT.md (D-01 horário, D-03 cron via SQL migration)
    - .planning/phases/12-automa-o-anivers-rio/12-02-SUMMARY.md (URL exata da edge function deployed)
  </read_first>
  <action>
Criar `supabase/migrations/20260515000002_aniversario_cron_schedule.sql` com o conteúdo SQL abaixo. Pattern replicado do RESEARCH Finding 6 (Migration #2), com cleanup defensivo + comentário explícito sobre Vault precisar existir antes:

```sql
-- Phase 12 / Plan 03 — Cron diário 09:00 UTC chamando aniversario-clientes (AUTO-01/AUTO-02)
-- Refs: CONTEXT D-01 (06:00 BR = 09:00 UTC), D-03 (cron via SQL migration)
--
-- PRÉ-REQUISITOS (NÃO podem estar em git — manuais):
--   1. Edge function `aniversario-clientes` JÁ DEPLOYED em prod (Wave 2, 12-02-PLAN concluído).
--      Caso contrário, primeiro run às 09:00 UTC dará 404 silencioso (pg_net não falha
--      em HTTP 4xx/5xx, só em TCP/DNS).
--   2. Secret `service_role_key` JÁ EXISTE no Vault (Wave 3 Task 1 / Lenny manual).
--      Confirmar com: SELECT count(*) FROM vault.secrets WHERE name='service_role_key';
--      Se faltar, o primeiro cron run monta Authorization 'Bearer ' (vazio) → 401.

BEGIN;

-- Extensões managed pela Supabase (no-op se já habilitadas)
CREATE EXTENSION IF NOT EXISTS pg_cron;
CREATE EXTENSION IF NOT EXISTS pg_net;

-- Cleanup defensivo (caso já exista do unschedule/reschedule)
DO $$
BEGIN
  IF EXISTS (SELECT 1 FROM cron.job WHERE jobname = 'aniversario-diario') THEN
    PERFORM cron.unschedule('aniversario-diario');
  END IF;
END
$$;

-- Schedule diário 09:00 UTC = 06:00 BR (CONTEXT D-01)
SELECT cron.schedule(
  'aniversario-diario',
  '0 9 * * *',
  $$
  SELECT net.http_post(
    url := 'https://jkewlaezvrbuicmncqbj.supabase.co/functions/v1/aniversario-clientes',
    headers := jsonb_build_object(
      'Content-Type', 'application/json',
      'Authorization', 'Bearer ' || (
        SELECT decrypted_secret FROM vault.decrypted_secrets
        WHERE name = 'service_role_key'
      )
    ),
    body := '{}'::jsonb,
    timeout_milliseconds := 60000  -- 60s cobre batch grande sem cortar Resend
  ) AS request_id;
  $$
);

COMMENT ON EXTENSION pg_cron IS
  'Phase 12: agendamento de jobs no Postgres (managed Supabase). Job principal: aniversario-diario @ 09:00 UTC.';

COMMENT ON EXTENSION pg_net IS
  'Phase 12: HTTP requests assíncronos a partir do SQL (managed Supabase). Usado pelo cron pra chamar edge function aniversario-clientes.';

COMMIT;
```

Pontos críticos:
- `CREATE EXTENSION IF NOT EXISTS` cobre estado atual (não-instalado, RESEARCH confirmou) e re-aplicação
- `DO $$ BEGIN ... END $$` em vez de `SELECT cron.unschedule(...) WHERE EXISTS (...)` — `cron.unschedule` retorna void/boolean, não funciona em WHERE; bloco DO permite condicional
- Subquery `SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name='service_role_key'` é executada **a cada cron run** (não substituída em tempo de schedule) — rotação do Vault propaga automaticamente
- `timeout_milliseconds := 60000` — 60s folgado pro volume atual (10 clientes max)
- `body := '{}'::jsonb` — edge fn não usa body, mas Content-Type JSON exige body válido
- URL hardcoded com project ref `jkewlaezvrbuicmncqbj` — projeto único do AURA, não há ambiente staging
- COMMENT em ambas extensions pra rastrear Phase 12 origin
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260515000002_aniversario_cron_schedule.sql','utf8');const checks=[/CREATE EXTENSION IF NOT EXISTS pg_cron/,/CREATE EXTENSION IF NOT EXISTS pg_net/,/cron\.schedule\(\s*'aniversario-diario'/,/'0 9 \* \* \*'/,/net\.http_post/,/functions\/v1\/aniversario-clientes/,/vault\.decrypted_secrets/,/name = 'service_role_key'/,/timeout_milliseconds := 60000/,/cron\.unschedule\('aniversario-diario'\)/];for(const r of checks){if(!r.test(s)){console.error('FAIL:',r);process.exit(1)}};if(/eyJ[A-Za-z0-9_-]{10,}/.test(s)){console.error('FAIL: parece conter JWT/service_role_key hardcoded');process.exit(1)};console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260515000002_aniversario_cron_schedule.sql` existe
    - `grep -q "CREATE EXTENSION IF NOT EXISTS pg_cron" supabase/migrations/20260515000002_aniversario_cron_schedule.sql`
    - `grep -q "CREATE EXTENSION IF NOT EXISTS pg_net" supabase/migrations/20260515000002_aniversario_cron_schedule.sql`
    - `grep -q "cron\.schedule('aniversario-diario'" supabase/migrations/20260515000002_aniversario_cron_schedule.sql`
    - `grep -q "'0 9 \* \* \*'" supabase/migrations/20260515000002_aniversario_cron_schedule.sql`
    - `grep -q "net\.http_post" supabase/migrations/20260515000002_aniversario_cron_schedule.sql`
    - `grep -q "functions/v1/aniversario-clientes" supabase/migrations/20260515000002_aniversario_cron_schedule.sql`
    - `grep -q "vault\.decrypted_secrets" supabase/migrations/20260515000002_aniversario_cron_schedule.sql`
    - `grep -q "cron\.unschedule" supabase/migrations/20260515000002_aniversario_cron_schedule.sql` (cleanup defensivo)
    - **NEGATIVE check:** `! grep -E "eyJ[A-Za-z0-9_-]{10,}" supabase/migrations/20260515000002_aniversario_cron_schedule.sql` (sem JWT hardcoded)
  </acceptance_criteria>
  <done>Arquivo de migration existe com extensions IF NOT EXISTS, cleanup defensivo, cron.schedule com URL correta, Vault subquery no Authorization header, sem hardcode de service_role_key.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] supabase db push migration #2 + smoke pós-deploy</name>
  <what-built>
Aplica migration #2 em prod (depende de Vault Task 1 done + edge fn Wave 2 deployed), confirma cron job ativo, e roda manualmente o command SQL do cron pra simular execução (sem esperar 09:00 UTC).
  </what-built>
  <how-to-verify>
1. **PRÉ-CHECK obrigatório (não pular):**
   ```sql
   -- Confirmar pré-requisitos antes de aplicar migration
   SELECT
     (SELECT count(*) FROM vault.secrets WHERE name='service_role_key') AS vault_secret,
     (SELECT count(*) FROM pg_extension WHERE extname='supabase_vault') AS vault_ext;
   -- Esperado: vault_secret=1, vault_ext=1
   ```
   Se `vault_secret=0` → Task 1 não foi feita, ABORT.

2. **Aplicar migration:**

   **Opção A — CLI:**
   ```bash
   cd C:/Users/lenny/Desktop/Luminatti/automa_aura/auraoramentos
   supabase db push
   ```

   **Opção B — MCP `apply_migration`:**
   - `name`: `20260515000002_aniversario_cron_schedule`
   - `query`: conteúdo completo do arquivo SQL

3. **Smoke pós-deploy via SQL Editor:**

   ```sql
   -- 3a. Confirmar extensions habilitadas
   SELECT extname, extversion FROM pg_extension WHERE extname IN ('pg_cron', 'pg_net');
   -- Esperado: 2 rows

   -- 3b. Confirmar cron job agendado
   SELECT jobid, jobname, schedule, active, command
   FROM cron.job
   WHERE jobname = 'aniversario-diario';
   -- Esperado: 1 row, schedule='0 9 * * *', active=true, command contém net.http_post + functions/v1/aniversario-clientes

   -- 3c. Smoke do command SQL — executar manualmente o mesmo SQL que o cron vai rodar
   -- (simula 09:00 UTC sem esperar)
   SELECT net.http_post(
     url := 'https://jkewlaezvrbuicmncqbj.supabase.co/functions/v1/aniversario-clientes',
     headers := jsonb_build_object(
       'Content-Type', 'application/json',
       'Authorization', 'Bearer ' || (
         SELECT decrypted_secret FROM vault.decrypted_secrets
         WHERE name = 'service_role_key'
       )
     ),
     body := '{}'::jsonb,
     timeout_milliseconds := 60000
   ) AS request_id;
   -- Esperado: retorna 1 UUID (request_id). Resposta assíncrona — ver step 3d.

   -- 3d. Confirmar resposta HTTP do edge function (pode levar alguns segundos pra popular)
   SELECT id, status_code, content_type, content, error_msg, created
   FROM net._http_response
   ORDER BY created DESC
   LIMIT 3;
   -- Esperado: status_code=200, content é JSON {processed,sent,failed,skipped,ano_referencia}
   -- error_msg deve ser NULL
   -- Se status_code=401 → Vault secret problema; revisar Task 1
   -- Se status_code=404 → edge fn não deployed; revisar Wave 2
   ```

4. **Registrar resultados em `.planning/phases/12-automa-o-anivers-rio/PUSH-LOG.md`:**
   - Migration aplicada: 20260515000002_aniversario_cron_schedule
   - Method: CLI ou MCP
   - Timestamp prod
   - jobid + active=true
   - Smoke pós-deploy: status_code do net._http_response, content do edge fn
   - Próxima execução real prevista: próxima ocorrência de 09:00 UTC

5. **Documentar comando de inspeção** pro Lenny (apenas referência, sem rodar agora):
   ```sql
   -- Últimas execuções do cron (quando 09:00 UTC passar)
   SELECT runid, status, return_message, start_time, end_time
   FROM cron.job_run_details
   WHERE jobid = (SELECT jobid FROM cron.job WHERE jobname='aniversario-diario')
   ORDER BY start_time DESC LIMIT 5;

   -- Pausar/reativar cron sem deletar
   SELECT cron.alter_job(
     job_id := (SELECT jobid FROM cron.job WHERE jobname='aniversario-diario'),
     active := false  -- ou true pra reativar
   );

   -- Deletar cron permanentemente
   SELECT cron.unschedule('aniversario-diario');
   ```

6. **Cleanup pós-smoke:** o smoke do step 3c criou logs em `net._http_response` e (possivelmente) em `aniversario_envios` se houver cliente real fazendo aniversário hoje+5d. Sem cliente teste cadastrado nesta task (Wave 2 fez cleanup), edge fn retorna `processed=0, sent=0` — log fica vazio. Nada a limpar.

7. **Se smoke falhar (status_code != 200):**
   - 401 → Vault secret vazio/errado. Reexecutar Task 1 com a service_role_key real.
   - 404 → Edge fn não deployed. Reexecutar Wave 2 Task 2.
   - 500 → Logs da edge fn (Supabase Dashboard > Functions > aniversario-clientes > Logs). Pode ser stored fn ausente (verificar Wave 1).
   - Timeout / DNS error em `net._http_response.error_msg` → pg_net não instalado corretamente. Reexecutar migration.
  </how-to-verify>
  <resume-signal>Digite "cron-live" quando migration aplicada + cron.job mostra active=true + smoke do command SQL retornou 200 em net._http_response. Descreva problemas se houver.</resume-signal>
  <acceptance_criteria>
    - `SELECT count(*) FROM pg_extension WHERE extname IN ('pg_cron','pg_net')` retorna 2
    - `SELECT count(*) FROM cron.job WHERE jobname='aniversario-diario' AND active=true` retorna 1
    - `SELECT schedule FROM cron.job WHERE jobname='aniversario-diario'` retorna `'0 9 * * *'`
    - `SELECT command FROM cron.job WHERE jobname='aniversario-diario'` contém substring `functions/v1/aniversario-clientes`
    - Smoke manual: `SELECT status_code FROM net._http_response ORDER BY created DESC LIMIT 1` retorna `200`
    - PUSH-LOG.md atualizado com timestamps + jobid + smoke result
  </acceptance_criteria>
  <done>Migration aplicada, extensões habilitadas, cron job ativo, smoke pós-deploy confirma chain completa (cron → pg_net → edge fn → Resend → log). Phase 12 entregue end-to-end. Cron real roda na próxima 09:00 UTC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vault → pg_cron command | service_role_key armazenada cifrada em vault.secrets; pg_cron lê via vault.decrypted_secrets em runtime |
| pg_cron → edge fn (HTTPS) | net.http_post envia Authorization Bearer service_role; Supabase Functions valida via JWT signature |
| Migration SQL → git | Migration NÃO contém service_role_key (Vault subquery lê em runtime); secret só vive no Vault + Dashboard Settings |
| Authenticated user → cron.job | Tabela cron.job é restrita ao role postgres/supabase_admin por default (não authenticated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | I (Info Disclosure) | service_role_key vaza via git history se hardcoded na migration | mitigate | Migration usa `(SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name='service_role_key')` — nenhum literal de JWT no SQL. Negative grep no acceptance_criteria garante (regex eyJ...). |
| T-12-03-02 | I | service_role_key vaza via PUSH-LOG.md ou SUMMARY se Claude/Lenny colar o valor | mitigate | Task 1 explicita "NÃO cole em arquivo"; PUSH-LOG só registra `length` ou `created_at`, nunca o valor. |
| T-12-03-03 | E (EOP) | Colab authenticated lendo cron.job pra ver o command (com Vault subquery) | accept | cron.job é gerenciado pelo Postgres role superuser/supabase_admin. Authenticated NÃO tem SELECT em cron.job por default. Mesmo se tivesse, veria a subquery (não o valor). |
| T-12-03-04 | T (Tampering) | Cron job sendo modificado/desabilitado por third-party | mitigate | cron.alter_job e cron.unschedule são SECURITY DEFINER + restritos ao role do dono do job. Apenas service_role/postgres pode mexer. |
| T-12-03-05 | D (DoS) | Cron rodando 2x por minuto por bug de schedule | mitigate | Schedule '0 9 * * *' é literal e validado pelo cron parser. UNIQUE constraint na aniversario_envios cobre dupla execução do mesmo run. |
| T-12-03-06 | R (Repudiation) | Disputa sobre se cron executou ou não em dia X | mitigate | `cron.job_run_details` registra todo run com timestamp + status. `net._http_response` registra resposta HTTP real (status_code + content). Documentação no how-to-verify step 5. |
| T-12-03-07 | I | pg_net retorna "succeeded" mesmo com HTTP 4xx/5xx do edge fn (Pitfall 5 do RESEARCH) | mitigate | Smoke pós-deploy step 3d valida `net._http_response.status_code = 200` explicitamente. Lenny pode adicionar alerta futuro (deferred — Phase 12 cobre detecção, não alerting). |
</threat_model>

<verification>
- Pré-check confirma Vault secret existe + supabase_vault habilitado
- Migration aplicada sem erro
- `SELECT * FROM pg_extension WHERE extname='pg_cron'` retorna 1 row
- `SELECT * FROM pg_extension WHERE extname='pg_net'` retorna 1 row
- `SELECT * FROM cron.job WHERE jobname='aniversario-diario'` retorna 1 row com active=true
- Smoke manual do command SQL retorna request_id UUID
- `net._http_response` mostra status_code=200 + content JSON do edge fn
- Migration NÃO contém literal de service_role_key (negative grep)
- PUSH-LOG.md atualizado SEM expor a key
</verification>

<success_criteria>
- Migration aplicada em prod, extensions habilitadas, cron ativo
- Smoke pós-deploy: chain completa cron → pg_net → edge fn → Resend → log validada manualmente
- Phase 12 entrega end-to-end: Wave 1 (schema) + Wave 2 (lógica) + Wave 3 (agendamento)
- Próxima 09:00 UTC executa automaticamente sem intervenção
- Phase 13 (Smoke & UAT Closure) pode rodar UAT cobrindo automação aniversário
</success_criteria>

<output>
After completion, create `.planning/phases/12-automa-o-anivers-rio/12-03-SUMMARY.md` com:
- Vault secret criado (timestamp, sem expor valor)
- Migration aplicada (timestamp, jobid retornado)
- Resultado smoke pós-deploy: status_code + content do net._http_response
- Estado final do cron (active, schedule, próxima execução prevista)
- Comandos de inspeção/pause/unschedule pro Lenny ter à mão
- Confirmação que Phase 12 está completa e Phase 13 pode começar
</output>
