---
phase: 12-automa-o-anivers-rio
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - supabase/functions/aniversario-clientes/index.ts
autonomous: false
requirements: [AUTO-01, AUTO-02]
requirements_addressed: [AUTO-01, AUTO-02]
tags: [supabase, edge-function, deno, resend, automation, email]

must_haves:
  truths:
    - "Edge function aniversario-clientes existe e responde POST em https://jkewlaezvrbuicmncqbj.supabase.co/functions/v1/aniversario-clientes"
    - "Edge function chama buscar_aniversariantes_d5 + buscar_admins_emails via service role (RPC)"
    - "Edge function envia email via Resend usando from address noreply@orcamentosaura.com.br (validado em prod via request-access)"
    - "Edge function insere log em aniversario_envios com INSERT ON CONFLICT (UNIQUE constraint cobre idempotência atomic)"
    - "Edge function trata cliente órfão (colab_email NULL) marcando status='skipped_no_owner' sem enviar"
    - "Edge function trata Resend failure marcando status='failed' + error_msg (sem retry conforme D-09)"
    - "Edge function deployed em prod via supabase functions deploy"
    - "Smoke manual curl com Authorization Bearer service_role retorna 200 + JSON {processed, sent, failed, skipped}"
  artifacts:
    - path: "supabase/functions/aniversario-clientes/index.ts"
      provides: "Edge function POST handler"
      contains: "Deno.serve"
      min_lines: 150
  key_links:
    - from: "edge fn"
      to: "buscar_aniversariantes_d5"
      via: "supabase.rpc()"
      pattern: "\\.rpc\\(['\"]buscar_aniversariantes_d5"
    - from: "edge fn"
      to: "buscar_admins_emails"
      via: "supabase.rpc()"
      pattern: "\\.rpc\\(['\"]buscar_admins_emails"
    - from: "edge fn"
      to: "aniversario_envios"
      via: "INSERT ON CONFLICT"
      pattern: "from\\(['\"]aniversario_envios['\"]\\)"
    - from: "edge fn"
      to: "Resend API"
      via: "resend.emails.send"
      pattern: "resend\\.emails\\.send"
---

<objective>
Criar edge function `aniversario-clientes` (Deno + Resend SDK + service role Supabase client) que: (1) busca clientes aniversariantes D-5 via stored fn, (2) busca emails de admins via stored fn, (3) loop por aniversariante fazendo INSERT log + envio Resend, (4) marca status correto (sent/failed/skipped_no_owner). Deploy em prod via `supabase functions deploy aniversario-clientes`.

Purpose: Phase 12 / AUTO-01/AUTO-02. Núcleo da automação. Replica padrão validado do `request-access/index.ts` (Resend + Deno + service role) trocando handler logic. Deploy em prod é BLOCKING porque Wave 3 (cron) chama essa URL — sem deploy, primeiro cron run retorna 404 silencioso.

Output: 1 edge function file + deploy em prod + smoke curl manual confirmado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-automa-o-anivers-rio/12-CONTEXT.md
@.planning/phases/12-automa-o-anivers-rio/12-RESEARCH.md
@.planning/phases/12-automa-o-anivers-rio/12-01-SUMMARY.md
@supabase/functions/request-access/index.ts

<interfaces>
<!-- Provided by Wave 1 (12-01) — confirmar via SUMMARY antes de implementar -->

Tabela aniversario_envios:
  id UUID PK DEFAULT gen_random_uuid()
  cliente_id UUID NOT NULL REFERENCES clientes(id) ON DELETE CASCADE
  ano_referencia INT NOT NULL
  destinatarios JSONB NOT NULL  -- {colab_email: string|null, admin_emails: string[]}
  sent_at TIMESTAMPTZ DEFAULT now()
  status TEXT CHECK IN ('sent', 'failed', 'skipped_no_owner')
  error_msg TEXT NULL
  UNIQUE(cliente_id, ano_referencia)  -- idempotência atomic, 23505 conflict_code

public.buscar_aniversariantes_d5() RETURNS TABLE:
  id UUID
  nome TEXT
  data_nascimento DATE
  contato TEXT
  user_id UUID
  colab_email TEXT  -- NULL se cliente órfão (auth.users deletado)

public.buscar_admins_emails() RETURNS TABLE:
  email TEXT  -- só não-nulos
</interfaces>

<canonical_pattern_to_replicate>
Arquivo: supabase/functions/request-access/index.ts

Pontos a replicar exato:
- Linha 1-2: imports `createClient` + `Resend`
- Linha 4-8: corsHeaders (mantém pra permitir curl/browser smoke)
- Linha 26-29: OPTIONS handler
- Linha 56-59: createClient com SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY (bypass RLS)
- Linha 127-129: `from: "Aura Orçamentos <noreply@orcamentosaura.com.br>"` (domínio validado)
- Linha 132-187: HTML template (header #1a1a2e, card, stats table) — adaptar pra body de aniversário (RESEARCH Finding 5)
- Linha 194-200: error handler com console.error + status 500

Diferenças vs request-access (Phase 12 = batch sem body):
- Phase 12 NÃO faz req.json() (cron passa body vazio)
- Phase 12 NÃO valida method estritamente (POST default, OPTIONS pra CORS preflight)
- Phase 12 retorna agregado {processed, sent, failed, skipped} em vez de {success}
</canonical_pattern_to_replicate>

<env_vars>
Já configuradas em prod (Function Secrets — não precisam ser adicionadas):
- SUPABASE_URL (auto-injected)
- SUPABASE_SERVICE_ROLE_KEY (auto-injected)
- RESEND_API_KEY (já existe pro request-access — mesma chave reusada)

NÃO usar:
- ADMIN_EMAIL (legacy do request-access; Phase 12 usa multi-admin dinâmico via stored fn)
</env_vars>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar edge function aniversario-clientes/index.ts</name>
  <files>supabase/functions/aniversario-clientes/index.ts</files>
  <read_first>
    - supabase/functions/request-access/index.ts (pattern canônico Deno + Resend + HTML template)
    - .planning/phases/12-automa-o-anivers-rio/12-RESEARCH.md (Code Examples — edge function esqueleto completo + Finding 5 HTML body)
    - .planning/phases/12-automa-o-anivers-rio/12-CONTEXT.md (D-04, D-05, D-06, D-09 — destinatário, admin multi, órfão, failure)
    - .planning/phases/12-automa-o-anivers-rio/12-01-SUMMARY.md (confirmar nome exato das stored functions criadas na Wave 1)
  </read_first>
  <action>
Criar `supabase/functions/aniversario-clientes/index.ts` com o conteúdo TypeScript abaixo. Pattern replicado do request-access/index.ts (linha 1-8, 56-59, 127-188) trocando o handler logic. HTML adaptado do RESEARCH Finding 5.

```typescript
// Phase 12 / Plan 02 — Edge function batch de aniversário
// Refs: CONTEXT D-01..D-09; replica pattern de supabase/functions/request-access/index.ts
//
// POST /functions/v1/aniversario-clientes
// Sem body. Auth via Authorization Bearer SERVICE_ROLE (Supabase Functions default).
// Cron chama uma vez por dia (Wave 3 / Migration 20260515000002_aniversario_cron_schedule.sql).
// Lenny pode disparar manual via curl pra smoke (Phase 13).

import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
import { Resend } from "npm:resend@2.0.0";

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers":
    "authorization, x-client-info, apikey, content-type, x-supabase-client-platform, x-supabase-client-platform-version, x-supabase-client-runtime, x-supabase-client-runtime-version",
};

interface ClienteAniversariante {
  id: string;
  nome: string;
  data_nascimento: string;
  contato: string | null;
  user_id: string;
  colab_email: string | null;
}

interface AdminEmailRow {
  email: string;
}

Deno.serve(async (req) => {
  if (req.method === "OPTIONS") {
    return new Response(null, { headers: corsHeaders });
  }

  try {
    const supabase = createClient(
      Deno.env.get("SUPABASE_URL")!,
      Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!
    );
    const resend = new Resend(Deno.env.get("RESEND_API_KEY"));

    // 1. Calcular ano_referencia a partir do target date (não today — evita bug de fim de ano, RESEARCH Pitfall 3)
    const target = new Date();
    target.setUTCDate(target.getUTCDate() + 5);
    const anoReferencia = target.getUTCFullYear();

    // 2. Buscar aniversariantes D-5 (stored fn cobre 29/02 + filtro de já notificados)
    const { data: aniversariantes, error: queryErr } = await supabase.rpc(
      "buscar_aniversariantes_d5"
    );

    if (queryErr) {
      console.error("buscar_aniversariantes_d5 failed:", queryErr);
      return new Response(
        JSON.stringify({ error: queryErr.message }),
        { status: 500, headers: { "Content-Type": "application/json", ...corsHeaders } }
      );
    }

    // 3. Buscar emails de admins UMA vez (stored fn, evita N+1)
    const { data: adminRows, error: adminErr } = await supabase.rpc(
      "buscar_admins_emails"
    );

    if (adminErr) {
      console.error("buscar_admins_emails failed:", adminErr);
      return new Response(
        JSON.stringify({ error: adminErr.message }),
        { status: 500, headers: { "Content-Type": "application/json", ...corsHeaders } }
      );
    }

    const adminEmails: string[] = ((adminRows ?? []) as AdminEmailRow[])
      .map((r) => r.email)
      .filter((e) => !!e && e.trim().length > 0);

    let processed = 0;
    let sent = 0;
    let failed = 0;
    let skipped = 0;

    console.log(
      `[aniversario-clientes] ano_referencia=${anoReferencia} aniversariantes=${
        (aniversariantes ?? []).length
      } admins=${adminEmails.length}`
    );

    // 4. Loop por aniversariante
    for (const cliente of (aniversariantes ?? []) as ClienteAniversariante[]) {
      processed++;

      // 4a. Cliente órfão (colab dono sem email) — registra skipped sem enviar (D-06)
      if (!cliente.colab_email || cliente.colab_email.trim() === "") {
        const { error: skipErr } = await supabase
          .from("aniversario_envios")
          .insert({
            cliente_id: cliente.id,
            ano_referencia: anoReferencia,
            destinatarios: { colab_email: null, admin_emails: adminEmails },
            status: "skipped_no_owner",
            error_msg: "colab dono sem email (auth.users.email nulo ou vazio)",
          });
        if (skipErr && skipErr.code !== "23505") {
          console.error(`[skip-insert ${cliente.id}] ${skipErr.message}`);
        }
        skipped++;
        continue;
      }

      const destinatarios = {
        colab_email: cliente.colab_email,
        admin_emails: adminEmails,
      };

      // 4b. INSERT log com status optimistic 'sent' — UNIQUE constraint garante idempotência (D-02)
      const { data: inserted, error: insErr } = await supabase
        .from("aniversario_envios")
        .insert({
          cliente_id: cliente.id,
          ano_referencia: anoReferencia,
          destinatarios,
          status: "sent",
        })
        .select("id")
        .single();

      if (insErr) {
        // 23505 = unique_violation → já notificado este ano (idempotência)
        if (insErr.code === "23505") {
          skipped++;
          continue;
        }
        console.error(`[log-insert ${cliente.id}] ${insErr.message}`);
        failed++;
        continue;
      }

      const logId = inserted!.id;

      // 4c. Enviar email via Resend
      const toList = [cliente.colab_email, ...adminEmails];
      const dataAniv = new Date(cliente.data_nascimento);
      const dataFormatada = `${String(dataAniv.getUTCDate()).padStart(2, "0")}/${String(
        dataAniv.getUTCMonth() + 1
      ).padStart(2, "0")}`;
      const idadeQueCompleta = anoReferencia - dataAniv.getUTCFullYear();

      try {
        const result = await resend.emails.send({
          from: "Aura Orçamentos <noreply@orcamentosaura.com.br>",
          to: toList,
          subject: `Aniversário em 5 dias: ${cliente.nome}`,
          html: buildHtml({
            nome: cliente.nome,
            dataFormatada,
            idade: idadeQueCompleta,
            contato: cliente.contato,
          }),
        });

        if (result.error) {
          throw new Error(result.error.message || JSON.stringify(result.error));
        }
        sent++;
      } catch (err) {
        const msg = err instanceof Error ? err.message : String(err);
        console.error(`[resend ${cliente.id}] ${msg}`);
        // D-09: sem retry. UNIQUE constraint impede tentativa nova no mesmo ano.
        await supabase
          .from("aniversario_envios")
          .update({ status: "failed", error_msg: msg })
          .eq("id", logId);
        failed++;
      }
    }

    console.log(
      `[aniversario-clientes done] processed=${processed} sent=${sent} failed=${failed} skipped=${skipped}`
    );

    return new Response(
      JSON.stringify({ processed, sent, failed, skipped, ano_referencia: anoReferencia }),
      { status: 200, headers: { "Content-Type": "application/json", ...corsHeaders } }
    );
  } catch (err) {
    console.error("aniversario-clientes fatal:", err);
    return new Response(
      JSON.stringify({ error: err instanceof Error ? err.message : "internal error" }),
      { status: 500, headers: { "Content-Type": "application/json", ...corsHeaders } }
    );
  }
});

function buildHtml(args: {
  nome: string;
  dataFormatada: string;
  idade: number;
  contato: string | null;
}): string {
  const { nome, dataFormatada, idade, contato } = args;
  const idadeLabel = idade > 0 ? ` (completa ${idade} anos)` : "";
  const contatoBlock = contato
    ? `
                      <tr>
                        <td style="padding:6px 0;">
                          <span style="color:#6b7280;font-size:13px;font-weight:500;">Contato</span><br>
                          <span style="color:#111827;font-size:15px;">${contato}</span>
                        </td>
                      </tr>`
    : "";

  return `
    <!DOCTYPE html>
    <html>
    <head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"></head>
    <body style="margin:0;padding:0;background:#f4f4f5;font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,sans-serif;">
      <table width="100%" cellpadding="0" cellspacing="0" style="background:#f4f4f5;padding:40px 0;">
        <tr><td align="center">
          <table width="560" cellpadding="0" cellspacing="0" style="background:#ffffff;border-radius:12px;overflow:hidden;box-shadow:0 2px 8px rgba(0,0,0,0.08);">
            <tr>
              <td style="background:#1a1a2e;padding:32px;text-align:center;">
                <h1 style="color:#ffffff;margin:0;font-size:22px;font-weight:600;">Aura · Criador de Orçamentos</h1>
              </td>
            </tr>
            <tr>
              <td style="padding:32px;">
                <h2 style="color:#1a1a2e;margin:0 0 16px;font-size:18px;">Aniversário próximo — 5 dias</h2>
                <p style="color:#6b7280;margin:0 0 24px;font-size:14px;">
                  <strong>${nome}</strong> faz aniversário em <strong>${dataFormatada}</strong>${idadeLabel}.
                  É uma boa hora pra mandar uma mensagem.
                </p>
                <table width="100%" cellpadding="0" cellspacing="0" style="background:#f9fafb;border-radius:8px;padding:20px;margin-bottom:28px;">
                  <tr>
                    <td style="padding:6px 0;">
                      <span style="color:#6b7280;font-size:13px;font-weight:500;">Cliente</span><br>
                      <span style="color:#111827;font-size:15px;font-weight:600;">${nome}</span>
                    </td>
                  </tr>
                  <tr>
                    <td style="padding:6px 0;">
                      <span style="color:#6b7280;font-size:13px;font-weight:500;">Data</span><br>
                      <span style="color:#111827;font-size:15px;">${dataFormatada}</span>
                    </td>
                  </tr>${contatoBlock}
                </table>
                <a href="https://orcamentosaura.com.br/admin?tab=clientes" style="display:block;text-align:center;background:#16a34a;color:#ffffff;text-decoration:none;padding:14px 20px;border-radius:8px;font-size:15px;font-weight:600;">
                  Abrir clientes no Aura
                </a>
                <p style="color:#9ca3af;font-size:12px;margin:24px 0 0;text-align:center;">Enviado automaticamente pelo Aura — 5 dias antes do aniversário do cliente.</p>
              </td>
            </tr>
          </table>
        </td></tr>
      </table>
    </body>
    </html>
  `;
}
```

Pontos críticos:
- `target.setUTCDate(target.getUTCDate() + 5)` + `anoReferencia = target.getUTCFullYear()` — corrige Pitfall 3 (ano_referencia derivado do target, não today)
- INSERT primeiro com status='sent' optimistic, depois UPDATE pra 'failed' se Resend errar — UNIQUE atomic cobre race
- `result.error` check (Resend SDK v2 retorna `{data, error}` mesmo em catch — duplo guard)
- console.log/error em vez de toast (edge fn não tem UI) — logs vão pro Supabase Dashboard > Functions > Logs
- HTML inline literal (sem MJML/react-email) replicando pattern do request-access
- CTA verde `#16a34a` (mesmo do request-access aprovar) → `https://orcamentosaura.com.br/admin?tab=clientes`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('supabase/functions/aniversario-clientes/index.ts','utf8');const checks=[/import \{ createClient \} from \"https:\/\/esm\.sh\/@supabase\/supabase-js@2\"/,/import \{ Resend \} from \"npm:resend@2\.0\.0\"/,/Deno\.serve/,/SUPABASE_SERVICE_ROLE_KEY/,/RESEND_API_KEY/,/\.rpc\(\s*[\"']buscar_aniversariantes_d5[\"']/,/\.rpc\(\s*[\"']buscar_admins_emails[\"']/,/from\([\"']aniversario_envios[\"']\)/,/resend\.emails\.send/,/noreply@orcamentosaura\.com\.br/,/Aniversário em 5 dias/,/skipped_no_owner/,/code === \"23505\"/,/status: \"failed\"/,/target\.setUTCDate\(target\.getUTCDate\(\) \+ 5\)/,/anoReferencia = target\.getUTCFullYear\(\)/,/https:\/\/orcamentosaura\.com\.br\/admin\?tab=clientes/];for(const r of checks){if(!r.test(s)){console.error('FAIL:',r);process.exit(1)}};console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/functions/aniversario-clientes/index.ts` existe
    - `grep -q "Deno\.serve" supabase/functions/aniversario-clientes/index.ts`
    - `grep -q "import { Resend } from \"npm:resend@2.0.0\"" supabase/functions/aniversario-clientes/index.ts`
    - `grep -q "buscar_aniversariantes_d5" supabase/functions/aniversario-clientes/index.ts`
    - `grep -q "buscar_admins_emails" supabase/functions/aniversario-clientes/index.ts`
    - `grep -q "from(\"aniversario_envios\")" supabase/functions/aniversario-clientes/index.ts`
    - `grep -q "resend\.emails\.send" supabase/functions/aniversario-clientes/index.ts`
    - `grep -q "noreply@orcamentosaura\.com\.br" supabase/functions/aniversario-clientes/index.ts`
    - `grep -q "skipped_no_owner" supabase/functions/aniversario-clientes/index.ts`
    - `grep -q "23505" supabase/functions/aniversario-clientes/index.ts` (unique violation handling)
    - `grep -q "target\.setUTCDate" supabase/functions/aniversario-clientes/index.ts` (Pitfall 3 fix)
    - `! grep -E "eyJ[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+" supabase/functions/aniversario-clientes/index.ts` (defense-in-depth: garantir que JWT não foi hardcoded ao copiar pattern de outro lugar)
  </acceptance_criteria>
  <done>Edge function file existe com pattern replicado de request-access, calls às 2 stored functions da Wave 1, INSERT ON CONFLICT pattern via try/catch 23505, HTML adaptado pra aniversário com CTA verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Deploy aniversario-clientes em prod + smoke curl manual</name>
  <what-built>
Edge function nova precisa ser deployed em prod (`jkewlaezvrbuicmncqbj`) ANTES da Wave 3 (cron) começar. Sem deploy, primeiro cron run dará 404 → log de envio nunca acontece, e Lenny vê tabela `aniversario_envios` vazia.

Smoke manual via curl insere cliente teste com `data_nascimento = today+5d`, dispara edge fn, e valida log + inbox.
  </what-built>
  <how-to-verify>
1. **Deploy edge function em prod:**
   ```bash
   cd C:/Users/lenny/Desktop/Luminatti/automa_aura/auraoramentos
   supabase functions deploy aniversario-clientes
   ```
   Esperar `Deployed Function aniversario-clientes`.

   **Alternativa via MCP:** `mcp__plugin_supabase_supabase__deploy_edge_function` com `name='aniversario-clientes'` e `files=[{name:'index.ts', content:<conteúdo>}]`.

2. **Pegar SERVICE_ROLE_KEY** (Lenny já tem em local — Supabase Dashboard > Settings > API > service_role secret).

3. **Smoke setup — inserir cliente teste com aniversário em D+5:**

   Via Supabase Dashboard SQL Editor (ou MCP `execute_sql`):
   ```sql
   -- Cria cliente fake com aniversário daqui a 5 dias, dono = primeiro admin
   INSERT INTO public.clientes (nome, data_nascimento, user_id, contato)
   SELECT
     'TESTE Aniversário Phase 12',
     (CURRENT_DATE + INTERVAL '5 days')::date,
     ur.user_id,
     '(11) 99999-0000'
   FROM public.user_roles ur
   WHERE ur.role = 'admin'::app_role
   LIMIT 1
   RETURNING id, nome, data_nascimento, user_id;
   ```
   Guarde o `id` retornado pra steps 6 e 8.

4. **Disparar edge function manual via curl:**
   ```bash
   curl -X POST https://jkewlaezvrbuicmncqbj.supabase.co/functions/v1/aniversario-clientes \
     -H "Authorization: Bearer <SERVICE_ROLE_KEY>" \
     -H "Content-Type: application/json"
   ```

   Esperado: HTTP 200 + JSON `{"processed":1,"sent":1,"failed":0,"skipped":0,"ano_referencia":<ano-corrente-ou-próximo>}`

5. **Confirmar inbox** (Lenny + Lucas devem receber email com subject "Aniversário em 5 dias: TESTE Aniversário Phase 12"):
   - From: Aura Orçamentos `<noreply@orcamentosaura.com.br>`
   - Body: nome do cliente, data DD/MM, idade, contato, CTA verde "Abrir clientes no Aura"
   - Render OK em Gmail (HTML não quebrado)

6. **Verificar log na tabela:**
   ```sql
   SELECT id, cliente_id, ano_referencia, status, destinatarios, sent_at, error_msg
   FROM public.aniversario_envios
   WHERE cliente_id = '<id-do-step-3>';
   ```
   Esperado: 1 row, `status='sent'`, `destinatarios` contém `colab_email` + `admin_emails[]` com ≥ 2 emails (Lenny + Lucas), `error_msg IS NULL`.

7. **Smoke idempotência — disparar curl 2x:**
   ```bash
   # Roda de novo, esperando idempotência
   curl -X POST https://jkewlaezvrbuicmncqbj.supabase.co/functions/v1/aniversario-clientes \
     -H "Authorization: Bearer <SERVICE_ROLE_KEY>"
   ```
   Esperado: HTTP 200 + `{"processed":1,"sent":0,"failed":0,"skipped":1, ...}` (skipped++ porque UNIQUE 23505).

8. **Cleanup:**
   ```sql
   DELETE FROM public.aniversario_envios WHERE cliente_id = '<id-do-step-3>';
   DELETE FROM public.clientes WHERE id = '<id-do-step-3>';
   ```

9. **Registrar resultado em `.planning/phases/12-automa-o-anivers-rio/SMOKE-RESULTS.md`** (Lenny ou Claude appenda):
   - Deploy timestamp
   - Resposta JSON dos 2 curls (sent=1 + skipped=1)
   - Confirmação inbox (screenshots opcional)
   - Confirmação SELECT log

10. **Se algo falhar:**
    - HTTP 401 → SERVICE_ROLE_KEY errado/expirado
    - HTTP 500 → ver Supabase Dashboard > Functions > aniversario-clientes > Logs (console.error)
    - `sent=0, failed=1` → Resend não entregou (rate limit? domínio?) — ver `error_msg` no log
    - Email não chega mas log diz sent → confirmar Resend Dashboard > Logs
  </how-to-verify>
  <resume-signal>Digite "deployed-ok" quando deploy + curl smoke + idempotência + inbox confirmados, ou descreva falhas.</resume-signal>
  <acceptance_criteria>
    - Deploy concluído sem erro (`supabase functions list` mostra `aniversario-clientes`)
    - Curl manual retorna HTTP 200 + JSON `{processed,sent,failed,skipped,ano_referencia}`
    - Cliente teste recebe row em `aniversario_envios` com status='sent' e destinatarios JSONB válido (colab_email + admin_emails[])
    - Lenny + Lucas recebem email no inbox (subject "Aniversário em 5 dias: TESTE Aniversário Phase 12")
    - Segundo curl retorna skipped=1 (idempotência via 23505)
    - SMOKE-RESULTS.md atualizado com timestamps + resultados
  </acceptance_criteria>
  <done>Edge function deployed em prod, smoke manual confirmado (envio + idempotência + log), inbox validado, SMOKE-RESULTS.md atualizado. Wave 3 pode começar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → edge fn POST | Supabase Functions exige Authorization Bearer (anon ou service_role) por default; sem token = 401 |
| pg_cron (Postgres) → edge fn (Wave 3) | Cron passa `Authorization: Bearer <service_role>` lido do Vault — bypass total |
| Curl manual (Lenny) → edge fn | Mesma rota; Lenny usa service_role real do Dashboard |
| edge fn → Resend API | HTTPS + API key (RESEND_API_KEY já configurada) |
| edge fn → Postgres (service role) | Service role bypass RLS — única forma de INSERT em aniversario_envios |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | S (Spoofing) | Terceiro chamando POST /functions/v1/aniversario-clientes pra forçar envio | mitigate | Supabase Functions default exige Authorization Bearer válido (anon ou service_role); ataque precisaria de chave Supabase do projeto. Sem rate limit adicional (volume é trivial: 1-10 envios/dia max). |
| T-12-02-02 | I (Info Disclosure) | RESEND_API_KEY ou SERVICE_ROLE_KEY logados em console.error | mitigate | console.error só loga objetos de erro (queryErr.message, insErr.message); nunca interpola env vars. Revisar antes de deploy: `grep -n "SUPABASE_SERVICE_ROLE_KEY\|RESEND_API_KEY" supabase/functions/aniversario-clientes/index.ts` deve só aparecer em `Deno.env.get()` |
| T-12-02-03 | I | Email pra colab/admin contém PII de cliente (nome, contato) — risco se inbox de admin for comprometido | accept | Dado interno de baixo valor; contato é texto livre opcional. Mesmo risco do request-access em prod desde v1.0. |
| T-12-02-04 | T (Tampering) | Resend SDK retorna sucesso mas email cai em spam — log diz sent | accept | Log registra que Resend aceitou; spam é fora do controle do app. Lenny vê falha de entrega no Resend Dashboard (não na tabela aniversario_envios). |
| T-12-02-05 | D (DoS) | Lenny dispara curl 1000x em loop → 1000 INSERTs em aniversario_envios | mitigate | UNIQUE(cliente_id, ano_referencia) faz 23505 a partir da 2ª tentativa do mesmo dia/cliente — só 1 INSERT real por cliente/ano. Smoke step 7 valida. |
| T-12-02-06 | E (EOP) | Edge fn usa service_role pra ler auth.users via stored fn SECURITY DEFINER | mitigate | Funções foram REVOKEd de authenticated/public na Wave 1; só service role chama via RPC. SECURITY DEFINER é necessário porque authenticated não tem acesso direto a auth.users. |
| T-12-02-07 | R (Repudiation) | Resend falha silenciosa (200 mas com error no body) | mitigate | `if (result.error) throw` faz check explícito do body; falha vira status='failed' + error_msg no log; auditável depois. |
</threat_model>

<verification>
- Edge function existe em `supabase/functions/aniversario-clientes/index.ts`
- Deploy bem-sucedido: `supabase functions list` mostra `aniversario-clientes`
- Curl manual retorna HTTP 200 + JSON estruturado
- Inbox Lenny + Lucas confirmados (subject + body)
- Tabela `aniversario_envios` tem 1 row pro cliente teste, status='sent', destinatarios JSONB com colab + admin emails
- Idempotência: 2º curl no mesmo dia retorna skipped=1
- Cleanup executado (cliente teste + log removidos)
</verification>

<success_criteria>
- Edge function deployed em prod e respondendo POST 200
- Smoke manual: envio funciona, log preenchido, inbox confirmado, idempotência validada
- Wave 3 pode começar (cron tem onde apontar)
</success_criteria>

<output>
After completion, create `.planning/phases/12-automa-o-anivers-rio/12-02-SUMMARY.md` com:
- Deploy timestamp + método (CLI / MCP)
- Resultado JSON do smoke curl (1º + 2º run)
- Confirmação inbox (Lenny + Lucas)
- Decisões técnicas (HTML adaptado, target-based ano_referencia, console.log granularity)
- URL exata do endpoint pra Wave 3 (`https://jkewlaezvrbuicmncqbj.supabase.co/functions/v1/aniversario-clientes`)
</output>
