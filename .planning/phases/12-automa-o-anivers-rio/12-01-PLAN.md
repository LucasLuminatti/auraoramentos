---
phase: 12-automa-o-anivers-rio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260515000001_aniversario_envios_table.sql
autonomous: false
requirements: [AUTO-01, AUTO-02]
requirements_addressed: [AUTO-01, AUTO-02]
tags: [supabase, migration, postgres, rls, automation, birthday]

must_haves:
  truths:
    - "Tabela aniversario_envios existe em prod com UNIQUE(cliente_id, ano_referencia) garantindo idempotência 1 envio/ano/cliente"
    - "RLS bloqueia SELECT pra colab/anon na aniversario_envios; só admin (has_role) lê — service role bypass naturalmente"
    - "Stored function buscar_aniversariantes_d5() retorna clientes elegíveis joined com auth.users.email do colab dono, cobrindo edge case 29/02"
    - "Stored function buscar_admins_emails() retorna lista de emails de admins via JOIN auth.users + user_roles (evita N+1 na edge fn)"
    - "Migration aplicada em prod via supabase db push — schema disponível pra Wave 2 (edge function) consumir"
  artifacts:
    - path: "supabase/migrations/20260515000001_aniversario_envios_table.sql"
      provides: "DDL tabela log + 2 stored functions + RLS"
      contains: "CREATE TABLE public.aniversario_envios"
  key_links:
    - from: "aniversario_envios"
      to: "clientes(id)"
      via: "REFERENCES clientes(id) ON DELETE CASCADE"
      pattern: "REFERENCES public\\.clientes\\(id\\)"
    - from: "buscar_aniversariantes_d5"
      to: "auth.users"
      via: "LEFT JOIN via c.user_id"
      pattern: "LEFT JOIN auth\\.users"
    - from: "RLS policy"
      to: "has_role(auth.uid(), 'admin'::app_role)"
      via: "USING clause"
      pattern: "has_role\\(auth\\.uid\\(\\), 'admin'::app_role\\)"
---

<objective>
Criar tabela log `aniversario_envios` (com UNIQUE constraint garantindo idempotência) e 2 stored functions (`buscar_aniversariantes_d5`, `buscar_admins_emails`) que serão consumidas pela edge function da Wave 2. Aplicar migration em prod via `supabase db push`.

Purpose: Phase 12 / AUTO-01/AUTO-02. Schema-first — sem este DDL, edge function da Wave 2 não tem onde inserir log nem como buscar aniversariantes. UNIQUE constraint é o único mecanismo atomic que garante "1 email/ano/cliente" mesmo se cron disparar 2x ou edge fn crashar no meio do batch.

Output: 1 migration aplicada em prod + tabela + 2 stored functions + RLS admin-only SELECT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-automa-o-anivers-rio/12-CONTEXT.md
@.planning/phases/12-automa-o-anivers-rio/12-RESEARCH.md
@supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql
@supabase/migrations/20260511000002_clientes_data_nascimento.sql
@supabase/migrations/20260514000002_orcamentos_status_rls.sql

<interfaces>
<!-- Schema upstream consumido por esta migration -->

clientes (Phase 7):
  id UUID PK
  nome TEXT NOT NULL
  data_nascimento DATE NULL  -- Phase 7 AUTO-03; index BTREE existente
  user_id UUID NOT NULL  -- Phase 7 RLS-03; FK auth.users.id ON DELETE RESTRICT
  contato TEXT NULL

auth.users (Supabase managed):
  id UUID
  email TEXT  -- canonical email source

user_roles:
  user_id UUID
  role app_role  -- enum ('admin', 'user')

public.has_role(_user_id uuid, _role app_role) RETURNS boolean
  STABLE, SECURITY DEFINER, search_path=public
  Padrão de cast: 'admin'::app_role (recomendado em SQL novo)
</interfaces>

<verified_facts>
Confirmado via MCP supabase em projeto jkewlaezvrbuicmncqbj (sa-east-1):
- supabase_vault INSTALADO (0.3.1) — Wave 3 usa
- pg_cron disponível (1.6.4) MAS NÃO instalado — Wave 3 habilita via CREATE EXTENSION
- pg_net disponível (0.20.0) MAS NÃO instalado — Wave 3 habilita via CREATE EXTENSION
- has_role signature: public.has_role(_user_id uuid, _role app_role) RETURNS boolean — SEMPRE usar cast 'admin'::app_role
</verified_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration 20260515000001_aniversario_envios_table.sql</name>
  <files>supabase/migrations/20260515000001_aniversario_envios_table.sql</files>
  <read_first>
    - supabase/migrations/20260511000001_arquitetos_clientes_user_id.sql (confirmar pattern de migrations aditivas + clientes.user_id NOT NULL)
    - supabase/migrations/20260511000002_clientes_data_nascimento.sql (confirmar pattern de index aditivo)
    - supabase/migrations/20260514000002_orcamentos_status_rls.sql (replicar pattern has_role(auth.uid(), 'admin'::app_role))
    - .planning/phases/12-automa-o-anivers-rio/12-CONTEXT.md §D-02, D-05, D-06, D-09 (specs exatos da tabela)
    - .planning/phases/12-automa-o-anivers-rio/12-RESEARCH.md Finding 1 e Finding 6 (SQL ready-to-paste)
  </read_first>
  <action>
Criar o arquivo `supabase/migrations/20260515000001_aniversario_envios_table.sql` com o conteúdo SQL completo abaixo (replicado direto do RESEARCH Finding 6 + Finding 1, com cast `'admin'::app_role` aplicado nas RLS policies conforme convenção do projeto):

```sql
-- Phase 12 / Plan 01 — Tabela log de envios de aniversário (AUTO-01/AUTO-02)
-- Refs: CONTEXT D-02 (UNIQUE cliente_id+ano), D-05 (multi-admin via has_role),
--       D-06 (skipped_no_owner), D-09 (failed sem retry)
-- Padrão has_role cast: 'admin'::app_role (replicado de 20260514000002_orcamentos_status_rls.sql)

BEGIN;

-- ===== Tabela log =====

CREATE TABLE public.aniversario_envios (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  cliente_id UUID NOT NULL REFERENCES public.clientes(id) ON DELETE CASCADE,
  ano_referencia INT NOT NULL,
  destinatarios JSONB NOT NULL,  -- {colab_email: string|null, admin_emails: string[]}
  sent_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  status TEXT NOT NULL CHECK (status IN ('sent', 'failed', 'skipped_no_owner')),
  error_msg TEXT NULL,
  UNIQUE (cliente_id, ano_referencia)
);

CREATE INDEX idx_aniversario_envios_cliente ON public.aniversario_envios(cliente_id);
CREATE INDEX idx_aniversario_envios_status_failed ON public.aniversario_envios(status) WHERE status = 'failed';

COMMENT ON TABLE public.aniversario_envios IS
  'Log de envios de email de aniversário (Phase 12). UNIQUE(cliente_id, ano_referencia) garante 1 envio/ano. Status sent|failed|skipped_no_owner.';

-- ===== RLS: admin SELECT-only, service role bypass =====

ALTER TABLE public.aniversario_envios ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Admins can read aniversario_envios"
  ON public.aniversario_envios
  FOR SELECT
  TO authenticated
  USING (public.has_role(auth.uid(), 'admin'::app_role));

COMMENT ON POLICY "Admins can read aniversario_envios" ON public.aniversario_envios IS
  'Phase 12 D-02: admin (has_role) lê tudo pra auditoria; colab não acessa direto. Service role bypass RLS naturalmente (edge fn escreve).';

-- Nenhuma policy INSERT/UPDATE/DELETE pra authenticated — só service role escreve.

-- ===== Stored function: buscar aniversariantes D-5 =====
-- Cobre edge case 29/02 em ano não-bissexto (D-08) e cliente órfão LEFT JOIN (D-06)
-- Filtra clientes já notificados (NOT IN aniversario_envios pro ano_referencia)

CREATE OR REPLACE FUNCTION public.buscar_aniversariantes_d5()
RETURNS TABLE (
  id UUID,
  nome TEXT,
  data_nascimento DATE,
  contato TEXT,
  user_id UUID,
  colab_email TEXT
)
LANGUAGE sql
STABLE
SECURITY DEFINER
SET search_path = public
AS $$
  WITH target AS (
    SELECT (CURRENT_DATE + INTERVAL '5 days')::date AS d
  ),
  ja_notificados AS (
    SELECT cliente_id
    FROM public.aniversario_envios
    WHERE ano_referencia = EXTRACT(YEAR FROM (SELECT d FROM target))::int
  )
  SELECT
    c.id,
    c.nome,
    c.data_nascimento,
    c.contato,
    c.user_id,
    u.email::text AS colab_email
  FROM public.clientes c
  CROSS JOIN target t
  LEFT JOIN auth.users u ON u.id = c.user_id
  WHERE c.data_nascimento IS NOT NULL
    AND c.id NOT IN (SELECT cliente_id FROM ja_notificados)
    AND (
      -- Caso comum: mês/dia bate exato
      (EXTRACT(MONTH FROM c.data_nascimento) = EXTRACT(MONTH FROM t.d)
       AND EXTRACT(DAY FROM c.data_nascimento) = EXTRACT(DAY FROM t.d))
      OR
      -- Caso 29/02 em ano não-bissexto: target = 28/02 e amanhã (1/3) confirma não-bissexto
      (EXTRACT(MONTH FROM c.data_nascimento) = 2
       AND EXTRACT(DAY FROM c.data_nascimento) = 29
       AND EXTRACT(MONTH FROM t.d) = 2
       AND EXTRACT(DAY FROM t.d) = 28
       AND EXTRACT(MONTH FROM (t.d + INTERVAL '1 day')) = 3)
    );
$$;

COMMENT ON FUNCTION public.buscar_aniversariantes_d5() IS
  'Phase 12 D-07/D-08: clientes que fazem aniversário em today+5d, com email do colab dono via JOIN auth.users. Cobre 29/02 em ano não-bissexto (dispara em 28/02). Filtra já notificados via UNIQUE(cliente_id, ano_referencia).';

REVOKE EXECUTE ON FUNCTION public.buscar_aniversariantes_d5() FROM PUBLIC;
REVOKE EXECUTE ON FUNCTION public.buscar_aniversariantes_d5() FROM authenticated;

-- ===== Stored function: buscar emails de admins =====
-- Evita N+1 via supabase.auth.admin.getUserById no edge function

CREATE OR REPLACE FUNCTION public.buscar_admins_emails()
RETURNS TABLE (email TEXT)
LANGUAGE sql
STABLE
SECURITY DEFINER
SET search_path = public
AS $$
  SELECT u.email::text
  FROM public.user_roles ur
  JOIN auth.users u ON u.id = ur.user_id
  WHERE ur.role = 'admin'::app_role
    AND u.email IS NOT NULL
    AND length(trim(u.email)) > 0;
$$;

COMMENT ON FUNCTION public.buscar_admins_emails() IS
  'Phase 12 D-05: lista emails de admins do sistema (multi-admin via has_role). Substitui hardcode de ADMIN_EMAIL — divergência consciente do REQ AUTO-02.';

REVOKE EXECUTE ON FUNCTION public.buscar_admins_emails() FROM PUBLIC;
REVOKE EXECUTE ON FUNCTION public.buscar_admins_emails() FROM authenticated;

COMMIT;
```

Pontos críticos:
- Cast `'admin'::app_role` (não inferred) — convenção interna replicada de 20260514000002_orcamentos_status_rls.sql
- `LEFT JOIN auth.users` (não INNER) — cobre D-06 cliente órfão retorna `colab_email = NULL`
- `REVOKE EXECUTE` em ambas funções — service role chama via RPC, authenticated nunca
- `SECURITY DEFINER` — funções leem `auth.users` que authenticated não tem acesso direto
- `STABLE` — não modifica dados, query pode ser cacheada por transaction
- Não inserir RLS de INSERT/UPDATE/DELETE — service role bypass naturalmente, authenticated nunca escreve
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260515000001_aniversario_envios_table.sql','utf8');const checks=[/CREATE TABLE public\.aniversario_envios/,/UNIQUE \(cliente_id, ano_referencia\)/,/CHECK \(status IN \('sent', 'failed', 'skipped_no_owner'\)\)/,/REFERENCES public\.clientes\(id\) ON DELETE CASCADE/,/has_role\(auth\.uid\(\), 'admin'::app_role\)/,/CREATE OR REPLACE FUNCTION public\.buscar_aniversariantes_d5/,/CREATE OR REPLACE FUNCTION public\.buscar_admins_emails/,/SECURITY DEFINER/,/LEFT JOIN auth\.users/,/EXTRACT\(MONTH FROM c\.data_nascimento\) = 2[\s\S]*EXTRACT\(DAY FROM c\.data_nascimento\) = 29/];for(const r of checks){if(!r.test(s)){console.error('FAIL:',r);process.exit(1)}};console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260515000001_aniversario_envios_table.sql` existe
    - `grep -q "CREATE TABLE public\.aniversario_envios" supabase/migrations/20260515000001_aniversario_envios_table.sql`
    - `grep -q "UNIQUE (cliente_id, ano_referencia)" supabase/migrations/20260515000001_aniversario_envios_table.sql`
    - `grep -q "REFERENCES public\.clientes(id) ON DELETE CASCADE" supabase/migrations/20260515000001_aniversario_envios_table.sql`
    - `grep -q "has_role(auth\.uid(), 'admin'::app_role)" supabase/migrations/20260515000001_aniversario_envios_table.sql`
    - `grep -q "CREATE OR REPLACE FUNCTION public\.buscar_aniversariantes_d5" supabase/migrations/20260515000001_aniversario_envios_table.sql`
    - `grep -q "CREATE OR REPLACE FUNCTION public\.buscar_admins_emails" supabase/migrations/20260515000001_aniversario_envios_table.sql`
    - `grep -q "LEFT JOIN auth\.users" supabase/migrations/20260515000001_aniversario_envios_table.sql`
    - `grep -q "SECURITY DEFINER" supabase/migrations/20260515000001_aniversario_envios_table.sql`
    - `grep -q "EXTRACT(DAY FROM c\.data_nascimento) = 29" supabase/migrations/20260515000001_aniversario_envios_table.sql`
  </acceptance_criteria>
  <done>Arquivo de migration existe com tabela, índices, RLS admin-only SELECT, e 2 stored functions (buscar_aniversariantes_d5 + buscar_admins_emails) com SECURITY DEFINER e REVOKE de authenticated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] supabase db push migration #1 em prod + smoke SQL</name>
  <what-built>
Migration `20260515000001_aniversario_envios_table.sql` precisa ser aplicada em prod (`jkewlaezvrbuicmncqbj`) ANTES da Wave 2 começar. Sem isso, edge function da Wave 2 não consegue inserir log nem chamar stored functions.

Alternativa autônoma: aplicar via MCP `mcp__plugin_supabase_supabase__apply_migration` (Lenny já autorizou em PUSH-LOGs anteriores). Se Claude rodar via MCP, ainda assim pedir Lenny pra confirmar smoke SQL no Dashboard.
  </what-built>
  <how-to-verify>
1. **Aplicar migration em prod** (opção A via CLI, opção B via MCP):

   **Opção A — CLI local (preferido se SUPABASE_ACCESS_TOKEN estiver configurado):**
   ```bash
   cd C:/Users/lenny/Desktop/Luminatti/automa_aura/auraoramentos
   supabase db push
   ```
   Esperar `Finished supabase db push.`

   **Opção B — via MCP apply_migration (fallback se CLI exigir prompt):**
   Claude chama `mcp__plugin_supabase_supabase__apply_migration` passando:
   - `name`: `20260515000001_aniversario_envios_table`
   - `query`: conteúdo completo do arquivo SQL

2. **Smoke SQL via Supabase Dashboard SQL Editor (ou MCP execute_sql):**

   ```sql
   -- Confirmar tabela criada com RLS
   SELECT
     tablename,
     rowsecurity,
     (SELECT count(*) FROM pg_policies WHERE tablename = 'aniversario_envios') AS num_policies
   FROM pg_tables
   WHERE schemaname = 'public' AND tablename = 'aniversario_envios';
   -- Esperado: 1 row, rowsecurity=true, num_policies=1

   -- Confirmar UNIQUE constraint
   SELECT conname, contype
   FROM pg_constraint
   WHERE conrelid = 'public.aniversario_envios'::regclass
     AND contype IN ('u', 'p', 'f', 'c');
   -- Esperado: 1 UNIQUE (cliente_id+ano_referencia), 1 PRIMARY (id), 1 FOREIGN (cliente_id), 1 CHECK (status)

   -- Confirmar stored functions criadas e REVOKEd
   SELECT
     p.proname,
     p.prosecdef AS security_definer,
     has_function_privilege('authenticated', p.oid, 'EXECUTE') AS auth_can_exec
   FROM pg_proc p
   JOIN pg_namespace n ON n.oid = p.pronamespace
   WHERE n.nspname = 'public'
     AND p.proname IN ('buscar_aniversariantes_d5', 'buscar_admins_emails');
   -- Esperado: 2 rows, security_definer=true, auth_can_exec=false

   -- Smoke: chamar buscar_admins_emails (deve retornar Lenny + Lucas hoje)
   SELECT * FROM public.buscar_admins_emails();
   -- Esperado: 2 rows mínimo (lenny + lucas), email não nulo

   -- Smoke: chamar buscar_aniversariantes_d5 (provavelmente 0 rows hoje — não há cliente com aniversário em D+5 cadastrado)
   SELECT * FROM public.buscar_aniversariantes_d5();
   -- Esperado: 0+ rows (sem erro)
   ```

3. **Registrar resultado em `.planning/phases/12-automa-o-anivers-rio/PUSH-LOG.md`** (Lenny ou Claude appenda):
   - Migration aplicada: 20260515000001_aniversario_envios_table
   - Method: CLI ou MCP
   - Timestamp prod
   - Resultado smoke SQL (especialmente quantos admins retornaram)

4. **Se smoke SQL falhar:** rollback via `DROP TABLE public.aniversario_envios CASCADE; DROP FUNCTION public.buscar_aniversariantes_d5(); DROP FUNCTION public.buscar_admins_emails();` e revisar migration.
  </how-to-verify>
  <resume-signal>Digite "pushed" quando migration aplicada + smoke SQL confirmado em prod, ou descreva problemas.</resume-signal>
  <acceptance_criteria>
    - Migration `20260515000001_aniversario_envios_table` aparece em `SELECT * FROM supabase_migrations.schema_migrations` (ou equivalente)
    - Smoke `SELECT count(*) FROM public.buscar_admins_emails()` retorna ≥ 1
    - Smoke `SELECT * FROM public.aniversario_envios LIMIT 0` retorna sem erro (tabela existe)
    - `PUSH-LOG.md` atualizado com timestamp + método de aplicação
  </acceptance_criteria>
  <done>Migration aplicada em prod, RLS ativo, 2 stored functions executáveis pelo service role, PUSH-LOG.md atualizado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service_role → DB | Service role bypassa RLS pra escrever em aniversario_envios (única forma legítima de INSERT) |
| authenticated colab → DB | Colab autenticado tenta SELECT em aniversario_envios → RLS bloqueia (não tem has_role admin) |
| authenticated admin → DB | Admin autenticado SELECT em aniversario_envios → RLS libera (auditoria) |
| authenticated → stored fn | Authenticated tenta CALL buscar_aniversariantes_d5/buscar_admins_emails → REVOKE bloqueia |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | I (Info Disclosure) | aniversario_envios.destinatarios JSONB (PII: emails de colab + admins) | mitigate | RLS policy `has_role(auth.uid(), 'admin'::app_role)` em SELECT; INSERT/UPDATE/DELETE sem policy = só service role (Postgres default deny pra authenticated) |
| T-12-01-02 | E (EOP) | Colab tentando chamar buscar_aniversariantes_d5 pra vazar lista de aniversariantes de outros colabs | mitigate | `REVOKE EXECUTE ... FROM authenticated` + `REVOKE EXECUTE ... FROM PUBLIC`; só service role chama via RPC |
| T-12-01-03 | I | buscar_aniversariantes_d5 expõe auth.users.email via SECURITY DEFINER | mitigate | Função é REVOKEd de authenticated; SECURITY DEFINER necessário porque authenticated não tem acesso direto a auth.users; quem chama é service role (bypass RLS naturalmente) |
| T-12-01-04 | T (Tampering) | Colab tentando manipular aniversario_envios pra forçar reenvio ou bloquear envio pra outro cliente | mitigate | Sem policy de INSERT/UPDATE/DELETE pra authenticated; UNIQUE(cliente_id, ano_referencia) impede duplicação mesmo via service role |
| T-12-01-05 | R (Repudiation) | Disputa sobre se email foi enviado ou não | mitigate | sent_at TIMESTAMPTZ DEFAULT now() + status + destinatarios JSONB com lista exata enviada — auditoria completa pra admin |
| T-12-01-06 | D (DoS) | Migration roda CREATE TABLE em prod e trava locks (improvável — tabela nova, sem dados) | accept | Tabela é nova, sem registros existentes; locks são triviais. CONCURRENTLY não aplicável a CREATE TABLE inicial. |
</threat_model>

<verification>
- Migration aplicada em prod sem erro
- `SELECT 1 FROM information_schema.tables WHERE table_schema='public' AND table_name='aniversario_envios'` retorna 1
- `SELECT rowsecurity FROM pg_tables WHERE tablename='aniversario_envios'` retorna true
- `SELECT count(*) FROM pg_policies WHERE tablename='aniversario_envios'` retorna 1
- `SELECT * FROM public.buscar_admins_emails()` retorna ≥ 1 row (Lenny + Lucas hoje)
- `SELECT * FROM public.buscar_aniversariantes_d5()` retorna 0+ rows sem erro
- `has_function_privilege('authenticated', 'public.buscar_aniversariantes_d5()', 'EXECUTE')` retorna false
- `has_function_privilege('authenticated', 'public.buscar_admins_emails()', 'EXECUTE')` retorna false
</verification>

<success_criteria>
- Tabela `aniversario_envios` existe em prod com UNIQUE + CHECK + RLS habilitado
- 2 stored functions criadas, SECURITY DEFINER, REVOKEd de authenticated
- Migration commitada em git e aplicada em prod (PUSH-LOG.md atualizado)
- Wave 2 pode começar (edge function tem onde inserir log + tem stored fn pra chamar)
</success_criteria>

<output>
After completion, create `.planning/phases/12-automa-o-anivers-rio/12-01-SUMMARY.md` com:
- Migration aplicada (timestamp + método)
- Resultado smoke SQL (quantos admins retornaram, confirmação RLS)
- Decisões técnicas (stored functions vs JOIN inline; SECURITY DEFINER + REVOKE)
- Links pra Wave 2 (12-02-PLAN consumirá buscar_aniversariantes_d5 + buscar_admins_emails)
</output>
