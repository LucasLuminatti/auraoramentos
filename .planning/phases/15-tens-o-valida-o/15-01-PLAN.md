---
phase: 15-tens-o-valida-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/orcamento.ts
  - src/components/Step3Revisao.tsx
autonomous: true
requirements: [TENS-02]
tags: [validacao, drivers, voltagem, resumo]
must_haves:
  truths:
    - "Dois sistemas com o mesmo driver em voltagens diferentes geram duas linhas distintas no Resumo Global de Drivers"
    - "O rótulo do driver no Resumo mostra código e voltagem juntos (ex.: LM2130 · 24V)"
    - "Não existe mais coluna Tensão separada no Resumo Global de Drivers"
    - "Orçamentos antigos continuam abrindo e o resumo renderiza sem erro de key duplicada no React"
  artifacts:
    - path: "src/types/orcamento.ts"
      provides: "calcularDriversPorProjeto agrupando por (codigo + voltagem)"
      contains: "${sis.driver.codigo}|${sis.driver.voltagem}"
    - path: "src/components/Step3Revisao.tsx"
      provides: "Resumo Global de Drivers com rótulo composto codigo · voltagem"
      contains: "· ${d.voltagem}V"
  key_links:
    - from: "src/types/orcamento.ts:calcularDriversPorProjeto"
      to: "src/components/Step3Revisao.tsx:resumoDrivers"
      via: "ResumoDriverProjeto[] (campo voltagem já existente)"
      pattern: "resumoDrivers.map"
---

<objective>
Corrigir o agrupamento global de drivers para distinguir o mesmo código de driver em voltagens diferentes, e ajustar o Resumo Global de Drivers no Step 3 para mostrar a voltagem de forma imediatamente visível (rótulo composto), removendo a coluna Tensão redundante.

Purpose: Hoje `calcularDriversPorProjeto` agrupa só por `sis.driver.codigo`. Quando dois ambientes usam o mesmo código de driver em voltagens diferentes (legítimo após desbloquear tensões por ambiente), as linhas colapsam numa só, somando consumo de voltagens incompatíveis — um subtotal fisicamente sem sentido (pitfall C-4 do STATE.md). Esta é a base de TENS-02 e precede o desbloqueio de tensões do Plano 02.
Output: `calcularDriversPorProjeto` com chave `(codigo + voltagem)` + Resumo Global no Step 3 com rótulo `LM2130 · 24V` e sem coluna Tensão separada (D-09a aprovado: Opção A).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tens-o-valida-o/15-CONTEXT.md
@.planning/phases/15-tens-o-valida-o/15-RESEARCH.md

<interfaces>
<!-- Contratos já existentes no codebase — usar diretamente, sem explorar. -->

De src/types/orcamento.ts (interface já tem `voltagem`, NÃO mudar a interface):
```typescript
export interface ResumoDriverProjeto {
  driverCodigo: string;
  driverDescricao: string;
  potenciaDriverW: number;
  voltagem: 12 | 24 | 48;
  totalConsumoW: number;
  totalDemandaM: number;
  limiteExtensaoM: number | null;
  qtdGlobal: number;
  qtdSomaIndividual: number;
  economiaDrivers: number;
}
```

Estado atual de `calcularDriversPorProjeto` (orcamento.ts L288-342) — chave do Map é só `cod`:
```typescript
const cod = sis.driver.codigo;
const existing = grupos.get(cod);
// ...
grupos.set(cod, { descricao, potenciaDriverW, voltagem: sis.driver.voltagem, ... });
// loop de saída:
for (const [cod, g] of grupos) {
  resultado.push({ driverCodigo: cod, ... });
}
```

Consumo no Step3Revisao.tsx (L776-794) — `key={d.driverCodigo}` + coluna Tensão separada (L767 head, L782 cell).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mudar grouping key de calcularDriversPorProjeto para (codigo + voltagem)</name>
  <files>src/types/orcamento.ts, src/types/orcamento.test.ts</files>
  <read_first>
    - src/types/orcamento.ts (L273-342 — interface ResumoDriverProjeto + calcularDriversPorProjeto)
    - .planning/phases/15-tens-o-valida-o/15-RESEARCH.md (seção "Fix do grouping key em calcularDriversPorProjeto", L301-328)
    - .planning/STATE.md (§"v1.2 Technical Notes" C-4)
  </read_first>
  <behavior>
    - Teste 1: dois sistemas com driver código "LM2130" em voltagem 12 e 24 → `calcularDriversPorProjeto` retorna 2 itens distintos (um com voltagem:12, outro voltagem:24), cada um com `driverCodigo === "LM2130"`.
    - Teste 2: dois sistemas com mesmo driver "LM1462" na MESMA voltagem 24 → retorna 1 item com `qtdSomaIndividual` somado dos dois.
    - Teste 3: `driverCodigo` no resultado nunca contém o caractere "|" (a chave composta é interna ao Map, não vaza para o campo de saída).
    - Edge: sistema com `sis.driver.codigo === ''` ou `potencia <= 0` ou `fita.wm` falsy → ignorado (comportamento atual preservado).
  </behavior>
  <action>
    Em `src/types/orcamento.ts`, função `calcularDriversPorProjeto` (~L288-342):

    1. Trocar a chave do Map de `const cod = sis.driver.codigo;` para uma chave composta:
       ```typescript
       const chave = `${sis.driver.codigo}|${sis.driver.voltagem}`;
       ```
       (NÃO remover o guard `if (!cod || sis.driver.potencia <= 0 || !sis.fita.wm) continue;` — apenas referenciar `sis.driver.codigo` direto nesse guard, já que `cod` deixa de existir.)

    2. Substituir `grupos.get(cod)` → `grupos.get(chave)` e `grupos.set(cod, {...})` → `grupos.set(chave, {...})`. Manter `voltagem: sis.driver.voltagem` no objeto do grupo (já está).

    3. No loop de saída `for (const [cod, g] of grupos)`: renomear para `for (const [chave, g] of grupos)` e extrair o código antes do separador:
       ```typescript
       const driverCodigo = chave.split('|')[0];
       resultado.push({ driverCodigo, driverDescricao: g.descricao, ... });
       ```
       Garantir que `driverCodigo` (sem voltagem) seja atribuído a `resultado.driverCodigo`. O campo `voltagem` continua vindo de `g.voltagem`.

    NÃO mudar a interface `ResumoDriverProjeto`. NÃO tocar em `calcularConsumoW`, `calcularDemandaFita`, `limiteExtensaoMetros` nem nos defaults de criação de sistema. Comentário em pt-BR explicando a chave composta (ex.: `// Chave composta codigo+voltagem — mesmo driver em voltagens diferentes = linhas distintas (D-08/C-4)`).

    Criar/atualizar o teste em `src/types/orcamento.test.ts` (ou arquivo de teste correspondente — se não existir, criar `src/types/orcamento.test.ts` importando de `@/types/orcamento`). Cobrir os 3 cenários do bloco behavior. Usar helpers mínimos para montar `Ambiente[]` (id via string fixa, fita com `wm > 0`, driver com `potencia > 0`).
  </action>
  <verify>
    <automated>cd auraoramentos && npx vitest run src/types/orcamento.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/orcamento.ts` contém a string literal `` `${sis.driver.codigo}|${sis.driver.voltagem}` ``
    - `src/types/orcamento.ts` contém `chave.split('|')[0]`
    - `calcularDriversPorProjeto` NÃO contém mais `const cod = sis.driver.codigo;` como chave do Map
    - Interface `ResumoDriverProjeto` inalterada (grep `voltagem: 12 | 24 | 48` ainda presente)
    - `npx vitest run src/types/orcamento.test.ts` passa com os 3 cenários
  </acceptance_criteria>
  <done>Dois sistemas com o mesmo driver em voltagens diferentes geram itens distintos; mesmo driver em mesma voltagem continua agregando; `driverCodigo` de saída não contém "|".</done>
</task>

<task type="auto">
  <name>Task 2: Rótulo composto codigo · voltagem no Resumo Global de Drivers + remover coluna Tensão</name>
  <files>src/components/Step3Revisao.tsx</files>
  <read_first>
    - src/components/Step3Revisao.tsx (L755-804 — bloco "Resumo Global de Drivers")
    - .planning/phases/15-tens-o-valida-o/15-RESEARCH.md (seção "D-09a — Checkpoint: Display de Voltagem", L158-191 — Opção A APROVADA)
    - .planning/phases/15-tens-o-valida-o/15-CONTEXT.md (D-09a)
  </read_first>
  <action>
    Em `src/components/Step3Revisao.tsx`, bloco "Resumo Global de Drivers" (~L755-804). Aplicar D-09a APROVADO (Opção A: rótulo composto, remover coluna Tensão separada):

    1. Na `<TableRow>` do map (L777), corrigir a `key` para incluir a voltagem — após o grouping fix dois itens podem ter o mesmo `driverCodigo`:
       ```tsx
       <TableRow key={`${d.driverCodigo}-${d.voltagem}`}>
       ```
       (Sem isso o React quebra com keys duplicadas — pitfall crítico.)

    2. No `<div className="font-mono text-xs">` (L779), trocar `{d.driverCodigo}` por rótulo composto:
       ```tsx
       <div className="font-mono text-xs">{d.driverCodigo} · {d.voltagem}V</div>
       ```

    3. Remover a coluna Tensão (redundante após o rótulo composto):
       - Remover o `<TableHead className="text-right">Tensão</TableHead>` (L767).
       - Remover a `<TableCell className="text-right">{d.voltagem}V</TableCell>` (L782).

    NÃO alterar as outras colunas (Consumo Total, Extensão Total, Qtd Global, Soma p/ Ambiente, Economia Potencial). NÃO tocar no Resumo Global de Fitas nem em outros blocos. Manter o texto explicativo em pt-BR ao final.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Step3Revisao.tsx` contém `{d.driverCodigo} · {d.voltagem}V`
    - `src/components/Step3Revisao.tsx` contém `key={`${d.driverCodigo}-${d.voltagem}`}`
    - `src/components/Step3Revisao.tsx` NÃO contém mais `<TableHead className="text-right">Tensão</TableHead>`
    - `src/components/Step3Revisao.tsx` NÃO contém mais a célula `<TableCell className="text-right">{d.voltagem}V</TableCell>`
    - `npm run build` conclui sem erro de tipo
  </acceptance_criteria>
  <done>O Resumo Global de Drivers exibe "LM2130 · 24V" no campo de identificação, sem coluna Tensão separada, e renderiza sem warning de key duplicada quando há mesmo código em voltagens distintas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Mudança puramente em lógica de cálculo client-side e renderização. Sem novo input não-confiável, sem mudança de auth/RLS/schema/edge function. Dados consumidos (`sis.driver.codigo`, `voltagem`) já vêm de produtos do catálogo selecionados pelo colaborador autenticado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `calcularDriversPorProjeto` chave string `${codigo}\|${voltagem}` | accept | Código de produto vem do catálogo (não input livre); pior caso seria um split incorreto se um código contivesse "\|" — códigos LM são alfanuméricos sem "\|". Risco baixo, sem exposição de dados. |
| T-15-02 | Information Disclosure | Resumo Global de Drivers (Step3Revisao) | accept | Renderiza apenas dados do próprio orçamento já visíveis ao colaborador. Nenhum dado novo exposto. |

**Conclusão (honesta):** fase frontend/lógica pura, sem severidade `high`. Nenhum bloqueio de segurança.
</threat_model>

<verification>
- `npx vitest run src/types/orcamento.test.ts` passa (grouping por codigo+voltagem)
- `npm run build` conclui sem erro
- Grep confirma os 5 acceptance_criteria de cada task
</verification>

<success_criteria>
- Success Criteria #3 do ROADMAP: "O Resumo Global de drivers agrupa por (código + voltagem), não apenas por código — dois sistemas com mesmo driver em voltagens diferentes geram linhas distintas" — ATENDIDO.
- D-08 e D-09a (Opção A) implementados exatamente como aprovados.
- TENS-02 (parte de agregação) coberta — o desbloqueio de tensões por ambiente é finalizado no Plano 02.
</success_criteria>

<output>
After completion, create `.planning/phases/15-tens-o-valida-o/15-01-SUMMARY.md`
</output>
