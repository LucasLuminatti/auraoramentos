---
phase: 15-tens-o-valida-o
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useProdutoSearch.ts
  - src/components/ProdutoAutocomplete.tsx
  - src/components/AmbienteCard.tsx
autonomous: true
requirements: [TENS-01, SIST-04, UX-02]
tags: [validacao, drivers, voltagem, autocomplete, tiny, advisory]
must_haves:
  truths:
    - "Ao selecionar uma fita com voltagem definida, o seletor de driver é pré-filtrado por voltagem compatível"
    - "Ao selecionar uma fita, o driver é pré-preenchido com uma sugestão compatível quando o driver ainda está vazio"
    - "Selecionar um driver de voltagem diferente da fita NÃO bloqueia — mostra toast orientativo + badge persistente de divergência"
    - "O badge de divergência some automaticamente quando a voltagem da fita e do driver coincidem"
    - "Criar Ambiente A 24V e Ambiente B 12V não gera bloqueio indevido — cada sistema é validado isoladamente"
    - "Ao adicionar uma luminária da linha TINY (sistema='tiny_magneto'), aparece toast + badge informando que requer driver 24V externo"
  artifacts:
    - path: "src/hooks/useProdutoSearch.ts"
      provides: "Pré-filtro de driver por voltagem (tensao = X OR tensao IS NULL)"
      contains: "filtroVoltagem"
    - path: "src/components/ProdutoAutocomplete.tsx"
      provides: "Prop filtroVoltagem repassada ao hook"
      contains: "filtroVoltagem"
    - path: "src/components/AmbienteCard.tsx"
      provides: "Pré-fill driver sugerido, badge divergência derivado do state, advisory TINY, toast não-bloqueante"
      contains: "buscarDriverSugerido"
  key_links:
    - from: "src/components/AmbienteCard.tsx:ProdutoAutocomplete driver"
      to: "src/hooks/useProdutoSearch.ts"
      via: "prop filtroVoltagem={sis.fita.voltagem}"
      pattern: "filtroVoltagem"
    - from: "src/components/AmbienteCard.tsx:handleSelectProdutoSistema(fita)"
      to: "supabase.from('produtos')"
      via: "buscarDriverSugerido async"
      pattern: "buscarDriverSugerido"
---

<objective>
Tornar a escolha do driver proativa e a validação de voltagem orientativa (não bloqueante): pré-filtrar o seletor de driver pela voltagem da fita, pré-preencher um driver compatível sugerido quando o driver estiver vazio, substituir o toast bloqueante por aviso + badge persistente de divergência, e adicionar advisory para luminárias da linha TINY (requer driver 24V externo).

Purpose: O UAT reportou (1) voltagem do driver manual (deveria inferir), (2) bloqueio indevido ao usar tensões diferentes entre ambientes, (3) linha TINY sem aviso de driver 24V. A causa-raiz do bloqueio é o `return` na validação atual (AmbienteCard L137-154) combinado com o default `voltagem: 24` do driver vazio — remover o `return` e tornar o aviso visual resolve sem mexer nos defaults (pitfall C-3). Cobre TENS-01, SIST-04, UX-02 e a parte "sem bloqueio indevido" de TENS-02 (D-10).
Output: hook + autocomplete com `filtroVoltagem`; `handleSelectProdutoSistema` async com pré-fill; badge de divergência derivado do state; toast orientativo sem `return`; advisory TINY (toast + badge).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tens-o-valida-o/15-CONTEXT.md
@.planning/phases/15-tens-o-valida-o/15-RESEARCH.md

<interfaces>
<!-- Contratos já existentes — usar diretamente. -->

De src/integrations/supabase/client.ts:
```typescript
import { supabase } from "@/integrations/supabase/client";
```

De src/hooks/useProdutoSearch.ts (assinatura atual):
```typescript
export type ProdutoFiltro = 'fita' | 'driver' | 'perfil' | 'luminaria' | 'todos';
export function useProdutoSearch(query: string, filtro: ProdutoFiltro = 'todos'): { results: Produto[]; loading: boolean };
// select com aliases: voltagem:tensao, driver_potencia_w:potencia_watts, driver_tipo:subtipo, sistema_magnetico:sistema, wm:watts_por_metro
```

De src/types/orcamento.ts (campos relevantes — NÃO mudar tipos):
```typescript
// ItemFitaLED: { codigo, descricao, wm, voltagem?: 12|24|48, metragemRolo, precoUnitario, precoMinimo, is_baby?, ... }
// ItemDriver:  { codigo, descricao, potencia, voltagem: 12|24|48, precoUnitario, precoMinimo, driver_tipo?, ... }
// ItemLuminaria: { ..., sistema?: string | null, tensao?: number | null }
// SistemaIluminacao: { perfil, fita, driver, metragemManual, passadasManual, local, ... }
// Produto: { codigo, descricao, preco_tabela, preco_minimo, voltagem?, driver_potencia_w?, sistema_magnetico?, ... }
export const MARGEM_SEGURANCA_DRIVER = 1.05;
export function calcularDemandaFita(sistema: SistemaIluminacao): number; // metragem efetiva (metragemManual*passadas ou perfil)
```

IMPORTANTE: NÃO existe campo `sis.fita.metragem`. A metragem efetiva do sistema é `calcularDemandaFita(sis)`. Use-a para a estimativa de consumo: se `> 0` → metragem real, senão fallback 5m.

Estado atual de AmbienteCard.tsx:
- `handleSelectProdutoSistema(produto, sistemaIndex, component)` é SÍNCRONO (L130).
- Toast bloqueante de tensão com `return` em L137-154 (dois blocos: driver e fita).
- `addSistema()` cria fita/driver com `voltagem: 24` default (L63-64) — NÃO ALTERAR (pitfall C-3).
- Seletor de driver: `<ProdutoAutocomplete ... filtro="driver" />` em L486.
- Advisory de luminária TINY: branch `produto.sistema_magnetico === 'tiny_magneto'` em L89-95 (handleSelectProdutoLuminaria).
- Badge de voltagem da fita já renderiza em L403.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender useProdutoSearch + ProdutoAutocomplete com filtroVoltagem</name>
  <files>src/hooks/useProdutoSearch.ts, src/components/ProdutoAutocomplete.tsx</files>
  <read_first>
    - src/hooks/useProdutoSearch.ts (arquivo inteiro)
    - src/components/ProdutoAutocomplete.tsx (arquivo inteiro)
    - .planning/phases/15-tens-o-valida-o/15-RESEARCH.md (seções "Padrão de pré-filtro de voltagem em useProdutoSearch" L197-227 e "Extensão de ProdutoAutocomplete com filtroVoltagem" L485-506; Pitfall 1 L397-404)
  </read_first>
  <action>
    1. Em `src/hooks/useProdutoSearch.ts`, adicionar 3º parâmetro opcional `filtroVoltagem?: number`:
       ```typescript
       export function useProdutoSearch(query: string, filtro: ProdutoFiltro = 'todos', filtroVoltagem?: number) {
       ```
       Dentro do `useEffect`, APÓS o bloco `if (filtro === 'fita' || ...)`, adicionar o pré-filtro de voltagem só para driver:
       ```typescript
       // Pré-filtro de voltagem do driver: 46/61 drivers têm tensao=null mas são compatíveis (D-01)
       if (filtro === 'driver' && filtroVoltagem !== undefined) {
         queryBuilder = queryBuilder.or(`tensao.eq.${filtroVoltagem},tensao.is.null`);
       }
       ```
       Adicionar `filtroVoltagem` ao array de dependências do `useEffect`: `[query, filtro, filtroVoltagem]`.

    2. Em `src/components/ProdutoAutocomplete.tsx`:
       - Adicionar `filtroVoltagem?: number;` à interface `ProdutoAutocompleteProps`.
       - Desestruturar `filtroVoltagem` nas props do componente.
       - Repassar ao hook: `useProdutoSearch(query, filtro, filtroVoltagem)`.

    NÃO mudar o comportamento para `filtro !== 'driver'`. NÃO mudar o select de colunas. Comentário em pt-BR no pré-filtro.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useProdutoSearch.ts` contém `filtroVoltagem?: number` na assinatura
    - `src/hooks/useProdutoSearch.ts` contém `tensao.eq.${filtroVoltagem},tensao.is.null`
    - `src/hooks/useProdutoSearch.ts` tem `filtroVoltagem` no array de dependências do useEffect
    - `src/components/ProdutoAutocomplete.tsx` contém `filtroVoltagem` na interface e na chamada `useProdutoSearch(query, filtro, filtroVoltagem)`
    - `npm run build` conclui sem erro
  </acceptance_criteria>
  <done>O seletor de driver pode receber `filtroVoltagem` e filtra por `tensao = X OR tensao IS NULL`; demais filtros inalterados.</done>
</task>

<task type="auto">
  <name>Task 2: Pré-fill de driver sugerido + toast/badge de divergência não-bloqueante</name>
  <files>src/components/AmbienteCard.tsx</files>
  <read_first>
    - src/components/AmbienteCard.tsx (L1-244 — handleSelectProdutoSistema, addSistema, toast atual; e L340-534 — render do sistema/fita/driver)
    - .planning/phases/15-tens-o-valida-o/15-RESEARCH.md (seções "D-02a — Regra de Sugestão" L86-154, "Padrão de pré-fill" L229-299, "Reformulação do toast" L357-381, "Query de driver sugerido" L443-465; Pitfalls 2/3/4/5 L406-435)
    - .planning/phases/15-tens-o-valida-o/15-CONTEXT.md (D-01, D-02/D-02a, D-03, D-04, D-05, D-10)
  </read_first>
  <action>
    Em `src/components/AmbienteCard.tsx`. Implementar D-01 (pré-fill+filtro), D-02a (regra aprovada), D-03 (só se vazio), D-04 (badge+toast), D-05 (não bloqueia), D-10 (por-sistema).

    **(A) Importar supabase:** adicionar `import { supabase } from "@/integrations/supabase/client";` no topo. Importar `calcularDemandaFita` se ainda não estiver no import de `@/types/orcamento` (já está importado — confirmar).

    **(B) Adicionar helper `buscarDriverSugerido`** dentro do componente (antes de `handleSelectProdutoSistema`). Regra D-02a APROVADA com refinamento da metragem real:
    ```typescript
    // D-02a: menor potência suficiente entre drivers de mesma voltagem com tensao preenchida.
    // Consumo estimado = metragem real (se já houver) * wm, senão 5m fallback; margem 1.05.
    const buscarDriverSugerido = async (voltagem: number, wm: number, metragemReal: number): Promise<Produto | null> => {
      const metragem = metragemReal > 0 ? metragemReal : 5;
      const consumoEstimado = wm * metragem * MARGEM_SEGURANCA_DRIVER;
      const { data } = await supabase
        .from('produtos')
        .select('id, codigo, descricao, preco_tabela, preco_minimo, voltagem:tensao, driver_potencia_w:potencia_watts, driver_tipo:subtipo')
        .eq('tipo_produto', 'driver')
        .eq('tensao', voltagem)
        .gte('potencia_watts', consumoEstimado)
        .not('descricao', 'ilike', '%DESCONTINUAR%')
        .order('potencia_watts', { ascending: true })
        .limit(1);
      return (data?.[0] as Produto) ?? null;
    };
    ```
    (Importar `MARGEM_SEGURANCA_DRIVER` de `@/types/orcamento`.)

    **(C) Tornar `handleSelectProdutoSistema` async.** Trocar `const handleSelectProdutoSistema = (...) => {` por `const handleSelectProdutoSistema = async (...) => {`.

    **(D) Remover o bloqueio (D-05).** Nos dois blocos de validação de tensão (L137-154):
    - REMOVER os dois `return;` que seguem os `toast.error(...)`.
    - Trocar `toast.error` por `toast.warning` com texto orientativo (pt-BR), ex.:
      ```typescript
      toast.warning(`Atenção: driver ${produto.voltagem}V com fita ${sis.fita.voltagem}V — confira se a combinação está correta.`, { duration: 6000 });
      ```
      e o equivalente no branch fita. SEM `return` — a seleção prossegue e salva. (Mantém a validação POR-SISTEMA: compara só `sis.fita` vs `produto`/`sis.driver` do mesmo sistema — D-10.)

    **(E) Pré-fill no branch `component === 'fita'`** (D-01/D-03). Dentro do branch que atualiza a fita (L198-212), depois de montar `fitaAtualizada` (criar essa const a partir do objeto fita atualizado), inserir:
    ```typescript
    let driverAtualizado = sis.driver;
    const fitaVolt = fitaAtualizada.voltagem;
    const driverVazio = !sis.driver.codigo; // D-03: só preenche se vazio
    if (driverVazio && fitaVolt) {
      const metragemReal = calcularDemandaFita(sis); // metragem efetiva atual (0 se ainda não definida)
      const sugerido = await buscarDriverSugerido(fitaVolt, fitaAtualizada.wm, metragemReal);
      if (sugerido) {
        driverAtualizado = {
          ...sis.driver,
          codigo: sugerido.codigo,
          descricao: sugerido.descricao,
          voltagem: (sugerido.voltagem ?? fitaVolt) as 12 | 24 | 48,
          potencia: sugerido.driver_potencia_w ?? sis.driver.potencia,
          precoUnitario: Math.round((sugerido.preco_tabela || 0) * 100) / 100,
          precoMinimo: Math.round((sugerido.preco_minimo || 0) * 100) / 100,
          driver_tipo: sugerido.driver_tipo,
        };
      }
    }
    updateSistema(sistemaIndex, { ...sis, fita: fitaAtualizada, driver: driverAtualizado });
    ```
    (Refatorar o `updateSistema` existente do branch fita para usar `fitaAtualizada` + `driverAtualizado`. NÃO sobrescrever driver não-vazio — D-03.)

    **(F) Passar `filtroVoltagem` ao seletor de driver** (L486):
    ```tsx
    <ProdutoAutocomplete value={sis.driver.codigo} onSelect={(p) => handleSelectProdutoSistema(p, si, 'driver')} placeholder="Código do driver" filtro="driver" filtroVoltagem={sis.fita.voltagem} />
    ```

    **(G) Badge de divergência derivado do state (D-04).** No render de cada sistema (dentro do `.map`, perto do header "Sistema {si + 1}", ~L350-356), adicionar:
    ```tsx
    {(() => {
      const fv = sis.fita.voltagem, dv = sis.driver.voltagem;
      const temDivergencia = !!sis.fita.codigo && !!sis.driver.codigo && fv !== undefined && dv !== undefined && fv !== dv;
      return temDivergencia ? (
        <Badge variant="destructive" className="text-[10px] px-1.5 py-0">⚠ {fv}V × {dv}V</Badge>
      ) : null;
    })()}
    ```
    (Derivado do state — some automaticamente quando `fv === dv`, sem estado próprio. D-04.)

    NÃO alterar `addSistema()` defaults (pitfall C-3). NÃO adicionar driver automaticamente em nenhum outro fluxo.
  </action>
  <verify>
    <automated>cd auraoramentos && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/AmbienteCard.tsx` contém `import { supabase } from "@/integrations/supabase/client";`
    - `src/components/AmbienteCard.tsx` contém `const buscarDriverSugerido = async`
    - `src/components/AmbienteCard.tsx` contém `.not('descricao', 'ilike', '%DESCONTINUAR%')`
    - `src/components/AmbienteCard.tsx` contém `const handleSelectProdutoSistema = async`
    - `src/components/AmbienteCard.tsx` NÃO contém mais `toast.error(` seguido de `return;` nos dois blocos de validação de tensão (grep: o texto "Tensão incompatível" não aparece mais OU não é seguido de return bloqueante)
    - `src/components/AmbienteCard.tsx` contém `filtroVoltagem={sis.fita.voltagem}` no seletor de driver
    - `src/components/AmbienteCard.tsx` contém `const driverVazio = !sis.driver.codigo`
    - `src/components/AmbienteCard.tsx` contém o badge `⚠ {fv}V × {dv}V` (variant destructive)
    - `npm run build` conclui sem erro
  </acceptance_criteria>
  <done>Selecionar fita pré-filtra e pré-preenche driver compatível (só se vazio); driver de voltagem divergente gera toast.warning + badge persistente sem bloquear; criar ambientes com tensões diferentes não bloqueia.</done>
</task>

<task type="auto">
  <name>Task 3: Advisory TINY — toast + badge "requer driver 24V externo"</name>
  <files>src/components/AmbienteCard.tsx</files>
  <read_first>
    - src/components/AmbienteCard.tsx (L76-128 — handleSelectProdutoLuminaria, branch tiny_magneto L89-95; e L307-332 — render das luminárias)
    - .planning/phases/15-tens-o-valida-o/15-RESEARCH.md (seção "Advisory TINY (D-06/D-07)" L330-355)
    - .planning/phases/15-tens-o-valida-o/15-CONTEXT.md (D-06, D-07)
  </read_first>
  <action>
    Em `src/components/AmbienteCard.tsx`. Implementar D-06 (advisory puro: toast + badge, sem montar sistema) e D-07 (detecção via dado `sistema='tiny_magneto'`, não regex).

    **(A) Toast no momento da inclusão** — o branch `produto.sistema_magnetico === 'tiny_magneto'` já existe (L89-95) e já dispara `toast.warning`. Garantir que a mensagem deixe explícito o driver 24V externo. Ajustar o texto do toast existente para (pt-BR):
    ```typescript
    toast.warning('⚡ TINY MAG 24V: requer driver 24V externo. Inclua o driver no sistema de iluminação correspondente.', { duration: 9000 });
    ```
    Manter a condição primária por dado `produto.sistema_magnetico === 'tiny_magneto'` (o `|| /TINY\s+MAG/.test(d)` pode permanecer só como fallback de diagnóstico — D-07).

    **(B) Badge persistente no card da luminária** (D-06). O campo `item.sistema` já é gravado em `handleSelectProdutoLuminaria` (L124: `sistema: produto.sistema_magnetico ?? null`). No render de cada luminária (~L307-332, dentro do `.map`, perto da descrição/badges), adicionar badge advisory derivado do state:
    ```tsx
    {item.sistema === 'tiny_magneto' && (
      <Badge variant="outline" className="text-[10px] border-amber-400 text-amber-700 bg-amber-50">requer driver 24V externo</Badge>
    )}
    ```
    Posicionar junto aos demais elementos do item (ex.: na linha de badges após o subtotal, ou abaixo da descrição). Badge é derivado do state — aparece sempre que `item.sistema === 'tiny_magneto'` e some se o item for trocado.

    NÃO adicionar driver automaticamente. NÃO montar sistema composto (advisory puro, escopo v1.2). NÃO mexer no advisory MAGNETO 48V existente (L81-88).
  </action>
  <verify>
    <automated>cd auraoramentos && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/AmbienteCard.tsx` contém o badge com texto `requer driver 24V externo` e classes `border-amber-400 text-amber-700 bg-amber-50`
    - O badge é condicionado a `item.sistema === 'tiny_magneto'`
    - O toast de TINY menciona `driver 24V externo` e é `toast.warning`
    - A detecção primária usa `produto.sistema_magnetico === 'tiny_magneto'` (não só regex)
    - `npm run build` conclui sem erro
  </acceptance_criteria>
  <done>Adicionar luminária TINY (sistema='tiny_magneto') dispara toast.warning e mostra badge amber persistente "requer driver 24V externo" no card; nenhum componente é adicionado automaticamente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| colaborador → Supabase (query de driver sugerido) | Nova query `supabase.from('produtos').select(...)` em `buscarDriverSugerido`. Leitura da tabela `produtos` (catálogo público interno), filtrada por `tipo_produto='driver'`, `tensao`, `potencia_watts`. Mesma tabela e RLS já usadas por `useProdutoSearch`. Sem novo input não-confiável (voltagem e wm vêm de produtos do catálogo já selecionados). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Injection | `buscarDriverSugerido` / pré-filtro `.or('tensao.eq.${filtroVoltagem},...')` | mitigate | `filtroVoltagem`/`voltagem` são tipados como `number` (12\|24\|48) derivados de `produto.voltagem` do catálogo, nunca string livre do usuário — interpolação numérica não permite injeção PostgREST. Garantir tipagem `number` na assinatura (já especificado). |
| T-15-04 | Information Disclosure | query de driver na tabela produtos | accept | Mesma tabela/colunas já expostas por `useProdutoSearch` ao mesmo papel. RLS de `produtos` inalterada. Nenhum dado novo exposto. |
| T-15-05 | DoS (race) | handler async `handleSelectProdutoSistema` | accept | Cliques rápidos podem disparar queries concorrentes (Pitfall 5). Impacto: no máximo um pré-fill sobrescrito por outro — sem corrupção de dados nem custo relevante (limit 1, query leve). Aceito; flag de loading é otimização opcional, não bloqueante. |

**Conclusão (honesta):** fase frontend/lógica pura. A única superfície nova é uma query de leitura na tabela `produtos` com a mesma RLS já em uso. Sem severidade `high`. Nenhum bloqueio de segurança.
</threat_model>

<verification>
- `npm run build` conclui sem erro de tipo após cada task
- Grep confirma os acceptance_criteria de cada task
- Teste manual (execute-phase / Playwright na validação): selecionar fita 24V → driver pré-preenchido + seletor filtrado; selecionar driver 12V → toast.warning + badge ⚠ sem bloqueio; criar Ambiente A 24V + B 12V → sem bloqueio; adicionar luminária TINY → toast + badge amber
</verification>

<success_criteria>
- ROADMAP Success Criteria #1 (pré-filtro + aviso de divergência), #2 (tensões diferentes sem bloqueio), #4 (advisory TINY 24V), #5 (pré-preenchimento de driver) — ATENDIDOS.
- D-01, D-02/D-02a (regra aprovada), D-03, D-04, D-05, D-06, D-07, D-10 implementados exatamente como aprovados.
- TENS-01, SIST-04, UX-02 cobertos; parte "sem bloqueio indevido" de TENS-02 (D-10) concluída.
- Defaults de `addSistema()` preservados (pitfall C-3); validação sempre por-sistema (D-10); driver manual nunca sobrescrito (D-03).
</success_criteria>

<output>
After completion, create `.planning/phases/15-tens-o-valida-o/15-02-SUMMARY.md`
</output>
